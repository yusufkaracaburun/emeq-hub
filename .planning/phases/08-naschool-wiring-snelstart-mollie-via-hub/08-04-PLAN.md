---
phase: 08-naschool-wiring-snelstart-mollie-via-hub
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php
  - app/Filament/Resources/Consumers/ConsumerResource.php
  - app/Filament/Resources/Consumers/Pages/ViewConsumer.php
  - app/Filament/Resources/Accounts/Schemas/AccountInfolist.php
  - app/Providers/Filament/AdminPanelProvider.php
  - tests/Feature/Admin/ConsumerInfolistHintTest.php
  - tests/Feature/Admin/AccountInfolistHintTest.php
autonomous: true
requirements: [NSCH-01]
must_haves:
  truths:
    - "ConsumerResource heeft een View-page met Infolist waarop een 'Wat is een Consumer?'-hint-Section bovenaan zichtbaar is"
    - "AccountResource Infolist toont een 'Wat is een Account?'-hint-Section bovenaan, gecollapsed by default"
    - "Beide hint-Sections bevatten EXACT de canonical NL-tekst uit UI-SPEC §S4 / CONTEXT D-07"
    - "Tenants-navgroup in admin-paneel heeft een tooltip/description die uitlegt 'SaaS-apps (Consumer) → hun klanten (Account) → partner-koppelingen (Connection)'"
    - "Geen Phase-9 admin-test in tests/Feature/Admin/ breekt op deze additieve wijziging"
  artifacts:
    - path: "app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php"
      provides: "Nieuwe Infolist-schema-class voor ConsumerResource (eerste van zijn soort)"
      contains: "class ConsumerInfolist + configure(Schema): Schema + Section::make('Wat is een Consumer?')"
    - path: "app/Filament/Resources/Consumers/Pages/ViewConsumer.php"
      provides: "Nieuwe Filament View-page voor ConsumerResource"
      contains: "class ViewConsumer extends ViewRecord"
    - path: "tests/Feature/Admin/ConsumerInfolistHintTest.php"
      provides: "Coverage dat hint-Section met canonical copy rendert"
      contains: "test_consumer_infolist_shows_hint_section"
    - path: "tests/Feature/Admin/AccountInfolistHintTest.php"
      provides: "Coverage dat AccountResource hint-Section met canonical copy rendert"
      contains: "test_account_infolist_shows_hint_section"
  key_links:
    - from: "app/Filament/Resources/Consumers/ConsumerResource.php"
      to: "app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php"
      via: "public static function infolist(Schema $schema): Schema { return ConsumerInfolist::configure($schema); }"
      pattern: "ConsumerInfolist::configure"
    - from: "app/Filament/Resources/Consumers/ConsumerResource.php"
      to: "app/Filament/Resources/Consumers/Pages/ViewConsumer.php"
      via: "getPages() entry 'view' => Pages\\ViewConsumer::route('/{record}')"
      pattern: "Pages\\\\ViewConsumer::route"
    - from: "app/Providers/Filament/AdminPanelProvider.php"
      to: "Tenants-navgroup label"
      via: "->navigationGroups([NavigationGroup::make('Tenants')->...])"
      pattern: "NavigationGroup::make"
---

<objective>
Lever de Filament Resource-infolist hint-Sections + Tenants-navgroup-tooltip (D-07, UI-SPEC S4): korte uitleg-teksten die Emeq-staff helpen Consumer / Account / Connection-terminology te onderscheiden, zodat onboarding van Naschool (en alle toekomstige Consumers) niet vastloopt op concept-verwarring.

Purpose: Operator-onboarding-clarity. Zonder deze hints kost het tijd om "wat is in vredesnaam een Account vs een Connection" uit te zoeken. Additieve change — geen impact op bestaande data-shapes of acties.

Output: 1 nieuwe Infolist-schema-class + 1 nieuwe ViewConsumer-page + ConsumerResource infolist()-method + AccountInfolist-extension + AdminPanelProvider navgroup-config + 2 testfiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-CONTEXT.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md
@CLAUDE.md
@.ai/rules/global.md
@.docs/decisions/filament-admin-panel.md

<!-- Closest analogs -->
@app/Filament/Resources/Accounts/Schemas/AccountInfolist.php
@app/Filament/Resources/Accounts/AccountResource.php
@app/Filament/Resources/Consumers/ConsumerResource.php
@app/Providers/Filament/AdminPanelProvider.php
@app/Filament/Resources/Connections/Pages/ViewConnection.php

<interfaces>
<!-- Key contracts extracted from codebase -->

From `app/Filament/Resources/Accounts/Schemas/AccountInfolist.php` (regel 1-32):
- Pattern: `class AccountInfolist { public static function configure(Schema $schema): Schema { return $schema->components([...TextEntry...]); } }`
- Namespace: `App\Filament\Resources\Accounts\Schemas`

From `app/Filament/Resources/Accounts/AccountResource.php` (regel 44-47):
- `public static function infolist(Schema $schema): Schema { return AccountInfolist::configure($schema); }`

From `app/Filament/Resources/Consumers/ConsumerResource.php` (regel 149-155):
- `public static function getPages(): array { return ['index' => Pages\ListConsumers::route('/'), 'create' => Pages\CreateConsumer::route('/create'), 'edit' => Pages\EditConsumer::route('/{record}/edit')]; }`
- No `view`-entry currently → ConsumerResource heeft geen ViewConsumer-page

From `Filament\Schemas\Components\Section` (vendor):
- `Section::make(string $heading)`
- `->description(string)`, `->collapsed()`, `->schema(array)`

From `app/Filament/Resources/Connections/Pages/ViewConnection.php`:
- `class ViewConnection extends ViewRecord { protected static string $resource = ConnectionResource::class; }` — copy-pattern voor ViewConsumer

From `Filament\Navigation\NavigationGroup` (vendor):
- `NavigationGroup::make(string $label)`
- API may include `->description()`, `->extraSidebarAttributes()`, etc. — executor checks `vendor/filament/panels/src/Navigation/NavigationGroup.php` voor exact API in v4.11
- Panel-config: `$panel->navigationGroups([NavigationGroup::make(...), ...])`

</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Filament Infolist | Authenticated Filament-staff crossen hier; Infolist is read-only |
| Browser → Tenants-navgroup-tooltip | HTML title-attribute is static text — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | Hint-Section text exposing internal architecture | accept | Tekst is publiek-domein (al gepubliceerd in CLAUDE.md). Geen tenant-data, geen secrets in hint-strings |
| T-08-04-02 | Tampering | Hint-Section content via untrusted input | mitigate | Strings zijn hardcoded compile-time constants in PHP-class. Geen runtime-rendering van user input |
| T-08-04-03 | Denial of Service | Infolist render-cost van extra Section | accept | Section met static description + lege schema heeft O(1) render-kost. Geen DB-query, geen N+1 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConsumerInfolist schema + ViewConsumer page + wire in ConsumerResource</name>
  <files>app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php, app/Filament/Resources/Consumers/Pages/ViewConsumer.php, app/Filament/Resources/Consumers/ConsumerResource.php, tests/Feature/Admin/ConsumerInfolistHintTest.php</files>
  <read_first>
    - app/Filament/Resources/Accounts/Schemas/AccountInfolist.php (regel 1-32 — exact pattern voor schema-class)
    - app/Filament/Resources/Accounts/AccountResource.php (regel 44-47 — infolist()-method + regel 39-43 — getPages() met 'view'-entry)
    - app/Filament/Resources/Connections/Pages/ViewConnection.php (ViewRecord-skeleton pattern)
    - app/Filament/Resources/Consumers/ConsumerResource.php (regel 149-155 — huidige getPages() WITHOUT view-entry)
    - tests/Feature/Admin/AccountInfolistTest.php (of vergelijkbaar Phase-9 test, gebruik als skeleton)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md (§Surface S4 — canonical NL-copy, EXACT overnemen)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md (§"ConsumerInfolist.php" + §"ConsumerResource.php EXTEND")
  </read_first>
  <behavior>
    - Test 1 (hint-Section rendert): staff opent `ViewConsumer`-page voor een Consumer → response bevat heading 'Wat is een Consumer?' EN body-tekst 'Eén SaaS-app die de Hub gebruikt (Naschool, Planny, externe app). Authenticeert met een Bearer-PAT. Een Consumer heeft Accounts (zijn klanten) en die Accounts hebben Connections (partner-koppelingen).'
    - Test 2 (Section collapsed-state): assert dat de Section default-collapsed is (UI-SPEC regel 211)
    - Test 3 (Infolist toont basis Consumer-velden): assert dat TextEntry's voor `id`, `name`, `slug` ook gerenderd worden (geen breaking change)
    - Test 4 (ViewConsumer-route bestaat): `php artisan route:list --path=admin/consumers` toont GET `admin/consumers/{record}` route
    - Test 5 (RBAC respecteerd): user zonder `view-consumers` → 403 op ViewConsumer-page (hergebruikt bestaande ConsumerResource canAccess)
  </behavior>
  <action>
    Create `app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php` (copy-exact pattern uit `AccountInfolist.php`):
    - Namespace `App\Filament\Resources\Consumers\Schemas`
    - `declare(strict_types=1);` (consistent met AccountInfolist regel 1)
    - Use-statements: `Filament\Infolists\Components\TextEntry`, `Filament\Schemas\Components\Section`, `Filament\Schemas\Schema`
    - Class `ConsumerInfolist` met `public static function configure(Schema $schema): Schema`:
      - Eerste component: `Section::make('Wat is een Consumer?')->description('Eén SaaS-app die de Hub gebruikt (Naschool, Planny, externe app). Authenticeert met een Bearer-PAT. Een Consumer heeft Accounts (zijn klanten) en die Accounts hebben Connections (partner-koppelingen).')->collapsed()->schema([])` (CANONICAL — UI-SPEC regel 207, exact copy)
      - Daarna `TextEntry::make('id')->label('ID')`, `TextEntry::make('name')`, `TextEntry::make('slug')`, `TextEntry::make('webhook_callback_url')->label('Webhook callback-URL')->placeholder('Niet ingesteld')`, `TextEntry::make('created_at')->dateTime()`, `TextEntry::make('updated_at')->dateTime()`. Voeg `accounts_count` via withCount() in resource indien al pattern (anders skip).

    Create `app/Filament/Resources/Consumers/Pages/ViewConsumer.php` (copy-exact pattern uit `ViewConnection.php`):
    - Namespace `App\Filament\Resources\Consumers\Pages`
    - Class `ViewConsumer extends Filament\Resources\Pages\ViewRecord`
    - `protected static string $resource = ConsumerResource::class;`

    Modify `app/Filament/Resources/Consumers/ConsumerResource.php`:
    - Voeg `use App\Filament\Resources\Consumers\Schemas\ConsumerInfolist;` toe.
    - Voeg `use Filament\Schemas\Schema;` toe (indien nog niet aanwezig).
    - Voeg `public static function infolist(Schema $schema): Schema { return ConsumerInfolist::configure($schema); }` method toe (copy-exact uit AccountResource regel 44-47, vlak na `form()`-method of waar logisch).
    - In `getPages()` (regel 149-155): voeg `'view' => Pages\ViewConsumer::route('/{record}')` toe (na 'create' of voor 'edit', kies positie analoog AccountResource).
    - In `recordActions` array (regel 131-139): voeg `ViewAction::make()` toe vóór `EditAction::make()` — anders is de View-page niet bereikbaar via de table-rij (PATTERN check uit ConnectionResource regel 149).
    - Voeg `use Filament\Actions\ViewAction;` indien nog niet aanwezig.

    Create `tests/Feature/Admin/ConsumerInfolistHintTest.php`:
    - Extend `Tests\TestCase` met `RefreshDatabase`.
    - `setUp()`: seed permissions + roles + create staff-user.
    - Implementeer alle 5 tests uit `<behavior>`. Voor copy-assertions: gebruik `assertSeeText('Wat is een Consumer?')` + `assertSeeText('Eén SaaS-app die de Hub gebruikt')` etc. Use Filament-Livewire test-helpers.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ConsumerInfolistHintTest</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php` bestaat met `class ConsumerInfolist` + `configure(Schema $schema): Schema`
    - `grep -c "Wat is een Consumer?" app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php` ≥ 1
    - `grep -c "Eén SaaS-app die de Hub gebruikt" app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php` ≥ 1 (CANONICAL copy)
    - `grep -c "->collapsed()" app/Filament/Resources/Consumers/Schemas/ConsumerInfolist.php` ≥ 1
    - File `app/Filament/Resources/Consumers/Pages/ViewConsumer.php` bestaat met `class ViewConsumer extends ViewRecord`
    - `app/Filament/Resources/Consumers/ConsumerResource.php` heeft `infolist(Schema $schema)`-method én `'view' => Pages\ViewConsumer::route` entry in `getPages()`
    - `php artisan route:list --path=admin/consumers | grep -c "consumers/{record}"` ≥ 1
    - `php artisan test --compact --filter=ConsumerInfolistHintTest` exits 0 met ≥ 5 passed
  </acceptance_criteria>
  <done>ConsumerInfolist-schema + ViewConsumer-page operationeel; canonical hint-Section bovenaan zichtbaar; 5 tests groen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AccountInfolist met hint-Section + AdminPanelProvider Tenants-navgroup-tooltip</name>
  <files>app/Filament/Resources/Accounts/Schemas/AccountInfolist.php, app/Providers/Filament/AdminPanelProvider.php, tests/Feature/Admin/AccountInfolistHintTest.php</files>
  <read_first>
    - app/Filament/Resources/Accounts/Schemas/AccountInfolist.php (regel 14-30 — huidige components-array, target voor extension)
    - app/Providers/Filament/AdminPanelProvider.php (regel 27-89 — huidige config, target voor navigationGroups-additie)
    - vendor/filament/panels/src/Navigation/NavigationGroup.php (check exact API voor description/tooltip in v4.11)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md (§S4 regel 209-210 — canonical AccountResource-hint-tekst + Tenants-tooltip)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md (§"AccountInfolist.php EXTEND" + §"AdminPanelProvider.php EXTEND")
    - tests/Feature/Admin/AccountResourceTest.php (Phase-9 admin-test-pattern voor regressie-checks)
  </read_first>
  <behavior>
    - Test 1 (AccountInfolist hint-Section): staff opent `ViewAccount` → response bevat heading 'Wat is een Account?' EN body 'Een klant van een Consumer (bv. school A bij Naschool). Niet de individuele eindgebruiker/ouder.'
    - Test 2 (Section collapsed-state): assert collapsed by default
    - Test 3 (bestaande AccountInfolist-velden behouden): assert dat TextEntry's voor `id`, `consumer.slug`, `external_id`, `display_name` blijven renderen (geen Phase-9 regressie)
    - Test 4 (Tenants navgroup tooltip): assert dat in admin-sidebar HTML voor de Tenants-groep ofwel een title-attribute ofwel een description-tekst aanwezig is met de string 'SaaS-apps (Consumer) → hun klanten (Account) → partner-koppelingen (Connection)' (UI-SPEC regel 210)
  </behavior>
  <action>
    Modify `app/Filament/Resources/Accounts/Schemas/AccountInfolist.php`:
    - Voeg `use Filament\Schemas\Components\Section;` toe.
    - In `configure(Schema $schema)`-method (regel 14-30): prepend `Section::make('Wat is een Account?')->description('Een klant van een Consumer (bv. school A bij Naschool). Niet de individuele eindgebruiker/ouder.')->collapsed()->schema([]),` als EERSTE element in de `components([...])`-array. Alle bestaande TextEntry's blijven op zelfde plek (geen reordering).

    Modify `app/Providers/Filament/AdminPanelProvider.php`:
    - Check eerst de Filament v4.11 API voor `NavigationGroup`-description via `vendor/filament/panels/src/Navigation/NavigationGroup.php`. Drie pad-opties (kies de eerste die werkt):
      1. **Voorkeur**: `NavigationGroup::make('Tenants')->description('SaaS-apps (Consumer) → hun klanten (Account) → partner-koppelingen (Connection)')` indien beschikbaar.
      2. Fallback A: `NavigationGroup::make('Tenants')->extraSidebarAttributes(['title' => '...'])`.
      3. Fallback B: render-hook (regel 67-86 is template) die een tooltip-element in de sidebar injecteert via `Blade::render(...)`.
    - Voeg toe in de panel-config-chain (na bestaande `->discoverPages(...)`): `->navigationGroups([NavigationGroup::make('Tenants')->...(...), NavigationGroup::make('Integraties'), NavigationGroup::make('Abonnementen'), NavigationGroup::make('Beheer')])`. **NB**: de andere 3 nav-groups MUST aanwezig zijn in dezelfde call — anders krijgen ze geen rendering-volgorde (Filament-conventie). Check ConsumerResource regel 29 + AccountResource regel 32 + andere 7 resources voor welke nav-groups bestaan.
    - Voeg `use Filament\Navigation\NavigationGroup;` toe.

    Create `tests/Feature/Admin/AccountInfolistHintTest.php`:
    - Extend `Tests\TestCase` met `RefreshDatabase`.
    - Implementeer tests 1-3 uit `<behavior>` (Account-specifiek). Tests 4 (navgroup-tooltip) kan in dezelfde file of in een aparte `tests/Feature/Admin/AdminPanelNavgroupTooltipTest.php` — kies cohesief.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter='AccountInfolistHintTest|AdminPanelNavgroupTooltipTest'</automated>
  </verify>
  <acceptance_criteria>
    - `app/Filament/Resources/Accounts/Schemas/AccountInfolist.php` bevat `Section::make('Wat is een Account?')` als eerste component
    - `grep -c "Een klant van een Consumer" app/Filament/Resources/Accounts/Schemas/AccountInfolist.php` ≥ 1 (CANONICAL copy)
    - `app/Providers/Filament/AdminPanelProvider.php` bevat `NavigationGroup::make('Tenants')` met tooltip/description config
    - `grep -c "SaaS-apps (Consumer)" app/Providers/Filament/AdminPanelProvider.php` ≥ 1
    - `php artisan test --compact --filter='AccountInfolistHintTest'` exits 0
    - `php artisan test --compact --filter='AccountResourceTest|ConnectionResourceTest|ConsumerResourceTest'` exits 0 (geen Phase-9 regressies)
    - `vendor/bin/pint --test --format agent app/Filament/Resources/Accounts/Schemas/ app/Providers/Filament/AdminPanelProvider.php` exits 0
  </acceptance_criteria>
  <done>AccountInfolist hint-Section gerenderd; Tenants-navgroup-tooltip operationeel; geen Phase-9 regressies.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter='ConsumerInfolistHintTest|AccountInfolistHintTest|AdminPanelNavgroup'` — alle nieuwe tests groen
- `php artisan test --compact --group=admin` — geen Phase-9 admin-tests breken
- `php artisan test --compact` — volledige suite groen
- `vendor/bin/pint --test --format agent` exits 0
- Smoke (browser): `bin/dev up` → `/admin/login` → quick-login → hover Tenants nav-group → tooltip zichtbaar → open Consumer-rij → Infolist toont Section bovenaan
</verification>

<success_criteria>
- ConsumerInfolist schema-class operationeel met canonical hint-Section
- ViewConsumer-page bereikbaar via getPages() + table-row ViewAction
- AccountInfolist hint-Section toegevoegd zonder Phase-9 regressies
- Tenants-navgroup heeft tooltip/description (Filament v4 API of fallback)
- 5+ nieuwe tests groen
- Geen breaking changes in tests/Feature/Admin/* Phase-9 suite
</success_criteria>

<output>
Create `.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-04-SUMMARY.md` na completion, met notitie over welke Filament v4 NavigationGroup API-variant gebruikt is.
</output>
