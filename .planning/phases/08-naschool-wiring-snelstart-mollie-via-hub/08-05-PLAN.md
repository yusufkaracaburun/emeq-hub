---
phase: 08-naschool-wiring-snelstart-mollie-via-hub
plan: 05
type: execute
wave: 2
depends_on: ["08-03"]
files_modified:
  - app/Services/PartnerStatus.php
  - resources/views/partners/partials/_domeinmodel.blade.php
  - resources/views/partners/partials/_status-widget.blade.php
  - resources/views/partners/index.blade.php
  - resources/views/partners/mollie/example.blade.php
  - resources/views/partners/snelstart/example.blade.php
  - routes/web.php
  - tests/Feature/Dev/PartnerPagesTest.php
autonomous: true
requirements: [NSCH-03]
must_haves:
  truths:
    - "PartnerStatus-service levert per-provider Account-status (connected/pending/revoked/none) in één eager-loaded query"
    - "`/dev/partners` index toont domeinmodel-blokje + per-provider card met status-totaal ('Mollie: 1/2 Accounts gekoppeld')"
    - "`/dev/partners/mollie` toont domeinmodel-blokje + koppel-stappen + 'Start OAuth-flow' CTA + live status-widget per Account"
    - "`/dev/partners/snelstart` toont domeinmodel-blokje + koppel-stappen + cURL-snippet + live status-widget per Account"
    - "Status-widget regels gebruiken canonical Heroicon + Tailwind kleur-tokens uit UI-SPEC §Color semantic palette"
    - "Mollie-CTA op `/dev/partners/mollie` triggert daadwerkelijk Phase-4 OAuth-init voor de geseede demo-Account — geen anchor-only stub"
    - "Dev-only OAuth-init route is bereikbaar in `local`/`testing`-env en 404 in production"
    - "Pagina's blijven `local`/`testing`-gated — 404 in production-env"
    - "Domeinmodel-blokje + status-widget partial bevatten CANONICAL NL-copy uit UI-SPEC §S3 (geen paraphrase)"
  artifacts:
    - path: "app/Services/PartnerStatus.php"
      provides: "Read-only aggregate service voor Account×Provider Connection-status"
      contains: "final class PartnerStatus + forProvider(string): Collection"
    - path: "resources/views/partners/partials/_domeinmodel.blade.php"
      provides: "Herbruikbare Blade-partial met Consumer/Account/Connection-uitleg"
      contains: "Consumer + Account + Connection bullets met canonical copy"
    - path: "resources/views/partners/partials/_status-widget.blade.php"
      provides: "Herbruikbare Blade-partial met per-Account status-regels per provider"
      contains: "@foreach \\$accountStatus as \\$entry + Heroicon-status"
    - path: "routes/web.php"
      provides: "Dev-only OAuth-init route die Phase-4 OAuthFlowRegistry server-side aanroept voor de geseede demo-Account"
      contains: "Route::get('/dev/partners/mollie/start-oauth') in env-gated block"
    - path: "tests/Feature/Dev/PartnerPagesTest.php"
      provides: "Coverage: env-gating + canonical copy aanwezig + status-widget rendert juiste state + PartnerStatus-service + dev OAuth-init"
      contains: "test_pages_404_in_production_env + test_index_shows_domeinmodel + test_mollie_page_shows_status_widget + test_partner_status_service_resolves_state + test_dev_partners_oauth_start_redirects_to_mollie_authorize + test_dev_partners_oauth_start_returns_404_in_production_env"
  key_links:
    - from: "resources/views/partners/mollie/example.blade.php"
      to: "app/Services/PartnerStatus.php"
      via: "@php \\$accountStatus = app(\\\\App\\\\Services\\\\PartnerStatus::class)->forProvider('mollie') @endphp"
      pattern: "PartnerStatus"
    - from: "resources/views/partners/index.blade.php"
      to: "app/Services/PartnerStatus.php"
      via: "inline service-call per provider voor status-totaal"
      pattern: "PartnerStatus"
    - from: "resources/views/partners/mollie/example.blade.php"
      to: "routes/web.php :: dev.partners.mollie.start-oauth"
      via: "CTA-anchor met href=route('dev.partners.mollie.start-oauth') — server-side redirect naar Mollie authorize-URL"
      pattern: "dev.partners.mollie.start-oauth"
    - from: "routes/web.php :: dev.partners.mollie.start-oauth"
      to: "app/OAuth/OAuthFlowRegistry.php"
      via: "app(OAuthFlowRegistry::class)->for('mollie')->getAuthorizationUrl(...) op pending Connection-stub van demo-Account"
      pattern: "OAuthFlowRegistry"
---

<objective>
Lever de `/dev/partners`-pagina-uitbreiding (D-06, UI-SPEC S3): nieuwe `PartnerStatus`-service voor per-provider Account-status + Blade-partials voor domeinmodel + status-widget + uitgebreide Mollie/Snelstart pagina-inhoud (koppel-stappen, CTA, cURL-snippet, live status-widget) + dev-only Mollie-CTA-route die de Phase-4 OAuth-init server-side triggert.

Purpose: Dev-pagina's worden bruikbaar voor (1) partner-certificering screenshots, (2) Emeq-staff demo, (3) Naschool-implementatie reference (school A's koppel-status visible in dev-env). Hub-side Naschool wiring SC-4 dev-reachability-helper. Mollie-CTA voldoet aan UI-SPEC §S3 regel 191 contract — geen reductie tot inert anchor.

Output: 1 nieuwe service + 2 nieuwe Blade-partials + 3 Blade-views extend + 1 dev-only route + 1 testfile. Routes-edit is chirurgisch: één extra Route::get binnen de bestaande env-gated `if`-block (regel 27 `routes/web.php`) — geen herstructurering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-CONTEXT.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md
@.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-03-PLAN.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@.docs/decisions/provider-credential-descriptor.md

<!-- Closest analogs -->
@app/Filament/Widgets/ConnectionStatsWidget.php
@app/Support/ProviderCredentialDescriptor.php
@config/hub-providers.php
@app/Models/Account.php
@app/Models/Connection.php

<!-- Current dev-routes + Blade -->
@routes/web.php
@resources/views/partners/index.blade.php
@resources/views/partners/mollie/example.blade.php
@resources/views/partners/snelstart/example.blade.php

<!-- Phase-4 init-pattern dat de dev-route hergebruikt -->
@app/Http/Controllers/Api/V1/OAuth/InitController.php
@app/OAuth/OAuthFlowRegistry.php

<interfaces>
<!-- Key contracts extracted from codebase -->

From `app/Filament/Widgets/ConnectionStatsWidget.php` (regel 24-41 — descriptor-driven per-provider count pattern):
- `foreach (ProviderCredentialDescriptor::all() as $descriptor)`
- `$base = Connection::query()->where('provider', $descriptor->key);`
- `$active = (clone $base)->whereNull('revoked_at')->count();`
- `$revoked = (clone $base)->whereNotNull('revoked_at')->count();`

From `app/Support/ProviderCredentialDescriptor.php`:
- `all(): array<self>`, `for(string $key): self`
- `key: string`, `label: string`, `oauthFlowKey: ?string`

From `app/Models/Account.php`:
- `connections(): HasMany`
- Properties: `id`, `consumer_id`, `external_id`, `display_name`

From `app/Models/Connection.php`:
- Properties: `provider`, `access_token`, `client_key`, `revoked_at`, `expires_at`, `oauth_state`

From `routes/web.php` regel 27-56 (current `/dev/partners`-routes):
- `if (app()->environment('local', 'testing')) { ... }` wrapper start regel 27 — alle dev-routes leven daarbinnen
- `Route::get('/dev/partners', fn () => ...)` + `Route::get('/dev/partners/{provider}', fn (string $provider) => ...)` zijn de huidige twee routes
- Production-env returnt 404 op alle routes binnen dit blok — pattern hergebruikt voor de nieuwe dev OAuth-init route

From `app/Http/Controllers/Api/V1/OAuth/InitController.php` (regel 22-51 — init-flow shape die de dev-route hergebruikt):
- `$state = Str::random(48);`
- `$connection = $account->connections()->create(['provider' => 'mollie', 'status' => 'pending', 'oauth_state' => $state, 'oauth_state_expires_at' => now()->addMinutes(30)]);`
- `$scopes = config('services.mollie.connect.scopes');`
- `$authorizationUrl = app(OAuthFlowRegistry::class)->for('mollie')->getAuthorizationUrl($account, $scopes, $state);`

</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /dev/partners | Public route in local/testing env; production = 404 (env-gated by `routes/web.php` regel 27) |
| Browser → /dev/partners/mollie/start-oauth | Dev-only Phase-4 init-trigger; production = 404 (zelfde env-gate) |
| Service → DB | Read-only eager-load query (no writes, no mutations) |
| Dev-route → DB | Schrijft pending Connection-stub voor demo-Account (geen credentials, alleen `oauth_state` + TTL) |
| Blade-view → Connection-data | Status-widget rendert alleen fingerprints + status-state — geen plain credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | `/dev/partners` exposure in production | mitigate | Routes wrapped in `app()->environment('local', 'testing')` check (regel 27 routes/web.php) — production-env returnt 404. Test: `Config::set('app.env', 'production')` + assert 404 op route |
| T-08-05-02 | Information Disclosure | Plain credentials in status-widget | mitigate | Service en widget lezen ALLEEN: `Account.display_name`, `Connection.provider`, `Connection.revoked_at`, `Connection.expires_at`, `Connection.access_token IS NULL`-check (NIET de value). Voor key-based providers: gebruik `Connection->fingerprint()`-accessor (descriptor-aware, sha256[0..12]) — NIET raw `client_key`. Test: `assertDontSee($plainKey)` op alle 3 partner-pages |
| T-08-05-03 | Information Disclosure | OAuth state-parameter via Mollie-CTA-link | mitigate | "Start OAuth-flow"-CTA op Mollie-Blade is een GET-link naar `route('dev.partners.mollie.start-oauth')` — geen state in URL. De dev-route doet server-side `Str::random(48)` + `Connection::create(['oauth_state' => $state])` + `redirect()->away($authorizationUrl)` (Phase-4 pattern). Test: response-body van Mollie-page bevat `/dev/partners/mollie/start-oauth` (geen state of token) |
| T-08-05-04 | Denial of Service | N+1 query op /dev/partners | mitigate | `PartnerStatus::forProvider($provider)` doet één eager-load via `Account::with(['connections' => fn ($q) => $q->where('provider', $provider)])->get()`. Test: assert query-count ≤ 2 via `DB::enableQueryLog()` |
| T-08-05-05 | Spoofing | Onbekende provider-key in URL → status-widget crash | mitigate | Route-callback heeft `abort_unless(array_key_exists($provider, config('hub-providers')), 404)` (regel 49). Service heeft eigen guard: `ProviderCredentialDescriptor::for($provider)` throws on unknown |
| T-08-05-06 | Elevation of Privilege | Dev-only OAuth-init route per ongeluk in productie bereikbaar | mitigate | Route leeft binnen het bestaande `if (app()->environment('local', 'testing'))`-blok in `routes/web.php` regel 27 — production-env registreert de route niet. Test: `Config::set('app.env', 'production')` + `RefreshApplication` trick OF Pest-style `the($this->app)->detectEnvironment(...)` mock → assert 404 op `/dev/partners/mollie/start-oauth`. Dev-route consumeert geen PAT, geen Sanctum-auth — vereiste is dat de env-gate de enige toegangscontrole is |
| T-08-05-07 | Tampering | Dev-route maakt orphan pending Connections aan als demo-Account ontbreekt | mitigate | Route doet `$account = Account::query()->whereHas('consumer', fn ($q) => $q->where('slug', 'naschool'))->first();` met `abort_unless($account !== null, 404, 'Geen demo-Account — draai EmeqStaffSeeder + Naschool-seed eerst.')`. Geen partial state |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PartnerStatus service + Blade-partials (domeinmodel + status-widget)</name>
  <files>app/Services/PartnerStatus.php, resources/views/partners/partials/_domeinmodel.blade.php, resources/views/partners/partials/_status-widget.blade.php, tests/Feature/Dev/PartnerPagesTest.php</files>
  <read_first>
    - app/Filament/Widgets/ConnectionStatsWidget.php (regel 24-41 — descriptor-driven aggregate pattern)
    - app/Support/ProviderCredentialDescriptor.php (regel 36-60 — all() + for() API)
    - app/Models/Account.php (connections() relation + properties)
    - app/Models/Connection.php (fingerprint() accessor regel 47-64)
    - resources/views/partners/index.blade.php (regel 1-46 — huidige inline-style aanpak)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md (§S3 regel 184-201 + §Color regel 116-124 status-palette + §Spacing regel 56-63 tokens)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md (§"PartnerStatus.php" + §"_status-widget.blade.php" no-analog + §"_domeinmodel.blade.php" no-analog)
  </read_first>
  <behavior>
    - Test 1 (service basic): `PartnerStatus::forProvider('mollie')` op lege DB → returnt empty Collection
    - Test 2 (service connected): seed Account + Mollie-Connection met `access_token=set`, `revoked_at=null` → returnt 1 entry met `status='connected'`
    - Test 3 (service pending): seed Account + Mollie-Connection met `access_token=null`, `oauth_state=set` → status='pending'
    - Test 4 (service revoked): seed Connection met `revoked_at=now()` → status='revoked'
    - Test 5 (service none): seed Account zonder Connection voor provider → status='none'
    - Test 6 (service N+1 guard): seed 5 Accounts + 5 Connections → `DB::enableQueryLog()` toont ≤ 2 queries voor `forProvider('mollie')` call
    - Test 7 (partial domeinmodel): render `_domeinmodel.blade.php` partial → response bevat 'Consumer', 'Account', 'Connection' + canonical bodies uit UI-SPEC regel 184-186
    - Test 8 (partial status-widget — connected state): render met `['accountStatus' => [['account' => fake, 'status' => 'connected', 'connection' => fake]], 'provider' => 'mollie']` → response bevat 'check-circle' icon-key + 'gekoppeld' text + tailwind class `text-emerald-600`
    - Test 9 (partial status-widget — pending state): zelfde met status='pending' → bevat 'clock' + 'pending' + `text-amber-600`
    - Test 10 (partial status-widget — empty state): lege `accountStatus` → bevat 'Geen demo-Accounts — draai `php artisan db:seed` eerst.' (UI-SPEC regel 198)
  </behavior>
  <action>
    Create `app/Services/PartnerStatus.php`:
    - Namespace `App\Services`
    - `declare(strict_types=1);`
    - `final class PartnerStatus` (geen readonly — public methods kunnen state-less zijn maar `final` zonder readonly is consistent met laravel-class patterns; PATTERNS.md noemt geen specifieke preference)
    - Method `public function forProvider(string $provider): \Illuminate\Support\Collection`:
      ```
      return Account::with(['connections' => fn ($q) => $q->where('provider', $provider)])
          ->get()
          ->map(fn (Account $a): array => [
              'account' => $a,
              'connection' => $a->connections->first(),
              'status' => $this->resolveStatus($a->connections->first()),
          ]);
      ```
    - Method `private function resolveStatus(?Connection $c): string`:
      ```
      if ($c === null) return 'none';
      if ($c->revoked_at !== null) return 'revoked';
      // 'connected' = OAuth-Connection has access_token, OF key-based Connection has client_key
      if ($c->access_token !== null) return 'connected';
      if ($c->client_key !== null) return 'connected';
      return 'pending';
      ```
    - Optional `public function totalsForProvider(string $provider): array` (gebruikt door index-card): `['connected' => ..., 'total' => ...]` — count-aggregaten via descriptor-driven query (analog ConnectionStatsWidget regel 24-41).

    Create `resources/views/partners/partials/_domeinmodel.blade.php`:
    - Pure Blade, GEEN PHP-state. Tailwind v4 utilities (no inline `<style>`).
    - Outer: `<section class="rounded-lg bg-gray-50 dark:bg-neutral-900 p-6 my-12">`
    - Heading: `<h2 class="text-xl font-semibold leading-tight mb-4">Hoe de Hub-tenancy in elkaar zit</h2>` (UI-SPEC regel 183)
    - 3 bullets met canonical copy (UI-SPEC regel 184-186, EXACT):
      ```
      <ul class="space-y-2 text-base">
          <li><strong>Consumer</strong> → jouw SaaS-app (Naschool, Planny, externe app). Authenticeert met een Bearer-PAT.</li>
          <li><strong>Account</strong> → een klant van jouw app (school A, vereniging C).</li>
          <li><strong>Connection</strong> → de partner-koppeling van die Account (school A's eigen Mollie- of Snelstart-account).</li>
      </ul>
      ```

    Create `resources/views/partners/partials/_status-widget.blade.php`:
    - Verwacht variabelen `$provider` (string) + `$accountStatus` (Collection van entries `{account, connection, status}`).
    - Outer: `<section class="rounded-lg border border-gray-200 p-6 my-8">`
    - Heading: `<h3 class="text-xl font-semibold leading-tight mb-4">Live koppel-status (dev-omgeving)</h3>` (UI-SPEC regel 197)
    - Empty-state branch:
      ```
      @if ($accountStatus->isEmpty())
          <p class="text-sm text-gray-500">Geen demo-Accounts — draai <code>php artisan db:seed</code> eerst.</p>
      @else
          <ul class="space-y-2">
              @foreach ($accountStatus as $entry)
                  @php
                      $statusConfig = match ($entry['status']) {
                          'connected' => ['icon' => 'check-circle', 'text' => 'gekoppeld', 'classes' => 'text-emerald-600 bg-emerald-50'],
                          'pending' => ['icon' => 'clock', 'text' => 'pending — wacht op OAuth-callback', 'classes' => 'text-amber-600 bg-amber-50'],
                          'revoked' => ['icon' => 'x-circle', 'text' => 'revoked at ' . optional($entry['connection']?->revoked_at)->format('Y-m-d H:i'), 'classes' => 'text-rose-600 bg-rose-50'],
                          default => ['icon' => 'minus-circle', 'text' => 'nog niet gekoppeld', 'classes' => 'text-gray-500 bg-gray-50'],
                      };
                      $expiresAt = optional($entry['connection']?->expires_at)->format('Y-m-d H:i');
                  @endphp
                  <li class="flex items-center gap-2 px-3 py-2 rounded {{ $statusConfig['classes'] }}">
                      <x-heroicon-o-{{ $statusConfig['icon'] }} class="h-5 w-5" aria-hidden="true" />
                      <span class="sr-only">Status: {{ $statusConfig['text'] }}</span>
                      <span class="text-sm font-medium">
                          {{ $entry['account']->display_name }}: {{ ucfirst($provider) }} {{ $statusConfig['text'] }}
                          @if ($entry['status'] === 'connected' && $expiresAt)
                              — expires {{ $expiresAt }}
                          @endif
                      </span>
                  </li>
              @endforeach
          </ul>
      @endif
      ```
    - **Heroicon-componenten**: check of `blade-ui-kit/blade-heroicons` is geïnstalleerd. Indien niet, fallback naar inline SVG of `<svg>`-strings — verifieer met `composer show | grep heroicons`. Indien afwezig: gebruik tekst-emoji ('✅', '⚠', '❌', '—') uit UI-SPEC regel 192-196 als fallback (documenteer choice in SUMMARY).

    Create `tests/Feature/Dev/PartnerPagesTest.php`:
    - Extend `Tests\TestCase` met `RefreshDatabase`.
    - Implementeer alle 10 tests uit `<behavior>` (Task 1 portion: tests 1-10). Voor partial-render-tests: gebruik `view('partners.partials._domeinmodel')->render()` direct, of via `$this->blade('@include(...)')`.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PartnerPagesTest</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Services/PartnerStatus.php` bestaat met `class PartnerStatus` + `forProvider(string)` method
    - `grep -c "Account::with" app/Services/PartnerStatus.php` ≥ 1 (eager-load — N+1 guard)
    - `grep -c "resolveStatus" app/Services/PartnerStatus.php` ≥ 1
    - File `resources/views/partners/partials/_domeinmodel.blade.php` bestaat
    - `grep -c "Consumer\|Account\|Connection" resources/views/partners/partials/_domeinmodel.blade.php` ≥ 3 (canonical 3 bullets)
    - File `resources/views/partners/partials/_status-widget.blade.php` bestaat
    - `grep -c "text-emerald-600\|text-amber-600\|text-rose-600\|text-gray-500" resources/views/partners/partials/_status-widget.blade.php` == 4 (alle 4 semantische kleuren)
    - `grep -c "sr-only" resources/views/partners/partials/_status-widget.blade.php` ≥ 1 (a11y per UI-SPEC §Accessibility regel 299)
    - `php artisan test --compact --filter=PartnerPagesTest` exits 0 met ≥ 10 passed voor task 1-scope tests
  </acceptance_criteria>
  <done>Service + partials operationeel; canonical copy + status-palette correct; 10 tests groen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend partners-views + voeg dev-only Mollie OAuth-init route toe</name>
  <files>resources/views/partners/index.blade.php, resources/views/partners/mollie/example.blade.php, resources/views/partners/snelstart/example.blade.php, routes/web.php, tests/Feature/Dev/PartnerPagesTest.php</files>
  <read_first>
    - resources/views/partners/index.blade.php (regel 1-46 — current structure)
    - resources/views/partners/mollie/example.blade.php (regel 1-57 — current structure)
    - resources/views/partners/snelstart/example.blade.php (regel 1-50 — current structure)
    - app/Services/PartnerStatus.php (net opgeleverd in Task 1)
    - routes/web.php (regel 27-56 — env-gating + bestaande dev-routes; nieuwe route komt binnen ditzelfde blok)
    - app/Http/Controllers/Api/V1/OAuth/InitController.php (regel 22-51 — Phase-4 init-pattern dat de dev-route 1:1 hergebruikt)
    - app/OAuth/OAuthFlowRegistry.php (registry-API)
    - app/Models/Account.php (`->connections()->create(...)`)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-UI-SPEC.md (§S3 Copywriting Contract regel 179-201 + §Color reserved-for amber regel 106-113 + regel 191 CTA-contract)
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-PATTERNS.md (§"partners/index.blade.php" + §"partners/mollie/example.blade.php" + §"partners/snelstart/example.blade.php")
    - .planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-CONTEXT.md (§D-06 §3 — Mollie-CTA moet live OAuth-init triggeren voor pre-selected demo-Account)
  </read_first>
  <behavior>
    - Test 1 (env-gating index): `Config::set('app.env', 'production')` → GET `/dev/partners` returnt 404
    - Test 2 (index rendert): GET `/dev/partners` in local env → 200 + bevat heading 'Partner previews' (existing) + heading 'Hoe de Hub-tenancy in elkaar zit' (new domeinmodel)
    - Test 3 (index per-card status-totaal): seed 1 Account met Mollie-Connection (connected) + 1 zonder → response bevat 'Mollie: 1/2 Accounts gekoppeld'
    - Test 4 (mollie page rendert): GET `/dev/partners/mollie` → 200 + bevat heading 'Koppelen via OAuth Connect' + 3 stap-bullets (canonical UI-SPEC regel 189-191)
    - Test 5 (mollie CTA wijst naar dev-route + triggert OAuth-init): response op `/dev/partners/mollie` bevat een anchor met `bg-amber-500`-class én `href` die `/dev/partners/mollie/start-oauth` bevat. Daarna: seed Naschool-Consumer + demo-Account → `GET /dev/partners/mollie/start-oauth` returnt 302 redirect met Location-header die start met `https://www.mollie.com/oauth2/authorize` (= Mollie Connect authorize-host uit `config('services.mollie.connect.authorize_url')` of equivalent). Mock `MollieConnectOAuthFlow::getAuthorizationUrl()` indien deterministische URL nodig
    - Test 6 (mollie dev-route creëert pending Connection): na `GET /dev/partners/mollie/start-oauth` → DB heeft één extra Connection-rij met `provider='mollie'`, `status='pending'`, `oauth_state` is 48-char string, `oauth_state_expires_at` ~30 min in future
    - Test 7 (mollie dev-route 404 zonder demo-Account): geen Naschool-seed → `GET /dev/partners/mollie/start-oauth` returnt 404 met message bevat 'demo-Account' (geen exception-leak)
    - Test 8 (mollie dev-route 404 in production): `Config::set('app.env', 'production')` → `GET /dev/partners/mollie/start-oauth` returnt 404 (env-gate werkt op nieuwe route net als op bestaande)
    - Test 9 (mollie status-widget): seed 1 Account met active Mollie-Connection → response op `/dev/partners/mollie` bevat status-regel met 'gekoppeld' + 'text-emerald-600'
    - Test 10 (snelstart page rendert): GET `/dev/partners/snelstart` → 200 + bevat heading 'Koppelen via credential-form' + 3 stap-bullets (UI-SPEC regel 194-196)
    - Test 11 (snelstart cURL-snippet): response bevat `curl -X POST` + `/v1/connections` + JSON body met `provider":"snelstart"`
    - Test 12 (no-secret-leak): seed Snelstart-Connection met plain client_key='SECRETKEY' → response op `/dev/partners/snelstart` NIET `'SECRETKEY'`-string (alleen fingerprint of niets — encrypted-cast invariant)
    - Test 13 (domeinmodel-blokje op beide provider-pages): GET `/dev/partners/mollie` AND `/dev/partners/snelstart` → beide bevatten 'Hoe de Hub-tenancy in elkaar zit'
  </behavior>
  <action>
    **Routes-edit (chirurgisch — eerst, want andere taken hangen af van route-name):**

    Modify `routes/web.php` binnen het bestaande `if (app()->environment('local', 'testing'))`-blok (regel 27-56). Voeg ÉÉN nieuwe route toe na de bestaande `'dev.partners.preview'`-route (regel 48-55):

    1. Use-statement bovenaan toevoegen: `use App\Models\Account;` (al aanwezig — `User` ook), `use App\OAuth\OAuthFlowRegistry;`, `use Illuminate\Support\Str;`.
    2. Nieuwe route:
       ```
       Route::get('/dev/partners/mollie/start-oauth', function () {
           $account = Account::query()
               ->whereHas('consumer', fn ($q) => $q->where('slug', 'naschool'))
               ->first();
           abort_unless($account !== null, 404, 'Geen demo-Account — draai EmeqStaffSeeder + Naschool-seed eerst.');

           $state = Str::random(48);
           $account->connections()->create([
               'provider' => 'mollie',
               'status' => 'pending',
               'oauth_state' => $state,
               'oauth_state_expires_at' => now()->addMinutes(30),
           ]);

           $scopes = config('services.mollie.connect.scopes');
           $url = app(OAuthFlowRegistry::class)->for('mollie')->getAuthorizationUrl($account, $scopes, $state);

           return redirect()->away($url);
       })->name('dev.partners.mollie.start-oauth');
       ```

    **NB**: dit is een chirurgische uitbreiding van `routes/web.php` (was eerder bewust niet aangeraakt in dit plan). De engineering-rule "chirurgisch wijzigen" wordt gerespecteerd door: (a) binnen het bestaande env-gated blok blijven, (b) één route toevoegen zonder andere routes te wijzigen, (c) geen extractie naar controller — same closure-pattern als de andere dev-routes. Reden voor de uitbreiding: UI-SPEC §S3 regel 191 + CONTEXT D-06 §3 vereisen dat de Mollie-CTA daadwerkelijk de Phase-4 OAuth-init triggert, niet een inert anchor.

    **Blade-edits:**

    Modify `resources/views/partners/index.blade.php`:
    - Voeg `@include('partners.partials._domeinmodel')` toe boven of na het `<h1>Partner previews</h1>`-blok (UI-SPEC §S3 regel 180-181 — uitbreiding eronder).
    - Voor elk `<li>` in de provider-list (regel 24-43): voeg per-card status-totaal toe via:
      ```blade
      @php $totals = app(\App\Services\PartnerStatus::class)->totalsForProvider($provider); @endphp
      <div class="text-sm text-gray-500 mt-1">
          {{ ucfirst($provider) }}: {{ $totals['connected'] }}/{{ $totals['total'] }} Accounts gekoppeld
      </div>
      ```
      (UI-SPEC regel 200)
    - Migreer inline `<style>` (regel 1-21) naar Tailwind utility-classes op de elements zelf. Verwijder de `<style>`-block. Vervang: container → `max-w-3xl mx-auto px-4 py-12`, headings → `text-3xl font-semibold leading-tight`, etc. (UI-SPEC §Typography).

    Modify `resources/views/partners/mollie/example.blade.php`:
    - Voeg `@include('partners.partials._domeinmodel')` boven `<h2>Use-cases</h2>`.
    - Voeg nieuwe sectie toe: `<h2 class="text-xl font-semibold leading-tight">Koppelen via OAuth Connect</h2>` + ordered-list met 3 stap-bullets (CANONICAL UI-SPEC regel 189-191).
    - Voeg CTA-anchor toe die naar de nieuwe dev-route wijst (UI-SPEC regel 191 + Color §reserved-for #6). Het is een GET-anchor styled als button — geen `<form>`, geen JS:
      ```
      <a href="{{ route('dev.partners.mollie.start-oauth') }}"
         class="inline-flex items-center gap-2 rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-white hover:bg-amber-600 mt-4">
          Start OAuth-flow
      </a>
      ```
      De anchor leidt naar de dev-only route die server-side de Phase-4 OAuth-init triggert (state-generatie + pending Connection + Mollie authorize-redirect). De pre-selected demo-Account is de Naschool-Consumer's eerste Account (geseed via `EmeqStaffSeeder` of `NaschoolDemoSeeder` indien aanwezig). HTML-semantiek: `<a>` met click-target matcht UI-SPEC §S3 regel 191 ("links naar `/v1/oauth/mollie/init` met pre-selected demo-Account") — implementatie verschilt (we routen via een dev-route i.p.v. `/v1/oauth/mollie/init` direct, omdat die endpoint PAT-auth vereist en de dev-page geen PAT heeft), maar het contract is identiek: één klik → server-side redirect naar Mollie authorize-URL.
    - Voeg `@php $accountStatus = app(\App\Services\PartnerStatus::class)->forProvider('mollie'); @endphp` + `@include('partners.partials._status-widget', ['provider' => 'mollie', 'accountStatus' => $accountStatus])` toe onderaan, vóór de footer-links.

    Modify `resources/views/partners/snelstart/example.blade.php`:
    - Voeg `@include('partners.partials._domeinmodel')` toe na de tweede `<p>` (boven `<h2>Use-cases</h2>` of vergelijkbaar).
    - Voeg `<h2 class="text-xl font-semibold leading-tight">Koppelen via credential-form</h2>` + ordered-list met 3 stappen (CANONICAL UI-SPEC regel 194-196).
    - Voeg `<pre><code>`-blok met cURL-snippet (CANONICAL UI-SPEC regel 196):
      ```
      curl -X POST {APP_URL}/v1/connections \
        -H "Authorization: Bearer {PAT}" \
        -H "Content-Type: application/json" \
        -d '{"account_external_id":"school1","provider":"snelstart","client_key":"…","subscription_key":"…","subscription_id":"…"}'
      ```
    - Voeg `@php $accountStatus = app(\App\Services\PartnerStatus::class)->forProvider('snelstart'); @endphp` + `@include('partners.partials._status-widget', ['provider' => 'snelstart', 'accountStatus' => $accountStatus])` toe onderaan.

    **Tests:**

    Update `tests/Feature/Dev/PartnerPagesTest.php` met tests 1-13 uit `<behavior>` (Task 2 scope). Maak gebruik van Account + Connection factories. Voor Test 5: bind een testdouble voor `OAuthFlowRegistry::for('mollie')` die een vaste `getAuthorizationUrl(...)`-return geeft (bv. `https://www.mollie.com/oauth2/authorize?client_id=test&state=...`) — gebruik Laravel's container `$this->app->bind(OAuthFlowRegistry::class, ...)` of mock via `Mockery`. Voor Test 8: gebruik `$this->app->detectEnvironment(fn () => 'production')` of `Config::set('app.env', 'production')` + `Route::getRoutes()->refreshNameLookups()` — kies pattern uit bestaande Phase-9 env-tests indien aanwezig, anders Pest-style env-mock.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PartnerPagesTest</automated>
  </verify>
  <acceptance_criteria>
    - `routes/web.php` bevat `Route::get('/dev/partners/mollie/start-oauth', ...)->name('dev.partners.mollie.start-oauth')` binnen het bestaande env-gated `if`-blok (regel 27)
    - `grep -c "dev.partners.mollie.start-oauth" routes/web.php` ≥ 1
    - `grep -c "OAuthFlowRegistry" routes/web.php` ≥ 1
    - `php artisan route:list --path=dev/partners/mollie/start-oauth` toont de route in local env
    - `resources/views/partners/index.blade.php` bevat `@include('partners.partials._domeinmodel')`
    - `resources/views/partners/index.blade.php` bevat per-provider status-totaal-blok
    - `grep -c "<style>" resources/views/partners/index.blade.php` == 0 (inline style verwijderd, Tailwind-utilities gebruikt)
    - `resources/views/partners/mollie/example.blade.php` bevat 'Koppelen via OAuth Connect' heading + 3 stap-bullets met CANONICAL copy
    - `grep -c "bg-amber-500" resources/views/partners/mollie/example.blade.php` ≥ 1 (CTA accent)
    - `grep -c "dev.partners.mollie.start-oauth\|/dev/partners/mollie/start-oauth" resources/views/partners/mollie/example.blade.php` ≥ 1 (CTA-anchor wijst naar dev-route, geen inert `#start-oauth`)
    - `grep -c "_status-widget" resources/views/partners/mollie/example.blade.php` ≥ 1
    - `resources/views/partners/snelstart/example.blade.php` bevat 'Koppelen via credential-form' + cURL-snippet
    - `grep -c "curl -X POST" resources/views/partners/snelstart/example.blade.php` ≥ 1
    - `grep -c "provider\":\"snelstart\"" resources/views/partners/snelstart/example.blade.php` ≥ 1
    - `php artisan test --compact --filter=PartnerPagesTest` exits 0 met ≥ 13 totaal voor Task 2 scope (cumulatief met Task 1 = 23 tests)
    - Smoke: `php artisan view:clear && curl -s http://hub.emeq.test:8090/dev/partners/mollie | grep -c "Hoe de Hub-tenancy"` ≥ 1
    - Smoke: `curl -I http://hub.emeq.test:8090/dev/partners/mollie/start-oauth` returnt 302 (na demo-seed) of 404 (zonder)
  </acceptance_criteria>
  <done>Alle 3 Blade-views uitgebreid; dev-route operationeel; Mollie-CTA triggert daadwerkelijk Phase-4 OAuth-init; alle tests groen.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=PartnerPagesTest` — 23+ tests groen
- `php artisan test --compact` — volledige suite groen (geen Phase-9 of Phase-3 regressies)
- `vendor/bin/pint --test --format agent app/Services/PartnerStatus.php resources/views/partners/ routes/web.php` exits 0
- Lokaal smoke (browser): `bin/dev up` → http://hub.emeq.test:8090/dev/partners → domeinmodel-blokje + 2 provider-cards met status-totaal → klik Mollie → 3 koppel-stappen + amber Start-OAuth CTA + status-widget per Account → klik op CTA → browser redirect naar Mollie authorize-URL (live OAuth-init) → klik Snelstart → 3 stappen + cURL + status-widget
</verification>

<success_criteria>
- PartnerStatus-service operationeel met N+1-guard
- 2 nieuwe Blade-partials (domeinmodel + status-widget) met canonical UI-SPEC copy
- Status-widget gebruikt canonical Heroicon + Tailwind kleur-tokens (emerald/amber/rose/gray)
- /dev/partners/index toont domeinmodel + per-provider status-totaal
- /dev/partners/mollie toont OAuth-koppel-stappen + amber CTA + live status-widget
- Mollie-CTA wijst naar dev-only route die Phase-4 OAuth-init server-side triggert (UI-SPEC §S3 regel 191 + CONTEXT D-06 §3 voldaan — geen inert anchor)
- Dev-only OAuth-init route is bereikbaar in `local`/`testing` en 404 in production
- /dev/partners/snelstart toont credential-form-stappen + cURL-snippet + live status-widget
- No-secret-leak: plain client_key/subscription_key komen nergens in HTML voor
- Env-gating intact: production = 404 op alle dev-routes (inclusief de nieuwe OAuth-init)
- Routes-edit is chirurgisch: 1 nieuwe route binnen het bestaande env-gated blok in `routes/web.php`
- 23+ feature-tests groen, geen regressies in Phase-9
</success_criteria>

<output>
Create `.planning/phases/08-naschool-wiring-snelstart-mollie-via-hub/08-05-SUMMARY.md` na completion, met notitie over Heroicon-component-keuze (blade-icon vs SVG vs emoji fallback) en over Mollie-CTA-pad (live dev-OAuth-init via `dev.partners.mollie.start-oauth`-route — geen anchor-only stub).
</output>
