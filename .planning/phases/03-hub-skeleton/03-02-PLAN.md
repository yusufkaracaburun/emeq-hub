---
phase: 03-hub-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - config/auth.php
  - bootstrap/app.php
  - app/Sanctum/TokenAbilities.php
autonomous: true
requirements:
  - HUB-01
tags:
  - laravel
  - sanctum
  - auth
  - routing
must_haves:
  truths:
    - "`config('auth.guards.sanctum')` retourneert `['driver' => 'sanctum', 'provider' => 'consumers']`"
    - "`config('auth.providers.consumers')` retourneert `['driver' => 'eloquent', 'model' => App\\\\Models\\\\Consumer::class]`"
    - "De `web`-guard + `users`-provider blijven bestaan (User is voor Filament Phase 9, niet verwijderen)"
    - "`bootstrap/app.php` registreert `routes/api.php` met `apiPrefix: 'v1'`"
    - "`App\\\\Sanctum\\\\TokenAbilities` exposeert de 6 abilities als public const + `all()`-helper"
  artifacts:
    - path: "config/auth.php"
      provides: "Sanctum-guard + consumers-provider naast de bestaande web/users"
      contains: "'sanctum'"
    - path: "bootstrap/app.php"
      provides: "withRouting met `api:` argument zodat /v1/* routes worden geladen"
      contains: "apiPrefix: 'v1'"
    - path: "app/Sanctum/TokenAbilities.php"
      provides: "Constants-class met SNELSTART_READ/WRITE, MOLLIE_READ/WRITE, CONSUMER_MANAGE_ACCOUNTS, ADMIN"
      contains: "final class TokenAbilities"
  key_links:
    - from: "config/auth.php"
      to: "app/Models/Consumer.php"
      via: "providers.consumers.model = Consumer::class"
      pattern: "App\\\\\\\\Models\\\\\\\\Consumer::class"
    - from: "bootstrap/app.php"
      to: "routes/api.php"
      via: "withRouting(api: ...)"
      pattern: "routes/api\\.php"
---

<objective>
Activeer Sanctum als `/v1/*`-auth-mechanisme door de Consumer-provider in `config/auth.php` te registreren, het `sanctum`-guard toe te voegen, en `bootstrap/app.php` zo te configureren dat `routes/api.php` automatisch onder `/v1/*` prefix wordt geladen met de `api`-middleware-group (throttle + bindings). Levert ook `App\Sanctum\TokenAbilities` als constants-class zodat plans 03-03 (PingTest), 03-05 (hub:consumer:create-command) en Phase 5b (pass-through ability-checks) consistente ability-strings hebben.

Purpose: Zonder dit plan bestaat er geen `auth:sanctum`-middleware-config en geen `/v1/*`-routing. HUB-01 vereist `Sanctum-PAT auth voor Consumer-routes` — dit plan is de configuratie-laag.

Output: 3 files aangeraakt (2 bestaande extend, 1 nieuw). Geen routes, geen controllers, geen tests in dit plan — wel een `routes/api.php`-skeleton bestand (leeg-met-import) zodat `bootstrap/app.php`'s `api:`-pad bestaat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hub-skeleton/03-CONTEXT.md
@.planning/phases/03-hub-skeleton/03-PATTERNS.md
@.planning/phases/03-hub-skeleton/03-01-SUMMARY.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@CLAUDE.md

<interfaces>
<!-- Huidige bootstrap/app.php (regels 1-19) — uitbreiden met `api:` en `apiPrefix: 'v1'`. -->
```php
return Application::configure(basePath: dirname(__DIR__))
    ->withRouting(
        web: __DIR__.'/../routes/web.php',
        commands: __DIR__.'/../routes/console.php',
        health: '/up',
    )
    ->withMiddleware(function (Middleware $middleware): void {
        $middleware->append(SetNoIndexHeaders::class);
    })
    ->withExceptions(function (Exceptions $exceptions): void { /* noop */ })
    ->create();
```

<!-- Huidige config/auth.php guards + providers (regels 40-74) — niet vervangen, uitbreiden. -->
```php
'guards' => [
    'web' => ['driver' => 'session', 'provider' => 'users'],
],
'providers' => [
    'users' => [
        'driver' => 'eloquent',
        'model' => env('AUTH_MODEL', User::class),
    ],
],
```

<!-- Sanctum's `auth:sanctum`-middleware verwacht een guard-config met `driver=sanctum, provider=consumers`. -->
```php
// vendor/laravel/sanctum/src/Guard.php — resolve provider via config('auth.providers.<name>')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TokenAbilities constants-class</name>
  <files>app/Sanctum/TokenAbilities.php</files>
  <read_first>
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regel 189 (PAT-abilities decision: `<provider>:<action>` conventie + `*` + `consumer:manage-accounts`)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 368-402 (TokenAbilities-skeleton)
    - .ai/rules/global.md (Engels voor identifiers — abilities-strings zijn identifiers)
  </read_first>
  <action>
    Maak de directory `app/Sanctum/` (bestaat nog niet) en plaats `TokenAbilities.php` als final-class met
    `public const` voor de zes abilities + een `all(): array`-helper.

    ```php
    <?php

    namespace App\Sanctum;

    final class TokenAbilities
    {
        public const SNELSTART_READ = 'snelstart:read';

        public const SNELSTART_WRITE = 'snelstart:write';

        public const MOLLIE_READ = 'mollie:read';

        public const MOLLIE_WRITE = 'mollie:write';

        public const CONSUMER_MANAGE_ACCOUNTS = 'consumer:manage-accounts';

        public const ADMIN = '*';

        /**
         * @return list<string>
         */
        public static function all(): array
        {
            return [
                self::SNELSTART_READ,
                self::SNELSTART_WRITE,
                self::MOLLIE_READ,
                self::MOLLIE_WRITE,
                self::CONSUMER_MANAGE_ACCOUNTS,
                self::ADMIN,
            ];
        }
    }
    ```

    **Waarom `final class` met `const` i.p.v. `enum TokenAbility: string`:** Sanctum vergelijkt ruwe strings
    via `tokenCan('snelstart:read')` — een enum zou bij elke check een `->value` toevoegen. Project heeft nog
    geen enums in repo (PATTERNS.md regel 402 — `.ai/rules/php` "TitleCase for Enum keys" geldt alleen voor enums
    die we elders al gebruiken). Constants matched de minimalistische conventie.

    Run `vendor/bin/pint --dirty --format agent app/Sanctum/TokenAbilities.php`.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute 'echo count(App\Sanctum\TokenAbilities::all()) === 6 ? "abilities:ok " : "abilities:FAIL "; echo App\Sanctum\TokenAbilities::SNELSTART_READ === "snelstart:read" ? "snel-read:ok " : "snel-read:FAIL "; echo App\Sanctum\TokenAbilities::ADMIN === "*" ? "admin:ok\n" : "admin:FAIL\n";'
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/Sanctum/TokenAbilities.php` bestaat
    - `grep -c "public const" app/Sanctum/TokenAbilities.php` == 6
    - `grep -c "final class TokenAbilities" app/Sanctum/TokenAbilities.php` == 1
    - Tinker-output bevat `abilities:ok snel-read:ok admin:ok`
    - `vendor/bin/pint --test --format agent app/Sanctum/TokenAbilities.php` exit code 0
  </acceptance_criteria>
  <done>
    `App\Sanctum\TokenAbilities` is bruikbaar als enkelvoudige bron voor ability-strings; 6 constants + `all()`-helper
    werken.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sanctum-guard + consumers-provider in config/auth.php</name>
  <files>config/auth.php</files>
  <read_first>
    - config/auth.php (huidige guards + providers; lijnen 40-74)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 451-498 (config/auth.php uitbreiding)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 158-171 (Sanctum-config decision)
  </read_first>
  <action>
    Uitbreiden — niet vervangen — de bestaande `'guards'` en `'providers'` arrays in `config/auth.php`. Voeg het
    `'sanctum'`-guard toe naast `'web'`, en voeg het `'consumers'`-provider toe naast `'users'`.

    **Top-of-file import-add** (regel 3, naast `use App\Models\User;`):
    ```php
    use App\Models\Consumer;
    ```

    **`'guards'`-array uitbreiden** (na de bestaande `'web'`-entry, vóór de sluit-bracket):
    ```php
    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],

        'sanctum' => [
            'driver' => 'sanctum',
            'provider' => 'consumers',
        ],
    ],
    ```

    **`'providers'`-array uitbreiden** (na de bestaande `'users'`-entry, vóór de commentaar-block met de
    database-driver-alternative):
    ```php
    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => env('AUTH_MODEL', User::class),
        ],

        'consumers' => [
            'driver' => 'eloquent',
            'model' => Consumer::class,
        ],

        // 'users' => [
        //     'driver' => 'database',
        //     'table' => 'users',
        // ],
    ],
    ```

    **Niet aanpassen:** `'defaults'`, `'passwords'`, `'password_timeout'` — die blijven exact zoals nu. De `web`-guard
    + `users`-provider mogen NIET worden verwijderd: `User` blijft voor Filament admin-paneel in Phase 9 (CONTEXT.md
    Claude's Discretion regel 277).

    Run `vendor/bin/pint --dirty --format agent config/auth.php`.
  </action>
  <verify>
    <automated>
      php artisan config:clear &amp;&amp;
      php artisan tinker --execute '$g = config("auth.guards.sanctum"); $p = config("auth.providers.consumers"); echo ($g["driver"] ?? null) === "sanctum" ? "guard:ok " : "guard:FAIL "; echo ($p["model"] ?? null) === App\Models\Consumer::class ? "provider:ok " : "provider:FAIL "; echo config("auth.guards.web.driver") === "session" ? "web-intact:ok\n" : "web-intact:FAIL\n";'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'sanctum'" config/auth.php` == 1
    - `grep -c "'consumers'" config/auth.php` == 1 (de provider-key)
    - `grep -c "Consumer::class" config/auth.php` == 1
    - `grep -c "User::class" config/auth.php` == 1 (web-provider blijft, niet weggehaald)
    - Tinker-output bevat `guard:ok provider:ok web-intact:ok`
    - `vendor/bin/pint --test --format agent config/auth.php` exit code 0
  </acceptance_criteria>
  <done>
    `config('auth.guards.sanctum')` en `config('auth.providers.consumers')` zijn beschikbaar; bestaande web/users
    guard+provider zijn intact (geen regressies in User-auth).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: bootstrap/app.php uitbreiden met api-routes-pad + routes/api.php skeleton</name>
  <files>
    bootstrap/app.php,
    routes/api.php
  </files>
  <read_first>
    - bootstrap/app.php (huidige withRouting-block; lijnen 8-13)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 406-446 (bootstrap-uitbreiding pattern)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 173-175 (bootstrap-decision: `apiPrefix: 'v1'`)
    - routes/web.php (huidige route-stijl voor consistency met `routes/api.php`-skeleton)
  </read_first>
  <action>
    **`bootstrap/app.php` aanpassing:** voeg `api: __DIR__.'/../routes/api.php'` en `apiPrefix: 'v1'` toe in
    `->withRouting(...)`. Volgorde van keys volgens Laravel-conventie: `web, api, apiPrefix, commands, health`.
    De `SetNoIndexHeaders`-middleware-append blijft ongewijzigd. **Geen** stateful-Sanctum-middleware
    (`EnsureFrontendRequestsAreStateful`) toevoegen — Hub gebruikt alleen Bearer-PAT, geen SPA-cookies (per
    PATTERNS.md regel 433).

    Volledige nieuwe `bootstrap/app.php`-body:
    ```php
    <?php

    use App\Http\Middleware\SetNoIndexHeaders;
    use Illuminate\Foundation\Application;
    use Illuminate\Foundation\Configuration\Exceptions;
    use Illuminate\Foundation\Configuration\Middleware;

    return Application::configure(basePath: dirname(__DIR__))
        ->withRouting(
            web: __DIR__.'/../routes/web.php',
            api: __DIR__.'/../routes/api.php',
            apiPrefix: 'v1',
            commands: __DIR__.'/../routes/console.php',
            health: '/up',
        )
        ->withMiddleware(function (Middleware $middleware): void {
            $middleware->append(SetNoIndexHeaders::class);
        })
        ->withExceptions(function (Exceptions $exceptions): void {
            //
        })->create();
    ```

    **`routes/api.php` skeleton (nieuw bestand):** Plan 03-03 vult dit met de `/ping`-route. In dit plan landen
    we alleen het skeleton zodat `bootstrap/app.php`'s `api:`-pad bestaat (anders faalt route-caching/route-list).
    ```php
    <?php

    use Illuminate\Support\Facades\Route;

    /*
    |--------------------------------------------------------------------------
    | API Routes — /v1/*
    |--------------------------------------------------------------------------
    | Prefix `v1` wordt gezet in bootstrap/app.php (apiPrefix). Auth via
    | Sanctum-PAT. Concrete routes worden gevuld door plan 03-03 (ping),
    | Phase 4 (oauth/mollie/*), Phase 5a (/v1/mollie/*) en Phase 5b
    | (/v1/accounts, /v1/connections, /v1/snelstart/*).
    */
    ```

    **Niet doen in dit plan:** geen `/v1/ping`-route — die landt in 03-03.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
      php artisan route:clear &amp;&amp;
      php artisan route:list --json 2>/dev/null | grep -c '"uri":"\\\\/up"' &amp;&amp;
      grep -c "apiPrefix: 'v1'" bootstrap/app.php
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apiPrefix: 'v1'" bootstrap/app.php` == 1
    - `grep -c "api: __DIR__" bootstrap/app.php` == 1
    - `routes/api.php` bestaat (`test -f routes/api.php` exit 0)
    - `php artisan route:list --except-vendor` slaagt zonder errors en toont nog steeds `/` en `/up` web-routes
    - `bootstrap/app.php` bevat nog steeds `SetNoIndexHeaders::class` (`grep -c "SetNoIndexHeaders" bootstrap/app.php` == 2 — één `use` + één `append`)
    - `vendor/bin/pint --test --format agent bootstrap/app.php routes/api.php` exit code 0
  </acceptance_criteria>
  <done>
    `bootstrap/app.php` registreert de api-routes-file met `/v1/*` prefix; `routes/api.php` bestaat als skeleton
    klaar voor 03-03; bestaande web-routes en SetNoIndexHeaders-middleware zijn intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet ↔ `/v1/*` | Externe Consumer-apps doen HTTP-calls met `Authorization: Bearer <PAT>` — Sanctum verifieert tegen `consumers`-provider |
| Application config ↔ runtime | `config('auth.*')` wordt gecached door Laravel — vergeten van `config:clear` bij wijziging veroorzaakt stale guard-config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | Sanctum-PAT brute-force op `/v1/*` | mitigate | `routes/api.php` krijgt automatisch `api`-middleware-group met `throttle:api` (Laravel 11+ default — 60 requests/minute per IP). Verifieer via `php artisan route:list` na plan 03-03 |
| T-03-07 | Spoofing | PAT-replay na lek | mitigate | Sanctum default-token-hashing (`hash('sha256', $plainToken)` in DB — `vendor/laravel/sanctum/src/HasApiTokens.php:103`). Compromise van `personal_access_tokens.token`-kolom levert geen plain-token op |
| T-03-08 | Elevation of Privilege | Misconfigured provider (`users` i.p.v. `consumers`) | mitigate | `'sanctum'`-guard verwijst expliciet naar `'consumers'`-provider (config/auth.php). Acceptance-criteria 2 verifieert via `config('auth.providers.consumers.model') === Consumer::class` |
| T-03-09 | Tampering | Stateful-cookie-injection via `EnsureFrontendRequestsAreStateful` (niet gewenst — alleen Bearer) | accept | Middleware bewust NIET toegevoegd; Hub is API-only, geen SPA-frontend (PATTERNS.md regel 433). Indien Phase 9 admin-UI dit nodig heeft, scope is daar |
</threat_model>

<verification>
- `php artisan config:clear` slaagt
- `php artisan route:clear` slaagt
- `php artisan route:list --except-vendor` werkt (geen "config exception"-errors)
- `config('auth.guards.sanctum')` retourneert het verwachte array
- `config('auth.providers.consumers.model') === App\Models\Consumer::class`
- `App\Sanctum\TokenAbilities::all()` retourneert 6 elementen
</verification>

<success_criteria>
- Sanctum-config is volledig: guard + provider gekoppeld aan Consumer-model
- `routes/api.php` bestaat als skeleton met `/v1/*`-prefix-config
- Bestaande User/web-auth-pad is intact (geen regressie in `/`, `/up`)
- TokenAbilities-class is bruikbaar voor 03-03 (PingTest abilities-payload) en 03-05 (hub:consumer:create --abilities)
</success_criteria>

<output>
Maak `.planning/phases/03-hub-skeleton/03-02-SUMMARY.md` met:
- Welke configs zijn aangepast (auth.guards, auth.providers, withRouting)
- Welke skeleton-files zijn aangemaakt (`routes/api.php`, `app/Sanctum/TokenAbilities.php`)
- Bevestiging dat User/web-auth intact is (geen regressie)
- Vervolg: plan 03-03 vult `routes/api.php` met `/ping`; plan 03-05 gebruikt `TokenAbilities::all()` voor `--abilities`-default
</output>
