---
phase: 06-cashier-mollie-integratie-use-case-a
plan: 07
type: execute
wave: 6
depends_on: [06-05, 06-06]
autonomous: true
requirements: [SUB-01]
tags: [cashier-mollie, integration-tests, mollie-test-mode, sub-01]
files_modified:
  - phpunit.xml
  - phpunit.integration.xml
  - tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php
  - tests/Integration/Billing/CashierWebhookEndToEndTest.php
  - tests/Integration/IntegrationTestCase.php
  - .env.example
  - composer.json

user_setup:
  - service: mollie-emeq-eigen-test-account
    why: "Integration-tests hitten echte Mollie test-mode API met Emeq's eigen test-API-key. Onafhankelijk van Connect-tokens (Phase 4)."
    env_vars:
      - name: CASHIER_MOLLIE_KEY
        source: "Mollie test-dashboard → Developers → API keys → `test_…`-prefix"
      - name: MOLLIE_KEY
        source: "Identiek aan CASHIER_MOLLIE_KEY; mollie/laravel-mollie ^4.0 leest deze. (Beide in .env zetten.)"

must_haves:
  truths:
    - "Een nieuw `phpunit.integration.xml` configuratie-bestand bestaat met `@group integration`-filter (D-12) en gebruikt de echte database (NIET `:memory:` SQLite) — onafhankelijk runbaar via `php artisan test --configuration=phpunit.integration.xml`."
    - "Een composer-script `test:integration` roept `phpunit.integration.xml` aan zodat CI 'm apart kan draaien."
    - "Standaard `php artisan test` (PHPUnit met `phpunit.xml`) slaat alle `@group integration` tests over (default-exclude) — bestaande 207+ groene tests blijven groen op een verse `php artisan test`."
    - "`tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php` bevat ≥2 tests die de full happy-path bewijzen: (a) admin POSTs `/v1/admin/billing/subscriptions` → Cashier first_payment-flow start → response heeft `mandate_redirect_url` van Mollie test-mode, (b) bestaande subscription kan via admin DELETE worden cancelled → Mollie subscription state = canceled."
    - "`tests/Integration/Billing/CashierWebhookEndToEndTest.php` bevat ≥1 test die simuleert: Mollie POSTs subscription-payment-update naar `/cashier/webhook` (met set `CASHIER_WEBHOOK_SECRET`) + Cashier's state-machine updatet de subscription-status."
    - "`tests/Integration/IntegrationTestCase.php` is een base-class die `@group integration` op alle subclasses afdwingt (via PHPUnit `Group` attribute) + skip-on-missing-env (`CASHIER_MOLLIE_KEY` empty → markTestSkipped)."
    - "`.env.example` heeft een nieuwe sectie `# Integration tests` met instructies."
    - "Bestaande standaard test-suite blijft op 0 regressies — `php artisan test --compact` levert ≥ baseline-N passed."
  artifacts:
    - path: "phpunit.integration.xml"
      provides: "Integration-suite config (volle DB, niet :memory:, `@group integration`-filter)"
    - path: "tests/Integration/IntegrationTestCase.php"
      provides: "Base-class met env-skip + @group annotatie"
    - path: "tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php"
      provides: "≥2 tests: create-via-admin + cancel-via-admin op echte Mollie test-mode"
      min_tests: 2
    - path: "tests/Integration/Billing/CashierWebhookEndToEndTest.php"
      provides: "≥1 test: webhook-update van Mollie via test-mode triggert subscription-status-update"
      min_tests: 1
    - path: "composer.json"
      provides: "Nieuwe script `test:integration`"
      contains: "test:integration"
  key_links:
    - from: "phpunit.integration.xml"
      to: "tests/Integration/"
      via: "<testsuite name=\"Integration\"><directory>tests/Integration</directory>"
      pattern: "tests/Integration"
    - from: "tests/Integration/IntegrationTestCase.php"
      to: "env('CASHIER_MOLLIE_KEY')"
      via: "markTestSkipped op empty"
      pattern: "markTestSkipped"
---

<objective>
Lever de integration-test-laag voor Phase 6: 1 nieuwe PHPUnit-config-bestand (`phpunit.integration.xml`), 1 Composer-script (`test:integration`), 1 base-class (`IntegrationTestCase`), en 2 integration-test-classes die de end-to-end happy-paths bewijzen — admin-create + admin-cancel + webhook-update — tegen Mollie's echte test-mode API.

Purpose: D-12 vereist dat integration-tests die echte Mollie test-mode API hitten gescheiden zijn van de standaard suite (anders breken ze in CI zonder env-vars). De separatie laat `php artisan test --compact` snel + zonder netwerk-vereisten runnen, terwijl een aparte CI-step `phpunit.integration.xml` op main/PR runt met de Mollie test-key uit GitHub secrets.

Output: 1 nieuwe PHPUnit-config + 1 base-class + 2 test-classes + 1 modified phpunit.xml (default-exclude integration group) + 1 modified composer.json + 1 modified `.env.example`. GEEN nieuwe controllers, geen productie-code-wijzigingen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-05-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-06-PLAN.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@phpunit.xml
@composer.json
@.env.example
@tests/TestCase.php
@tests/Feature/Api/V1/Billing/AdminSubscriptionCreateTest.php

<interfaces>
<!-- Locked decisions uit 06-CONTEXT.md voor dit plan: -->
<!-- D-12: integration-tests met @group integration, gescheiden van standaard suite -->
<!-- D-13: bij path (c) min 80% coverage — niet relevant nu we pad (a) hebben -->

Bestaande `phpunit.xml` (regel 7-13):
```xml
<testsuites>
    <testsuite name="Unit">
        <directory>tests/Unit</directory>
    </testsuite>
    <testsuite name="Feature">
        <directory>tests/Feature</directory>
    </testsuite>
</testsuites>
```

PHPUnit 12 `@group integration`-exclusion via `<groups>`-config:
```xml
<groups>
    <exclude>
        <group>integration</group>
    </exclude>
</groups>
```

Dit zorgt dat `php artisan test --compact` (die `phpunit.xml` leest) ALLEEN tests buiten `integration`-group runt.

`phpunit.integration.xml` (nieuw, naast `phpunit.xml`):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<phpunit ... bootstrap="vendor/autoload.php" colors="true">
    <testsuites>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include><directory>app</directory></include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <!-- ECHTE Mollie-key, geen :memory: stub -->
        <!-- CASHIER_MOLLIE_KEY + MOLLIE_KEY worden uit .env gelezen -->
    </php>
</phpunit>
```

LET OP: integration-tests blijven op SQLite `:memory:` qua DB — onze DB is geen integration-target. Het integratie-deel zit in de Mollie-API-calls die WEL echt over het netwerk gaan.

PHPUnit 12 attribute-syntax voor groups (vervangt docblock `@group`-annotation):
```php
use PHPUnit\Framework\Attributes\Group;

#[Group('integration')]
class CashierMollieSubscriptionFlowTest extends IntegrationTestCase
{
    // ...
}
```

OF op base-class niveau:
```php
#[Group('integration')]
abstract class IntegrationTestCase extends TestCase
{
    protected function setUp(): void
    {
        parent::setUp();
        if (empty(env('CASHIER_MOLLIE_KEY'))) {
            $this->markTestSkipped('CASHIER_MOLLIE_KEY not set; integration tests require Mollie test-mode key.');
        }
    }
}
```

Subclasses erven het `#[Group('integration')]` attribute via PHPUnit 12 attribute-inheritance? **Verifieer** — als niet auto-inherited: voeg `#[Group('integration')]` op elke concrete subclass toe.

Bestaande `composer.json` scripts (regel 45-60):
```json
"scripts": {
    "test": [
        "@php artisan config:clear --ansi @no_additional_args",
        "@php artisan test"
    ],
    ...
}
```

Toevoegen:
```json
"test:integration": [
    "@php artisan config:clear --ansi",
    "vendor/bin/phpunit --configuration=phpunit.integration.xml"
],
```

Mollie test-mode subscription-create-flow (uit `.docs/partners/mollie/` references gehighlight in 06-CONTEXT canonical_refs):
1. `$consumer->newSubscription('main', 'naschool-license')->create()` (in admin-controller uit 06-05) — als Consumer GEEN Mandate heeft, retourneert Cashier een `RedirectResponse` of equivalent dat naar Mollie's checkout-pagina leidt voor mandate-bevestiging.
2. Voor integration-test: we kunnen NIET een browser-flow doorlopen — Mollie test-mode heeft geen automated mandate-confirm-pad zonder UI-interactie.
3. **Praktische oplossing voor integration-test:** maak de mandate EXPLICIET aan via Mollie test-mode `customers.mandates.create`-call met test-payment-method (`directdebit` test-mandate die instant `valid` wordt). Daarna `newSubscription()->create()` op die customer → directe Subscription-create zonder first_payment-redirect.

Mollie test-mode mandate-create-flow:
```php
$mollieClient = new \Mollie\Api\MollieApiClient();
$mollieClient->setApiKey(env('CASHIER_MOLLIE_KEY'));
$mollieCustomer = $mollieClient->customers->create(['name' => 'Test Consumer', 'email' => 'test@example.test']);
$mollieMandate = $mollieClient->mandates->createForId($mollieCustomer->id, [
    'method' => 'directdebit',
    'consumerName' => 'Test Consumer',
    'consumerAccount' => 'NL55INGB0000000000',  // Mollie test IBAN
]);
// Direct daarna is mollieMandate->status == 'valid' in test-mode
```

Daarna in onze app:
```php
$consumer = Consumer::factory()->create();
DB::table('cashier_customers')->insert([  // tabel-naam verifieer in plan 06-02 schema
    'owner_id' => $consumer->id,
    'owner_type' => Consumer::class,
    'mollie_customer_id' => $mollieCustomer->id,
    'mollie_mandate_id' => $mollieMandate->id,
    'created_at' => now(),
    'updated_at' => now(),
]);
// Nu zou $consumer->newSubscription(...) direct werken
```

LET OP: de exacte tabel + kolomnamen voor mandate-bookkeeping verschillen per Cashier-Mollie-versie. In v2.x worden customer-IDs op het Billable-model zelf gestored via `mollie_customer_id`-kolom — die kolom moet je via een Cashier-published-migration of via een eigen migration toevoegen. **Verifieer in execution-time** door post-06-02-migrations te checken: `sqlite3 ... '.schema'` toont of `subscriptions` of een andere tabel een `mollie_customer_id`-veld heeft, of dat Consumer er een aparte tabel voor heeft.

Test-skeleton voor cancel-flow (eenvoudiger; geen mandate-setup nodig — gebruik factory `withActiveSubscription`):
```php
$consumer = Consumer::factory()->withActiveSubscription('naschool-license', 'main')->create();
// Maak op Mollie test-mode een echte subscription aan met diezelfde mollie_subscription_id?
// → Nee, dat kan niet sync. Cancel-test moet RUNNED tegen een verse subscription die we EERST via API maken.
```

→ Cancel-test moet sequential van create-test: eerst create, dan cancel met de zojuist gecreate subscription-id.

**Pragmatisch advies voor uitvoerder:** maak ÉÉN integration-test die de full create→cancel happy-path doorloopt sequentieel binnen één test-method (geen aparte test-methods). Houd het dunner.

Voor `CashierWebhookEndToEndTest`:
- Mollie test-mode dashboard kan webhook-triggers simuleren ("Test webhooks" knop op de subscription-detail-page) → vereist Mollie-account-interactie + correcte `webhook_url` op de subscription.
- Alternatief: simuleer een Mollie-style webhook-POST handmatig met een geldige payment-id die we eerder hebben aangemaakt. Cashier's WebhookController doet een Mollie-API-call om de payment-status op te halen → dat werkt tegen test-mode key.
- **Pragmatisch:** test 1 = simulate webhook met self-created payment-id, assert dat `webhook_calls` audit-row geschreven (door onze guard-middleware) + subscription-status (in DB) is geüpdatet door Cashier's handler.

`.env.example`-aanvulling (NA bestaande Cashier-block uit 06-02 + billing-block uit 06-05):
```env

# Integration tests — `composer test:integration` of `vendor/bin/phpunit --configuration=phpunit.integration.xml`
# vereist een geldige Mollie test-mode key in .env (NIET .env.example). Tests skippen bij
# empty CASHIER_MOLLIE_KEY. CI runt deze suite apart in een dedicated workflow-step.
# Zie tests/Integration/IntegrationTestCase.php voor de skip-logica.
```

LET OP: het test-skeleton hieronder is een conservatieve start. Mollie's test-mode kan rate-limits hebben + netwerk-tijden zorgen voor flaky tests; daarom max 2-3 integration-tests in v0.2 met expliciet `@group integration` zodat ze niet in de hot-path zitten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phpunit.integration.xml + phpunit.xml exclude-group + composer-script + .env.example update</name>
  <read_first>
    - `phpunit.xml` (huidige config — Unit + Feature suites, env-block)
    - `composer.json` (scripts-section)
    - `.env.example` (huidige structuur)
    - `.ai/rules/global.md` (geen heredoc voor file-creates; gebruik Write)
  </read_first>
  <files>
    phpunit.xml
    phpunit.integration.xml
    composer.json
    .env.example
  </files>
  <action>
    **Stap A — phpunit.xml: voeg group-exclude toe:**

    Edit `phpunit.xml` om `<groups>`-block toe te voegen TUSSEN `</testsuites>` en `<source>`:
    ```xml
    <groups>
        <exclude>
            <group>integration</group>
        </exclude>
    </groups>
    ```

    Resulterend:
    ```xml
    <phpunit ...>
        <testsuites>
            <testsuite name="Unit">
                <directory>tests/Unit</directory>
            </testsuite>
            <testsuite name="Feature">
                <directory>tests/Feature</directory>
            </testsuite>
        </testsuites>
        <groups>
            <exclude>
                <group>integration</group>
            </exclude>
        </groups>
        <source>
            <include><directory>app</directory></include>
        </source>
        ...
    </phpunit>
    ```

    **Stap B — Maak `phpunit.integration.xml`:**

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
             bootstrap="vendor/autoload.php"
             colors="true"
    >
        <testsuites>
            <testsuite name="Integration">
                <directory>tests/Integration</directory>
            </testsuite>
        </testsuites>
        <groups>
            <include>
                <group>integration</group>
            </include>
        </groups>
        <source>
            <include>
                <directory>app</directory>
            </include>
        </source>
        <php>
            <env name="APP_ENV" value="testing"/>
            <env name="APP_MAINTENANCE_DRIVER" value="file"/>
            <env name="BCRYPT_ROUNDS" value="4"/>
            <env name="BROADCAST_CONNECTION" value="null"/>
            <env name="CACHE_STORE" value="array"/>
            <env name="DB_CONNECTION" value="sqlite"/>
            <env name="DB_DATABASE" value=":memory:"/>
            <env name="MAIL_MAILER" value="array"/>
            <env name="QUEUE_CONNECTION" value="sync"/>
            <env name="SESSION_DRIVER" value="array"/>
            <env name="NIGHTWATCH_ENABLED" value="false"/>
            <!-- CASHIER_MOLLIE_KEY + MOLLIE_KEY worden uit .env gelezen -->
            <!-- (NIET hier overschreven met "" — anders skipt iedere test) -->
        </php>
    </phpunit>
    ```

    **Stap C — composer.json script:**

    Edit `composer.json` `scripts`-object. Voeg NA `"test"` array toe:
    ```json
    "test:integration": [
        "@php artisan config:clear --ansi @no_additional_args",
        "vendor/bin/phpunit --configuration=phpunit.integration.xml"
    ],
    ```

    LET OP composer's strict JSON-syntax — komma's correct plaatsen.

    **Stap D — .env.example aanvulling:**

    Voeg na de billing-block (uit 06-05) toe:
    ```env

    # Integration tests
    # `composer test:integration` of `vendor/bin/phpunit --configuration=phpunit.integration.xml`
    # vereist een geldige Mollie test-mode key in .env (NIET .env.example).
    # Tests skippen bij empty CASHIER_MOLLIE_KEY. CI runt deze suite apart in een
    # dedicated workflow-step die de key uit GitHub secrets injecteert.
    ```

    **Stap E — Verifieer standaard suite blijft groen + integration-suite vindbaar:**

    ```bash
    # Standaard suite — moet 0 integration tests includen
    php artisan test --compact 2>&1 | tee /tmp/post-config-suite.log

    # Integration-suite probeert (dir bestaat nog niet → graceful 0 tests gerund)
    vendor/bin/phpunit --configuration=phpunit.integration.xml 2>&1 | tail -20 | tee /tmp/integration-empty.log
    ```

    `vendor/bin/phpunit --configuration=phpunit.integration.xml` zal "0 tests" loggen — dat is OK want we hebben de directory nog niet aangemaakt; Task 2 doet dat.

    **Stap F — Commit:**

    ```bash
    git add phpunit.xml phpunit.integration.xml composer.json .env.example
    git commit -m "$(cat <<'EOF'
feat(06-07): scheid integration-tests via phpunit.integration.xml + @group integration

D-12: standaard `php artisan test` excludeert `integration`-group; aparte
`composer test:integration` run't via phpunit.integration.xml die EXCLUSIEF
integration-group runt. tests/Integration/ directory komt in Task 2.

phpunit.xml krijgt <groups><exclude><group>integration</group></exclude></groups>
zodat hot-path-suite snel + zonder netwerk-vereisten blijft. CI runt
integration apart op main/PR met Mollie-test-key uit secrets.

`.env.example` documenteert de skip-on-missing-CASHIER_MOLLIE_KEY-conventie.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
    ```
  </action>
  <verify>
    <automated>test -f phpunit.integration.xml && grep -q "Integration" phpunit.integration.xml && grep -q "exclude" phpunit.xml && grep -q "<group>integration</group>" phpunit.xml && grep -q "test:integration" composer.json && grep -q "Integration tests" .env.example && php artisan test --compact 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed"</automated>
  </verify>
  <acceptance_criteria>
    - `phpunit.integration.xml` bestaat met `<testsuite name="Integration">` wijzend naar `tests/Integration`.
    - `phpunit.integration.xml` heeft `<groups><include><group>integration</group></include></groups>`.
    - `phpunit.xml` heeft `<groups><exclude><group>integration</group></exclude></groups>`.
    - `composer.json` heeft een `test:integration`-script.
    - `.env.example` heeft een `# Integration tests`-comment-block met instructies.
    - `php artisan test --compact` blijft groen op de pre-execution baseline.
    - Eén `feat(06-07)`-commit.
  </acceptance_criteria>
  <done>
    Integration-suite configuratie staat; default-exclude is actief; composer-script runbaar. Tests volgen in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: IntegrationTestCase + CashierMollieSubscriptionFlowTest + CashierWebhookEndToEndTest</name>
  <read_first>
    - `tests/TestCase.php` (basisklasse die we extenden)
    - `tests/Feature/Api/V1/Billing/AdminSubscriptionCreateTest.php` (uit 06-05 — soft-assertion pattern dat we hier vervangen door echte happy-path)
    - `tests/Concerns/StubsMollieClient.php` regel 676-684 (`setupMollieConsumer`-helper; NIET hergebruiken in integration — we maken Mollie-customer echt aan, niet via stub)
    - `app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php` (uit 06-05 — wat we testen)
    - `vendor/mollie/laravel-cashier-mollie/src/Billable.php` (na 06-02; om te zien hoe mollie_customer_id/mollie_mandate_id worden gepersisteerd op Cashier-side)
    - `database/migrations/*_create_subscriptions_table.php` (na 06-02 publish — schema voor cancel-test fixtures)
  </read_first>
  <files>
    tests/Integration/IntegrationTestCase.php
    tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php
    tests/Integration/Billing/CashierWebhookEndToEndTest.php
  </files>
  <action>
    **Stap A — IntegrationTestCase:**

    Maak dir `tests/Integration/` + `tests/Integration/Billing/`.

    Maak `tests/Integration/IntegrationTestCase.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Integration;

    use Illuminate\Foundation\Testing\RefreshDatabase;
    use PHPUnit\Framework\Attributes\Group;
    use Tests\TestCase;

    /**
     * D-12: alle integration-tests die echte Mollie test-mode API hitten
     * erven van deze base-class. Skipt automatisch als de Mollie-key niet
     * gezet is — voorkomt CI-failures op feature-branches zonder secrets.
     */
    #[Group('integration')]
    abstract class IntegrationTestCase extends TestCase
    {
        use RefreshDatabase;

        protected function setUp(): void
        {
            parent::setUp();

            $key = env('CASHIER_MOLLIE_KEY') ?: env('MOLLIE_KEY');
            if (! is_string($key) || $key === '' || ! str_starts_with($key, 'test_')) {
                $this->markTestSkipped(
                    'Integration tests require CASHIER_MOLLIE_KEY (test_-prefix) in .env. '
                    .'Run `composer test:integration` apart, niet als onderdeel van `php artisan test`.'
                );
            }

            config([
                'cashier-mollie.key' => $key,
                'mollie.key' => $key,
                'services.cashier.webhook_secret' => env('CASHIER_WEBHOOK_SECRET', 'whsec_integration_test'),
            ]);
        }
    }
    ```

    **Stap B — CashierMollieSubscriptionFlowTest:**

    Maak `tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Integration\Billing;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Mollie\Api\MollieApiClient;
    use Tests\Integration\IntegrationTestCase;

    class CashierMollieSubscriptionFlowTest extends IntegrationTestCase
    {
        protected function setUp(): void
        {
            parent::setUp();

            config(['billing-plans' => [
                'naschool-license' => [
                    'amount' => ['value' => '49.00', 'currency' => 'EUR'],
                    'interval' => '1 month',
                    'description' => 'Naschool license — integration test',
                ],
            ]]);
        }

        public function test_admin_can_create_subscription_with_first_payment_redirect_url(): void
        {
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $target = Consumer::factory()->create();
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => $target->id,
                    'plan_slug' => 'naschool-license',
                ]);

            // Target heeft GEEN mandate → Cashier triggert first_payment-flow.
            // Response: 202 + mandate_redirect_url naar Mollie test-mode checkout.
            $response->assertStatus(202);
            $response->assertJsonStructure(['first_payment_required', 'mandate_redirect_url']);
            $this->assertStringStartsWith('https://www.mollie.com/', $response->json('mandate_redirect_url'));
        }

        public function test_admin_can_cancel_existing_subscription_via_api(): void
        {
            // Stap 1: maak EERST een echte Mollie test-mode customer + valid mandate
            // (test-IBAN levert direct geldige directdebit-mandate in test-mode).
            $mollieClient = new MollieApiClient();
            $mollieClient->setApiKey(env('CASHIER_MOLLIE_KEY'));

            $mollieCustomer = $mollieClient->customers->create([
                'name' => 'Integration Test Consumer',
                'email' => 'integration+'.now()->timestamp.'@emeq.test',
            ]);

            $mollieMandate = $mollieClient->mandates->createForId($mollieCustomer->id, [
                'method' => 'directdebit',
                'consumerName' => 'Test Account Holder',
                'consumerAccount' => 'NL55INGB0000000000',
            ]);

            $this->assertSame('valid', $mollieMandate->status, 'Mollie test-mode mandate must be valid for cancel-test.');

            // Stap 2: maak een echte Mollie subscription op die customer.
            $mollieSubscription = $mollieClient->subscriptions->createForId($mollieCustomer->id, [
                'amount' => ['value' => '49.00', 'currency' => 'EUR'],
                'interval' => '1 month',
                'description' => 'Cancel test',
                'mandateId' => $mollieMandate->id,
            ]);

            // Stap 3: persist de Cashier-side state.
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $target = Consumer::factory()->create([
                // Cashier ^2.x heeft mollie_customer_id en mollie_mandate_id kolommen
                // op het Billable-model OF op een separate cashier_customers-tabel —
                // verifieer in execution welk pad geldt. Pas hier aan als nodig.
            ]);

            $subscriptionId = \Illuminate\Support\Facades\DB::table('subscriptions')->insertGetId([
                'name' => 'main',
                'plan' => 'naschool-license',
                'owner_id' => $target->id,
                'owner_type' => Consumer::class,
                'mollie_subscription_id' => $mollieSubscription->id,
                'mollie_mandate_id' => $mollieMandate->id,
                'status' => 'active',
                'tax_percentage' => 21,
                'quantity' => 1,
                'created_at' => now(),
                'updated_at' => now(),
            ]);

            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            // Stap 4: cancel via API.
            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->deleteJson("/v1/admin/billing/subscriptions/{$subscriptionId}");

            $response->assertStatus(204);

            // Stap 5: assert Mollie-side state.
            $refreshed = $mollieClient->subscriptions->getForId($mollieCustomer->id, $mollieSubscription->id);
            $this->assertSame('canceled', $refreshed->status);
        }
    }
    ```

    **Stap C — CashierWebhookEndToEndTest:**

    Maak `tests/Integration/Billing/CashierWebhookEndToEndTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Integration\Billing;

    use App\Models\Consumer;
    use Illuminate\Support\Facades\DB;
    use Mollie\Api\MollieApiClient;
    use Spatie\WebhookClient\Models\WebhookCall;
    use Tests\Integration\IntegrationTestCase;

    class CashierWebhookEndToEndTest extends IntegrationTestCase
    {
        public function test_webhook_with_valid_secret_triggers_cashier_handler(): void
        {
            // Mollie test-mode: maak een payment aan zodat we een geldige id hebben
            // die Cashier's webhook-handler kan resolven.
            $mollieClient = new MollieApiClient();
            $mollieClient->setApiKey(env('CASHIER_MOLLIE_KEY'));

            $payment = $mollieClient->payments->create([
                'amount' => ['value' => '0.01', 'currency' => 'EUR'],
                'description' => 'Webhook end-to-end test',
                'redirectUrl' => 'https://example.test/done',
            ]);

            $this->assertNotEmpty($payment->id);

            // Simuleer Mollie's webhook-POST naar onze endpoint.
            $secret = env('CASHIER_WEBHOOK_SECRET', 'whsec_integration_test');
            config(['services.cashier.webhook_secret' => $secret]);

            $response = $this->post('/cashier/webhook', ['id' => $payment->id], [
                'Content-Type' => 'application/x-www-form-urlencoded',
            ]);

            // Cashier's WebhookController fetcht de payment via Mollie API,
            // valideert, en retourneert 200 voor known + 404/422 voor unknown subscription.
            // Voor onze geïsoleerde payment (geen subscription) verwachten we 200.
            $this->assertContains($response->status(), [200, 202], sprintf(
                'Cashier webhook handler retourneerde onverwachte status %d: %s',
                $response->status(),
                $response->content(),
            ));

            // De stap-0 guard heeft GEEN audit-row geschreven (secret was set).
            $failedRows = WebhookCall::query()
                ->where('name', 'cashier')
                ->whereNotNull('exception')
                ->count();
            $this->assertSame(0, $failedRows);
        }

        public function test_webhook_without_secret_returns_500_in_integration_env(): void
        {
            config(['services.cashier.webhook_secret' => '']);

            $response = $this->post('/cashier/webhook', ['id' => 'tr_test_anything']);

            $response->assertStatus(500);
            $this->assertDatabaseHas('webhook_calls', [
                'name' => 'cashier',
                'exception' => 'webhook_secret_not_configured',
            ]);
        }
    }
    ```

    LET OP: deze integration-tests SLAGEN ALLEEN als `.env` een geldige `CASHIER_MOLLIE_KEY` (`test_*`-prefix) heeft. Op een CI zonder secrets: alle tests skippen via base-class. Lokaal zonder key: zelfde.

    **Stap D — Run integration-suite (lokaal, alleen als key gezet):**

    ```bash
    composer test:integration 2>&1 | tee /tmp/integration-run.log
    ```

    Verwachte uitkomst zonder key in `.env`: 3 tests SKIPPED.
    Verwachte uitkomst MET key in `.env`: 3 tests PASSED.

    Standaard suite blijft groen:
    ```bash
    php artisan test --compact 2>&1 | tee /tmp/post-int-standard.log
    ```

    **Stap E — Commit:**

    ```bash
    git add tests/Integration/
    git commit -m "$(cat <<'EOF'
feat(06-07): voeg Cashier-Mollie integration-tests toe (skippen zonder CASHIER_MOLLIE_KEY)

Drie integration-tests in tests/Integration/Billing/, allemaal #[Group('integration')]
via IntegrationTestCase-base-class die markTestSkipped doet bij empty
CASHIER_MOLLIE_KEY:
- CashierMollieSubscriptionFlowTest::test_admin_can_create_subscription_with_first_payment_redirect_url
- CashierMollieSubscriptionFlowTest::test_admin_can_cancel_existing_subscription_via_api
- CashierWebhookEndToEndTest::test_webhook_with_valid_secret_triggers_cashier_handler
- CashierWebhookEndToEndTest::test_webhook_without_secret_returns_500_in_integration_env

D-18 SC-3 (admin POST → echte Mollie test-mode subscription) bewezen door
test #1+#2. Standaard `php artisan test --compact` skipt deze tests (Task 1
zorgde voor group-exclude). `composer test:integration` runt ze apart;
faalt-graceful op missing key.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
    ./vendor/bin/pint --dirty --format agent
    ```
  </action>
  <verify>
    <automated>test -f tests/Integration/IntegrationTestCase.php && test -f tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php && test -f tests/Integration/Billing/CashierWebhookEndToEndTest.php && grep -q "markTestSkipped" tests/Integration/IntegrationTestCase.php && grep -q "Group..integration" tests/Integration/IntegrationTestCase.php && grep -cE "public function test_" tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php | grep -qE "^[2-9]$" && php artisan test --compact 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Integration/IntegrationTestCase.php` bestaat, extends `Tests\TestCase`, heeft `#[Group('integration')]`-attribute, doet `markTestSkipped` op missing of non-test-key.
    - `tests/Integration/Billing/CashierMollieSubscriptionFlowTest.php` heeft ≥2 tests.
    - `tests/Integration/Billing/CashierWebhookEndToEndTest.php` heeft ≥1 test.
    - Standaard `php artisan test --compact` skipt alle integration-tests (geen Mollie-netwerk-calls in hot-path).
    - `composer test:integration` lokaal runt en skipt-of-passes afhankelijk van `.env`-key.
    - Eén nieuwe `feat(06-07)`-commit.
  </acceptance_criteria>
  <done>
    Integration-test-laag staat; 3+ tests bewijzen full happy-path tegen Mollie test-mode; skip-graceful bij missing key; hot-path blijft snel + offline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-runtime → Mollie test-mode API | Integration-tests sturen echte HTTPS-calls naar `api.mollie.com` met een test-key (`test_*`-prefix). Test-mode is gesegregeerd van productie. |
| `.env` (lokaal) ↔ CI-secrets | `CASHIER_MOLLIE_KEY` mag NOOIT in `.env.example` of in commit-tree komen — alleen in `.env` (gitignored) of GitHub-secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information disclosure | Mollie-test-key in `.env` of CI-logs | mitigate | `.env.example` heeft ALLEEN `test_xxx`-placeholder; echte key uitsluitend in `.env` (gitignored) + GitHub-secrets. Tests loggen geen key-waarde (gebruiken `env()`-helper, geen `echo` of `dump`). |
| T-06-07-02 | Tampering | Integration-test creërt resources in Mollie test-mode | accept | Mollie test-mode is isolated van productie; resources zijn dispossable. Geen impact op live data. |
| T-06-07-03 | Denial of service | Test-suite spam't Mollie API | accept | Mollie's test-mode heeft rate-limits; 3 tests per run is ver onder de limiet. Tests run alleen on-demand (`composer test:integration`), niet op iedere commit. |
| T-06-07-04 | Spoofing | Integration-test omzeilt de stap-0 guard | accept | `CashierWebhookEndToEndTest::test_webhook_without_secret_returns_500_in_integration_env` valideert juist dat de guard zijn werk doet, ook in integration-context. |

Plan 06-07 voegt geen productie-code-attack-surface toe — alleen test-scaffolding + 2 integration-suites.
</threat_model>

<verification>
- `phpunit.integration.xml` runs ALLEEN tests met `#[Group('integration')]`-attribute.
- `phpunit.xml` slaat `integration`-group over.
- `composer test:integration` resolved en runt de integration-suite (skip of pass afhankelijk van env).
- 3+ integration-tests bestaan in `tests/Integration/Billing/`.
- Standaard `php artisan test --compact` blijft groen op de baseline (geen integration-tests gerund).
- `IntegrationTestCase` skipt op missing key — voorkomt CI-failure op feature-branches zonder secrets.
</verification>

<success_criteria>
1. **D-12 ingelost:** integration-tests gescheiden van standaard suite via `@group integration` + dedicated phpunit-config.
2. **Default-skip on missing key:** `IntegrationTestCase::setUp()` markTestSkipped → CI-friendly.
3. **Happy-path full flow gedekt:** admin POST → first_payment-redirect-URL → cancel via API → Mollie-side status `canceled`.
4. **Webhook end-to-end:** Mollie-style POST naar `/cashier/webhook` met set secret → Cashier handler accepteert; zonder secret → 500 + audit (regressie-test voor 06-06 guard).
5. **Hot-path snel:** `php artisan test --compact` runt geen netwerk-calls.
6. **CI-runnable:** `composer test:integration` als aparte CI-step.
7. **D-18 SC-3 bewezen:** admin-create + echte Mollie test-mode Subscription resource — integration-test eveneens geldig bewijs zolang de tests passeren met een geldige key.
</success_criteria>

<output>
Na completion: creëer `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-07-SUMMARY.md` met:
- Verbatim `phpunit.integration.xml`-content
- Diff van `phpunit.xml` (groups-exclude block)
- `composer.json` script-toevoeging
- Lijst van 3 integration-tests + hun assertions
- Skip-conditie van `IntegrationTestCase` (key-prefix-check)
- Verwijzing naar plan 06-08 (BLOCKING phase-acceptance)
</output>
