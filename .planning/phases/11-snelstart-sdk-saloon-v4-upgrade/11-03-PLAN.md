---
phase: 11-snelstart-sdk-saloon-v4-upgrade
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - .planning/STATE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - SNEL-03
  - SNEL-04
must_haves:
  truths:
    - "ADR `.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` bestaat lokaal (gitignored werkdocument) en documenteert de Saloon v3 → v4 migratie, 3 advisories en Hub-side composer-update."
    - "`.planning/codebase/STACK.md` regel 150 (audit-ignores) én regel 151 (minimum-stability-motivatie) zijn geüpdatet om de post-Phase-11 realiteit te reflecteren."
    - "`.planning/codebase/CONCERNS.md` regel die het audit-risk noemt is gemarkeerd resolved."
    - "`.planning/STATE.md` Current Position, Resolved Blockers en progress-frontmatter reflecteren Phase 11 closure."
    - "Smoke-test bewijs: Snelstart pass-through + HMAC-webhook-flow blijven groen op v0.2.0 (subset PHPUnit-run)."
    - "Eén Hub-commit raakt exact 3 tracked files (`.planning/STATE.md`, `.planning/codebase/STACK.md`, `.planning/codebase/CONCERNS.md`); de ADR-file leeft lokaal in het gitignored `.docs/`-pad en wordt in de SUMMARY met pad + status gedocumenteerd."
  artifacts:
    - path: ".docs/decisions/snelstart-sdk-saloon-v4-upgrade.md"
      provides: "ADR met Status / Keuze / Context / Consequenties (lokaal-only werkdocument, niet in commit)"
      contains: "Saloon v4"
    - path: ".planning/STATE.md"
      provides: "Frontmatter-progress + Current Position + Resolved Blockers post-Phase-11"
      contains: "completed_phases: 1"
    - path: ".planning/codebase/STACK.md"
      provides: "Audit-sectie zonder PKSA-IDs + bijgewerkte stability-motivatie"
      contains: "Phase 11"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Audit-risk-regel gemarkeerd als resolved"
      contains: "resolved"
  key_links:
    - from: ".planning/codebase/STACK.md regel 150"
      to: "composer.json audit-config"
      via: "regel-update na audit-ignore-removal in Plan 11-02"
      pattern: "audit|PKSA"
    - from: ".planning/codebase/STACK.md regel 151"
      to: "composer.json minimum-stability"
      via: "motivatie verschuift van snelstart-dev-master naar Mollie-alpha (post-pinning)"
      pattern: "minimum-stability|alpha"
    - from: ".planning/STATE.md Resolved Blockers"
      to: ".docs/decisions/snelstart-sdk-saloon-v4-upgrade.md"
      via: "verwijzing naar lokaal ADR-pad voor traceability"
      pattern: "snelstart-sdk-saloon-v4-upgrade"
---

<objective>
Sluit Phase 11 documentair af: schrijf de ADR voor de Saloon v4 upgrade als lokaal werkdocument in het gitignored `.docs/`-pad, sync de tracked `.planning/codebase/`-drift (STACK.md regel 150 + 151 + CONCERNS.md regel 189), update STATE.md, en bewijs met een korte smoke-run dat Snelstart-pass-through + webhook-flow groen blijft op v0.2.0.

Purpose: Dekt success-criterium #4 van Phase 11 (CHANGELOG/ADR-documentatie) op Hub-side via een ADR — de centrale plek voor architecturele beslissingen volgens `CLAUDE.md`. Schoont docs-drift op die de `docs-sync`-skill anders later zou flaggen. De ADR zelf leeft in `.docs/decisions/` — dat hele pad is per `CLAUDE.md` "Werkdocumentatie (lokaal, gitignored)" en gaat niet mee in de commit.
Output: Lokale ADR + 3 tracked file-edits (.planning/STATE.md, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md), gecommit in één Hub-commit. SUMMARY documenteert lokaal-aanwezigheid van de ADR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-01-SUMMARY.md
@.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-02-SUMMARY.md
@.docs/decisions/snelstart-webhook-ingress.md
@.docs/decisions/sdk-redistributability-boundary.md
@.docs/README.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@.ai/rules/engineering.md
@.claude/skills/docs-sync/SKILL.md

ADR-template-stijl (gebaseerd op `snelstart-webhook-ingress.md` en `sdk-redistributability-boundary.md`):
  - Titel
  - Status — Geaccepteerd YYYY-MM-DD plus korte achtergrond
  - Keuze — bullet-list van concrete beslissingen
  - Context — drijfveren
  - Consequenties — wat verandert plus wat moet je in de toekomst onthouden
  - Bronnen — optionele referenties naar plans, commits, partner-docs

Gitignore-realiteit: `/.docs` staat in `.gitignore` (regel 35). Dat geldt voor de hele `.docs/`-tree inclusief `decisions/`. `.docs/README.md` heeft daarnaast geen per-ADR Decisions-index — alleen een folder-taxonomy. Conclusie: de ADR is een **lokaal werkdocument**, niet committeerbaar via deze workflow. Traceability gebeurt via (a) de SUMMARY van Plan 11-03 die het ADR-pad noemt, en (b) een verwijzing in STATE.md Resolved Blockers.

Drift-targets in `.planning/codebase/`:
  - STACK.md regel 150 vermeldt de 3 ignored advisories letterlijk → vervangen.
  - STACK.md regel 151 motiveert `minimum-stability: dev` met "om emeq/snelstart-api: dev-master toe te staan" → motivatie verschuift naar Mollie-SDK-alpha na pinning. Verifier-pad: kijk naar andere alpha/dev deps in `composer.json` om de nieuwe motivatie te bevestigen.
  - CONCERNS.md regel 189 flagt het audit-risk → markeer resolved.

STATE.md secties die update vereisen:
  - YAML-frontmatter `progress` (completed_phases, total_plans, completed_plans, percent)
  - YAML-frontmatter `last_updated` (volledige ISO-datetime, quoted) en `last_activity` (kale date)
  - Current Position-sectie (Phase 11 status + Next action options)
  - Resolved Blockers-sectie (add Phase 11 closure)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schrijf ADR snelstart-sdk-saloon-v4-upgrade.md als lokaal werkdocument</name>
  <files>.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md</files>
  <read_first>
    - .docs/decisions/snelstart-webhook-ingress.md (template-stijl van een recente Snelstart-ADR)
    - .docs/decisions/sdk-redistributability-boundary.md (template-stijl voor SDK-boundary ADRs)
    - .planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-01-SUMMARY.md (SDK commit-SHA + tag)
    - .planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-02-SUMMARY.md (Hub commit-SHA + audit-output + test-aantallen)
  </read_first>
  <action>
    Schrijf `.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` met de template-stijl uit `snelstart-webhook-ingress.md`. Deze file leeft in het gitignored `.docs/`-pad (zie `.gitignore` regel 35: `/.docs`) en gaat **niet** mee in de commit van Task 3 — dat is een bewuste keuze conform `CLAUDE.md` "Werkdocumentatie (lokaal, gitignored)". Inhoud:

    Titel: `# Snelstart-SDK Saloon v4 upgrade — risk-closure`

    Status-sectie: geaccepteerd 2026-05-18, Phase 11 (v0.3) gesloten. SDK getagd `v0.2.0`. Hub `composer.json` pint `^0.2.0`, geen audit-ignores.

    Keuze-sectie — bullet-list met deze concrete beslissingen:
      - SDK-package `emeq/snelstart-api` upgrade Saloon v3 naar v4 (`saloonphp/saloon: ^4.0`, `saloonphp/laravel-plugin: ^4.0`).
      - Hub pint op `^0.2.0` (semver-stable) in plaats van `dev-master` zodat een toekomstige master-push niet stilletjes wordt opgepakt.
      - Drie audit-ignores in Hub `composer.json` verwijderd: `PKSA-xnj5-w74d-6wmz` (HIGH, insecure deserialization), `PKSA-5szq-gvrg-ttfq` (MEDIUM, SSRF via absolute URL endpoint-override), `PKSA-rnpm-45mg-w6ht` (MEDIUM, Fixture Name Path Traversal). `composer audit` retourneert exit 0 zonder ignores.
      - Geen API-contract-change: `SnelstartConnector`, `AuthConnector` en `ClientKeyAuthenticator` gebruiken al `resolveBaseUrl()` (v4-pattern). MockClient-fixtures werken ongewijzigd.

    Context-sectie — uitleg waarom:
      - Saloon v3 had drie gedocumenteerde advisories waarvan één HIGH (deserialisatie-RCE-impact in `AccessTokenAuthenticator`). Hub-`composer.json` ignoreerde ze sinds v0.1 zonder rationale in-code (geflagged in `.planning/codebase/CONCERNS.md` regel 189).
      - v0.3 milestone-doel is risk-reductie vóór Naschool live E2E (`NSCH-LIVE-E2E`, Phase 14) en Snelstart productie-certificering (Phase 12). Een gecertificeerde Hub kan niet op een SDK draaien met een open HIGH-deserialisatie-advisory.
      - Saloon v4.0.0 lost alle drie defecten op. Bron: `https://packagist.org/packages/saloonphp/saloon/advisories`.
      - De SDK-werkbench was al geüpgraded in commit `e9aff1a chore(deps): bump Saloon v3 → v4 (incl. laravel-plugin v4.3)` op 2026-05-17. Plan 11-01 sloot dit af met een formele tag (`v0.2.0`) plus CHANGELOG.

    Consequenties-sectie — wat verandert plus wat onthouden:
      - SDK Pest-suite: minimaal 107 passed (huidige run: 122 / 211 assertions). MockClient-fixtures blijven werken in v4.
      - Hub PHPUnit Snelstart-subset: 45 tests / 207 assertions blijven groen (exacte aantallen in 11-02-SUMMARY).
      - Snelstart-pass-through `/v1/snelstart/odata/*` en webhook-ingress `/webhooks/snelstart` ongewijzigd qua public-API gedrag.
      - Toekomstige SDKs die Saloon gebruiken (geplande `emeq/moneybird-api`, `emeq/exact-api`, `emeq/ibanity-api` in v0.4+) volgen direct v4-pattern. `emeq/mollie-api` gebruikt geen Saloon — pad-A-keuze in v0.2 Phase 2.
      - Aanbeveling voor v0.4+: signed-tags op SDK-releases (`git tag -s`). Niet in v0.3 omdat single-maintainer + private dev-loop het risk LOW houdt (zie T-11-05 in Plan 11-02).

    Bronnen-sectie:
      - SDK upgrade-commit: `e9aff1a` (zie `packages/snelstart-api/CHANGELOG.md` v0.2.0-sectie).
      - SDK release-tag-commit-SHA: vul in uit `11-01-SUMMARY.md`.
      - Hub composer-update-commit-SHA: vul in uit `11-02-SUMMARY.md`.
      - Advisories: `https://packagist.org/packages/saloonphp/saloon/advisories`.
      - Plan-files: `.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-01-PLAN.md`, `11-02-PLAN.md`, `11-03-PLAN.md`.

    Stijl-eisen: Nederlands. Geen anti-AI-cliché's (zie `.ai/rules/global.md` — geen "seamless", "naadloos", "transform", "innovative"). Compacte bullets. Geen marketing-taal. Geen fenced code blocks voor identifier-lijsten — backtick-spans volstaan.
  </action>
  <verify>
    <automated>test -f .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q 'Saloon v4' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q 'PKSA-xnj5-w74d-6wmz' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q 'PKSA-5szq-gvrg-ttfq' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q 'PKSA-rnpm-45mg-w6ht' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q '## Status' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q '## Keuze' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && grep -q '## Consequenties' .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md && git check-ignore -q .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md</automated>
  </verify>
  <acceptance_criteria>
    - File bestaat op exact pad `.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md`.
    - Heeft secties Status, Keuze, Context, Consequenties (en optioneel Bronnen).
    - Bevat alle 3 PKSA-ID's letterlijk.
    - Bevat verwijzing naar upgrade-commit `e9aff1a` en Hub composer-update-commit-SHA uit 11-02-SUMMARY.
    - Nederlandse prose, geen anti-AI-cliché's.
    - `git check-ignore` bevestigt dat de file door `.gitignore` (regel `/.docs`) wordt genegeerd — bewuste lokaal-only status.
  </acceptance_criteria>
  <done>ADR bestaat lokaal en dekt context plus keuze plus consequenties van de Saloon-v4-upgrade; gitignore-status bevestigd.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sync .planning/codebase/-drift plus STATE.md (3 tracked files)</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/codebase/STACK.md (regel 150 vermeldt de 3 ignored advisories; regel 151 motiveert minimum-stability met snelstart-dev-master)
    - .planning/codebase/CONCERNS.md (regel 189 flagt het audit-risk)
    - .planning/STATE.md (frontmatter `progress` plus `last_updated` plus `last_activity` plus Current Position plus Resolved Blockers)
    - composer.json (verifier-pad voor STACK.md regel 151: bevestig welke andere dev/alpha-deps `minimum-stability: dev` nodig maken — verwacht `emeq/mollie-api` of vergelijkbare alpha-constraint)
  </read_first>
  <action>
    Drie chirurgische doc-syncs in één commit. Geen `.docs/README.md`-edit (heel `.docs/` is gitignored en de README heeft geen per-ADR Decisions-index — niet vereist en niet committeerbaar).

    1. `.planning/codebase/STACK.md` regel 150 — vervang de regel die luidt `composer.json audit: ignore: ["PKSA-xnj5-w74d-6wmz", "PKSA-5szq-gvrg-ttfq", "PKSA-rnpm-45mg-w6ht"] (geïgnoreerde abandoned-advisories).` door `composer.json audit: alleen "abandoned": "report" (drie Saloon v3-advisories opgelost in Phase 11 — Saloon ^4.0 in SDK).`. Behoud omliggende context-regels exact.

    2. `.planning/codebase/STACK.md` regel 151 — vervang de regel die luidt `minimum-stability: dev + prefer-stable: true om emeq/snelstart-api: dev-master toe te staan zonder volledige dev-resolutie.` door `minimum-stability: dev + prefer-stable: true om emeq/mollie-api: ^0.1.0-alpha.1 (en vergelijkbare alpha/dev-deps) toe te staan zonder volledige dev-resolutie. emeq/snelstart-api staat sinds Phase 11 op ^0.2.0 (stable tag).` Verifier-pad: open `composer.json`, controleer welke dependency een alpha-constraint heeft. Als de werkelijke motivatie afwijkt (bijvoorbeeld een andere alpha-package), pas de regel aan om die accuraat te beschrijven en log de afwijking in 11-03-SUMMARY.

    3. `.planning/codebase/CONCERNS.md` regel 189 — markeer het audit-risk als resolved. Lees omliggende regels 185-195 om de conventie te bepalen (doorgehaalde tekst plus "Resolved YYYY-MM-DD" of move naar een Resolved-sectie). Volg die conventie. Bewaar de PKSA-ID's in de resolved-notatie zodat traceability blijft.

    4. `.planning/STATE.md` — drie sub-edits:
      - YAML-frontmatter: `progress.completed_phases` 0 naar 1; `progress.total_plans` 0 naar 3; `progress.completed_plans` 0 naar 3; `progress.percent` 0 naar 20 (1 van 5 phases). Vul `last_updated` met een verse ISO-timestamp in het exacte bestaande format `"2026-05-18T<HH>:<MM>:<SS>.000Z"` (behoud quotes en volledige ISO-datetime — verifier-pad: kopieer de exacte format-structuur van de huidige waarde `"2026-05-18T07:14:19.717Z"` en vervang alleen tijd-component). Vul `last_activity` met een kale date `2026-05-18` (geen quotes, geen tijd-component — match het bestaande format op regel 7).
      - Current Position-sectie regels 28-31: "Phase: 11 — Snelstart-SDK Saloon v4 upgrade (not started)" wordt "Phase: 11 — Snelstart-SDK Saloon v4 upgrade (Complete 2026-05-18, 3/3 plans)". "Status" en "Last activity" navenant. Next action options: verwijder de Phase 11-optie, behoud Phase 12 plus 13 plus 15.
      - Resolved Blockers-sectie: voeg regel toe na "Phase 5b verification-debt closed" met inhoud: `- ~~Saloon v3 → v4 upgrade + 3 advisories (PKSA-xnj5-w74d-6wmz HIGH, PKSA-5szq-gvrg-ttfq MEDIUM, PKSA-rnpm-45mg-w6ht MEDIUM)~~ — closed 2026-05-18 via Phase 11 (3 plans, SDK getagd v0.2.0, Hub composer audit exit 0 zonder ignores). ADR (lokaal): .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md.`

    Alle edits chirurgisch (zie `.ai/rules/engineering.md`): geen reformat van omliggende regels, geen herordening van andere lijst-items, geen comments-cleanup. Match bestaande markdown-style (de bestaande Resolved-bullets gebruiken `~~doorhaling~~` met tilde-marks).
  </action>
  <verify>
    <automated>! grep -q 'PKSA-xnj5-w74d-6wmz' .planning/codebase/STACK.md && grep -q 'Phase 11' .planning/codebase/STACK.md && ! grep -q 'emeq/snelstart-api: dev-master toe te staan' .planning/codebase/STACK.md && grep -q 'completed_phases: 1' .planning/STATE.md && grep -Eq 'last_updated: "2026-05-18T[0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3}Z"' .planning/STATE.md && grep -Eq '^last_activity: 2026-05-18$' .planning/STATE.md && grep -q 'Phase: 11' .planning/STATE.md && grep -q 'Saloon v3' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/STACK.md` bevat geen PKSA-ID's meer (regel 150 vervangen door audit-clean tekst die "Phase 11" noemt).
    - `.planning/codebase/STACK.md` regel 151 noemt niet meer "emeq/snelstart-api: dev-master toe te staan" — motivatie verschoven naar Mollie-alpha (of de werkelijk geconstateerde alpha-dep).
    - `.planning/codebase/CONCERNS.md` toont de audit-risk-regel als resolved (volgens lokale conventie).
    - `.planning/STATE.md` YAML: `progress.completed_phases: 1`, `progress.total_plans: 3`, `progress.completed_plans: 3`, `progress.percent: 20`.
    - `.planning/STATE.md` YAML: `last_updated` matcht regex `"2026-05-18T\d{2}:\d{2}:\d{2}\.\d{3}Z"` (quoted volledige ISO-datetime, identiek format aan bestaande waarde).
    - `.planning/STATE.md` YAML: `last_activity` is exact `2026-05-18` (geen quotes, geen tijd-component — match bestaande format).
    - `.planning/STATE.md` Current Position regel zegt "Phase: 11 — Snelstart-SDK Saloon v4 upgrade (Complete...)".
    - `.planning/STATE.md` Resolved Blockers bevat de Saloon v4-closure-regel met alle 3 PKSA-ID's én een verwijzing naar het lokale ADR-pad.
  </acceptance_criteria>
  <done>Drie tracked file-edits zijn geland; geen drift meer over de audit-ignores, stability-motivatie of Phase 11-status.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hub smoke-test plus commit 3 tracked Plan 11-03 docs</name>
  <files>(commit only — geen nieuwe file-edits, alle edits zijn al gedaan in Task 1 en 2)</files>
  <read_first>
    - Output van Task 1 (lokaal ADR) en Task 2 (3 tracked doc-edits)
    - ROADMAP.md Phase 11 success criteria 1-4 (om Plan 11-03 SUMMARY-mapping correct te schrijven)
  </read_first>
  <action>
    Stap A — Smoke-test bewijs op Hub-side. Run exact dit commando voor de pass-through plus webhook-subset: `php artisan test --compact tests/Feature/Api/V1/Snelstart tests/Feature/SnelstartWebhookControllerTest.php tests/Feature/SnelstartWebhookEndToEndTest.php tests/Feature/ForwardSnelstartWebhookToConsumerJobTest.php tests/Feature/PassThroughCallInboundScopesTest.php tests/Feature/ConnectionAdministratieIdTest.php`. Verwacht JSON-output met `"result":"passed"`, minimaal 45 tests. Leg de exacte aantallen vast voor SUMMARY.

    Stap B — Run nogmaals `composer audit --no-cache` als finale verificatie dat exit 0 nog steeds geldt na de doc-changes. Doc-changes raken `composer.json` niet, dus dit moet identiek zijn aan Plan 11-02 Task 2 Stap C. Vermeld de output regel-voor-regel in SUMMARY.

    Stap C — Pint is niet vereist (geen PHP-files gewijzigd). Geen `vendor/bin/pint --dirty` nodig.

    Stap D — Stage de **3 tracked files** en commit: `git add .planning/STATE.md .planning/codebase/STACK.md .planning/codebase/CONCERNS.md`. NIET `git add .docs/...` — die files zitten in gitignored `.docs/` en horen lokaal te blijven. Commit-message: `docs(11): sync codebase-drift + STATE.md closure voor Saloon v4 upgrade`. Body in Nederlands: `Phase 11 docs-closure (tracked artefacten): STACK.md regel 150 vervangen (audit-ignores weg na Plan 11-02), regel 151 vervangen (minimum-stability motivatie verschoven naar Mollie-alpha na snelstart-pinning), CONCERNS.md audit-risk gemarkeerd resolved, STATE.md progress + Resolved Blockers bijgewerkt. ADR voor Saloon v3 → v4 upgrade en drie advisories closed (PKSA-xnj5, PKSA-5szq, PKSA-rnpm) leeft lokaal in .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md — gitignored werkdocument conform CLAUDE.md.` Co-Authored-By footer.

    Geen push.

    Stap E — Verifieer dat `git status` clean is voor de 3 tracked file-paths en dat geen `.docs/`-files per ongeluk in de commit zitten via `git show --stat HEAD`. Verifieer ook dat `git status --ignored` de lokale ADR-file lijst onder ignored — bevestigt bewuste lokaal-only status.

    Stap F — Documenteer in `11-03-SUMMARY.md` (zie `<output>`-sectie hieronder): pad naar de lokale ADR, aanmaakdatum (2026-05-18), en redactie-status (`compleet — bevat alle 3 PKSA-ID's, SDK + Hub commit-SHA's, en aanbeveling voor v0.4+ signed-tags`). Dit is de traceability-borging die compenseert dat de ADR-file zelf niet in git zit.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Api/V1/Snelstart tests/Feature/SnelstartWebhookControllerTest.php tests/Feature/SnelstartWebhookEndToEndTest.php tests/Feature/ForwardSnelstartWebhookToConsumerJobTest.php 2>&1 | grep -E '"result":"passed"' && composer audit --no-cache 2>&1 | grep -q "No security vulnerability advisories found" && [ "$(git show --stat HEAD | grep -c '^ \.')" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - PHPUnit-subset toont `"result":"passed"` met minimaal 45 tests.
    - `composer audit --no-cache` exit 0.
    - Hub-working-tree heeft een commit met subject `docs(11): sync codebase-drift + STATE.md closure voor Saloon v4 upgrade`.
    - `git show --stat HEAD` toont exact 3 gewijzigde files: `.planning/STATE.md`, `.planning/codebase/STACK.md`, `.planning/codebase/CONCERNS.md`. Geen `.docs/`-files in de commit (die zijn gitignored).
    - `git status --ignored` toont `.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` onder ignored — bevestigt bewuste lokaal-only status.
    - Geen push naar remote.
  </acceptance_criteria>
  <done>Smoke-test groen, audit schoon, 3 tracked docs gecommit in één Hub-commit. Lokale ADR aanwezig en correct geïgnoreerd door git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy → future-developer-decisions | ADR is de bron van waarheid voor toekomstige SDK-upgrades. Een verkeerd of incompleet ADR leidt tot herhaalde mistakes (bijvoorbeeld een nieuwe `audit-ignore` zonder rationale). |
| Lokaal ADR-pad → cross-machine consistentie | De ADR leeft in gitignored `.docs/`, dus is niet aanwezig op andere clones of CI. Traceability hangt af van de tracked verwijzing in STATE.md + plan-SUMMARYs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | ADR-bestand `.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` onthult interne process-details, advisory-ID's en infra-keuzes (zoals het ontbreken van signed-tags) die niet publiek mogen zijn. | mitigate | Het hele `.docs/`-pad is per `.gitignore` regel 35 (`/.docs`) en `CLAUDE.md` ("Werkdocumentatie (lokaal, gitignored)") **lokaal-only**. Geen exceptions voor `decisions/`. Task 1 verifieert dit expliciet met `git check-ignore`, Task 3 verifieert dat de file niet per ongeluk in de commit komt via `git show --stat HEAD`-bullet-count en `git status --ignored`. Traceability gebeurt via de tracked SUMMARY (pad + datum + status) en STATE.md Resolved Blockers (pad-referentie). PKSA-ID's verschijnen in STATE.md, maar dat zijn publieke advisory-ID's uit packagist — geen interne secrets. |
| T-11-09 | Repudiation | Geen audit-trail voor wie de PKSA-ignores oorspronkelijk toevoegde | accept | Git-history geeft blame-trail. ADR voegt rationale + closure-bewijs toe waar voorheen geen rationale was — strict verbetering. Het feit dat het ADR lokaal is, beperkt cross-clone-verbreiding maar niet de lokale audit-trail. |
| T-11-10 | Information Disclosure | Verwijzing naar lokaal-only ADR-pad in tracked STATE.md kan suggereren dat er een file beschikbaar is die niet aanwezig is op andere checkouts. | accept | Risico = LOW (single-maintainer workflow). De STATE.md-regel labelt het pad expliciet als "ADR (lokaal)" zodat een toekomstige lezer begrijpt dat de file gitignored is. Mitigatie voor v0.4+: overweeg een `.docs/.readme`-style stub in tracked space, of verschuif belangrijke ADR-conclusies naar een tracked `.planning/decisions/`-pad. Niet in v0.3. |
| T-11-SC | Tampering | npm/pip/cargo install attack surface | accept | N.v.t. — Plan 11-03 is pure docs + één PHPUnit-run. Geen package-installs. |
</threat_model>

<verification>
- `test -f .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` true.
- `git check-ignore -q .docs/decisions/snelstart-sdk-saloon-v4-upgrade.md` exit 0 (file is correct gitignored).
- `grep -c 'PKSA-' .planning/codebase/STACK.md` is 0 (of PKSA's staan alleen in resolved-context).
- `grep 'completed_phases:' .planning/STATE.md` toont `1`.
- `grep -E 'last_updated: "2026-05-18T[0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3}Z"' .planning/STATE.md` matcht.
- `php artisan test --compact tests/Feature/Api/V1/Snelstart ...` exit 0.
- `composer audit --no-cache` exit 0.
- `git show --stat HEAD` toont 3 tracked files (geen `.docs/`-paths).
</verification>

<success_criteria>
- ADR bestaat lokaal en is correct (en correct gitignored).
- `.planning/codebase/` drift weg (STACK.md regel 150 + 151 + CONCERNS.md regel 189).
- STATE.md reflecteert Phase 11 closure met correct ISO-datetime format.
- Smoke-test plus audit groen op de gepinte v0.2.0.
- Eén Hub-commit voor 3 tracked docs-syncs; ADR-pad gedocumenteerd in SUMMARY.
</success_criteria>

<output>
Schrijf `.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-03-SUMMARY.md` met:
  - Pad naar de **lokale** ADR (`.docs/decisions/snelstart-sdk-saloon-v4-upgrade.md`), aanmaakdatum (2026-05-18) en redactie-status. Expliciet noemen dat dit een gitignored werkdocument is conform `CLAUDE.md` "Werkdocumentatie (lokaal, gitignored)".
  - Snelstart-subset PHPUnit-aantallen (N passed / M assertions).
  - `composer audit`-uitkomst.
  - Hub commit-SHA van de tracked docs-commit (3 files: STATE.md, STACK.md, CONCERNS.md).
  - Eventuele afwijkingen in de codebase-drift-edits (bijvoorbeeld als CONCERNS.md een andere resolved-conventie bleek te volgen dan verwacht, of als STACK.md regel 151 een andere alpha-dep bleek te motiveren dan Mollie).
  - Phase 11 closure-statement: alle 4 ROADMAP success-criteria gedekt door de combinatie 11-01 + 11-02 + 11-03.
</output>
</content>
</invoke>