---
phase: 11-snelstart-sdk-saloon-v4-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - composer.json
  - composer.lock
autonomous: true
requirements:
  - SNEL-03
  - SNEL-04
must_haves:
  truths:
    - "Hub `composer.json` heeft constraint `emeq/snelstart-api: ^0.2.0` (geen `dev-master` meer)."
    - "Hub `composer.lock` pint `emeq/snelstart-api` op tag `v0.2.0` (geen `dev-master`-reference meer)."
    - "Hub `composer.json` heeft géén `config.audit.ignore`-array meer (de drie PKSA-ID's zijn verwijderd)."
    - "`composer audit` exit-status 0 in de Hub-working-tree zonder ignores."
    - "Alle bestaande Snelstart-Hub-tests blijven groen na `composer update emeq/snelstart-api` (45+ tests in Feature/Api/V1/Snelstart, Feature/SnelstartWebhook*, Feature/ForwardSnelstartWebhookToConsumerJob)."
    - "Volledige Hub-PHPUnit-suite blijft groen (geen regressie buiten Snelstart)."
  artifacts:
    - path: "composer.json"
      provides: "Pinned constraint + lege audit-ignore"
      contains: "\"emeq/snelstart-api\": \"^0.2.0\""
    - path: "composer.lock"
      provides: "Resolved v0.2.0 tag reference"
      contains: "\"version\": \"v0.2.0\""
  key_links:
    - from: "composer.json require"
      to: "composer.lock packages[emeq/snelstart-api]"
      via: "composer update lock-regeneration"
      pattern: "emeq/snelstart-api.*v0\\.2\\.0"
    - from: "composer.json config.audit"
      to: "composer audit exit 0"
      via: "removed ignore-array"
      pattern: "audit.*ignore"
---

<objective>
Pin de Hub op `emeq/snelstart-api ^0.2.0` (i.p.v. `dev-master`), verwijder de drie stale `audit-ignores` uit `composer.json`, en bevestig met `composer audit` exit 0 + groene Hub-PHPUnit-suite dat er geen regressie is op `/v1/snelstart/*` of `/webhooks/snelstart`.

Purpose: Sluit SNEL-04 (audit exit 0 zonder ignores) en de Hub-helft van SNEL-03 (geen regressie na composer update) af. Dit is een autonomous Hub-only change — geen SDK-edits, geen externe-API-calls, geen credentials nodig.
Output: Hub commit met `composer.json` + `composer.lock` updates + bewijs van groene tests in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-01-SUMMARY.md
@.ai/rules/engineering.md
@composer.json
@CLAUDE.md

NB: De packages-conventie (VCS-distributie, `composer update emeq/<naam>` na een SDK-tag-bump) staat embedded in `CLAUDE.md` onder `=== .ai/packages rules ===`. `CLAUDE.md` is auto-geladen — geen aparte include nodig.

<interfaces>
<!-- Huidige Hub composer.json kerneigenschappen (regel-referenties t.o.v. file als geland op 2026-05-18): -->
<!-- require: "emeq/snelstart-api": "dev-master" (regel 12) — moet worden gepind. -->
<!-- repositories: VCS-repo voor emeq-snelstart-api blijft staan (regel 122-125), wel anonymous (https). -->
<!-- config.audit.ignore: drie PKSA-ID's (regel 106-110) — moeten weg. -->
<!-- minimum-stability: "dev" + prefer-stable: true (regel 113-114) — kan blijven; v0.2.0 is een stable tag. -->

<!-- Hub-side Snelstart-tests die groen moeten blijven (baseline 45 tests / 207 assertions per run op 2026-05-18): -->
<!-- - tests/Feature/Api/V1/Snelstart/* (pass-through OData) -->
<!-- - tests/Feature/SnelstartWebhookControllerTest.php -->
<!-- - tests/Feature/SnelstartWebhookEndToEndTest.php -->
<!-- - tests/Feature/ForwardSnelstartWebhookToConsumerJobTest.php -->
<!-- - tests/Feature/PassThroughCallInboundScopesTest.php -->
<!-- - tests/Feature/ConnectionAdministratieIdTest.php -->
</interfaces>

**Belangrijk — scope:**
- Dit plan raakt **alleen** Hub-side: `composer.json`, `composer.lock`, commit. Geen edits in `vendor/`, `packages/`, of de SDK-repo.
- Wacht op approval-signaal van Plan 11-01 Task 3 (tag `v0.2.0` gepubliceerd op `github.com:yusufkaracaburun/emeq-snelstart-api`). Zonder remote-tag faalt `composer update emeq/snelstart-api` met "could not find a version matching ^0.2.0".
- Geen `--no-verify` op commit. Geen push zonder approval (zie het `=== .ai/git-policy rules ===` block in `CLAUDE.md`).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pin emeq/snelstart-api op ^0.2.0 in composer.json + verwijder audit-ignores</name>
  <files>composer.json</files>
  <read_first>
    - composer.json (huidige `dev-master`-constraint op regel 12, `audit.ignore`-array op regel 106-110)
    - `=== .ai/packages rules ===` block in `CLAUDE.md` (VCS-distributie zonder auth-conventie)
  </read_first>
  <action>
    Pas in `composer.json` exact deze twee changes toe (geen andere edits — `vendor/bin/pint` wordt niet getriggerd voor JSON):

    1. In `require`: vervang `"emeq/snelstart-api": "dev-master"` door `"emeq/snelstart-api": "^0.2.0"`. Behoud alfabetische sorteervolgorde van het require-blok (`sort-packages: true` is config-aan, dus composer hersorteert later automatisch indien nodig — handmatige edit moet de sortering al respecteren).

    2. In `config.audit`: verwijder het volledige `"ignore"`-array met de drie PKSA-ID's. Behoud `"abandoned": "report"`. Resultaat is `"audit": { "abandoned": "report" }`.

    Niet aanpassen:
    - `repositories` (VCS-entry voor emeq-snelstart-api blijft staan — versie-resolution gebeurt via tag-discovery).
    - `minimum-stability: "dev"` + `prefer-stable: true` (laat staan — Mollie-SDK gebruikt `^0.1.0-alpha.1`).
    - Andere advisories in dev-deps (geen audit-ignores elders aanwezig — geverifieer met `grep -A5 '"audit"' composer.json`).

    JSON moet valide blijven. Validate met `php -r 'json_decode(file_get_contents("composer.json"), false, 512, JSON_THROW_ON_ERROR); echo "ok\n";'`.
  </action>
  <verify>
    <automated>php -r 'json_decode(file_get_contents("composer.json"), false, 512, JSON_THROW_ON_ERROR);' && grep -q '"emeq/snelstart-api": "\^0.2.0"' composer.json && ! grep -q 'PKSA-xnj5-w74d-6wmz\|PKSA-5szq-gvrg-ttfq\|PKSA-rnpm-45mg-w6ht' composer.json && ! grep -q '"dev-master"' composer.json</automated>
  </verify>
  <acceptance_criteria>
    - `composer.json` is parseable JSON (geen syntax-errors).
    - `require["emeq/snelstart-api"]` is exact `"^0.2.0"`.
    - Geen van de drie PKSA-ID's komt nog voor in `composer.json`.
    - Geen `"dev-master"`-string meer in `composer.json`.
    - `config.audit.abandoned` is nog steeds `"report"`.
  </acceptance_criteria>
  <done>composer.json pint v0.2.0 en heeft geen stale audit-ignores meer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: composer update emeq/snelstart-api + composer audit + Hub-PHPUnit groene baseline</name>
  <files>composer.lock</files>
  <read_first>
    - composer.json (post-Task-1 staat: ^0.2.0 + geen audit-ignores)
    - composer.lock (huidige reference op `dev-master` met SHA `e9076d40a226a1587b123bfa808897d813321ed1`)
  </read_first>
  <action>
    Stap A — Trigger composer-resolution: `composer update emeq/snelstart-api --no-interaction --no-cache --with-dependencies`. Dit moet `emeq/snelstart-api` resolven naar de tag `v0.2.0` uit `https://github.com/yusufkaracaburun/emeq-snelstart-api.git` en `composer.lock` herschrijven. `--with-dependencies` zorgt dat Saloon/laravel-plugin meegetrokken worden als de SDK transitief nieuwere constraints heeft.

    Stap B — Verifieer dat composer.lock nu `"version": "v0.2.0"` heeft onder de `emeq/snelstart-api`-package-entry (gebruik `grep -A2 '"name": "emeq/snelstart-api"' composer.lock | head -5`).

    Stap C — Draai `composer audit --no-cache`. Verwacht exit-status 0 met output `No security vulnerability advisories found.` Geen ignores actief.

    Stap D — Draai Hub-PHPUnit subset voor Snelstart-pass-through + webhook tests: `php artisan test --compact tests/Feature/Api/V1/Snelstart tests/Feature/SnelstartWebhookControllerTest.php tests/Feature/SnelstartWebhookEndToEndTest.php tests/Feature/ForwardSnelstartWebhookToConsumerJobTest.php tests/Feature/PassThroughCallInboundScopesTest.php tests/Feature/ConnectionAdministratieIdTest.php`. Verwacht ≥45 passed, 0 failed (baseline: 45/207 op 2026-05-18 pre-update).

    Stap E — Draai volledige Hub-PHPUnit suite: `php artisan test --compact`. Verwacht 0 failed. Indien een test breekt door Saloon v4 breaking-changes (bijv. een MockClient-fixture die `resolveRequestUrl()` op een Connector-extend stub gebruikt): STOP en log de failure in SUMMARY; doe geen ad-hoc fix in dit plan, escalleer naar een gap-closure-plan.

    Stap F — Indien stappen A-E groen zijn: stage `composer.json` + `composer.lock` en commit met message: `chore(deps): pin emeq/snelstart-api ^0.2.0 + drop stale audit-ignores`. Body in Nederlands: `Saloon v3 → v4 upgrade is geland in de SDK (tag v0.2.0). Drie advisories (PKSA-xnj5-w74d-6wmz, PKSA-5szq-gvrg-ttfq, PKSA-rnpm-45mg-w6ht) zijn opgelost in Saloon v4.0.0 — composer.json audit-ignores verwijderd, composer audit retourneert exit 0.` Co-Authored-By footer.

    Geen push.

    Gebruik bij voorkeur Laravel Boost MCP-tools voor secundaire verificatie: `application-info` om SDK-package-versie te bevestigen na update, en `database-query` of `database-schema` zijn hier irrelevant (geen DB-changes).
  </action>
  <verify>
    <automated>composer audit --no-cache 2>&1 | grep -q "No security vulnerability advisories found" && grep -A2 '"name": "emeq/snelstart-api"' composer.lock | grep -q '"version": "v0.2.0"' && php artisan test --compact tests/Feature/Api/V1/Snelstart tests/Feature/SnelstartWebhookControllerTest.php tests/Feature/SnelstartWebhookEndToEndTest.php tests/Feature/ForwardSnelstartWebhookToConsumerJobTest.php tests/Feature/PassThroughCallInboundScopesTest.php tests/Feature/ConnectionAdministratieIdTest.php 2>&1 | grep -E '"result":"passed"' && php artisan test --compact 2>&1 | tail -5 | grep -E 'Tests:.*passed'</automated>
  </verify>
  <acceptance_criteria>
    - `composer audit --no-cache` exit 0, output bevat `No security vulnerability advisories found.`
    - `composer.lock` heeft `"name": "emeq/snelstart-api"` met `"version": "v0.2.0"` (geen `dev-master`).
    - Hub-Snelstart-subset PHPUnit-run toont JSON `"result":"passed"` met ≥45 tests, gedraaid over alle zes subset-files (Api/V1/Snelstart, SnelstartWebhookController, SnelstartWebhookEndToEnd, ForwardSnelstartWebhookToConsumerJob, PassThroughCallInboundScopes, ConnectionAdministratieId).
    - Volledige `php artisan test --compact` toont 0 failed en heeft `Tests: N passed` in de samenvatting.
    - Hub-working-tree heeft een commit met `composer.json` + `composer.lock` (en geen andere files).
  </acceptance_criteria>
  <done>Hub pakt v0.2.0, audit is schoon, alle Snelstart-tests groen, volledige Hub-suite groen, commit gemaakt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub Composer-resolution → SDK-tag op GitHub | Hub vertrouwt dat de tag `v0.2.0` op de SDK-remote correspondeert met de reviewed master-tip. Zonder branch-protection of signed-tags kan een gecompromitteerde push de tag herschrijven. |
| Hub `composer.json` audit-config → CI/dev-feedback | Het verwijderen van audit-ignores is een eenmalige actie; toekomstige advisories landen direct in de feedback-loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | Hub-side `composer update` kan een herschreven tag binnenhalen | accept | Lock-file pint commit-SHA. Plan 11-03 documenteert in de ADR de aanbeveling om in v0.4+ signed-tags op SDK-releases toe te voegen. Geen actie nu — risk = LOW (1 maintainer, private dev-loop). |
| T-11-06 | Information Disclosure | Drie opgeloste Saloon v3-advisories | mitigate | `composer audit` exit 0 zonder ignores als gate. Toekomstige Saloon-advisories triggeren CI/dev-loop direct in plaats van te worden verstopt achter ignores. |
| T-11-07 | Denial of Service | Composer-resolution faalt als SDK-tag ontbreekt | mitigate | Plan-dependency op 11-01 Task 3 (tag-push approval). Plan 11-02 mag pas starten als 11-01-SUMMARY bevestigt dat `v0.2.0` op de remote staat. |
| T-11-SC | Tampering | npm/pip/cargo install attack surface | accept | N.v.t. — `composer` is de package-manager. Geen npm/pip/cargo-installs in dit plan. Composer-installs van bestaande, gelocked packages (geen nieuwe deps toegevoegd). |
</threat_model>

<verification>
- `composer audit --no-cache` → exit 0, "No security vulnerability advisories found."
- `grep -A2 '"name": "emeq/snelstart-api"' composer.lock | head -5` → toont `"version": "v0.2.0"`.
- `php artisan test --compact` → 0 failed (volledige Hub-suite).
- `grep -c 'PKSA-' composer.json` → 0.
- `git log --oneline -1` → toont `chore(deps): pin emeq/snelstart-api ^0.2.0 + drop stale audit-ignores`.
</verification>

<success_criteria>
- composer.json heeft `^0.2.0` en geen audit-ignores.
- composer.lock pint `v0.2.0`.
- composer audit exit 0.
- Hub PHPUnit-suite groen (Snelstart-subset + totaal).
- Commit gemaakt, niet gepusht.
</success_criteria>

<output>
Schrijf `.planning/phases/11-snelstart-sdk-saloon-v4-upgrade/11-02-SUMMARY.md` met:
- Resolved SDK-versie + commit-SHA uit composer.lock
- `composer audit`-output (kopie van de "no advisories"-regel)
- PHPUnit-test-aantallen: Snelstart-subset N/M + Hub-totaal N/M (passed/assertions)
- Hub commit-SHA van de composer.json + composer.lock-commit
- Eventuele afwijkingen / surprise breaking-changes die uit Saloon v4 boven kwamen
</output>
</content>
</invoke>