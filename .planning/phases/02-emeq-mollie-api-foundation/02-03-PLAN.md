---
phase: 02-emeq-mollie-api-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/mollie-api/src/Exceptions/MollieException.php
  - packages/mollie-api/src/Exceptions/MissingCredentialResolverException.php
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "MollieException bestaat als RuntimeException-subclass — package-base voor alle SDK-eigen exceptions"
    - "MissingCredentialResolverException extends MollieException en heeft een static ::notBound() factory die de FQCN van de resolver in de message zet"
    - "Geen raw apiKey of accessToken kan in de exception-messages terechtkomen (geen $secret-acceptatie in ::notBound())"
  artifacts:
    - path: "packages/mollie-api/src/Exceptions/MollieException.php"
      provides: "Package-base exception"
      contains: "class MollieException extends RuntimeException"
    - path: "packages/mollie-api/src/Exceptions/MissingCredentialResolverException.php"
      provides: "Helpful error when host app forgot to bind the resolver"
      contains: "notBound"
  key_links:
    - from: "src/Exceptions/MissingCredentialResolverException.php"
      to: "src/Exceptions/MollieException.php"
      via: "extends MollieException"
      pattern: "extends MollieException"
    - from: "src/Exceptions/MissingCredentialResolverException.php"
      to: "src/Contracts/MollieCredentialResolver.php"
      via: "FQCN in error-message"
      pattern: "MollieCredentialResolver::class"
---

<objective>
Maak de minimale exception-laag voor het pakket: `MollieException` als package-base (subclass van RuntimeException) en `MissingCredentialResolverException` met `::notBound()` static factory die een behulpzame message produceert die de FQCN van het contract noemt. Geen verdere HTTP-status-specifieke exceptions in Phase 2 — die staan in `mollie/mollie-api-php` zelf (zie CONTEXT.md "Bewust NIET": geen eigen exception-hiërarchie voor HTTP-statuses).

Purpose: Plan 02-04 (`MollieServiceProvider`) heeft `MissingCredentialResolverException` nodig om de container-binding van `Mollie::class` te bewaken. Snelle, op zichzelf staande task — bewust geïsoleerd om wave 1 parallelliseerbaar te houden.

Output: 2 exception-classes (~30 regels code totaal). Geen dedicated test-file in dit plan — de exception is een trigger-mechanism, getest in MollieServiceProviderTest / MollieTest (plan 02-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
Snelstart-SDK reference (1-op-1 pattern):

packages/snelstart-api/src/Exceptions/SnelstartException.php:
  class SnelstartException extends RuntimeException

packages/snelstart-api/src/Exceptions/MissingCredentialResolverException.php:
  final class MissingCredentialResolverException extends SnelstartException
  static notBound(): self -- sprintf-message met SnelstartCredentialResolver::class

Spec: NIET veel meer dan dit. Geen HTTP-status-mapping exceptions in dit plan.
Mollie's eigen Mollie\Api\Exceptions\{ApiException, ValidationException, UnauthorizedException, NotFoundException, TooManyRequestsException, ServerException, ServiceUnavailableException} worden direct door host-apps gecatched, niet via een eigen hiërarchie gemapped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MollieException + MissingCredentialResolverException</name>
  <read_first>
    - packages/snelstart-api/src/Exceptions/SnelstartException.php
    - packages/snelstart-api/src/Exceptions/MissingCredentialResolverException.php
    - packages/mollie-api/src/Contracts/MollieCredentialResolver.php (om de FQCN te bevestigen die in de message moet)
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Géén Saloon, géén eigen exception-hiërarchie voor HTTP-statuses"
  </read_first>
  <files>
    packages/mollie-api/src/Exceptions/MollieException.php
    packages/mollie-api/src/Exceptions/MissingCredentialResolverException.php
  </files>
  <action>
Maak `packages/mollie-api/src/Exceptions/MollieException.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Exceptions;

use RuntimeException;

/**
 * Base exception for every error raised by emeq/mollie-api itself
 * (i.e. package-level wiring/config errors, NOT HTTP-level errors).
 *
 * Errors that originate from the underlying mollie/mollie-api-php library
 * (HTTP 4xx/5xx, validation, rate-limiting, etc.) are surfaced as the
 * library's own exceptions:
 *  - Mollie\Api\Exceptions\ApiException                  (generic)
 *  - Mollie\Api\Exceptions\ValidationException           (422 + ::getField())
 *  - Mollie\Api\Exceptions\UnauthorizedException         (401)
 *  - Mollie\Api\Exceptions\NotFoundException             (404)
 *  - Mollie\Api\Exceptions\TooManyRequestsException      (429)
 *  - Mollie\Api\Exceptions\ServerException               (5xx)
 *  - Mollie\Api\Exceptions\ServiceUnavailableException   (503)
 *
 * Host apps catch those directly. We do NOT wrap them in our own hierarchy.
 *
 * MollieException subclasses (in this package):
 *  - MissingCredentialResolverException — host app forgot to bind the resolver
 *  - (future) production-env-guard violations when enforce_environment=true
 */
class MollieException extends RuntimeException
{
}
```

Maak `packages/mollie-api/src/Exceptions/MissingCredentialResolverException.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Exceptions;

use Emeq\MollieApi\Contracts\MollieCredentialResolver;

/**
 * Thrown when the package is used but the host app never bound a
 * MollieCredentialResolver in the container.
 *
 * The package is intentionally tenant-agnostic — it does not assume any
 * particular multi-tenancy layer (stancl/tenancy, spatie/multitenancy,
 * Emeq Hub Connection model, custom), so the host app MUST tell it how
 * to fetch credentials.
 */
final class MissingCredentialResolverException extends MollieException
{
    public static function notBound(): self
    {
        return new self(
            sprintf(
                'No %s binding found in the container. Bind your resolver in a ServiceProvider, e.g.: ' .
                "\$this->app->bind(%s::class, YourTenantResolver::class);",
                MollieCredentialResolver::class,
                MollieCredentialResolver::class,
            ),
        );
    }
}
```

NIET parameters accepteren in `::notBound()` die een secret kunnen lekken. De message bevat alleen de FQCN van het contract — een interface-naam, geen runtime-data.
  </action>
  <verify>
    <automated>cd packages/mollie-api && grep -q 'class MollieException extends RuntimeException' src/Exceptions/MollieException.php && grep -q 'final class MissingCredentialResolverException extends MollieException' src/Exceptions/MissingCredentialResolverException.php && grep -q 'public static function notBound(): self' src/Exceptions/MissingCredentialResolverException.php && grep -q 'MollieCredentialResolver::class' src/Exceptions/MissingCredentialResolverException.php</automated>
  </verify>
  <done>MollieException is package-base RuntimeException. MissingCredentialResolverException extends MollieException, heeft `::notBound()` static factory, en lekt geen secrets in zijn message — alleen de FQCN van het contract.</done>
</task>

</tasks>

<verification>
- grep `class MollieException extends RuntimeException` in src/Exceptions/MollieException.php → 1 hit
- grep `final class MissingCredentialResolverException extends MollieException` in src/Exceptions/MissingCredentialResolverException.php → 1 hit
- grep `MollieCredentialResolver::class` in src/Exceptions/MissingCredentialResolverException.php → ≥1 hit (de FQCN-referentie)
- Geen `$apiKey` of `$accessToken` of `secret` keywords in de exception-files (security-property)
</verification>

<success_criteria>
- Exception-laag is minimaal en in lijn met CONTEXT.md decision "geen eigen HTTP-exception-hiërarchie"
- `MollieException` is leeg-body subclass van RuntimeException
- `MissingCredentialResolverException::notBound()` is parameter-loos en produceert een actionable error-message met copy-paste-ready bind-snippet
- Security-invariant gerespecteerd: geen weg om een raw secret in een exception-message te krijgen via deze classes
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-03-SUMMARY.md`
</output>
