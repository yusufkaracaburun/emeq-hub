---
phase: 02-emeq-mollie-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mollie-api/composer.json
  - packages/mollie-api/.gitignore
  - packages/mollie-api/.gitattributes
  - packages/mollie-api/.editorconfig
  - packages/mollie-api/pint.json
  - packages/mollie-api/phpstan.neon.dist
  - packages/mollie-api/phpunit.xml.dist
  - packages/mollie-api/README.md
  - packages/mollie-api/LICENSE.md
  - packages/mollie-api/CHANGELOG.md
  - packages/mollie-api/config/mollie.php
  - packages/mollie-api/.gitkeep-src
  - packages/mollie-api/.gitkeep-tests
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "Een feature-branch `feat/foundation` bestaat in packages/mollie-api/ (sub-repo), afgesplitst vanaf de lege initial state"
    - "composer.json beschrijft package emeq/mollie-api met namespace Emeq\\MollieApi\\, PHP ^8.3, mollie/mollie-api-php ^3.11 als kerndep, en auto-discovery voor MollieServiceProvider + Mollie-facade-alias"
    - "composer.json bevat GEEN `post-autoload-dump: @composer run prepare` script in dit plan — die wordt pas door plan 02-04 toegevoegd zodra MollieServiceProvider concreet bestaat"
    - "cd packages/mollie-api && composer install slaagt zonder errors en plaatst mollie/mollie-api-php in packages/mollie-api/vendor/"
    - ".gitignore mirrort Snelstart 1-op-1 inclusief `composer.lock` (SDK-best-practice: lib's lock niet shippen)"
    - "Configuratiebestand config/mollie.php bestaat met enforce_environment / http.timeout / idempotency.generator keys"
    - "Skelet-metadata (.gitignore, .gitattributes, .editorconfig, pint.json, phpstan.neon.dist, phpunit.xml.dist, README.md, LICENSE.md, CHANGELOG.md) gevuld volgens Snelstart-SDK-conventies"
  artifacts:
    - path: "packages/mollie-api/composer.json"
      provides: "Package manifest met mollie/mollie-api-php als kerndep"
      contains: "emeq/mollie-api"
    - path: "packages/mollie-api/.gitignore"
      provides: "Gitignore met composer.lock excluded (SDK-best-practice)"
      contains: "composer.lock"
    - path: "packages/mollie-api/config/mollie.php"
      provides: "Configuratie voor enforce_environment / http / idempotency"
      contains: "enforce_environment"
    - path: "packages/mollie-api/phpunit.xml.dist"
      provides: "PHPUnit/Pest configuration"
      contains: "tests"
    - path: "packages/mollie-api/LICENSE.md"
      provides: "MIT license"
      contains: "MIT License"
  key_links:
    - from: "packages/mollie-api/composer.json"
      to: "Emeq\\MollieApi\\MollieServiceProvider"
      via: "extra.laravel.providers"
      pattern: "Emeq.*MollieApi.*MollieServiceProvider"
    - from: "packages/mollie-api/composer.json"
      to: "mollie/mollie-api-php"
      via: "require"
      pattern: "mollie/mollie-api-php"
---

<objective>
Zet het lege sub-repo `packages/mollie-api/` op met de volledige package-skeleton: `composer.json`, alle dotfiles, `pint.json`, `phpstan.neon.dist`, `phpunit.xml.dist`, `README.md`, `LICENSE.md` (MIT), `CHANGELOG.md`, en `config/mollie.php`. Spiegelt het Snelstart-SDK-pattern 1-op-1 maar met `mollie/mollie-api-php ^3.11` als kerndep i.p.v. Saloon. Eindigt met groene `composer install` en een `feat/foundation` feature-branch klaar voor plan 02-02.

Purpose: Zonder werkende package-skeleton kunnen latere plans (02-02 t/m 02-07) geen `src/` of `tests/` toevoegen — Composer's autoload + PSR-4 mapping en Testbench's package discovery moeten eerst staan. Dit plan is "alleen boilerplate" — geen logica.

Output: Een werkende, lege Emeq\MollieApi-package-skeleton (geen `src/*.php` of `tests/*.php` files yet — die komen in volgende plans) met `composer install` groen en autoload-rules klaar. Een feature-branch `feat/foundation` in de sub-repo.

NOTE (W-2 revision): de `post-autoload-dump: @composer run prepare` script is BEWUST weggelaten uit dit plan. Plan 02-04 voegt 'm pas toe zodra `MollieServiceProvider` concreet is. Dat elimineert de non-deterministische "stub-fallback"-conditional die in iteratie 0 in task 3 stond.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@.ai/packages

<interfaces>
<!-- Sub-repo state op planning-time -->
Working repo: packages/mollie-api/ (sub-repo, eigen git, geklond uit github.com:yusufkaracaburun/emeq-mollie-api.git op 2026-05-14)
Sub-repo branch: master (geen commits), default branch nog niet ingesteld
Working tree: leeg (geen files)

Snelstart-SDK referentie-files (1-op-1 pattern voor outer-layer):
  packages/snelstart-api/composer.json
  packages/snelstart-api/.gitignore           — heeft `composer.lock` als eerste entry (SDK-best-practice)
  packages/snelstart-api/.gitattributes
  packages/snelstart-api/.editorconfig
  packages/snelstart-api/pint.json
  packages/snelstart-api/phpstan.neon.dist
  packages/snelstart-api/phpunit.xml.dist
  packages/snelstart-api/LICENSE.md
  packages/snelstart-api/config/snelstart.php

Snelstart-SDK composer.json relevant: `"php": "^8.3"`, scripts contain `post-autoload-dump: @composer run prepare` — voor onze SDK doen we hetzelfde voor PHP-constraint (B-2 Optie A: matchen) maar `prepare` wordt PAS in plan 02-04 toegevoegd (W-2 Optie B).

Hub composer.json: heeft al een Snelstart path-repository entry (`snelstart-path` + `snelstart-vcs`). Mollie path-repo entry wordt PAS in plan 02-08 toegevoegd.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: composer.json + autoload-skeleton + feature-branch (zonder `prepare`-script)</name>
  <read_first>
    - packages/snelstart-api/composer.json (template — 1-op-1 pattern behalve require-section en `scripts.post-autoload-dump`)
    - packages/snelstart-api/.gitignore (template — eerste regel is `composer.lock`; SDK-best-practice)
    - packages/mollie-api/ (controleer dat repo écht leeg is: `cd packages/mollie-api && git status` toont "No commits yet")
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Dependencies"
  </read_first>
  <files>
    packages/mollie-api/composer.json
    packages/mollie-api/.gitignore
    packages/mollie-api/.gitattributes
    packages/mollie-api/.editorconfig
  </files>
  <action>
Eerst checken dat we in de sub-repo zijn en op een feature-branch landen (NIET op master):

```bash
cd packages/mollie-api
git status              # verwacht: "On branch master / No commits yet"
git checkout -b feat/foundation
```

Maak `packages/mollie-api/composer.json` met de exacte volgende inhoud. LET OP:
- `require.php` = `^8.3` (matchen Snelstart-SDK; PHP 8.4-host draait dit prima)
- `scripts` heeft GEEN `post-autoload-dump`/`prepare` entry — die wordt door plan 02-04 toegevoegd (W-2 Optie B)
- `require` heeft GEEN saloonphp deps, WEL `mollie/mollie-api-php`

```json
{
    "name": "emeq/mollie-api",
    "description": "Multi-tenant Laravel SDK rond mollie/mollie-api-php — credential resolver + dual creds (API key + OAuth) + facade.",
    "keywords": [
        "emeq",
        "laravel",
        "mollie",
        "mollie-api",
        "payments",
        "oauth",
        "connect",
        "multi-tenant"
    ],
    "homepage": "https://github.com/yusufkaracaburun/emeq-mollie-api",
    "license": "MIT",
    "authors": [
        {
            "name": "Yusuf Karacaburun",
            "email": "info@emeq.nl",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.3",
        "ext-json": "*",
        "illuminate/contracts": "^11.0||^12.0||^13.0",
        "mollie/mollie-api-php": "^3.11",
        "spatie/laravel-data": "^4.0",
        "spatie/laravel-package-tools": "^1.16"
    },
    "require-dev": {
        "larastan/larastan": "^3.0",
        "laravel/pint": "^1.14",
        "nunomaduro/collision": "^8.8",
        "orchestra/testbench": "^9.0||^10.0||^11.0",
        "pestphp/pest": "^3.0||^4.0",
        "pestphp/pest-plugin-arch": "^3.0||^4.0",
        "pestphp/pest-plugin-laravel": "^3.0||^4.0",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpstan-deprecation-rules": "^2.0",
        "phpstan/phpstan-phpunit": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "Emeq\\MollieApi\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Emeq\\MollieApi\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "analyse": "vendor/bin/phpstan analyse",
        "test": "vendor/bin/pest",
        "test-coverage": "vendor/bin/pest --coverage",
        "format": "vendor/bin/pint"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "phpstan/extension-installer": true
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Emeq\\MollieApi\\MollieServiceProvider"
            ],
            "aliases": {
                "Mollie": "Emeq\\MollieApi\\Facades\\Mollie"
            }
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
```

Maak `.gitignore` 1-op-1 gekopieerd van `packages/snelstart-api/.gitignore`. **Belangrijk** (B-3): de eerste regel `composer.lock` MOET aanwezig zijn. Dit is SDK-best-practice — een library shipt zijn lock niet (consumers krijgen daardoor reproducible installs vanuit hun eigen `composer.lock`). Verifieer met `grep -q '^composer.lock$' packages/mollie-api/.gitignore` na het schrijven. Volledige inhoud (1-op-1 van Snelstart):

```
# Composer Related
composer.lock
/vendor

# Frontend Assets
/node_modules

# Logs
npm-debug.log
yarn-error.log

# Caches
.phpunit.cache
.phpunit.result.cache
/build

# IDE Helper
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# Editors
/.idea
/.fleet
/.vscode

# Misc
phpunit.xml
phpstan.neon
testbench.yaml
/docs
/coverage

# Planning scratchpads (lokale diagnostiek; horen niet in de SDK-repo)
.planning-scratch/
```

Maak `.gitattributes` 1-op-1 gekopieerd van `packages/snelstart-api/.gitattributes` (export-ignore entries voor /.github, /tests, /phpunit.xml.dist, etc.).

Maak `.editorconfig` 1-op-1 gekopieerd van `packages/snelstart-api/.editorconfig`.

Belangrijk: namespace = `Emeq\MollieApi\` (NIET `Emeq\Mollie\` of `Mollie\Emeq\`). Facade-alias = `Mollie` (NIET `EmeqMollie` — zie CONTEXT.md decision).
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -q '^feat/foundation$' &amp;&amp; grep -q '"emeq/mollie-api"' composer.json &amp;&amp; grep -q '"mollie/mollie-api-php": "\^3.11"' composer.json &amp;&amp; grep -q '"Emeq\\\\MollieApi\\\\": "src/"' composer.json &amp;&amp; grep -q 'MollieServiceProvider' composer.json &amp;&amp; grep -q '"Mollie": "Emeq\\\\MollieApi\\\\Facades\\\\Mollie"' composer.json &amp;&amp; ! grep -q 'post-autoload-dump' composer.json &amp;&amp; ! grep -q '"prepare"' composer.json &amp;&amp; grep -q '^composer.lock$' .gitignore &amp;&amp; [ -f .gitattributes ] &amp;&amp; [ -f .editorconfig ]</automated>
  </verify>
  <done>composer.json toont `"name": "emeq/mollie-api"`, `mollie/mollie-api-php ^3.11` als require, namespace `Emeq\MollieApi\` mapt naar src/, auto-discovery wijst naar `Emeq\MollieApi\MollieServiceProvider` + alias `Mollie` → facade, GEEN `post-autoload-dump`/`prepare` script (toegevoegd in 02-04). `.gitignore` heeft `composer.lock` als eerste entry. `.gitattributes`/`.editorconfig` 1-op-1 mirror van Snelstart-SDK. Feature-branch `feat/foundation` is uitgechecked in de sub-repo.</done>
</task>

<task type="auto">
  <name>Task 2: Tooling-config + skeleton-metadata + config/mollie.php</name>
  <read_first>
    - packages/snelstart-api/pint.json (1-op-1 copy)
    - packages/snelstart-api/phpstan.neon.dist (template — paths zijn identiek, excludePaths is anders: GEEN src/Dto/* uitsluiting, GEEN larastan.noEnvCallsOutsideOfConfig override)
    - packages/snelstart-api/phpunit.xml.dist (1-op-1 copy maar testsuite-naam aanpassen)
    - packages/snelstart-api/LICENSE.md (template — naam blijft Yusuf Karacaburun, jaartal blijft staan zoals in Snelstart-versie)
    - packages/snelstart-api/config/snelstart.php (referentie voor docblock-stijl en env() pattern)
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "config/mollie.php structuur"
  </read_first>
  <files>
    packages/mollie-api/pint.json
    packages/mollie-api/phpstan.neon.dist
    packages/mollie-api/phpunit.xml.dist
    packages/mollie-api/README.md
    packages/mollie-api/LICENSE.md
    packages/mollie-api/CHANGELOG.md
    packages/mollie-api/config/mollie.php
  </files>
  <action>
Maak `pint.json` 1-op-1 kopie van `packages/snelstart-api/pint.json`. Geen wijzigingen.

Maak `phpstan.neon.dist` met de volgende exacte inhoud (level 6, geen Dto-exclude, geen env-override want config/mollie.php is clean):

```yaml
parameters:
    level: 6
    paths:
        - src
        - config
    tmpDir: build/phpstan
```

Maak `phpunit.xml.dist` 1-op-1 kopie van `packages/snelstart-api/phpunit.xml.dist`, met als enige wijziging het `<testsuite name="...">` attribuut:

```xml
<testsuite name="emeq/mollie-api Test Suite">
```

Maak `LICENSE.md` 1-op-1 kopie van `packages/snelstart-api/LICENSE.md` (MIT, Copyright Yusuf Karacaburun <info@emeq.nl>).

Maak `CHANGELOG.md` met deze placeholder-inhoud:

```markdown
# Changelog

Alle noemenswaardige wijzigingen aan `emeq/mollie-api` worden hier bijgehouden.

Het formaat is gebaseerd op [Keep a Changelog](https://keepachangelog.com/nl/1.1.0/),
en dit project volgt [Semantic Versioning](https://semver.org/lang/nl/).

## [Unreleased]

### Added

- Initiële package-skeleton (Phase 2 emeq/hub v0.2): `MollieServiceProvider`, `Mollie`-facade,
  `MollieCredentialResolver`-contract, dual creds (`MollieApiKeyCredentials` + `MollieOAuthCredentials`),
  exception-laag (`MollieException` + `MissingCredentialResolverException`), Pest-suite ≥10 cases.
```

Maak `README.md` met deze inhoud (kort — gedetailleerde docs volgen in latere phases):

```markdown
# emeq/mollie-api

> Multi-tenant Laravel SDK rond [`mollie/mollie-api-php`](https://github.com/mollie/mollie-api-php) — credential resolver, dual credentials (API key + Mollie Connect OAuth) en een facade.

## Installatie

```bash
composer require emeq/mollie-api
```

## Gebruik

```php
use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Facades\Mollie;

// 1. Bind een resolver (in een ServiceProvider van de host-app)
app()->bind(MollieCredentialResolver::class, fn () => new YourTenantResolver());

// 2. Roep de Mollie facade aan
$client = Mollie::client(); // \Mollie\Api\MollieApiClient
$payment = $client->payments->create([...]);
```

### Dual credentials

Een `MollieCredentialResolver::resolve()` returnt óf `MollieApiKeyCredentials` óf `MollieOAuthCredentials`. De SDK swapped runtime tussen `setApiKey()` (API-key flow) en `setAccessToken()` (Mollie Connect OAuth flow) zonder dat de host-app hoeft te weten welk type credential terugkomt.

### Facade-alias collision

Bij gebruik naast [`mollie/laravel-mollie`](https://github.com/mollie/laravel-mollie) (bijv. transitive via Cashier-Mollie) moet één van beide aliases worden uitgeschakeld of FQN-imported. Zie [docs/integraties](#) (volgt in Phase 6 — SUB-01).

## Onderdeel van

Deze SDK is onderdeel van [`emeq/hub`](https://github.com/yusufkaracaburun/emeq-hub) — een integratie-platform dat Mollie + Snelstart + andere NL providers multi-tenant aanbiedt.

## License

MIT — zie [LICENSE.md](LICENSE.md).
```

Maak `config/mollie.php` met deze exacte inhoud (`idempotency.generator` docblock documenteert beide accepteerbare values: FQCN of container alias — B-8):

```php
<?php

declare(strict_types=1);

/**
 * Configuratie voor emeq/mollie-api.
 */
return [

    /*
     * Wanneer true (en de Laravel-env is "production"), gooit Mollie::client()
     * een MollieException als de resolved credentials een test_-prefix hebben.
     * Beschermt tegen het per-ongeluk gebruiken van een test-key in productie.
     */
    'enforce_environment' => env('MOLLIE_ENFORCE_ENVIRONMENT', false),

    'http' => [
        /*
         * Guzzle request-timeout in seconden. Wordt gebruikt om een custom
         * Guzzle-client te bouwen die aan MollieApiClient wordt doorgegeven.
         */
        'timeout' => env('MOLLIE_HTTP_TIMEOUT', 30),

        /*
         * Extra Guzzle-options merged op de default. Leeg laten tenzij je
         * proxy/CA-bundle configuratie nodig hebt.
         *
         * @var array<string, mixed>
         */
        'guzzle_options' => [],
    ],

    'idempotency' => [
        /*
         | 'generator' accepteert ofwel:
         |   - een fully-qualified class name implementing
         |     Mollie\Api\Idempotency\IdempotencyKeyGeneratorContract, bv:
         |       'App\\Mollie\\JobAwareIdempotencyGenerator'
         |   - een container alias of binding-key die naar zo'n instance resolved,
         |     bv:
         |       'mollie.idempotency-generator'
         |
         | Mollie::client() roept `$container->make($value)` aan; beide paden
         | resolven naar dezelfde instance. Null = laat Mollie's default
         | generator (random UUID) gebruiken.
         */
        'generator' => null,
    ],

];
```

Commit niets — dat gebeurt pas in plan 02-08. Files staan nu in working tree.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; [ -f pint.json ] &amp;&amp; grep -q 'level: 6' phpstan.neon.dist &amp;&amp; grep -q 'emeq/mollie-api Test Suite' phpunit.xml.dist &amp;&amp; grep -q 'MIT License' LICENSE.md &amp;&amp; grep -q 'enforce_environment' config/mollie.php &amp;&amp; grep -q 'MOLLIE_ENFORCE_ENVIRONMENT' config/mollie.php &amp;&amp; grep -q 'MOLLIE_HTTP_TIMEOUT' config/mollie.php &amp;&amp; grep -q 'idempotency' config/mollie.php &amp;&amp; grep -q 'container alias' config/mollie.php &amp;&amp; grep -q '# emeq/mollie-api' README.md</automated>
  </verify>
  <done>Alle skeleton-files staan in de working tree van `packages/mollie-api/`. `config/mollie.php` heeft drie top-level keys (`enforce_environment`, `http`, `idempotency`), waarbij de docblock van `idempotency.generator` expliciet zowel FQCN als container-alias documenteert. `phpstan.neon.dist` is level 6. `phpunit.xml.dist` heeft Mollie-suite-naam. LICENSE.md is MIT.</done>
</task>

<task type="auto">
  <name>Task 3: composer install + autoload smoke + lege src/ + tests/ dirs (deterministisch — geen stub-fallback)</name>
  <read_first>
    - packages/mollie-api/composer.json (verifieer dat het bestaat uit task 1)
    - packages/snelstart-api/ (controleer dat `vendor/` en `composer.lock` gegenereerd zijn als referentie van wat we verwachten)
  </read_first>
  <files>
    packages/mollie-api/composer.lock
    packages/mollie-api/vendor/
    packages/mollie-api/src/
    packages/mollie-api/tests/
  </files>
  <action>
Maak in de sub-repo de lege namespace-roots aan (deze raken in plan 02-02..02-07 gevuld):

```bash
cd packages/mollie-api
mkdir -p src tests config
```

Run `composer install` in de sub-repo om vendor + composer.lock te genereren:

```bash
cd packages/mollie-api
composer install --no-interaction --prefer-dist
```

Dit moet:
- `vendor/mollie/mollie-api-php/` aanmaken
- `vendor/spatie/laravel-package-tools/` aanmaken
- `vendor/pestphp/pest/` aanmaken (require-dev)
- `vendor/orchestra/testbench/` aanmaken
- `composer.lock` schrijven (gitignored — komt niet in commits)

W-2 revision: omdat composer.json in dit plan GEEN `post-autoload-dump: @composer run prepare` script bevat, draait Testbench's `package:discover` NIET en is er dus ook geen risico dat het faalt op een nog-niet-bestaande `MollieServiceProvider`. Geen stub-fallback nodig — de install is deterministisch groen. Plan 02-04 voegt het `prepare`-script toe wanneer de SP concreet is.

Verifieer autoload-mapping: `composer dump-autoload -o` moet zonder errors lopen.

Verifieer dat Mollie-lib geïnstalleerd is en de juiste minor heeft:

```bash
cd packages/mollie-api
composer show mollie/mollie-api-php | head -3
```

Resultaat moet versions 3.11.* tonen.

B-3 reminder: `composer.lock` is NU geschreven naar disk maar staat in `.gitignore` (eerste regel) — controleer met:

```bash
cd packages/mollie-api
git check-ignore -v composer.lock     # moet 1 regel uitvoer geven die .gitignore noemt
```
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; [ -d vendor/mollie/mollie-api-php ] &amp;&amp; [ -f composer.lock ] &amp;&amp; [ -d src ] &amp;&amp; [ -d tests ] &amp;&amp; git check-ignore composer.lock &gt;/dev/null &amp;&amp; composer show mollie/mollie-api-php 2&gt;/dev/null | head -1 | grep -q '^name *: *mollie/mollie-api-php$' &amp;&amp; composer dump-autoload -o --no-interaction 2&gt;&amp;1 | grep -qiE 'generated|files'</automated>
  </verify>
  <done>`vendor/mollie/mollie-api-php/` bestaat en de versie is ^3.11. `composer.lock` is geschreven en correct gitignored. `src/` en `tests/` directories bestaan (leeg — geen stub-SP). `composer dump-autoload` slaagt. Install was deterministisch zonder fallback-pad.</done>
</task>

</tasks>

<verification>
- `cd packages/mollie-api && composer install` slaagt zonder errors
- `cd packages/mollie-api && composer show mollie/mollie-api-php` toont versie 3.11.*
- composer.json claimt namespace `Emeq\MollieApi\` → src/ en auto-discovery voor `Emeq\MollieApi\MollieServiceProvider` + alias `Mollie`
- composer.json bevat GEEN `post-autoload-dump`/`prepare` scripts (komt in 02-04)
- Skeleton-metadata bestanden (`.gitignore`, `.gitattributes`, `.editorconfig`, `pint.json`, `phpstan.neon.dist`, `phpunit.xml.dist`, `LICENSE.md`, `CHANGELOG.md`, `README.md`) mirroren Snelstart-SDK 1-op-1 (qua structuur — content is Mollie-specifiek)
- `.gitignore` heeft `composer.lock` als entry (`grep -q '^composer.lock$' .gitignore`)
- `composer.lock` is gegenereerd op disk maar `git check-ignore composer.lock` bevestigt dat het gitignored is
- `config/mollie.php` heeft `enforce_environment` / `http` / `idempotency` keys + `idempotency.generator` docblock documenteert FQCN OR container alias
- Working tree is op feature-branch `feat/foundation`, niet op `master` of `main`
</verification>

<success_criteria>
- Package is composer-installable in isolation (`cd packages/mollie-api && composer install` werkt deterministisch — geen stub-fallback nodig)
- `vendor/mollie/mollie-api-php/` is geïnstalleerd
- Geen `src/*.php` of `tests/*.php` files (komen in 02-02..02-07)
- `composer.lock` is gegenereerd maar gitignored — wordt NIET gestaged in plan 02-08
- Sub-repo working tree clean genoeg om in plan 02-08 in één commit te stagen samen met de outputs van 02-02..02-07
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-01-SUMMARY.md`
</output>
</content>
</invoke>