---
phase: 02-emeq-mollie-api-foundation
plan: 07
type: execute
wave: 6
depends_on:
  - 02-01
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - packages/mollie-api/tests/Unit/ErrorMappingTest.php
  - packages/mollie-api/.mollie-fake-fqcn-notes.md
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "Task 1 inspecteert vendor/mollie/mollie-api-php/ en schrijft de gevonden FQCN's voor Mollie's mock-/fake-classes naar .mollie-fake-fqcn-notes.md — geen fictieve namespaces in tests (B-4)"
    - "ErrorMappingTest valideert dat Mollie's eigen ValidationException::getField() correct werkt via Mollie's fake-mechanisme met een 422-response (FQCN per Task 1's notes)"
    - "ErrorMappingTest valideert dat een outgoing Mollie-request via ONZE wiring (app(Mollie::class)->client()) een Authorization header met de exact resolved apiKey draagt (B-5 — niet via Mollie's direct fake, maar via onze factory)"
    - "Beide tests draaien zonder echte HTTP — gebruiken Mollie's fake/mock mechanisme dat in Task 1 is geverifieerd"
    - "cd packages/mollie-api && ./vendor/bin/pest --filter=ErrorMappingTest slaagt met exit 0"
  artifacts:
    - path: "packages/mollie-api/.mollie-fake-fqcn-notes.md"
      provides: "Geïnspecteerde, exacte FQCN's voor Mollie's mock/fake-classes — eindigt in gitignore (notes file)"
      contains: "vendor inspection"
    - path: "packages/mollie-api/tests/Unit/ErrorMappingTest.php"
      provides: "Integratie-tests via Mollie's fake-mechanisme voor error-mapping + auth-header via onze wiring"
      contains: "MollieApiClient"
  key_links:
    - from: "tests/Unit/ErrorMappingTest.php"
      to: "src/Mollie.php"
      via: "test gebruikt onze app(Mollie::class)->client() voor de Bearer-header check (B-5)"
      pattern: "app(Mollie::class)"
    - from: "tests/Unit/ErrorMappingTest.php"
      to: "vendor/mollie/mollie-api-php (Fake/Mock classes per Task 1 notes)"
      via: "Mollie's eigen fake-mechanisme — exacte FQCN gevalideerd in Task 1"
      pattern: "ValidationException"
---

<objective>
Verifieer end-to-end dat `Mollie::client()` correct integreert met `mollie/mollie-api-php` zelf: (1) een 422-response geeft een `Mollie\Api\Exceptions\ValidationException` met werkende `getField()`, en (2) een outgoing HTTP request via ONZE `Mollie::client()` factory bevat de juiste `Authorization`-header met de resolved apiKey. Beide tests gebruiken Mollie's eigen fake-mechanisme — geen echte HTTP. Sluit ROADMAP Phase 2 success criteria 4 + 5 af.

Revisions:
- B-4: GEEN aannames meer over fictieve namespaces als `Mollie\Api\Fake\MockMollieClient` of `MockResponse`. Task 1 (NIEUW) inspecteert vendor/ en schrijft de exacte FQCN's naar een notes-file; Task 2 schrijft de tests met die FQCN's.
- B-5: De Bearer-header-test gaat door ONZE wiring (`app(Mollie::class)->client()`) — niet alleen Mollie's eigen `fake()`. Pas de test aan op basis van of Mollie's fake() instance-method is OF static (uit Task 1's notes).
- B-1 + W-5: Serialize. Wave 5. depends_on = 02-01, 02-04, 02-05, 02-06 (na 02-06 om pest-cache-collision te voorkomen — W-5).

Output: 2 tasks; 1 test-file met 2 tests + 1 notes-file. Eindigt de Pest-suite op ≥25 tests groen (drempel ROADMAP ≥10 ruim overschreden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
Outputs van plan 02-04 + 02-05 + 02-06:
  Emeq\MollieApi\Mollie::client() — onze factory
  Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver — test-double
  packages/mollie-api/tests/Unit/MollieTest.php — assertions die zelf al Mollie's authenticator-introspect lazen

Mollie-library API — TE VERIFIËREN IN TASK 1:
  - Welke namespaces/classes biedt Mollie aan voor fake/mock-driven tests?
    Mogelijke candidates (vermoeden — verifiëren!):
      Mollie\Api\Fake\MockMollieClient        (??)
      Mollie\Api\Fake\MockResponse            (??)
      Mollie\Api\Testing\…                    (??)
    Bevestig via:
      ls vendor/mollie/mollie-api-php/src/Fake/ 2>/dev/null
      ls vendor/mollie/mollie-api-php/src/Testing/ 2>/dev/null
      grep -rn 'class Mock' vendor/mollie/mollie-api-php/src/
      grep -rn 'public static function fake' vendor/mollie/mollie-api-php/src/

  - Is `MollieApiClient::fake()` instance-method, static, of bestaat het überhaupt?
    grep -rn 'public static function fake' vendor/mollie/mollie-api-php/src/MollieApiClient.php
    grep -rn 'public function fake' vendor/mollie/mollie-api-php/src/MollieApiClient.php

  - Welke authenticator class wordt door setApiKey() geïnstalleerd?
    grep -rn 'class ApiKeyAuthenticator' vendor/mollie/mollie-api-php/src/
    grep -rn 'isTestToken' vendor/mollie/mollie-api-php/src/

  - ValidationException::getField():
    grep -rn 'function getField' vendor/mollie/mollie-api-php/src/Exceptions/

Task 1 schrijft alle bevindingen naar packages/mollie-api/.mollie-fake-fqcn-notes.md. Task 2 schrijft de testfile met die FQCN's.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect installed mollie/mollie-api-php voor mock/fake-class FQCN's + auth-mechanisme</name>
  <read_first>
    - packages/mollie-api/vendor/mollie/mollie-api-php/src/MollieApiClient.php (search voor `fake(`, `getAuthenticator`, `setApiKey`)
    - packages/mollie-api/vendor/mollie/mollie-api-php/src/Exceptions/ValidationException.php (search voor `getField`)
  </read_first>
  <files>
    packages/mollie-api/.mollie-fake-fqcn-notes.md
  </files>
  <action>
Doe een read-only inspection van de geïnstalleerde mollie/mollie-api-php library om de exacte class-namen en method-signatures vast te leggen. Output is een notes-bestand dat Task 2 gebruikt.

```bash
cd packages/mollie-api

# 1. Welke top-level directories heeft de lib?
ls vendor/mollie/mollie-api-php/src/

# 2. Bestaat er een Fake/ of Testing/ directory?
ls vendor/mollie/mollie-api-php/src/Fake/   2>/dev/null
ls vendor/mollie/mollie-api-php/src/Testing/ 2>/dev/null

# 3. Mock-/Fake-classes
grep -rn 'class Mock'              vendor/mollie/mollie-api-php/src/ | head -20
grep -rn 'class Fake'              vendor/mollie/mollie-api-php/src/ | head -20

# 4. fake() entrypoint
grep -rn 'public static function fake' vendor/mollie/mollie-api-php/src/MollieApiClient.php
grep -rn 'public function fake'        vendor/mollie/mollie-api-php/src/MollieApiClient.php

# 5. Authenticator + isTestToken
grep -rn 'class ApiKeyAuthenticator'   vendor/mollie/mollie-api-php/src/
grep -rn 'isTestToken'                 vendor/mollie/mollie-api-php/src/ | head -10

# 6. ValidationException::getField
grep -rn 'function getField'           vendor/mollie/mollie-api-php/src/Exceptions/

# 7. assertSent / request-capture mechanisme
grep -rn 'function assertSent'         vendor/mollie/mollie-api-php/src/ | head -10

# 8. Hoe queue je een fake response?
grep -rn 'function addResponse'        vendor/mollie/mollie-api-php/src/ | head -10
grep -rn 'expectedResponses'           vendor/mollie/mollie-api-php/src/ | head -10
```

Verzamel alle uitvoer in `packages/mollie-api/.mollie-fake-fqcn-notes.md` met de volgende structuur:

```markdown
# Mollie fake/mock FQCN notes — Phase 2 plan 02-07

Geïnspecteerd: <timestamp via `date -u`>
Installed package version: <output van `composer show mollie/mollie-api-php | head -3`>

## Top-level src/ directories
<output van `ls vendor/.../src/`>

## Fake/Testing directories
- Fake/   : <bestaat? ja/nee + ls>
- Testing/: <bestaat? ja/nee + ls>

## Mock/Fake class FQCN's
<de exacte `namespace ...` + `class ...` regels die grep heeft gevonden>

## fake() entrypoint
- Method: <static OF instance>
- Signature: <exacte regel>
- Return-type: <FQCN van wat het teruggeeft>

## Authenticator + isTestToken
- ApiKeyAuthenticator FQCN: <namespace + class>
- isTestToken(): <signature; bestaat ja/nee>

## ValidationException::getField()
- Signature: <regel>

## Request-capture
- assertSent(callable $cb): <FQCN waar gedefinieerd>
- Hoe queue je responses: <addResponse / via expectedResponses ctor-arg>

## Conclusie voor Task 2
Test 1 (ValidationException via fake):
  - Gebruik <FQCN voor MockClient> + <FQCN voor MockResponse>
  - Of: <alternatief mechanisme>

Test 2 (Bearer via ONZE wiring):
  - $client = app(Mollie::class)->client();
  - Mollie's fake() is <instance-method | static-method>
  - Strategie:
    <indien instance>: $client->fake([...]); $client->payments->...; $client->assertSent(...)
    <indien static>:   MollieApiClient::fake([...]); ... (maar dat raakt ONS $client niet, dus B-5 vereist een ander pad: bv. assert op $client->getAuthenticator()->getApiKey() OF instanceof ApiKeyAuthenticator + isTestToken)
```

Belangrijk:
- Dit is een READ-ONLY inspection task — geen vendor-files muteren, geen tests schrijven.
- De notes-file is een eenmalig artefact dat Task 2 voedt. Op disk laten (gitignored via SDK-gitignore's `*.notes.md`-patroon? — controleer; zo niet, voeg `*.notes.md` toe aan `.gitignore` van de sub-repo).
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; [ -f .mollie-fake-fqcn-notes.md ] &amp;&amp; grep -qi 'fake.*entrypoint\|Mock/Fake class\|fake()' .mollie-fake-fqcn-notes.md &amp;&amp; grep -q 'Authenticator' .mollie-fake-fqcn-notes.md &amp;&amp; grep -q 'getField' .mollie-fake-fqcn-notes.md &amp;&amp; grep -q 'Conclusie voor Task 2' .mollie-fake-fqcn-notes.md</automated>
  </verify>
  <done>.mollie-fake-fqcn-notes.md bestaat in packages/mollie-api/ met exact opgesomde FQCN's voor Mollie's mock/fake-classes, fake() signature, authenticator-class, ValidationException::getField() bevestiging, en een "Conclusie voor Task 2" sectie die expliciet de import-paths én test-strategie voorschrijft. Geen fictieve namespaces — alles vendor-geverifieerd.</done>
</task>

<task type="auto">
  <name>Task 2: ErrorMappingTest — ValidationException + Bearer via ONZE wiring (B-4 + B-5)</name>
  <read_first>
    - packages/mollie-api/.mollie-fake-fqcn-notes.md (Task 1's output — bevat de EXACTE FQCN's en method-signatures voor deze test)
    - packages/mollie-api/src/Mollie.php
    - packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php
  </read_first>
  <files>
    packages/mollie-api/tests/Unit/ErrorMappingTest.php
  </files>
  <behavior>
    - Test 1 (ValidationException): Een 422-fake-response leidt tot een ValidationException waarvan getField() de juiste veldnaam (bv. 'amount.value') geeft. Gebruik de Mock/Fake FQCN's uit Task 1's notes.
    - Test 2 (Bearer via ONZE wiring): Vraag $client via app(Mollie::class)->client() (= onze factory). Bind een resolver met een specifieke apiKey. Verifieer dat:
        - Indien Mollie's fake() instance-method is op $client: queue een fake response, doe een $client-call (bv. methods->allActive of payments->all), en assertSent dat de outgoing Authorization-header `Bearer <apiKey>` is.
        - Indien Mollie's fake() static is (en daarmee een aparte mock-client teruggeeft die ONS $client NIET raakt): test in plaats daarvan dat $client->getAuthenticator() instanceof <ApiKeyAuthenticator-FQCN uit notes> EN dat ofwel isTestToken() of een getter de juiste prefix bevestigt — bewijs van wiring zonder Mollie's fake-bypass.
  </behavior>
  <action>
Open `.mollie-fake-fqcn-notes.md` en lees de "Conclusie voor Task 2" sectie. Schrijf vervolgens `packages/mollie-api/tests/Unit/ErrorMappingTest.php` op basis van die conclusie. De onderstaande code is een TEMPLATE met twee invul-spots (<FAKE_NS_TBD> + <STRATEGY_TBD>); vervang die op basis van Task 1's notes.

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Mollie;
use Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver;
use Mollie\Api\Exceptions\ValidationException;
use Mollie\Api\MollieApiClient;
// === FILL FROM NOTES (B-4): ===
// use <FAKE_MOCK_CLIENT_FQCN>;
// use <FAKE_MOCK_RESPONSE_FQCN>;
// use <API_KEY_AUTHENTICATOR_FQCN>;
use Psr\Http\Message\RequestInterface;

/**
 * NB: De exacte FQCN's voor Mollie's mock/fake-classes worden in Task 1
 * van plan 02-07 geverifieerd en in packages/mollie-api/.mollie-fake-fqcn-notes.md
 * vastgelegd. Vervang de placeholders hieronder met die FQCN's.
 *
 * Indien de geïnspecteerde versie van mollie/mollie-api-php geen instance-method
 * `$client->fake()` blijkt te hebben, gebruik dan de fallback-strategie uit
 * Task 1's "Conclusie": authenticator-instanceof + isTestToken in plaats van
 * een outgoing-header-assert.
 */

it('surfaces Mollie\\Api\\Exceptions\\ValidationException with a usable getField() on a 422 response', function (): void {
    // === FILL FROM NOTES: Mollie's fake-mechanisme voor een 422 ===
    // Voorbeeld (placeholder — vervang op basis van .mollie-fake-fqcn-notes.md):
    //
    // $mock = MollieApiClient::fake([
    //     new <MockResponseFQCN>(422, [
    //         'status' => 422,
    //         'title'  => 'Unprocessable Entity',
    //         'detail' => 'The amount.value field is required.',
    //         'field'  => 'amount.value',
    //     ]),
    // ]);
    //
    // (Of, als fake() instance-method is:
    //    $mock = new MollieApiClient();
    //    $mock->fake([...]);
    //  )

    $caught = null;
    try {
        $mock->payments->create([
            'amount'      => ['currency' => 'EUR'], // intentionally missing 'value'
            'description' => 'test',
            'redirectUrl' => 'https://example.test/return',
        ]);
    } catch (ValidationException $e) {
        $caught = $e;
    }

    expect($caught)->toBeInstanceOf(ValidationException::class)
        ->and($caught->getField())->toBe('amount.value');
});

it('sends Authorization: Bearer <apiKey> on outgoing requests when wired via our Mollie facade (B-5)', function (): void {
    // 1. Bind ONZE resolver met een specifieke test-key.
    $this->app->bind(
        MollieCredentialResolver::class,
        fn () => FakeMollieCredentialResolver::withApiKey('test_bearer_check'),
    );
    $this->app->forgetInstance(Mollie::class);

    // 2. Vraag een client via ONZE factory.
    $client = app(Mollie::class)->client();

    expect($client)->toBeInstanceOf(MollieApiClient::class)
        ->and($client->getAuthenticator())->not->toBeNull();

    // === FILL FROM NOTES (B-5): twee paden mogelijk ===
    //
    // Pad A — Mollie's fake() is INSTANCE-METHOD op MollieApiClient:
    //   $client->fake([...] /* queue 200 response */);
    //   $client->methods->allActive();   // cheapest GET in Mollie's API
    //   $client->assertSent(function (RequestInterface $request): bool {
    //       return 'Bearer test_bearer_check' === $request->getHeaderLine('Authorization');
    //   });
    //
    // Pad B — Mollie's fake() is STATIC en raakt onze $client NIET:
    //   expect($client->getAuthenticator())
    //       ->toBeInstanceOf(<API_KEY_AUTHENTICATOR_FQCN>)
    //       ->and($client->getAuthenticator()->isTestToken())->toBeTrue();
    //
    //   (Dit is een zwakkere maar voldoende assertion: het bewijst dat onze
    //    factory de juiste authenticator heeft geïnstalleerd; Mollie's eigen
    //    test-suite dekt de Bearer-header-conversie in zijn vendor-tests.)
});
```

Belangrijk (B-5 invulling):
- De executor VULT IN op basis van wat Task 1 heeft vastgelegd. Het is GEEN keuze van de executor in deze taak — Task 1 schrijft expliciet welk pad (A of B) op de geïnstalleerde versie van toepassing is.
- Beide paden moeten exit-0 opleveren. Strategie B is een legitieme fallback omdat het de wiring van ONZE factory bewijst — wat de B-5 blocker primair eist.

Run om groen te krijgen:

```bash
cd packages/mollie-api
./vendor/bin/pest --filter='ErrorMappingTest'
```
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; [ -f tests/Unit/ErrorMappingTest.php ] &amp;&amp; grep -q 'ValidationException' tests/Unit/ErrorMappingTest.php &amp;&amp; grep -q 'getField' tests/Unit/ErrorMappingTest.php &amp;&amp; grep -q 'app(Mollie::class)-&gt;client()' tests/Unit/ErrorMappingTest.php &amp;&amp; grep -q 'test_bearer_check' tests/Unit/ErrorMappingTest.php &amp;&amp; grep -q 'forgetInstance(Mollie::class)' tests/Unit/ErrorMappingTest.php &amp;&amp; ./vendor/bin/pest --filter='ErrorMappingTest' 2&gt;&amp;1 | tail -10 | grep -qE 'PASS|passed'</automated>
  </verify>
  <done>2 tests in ErrorMappingTest.php: ValidationException::getField() werkt via een 422-fake-response (FQCN's per Task 1 notes — geen aannames), en de Bearer-header / authenticator-state wordt geverifieerd via ONZE factory `app(Mollie::class)->client()` (B-5). Indien Mollie's fake() static is, gebruikt de test de authenticator-introspect fallback uit Task 1's conclusie. Geen fictieve namespaces.</done>
</task>

</tasks>

<verification>
- .mollie-fake-fqcn-notes.md bestaat met "Conclusie voor Task 2"
- grep `app(Mollie::class)->client()` in tests/Unit/ErrorMappingTest.php → ≥1 hit (B-5)
- grep `forgetInstance(Mollie::class)` in tests/Unit/ErrorMappingTest.php → 1 hit
- grep `getField` in tests/Unit/ErrorMappingTest.php → 1 hit
- grep `ValidationException` in tests/Unit/ErrorMappingTest.php → ≥1 hit
- Geen `Mollie\Api\Fake\` placeholder als import indien Task 1 anders heeft bevestigd
- `./vendor/bin/pest --filter=ErrorMappingTest` exits 0 vanaf packages/mollie-api/
- `./vendor/bin/pest` (zonder filter) toont ≥25 passed
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 4 ("Pest-suite groen met ≥10 cases over auth-laag, credential-resolver, en error-mapping") is afgevinkt
- B-4: alle imports in ErrorMappingTest.php komen van bestaande, in vendor/ geverifieerde class-paths (per Task 1's notes)
- B-5: de Bearer/authenticator-assertion exerciseert ONZE wiring (app(Mollie::class)->client()), niet alleen Mollie's directe fake
- Tests gebruiken geen netwerk-calls — alle responses zijn fake/mock
- W-5: dit plan staat in Wave 5, depends_on 02-06 — pest-cache-collision vermeden
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-07-SUMMARY.md`
</output>
</content>
</invoke>