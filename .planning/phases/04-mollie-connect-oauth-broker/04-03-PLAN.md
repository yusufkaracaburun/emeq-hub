---
phase: 04-mollie-connect-oauth-broker
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - app/Mollie/MollieConnectionContext.php
  - app/Mollie/HubMollieCredentialResolver.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Mollie/HubMollieCredentialResolverTest.php
  - composer.json
  - composer.lock
autonomous: true
requirements: [MOLL-02]
tags:
  - laravel
  - mollie
  - sdk-binding
  - phpunit

must_haves:
  truths:
    - "MollieConnectionContext is een scoped (per-request) singleton met set(Connection) + current(): Connection + has(): bool"
    - "HubMollieCredentialResolver implementeert Emeq\\MollieApi\\Contracts\\MollieCredentialResolver"
    - "HubMollieCredentialResolver::resolve() doet lazy-refresh als expires_at < now()+5min via OAuthFlowRegistry::for('mollie')->refreshToken() — D-04 + D-06"
    - "HubMollieCredentialResolver returnt MollieOAuthCredentials met current access_token + expires_at-timestamp"
    - "AppServiceProvider bindt MollieConnectionContext (scoped) + MollieCredentialResolver → HubMollieCredentialResolver (bind) — D-16"
    - "emeq/mollie-api package is daadwerkelijk geïnstalleerd via composer (VCS of path-fallback) en class-resolvable"
    - "Phase 5a kan Mollie::client() rechtstreeks aanroepen zodra een MollieConnectionContext->set() is gedaan"
  artifacts:
    - path: "app/Mollie/MollieConnectionContext.php"
      provides: "Per-request current-Connection service (scoped binding)"
      contains: "final class MollieConnectionContext"
    - path: "app/Mollie/HubMollieCredentialResolver.php"
      provides: "Bridge tussen Hub Connection-model en Emeq\\MollieApi\\Contracts\\MollieCredentialResolver"
      contains: "implements MollieCredentialResolver"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "scoped(MollieConnectionContext) + bind(MollieCredentialResolver, HubMollieCredentialResolver)"
      contains: "MollieConnectionContext::class"
    - path: "tests/Feature/Mollie/HubMollieCredentialResolverTest.php"
      provides: "Coverage voor 3 paden: fresh-token, near-expiry-refresh, no-context-throws"
      contains: "MollieConnectionContext"
    - path: "composer.json"
      provides: "emeq/mollie-api dependency + VCS- of path-repository entry"
      contains: "emeq/mollie-api"
  key_links:
    - from: "HubMollieCredentialResolver::resolve"
      to: "MollieConnectionContext::current"
      via: "constructor-injection"
      pattern: "MollieConnectionContext"
    - from: "HubMollieCredentialResolver::resolve (near-expiry)"
      to: "OAuthFlowRegistry::for('mollie')->refreshToken"
      via: "5min-window check (D-06)"
      pattern: "->refreshToken"
    - from: "Emeq\\MollieApi\\Contracts\\MollieCredentialResolver"
      to: "HubMollieCredentialResolver"
      via: "$this->app->bind"
      pattern: "MollieCredentialResolver::class"
---

<objective>
SDK-binding-laag (D-16): bindt `Emeq\MollieApi\Contracts\MollieCredentialResolver` aan een Hub-implementatie die op een current-Connection leest uit een per-request `MollieConnectionContext`. De lazy-refresh-laag (D-04/D-06) zit hierin: vóór resolve checkt de resolver `expires_at < now()+5min` en delegeert dan naar `OAuthFlowRegistry::for('mollie')->refreshToken()` (Plan 04-02 implementatie).

Purpose: Phase 5a's pass-through-controllers willen direct `Mollie::client()` aanroepen zonder zelf credentials-wiring te doen. Deze plan-laag is daarvoor de fundering. Zonder deze laag zou Phase 5a stranden bij `MissingCredentialResolverException`. CONTEXT.md D-16 expliciet: "bindt al in Phase 4 (niet Phase 5a)".

Output: 2 nieuwe files (MollieConnectionContext + HubMollieCredentialResolver) + 1 nieuwe test + 1 modified file (AppServiceProvider — extra bindings naast OAuthFlowRegistry uit Plan 04-02) + composer.json/composer.lock mutaties als emeq/mollie-api nog niet geïnstalleerd is.

**Note over Phase 2 dependency:** Dit plan importeert `Emeq\MollieApi\Contracts\MollieCredentialResolver` + `Emeq\MollieApi\Data\MollieOAuthCredentials`. Die types staan in `packages/mollie-api/src/` maar `emeq/mollie-api` is **nog niet via composer geïnstalleerd** (Phase 2 niet shipped). De installatie gebeurt in Task 1 (Wave 0 step) vóór de file-writes — zie acceptance criteria voor verificatie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md
@.planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md
@.planning/phases/04-mollie-connect-oauth-broker/04-02-PLAN.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@app/Providers/AppServiceProvider.php
@app/Models/Connection.php
@packages/mollie-api/src/Contracts/MollieCredentialResolver.php
@packages/mollie-api/src/Data/MollieOAuthCredentials.php

<interfaces>
<!-- SDK-laag contracten die Plan 04-03 gaat implementeren -->

From packages/mollie-api/src/Contracts/MollieCredentialResolver.php:
```php
namespace Emeq\MollieApi\Contracts;

interface MollieCredentialResolver {
    public function resolve(): \Emeq\MollieApi\Data\MollieCredentials;
}
```

From packages/mollie-api/src/Data/MollieOAuthCredentials.php (constructor valideert 'access_' prefix):
```php
namespace Emeq\MollieApi\Data;

final class MollieOAuthCredentials extends MollieCredentials {
    public function __construct(
        public readonly string $accessToken,
        public readonly ?int $expiresAt = null,
    ) {
        if (!str_starts_with($accessToken, 'access_')) {
            throw new \InvalidArgumentException(...);
        }
    }
}
```

From Plan 04-02 (already shipped):
```php
namespace App\OAuth;
final class OAuthFlowRegistry {
    public function for(string $provider): OAuthFlow;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: composer install emeq/mollie-api + MollieConnectionContext + HubMollieCredentialResolver + AppServiceProvider-bindings</name>
  <files>
    composer.json
    composer.lock
    app/Mollie/MollieConnectionContext.php
    app/Mollie/HubMollieCredentialResolver.php
    app/Providers/AppServiceProvider.php
  </files>
  <read_first>
    .planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md (sections: HubMollieCredentialResolver, MollieConnectionContext, AppServiceProvider — D-16 binding)
    .planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md (D-04, D-06, D-16)
    packages/mollie-api/src/Contracts/MollieCredentialResolver.php (interface that Hub implements)
    packages/mollie-api/src/Data/MollieOAuthCredentials.php (DTO + 'access_' prefix-validation)
    app/Providers/AppServiceProvider.php (huidige register() — Plan 04-02 voegde al OAuthFlowRegistry-binding toe; deze plan voegt 2 extra bindings toe)
    .ai/rules/engineering.md (geen Hub-domeinmodellen importeren in SDK-grens — Connection NIET direct in resolver, alleen via context)
    .ai/packages.md (composer-conventie: VCS-repo primair, path-fallback dev-only)
  </read_first>
  <action>
    **STEP 0 (BLOCKING WAVE-0) — composer-dependency-install met expliciete verificatie:**

    Voer in volgorde uit en log elke stap in SUMMARY:

    1. Check huidige staat: `composer show emeq/mollie-api 2>/dev/null && echo INSTALLED || echo MISSING`.
    2. **Als INSTALLED:** ga naar Step 1 (file-writes).
    3. **Als MISSING:** probeer VCS-install:
       ```bash
       composer config repositories.emeq-mollie-api '{"type":"vcs","url":"git@github.com:yusufkaracaburun/emeq-mollie-api.git"}' --no-interaction
       composer require emeq/mollie-api:dev-master --no-interaction
       ```
    4. **Als VCS-install faalt** (repo leeg per STATE.md / netwerk-issue): fallback naar path-repository:
       ```bash
       composer config repositories.emeq-mollie-api '{"type":"path","url":"packages/mollie-api"}' --no-interaction
       composer require 'emeq/mollie-api:*' --no-interaction
       ```
       Documenteer in SUMMARY dat path-fallback gebruikt is — dit is dev-only (zie `.ai/packages.md`).

    **Post-install assertion (BLOCKING):**
    - `composer show emeq/mollie-api` exits 0 (anders: STOP, geen file-writes, rapporteer faal).
    - `php -r "require 'vendor/autoload.php'; new ReflectionClass(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class); echo 'RESOLVED';"` outputs `RESOLVED` (anders: STOP).
    - `php -r "require 'vendor/autoload.php'; new ReflectionClass(\Emeq\MollieApi\Data\MollieOAuthCredentials::class); echo 'RESOLVED';"` outputs `RESOLVED` (anders: STOP).

    Composer-mutaties (composer.json + composer.lock) zijn **expected** bij VCS- of path-fallback en moeten in dezelfde commit als de file-writes meegaan (CLAUDE.md packages-conventie: "Commit `composer.lock` in de Hub").

    ---

    **STEP 1 — file-writes (alleen uitvoeren als Step 0 RESOLVED'd):**

    Maak 2 files aan en wijzig 1 file.

    **1. app/Mollie/MollieConnectionContext.php** (nieuw) — Per PATTERNS.md §"MollieConnectionContext" copy-pattern (regels 454-490). Namespace `App\Mollie`. Hub-tree → géén `declare(strict_types=1)`. Inhoud:

    ```php
    <?php

    namespace App\Mollie;

    use App\Models\Connection;
    use RuntimeException;

    final class MollieConnectionContext
    {
        private ?Connection $connection = null;

        public function set(Connection $connection): void
        {
            $this->connection = $connection;
        }

        public function current(): Connection
        {
            if ($this->connection === null) {
                throw new RuntimeException(
                    'MollieConnectionContext: geen current Connection gezet. '
                    .'Roep set() aan voordat HubMollieCredentialResolver wordt aangeroepen.'
                );
            }

            return $this->connection;
        }

        public function has(): bool
        {
            return $this->connection !== null;
        }
    }
    ```

    **2. app/Mollie/HubMollieCredentialResolver.php** (nieuw) — Per PATTERNS.md §"HubMollieCredentialResolver" copy-pattern (regels 398-431). Namespace `App\Mollie`. Hub-tree → géén `declare(strict_types=1)`. Inhoud:

    ```php
    <?php

    namespace App\Mollie;

    use App\OAuth\OAuthFlowRegistry;
    use Emeq\MollieApi\Contracts\MollieCredentialResolver;
    use Emeq\MollieApi\Data\MollieCredentials;
    use Emeq\MollieApi\Data\MollieOAuthCredentials;

    final class HubMollieCredentialResolver implements MollieCredentialResolver
    {
        public function __construct(
            private readonly MollieConnectionContext $context,
            private readonly OAuthFlowRegistry $registry,
        ) {}

        public function resolve(): MollieCredentials
        {
            $connection = $this->context->current();

            // Lazy refresh (D-04 + D-06): expires_at < now()+5min → refresh via Mollie OAuth flow
            if ($connection->expires_at && $connection->expires_at->lt(now()->addMinutes(5))) {
                $connection = $this->registry->for('mollie')->refreshToken($connection);
            }

            return new MollieOAuthCredentials(
                accessToken: $connection->access_token,
                expiresAt: $connection->expires_at?->getTimestamp(),
            );
        }
    }
    ```

    **LANDMINES** (PATTERNS.md regel 443-445):
    - `MollieOAuthCredentials` constructor valideert `access_`-prefix — fake-tokens in tests moeten `access_test_…` heten.
    - Géén direct `Connection`-import in deze laag's interface (resolver implementeert SDK-contract; Hub-domeinmodellen leven achter de Context). Concrete impl mag uiteraard via Context wel Connection-objecten lezen.

    **3. app/Providers/AppServiceProvider.php** (modify) — Plan 04-02 heeft al `OAuthFlowRegistry`-binding in `register()` toegevoegd. Voeg **toe** (niet vervang): scoped-binding voor `MollieConnectionContext` + bind voor SDK-contract. Definitieve `register()`-shape:

    ```php
    public function register(): void
    {
        $this->app->scoped(\App\Mollie\MollieConnectionContext::class);

        $this->app->singleton(\App\OAuth\OAuthFlowRegistry::class, function (\Illuminate\Contracts\Foundation\Application $app) {
            $registry = new \App\OAuth\OAuthFlowRegistry($app);
            $registry->register('mollie', \App\OAuth\Mollie\MollieConnectOAuthFlow::class);

            return $registry;
        });

        $this->app->bind(
            \Emeq\MollieApi\Contracts\MollieCredentialResolver::class,
            \App\Mollie\HubMollieCredentialResolver::class,
        );
    }
    ```

    **LANDMINES** (PATTERNS.md regel 927-931):
    - `scoped()` (niet `singleton()`) voor `MollieConnectionContext` — Laravel maakt 'm vers per request/job; voorkomt cross-request lekkage.
    - `register()` NOOIT facades aanroepen (Laravel rule). Alleen container-bindings.
    - `boot()` blijft ongewijzigd (Gate + RateLimiter + Scramble).
  </action>
  <verify>
    <automated>composer show emeq/mollie-api &amp;&amp; php -r "require 'vendor/autoload.php'; new ReflectionClass(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class); new ReflectionClass(\Emeq\MollieApi\Data\MollieOAuthCredentials::class); echo 'OK';" &amp;&amp; php artisan test --compact</automated>
  </verify>
  <acceptance_criteria>
    - `composer show emeq/mollie-api` exits 0 (package is geïnstalleerd via VCS of path-fallback)
    - `php -r "require 'vendor/autoload.php'; new ReflectionClass(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class); echo 'OK';"` outputs `OK` (class is autoloadable)
    - `php -r "require 'vendor/autoload.php'; new ReflectionClass(\Emeq\MollieApi\Data\MollieOAuthCredentials::class); echo 'OK';"` outputs `OK`
    - `grep -q "emeq/mollie-api" composer.json` exits 0 (require-entry aanwezig)
    - `grep -q "emeq/mollie-api" composer.lock` exits 0 (lock-entry aanwezig)
    - File `app/Mollie/MollieConnectionContext.php` bestaat
    - `grep -q "final class MollieConnectionContext" app/Mollie/MollieConnectionContext.php` exits 0
    - `grep -q "public function set(Connection " app/Mollie/MollieConnectionContext.php` exits 0
    - `grep -q "public function current(): Connection" app/Mollie/MollieConnectionContext.php` exits 0
    - `grep -q "public function has(): bool" app/Mollie/MollieConnectionContext.php` exits 0
    - File `app/Mollie/HubMollieCredentialResolver.php` bestaat
    - `grep -q "implements MollieCredentialResolver" app/Mollie/HubMollieCredentialResolver.php` exits 0
    - `grep -q "MollieConnectionContext" app/Mollie/HubMollieCredentialResolver.php` exits 0
    - `grep -q "OAuthFlowRegistry" app/Mollie/HubMollieCredentialResolver.php` exits 0
    - `grep -q "addMinutes(5)" app/Mollie/HubMollieCredentialResolver.php` exits 0
    - `grep -q "MollieOAuthCredentials" app/Mollie/HubMollieCredentialResolver.php` exits 0
    - `grep -q "->scoped(.App.Mollie.MollieConnectionContext" app/Providers/AppServiceProvider.php` exits 0
    - `grep -q "->bind(" app/Providers/AppServiceProvider.php` exits 0
    - `grep -q "MollieCredentialResolver::class" app/Providers/AppServiceProvider.php` exits 0
    - `php artisan tinker --execute 'echo get_class(app(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class));'` outputs `App\Mollie\HubMollieCredentialResolver`
  </acceptance_criteria>
  <done>emeq/mollie-api geïnstalleerd, SDK-classes resolvable, beide files bestaan, AppServiceProvider bindings staan, Mollie SDK kan via Hub resolven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubMollieCredentialResolverTest — 3 paden gedekt</name>
  <files>
    tests/Feature/Mollie/HubMollieCredentialResolverTest.php
  </files>
  <read_first>
    app/Mollie/MollieConnectionContext.php (uit Task 1)
    app/Mollie/HubMollieCredentialResolver.php (uit Task 1)
    app/OAuth/Testing/FakeOAuthFlow.php (uit Plan 04-01; gebruik om de refresh-pad deterministic te testen)
    tests/Feature/ConnectionEncryptionTest.php (class-shape voor PHPUnit feature-test)
    packages/mollie-api/src/Data/MollieOAuthCredentials.php ('access_'-prefix valideert; test-tokens moeten access_test_…)
  </read_first>
  <behavior>
    - Test 1 (fresh-token, no refresh needed): set MollieConnectionContext op een active Mollie-Connection met expires_at=now()+1uur, access_token='access_test_freshxyz'. Bind FakeOAuthFlow als MollieConnectOAuthFlow. Resolve → MollieOAuthCredentials.accessToken === 'access_test_freshxyz'. FakeOAuthFlow::wasCalled('refreshToken') === 0.
    - Test 2 (near-expiry → refresh-pad): set Connection met expires_at=now()+2min (binnen 5min-window). Bind FakeOAuthFlow. Resolve → FakeOAuthFlow::wasCalled('refreshToken') === 1. Returned access_token start met 'access_test_fake_' (FakeOAuthFlow regenereert).
    - Test 3 (no context → throws): Bind een lege MollieConnectionContext (zonder set()). expectException RuntimeException. Resolve → throws met message bevat 'geen current Connection'.
  </behavior>
  <action>
    Maak 1 file aan: `tests/Feature/Mollie/HubMollieCredentialResolverTest.php`. Namespace `Tests\Feature\Mollie`, extends `Tests\TestCase`, `use RefreshDatabase`.

    3 testmethodes:

    1. `test_resolve_returns_fresh_token_when_not_near_expiry()`:
       - Maak Mollie-Connection: `$connection = Connection::factory()->forMollie()->active()->create(['access_token' => 'access_test_freshxyz', 'expires_at' => now()->addHour()])`.
       - Bind FakeOAuthFlow: `$fake = new FakeOAuthFlow(); $this->app->instance(MollieConnectOAuthFlow::class, $fake);`.
       - Set context: `$this->app->make(MollieConnectionContext::class)->set($connection);`.
       - Resolve: `$creds = $this->app->make(HubMollieCredentialResolver::class)->resolve();`.
       - Assert: `assertInstanceOf(MollieOAuthCredentials::class, $creds)`, `assertSame('access_test_freshxyz', $creds->accessToken)`, `assertSame(0, $fake->wasCalled('refreshToken'))`.

    2. `test_resolve_triggers_refresh_when_within_five_minute_window()`:
       - Maak Mollie-Connection met `expires_at => now()->addMinutes(2)` (binnen 5min-window) en `access_token => 'access_test_aboutoexpire'`.
       - Bind FakeOAuthFlow + set context.
       - Resolve.
       - Assert: `assertSame(1, $fake->wasCalled('refreshToken'))`, `assertStringStartsWith('access_test_fake_', $creds->accessToken)`.

    3. `test_resolve_throws_when_context_has_no_connection()`:
       - Géén `$context->set()` aanroep.
       - `$this->expectException(\RuntimeException::class); $this->expectExceptionMessage('geen current Connection');`.
       - `$this->app->make(HubMollieCredentialResolver::class)->resolve();`.

    **CRITICAL BINDING DETAIL:** De `HubMollieCredentialResolver` injecteert `OAuthFlowRegistry`, niet `MollieConnectOAuthFlow` direct. Voor Test 2 (refresh-trigger) moet de FakeOAuthFlow via de Registry komen. Aanpak: bind in test `$this->app->instance(MollieConnectOAuthFlow::class, $fake)` — de Registry doet `$container->make($className)` waarvoor `instance()` correct returnt. Verifieer dit pad in implementatie van test 2 vóór andere assertions.

    Test-naam snake_case `test_<scenario>_<expected>` (matcht repo-conventie).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=HubMollieCredentialResolverTest</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/Mollie/HubMollieCredentialResolverTest.php` bestaat
    - `grep -c "public function test_" tests/Feature/Mollie/HubMollieCredentialResolverTest.php` returns 3
    - `grep -q "wasCalled('refreshToken')" tests/Feature/Mollie/HubMollieCredentialResolverTest.php` exits 0
    - `grep -q "expectException" tests/Feature/Mollie/HubMollieCredentialResolverTest.php` exits 0
    - `php artisan test --compact --filter=HubMollieCredentialResolverTest` exits 0 (3 passed)
  </acceptance_criteria>
  <done>HubMollieCredentialResolver bewezen voor fresh-pad, refresh-pad en error-pad; SC-3 ("refresh ruim vóór 401") deels gedekt via test 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubMollieCredentialResolver → Hub Connection-model | Trust-grens: SDK-laag mag géén Hub-modellen importeren in interface, wel via Context-service (Hub-laag injecteert) |
| MollieConnectionContext → request-scope | Per-request vers; geen cross-request token-lekkage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Cross-request token-leak | mitigate | `$this->app->scoped(...)` (niet `singleton()`) — Laravel maakt context vers per request/job. PATTERNS.md regel 928. |
| T-04-03-02 | Spoofing | resolve() zonder context | mitigate | `MollieConnectionContext::current()` throws RuntimeException als context leeg is. Phase 5a controllers moeten `->set()` aanroepen alvorens credentials te resolven. Test 3 dekt dit pad. |
| T-04-03-03 | Tampering | Resolver lekt Hub-Connection in SDK-grens | accept | Resolver-interface returnt `MollieCredentials` (SDK-DTO), niet `Connection`. Implementatie leest wel Connection via Context — dat is de Hub-laag toegestaan, alleen het contract blijft schoon. Conform `.ai/rules/engineering.md` invariant. |
</threat_model>

<verification>
- `composer show emeq/mollie-api` exits 0; SDK-classes resolvable via PHP reflection.
- `php artisan test --compact --filter=HubMollieCredentialResolverTest` → 3 passed.
- Tinker bewijst: `app(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class)` returnt `HubMollieCredentialResolver`-instance.
- Volledige test-suite blijft groen (`php artisan test --compact`).
- ROADMAP SC-3 deels bewezen: refresh-pad getriggerd wanneer expires_at < now()+5min.
</verification>

<success_criteria>
- emeq/mollie-api package geïnstalleerd; SDK-types autoloadable.
- MollieConnectionContext bestaat als scoped singleton met set/current/has methodes.
- HubMollieCredentialResolver implementeert SDK-contract en triggert refresh in 5min-window (D-04 + D-06).
- AppServiceProvider bindt beide klassen + SDK-contract → Hub-impl (D-16).
- 3 testpaden groen: fresh-token, refresh-trigger, no-context-throws.
- Phase 5a kan straks Mollie-SDK aanroepen na `MollieConnectionContext->set($conn)`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mollie-connect-oauth-broker/04-03-SUMMARY.md` met expliciete vermelding welke installatie-route (VCS-primair vs path-fallback) gebruikt is.
</output>
