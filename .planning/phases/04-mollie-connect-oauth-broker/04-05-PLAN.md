---
phase: 04-mollie-connect-oauth-broker
plan: 05
type: execute
wave: 5
depends_on: ["04-01", "04-03", "04-04"]
files_modified:
  - app/Console/Commands/PruneOAuthPendingConnections.php
  - tests/Feature/Console/PruneOAuthPendingConnectionsTest.php
autonomous: false
requirements: [MOLL-02, HUB-02]
tags:
  - laravel
  - artisan
  - oauth
  - cleanup
  - phpunit
  - blocking

must_haves:
  truths:
    - "Artisan-command `oauth:prune-pending` ruimt status=pending AND oauth_state_expires_at &lt; now() op (D-09)"
    - "Command heeft --dry-run flag die count toont zonder rows te verwijderen"
    - "Command is NIET geregistreerd in schedule — handmatig of via deploy-hook (D-09 + D-04 geen-cron-filosofie)"
    - "php artisan migrate is uitgevoerd na alle schema-modifying tasks vóór de volledige test-suite-verificatie"
    - "Volledige test-suite (php artisan test --compact) exits 0 — alle Phase-4 + bestaande Phase-3 tests groen"
    - "Alle 5 ROADMAP success criteria (SC-1 t/m SC-5) zijn aantoonbaar gedekt door tests die in dit plan worden gerund"
  artifacts:
    - path: "app/Console/Commands/PruneOAuthPendingConnections.php"
      provides: "Artisan-command oauth:prune-pending met --dry-run optie"
      contains: "protected .signature = 'oauth:prune-pending"
    - path: "tests/Feature/Console/PruneOAuthPendingConnectionsTest.php"
      provides: "2 testpaden: prunes-expired + dry-run-no-delete"
      contains: "oauth:prune-pending"
  key_links:
    - from: "PruneOAuthPendingConnections::handle"
      to: "Connection-model query (status=pending AND oauth_state_expires_at &lt; now)"
      via: "Eloquent->delete()"
      pattern: "where('status', 'pending')"
    - from: "Phase 4 deliverables"
      to: "Volledige test-suite groen"
      via: "php artisan test --compact"
      pattern: "PASS"
---

<objective>
Operations-laag + BLOCKING phase-verificatie:
- Artisan-command `oauth:prune-pending` (D-09: handmatige cleanup, géén cron) voor expired pending-Connection-rows.
- BLOCKING `php artisan migrate` als alle schema-changes uit Plan 04-01 al via die plan zijn gerund — hier confirm-run als safety net.
- BLOCKING volledige test-suite-run — bewijst dat alle Phase-4 deliverables samen werken en geen Phase-3 tests gebroken zijn.

Purpose: Sluit Phase 4 af. De prune-command is operationeel nodig (zonder cron stapelen orphan rows op als gebruikers /init aanroepen en niet voltooien). De BLOCKING-verificatie is de phase-acceptance.

Output: 1 nieuwe artisan-command + 1 nieuwe test + uitgevoerde `php artisan migrate` + uitgevoerde `php artisan test --compact` met all-green result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md
@.planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md
@.planning/phases/04-mollie-connect-oauth-broker/04-01-PLAN.md
@.planning/phases/04-mollie-connect-oauth-broker/04-02-PLAN.md
@.planning/phases/04-mollie-connect-oauth-broker/04-03-PLAN.md
@.planning/phases/04-mollie-connect-oauth-broker/04-04-PLAN.md
@CLAUDE.md
@.ai/rules/global.md
@app/Console/Commands/HubConsumerCreate.php
@tests/Feature/Console/HubConsumerCreateTest.php

<interfaces>
<!-- Bestaande command-pattern + factory-states die deze plan gebruikt -->

From app/Console/Commands/HubConsumerCreate.php (analog):
```php
class HubConsumerCreate extends Command {
    protected $signature = 'hub:consumer:create
                            {--slug= : ...}
                            {--name= : ...}';
    protected $description = 'Maak een Consumer + Personal Access Token aan vanaf de CLI';

    public function handle(): int {
        // ... return self::SUCCESS / INVALID / FAILURE
    }
}
```

From Plan 04-01 (factory-states):
```php
Connection::factory()->forMollie()->pending()->create(); // status=pending, oauth_state_expires_at=now()+30min
Connection::factory()->forMollie()->pending()->expired()->create(); // oauth_state_expires_at=now()-1min
Connection::factory()->forMollie()->active()->create(); // status=active, oauth_state=null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PruneOAuthPendingConnections artisan-command + test</name>
  <files>
    app/Console/Commands/PruneOAuthPendingConnections.php
    tests/Feature/Console/PruneOAuthPendingConnectionsTest.php
  </files>
  <read_first>
    .planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md (sections: PruneOAuthPendingConnections + test)
    .planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md (D-04 + D-09)
    app/Console/Commands/HubConsumerCreate.php (exact analog: property-stijl signature + Nederlands description + exit-codes)
    tests/Feature/Console/HubConsumerCreateTest.php (artisan-test-pattern: $this->artisan()->assertExitCode + expectsOutputToContain)
    database/factories/ConnectionFactory.php (forMollie + pending + active + expired states uit Plan 04-01)
  </read_first>
  <behavior>
    - Test 1 (prune-expired): maak 3 Connections: pending+expired, pending+fresh, active. Run `php artisan oauth:prune-pending`. Assert: expired is gone (`Connection::find($expired->id)` is null), fresh blijft, active blijft. Exit 0.
    - Test 2 (dry-run): maak 1 pending+expired. Run met `--dry-run`. Assert: output bevat 'Dry-run'. Connection bestaat nog. Exit 0.
  </behavior>
  <action>
    Maak 2 files aan.

    **1. app/Console/Commands/PruneOAuthPendingConnections.php** — Per PATTERNS.md §"PruneOAuthPendingConnections" copy-pattern (regels 705-740). Property-stijl `$signature` + `$description` (matcht `HubConsumerCreate` per Phase 3 D-pattern; geen `#[AsCommand]`-attribuut). Description in Nederlands. Inhoud:

    ```php
    <?php

    namespace App\Console\Commands;

    use App\Models\Connection;
    use Illuminate\Console\Command;

    class PruneOAuthPendingConnections extends Command
    {
        protected $signature = 'oauth:prune-pending
                                {--dry-run : Toon welke rows verwijderd zouden worden zonder ze te raken}';

        protected $description = 'Ruim expired pending OAuth-Connections op (status=pending AND oauth_state_expires_at < now)';

        public function handle(): int
        {
            $query = Connection::query()
                ->where('status', 'pending')
                ->where('oauth_state_expires_at', '<', now());

            if ($this->option('dry-run')) {
                $count = $query->count();
                $this->info("Dry-run: {$count} pending Connection(s) zouden worden verwijderd.");

                return self::SUCCESS;
            }

            $deleted = $query->delete();

            $this->info("Verwijderd: {$deleted} expired pending Connection(s).");

            return self::SUCCESS;
        }
    }
    ```

    **LANDMINES** (PATTERNS.md regel 743-747):
    - D-09: NIET registreren in `routes/console.php` als scheduled task. Handmatige of deploy-hook trigger. Geen `app/Console/Kernel.php`-edit nodig — Laravel 11+ auto-discovers commands in `app/Console/Commands/`.
    - Description in Nederlands (user-facing CLI-output per `.ai/rules/global.md`).
    - `delete()` (niet soft-delete) — Connection-model heeft geen SoftDeletes-trait; expired-pending zijn pure orphans.

    **2. tests/Feature/Console/PruneOAuthPendingConnectionsTest.php** — Per PATTERNS.md §"PruneOAuthPendingConnectionsTest" copy-pattern (regels 1443-1479). Namespace `Tests\Feature\Console`, extends `Tests\TestCase`, `use RefreshDatabase`. 2 testmethodes:

    1. `test_prunes_expired_pending_connections()`:
       - `$expired = Connection::factory()->forMollie()->pending()->expired()->create();`
       - `$fresh = Connection::factory()->forMollie()->pending()->create();`
       - `$active = Connection::factory()->forMollie()->active()->create();`
       - `$this->artisan('oauth:prune-pending')->assertExitCode(0);`
       - `$this->assertNull(Connection::find($expired->id));`
       - `$this->assertNotNull(Connection::find($fresh->id));`
       - `$this->assertNotNull(Connection::find($active->id));`

    2. `test_dry_run_does_not_delete_anything()`:
       - `$expired = Connection::factory()->forMollie()->pending()->expired()->create();`
       - `$this->artisan('oauth:prune-pending', ['--dry-run' => true])->expectsOutputToContain('Dry-run')->assertExitCode(0);`
       - `$this->assertNotNull(Connection::find($expired->id));`
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PruneOAuthPendingConnectionsTest</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Console/Commands/PruneOAuthPendingConnections.php` bestaat
    - `grep -q "oauth:prune-pending" app/Console/Commands/PruneOAuthPendingConnections.php` exits 0
    - `grep -q "--dry-run" app/Console/Commands/PruneOAuthPendingConnections.php` exits 0
    - `grep -q "where('status', 'pending')" app/Console/Commands/PruneOAuthPendingConnections.php` exits 0
    - `grep -q "where('oauth_state_expires_at', '<', now())" app/Console/Commands/PruneOAuthPendingConnections.php` exits 0
    - `php artisan list | grep -q "oauth:prune-pending"` exits 0
    - File `tests/Feature/Console/PruneOAuthPendingConnectionsTest.php` bestaat
    - `grep -c "public function test_" tests/Feature/Console/PruneOAuthPendingConnectionsTest.php` returns 2
    - `php artisan test --compact --filter=PruneOAuthPendingConnectionsTest` exits 0 (2 passed)
  </acceptance_criteria>
  <done>Command bestaat + werkt + 2 tests groen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Migrate + volledige test-suite + Phase 4 acceptance</name>
  <files>
    (geen file-wijzigingen — alleen runtime-verificatie)
  </files>
  <read_first>
    .planning/ROADMAP.md (Phase 4 success criteria SC-1 t/m SC-5)
    .planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md (alle D-01 t/m D-16)
    .planning/phases/04-mollie-connect-oauth-broker/04-01-SUMMARY.md (na Plan 04-01)
    .planning/phases/04-mollie-connect-oauth-broker/04-02-SUMMARY.md (na Plan 04-02)
    .planning/phases/04-mollie-connect-oauth-broker/04-03-SUMMARY.md (na Plan 04-03)
    .planning/phases/04-mollie-connect-oauth-broker/04-04-SUMMARY.md (na Plan 04-04)
  </read_first>
  <what-built>
    Phase 4 deliverables:
    - OAuthFlow-contract + FakeOAuthFlow + OAuthFlowContractTest (Plan 04-01)
    - Migration + Connection-model edit + factory-states (Plan 04-01)
    - MollieConnectOAuthFlow + OAuthFlowRegistry + config + AppServiceProvider OAuthFlow-bindings + MollieConnectOAuthFlowTest (Plan 04-02)
    - MollieConnectionContext + HubMollieCredentialResolver + AppServiceProvider SDK-bindings + HubMollieCredentialResolverTest (Plan 04-03)
    - InitController + CallbackController + routes/api.php uitbreiding + InitTest + CallbackTest (Plan 04-04)
    - PruneOAuthPendingConnections command + test (deze plan Task 1)
  </what-built>
  <action>
    Voer in volgorde uit (autonomous; user-verify is alleen op final assert-output):

    **1. Idempotent migrate-confirm:**
    ```bash
    php artisan migrate --no-interaction --force
    ```
    Verwacht: "Nothing to migrate." (omdat Plan 04-01 al gerund heeft) of een vers-toegepaste migration als de development-DB tussentijds is gereset. Beide outputs zijn acceptabel — wat NIET acceptabel is: een failed migration-run.

    **2. Verifieer connections-schema:**
    ```bash
    php artisan tinker --execute 'use Illuminate\Support\Facades\Schema; echo Schema::hasColumns("connections", ["oauth_state","oauth_state_expires_at"]) ? "SCHEMA_OK" : "SCHEMA_FAIL";'
    ```
    Verwacht: `SCHEMA_OK`.

    **3. Verifieer route-registratie:**
    ```bash
    php artisan route:list --path=oauth/mollie --json
    ```
    Verwacht: JSON met 2 routes (POST init + GET callback).

    **4. Verifieer container-bindings:**
    ```bash
    php artisan tinker --execute 'echo get_class(app(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class)).PHP_EOL;'
    php artisan tinker --execute 'echo get_class(app(\App\OAuth\OAuthFlowRegistry::class)->for("mollie")).PHP_EOL;'
    ```
    Verwacht:
    - `App\Mollie\HubMollieCredentialResolver`
    - `App\OAuth\Mollie\MollieConnectOAuthFlow`

    **5. Verifieer artisan-command-registratie:**
    ```bash
    php artisan list | grep oauth:prune-pending
    ```
    Verwacht: één regel met `oauth:prune-pending  Ruim expired pending OAuth-Connections op (...)`.

    **6. BLOCKING: volledige test-suite:**
    ```bash
    php artisan test --compact
    ```
    Verwacht: alle tests passed (Phase 3's bestaande 27 passed + 1 incomplete + Phase 4's nieuwe tests). Geen failed.

    **7. Format-check:**
    ```bash
    ./vendor/bin/pint --dirty --format agent
    ```
    Verwacht: 0 file-wijzigingen óf alleen automatische fixes (geen errors).

    **8. ROADMAP success-criteria mapping (handmatige check op test-output):**
    - SC-1 (init returnt redirect-URL met juiste client_id/state/redirect_uri) → `InitTest::test_init_creates_pending_connection_and_returns_redirect_url` PASS.
    - SC-2 (callback ruilt code in voor access+refresh tokens encrypted-at-rest) → `CallbackTest::test_callback_exchanges_code_when_state_matches` PASS + Phase 3 `ConnectionEncryptionTest` blijft groen.
    - SC-3 (Connection met expires_at < 5min triggert refresh, pass-through ziet geen 401) → `HubMollieCredentialResolverTest::test_resolve_triggers_refresh_when_within_five_minute_window` PASS + `MollieConnectOAuthFlowTest::test_exchange_code_writes_encrypted_tokens` voor refresh-shape.
    - SC-4 (OAuthFlow-contract heeft tweede dummy-implementatie niet-Mollie-specifiek) → `OAuthFlowContractTest` PASS (3 tests).
    - SC-5 (tampered state-parameter → 400) → `CallbackTest::test_callback_with_invalid_state_returns_400` + `test_callback_with_expired_state_returns_400` + `test_second_callback_with_same_state_returns_400` PASS.

    **Surface ROADMAP-vs-CONTEXT delta in SUMMARY:** ROADMAP SC-1 zegt `GET /v1/oauth/mollie/authorize?account=…`; CONTEXT D-01+D-08 + implementatie locken op `POST /v1/oauth/mollie/init` met JSON-return. Phase-acceptance volgt CONTEXT. Documenteer in PLAN-05-SUMMARY: "Phase 4 implements D-01/D-08 design (POST /init returns JSON), supersedes ROADMAP SC-1 wording — needs ROADMAP update at phase-close."
  </action>
  <how-to-verify>
    Lees stdout van bovenstaande commands.

    Acceptance:
    - Migrate exit 0, geen errors.
    - Schema check returnt `SCHEMA_OK`.
    - route:list toont 2 oauth-routes.
    - Tinker container-resolutions returnen exacte class-names hierboven.
    - `php artisan list | grep oauth:prune-pending` returnt één regel.
    - `php artisan test --compact` toont 0 failures (ALL PASSED of PASSED+INCOMPLETE).
    - `pint --dirty` exit 0.

    Bij ENIGE faal: stop en rapporteer; voer geen rollback uit (forward-only — eventuele fixes komen in een nieuw plan of quick-fix).
  </how-to-verify>
  <resume-signal>Type "approved" als alle 8 checks groen zijn, of beschrijf welke check faalde voor follow-up.</resume-signal>
  <verify>
    <automated>php artisan migrate --no-interaction --force &amp;&amp; php artisan test --compact &amp;&amp; ./vendor/bin/pint --dirty --format agent</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate --no-interaction --force` exits 0
    - `php artisan tinker --execute 'use Illuminate\Support\Facades\Schema; echo Schema::hasColumns("connections", ["oauth_state","oauth_state_expires_at"]) ? "OK" : "FAIL";'` outputs `OK`
    - `php artisan route:list --path=oauth/mollie` shows POST init + GET callback (2 routes)
    - `php artisan tinker --execute 'echo get_class(app(\Emeq\MollieApi\Contracts\MollieCredentialResolver::class));'` outputs `App\Mollie\HubMollieCredentialResolver`
    - `php artisan tinker --execute 'echo get_class(app(\App\OAuth\OAuthFlowRegistry::class)->for("mollie"));'` outputs `App\OAuth\Mollie\MollieConnectOAuthFlow`
    - `php artisan list | grep -c oauth:prune-pending` returns 1
    - `php artisan test --compact` exits 0 — 0 failed tests
    - `./vendor/bin/pint --dirty --format agent` exits 0
    - SUMMARY documents ROADMAP-vs-CONTEXT delta on SC-1 wording
  </acceptance_criteria>
  <done>Phase 4 acceptance volledig groen; alle 5 ROADMAP success criteria gedekt door tests; pint clean; ROADMAP-update voor SC-1-wording surfacd in SUMMARY voor phase-close commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artisan-CLI → DB (delete) | Operator initiates; pure dev/deploy-context, geen Consumer-input |
| Test-suite → application-state | Tests use RefreshDatabase trait — geen kruisbestuiving met dev-data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | Prune-command verwijdert te veel | mitigate | Twee `where`-clauses (`status=pending` EN `oauth_state_expires_at < now()`) — kan nooit een active/revoked Connection raken. Test 1 bewijst alle 3 paden expliciet. |
| T-04-05-02 | DoS | Prune-command blokkeert DB | accept | Phase 4 produceert per /init slechts één row + 30min TTL — orphan-volume blijft laag. `delete()` op indexed `status` + `oauth_state_expires_at` is snelle index-scan. Geen batching nodig in v0.2. |
| T-04-05-03 | Repudiation | Prune zonder audit-trail | accept | Pending Connections die nooit voltooid zijn dragen geen business-value (geen tokens, geen Connection-link). Géén audit nodig. Phase 9 admin-paneel kan dit later visualiseren via Filament als nodig. |
</threat_model>

<verification>
- BLOCKING test-suite exit 0.
- Alle 5 ROADMAP SC's gedekt door automated tests (zie task 2 actie-stap 8).
- Pint clean.
- 16 CONTEXT decisions (D-01 t/m D-16) traceerbaar in deliverables:
  - D-01 (pre-create pending) → InitController
  - D-02 (30min TTL) → InitController + ConnectionFactory::pending()
  - D-03 (state-verify + idempotency) → CallbackController + CallbackTest 4
  - D-04 (pure lazy refresh, geen cron) → HubMollieCredentialResolver + dit plan's "geen schedule-registratie"
  - D-05 (Redis-lock per connection_id) → MollieConnectOAuthFlow
  - D-06 (5min refresh-window) → HubMollieCredentialResolver + MollieConnectOAuthFlow
  - D-07 (twee endpoints, twee auth-modes) → routes/api.php
  - D-08 (JSON-return, geen HTTP-redirect) → InitController
  - D-09 (prune via command, geen cron) → PruneOAuthPendingConnections + dit plan
  - D-10 (scopes hard-coded in config) → config/services.php
  - D-11 (scopes-jsonb-kolom) → MollieConnectOAuthFlow::exchangeCode fills `scopes`
  - D-12 (FakeOAuthFlow in app/OAuth/Testing/) → Plan 04-01
  - D-13 (OAuthFlow-contract in app/, niet packages/) → Plan 04-01
  - D-14 (Registry::for) → OAuthFlowRegistry + AppServiceProvider
  - D-15 (geen Saloon, directe Http::post) → MollieConnectOAuthFlow
  - D-16 (HubMollieCredentialResolver bindt in Phase 4) → Plan 04-03 + AppServiceProvider
</verification>

<success_criteria>
- PruneOAuthPendingConnections command + test bestaan en werken.
- BLOCKING migrate-run + test-suite-run exit 0.
- Alle 5 ROADMAP success criteria zijn traceerbaar groen via automated tests.
- ROADMAP-vs-CONTEXT delta op SC-1-wording is surfaced in SUMMARY.
- Phase 4 is volledig accepteerbaar voor commit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mollie-connect-oauth-broker/04-05-SUMMARY.md` met expliciete ROADMAP-update-todo voor de phase-close-commit (SC-1 wording moet "POST /v1/oauth/mollie/init returns JSON {connection_id, redirect_url}" worden).
</output>
