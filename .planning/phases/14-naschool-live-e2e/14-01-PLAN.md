---
phase: 14-naschool-live-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "[NASCHOOL-REPO] composer.json"
  - "[NASCHOOL-REPO] composer.lock"
  - "[NASCHOOL-REPO] config/services.php"
  - "[NASCHOOL-REPO] .env.example"
  - "[NASCHOOL-REPO] app/Support/EmeqHub/EmeqHubConfig.php (new)"
  - "[NASCHOOL-REPO] tests/Feature/Support/EmeqHub/EmeqHubConfigTest.php (new)"
autonomous: false
requirements: [NSCH-04]
notes: |
  Cross-repo plan. Werkt in /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/.
  Geen Hub-repo files gewijzigd. Sluit Phase 8 human_verification item #4 (SC-1 composer-resolve).
  Phase-11-baseline-pin: emeq/snelstart-api MOET ≥ v0.2.0 (Saloon-v4 baseline + SNEL-03/04 advisories-closure). Lagere tags regressen Phase-11-werk via transitive consumer.
must_haves:
  truths:
    - "Naschool's composer.json declareert emeq/snelstart-api en emeq/mollie-api via publieke VCS-repositories — geen GitHub-auth-token vereist"
    - "Naschool's composer.json pinst emeq/snelstart-api op ^0.2.0 (Phase-11 Saloon-v4-baseline) en emeq/mollie-api op ^0.2.0"
    - "composer update --no-cache slaagt fresh op een schone vendor-folder; composer.lock pinst beide SDKs op een concrete commit ≥ v0.2.0"
    - "Naschool heeft een centrale EmeqHubConfig-helper die hub-base-URL + Consumer-PAT + naschool-Account-id uit config/services.php leest (env-driven, géén hardcoded waarden)"
    - "Test bewijst dat EmeqHubConfig de drie velden correct retourneert en throwt bij ontbrekende config"
  artifacts:
    - path: "[NASCHOOL-REPO] composer.json"
      provides: "VCS-repository-entries voor emeq/snelstart-api + emeq/mollie-api + require-statements met ^0.2.0 pin"
      contains: "yusufkaracaburun/emeq-snelstart-api + yusufkaracaburun/emeq-mollie-api"
    - path: "[NASCHOOL-REPO] composer.lock"
      provides: "Reproducible install-state met beide SDKs gepinst op v0.2.0+"
      contains: "emeq/snelstart-api + emeq/mollie-api"
    - path: "[NASCHOOL-REPO] config/services.php"
      provides: "emeq_hub-block met base_url + pat + naschool_account_id env-bindings"
      contains: "'emeq_hub' =>"
    - path: "[NASCHOOL-REPO] app/Support/EmeqHub/EmeqHubConfig.php"
      provides: "Centrale config-reader voor alle Hub-pass-through-calls vanuit Naschool"
      contains: "final readonly class EmeqHubConfig"
    - path: "[NASCHOOL-REPO] tests/Feature/Support/EmeqHub/EmeqHubConfigTest.php"
      provides: "Happy-path + missing-config-throws coverage"
      contains: "test_throws_when_pat_missing"
  key_links:
    - from: "[NASCHOOL-REPO] composer.json"
      to: "github.com:yusufkaracaburun/emeq-snelstart-api + emeq-mollie-api"
      via: "repositories[].type=vcs + url=https"
      pattern: "\"type\": \"vcs\""
    - from: "[NASCHOOL-REPO] EmeqHubConfig"
      to: "config('services.emeq_hub.*')"
      via: "Config::get-reads in constructor"
      pattern: "config\\('services\\.emeq_hub"
---

<objective>
Bring Naschool's backend up to a state where it can compile-time depend on the Hub-SDKs and runtime-call the Hub via a centralised, env-driven config helper. Phase 14 daarna kan listener + UAT bouwen zonder repeat-werk aan composer- of config-laag.

Purpose: SC-1 uit Phase 14 (composer-resolve) sluit Phase 8 human_verification item #4 én legt het fundament voor NSCH-05 (StancltenancyCredentialResolver in Plan 14-02) + NSCH-06 (listener in Plan 14-03). Pin op ^0.2.0 garandeert Phase-11 Saloon-v4-baseline (anders regressie van SNEL-03/04 via transitive consumer).
Output: Naschool composer.json/lock met beide SDKs gepinst op ^0.2.0, config/services.php emeq_hub-block, EmeqHubConfig-helper + feature-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v0.2-phases/08-naschool-wiring-snelstart-mollie-via-hub/08-VERIFICATION.md

# Cross-repo guidance — Hub-side composer.json pattern als referentie
@composer.json
</context>

<interfaces>
<!-- Hub-side VCS-repository convention die Naschool moet spiegelen. Uit emeq-hub/composer.json: -->

```json
"repositories": [
    {
        "type": "vcs",
        "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git"
    },
    {
        "type": "vcs",
        "url": "https://github.com/yusufkaracaburun/emeq-mollie-api.git"
    }
]
```

Bestaande Naschool composer.json gebruikt al `repositories[].type=path` voor 4 lokale packages (notable, sluggable, laravel-model-settings, laravel-settings) — voeg de twee VCS-entries toe naast die bestaande lijst, niet vervangen.

Pinning-strategie: `"emeq/snelstart-api": "^0.2.0"` en `"emeq/mollie-api": "^0.2.0"`. De `^0.2.0`-constraint op `emeq/snelstart-api` is **hard required** — Phase 11 tagde v0.2.0 als de Saloon-v4-baseline inclusief SNEL-03/04 advisories-closure. Een lagere tag (≤ v0.1.x) regresseert Phase-11-werk via Naschool als transitive consumer. Beide SDKs zijn publiek; géén `auth.json` of OAuth-token nodig.
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Naschool composer.json VCS-entries + composer update</name>
  <files>[NASCHOOL-REPO] /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/composer.json, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/composer.lock</files>
  <read_first>
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/composer.json (huidige repositories[]-block en require-block)
    - /Users/yusufkaracaburun/Sites/localhost/emeq-hub/composer.json (Hub-side referentie-pattern voor VCS-entries — alleen de repositories[]-block en de twee require-keys)
  </read_first>
  <action>
    In de Naschool-repo (/Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/):

    1. Voeg twee VCS-repository-entries toe aan composer.json `repositories[]` (naast de bestaande 4 path-repos):
       - `{"type": "vcs", "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git"}`
       - `{"type": "vcs", "url": "https://github.com/yusufkaracaburun/emeq-mollie-api.git"}`
    2. Voeg twee require-statements toe aan `require` met pin-constraint `^0.2.0`:
       - `"emeq/snelstart-api": "^0.2.0"` ← Phase-11 Saloon-v4-baseline. NIET een lagere of bredere constraint (`*`, `dev-master`, `^0.1`) — dat regresseert SNEL-03/04 advisories-closure via transitive consumer.
       - `"emeq/mollie-api": "^0.2.0"`
    3. Run `composer update emeq/snelstart-api emeq/mollie-api --no-cache --with-all-dependencies` (vanaf een schone state — eventueel eerst `rm -rf vendor/emeq` voor cache-bewijs).
    4. Bevestig dat de install slaagt zonder dat Composer naar GitHub-auth vraagt (beide SDK-repos zijn publiek).
    5. Verifieer pin: `grep -A1 '"emeq/snelstart-api"' composer.lock | grep version` moet een v0.2.x+ tag tonen.
    6. Commit composer.json + composer.lock atomisch met message `chore(deps): add emeq/snelstart-api ^0.2.0 + emeq/mollie-api ^0.2.0 SDK-dependencies` in de Naschool-repo (op een feature-branch, GEEN push naar master zonder approval; volg Naschool's eigen git-policy).

    Geen wijzigingen aan de Hub-repo in deze task. Geen edits aan vendor/ (Composer-beheerd).
  </action>
  <verify>
    <automated>cd /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend && composer show emeq/snelstart-api emeq/mollie-api | grep -E '^name|^versions' && grep -A2 '"name": "emeq/snelstart-api"' composer.lock | grep version</automated>
  </verify>
  <acceptance_criteria>
    1. `composer show emeq/snelstart-api` retourneert pakket-info met een concrete versie ≥ v0.2.0 (geen "not installed"-error).
    2. `composer show emeq/mollie-api` retourneert pakket-info met een concrete versie ≥ v0.2.0.
    3. `composer.lock` pinst `emeq/snelstart-api` op `^0.2.0` (Phase-11 Saloon-v4-baseline) — verifieerbaar via `grep -A2 '"name": "emeq/snelstart-api"' composer.lock | grep version` moet een v0.2.x of hoger tonen, NIET v0.1.x.
    4. `composer.lock` bevat regels die `emeq/snelstart-api` en `emeq/mollie-api` met `dist.url` op github.com vermelden.
    5. Een fresh `composer install --no-cache` op een nieuw cloned werkkopie slaagt zonder auth-prompt (handmatig getest of via CI-bewijs).
  </acceptance_criteria>
  <done>Beide SDK-packages installeren publiek zonder auth in Naschool met emeq/snelstart-api ≥ v0.2.0 (Phase-11 Saloon-v4-baseline) en emeq/mollie-api ≥ v0.2.0; composer.json + composer.lock atomisch gecommit in Naschool-repo.</done>
</task>

<task type="auto">
  <name>Task 2: config/services.php emeq_hub-block + EmeqHubConfig helper + feature-test</name>
  <files>[NASCHOOL-REPO] /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/services.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/.env.example, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Support/EmeqHub/EmeqHubConfig.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/tests/Feature/Support/EmeqHub/EmeqHubConfigTest.php</files>
  <read_first>
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/services.php (bestaande structuur — gebruik dezelfde array-style, env() bindings)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/.env.example (env-conventie in Naschool)
    - /Users/yusufkaracaburun/Sites/localhost/emeq-hub/routes/api.php (Hub-side endpoints + middleware-stack — bewijs dat `X-Account-Id` header conventie is voor pass-through + Bearer-PAT voor auth)
  </read_first>
  <action>
    In de Naschool-repo:

    1. **config/services.php** — voeg een `'emeq_hub'`-block toe (naast bestaande blocks zoals `mailgun`, `resend`):
       ```php
       'emeq_hub' => [
           'base_url' => env('EMEQ_HUB_BASE_URL'),
           'pat' => env('EMEQ_HUB_PAT'),
           'naschool_account_id' => env('EMEQ_HUB_NASCHOOL_ACCOUNT_ID'),
       ],
       ```
       Geen defaults — env-only zodat ontbrekende config hard faalt in productie i.p.v. silent fallback.

    2. **.env.example** — voeg de drie env-keys toe met lege placeholder-waarden + één-regel-comment per key (Nederlands: "Hub-base-URL (bv. https://hub.emeq.test:8090)", "Sanctum PAT uit Hub OnboardConsumer-wizard", "Account-id van de Naschool-Consumer in de Hub").

    3. **app/Support/EmeqHub/EmeqHubConfig.php** — `final readonly class` met constructor-property-promotion (3 string-velden) + één static `fromConfig(): self`-factory die `config('services.emeq_hub.*')` leest. Throw `\RuntimeException` met een duidelijke message als één van de drie velden leeg/null is. Geen Saloon, geen HTTP-laag — pure config-reader.

    4. **tests/Feature/Support/EmeqHub/EmeqHubConfigTest.php** — PHPUnit feature-test (Naschool gebruikt PHPUnit 12; volg bestaande tests/Feature/-patroon). Vier tests:
       - `test_from_config_reads_all_three_fields`
       - `test_throws_when_base_url_missing`
       - `test_throws_when_pat_missing`
       - `test_throws_when_account_id_missing`
       Gebruik `config()->set()` om per test de drie keys te bepalen.

    5. Run `php artisan test --compact --filter=EmeqHubConfigTest` in de Naschool-repo. Pint waar nodig: `./vendor/bin/pint --dirty`.

    6. Commit als één commit: `feat(emeq-hub): config + EmeqHubConfig helper + feature-test` in Naschool-repo.

    Geen Hub-repo wijzigingen in deze task.
  </action>
  <verify>
    <automated>cd /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend && php artisan test --compact --filter=EmeqHubConfigTest</automated>
  </verify>
  <acceptance_criteria>
    1. `config/services.php` heeft een `emeq_hub`-key met de drie sub-keys (`base_url`, `pat`, `naschool_account_id`).
    2. `EmeqHubConfig::fromConfig()` retourneert een instance wanneer alle drie env-vars gezet zijn.
    3. Alle vier feature-tests passed (test_from_config + 3 throw-tests).
    4. `.env.example` bevat de drie env-keys met Nederlandstalig één-regel-comment per key.
    5. Pint exit 0 op de nieuwe files.
  </acceptance_criteria>
  <done>EmeqHubConfig is in Naschool gelanded, env-driven, throw-on-missing, getest. Listener (Plan 14-03) kan deze helper via DI consumeren.</done>
</task>

</tasks>

<verification>
- Beide SDKs installeren publiek in Naschool zonder auth-prompt (Task 1)
- emeq/snelstart-api pinst op ^0.2.0 in composer.lock (Phase-11 Saloon-v4-baseline behouden)
- EmeqHubConfig-helper is in Naschool DI-resolvable en throw-bewezen (Task 2)
- Geen wijzigingen in deze Hub-repo (alleen dit PLAN-document + later SUMMARY)
</verification>

<success_criteria>
1. `composer show emeq/snelstart-api emeq/mollie-api` in Naschool retourneert beide packages met concrete versies ≥ v0.2.0.
2. `php artisan test --compact --filter=EmeqHubConfigTest` in Naschool: 4 passed.
3. Naschool composer.json + composer.lock + config/services.php + EmeqHubConfig + test gecommit op een Naschool-feature-branch.
</success_criteria>

<output>
Create `/Users/yusufkaracaburun/Sites/localhost/emeq-hub/.planning/phases/14-naschool-live-e2e/14-01-SUMMARY.md` when done.
SUMMARY moet de Naschool-commit-SHAs vermelden + de exacte gepinde versies van emeq/snelstart-api en emeq/mollie-api uit composer.lock zodat downstream plans + verifier exact kunnen lokaliseren wat geland is en de Phase-11-baseline aantoonbaar is.
</output>
