---
phase: 14-naschool-live-e2e
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - "[NASCHOOL-REPO] app/Events/Tenant/EnrollmentConfirmed.php (new)"
  - "[NASCHOOL-REPO] app/Listeners/Tenant/DispatchSnelstartInvoiceSync.php (new)"
  - "[NASCHOOL-REPO] app/Jobs/Tenant/SyncEnrollmentToSnelstartJob.php (new)"
  - "[NASCHOOL-REPO] app/Providers/EventServiceProvider.php (extended)"
  - "[NASCHOOL-REPO] config/horizon.php (extended — naschool-queue supervisor)"
  - "[NASCHOOL-REPO] config/queue.php (extended — naschool-connection)"
  - "[NASCHOOL-REPO] config/sentry.php (extended — failed-job bridge)"
  - "[NASCHOOL-REPO] tests/Feature/Listeners/Tenant/DispatchSnelstartInvoiceSyncTest.php (new)"
  - "[NASCHOOL-REPO] tests/Feature/Jobs/Tenant/SyncEnrollmentToSnelstartJobTest.php (new)"
autonomous: false
requirements: [NSCH-06]
notes: |
  Cross-repo plan. Werkt in /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/.
  Sluit Phase 8 human_verification item #3 (SC-2 + NSCH-02 listener-pad).
must_haves:
  truths:
    - "Naschool dispatcht `SyncEnrollmentToSnelstartJob` automatisch wanneer een EnrollmentConfirmed event fired — geen handmatige queue-push"
    - "Job draait op een dedicated Horizon-supervisor (queue: naschool, connection: redis) — isolatie van Naschool's andere queues + per-queue-failed-job-retention"
    - "Job consumeert EmeqHubClient (Plan 14-02) en bouwt Snelstart-Verkoopfactuur-payload op basis van de Enrollment-data"
    - "Failed job na `tries` exhausted → Sentry-event met enrollment-id + tenant-id (géén PAT/clientkey in payload)"
    - "Listener-test bewijst dat het event-firing de job dispatcht (Queue::fake + assertPushed) — geen synchrone Snelstart-call in de listener zelf"
    - "Job-test bewijst happy-path met Http::fake op /v1/snelstart/Verkoopfacturen + retry-on-5xx-behavior"
  artifacts:
    - path: "[NASCHOOL-REPO] app/Events/Tenant/EnrollmentConfirmed.php"
      provides: "Domain-event met enrollment-id payload, fired wanneer een vrijwillige-bijdrage-betaling is afgerond"
      contains: "class EnrollmentConfirmed"
    - path: "[NASCHOOL-REPO] app/Listeners/Tenant/DispatchSnelstartInvoiceSync.php"
      provides: "Dunne listener die alleen Queue::dispatch doet — geen business-logica"
      contains: "SyncEnrollmentToSnelstartJob::dispatch"
    - path: "[NASCHOOL-REPO] app/Jobs/Tenant/SyncEnrollmentToSnelstartJob.php"
      provides: "Horizon-job die EmeqHubClient consumeert + Snelstart-Verkoopfactuur creëert"
      contains: "implements ShouldQueue + public string $queue = 'naschool'"
    - path: "[NASCHOOL-REPO] tests/Feature/Listeners/Tenant/DispatchSnelstartInvoiceSyncTest.php"
      provides: "Event→Job dispatch coverage met Queue::fake"
      contains: "Queue::assertPushed"
    - path: "[NASCHOOL-REPO] tests/Feature/Jobs/Tenant/SyncEnrollmentToSnelstartJobTest.php"
      provides: "Job happy-path + 5xx-retry + Sentry-on-fail coverage"
      contains: "Http::fake"
  key_links:
    - from: "[NASCHOOL-REPO] EnrollmentConfirmed event"
      to: "DispatchSnelstartInvoiceSync listener"
      via: "EventServiceProvider $listen mapping (or attribute-based discovery)"
      pattern: "EnrollmentConfirmed::class"
    - from: "[NASCHOOL-REPO] DispatchSnelstartInvoiceSync"
      to: "SyncEnrollmentToSnelstartJob"
      via: "Bus::dispatch / SyncEnrollmentToSnelstartJob::dispatch"
      pattern: "SyncEnrollmentToSnelstartJob::dispatch"
    - from: "[NASCHOOL-REPO] SyncEnrollmentToSnelstartJob::handle()"
      to: "EmeqHubClient::createSnelstartVerkoopfactuur"
      via: "constructor-DI + handle-call"
      pattern: "createSnelstartVerkoopfactuur"
    - from: "[NASCHOOL-REPO] horizon supervisor"
      to: "naschool-queue"
      via: "config/horizon.php environments[].supervisor-1.queue"
      pattern: "'queue' => \\['naschool'"
---

<objective>
Wire het EnrollmentConfirmed → Horizon-job → Hub-pass-through → Snelstart-Verkoopfactuur-pad in Naschool. Dit is de productie-loop die de E2E in Plan 14-04 zal valideren.

Purpose: NSCH-06 sluit + Phase 8 human_verification item #3 (SC-2 listener-pad) sluit. Levert het laatste Naschool-side stuk voordat de live UAT in Plan 14-04 kan draaien.
Output: Event + Listener + Horizon-Job + EventServiceProvider-mapping + Horizon-supervisor + Sentry-bridge + 2 feature-tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-naschool-live-e2e/14-01-PLAN.md
@.planning/phases/14-naschool-live-e2e/14-02-PLAN.md
@.planning/milestones/v0.2-phases/08-naschool-wiring-snelstart-mollie-via-hub/08-VERIFICATION.md

# Hub-side Snelstart pass-through reference (zodat job de juiste payload-shape produceert)
@routes/api.php
</context>

<interfaces>
<!-- Snelstart Verkoopfactuur payload-shape (officiële Snelstart-OData v2-spec). -->
<!-- BRON: SDK packages/snelstart-api/docs/partners/ in deze repo. NIET verzinnen — als de exacte payload onduidelijk is, lees eerst de officiële Snelstart-docs. -->

POST {hub-base-url}/v1/snelstart/Verkoopfacturen verwacht (minimum-veld-set):
```json
{
  "factuurnummer": "...",
  "factuurdatum": "YYYY-MM-DD",
  "factuurbedrag": 0.00,
  "openstaandSaldo": 0.00,
  "boekingsperiode": { "id": "uuid" },
  "factuurregels": [
    {
      "omschrijving": "...",
      "aantal": 1,
      "stuksprijs": 0.00,
      "btwTarief": "Geen"
    }
  ],
  "relatie": { "id": "uuid" }
}
```

**Belangrijke onbekenden** die de executor moet beslissen of vraag-en/of-stub-out:
1. Welke `relatie.id` hoort bij een eindgebruiker (ouder) bij school A? → Twee opties:
   a. Pre-existing Snelstart-Relatie per tenant + lookup via tenant-config
   b. Inline `Relaties`-create-flow vóór de Verkoopfactuur (extra pass-through-call)
   Plan-aanname: gebruik (a) — voeg `emeq_snelstart_default_relatie_id` toe aan Naschool tenant-attributes naast de `emeq_hub_account_id` uit Plan 14-02. Documenteer dit in commit-msg + SUMMARY.
2. Welke `boekingsperiode.id`? → Lees de actieve periode via een aparte `GET /v1/snelstart/Boekingsperiodes` of pin in tenant-config. Plan-aanname: pin een `emeq_snelstart_boekingsperiode_id` per tenant; productie-flow kan later auto-discover.

Beide aannames zijn temp-acceptable voor de Naschool-POC; documenteer in SUMMARY als follow-up-TODO voor productie-hardening.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: EnrollmentConfirmed event + DispatchSnelstartInvoiceSync listener + EventServiceProvider mapping + listener-test</name>
  <files>[NASCHOOL-REPO] /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Events/Tenant/EnrollmentConfirmed.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Listeners/Tenant/DispatchSnelstartInvoiceSync.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Providers/EventServiceProvider.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/tests/Feature/Listeners/Tenant/DispatchSnelstartInvoiceSyncTest.php</files>
  <read_first>
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Events/Tenant/ActivityRegistrationOpened.php (bestaande event-pattern: constructor-property-promotion, SerializesModels-trait, etc.)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Listeners/Tenant/SendParentProfileActivityAlerts.php (bestaande listener-pattern: implements ShouldQueue, queue-attributes)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Providers/EventServiceProvider.php (huidige $listen-array of attribute-based-discovery)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Models/ — vind het Enrollment-model (`find . -name "Enrollment.php"` of `grep -r 'class Enrollment' app/`)
  </read_first>
  <action>
    In de Naschool-repo:

    1. **app/Events/Tenant/EnrollmentConfirmed.php** — class met constructor-property-promotion: `public function __construct(public readonly int $enrollmentId)`. Gebruik `Dispatchable + SerializesModels` traits zoals het bestaande ActivityRegistrationOpened-event. Bewust géén full Enrollment-model in de payload — alleen id, om serialization-overhead en stale-state-risk te vermijden (job laadt fresh).

    2. **app/Listeners/Tenant/DispatchSnelstartInvoiceSync.php** — dunne listener (`implements ShouldQueue` NIET — listener mag synchroon draaien want hij doet alleen Job-dispatch). Eén methode: `handle(EnrollmentConfirmed $event): void { SyncEnrollmentToSnelstartJob::dispatch($event->enrollmentId)->onQueue('naschool'); }`. Géén business-logica.

    3. **app/Providers/EventServiceProvider.php** — voeg de mapping toe in de bestaande `$listen` array (of via `Event::listen()` in boot() als Naschool die conventie heeft). Map: `EnrollmentConfirmed::class => [DispatchSnelstartInvoiceSync::class]`.

    4. **tests/Feature/Listeners/Tenant/DispatchSnelstartInvoiceSyncTest.php** — drie tests met `Queue::fake()`:
       - `test_event_dispatches_sync_job_to_naschool_queue` (fire EnrollmentConfirmed → `Queue::assertPushed(SyncEnrollmentToSnelstartJob::class, fn($job) => $job->queue === 'naschool' && $job->enrollmentId === 42)`)
       - `test_listener_does_not_call_hub_directly` (assert `Http::wasNever()` na event-fire)
       - `test_multiple_confirmations_dispatch_multiple_jobs` (3x fire → `Queue::assertPushed(... times: 3)`)

    5. Run `php artisan test --compact --filter=DispatchSnelstartInvoiceSyncTest`. Pint.

    6. Commit: `feat(emeq-hub): EnrollmentConfirmed event + DispatchSnelstartInvoiceSync listener` in Naschool-repo.

    Geen Hub-repo wijzigingen.
  </action>
  <verify>
    <automated>cd /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend && php artisan test --compact --filter=DispatchSnelstartInvoiceSyncTest</automated>
  </verify>
  <acceptance_criteria>
    1. Event + listener + service-provider-mapping bestaan; `php artisan event:list` toont de mapping.
    2. Alle 3 listener-tests passed.
    3. Listener doet géén synchrone Http-call — strikt dispatch-only (assert via Http::fake-zero-call).
    4. Job wordt op de `naschool`-queue gepusht (assert via Queue::assertPushed met queue-check).
  </acceptance_criteria>
  <done>Event-listener-pad gewired en getest. Job-skeleton wordt in Task 2 geïmplementeerd.</done>
</task>

<task type="auto">
  <name>Task 2: SyncEnrollmentToSnelstartJob + Horizon-supervisor + Sentry-bridge + job-test</name>
  <files>[NASCHOOL-REPO] /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Jobs/Tenant/SyncEnrollmentToSnelstartJob.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/horizon.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/queue.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/sentry.php, /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/tests/Feature/Jobs/Tenant/SyncEnrollmentToSnelstartJobTest.php</files>
  <read_first>
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Jobs/ — bestaande job-conventies (queue-naming, tries, backoff)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/horizon.php (huidige environments[]-block + supervisors)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/queue.php (huidige connections — redis-default)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/config/sentry.php als die bestaat; anders documenteren dat Naschool nog geen Sentry heeft en dat dit een follow-up wordt (geen scope-creep in deze plan — als Sentry niet geconfigureerd is, log naar Laravel-channel `naschool` of equivalent en flag voor productie-hardening in SUMMARY)
    - /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend/app/Support/EmeqHub/EmeqHubClient.php (Plan 14-02 output — DI-target)
  </read_first>
  <action>
    In de Naschool-repo:

    1. **app/Jobs/Tenant/SyncEnrollmentToSnelstartJob.php** — `final class SyncEnrollmentToSnelstartJob implements ShouldQueue` met traits `Dispatchable, InteractsWithQueue, Queueable, SerializesModels`. Constructor: `public function __construct(public readonly int $enrollmentId)`. Job-properties:
       - `public int $tries = 5;`
       - `public array $backoff = [10, 30, 60, 300, 900];` (exponential)
       - `public int $timeout = 30;`
       - `public string $queue = 'naschool';` (default-instance-property; listener-dispatch overschrijft toch, dit is veiligheid)

       `handle(EmeqHubClient $client): void`:
       - Laad Enrollment fresh: `$enrollment = Enrollment::findOrFail($this->enrollmentId)`
       - Bouw payload (zie `<interfaces>` boven voor het Snelstart-schema; gebruik tenant-config voor `relatie.id` + `boekingsperiode.id` zoals daar gemotiveerd)
       - Call `$client->createSnelstartVerkoopfactuur($payload)`
       - Bij `\RuntimeException` van de client: `throw $e` (Horizon retry-stack pakt het op via `tries` + `backoff`)

       `failed(\Throwable $e): void`:
       - Capture naar Sentry als `app()->bound('sentry')`: `app('sentry')->captureException($e, ['tags' => ['enrollment_id' => $this->enrollmentId]])`
       - Log altijd naar Laravel-log: `Log::error('SyncEnrollmentToSnelstartJob failed permanently', ['enrollment_id' => $this->enrollmentId, 'exception' => $e->getMessage()])` (geen PAT/clientkey-leak, alleen exception-message die in Plan 14-02 al geredacteerd was).

    2. **config/horizon.php** — voeg in elke `environments[]`-block (production, local, etc.) een supervisor toe of update de bestaande:
       - `'supervisor-naschool' => ['connection' => 'redis', 'queue' => ['naschool'], 'balance' => 'auto', 'maxProcesses' => 3, 'tries' => 0]` (job's `$tries` is bron-van-waarheid; supervisor-tries 0 = onbeperkt aan job overlaten)
       - Zorg dat de bestaande supervisors hun queue-array `['default', ...]` houden zonder `'naschool'` (om interferentie te voorkomen).

    3. **config/queue.php** — alleen aanpassen als er nog geen redis-`failed`-config staat. Failed-job retention: zet `'failed' => ['driver' => 'database-uuids', 'database' => env('DB_CONNECTION', 'pgsql'), 'table' => 'failed_jobs']` (Naschool's bestaande conventie volgen). Geen wijziging als al aanwezig.

    4. **config/sentry.php** — alleen als Naschool al Sentry heeft (check via `composer show sentry/sentry-laravel`). Zo ja: `'before_send' => fn ($event) => $event;` is voldoende (default werkt). Zo nee: documenteer in SUMMARY dat Sentry-installatie een follow-up is en de job-`failed()`-method valt automatisch terug op `Log::error`.

    5. **tests/Feature/Jobs/Tenant/SyncEnrollmentToSnelstartJobTest.php** — vijf tests met `Http::fake()`:
       - `test_handle_calls_hub_with_correct_snelstart_payload` (Http::fake → assert request-URL + body-shape)
       - `test_handle_succeeds_on_201_response`
       - `test_handle_throws_on_5xx_response` (bewijst retry-trigger)
       - `test_handle_throws_on_4xx_response`
       - `test_failed_method_logs_error_with_enrollment_id` (gebruik `Log::spy()` of bind een test-channel)

       Plus de Sentry-test alleen als sentry/sentry-laravel installed is, anders skip met `markTestSkipped`.

    6. Run `php artisan test --compact --filter=SyncEnrollmentToSnelstartJobTest`. Pint.

    7. Commit: `feat(emeq-hub): SyncEnrollmentToSnelstartJob + Horizon naschool-supervisor` in Naschool-repo.

    Geen Hub-repo wijzigingen.
  </action>
  <verify>
    <automated>cd /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend && php artisan test --compact --filter=SyncEnrollmentToSnelstartJobTest</automated>
  </verify>
  <acceptance_criteria>
    1. Job-class bestaat met `tries=5 + backoff-array + timeout=30 + queue=naschool`.
    2. Alle job-tests passed (minimum 4; Sentry-test optioneel).
    3. `php artisan horizon:list` (na Horizon-start in dev) toont de `supervisor-naschool` met queue `naschool`.
    4. Failed-job-test bewijst dat `failed()` Log::error aanroept met enrollment-id (en géén PAT-leak in de log-context).
    5. Job-payload bevat ten minste de minimum Snelstart-velden (factuurnummer, factuurdatum, factuurbedrag, factuurregels, relatie.id, boekingsperiode.id) — assert via Http::fake-body-inspection.
  </acceptance_criteria>
  <done>Job, Horizon-supervisor, failed-job-bridge, en 5 tests gelanded in Naschool. Live UAT (Plan 14-04) heeft nu een complete event→job→Hub→Snelstart-loop om te testen.</done>
</task>

</tasks>

<verification>
- Listener-pad: event → listener → job-dispatch op naschool-queue (3 tests in Task 1)
- Job-pad: handle calls EmeqHubClient → Snelstart pass-through (5 tests in Task 2)
- Horizon-supervisor `supervisor-naschool` actief
- Failed-job retention + Log/Sentry-bridge geconfigureerd
</verification>

<success_criteria>
1. Naschool: `php artisan test --compact --filter='DispatchSnelstartInvoiceSyncTest|SyncEnrollmentToSnelstartJobTest'` retourneert ≥8 passed.
2. `php artisan event:list | grep EnrollmentConfirmed` toont de listener-mapping.
3. `php artisan horizon:list` toont `supervisor-naschool`.
4. Alle changes gecommit op een Naschool-feature-branch.
</success_criteria>

<output>
Create `/Users/yusufkaracaburun/Sites/localhost/emeq-hub/.planning/phases/14-naschool-live-e2e/14-03-SUMMARY.md` when done.
SUMMARY moet vermelden: (1) Naschool-commit-SHAs; (2) of Sentry geconfigureerd was of follow-up is; (3) de gekozen `relatie.id` + `boekingsperiode.id` tenant-config-strategie (assumption uit `<interfaces>`).
</output>
