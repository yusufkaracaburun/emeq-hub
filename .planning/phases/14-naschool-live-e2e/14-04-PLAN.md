---
phase: 14-naschool-live-e2e
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md (new)
  - .planning/phases/14-naschool-live-e2e/artifacts/ (new — screenshots + DB-snippets)
  - .planning/STATE.md (updated — Phase 14 closure)
  - .planning/ROADMAP.md (updated — Phase 14 mark complete)
autonomous: false
requirements: [NSCH-07]
notes: |
  Hybrid plan: Task 1 = autonomous evidence-template-bootstrap in Hub-repo; Task 2 = blocking human-UAT met live test-ouder.
  Sluit Phase 8 human_verification items #1, #2, #5 (SC-3 Mollie checkout, SC-4 webhook fan-out, SC-5 smoke-runbook).
  E2E gebruikt Snelstart **test-env** subscription-key — NIET productie. Production-cutover wacht op Phase 12 closure.
  Phase-11-baseline-check: pre-flight verifieert dat Naschool's composer.lock emeq/snelstart-api pinst op ≥ v0.2.0 (Saloon-v4 baseline + SNEL-03/04 advisories-closure uit Phase 11).
must_haves:
  truths:
    - "Een echte test-ouder heeft minimaal één vrijwillige-bijdrage-checkout via Naschool-UI doorlopen waarbij Mollie-payment succeeded"
    - "De resulterende Mollie-webhook landde op de Hub en faned-out naar Naschool's webhook_callback_url; Naschool's enrollment-status werd zonder handmatige interventie ge-update"
    - "EnrollmentConfirmed-event triggerde SyncEnrollmentToSnelstartJob die een Snelstart-Verkoopfactuur creëerde in school A's Snelstart **test**-env"
    - "Pre-flight bevestigt expliciet dat Naschool's composer.lock emeq/snelstart-api ≥ v0.2.0 pinst (Phase-11 Saloon-v4-baseline) — anders regressie van SNEL-03/04 via transitive consumer"
    - "Bewijs is vastgelegd: (a) Mollie-dashboard-screenshot van school A's eigen Mollie-test-account; (b) Naschool-UI-screenshot met enrollment-status `paid`; (c) Hub pass_through_calls-DB-rij voor de Snelstart POST; (d) Snelstart-Mutaties-screenshot of API-GET-respons"
    - "Live E2E expliciet tegen Snelstart **test-env** — production-cutover gemarkeerd als follow-up backlog-item gekoppeld aan Phase 12"
  artifacts:
    - path: ".planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md"
      provides: "Single-document bewijs voor SC-4 + SC-5 incl. screenshots, DB-snippets, runbook-stap-volgorde"
      contains: "## Test-ouder UAT — gegevens"
    - path: ".planning/phases/14-naschool-live-e2e/artifacts/"
      provides: "Screenshots-folder met Mollie-dashboard, Naschool-UI, Snelstart-Mutaties beelden"
      contains: "*.png"
    - path: ".planning/STATE.md"
      provides: "Phase 14 closure-entry + NSCH-04..07 status → Complete"
      contains: "Phase 14"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 plan-list met 4 plans + status Complete"
      contains: "Phase 14: Naschool live E2E"
  key_links:
    - from: "NSCH-LIVE-EVIDENCE.md screenshots-references"
      to: "artifacts/*.png files"
      via: "relative markdown image-links"
      pattern: "!\\[.*\\]\\(artifacts/"
    - from: "NSCH-LIVE-EVIDENCE.md pass_through_calls-rij"
      to: "Hub-DB pass_through_calls table"
      via: "ingebedde SQL-snippet + JSON-dump van de relevante rij"
      pattern: "pass_through_calls"
---

<objective>
Voer de live end-to-end test uit met een echte test-ouder en leg het bewijs vast in `NSCH-LIVE-EVIDENCE.md`. Markeer Phase 14 + alle 4 NSCH-requirements als Complete in ROADMAP + STATE.

Purpose: NSCH-07 sluit, en daarmee sluit Phase 14 én sluit het laatste deel van v0.3 carry-forward voor de Naschool-track. Sluit ook drie Phase-8 human_verification items in één klap.
Output: NSCH-LIVE-EVIDENCE.md + screenshots-folder + STATE/ROADMAP-update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-naschool-live-e2e/14-01-PLAN.md
@.planning/phases/14-naschool-live-e2e/14-02-PLAN.md
@.planning/phases/14-naschool-live-e2e/14-03-PLAN.md
@.planning/milestones/v0.2-phases/08-naschool-wiring-snelstart-mollie-via-hub/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schrijf NSCH-LIVE-EVIDENCE.md template + pre-flight checklist + commit Phase 14-progress in STATE</name>
  <files>.planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md, .planning/phases/14-naschool-live-e2e/artifacts/.gitkeep, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (huidige `stopped_at` + `Open Blockers / Carry-forward`-sectie — voor accurate Phase 14-statusupdate)
    - .planning/ROADMAP.md regels 99-113 (Phase 14-block — gebruik dezelfde Plans-syntax als andere phases)
  </read_first>
  <action>
    In de Hub-repo (deze repo):

    1. **`.planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md`** — maak het document met de volgende secties:

       ```markdown
       # NSCH-LIVE-EVIDENCE — Phase 14 Live E2E
       
       **Test uitgevoerd:** YYYY-MM-DD HH:MM CEST
       **Test-ouder:** {alias of testbenaming — geen PII zoals echte naam/email; gebruik bv. "test-ouder-1"}
       **School:** {tenant-naam in Naschool, bv. "school-A-uat"}
       **Snelstart-env:** **TEST** (subscription-key uit test-account)
       **Mollie-mode:** test-mode (Connect op school A's test-Mollie-account)
       
       ## Pre-flight checklist
       
       - [ ] Plan 14-01 gelanded (composer.json + EmeqHubConfig) — commit-SHA: ___
       - [ ] Naschool composer.lock pinst emeq/snelstart-api ≥ v0.2.0 (Phase 11 Saloon-v4-baseline) — bevestig via `grep -A1 'emeq/snelstart-api' composer.lock`; gepinde versie: ___
       - [ ] Plan 14-02 gelanded (StancltenancyCredentialResolver + EmeqHubClient) — commit-SHA: ___
       - [ ] Plan 14-03 gelanded (event/listener/job/Horizon-supervisor) — commit-SHA: ___
       - [ ] Hub OnboardConsumer-wizard gebruikt om Naschool-Consumer aan te maken (incl. PAT-uitgifte) — Consumer-id: ___
       - [ ] Eerste Account aangemaakt voor school A — Account-id: ___
       - [ ] Mollie-OAuth via StartOAuthFlowAction gestart en succesvol callback ontvangen → Mollie-Connection.access_token aanwezig
       - [ ] Snelstart-Connection aangemaakt met test-env clientkey + subscription_key + subscription_id
       - [ ] Naschool tenant van school A heeft `emeq_hub_account_id` + `emeq_snelstart_default_relatie_id` + `emeq_snelstart_boekingsperiode_id` gezet in tenant.data jsonb (Plan 14-02/14-03 assumptions)
       - [ ] Naschool .env heeft `EMEQ_HUB_BASE_URL`, `EMEQ_HUB_PAT`, `EMEQ_HUB_NASCHOOL_ACCOUNT_ID` gezet
       - [ ] Horizon draait in Naschool met `supervisor-naschool` actief
       - [ ] Test-activiteit met vrijwillige bijdrage geseed in school A's tenant-DB
       
       ## Runbook — stap-volgorde
       
       1. Test-ouder logt in op Naschool-tenant van school A (URL: ___)
       2. Schrijft kind in voor de test-activiteit met vrijwillige bijdrage van € X,XX
       3. Naschool redirect naar Mollie checkout-URL (verkregen via Hub `/v1/mollie/payments`)
       4. Test-ouder voltooit Mollie-test-betaling (test-iDEAL-flow)
       5. Mollie POST webhook naar Hub
       6. Hub signature-verifieert + audit-logt + fan-out naar Naschool's `webhook_callback_url`
       7. Naschool verwerkt callback, update enrollment-status naar `paid`, fired `EnrollmentConfirmed`-event
       8. `DispatchSnelstartInvoiceSync` listener pushed `SyncEnrollmentToSnelstartJob` op `naschool`-queue
       9. Horizon picks up, job calls Hub `/v1/snelstart/Verkoopfacturen` met school A's `X-Account-Id`
       10. Hub doet Snelstart test-env API-call; Verkoopfactuur verschijnt in school A's Snelstart test-Mutaties
       
       ## Bewijs
       
       ### 1. Mollie-dashboard (school A's eigen Mollie test-account)
       ![Mollie payment](artifacts/01-mollie-payment.png)
       Payment-id: `tr_xxx`; status `paid`; amount € X,XX
       
       ### 2. Naschool-UI met enrollment-status `paid`
       ![Naschool enrollment paid](artifacts/02-naschool-enrollment-paid.png)
       Enrollment-id: `___`; verwachte status `paid`; observed `___`
       
       ### 3. Hub pass_through_calls — Snelstart POST-rij
       ```sql
       SELECT id, consumer_id, account_id, provider, method, path, status_code, created_at
       FROM pass_through_calls
       WHERE provider = 'snelstart' AND created_at > 'YYYY-MM-DD HH:MM:00'
       ORDER BY created_at DESC LIMIT 1;
       ```
       Resultaat:
       ```
       {plak hier psql-output of database-query MCP-output}
       ```
       
       ### 4. Snelstart-Mutaties bevestiging
       ![Snelstart verkoopfactuur](artifacts/04-snelstart-verkoopfactuur.png)
       Factuurnummer: `___`; bedrag: € X,XX; relatie: `___`
       
       ## Resultaat
       
       - [ ] SC-1 Composer-resolve (Plan 14-01) ✅
       - [ ] SC-2 StancltenancyCredentialResolver (Plan 14-02) ✅
       - [ ] SC-3 EnrollmentConfirmed listener+job (Plan 14-03) ✅
       - [ ] SC-4 Live E2E: ouder → Mollie → webhook → status `paid` → Snelstart-factuur ✅
       - [ ] SC-5 Evidence vastgelegd in dit document ✅
       
       ## Notes / Issues
       
       _(Vrije ruimte voor afwijkingen tijdens UAT, e.g. webhook-vertraging, retry-events, etc. Indien blocker → switch naar gap-closure-plan.)_
       
       ## Production-cutover follow-up
       
       Deze E2E draaide op Snelstart **test-env**. Productie-cutover (subscription-key wissel + opnieuw verifiëren) is een follow-up gekoppeld aan **Phase 12 (Snelstart productie-cert closeout)**. Deadline Phase 12: 2026-05-26. Geen Phase 14-scope.
       ```

    2. **`.planning/phases/14-naschool-live-e2e/artifacts/.gitkeep`** — lege placeholder zodat de folder commit-baar is voor het bewijs.

    3. **`.planning/STATE.md`** — update `stopped_at` + `last_activity` + `Open Blockers / Carry-forward`-sectie:
       - `stopped_at`: "Phase 14 plans geland + pre-flight evidence-template klaar — wacht op live UAT (Task 2)"
       - `last_activity`: vandaag (`Bash date +%Y-%m-%d`)
       - `Current Position` block: phase 14, plan 14-04
       - LAAT de Open Blockers-sectie ongewijzigd voor nu — Task 2 (na succesvolle UAT) verplaatst NSCH-LIVE-E2E naar Resolved.

    4. Commit deze drie files atomisch: `docs(14): NSCH-LIVE-EVIDENCE template + pre-flight checklist + STATE.md voortgang` in deze Hub-repo (op een feature-branch, niet master — git-policy).
  </action>
  <verify>
    <automated>test -f .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md && test -d .planning/phases/14-naschool-live-e2e/artifacts && grep -q "Pre-flight checklist" .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md && grep -q "Snelstart-env:.*TEST" .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md && grep -q "emeq/snelstart-api.*v0.2.0" .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    1. `NSCH-LIVE-EVIDENCE.md` bestaat met alle 6 secties (header, pre-flight, runbook, bewijs, resultaat, follow-up).
    2. `artifacts/.gitkeep` bestaat (folder is commit-baar).
    3. `STATE.md` heeft `stopped_at` ge-update naar Phase 14 + Plan 14-04.
    4. Document vermeldt expliciet "Snelstart-env: **TEST**" + production-cutover-follow-up koppelt aan Phase 12.
    5. Pre-flight checklist bevat de Phase-11-baseline-check-regel voor `emeq/snelstart-api ≥ v0.2.0` met `grep`-hint.
    6. Commit gelanded met message `docs(14): NSCH-LIVE-EVIDENCE template + pre-flight checklist + STATE.md voortgang`.
  </acceptance_criteria>
  <done>Evidence-template is klaar; UAT-uitvoerder weet welke screenshots/SQL-output in te vullen. Pre-flight checklist surfacert blockers (incl. Phase-11 SDK-pin) vóór de live test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT met test-ouder + vul evidence in + Phase-closure</name>
  <files>.planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md (filled), .planning/phases/14-naschool-live-e2e/artifacts/*.png (added), .planning/STATE.md (closure), .planning/ROADMAP.md (closure), .planning/REQUIREMENTS.md (NSCH-04..07 → Complete)</files>
  <what-built>
    Plans 14-01, 14-02, 14-03 hebben Naschool composer-wiring + EmeqHubConfig + StancltenancyCredentialResolver + EmeqHubClient + EnrollmentConfirmed event + listener + SyncEnrollmentToSnelstartJob + Horizon-supervisor gebouwd. Task 1 van dit plan heeft de evidence-template klaargezet. Deze checkpoint voert de live test uit en sluit de phase.
  </what-built>
  <action>
    Dit is een blocking checkpoint:human-verify. De `<how-to-verify>`-sectie hieronder bevat de complete uitvoeringsprocedure: pre-flight (incl. Phase-11 SDK-pin-check), live UAT door test-ouder, evidence-capture, en phase-closure (REQUIREMENTS/ROADMAP/STATE sync + closure-commit). Géén autonomous Claude-actie — uitvoerder is een mens met live Mollie-test-account, live Snelstart-test-env, en een test-ouder.
  </action>
  <how-to-verify>
    **Pre-flight (volg de checklist in NSCH-LIVE-EVIDENCE.md eerst — vul elke SHA + uuid in vóór de live test):**

    1. Bevestig dat alle drie Plan 14-01..03-commits op een lokale Naschool-branch staan en `php artisan test --compact` in Naschool groen is.
    2. **Phase-11-baseline-check (SDK-pin) — kritiek:**
       - [ ] Naschool composer.lock pinst emeq/snelstart-api ≥ v0.2.0 (Phase 11 Saloon-v4-baseline) — bevestig via `cd /Users/yusufkaracaburun/Sites/localhost/school-activities-hub/backend && grep -A1 'emeq/snelstart-api' composer.lock | grep version`. Verwachte output: `"version": "v0.2.x"` of hoger. Een v0.1.x-pin = harde STOP (regressie van SNEL-03/04 advisories-closure via transitive consumer; corrigeer eerst Plan 14-01 alvorens UAT te starten).
       - [ ] Naschool composer.lock pinst emeq/mollie-api ≥ v0.2.0 — zelfde grep-check op `'emeq/mollie-api'`.
       - Noteer beide gepinde versies in de NSCH-LIVE-EVIDENCE.md pre-flight-checklist-regel.
    3. Run `php artisan migrate:fresh --seed` in Naschool's tenant-DB voor school-A-uat indien nodig (geen prod-data).
    4. Start Horizon in Naschool: `php artisan horizon` — bevestig `supervisor-naschool` actief via `php artisan horizon:list`.
    5. Open een terminal die `pail`/`tail -f` op zowel Hub `storage/logs/laravel.log` als Naschool `storage/logs/laravel.log` draait.

    **Live UAT (test-ouder doorloopt):**

    6. Test-ouder logt in op Naschool-school-A-uat URL.
    7. Schrijft kind in voor de geseede vrijwillige-bijdrage-test-activiteit (€ 1,00 of vergelijkbaar laag bedrag).
    8. Wordt geredirected naar Mollie-test-checkout; voltooit met test-iDEAL "paid".
    9. Mollie-test-mode webhook landt binnen ~5s op Hub; controleer in Hub:
       ```bash
       psql -d emeq_hub -c "SELECT id, provider, method, path, status_code, created_at FROM webhook_calls WHERE provider='mollie' ORDER BY created_at DESC LIMIT 3;"
       ```
    10. Naschool verwerkt callback; enrollment-status moet `paid` worden in de tenant-UI binnen ~10s.
    11. `EnrollmentConfirmed` event fired → SyncEnrollmentToSnelstartJob pushed op `naschool`-queue → Horizon executed binnen ~30s.
    12. Controleer Hub `pass_through_calls` voor de Snelstart-POST:
        ```bash
        psql -d emeq_hub -c "SELECT id, account_id, provider, method, path, status_code, created_at FROM pass_through_calls WHERE provider='snelstart' AND path LIKE '%Verkoopfacturen%' ORDER BY created_at DESC LIMIT 1;"
        ```
    13. Login op school A's Snelstart-test-env-UI; bevestig dat de Verkoopfactuur zichtbaar is met correct bedrag + Relatie.

    **Bewijs vastleggen + commit (na succesvolle UAT):**

    14. Maak 4 screenshots: Mollie-dashboard, Naschool-UI enrollment-paid, Snelstart-Verkoopfactuur. Plaats ze in `.planning/phases/14-naschool-live-e2e/artifacts/` met de namen uit het template (01-04).
    15. Vul `NSCH-LIVE-EVIDENCE.md` in: SHAs, uuids, payment-id, enrollment-id, factuurnummer, psql-output van de pass_through_calls-query, gepinde SDK-versies uit pre-flight stap 2. Vink alle SC-checkboxes.
    16. Update `.planning/REQUIREMENTS.md` regels 13-16: NSCH-04, NSCH-05, NSCH-06, NSCH-07 markeer als `[x]` (Complete). Update traceability-tabel (regels 86-90) `Pending` → `Complete`.
    17. Update `.planning/ROADMAP.md` Phase 14-block: `**Plans:** 4 plans (Complete)` + plan-list met checkmarks (analoog aan Phase 13 + Phase 15 patterns). Update progress-tabel (regel 147): `4/4 | Complete | YYYY-MM-DD`.
    18. Update `.planning/STATE.md`:
        - `stopped_at`: "Phase 14 complete — NSCH-04..07 closed via live UAT op {date}; v0.3 op 11/12 (alleen Phase 12 open)"
        - `last_activity`: vandaag
        - `progress.completed_phases`: +1
        - `progress.completed_plans`: +4
        - `progress.percent`: hercompute
        - Verwijder `NSCH-LIVE-E2E` uit Open Blockers + voeg toe aan Resolved Blockers met `closed YYYY-MM-DD Phase 14`
    19. Commit alle changes als één commit: `docs(14): Phase 14 closure — NSCH-LIVE-EVIDENCE + ROADMAP/STATE/REQUIREMENTS sync (NSCH-04..07 Complete)`.

    **Bij blockers tijdens UAT:**

    - Pre-flight stap 2 faalt (emeq/snelstart-api < v0.2.0 of niet gepinst) → STOP, terug naar Plan 14-01 om de pin te corrigeren; UAT mag niet starten met een Phase-11-regressie.
    - Mollie-webhook landt niet → check Hub-side `webhook_calls`-tabel + Spatie-server-config; geen fix in dit plan, log issue + run `/gsd-debug` voor de specifieke component.
    - Enrollment-status update niet → check Naschool's webhook-callback-handler (bestaat die? — Phase 8 verification flagde dit als out_of_scope; mogelijk Naschool-side gap). Documenteer in NSCH-LIVE-EVIDENCE.md "Notes / Issues"-sectie en switch naar `/gsd-plan-phase 14 --gaps`.
    - Snelstart Verkoopfactuur faalt met 4xx → check payload-shape tegen partner-docs; mogelijk `relatie.id` / `boekingsperiode.id` assumption-correctie nodig (zie Plan 14-03 interfaces).
  </how-to-verify>
  <verify>
    <automated>grep -q "TEST" .planning/phases/14-naschool-live-e2e/NSCH-LIVE-EVIDENCE.md && grep -E "^\- \[x\] \*\*NSCH-0[4567]\*\*" .planning/REQUIREMENTS.md | wc -l | grep -q '^[[:space:]]*4$' && ls .planning/phases/14-naschool-live-e2e/artifacts/*.png 2>/dev/null | wc -l | grep -qE '^[[:space:]]*[4-9]$|^[[:space:]]*[1-9][0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    1. Pre-flight Phase-11-baseline-check uitgevoerd en bevestigd: Naschool composer.lock pinst emeq/snelstart-api ≥ v0.2.0 (gepinde versie ingevuld in NSCH-LIVE-EVIDENCE.md).
    2. Test-ouder heeft minimaal één succesvolle checkout doorlopen (Mollie-dashboard bevestigt `paid`).
    3. Hub `webhook_calls` toont de Mollie-webhook met status_code 2xx.
    4. Hub `pass_through_calls` toont de Snelstart-POST met status_code 2xx en provider `snelstart`.
    5. Snelstart-test-Mutaties tonen de Verkoopfactuur met juiste bedrag + relatie.
    6. `NSCH-LIVE-EVIDENCE.md` is volledig ingevuld (alle SHAs, uuids, payment-id, factuurnummer, SDK-versies + 4 screenshots in artifacts/).
    7. STATE, ROADMAP, REQUIREMENTS gesynchroniseerd: NSCH-04..07 = Complete; Phase 14 = Complete; v0.3 progress geüpdatet.
    8. Closure-commit gelanded.
  </acceptance_criteria>
  <done>Live UAT succesvol; NSCH-04..07 = Complete in REQUIREMENTS; Phase 14 = Complete in ROADMAP; v0.3 op 11/12 (alleen Phase 12 productie-cert open); closure-commit gelanded; NSCH-LIVE-EVIDENCE.md is single-source-bewijs incl. Phase-11-baseline-check.</done>
  <resume-signal>Type "approved" wanneer alle 8 acceptance-criteria gehaald zijn en de closure-commit is gelanded. Of beschrijf welke acceptance gefaald is + welke gap-closure-plan nodig is.</resume-signal>
</task>

</tasks>

<verification>
- NSCH-LIVE-EVIDENCE.md bestaat, volledig ingevuld, met 4 screenshots in artifacts/
- Pre-flight Phase-11-baseline-check geslaagd (emeq/snelstart-api ≥ v0.2.0 in Naschool composer.lock)
- Snelstart-env in document is bevestigd als TEST (geen productie-call)
- STATE, ROADMAP, REQUIREMENTS gesynchroniseerd
- Closure-commit gelanded
</verification>

<success_criteria>
1. Live UAT door test-ouder geslaagd (Mollie-paid → Snelstart-Verkoopfactuur zichtbaar).
2. NSCH-04..07 in REQUIREMENTS.md = Complete.
3. Phase 14 in ROADMAP.md = Complete (4/4 plans).
4. STATE.md `progress.completed_phases` = 4 (van 5 in v0.3); `Open Blockers` heeft alleen Snelstart productie-cert nog.
5. `NSCH-LIVE-EVIDENCE.md` is een single-document bewijs dat een externe reviewer kan inspecteren zonder nadere context (incl. bevestigde Phase-11-baseline-pin).
</success_criteria>

<output>
Create `/Users/yusufkaracaburun/Sites/localhost/emeq-hub/.planning/phases/14-naschool-live-e2e/14-04-SUMMARY.md` when done.
SUMMARY moet de UAT-datum + test-ouder-alias + 4 evidence-snippet-refs (commits/uuids/factuurnummer) + gepinde SDK-versies vermelden voor traceability.
</output>
