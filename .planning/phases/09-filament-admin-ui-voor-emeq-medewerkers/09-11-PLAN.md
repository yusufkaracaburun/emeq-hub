---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 11
type: execute
wave: 4
depends_on: ['09-01', '09-02', '09-03', '09-04', '09-05', '09-06', '09-07', '09-08', '09-09', '09-10']
files_modified:
  - .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md
  - .docs/decisions/filament-admin-panel.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: ['HUB-04']
must_haves:
  truths:
    - "Een geseede super-admin kan inloggen op `/admin` en ziet 7 resource-lijsten (Consumers / Connections / Accounts / WebhookCalls / AccountSubscriptions / Cashier-Subscriptions / Users)"
    - "Een geseede staff (zonder super-admin-role) ziet 6 resources (geen UserResource in sidebar) en krijgt 403 op `/admin/users`"
    - "ALLE 10 HUB-04 success-criteria uit `09-CONTEXT.md` (regels 234-246) zijn aantoonbaar bewezen — per criterium één bullet in 09-11-ACCEPTANCE.md met evidence (test-naam of CLI-output)"
    - "ADR `.docs/decisions/filament-admin-panel.md` documenteert de 5 D-decisions (D-01 scope / D-02 webhook-audit / D-03 PAT-presets / D-04 ProviderCredentialDescriptor / D-05 Spatie RBAC) + 3 expliciet uitgestelde items (audit-log / 2FA / Consumer self-service)"
    - "ROADMAP.md Phase 9 entry: `[x]` + `Done` status + `Plans: N/N` + `completed: 2026-XX-XX`"
    - "REQUIREMENTS.md HUB-04: `[x]` + `Complete`"
    - "STATE.md `progress` update: `completed_phases: 7`, `total_plans: 62` (51 + 11), `completed_plans: 52`, `percent: 84`"
  artifacts:
    - path: ".planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md"
      provides: "Phase-acceptance evidence-document met 10 success-criteria afvink + Decisions-tabel"
      contains: "## Acceptance Evidence"
    - path: ".docs/decisions/filament-admin-panel.md"
      provides: "ADR Phase 9 — 5 D-decisions + 3 deferred items"
      contains: "Phase 9: Filament admin-paneel"
  key_links:
    - from: ".planning/STATE.md"
      to: "ROADMAP.md + REQUIREMENTS.md"
      via: "Phase + HUB-04 status-sync"
      pattern: "HUB-04.*Complete"
---

<objective>
Sluit Phase 9 af volgens Phase-7-08-precedent: schrijf één acceptance-document met evidence voor alle 10 success-criteria, schrijf één ADR die de 5 D-decisions vastlegt, en sync de drie planning-bestanden (ROADMAP/REQUIREMENTS/STATE) zodat de milestone-tracker `HUB-04` als Complete reflecteert. Sluit af met human-verify checkpoint voor visuele review van het admin-paneel.

Geen Filament-code-mutaties — alle Resources zijn klaar na 09-05 t/m 09-10. Dit plan is uitsluitend papierwerk + checkpoint.

Purpose: Closure-discipline (zoals Phase 6-08, Phase 7-08): elke phase eindigt met evidence + ADR + state-sync zodat toekomstige phases op een geconsolideerde basis bouwen.

Output:
- 1 nieuw `.planning/phases/09-…/09-11-ACCEPTANCE.md`
- 1 nieuw `.docs/decisions/filament-admin-panel.md`
- 3 sync-edits (ROADMAP/REQUIREMENTS/STATE)
- 1 human-verify checkpoint voor visuele review
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@.planning/phases/07-account-level-subscriptions-use-case-b/07-08-ACCEPTANCE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 09-11-ACCEPTANCE.md — evidence voor alle 10 SC's</name>
  <files>.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md</files>
  <read_first>
    - `.planning/phases/07-account-level-subscriptions-use-case-b/07-08-ACCEPTANCE.md` — pattern voor acceptance-doc (frontmatter + SC-evidence-tabel + test-counts + Decisions-tabel)
    - 09-CONTEXT.md §"Success Criteria (revised)" regels 234-246 — 10 SC's
    - Outputs van plans 09-01 t/m 09-10 (test-counts uit hun verify-blocks)
  </read_first>
  <acceptance_criteria>
    - File bestaat onder `.planning/phases/09-…/09-11-ACCEPTANCE.md` met YAML-frontmatter (phase: 9, plan: 11, type: acceptance, status: pending-checkpoint)
    - `## Acceptance Evidence` sectie met genummerde tabel `[1..10] | SC-statement | Status (✅/⏭️/❌) | Evidence (test-naam of CLI-output)`
    - Per SC één bullet met concrete evidence:
      1. (super-admin 7 resources) → `php artisan route:list --path=admin` toont 7 resource-prefixes ➜ acceptance handmatig in human-verify checkpoint
      2. (staff 6 resources) → `PermissionGatingTest::test_staff_user_does_not_see_user_navigation_link`
      3. (non-role 403) → `PanelAccessTest::test_authenticated_user_without_role_cannot_access_admin_panel`
      4. (Issue-PAT) → `ConsumerTokenActionTest::test_staff_user_can_issue_pat_with_mollie_read_preset` + `PatAbilityPresetsTest::test_every_token_ability_is_covered`
      5. (no raw secrets in HTML) → `ConnectionFingerprintTest` 4 tests
      6. (Revoke via OAuthFlow) → `ConnectionRevokeActionTest::test_revoke_action_calls_oauth_flow_revoke`
      7. (WebhookCall filters) → `WebhookCallResourceTest::test_direction_filter_narrows_to_incoming`
      8. (state-machine respect) → `AccountSubscriptionStateActionsTest::test_cancel_action_on_already_canceled_throws_validation_notification_without_db_mutation`
      9. (UserResource super-admin only) → `PermissionGatingTest::test_staff_user_cannot_access_user_resource`
      10. (D-04 theoretical provider) → `ProviderDescriptorTest::test_adding_theoretical_provider_appears_in_all`
    - `## Test counts` sectie met total/full-suite (Phase 9 voegt ~30+ tests toe; verifieer exact aantal)
    - `## Decisions referenced` met D-01 t/m D-05 + 5 deferred items
    - `## Pending human-verify` sectie die checkpoint-criteria opsomt
  </acceptance_criteria>
  <action>
    Schrijf `09-11-ACCEPTANCE.md` met de exacte format-conventie uit `07-08-ACCEPTANCE.md`. Frontmatter:
    ```
    ---
    phase: 9
    plan: 11
    type: acceptance
    status: pending-checkpoint
    created: <today>
    ---
    ```

    Hoofdsecties:
    1. `# Phase 9 Acceptance — Filament admin-UI`
    2. `## Acceptance Evidence` — 10-rij tabel met SC + Status + Evidence
    3. `## Test counts` — full-suite count + delta-Phase-9 (gebruik `php artisan test --compact` post-run om exact getal te krijgen)
    4. `## Decisions referenced` — D-01 t/m D-05 + 5 deferred (zoals in CONTEXT.md regels 73-81)
    5. `## Pending human-verify` — checkpoint-criteria (login + 7 resources zien + 1× Issue-PAT + 1× Revoke + 1× Pause/Resume op AccountSubscription)
    6. `## Next steps after acceptance` — `HUB-04 → Complete`, mark Phase 9 in ROADMAP `[x]`, optioneel quick-task voor docs-sync skill activate
  </action>
  <verify>
    <automated>
    test -f .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md &&
    grep -q "## Acceptance Evidence" .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md &&
    grep -q "ProviderDescriptorTest" .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md &&
    grep -q "D-01" .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md &&
    grep -q "D-05" .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md
    </automated>
  </verify>
  <done>
    - 09-11-ACCEPTANCE.md geschreven met 10 SC-evidence-rijen
    - 5 D-decisions + deferred items gedocumenteerd
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR .docs/decisions/filament-admin-panel.md</name>
  <files>.docs/decisions/filament-admin-panel.md</files>
  <read_first>
    - Bestaande ADR's in `.docs/decisions/` als template (lees bv. `.docs/decisions/account-subscriptions.md` of `.docs/decisions/pass-through-calls-table.md` als die bestaan; gebruik anders een minimal-ADR-format Status/Context/Decision/Consequences)
    - 09-CONTEXT.md §"Decisions (2026-05-15 discussion)" regels 60-160 — D-01 t/m D-05 + rationale
  </read_first>
  <acceptance_criteria>
    - `.docs/decisions/filament-admin-panel.md` bestaat (let op: `.docs/` is gitignored per repo-conventie; file blijft lokaal, geen git-commit voor deze file — zie CLAUDE.md "Werkdocumentatie (lokaal, gitignored)")
    - ADR-format: Status (Accepted) / Date (today) / Context / Decisions (5 sub-secties D-01 t/m D-05) / Consequences / Out-of-scope (5 deferred items) / Verification-path (link naar 09-11-ACCEPTANCE.md)
    - Elke D-decision heeft: titel + rationale + concrete consequenties + link naar relevant plan-nummer (09-01 t/m 09-10)
  </acceptance_criteria>
  <action>
    Schrijf `.docs/decisions/filament-admin-panel.md` met de 5 D-decisions als sub-secties:
    - **D-01: 7 resources, PassThroughCall out-of-scope** — rationale + waarom PassThroughCall ge-deferred is (high volume, query-optimization niet binnen Phase-9-scope; backlog HUB-OBSERVABILITY)
    - **D-02: webhook_calls schema uitbreiden vóór Resource** — rationale (Spatie-default-shape mist direction/provider/consumer_id/status) + plan-nummer (09-01)
    - **D-03: PAT-abilities UX = 5 presets + custom-mode** — rationale (UX vs. flexibility tradeoff) + preset-test-vangnet (09-05)
    - **D-04: ProviderCredentialDescriptor-laag** — rationale (vermijd Resource-multiplicatie per provider) + invariant (één Resource + descriptor-aware sections) + plan-nummer (09-04 + 09-06)
    - **D-05: Spatie laravel-permission, drop is_emeq_staff boolean** — rationale (alignment met Spatie-stack + future-proof voor HUB-AUDIT) + 2-rol-model (super-admin/staff) + 6 permissions + plan-nummer (09-02 + 09-03 + 09-10)

    Out-of-scope sectie: PassThroughCallResource (HUB-OBSERVABILITY), audit-log (HUB-AUDIT), 2FA/MFA (v1.0+), Consumer self-service (v1.0+), Tailwind-thema-customizing, e-mail notifications, bulk-actions.

    Verification-path: link naar `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md` + bewijslast in test-suite (`tests/Feature/Admin/*`).
  </action>
  <verify>
    <automated>
    test -f .docs/decisions/filament-admin-panel.md &&
    grep -q "D-01" .docs/decisions/filament-admin-panel.md &&
    grep -q "D-05" .docs/decisions/filament-admin-panel.md &&
    grep -q "Out-of-scope\\|Out of scope" .docs/decisions/filament-admin-panel.md
    </automated>
  </verify>
  <done>
    - ADR bestaat met 5 D-decisions + deferred items + verification-path
  </done>
</task>

<task type="auto">
  <name>Task 3: Sync ROADMAP.md + REQUIREMENTS.md + STATE.md</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - `.planning/ROADMAP.md` regels 28-32 (Phase 9 checkbox) + regels 318-358 (Phase 9 Details) + regels 359-372 (Progress-tabel) + regels 392 (Coverage)
    - `.planning/REQUIREMENTS.md` regels 29-30 (HUB-04 entry) + regels 90 (Traceability)
    - `.planning/STATE.md` regels 1-15 (frontmatter) + `.planning/STATE.md` regels 28-31 (Current Position) + Accumulated Context (Decisions appendix)
  </read_first>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 9 hoofd-checkbox `- [x]` op regel 32; `(voltooid <YYYY-MM-DD>; 11/11 plans, HUB-04 SC-1..SC-10 bewezen)` append na korte beschrijving
    - ROADMAP Phase 9 Details: `**Plans:** 11 plans` (vervangt `**Plans:** TBD`); plan-checklist met 11 entries (`- [x] 09-01-PLAN.md — …` … `- [x] 09-11-PLAN.md — …`); refresh **Goal** indien noodzakelijk (ROADMAP zegt nu "4 resources" — update naar "7 resources" per D-01)
    - ROADMAP Progress-tabel regel "9. Filament admin-UI…": `11/11` + `Done` + `<date>`
    - `.planning/REQUIREMENTS.md` HUB-04: `- [x] **HUB-04**: …` + append `*Validated in Phase 9 (<date>) — 11/11 plans, HUB-04 SC-1..SC-10 bewezen via tests/Feature/Admin/*.*`; Traceability-tabel: `| HUB-04 | Phase 9 | Complete |`
    - `.planning/STATE.md` frontmatter: `status: ready`, `stopped_at: Phase 9 ACCEPTED <date> — HUB-04 Complete`, `last_updated: <today>T<time>+02:00`, `progress.completed_phases: 7` (was 6), `progress.total_plans: 62` (was 51), `progress.completed_plans: 52` (was 41), `progress.percent: 84` (was 60)
    - STATE.md `## Current Position`: `Phase: — (Phase 9 ACCEPTED; Phase 8 / Phase 5b / Phase 5c blijven open)`
    - STATE.md `## Accumulated Context > ### Decisions` append: `🆕 New <date> (09-…): <key decision summary>` voor elke D-01 t/m D-05 + plan-1-tot-10 milestones
    - STATE.md `## Pending Todos`: schrap `Phase 9 (parallel)` mention zodra het Complete is; voeg `/docs-sync skill activate` toe als suggested next-step (project skill geadviseerd in CLAUDE.md voor model/route/migration-changes)
  </acceptance_criteria>
  <action>
    1. Open `.planning/ROADMAP.md`. Wijzig regel 32 (Phase 9 checkbox) van `[ ]` naar `[x]` + append voltooid-info. Wijzig regel 320 (Goal) van "4 resources beheren" naar "7 resources beheren" (Consumers + Connections + Accounts + WebhookCalls + AccountSubscriptions + Cashier-Subscriptions + Users) per D-01. Vervang regel 356 (`**Plans:** TBD`) door `**Plans:** 11 plans` + plan-checklist met 11 entries:
       - `- [x] 09-01-PLAN.md — webhook_calls audit-kolommen-migratie (D-02)`
       - `- [x] 09-02-PLAN.md — Filament v4 + Spatie permission install`
       - `- [x] 09-03-PLAN.md — User-model (HasRoles + FilamentUser) + EmeqStaffSeeder`
       - `- [x] 09-04-PLAN.md — ProviderCredentialDescriptor + Connection::fingerprint() refactor (D-04)`
       - `- [x] 09-05-PLAN.md — ConsumerResource CRUD + Issue-PAT-action (D-03)`
       - `- [x] 09-06-PLAN.md — ConnectionResource read + revoke + no-secret-leak`
       - `- [x] 09-07-PLAN.md — AccountResource + WebhookCallResource (read-only)`
       - `- [x] 09-08-PLAN.md — AccountSubscriptionResource read + Pause/Resume/Cancel`
       - `- [x] 09-09-PLAN.md — Cashier\\SubscriptionResource read-only`
       - `- [x] 09-10-PLAN.md — UserResource super-admin gated + Spatie RBAC (D-05)`
       - `- [x] 09-11-PLAN.md — Phase-acceptance + ADR + planning-sync`
       Wijzig Progress-tabel regel: `| 9. Filament admin-UI voor Emeq-medewerkers | 11/11 | Done | <date> |`.

    2. Open `.planning/REQUIREMENTS.md`. Wijzig HUB-04 checkbox `[ ]` → `[x]` + append `*Validated in Phase 9 (<date>) — 11/11 plans, HUB-04 SC-1..SC-10 bewezen via tests/Feature/Admin/*.*`. Wijzig Traceability-tabel rij `HUB-04`: status van `Pending` → `Complete`.

    3. Open `.planning/STATE.md`. Update frontmatter:
       - `status: ready`
       - `stopped_at: Phase 9 ACCEPTED <date> — HUB-04 Complete`
       - `last_updated: <today>T<HH:MM>+02:00`
       - `last_activity: <date> -- Phase 09 ACCEPTED via human-verify checkpoint; 11/11 plans, ADR filament-admin-panel.md geland`
       - `progress.completed_phases: 7`
       - `progress.total_plans: 62`
       - `progress.completed_plans: 52`
       - `progress.percent: 84`

    4. Update `## Current Position`-block om Phase 9 te markeren als ACCEPTED + Phase 8 next (of parallel met Phase 5b/5c).

    5. Append `## Accumulated Context > ### Decisions` met:
       - `🆕 New <date> (09-01): webhook_calls krijgt direction/provider/consumer_id/status audit-kolommen — additive forward-only migratie; defaults (incoming/processed) maken bestaande Spatie-rows valide. Phase 5c (Snelstart webhook-handler in progress) vult automatisch in.`
       - `🆕 New <date> (09-02): Filament v4 + Spatie laravel-permission ^6 geïnstalleerd; AdminPanelProvider op /admin met web-guard.`
       - `🆕 New <date> (09-03): RBAC via Spatie 2-rol-model (super-admin + staff) met 6 permissions; is_emeq_staff boolean reversed (D-05). EmeqStaffSeeder env-driven idempotent.`
       - `🆕 New <date> (09-04): ProviderCredentialDescriptor-laag — config/hub-providers.php + value-object. Connection::fingerprint() descriptor-aware refactor met gedragsbehoud (ConnectionEncryptionTest blijft groen).`
       - `🆕 New <date> (09-05..09-10): 7 Filament Resources gelanced (Consumer CRUD / Connection read+revoke / Account read / WebhookCall viewer / AccountSubscription read+state-flip / Cashier Subscription read / User super-admin-gated). 30+ feature-tests onder tests/Feature/Admin/.`

    6. In `### Pending Todos`: schrap "of parallel Phase 9 (Filament admin-UI)" uit Phase-7-completion-bullet. Append: `- 🆕 Suggested next: /docs-sync skill activate vóór commit — Phase 9 raakte User-model + AppServiceProvider + bestaande Connection-model; documentatie-drift mogelijk in .docs/`.

    Geen git-commits in dit task — checkpoint volgt.
  </action>
  <verify>
    <automated>
    grep -q "^- \\[x\\] \\*\\*Phase 9:" .planning/ROADMAP.md &&
    grep -q "11/11" .planning/ROADMAP.md &&
    grep -q "09-01-PLAN.md" .planning/ROADMAP.md &&
    grep -q "09-11-PLAN.md" .planning/ROADMAP.md &&
    grep -q "^- \\[x\\] \\*\\*HUB-04\\*\\*" .planning/REQUIREMENTS.md &&
    grep -q "HUB-04 | Phase 9 | Complete" .planning/REQUIREMENTS.md &&
    grep -q "completed_phases: 7" .planning/STATE.md &&
    grep -q "Phase 9 ACCEPTED" .planning/STATE.md
    </automated>
  </verify>
  <done>
    - ROADMAP Phase 9 [x] + 11/11 + 11 plan-bullets
    - REQUIREMENTS HUB-04 [x] + Complete
    - STATE frontmatter + Current Position + Decisions + Pending Todos gesynced
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify checkpoint — visuele review admin-paneel</name>
  <what-built>
    11/11 plans afgerond. 7 Filament Resources live op `/admin`. 5 D-decisions geïmplementeerd. Acceptance-evidence + ADR geschreven. ROADMAP/REQUIREMENTS/STATE klaar voor commit.
  </what-built>
  <how-to-verify>
    1. Run lokaal:
       ```
       docker compose up -d
       EMEQ_STAFF_SEED_EMAIL=admin@emeq.test \
         EMEQ_STAFF_SEED_PASSWORD=demo-secret \
         php artisan db:seed --class=EmeqStaffSeeder --no-interaction
       php artisan serve --port=8001 &
       ```

    2. Open `http://hub.emeq.test:8090/admin/login` in browser. Login als `admin@emeq.test` / `demo-secret`.

    3. Visuele check (super-admin sees 7 resources):
       - Sidebar toont: Consumers / Connections / Accounts / Webhook calls / Account subscriptions / Cashier subscriptions / Users (7 navigation-items)
       - Klik door alle 7 lijsten — moeten allemaal renderen zonder 500-errors

    4. Functionele check (1 happy-path per critical action):
       - **ConsumerResource**: maak een test-Consumer `slug=ckpt-test-09`; klik Issue PAT met preset "Mollie read-only" → Notification toont plain-token. Kopieer token. Test via `curl -H "Authorization: Bearer <token>" http://hub.emeq.test:8090/v1/ping` → moet 200 retourneren.
       - **ConnectionResource**: open Detail-view van een bestaande Mollie-Connection (seed via factory in tinker als nodig). Bevestig dat Mollie-section zichtbaar is + fingerprint-kolom toont sha256-hash, GEEN raw access_token. Open een Snelstart-Connection — Snelstart-section zichtbaar; Revoke-action verborgen.
       - **AccountSubscriptionResource**: seed een Active-subscription. Klik Pause-action → bevestig dat status flipt naar Paused (in browser-UI). Klik Resume → terug naar Active.
       - **UserResource (super-admin)**: bereik `/admin/users`. Maak een nieuwe User. Klik Assign role → kies "staff".
       - **UserResource (staff)**: log uit, log in als de zojuist aangemaakte staff-user (`SetCookie`/incognito). Sidebar moet GEEN "Users" link tonen. Bereik direct `/admin/users` via URL → 403.

    5. Sluit `php artisan serve`-proces.

    6. Reply: typ "approved" als alle 5 functionele checks slagen, of beschrijf welke faalt + verwachte vs. werkelijk gedrag.
  </how-to-verify>
  <action>Pauzeer execution — geen geautomatiseerde mutaties tot user "approved" reply op de visuele review-checklist in &lt;how-to-verify&gt;.</action>
  <resume-signal>Type "approved" om Phase 9 te accepteren + planning-sync te committen, of beschrijf bezwaren (specifiek welk Resource + welke check faalt).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Git-commit planning-sync (na "approved")</name>
  <files>
    .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - Phase 7-08 commit-pattern (controleer `git log --grep="07-08"`) — commit-message-stijl
  </read_first>
  <acceptance_criteria>
    - Commit-message: `docs(phase-09): accepteer Phase 9 — HUB-04 Complete, Filament admin-paneel live op /admin` (NL-conventie)
    - Commit bevat: 09-11-ACCEPTANCE.md + ROADMAP.md + REQUIREMENTS.md + STATE.md (NIET `.docs/decisions/filament-admin-panel.md` — gitignored)
    - Geen `--no-verify`, geen force-push (CLAUDE.md git-policy)
    - Geen `git push` (CLAUDE.md: nooit pushen zonder approval)
  </acceptance_criteria>
  <action>
    1. `git status` om te bevestigen welke files staged moeten worden.
    2. `git add` voor exact: `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`. **NIET** `.docs/decisions/filament-admin-panel.md` (gitignored).
    3. `git commit -m "docs(phase-09): accepteer Phase 9 — HUB-04 Complete, Filament admin-paneel live op /admin"` (geen Claude-attribution per CLAUDE.md conventie).
    4. `git status` om te bevestigen dat de commit landde.
    5. **Geen `git push`** — wachten op user-approval voor push (CLAUDE.md git-policy).
  </action>
  <verify>
    <automated>
    git log --oneline -1 | grep -q "docs(phase-09)" &&
    git log --oneline -1 | grep -q "HUB-04 Complete"
    </automated>
  </verify>
  <done>
    - Commit geland (lokaal)
    - User kan zelf `git push` doen wanneer gewenst
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Acceptance-document → repo | Documentatie-artefact; geen runtime risk. |
| Planning-sync → milestone-tracker | ROADMAP/REQUIREMENTS/STATE-mutatie heeft geen prod-impact; alleen documentatie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11-01 | Information Disclosure | EMEQ_STAFF_SEED_PASSWORD per ongeluk in 09-11-ACCEPTANCE.md gecommit | mitigate | Geen plain-text password in acceptance-doc. Productie-bootstrap-proces (env-vars) staat in ADR maar zonder concrete waarde. CLAUDE.md "never commit secrets" verbiedt dit expliciet. |
| T-09-11-02 | Tampering | Force-push of `--no-verify` tijdens commit | mitigate | CLAUDE.md git-policy expliciet verbiedt beide; Task 5 action zegt `geen --no-verify`. |
| T-09-11-03 | Repudiation | Wie accepteerde Phase 9? | accept | Human-verify checkpoint capture is per ontwerp informeel — user-conversatie geldt als accept. Toekomstige tightening: digitale signing of audit-log (HUB-AUDIT backlog). |
| T-09-11-04 | Elevation of Privilege | Per ongeluk `php artisan db:seed --class=EmeqStaffSeeder` in productie zonder env-vars-gate | accept | Seeder is no-op zonder env-vars (plan 09-03 task 2 acceptance_criteria). Documenteren in ADR. |
| T-09-11-SC | Tampering | Geen package-install | accept | Plan 09-11 wijzigt alleen documentatie + git-state. |
</threat_model>

<verification>
1. `09-11-ACCEPTANCE.md` bestaat met 10 SC-evidence-rijen
2. `.docs/decisions/filament-admin-panel.md` bestaat met 5 D-decisions
3. ROADMAP.md Phase 9 `[x]` + 11/11 + 11 plan-bullets
4. REQUIREMENTS.md HUB-04 `[x]` + Complete
5. STATE.md frontmatter completed_phases=7, percent=84
6. Human-verify checkpoint approved
7. Git-commit landde (geen push)
</verification>

<success_criteria>
- Phase 9 volledig afgesloten met acceptance-evidence + ADR + planning-sync
- HUB-04 Complete in milestone-tracker
- v0.2-milestone-progress: 7/10 phases done (60% → 70%; restant: Phase 5b, 5c, 8)
- User kan via `git push` (na separate approval) Phase 9 naar remote brengen
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-SUMMARY.md` when done
</output>
