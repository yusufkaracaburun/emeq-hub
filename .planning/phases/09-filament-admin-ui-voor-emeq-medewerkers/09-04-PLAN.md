---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 04
type: execute
wave: 2
depends_on: ['09-02']
files_modified:
  - config/hub-providers.php
  - app/Support/ProviderCredentialDescriptor.php
  - app/Models/Connection.php
  - tests/Feature/Admin/ProviderDescriptorTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "`ProviderCredentialDescriptor::for('mollie')` retourneert een descriptor met `encryptedFields=['access_token','refresh_token']`, `primaryFingerprintLabel='OAuth token'`, `oauthFlowKey='mollie'`"
    - "`ProviderCredentialDescriptor::for('snelstart')` retourneert een descriptor met `encryptedFields=['client_key','subscription_key']`, `primaryFingerprintLabel='Client key'`, `oauthFlowKey=null`"
    - "`ProviderCredentialDescriptor::all()` retourneert ALLE descriptors via config-discovery — een nieuwe rij in `config/hub-providers.php` verschijnt automatisch zonder Filament-code-wijziging (D-04 invariant)"
    - "`Connection::fingerprint()` retourneert dezelfde SHA256[0..12]-output als vóór de refactor voor Mollie en Snelstart Connections (bestaande tests blijven groen)"
    - "`Connection::fingerprint()` op een Connection met onbekende provider retourneert `null` (defensive fallback, bestaande test blijft groen)"
  artifacts:
    - path: "config/hub-providers.php"
      provides: "Provider-descriptor declarations (D-04)"
      contains: "'mollie' => ["
    - path: "app/Support/ProviderCredentialDescriptor.php"
      provides: "Value-object + factory voor provider-credential-metadata"
      contains: "final class ProviderCredentialDescriptor"
    - path: "tests/Feature/Admin/ProviderDescriptorTest.php"
      provides: "Test die discovery-contract + theoretische provider-toevoeging bewijst"
      contains: "class ProviderDescriptorTest"
  key_links:
    - from: "app/Support/ProviderCredentialDescriptor.php"
      to: "config/hub-providers.php"
      via: "config('hub-providers.{$provider}') lookup"
      pattern: "config\\(\"hub-providers"
    - from: "app/Models/Connection.php"
      to: "app/Support/ProviderCredentialDescriptor.php"
      via: "descriptor-aware fingerprint()-method"
      pattern: "ProviderCredentialDescriptor::for"
---

<objective>
Land de provider-agnostische credential-laag (D-04). Maak een `final class ProviderCredentialDescriptor` met `::for($provider)` + `::all()` discovery-methods die `config/hub-providers.php` lezen. Refactor `Connection::fingerprint()` zodat het de descriptor gebruikt in plaats van de hardcoded `match`-arm — zónder gedrag-verandering voor Mollie en Snelstart. Schrijf één test die bewijst dat een theoretische nieuwe provider via config-row automatisch ontdekt wordt zonder Filament-code te wijzigen (success-criterium 10 in CONTEXT.md).

Geen Filament-Resource in dit plan. ConnectionResource (plan 09-06) gebruikt deze laag — Connection-Resource form-detail-sections conditioneren op `descriptor->oauthFlowKey !== null`.

Purpose: Vermijdt resource-multiplicatie per provider (zelfde scaling-lesson als Scramble-groepering, zie ROADMAP-backlog SCRAMBLE-NESTED-GROUPS). Een nieuwe provider toevoegen = nieuwe descriptor-rij + nieuwe form-section, niet een nieuwe Filament-class.

Output:
- 1 nieuwe config-file
- 1 nieuw Support-class
- 1 model-mutatie (Connection::fingerprint() refactor — gedragsbehoud!)
- 1 nieuwe feature-test
- 0 wijzigingen aan ConnectionEncryptionTest of bestaande fingerprint-asserts (moeten groen blijven)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@app/Sanctum/TokenAbilities.php
@config/billing-plans.php
@app/Models/Connection.php
@app/Billing/PlanResolver.php
@tests/Feature/ConnectionEncryptionTest.php

<interfaces>
<!-- Discovery-contract analog -->
From app/Sanctum/TokenAbilities.php:
```php
final class TokenAbilities
{
    public const SNELSTART_READ = 'snelstart:read';
    // ...

    /** @return list<string> */
    public static function all(): array
    {
        return [self::SNELSTART_READ, ...];
    }
}
```
→ ProviderCredentialDescriptor volgt zelfde shape: `final class` + static factory + static `::all()` discovery.

<!-- Config-array analog -->
From config/billing-plans.php:
```php
return [
    'naschool-license' => [
        'amount' => [...],
        'interval' => '1 month',
        // ...
    ],
];
```
→ `config/hub-providers.php` volgt zelfde shape: slug-keyed associative array.

<!-- Bestaande fingerprint-method (refactor-target) -->
From app/Models/Connection.php regels 46-55:
```php
public function fingerprint(): ?string
{
    $secret = match ($this->provider) {
        'snelstart' => $this->client_key,
        'mollie' => $this->access_token,
        default => null,
    };

    return $secret ? substr(hash('sha256', $secret), 0, 12) : null;
}
```
→ Refactor: vervang `match`-arm door descriptor-lookup. Bestaande tests in `tests/Feature/ConnectionEncryptionTest.php` (test_fingerprint_returns_truncated_sha256_for_mollie + test_fingerprint_returns_null_for_unknown_provider) MOETEN groen blijven.

<!-- Config-driven exception pattern -->
From app/Billing/PlanResolver.php:
```php
public function find(string $slug): array
{
    $plan = config("billing-plans.{$slug}");
    if (! is_array($plan)) {
        throw UnknownPlanException::forSlug($slug);
    }
    return $plan;
}
```
→ ProviderCredentialDescriptor::for() gebruikt zelfde guard-pattern; gooit `InvalidArgumentException` met NL-message bij onbekende provider (consistent met `OAuthFlowRegistry::for()`-conventie uit Phase 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config/hub-providers.php + ProviderCredentialDescriptor value-object</name>
  <files>
    config/hub-providers.php,
    app/Support/ProviderCredentialDescriptor.php
  </files>
  <read_first>
    - `app/Sanctum/TokenAbilities.php` (volledig, ~25 regels) — `final class` + `::all()` pattern + `declare(strict_types=1)`
    - `config/billing-plans.php` (volledig) — slug-keyed associative array shape + decision-tag comment-stijl
    - `app/Billing/PlanResolver.php` regels 1-30 — `config(...)` + guard-clause exception-pattern
    - 09-CONTEXT.md §D-04 (regels 111-134) — descriptor-shape + 4 fields (`key`, `encryptedFields`, `primaryFingerprintLabel`, `oauthFlowKey`)
    - 09-PATTERNS.md regels 306-449 — exacte twee files-shape (config + value-object)
  </read_first>
  <acceptance_criteria>
    - `config/hub-providers.php` bestaat met `declare(strict_types=1)` (config-files in deze repo: bekijk bestaande conventie — `config/billing-plans.php` heeft het niet; **volg de billing-plans-conventie**: geen `declare`, wel decision-tag-comment top-of-file met `D-04`-reference)
    - Config heeft 2 entries: `'mollie' => [...]` + `'snelstart' => [...]` met 3 keys per entry: `encrypted_fields` (array), `primary_label` (string), `oauth_flow_key` (string|null)
    - `mollie`: `encrypted_fields=['access_token', 'refresh_token']`, `primary_label='OAuth token'`, `oauth_flow_key='mollie'`
    - `snelstart`: `encrypted_fields=['client_key', 'subscription_key']`, `primary_label='Client key'`, `oauth_flow_key=null`
    - `app/Support/ProviderCredentialDescriptor.php` bestaat als `final class` met `declare(strict_types=1)` (volg `TokenAbilities` + `MollieHeaderForwarder` conventie voor Support/Sanctum files)
    - Class heeft `__construct(public readonly string $key, public readonly array $encryptedFields, public readonly string $primaryFingerprintLabel, public readonly ?string $oauthFlowKey)` (constructor property promotion)
    - Static `for(string $provider): self` method leest `config("hub-providers.{$provider}")` + gooit `InvalidArgumentException` met NL-message bij onbekend provider
    - Static `all(): array` returnt `list<self>` via `array_map` + `array_keys(config('hub-providers', []))`
    - Pint clean
    - `php artisan tinker --execute 'echo (App\Support\ProviderCredentialDescriptor::for("mollie"))->oauthFlowKey;'` print `mollie`
  </acceptance_criteria>
  <action>
    **`config/hub-providers.php`:** Maak handmatig aan (geen artisan-command voor config-files). Body volgt `config/billing-plans.php`-stijl:
    - Top-of-file `<?php\n\n` (geen `declare(strict_types=1)` — config-conventie)
    - Comment-block boven `return`:
      ```
      /*
       * D-04: ProviderCredentialDescriptor declarations. Eén rij per provider.
       * Filament's ConnectionResource leest dit voor per-provider conditional
       * form-sections, fingerprint-resolution en revoke-action visibility.
       */
      ```
    - `return [` met 2 entries:
      - `'mollie' => ['encrypted_fields' => ['access_token', 'refresh_token'], 'primary_label' => 'OAuth token', 'oauth_flow_key' => 'mollie'],`
      - `'snelstart' => ['encrypted_fields' => ['client_key', 'subscription_key'], 'primary_label' => 'Client key', 'oauth_flow_key' => null],`

    **`app/Support/ProviderCredentialDescriptor.php`:** Maak aan via `php artisan make:class Support/ProviderCredentialDescriptor --no-interaction`. Vervang body door:
    - `declare(strict_types=1);` (volg `TokenAbilities` conventie)
    - Namespace `App\Support`
    - Use: `InvalidArgumentException`
    - `final class ProviderCredentialDescriptor`
    - Constructor met 4 readonly properties (`$key`, `$encryptedFields`, `$primaryFingerprintLabel`, `$oauthFlowKey`) — PHPDoc `@param list<string> $encryptedFields`
    - `public static function for(string $provider): self` — lees `$cfg = config("hub-providers.{$provider}");`, guard `if (! is_array($cfg)) throw new InvalidArgumentException("Onbekende provider: {$provider}");`, return `new self(key: $provider, encryptedFields: $cfg['encrypted_fields'], primaryFingerprintLabel: $cfg['primary_label'], oauthFlowKey: $cfg['oauth_flow_key'] ?? null);`
    - `public static function all(): array` — return `array_map(fn (string $key) => self::for($key), array_keys(config('hub-providers', [])));`

    Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    test -f config/hub-providers.php &&
    test -f app/Support/ProviderCredentialDescriptor.php &&
    grep -q "'mollie' =>" config/hub-providers.php &&
    grep -q "'snelstart' =>" config/hub-providers.php &&
    grep -q "final class ProviderCredentialDescriptor" app/Support/ProviderCredentialDescriptor.php &&
    grep -q "public static function for" app/Support/ProviderCredentialDescriptor.php &&
    grep -q "public static function all" app/Support/ProviderCredentialDescriptor.php &&
    php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$k = \$app->make(Illuminate\Contracts\Console\Kernel::class); \$k->bootstrap(); \$d = App\Support\ProviderCredentialDescriptor::for('mollie'); echo (\$d->oauthFlowKey === 'mollie' && \$d->primaryFingerprintLabel === 'OAuth token') ? 'OK' : 'FAIL';" &&
    ./vendor/bin/pint --test --format agent app/Support/ProviderCredentialDescriptor.php config/hub-providers.php
    </automated>
  </verify>
  <done>
    - 2 nieuwe files: config + value-object
    - `::for()` + `::all()` werken
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connection::fingerprint() refactor (gedragsbehoud)</name>
  <files>app/Models/Connection.php</files>
  <read_first>
    - `app/Models/Connection.php` regels 46-55 — huidige `fingerprint()`-method (de match-arm die we vervangen)
    - `tests/Feature/ConnectionEncryptionTest.php` `test_fingerprint_returns_truncated_sha256_for_mollie` + `test_fingerprint_returns_truncated_sha256_for_snelstart` + `test_fingerprint_returns_null_for_unknown_provider` — MOETEN groen blijven na refactor
    - 09-PATTERNS.md regels 383-401 — refactor-pattern (encrypted_fields[0] als primary)
  </read_first>
  <acceptance_criteria>
    - `Connection::fingerprint()`-method gebruikt geen `match($this->provider)` meer — wel `ProviderCredentialDescriptor::for($this->provider)`
    - Voor onbekende provider: descriptor gooit `InvalidArgumentException`. Vang dat en return `null` (gedragsbehoud — bestaande test `test_fingerprint_returns_null_for_unknown_provider`)
    - Voor Mollie: primary encrypted-field is `'access_token'` (eerste element van `encryptedFields`); `substr(hash('sha256', $this->access_token), 0, 12)` is identiek aan voorheen
    - Voor Snelstart: primary encrypted-field is `'client_key'`; `substr(hash('sha256', $this->client_key), 0, 12)` is identiek aan voorheen
    - Bestaande tests in `tests/Feature/ConnectionEncryptionTest.php` blijven groen ZONDER wijziging aan die file (gedrag is exact behouden)
    - Pint clean
  </acceptance_criteria>
  <behavior>
    - Identiek output voor Mollie + Snelstart Connections
    - Null voor onbekende provider (gevangen exception)
    - Geen wijziging aan ConnectionEncryptionTest
  </behavior>
  <action>
    Open `app/Models/Connection.php`. Voeg toe boven de class: `use App\Support\ProviderCredentialDescriptor;` + `use InvalidArgumentException;`.

    Vervang `fingerprint()`-method body (regels 46-55) door:
    ```
    public function fingerprint(): ?string
    {
        try {
            $descriptor = ProviderCredentialDescriptor::for($this->provider);
        } catch (InvalidArgumentException) {
            return null;
        }

        $primaryField = $descriptor->encryptedFields[0] ?? null;

        if (! $primaryField) {
            return null;
        }

        $secret = $this->{$primaryField};

        return $secret ? substr(hash('sha256', (string) $secret), 0, 12) : null;
    }
    ```

    Geen andere wijzigingen in Connection-model (engineering.md "chirurgisch wijzigen"). Run `./vendor/bin/pint --dirty --format agent`.

    Verifieer immediately dat bestaande tests groen blijven: `php artisan test --compact --filter=ConnectionEncryptionTest` MOET 100% groen blijven zónder dat we de testfile zelf wijzigen.
  </action>
  <verify>
    <automated>
    grep -q "ProviderCredentialDescriptor::for" app/Models/Connection.php &&
    ! grep -E "match \(\\\$this->provider\)" app/Models/Connection.php &&
    php artisan test --compact --filter=ConnectionEncryptionTest &&
    ./vendor/bin/pint --test --format agent app/Models/Connection.php
    </automated>
  </verify>
  <done>
    - `match`-arm vervangen door descriptor-lookup
    - Bestaande ConnectionEncryptionTest groen (3 fingerprint-tests + 4 andere)
    - Geen wijziging aan testfile
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProviderDescriptorTest — config-driven discovery</name>
  <files>tests/Feature/Admin/ProviderDescriptorTest.php</files>
  <read_first>
    - `tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php` regels 1-60 — `config(...)`-runtime-override pattern + constant-asserter
    - 09-PATTERNS.md regels 793-842 — exact test-shape (3 testcases)
    - 09-CONTEXT.md §"Success Criteria" punt 10 — D-04 invariant: nieuwe provider toevoegen zonder Filament-code-wijziging
  </read_first>
  <acceptance_criteria>
    - `tests/Feature/Admin/ProviderDescriptorTest.php` bestaat met namespace `Tests\Feature\Admin`
    - Test 1: `test_mollie_descriptor_resolves_from_config` — `ProviderCredentialDescriptor::for('mollie')` → asserteer `encryptedFields === ['access_token', 'refresh_token']`, `primaryFingerprintLabel === 'OAuth token'`, `oauthFlowKey === 'mollie'`
    - Test 2: `test_snelstart_descriptor_has_null_oauth_flow_key` — `ProviderCredentialDescriptor::for('snelstart')` → asserteer `oauthFlowKey === null`
    - Test 3: `test_unknown_provider_throws_invalid_argument_exception` — `$this->expectException(InvalidArgumentException::class); ProviderCredentialDescriptor::for('unknown-provider');`
    - Test 4: `test_adding_theoretical_provider_appears_in_all` — gebruik `config(['hub-providers.moneybird' => [...]])` runtime-override; asserteer `'moneybird'` voorkomt in `array_map(fn ($d) => $d->key, ProviderCredentialDescriptor::all())` (D-04 success-criterium 10)
    - Alle 4 tests groen
    - Full suite blijft groen
  </acceptance_criteria>
  <behavior>
    - Config-lookup werkt voor bestaande providers
    - Onbekende provider gooit InvalidArgumentException
    - Runtime config-overlay → discovery-output verandert (config-driven)
  </behavior>
  <action>
    Maak `tests/Feature/Admin/ProviderDescriptorTest.php` aan via `php artisan make:test --phpunit Admin/ProviderDescriptorTest --no-interaction`. **Geen** `RefreshDatabase` — test draait op constants + config, geen DB.

    Imports: `App\Support\ProviderCredentialDescriptor`, `InvalidArgumentException`, `Tests\TestCase`.

    Schrijf 4 tests zoals beschreven in acceptance_criteria. Voor Test 4: gebruik `config(['hub-providers.moneybird' => ['encrypted_fields' => ['access_token', 'refresh_token'], 'primary_label' => 'OAuth token', 'oauth_flow_key' => 'moneybird']])` om een theoretische provider toe te voegen tijdens de test. Asserteer dat `array_map(fn ($d) => $d->key, ProviderCredentialDescriptor::all())` deze key bevat.

    Run via `php artisan test --compact --filter=ProviderDescriptorTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ProviderDescriptorTest && php artisan test --compact --filter=ConnectionEncryptionTest && php artisan test --compact</automated>
  </verify>
  <done>
    - 4 tests groen
    - Full suite groen
    - D-04 success-criterium 10 bewezen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/hub-providers.php` → Connection-model | Config is read-only repo-artefact (geen runtime mutation in productie). Descriptor wordt op runtime opgehaald per Connection-fingerprint-call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | `config/hub-providers.php` aangepast om primary_field op niet-encrypted veld te wijzen → fingerprint zou een non-secret kunnen hashen, of `encryptedFields[0]` wijst per ongeluk op `subscription_id` (Snelstart's tenant-UUID, niet-encrypted per Phase-3 decision) | mitigate | Config-file is gitignored / committed via PR-review. ProviderDescriptorTest Test 2 asserteert dat `snelstart.encrypted_fields` exact `['client_key', 'subscription_key']` is — wijziging triggert test-failure in CI. Plan 09-06 ConnectionResource test (no-secret-leak) bewijst extra dat raw waarde niet in HTML komt. |
| T-09-04-02 | Information Disclosure | `Connection::fingerprint()` exposed via API/UI mocht een onbekende provider geen-null-maar-leaked-message retourneren | mitigate | Try-catch op `InvalidArgumentException` returnt `null` (gedragsbehoud). ConnectionEncryptionTest `test_fingerprint_returns_null_for_unknown_provider` blijft groen — bewijst geen exception bubblet richting Filament. |
| T-09-04-03 | Tampering | Descriptor-class is mutable | accept | `final class` + `readonly`-properties — geen reflection-bypass mogelijk via gewone codepaden. Tests bevestigen factory-output. |
| T-09-04-04 | Information Disclosure | `primaryFingerprintLabel` per ongeluk ingesteld op iets dat een raw secret bevat ("OAuth token: live_abc123") | accept | Conventie: labels zijn statische human-readable strings ("OAuth token", "Client key"). Code-review check; geen runtime-injection. |
| T-09-04-SC | Tampering | Geen package-install | accept | Plan 09-04 wijzigt alleen config + Support + Model + test. Geen `composer require`. Slopcheck N/A. |
</threat_model>

<verification>
1. `config/hub-providers.php` bestaat met 2 entries
2. `app/Support/ProviderCredentialDescriptor.php` bestaat als `final class` met `for()` + `all()`
3. `Connection::fingerprint()` gebruikt geen `match($this->provider)` meer
4. `php artisan test --compact --filter=ConnectionEncryptionTest` exit 0 (bestaande tests groen na refactor — gedragsbehoud!)
5. `php artisan test --compact --filter=ProviderDescriptorTest` exit 0 (4 nieuwe tests groen)
6. `php artisan test --compact` exit 0 (geen regressie)
7. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- D-04 ProviderCredentialDescriptor-laag staat
- Connection::fingerprint() is descriptor-aware zonder gedragsverandering
- D-04 success-criterium 10 (theoretische provider via config) bewezen
- Plan 09-06 (ConnectionResource) kan descriptor-aware form-sections + revoke-visibility bouwen op deze laag
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-04-SUMMARY.md` when done
</output>
