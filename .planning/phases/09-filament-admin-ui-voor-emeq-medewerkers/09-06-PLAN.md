---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 06
type: execute
wave: 3
depends_on: ['09-03', '09-04']
files_modified:
  - app/Filament/Resources/ConnectionResource.php
  - app/Filament/Resources/ConnectionResource/Pages/ListConnections.php
  - app/Filament/Resources/ConnectionResource/Pages/ViewConnection.php
  - tests/Feature/Admin/ConnectionFingerprintTest.php
  - tests/Feature/Admin/ConnectionRevokeActionTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "`/admin/connections` is bereikbaar voor een staff-User en toont een tabel met `provider`-badge, `account.external_id`, `fingerprint` (via Connection::fingerprint()), `revoked_at`"
    - "Tabel bevat filters voor `provider`, `consumer_id` (via Account-relation), `revoked` (bool)"
    - "ViewAction toont per-provider conditional form-sections via `ProviderCredentialDescriptor` (D-04): Mollie-sectie alleen zichtbaar voor mollie-Connections, Snelstart-sectie alleen voor snelstart-Connections"
    - "Revoke-action is alleen zichtbaar voor providers met `oauthFlowKey !== null` (Mollie ja, Snelstart nee per D-04)"
    - "Revoke-action roept `OAuthFlowRegistry::for($connection->provider)->revoke($connection)` aan (Phase 4-contract)"
    - "GEEN plain-text waarde van `access_token`/`refresh_token`/`client_key`/`subscription_key` verschijnt in de HTML-output van `/admin/connections` of `ListConnections`-Livewire-render"
  artifacts:
    - path: "app/Filament/Resources/ConnectionResource.php"
      provides: "Filament Resource voor Connection read + revoke (descriptor-aware)"
      contains: "class ConnectionResource extends Resource"
    - path: "tests/Feature/Admin/ConnectionFingerprintTest.php"
      provides: "No-secret-leak feature-test (HTML-scan voor raw credentials)"
      contains: "class ConnectionFingerprintTest"
    - path: "tests/Feature/Admin/ConnectionRevokeActionTest.php"
      provides: "Test die Revoke-action via OAuthFlowRegistry bewijst"
      contains: "class ConnectionRevokeActionTest"
  key_links:
    - from: "app/Filament/Resources/ConnectionResource.php"
      to: "app/Support/ProviderCredentialDescriptor.php"
      via: "Per-provider section-visibility via descriptor->oauthFlowKey"
      pattern: "ProviderCredentialDescriptor::for"
    - from: "app/Filament/Resources/ConnectionResource.php"
      to: "app/OAuth/OAuthFlowRegistry.php"
      via: "revoke-action delegates to OAuthFlow::revoke"
      pattern: "OAuthFlowRegistry"
---

<objective>
Bouw `ConnectionResource` als read-only Filament-Resource met provider-agnostische tabel (één `fingerprint`-kolom via `Connection::fingerprint()`), per-provider conditional view-form-sections (via `ProviderCredentialDescriptor`, D-04), en een Revoke-action die alleen zichtbaar is voor providers met `oauthFlowKey !== null` (delegates to `OAuthFlowRegistry::for($provider)->revoke($connection)`).

Geen Edit-page — Connections zijn read-only via admin-UI (create/update gebeurt via OAuth-flow in Phase 4 of via `hub:consumer:create`-CLI). Tabel toont filterbare lijst; ViewAction toont detail-sheet.

Critical invariant: GEEN plain-text waarde van `access_token`/`refresh_token`/`client_key`/`subscription_key` mag in de HTML-output verschijnen. Dit wordt expliciet getest in `ConnectionFingerprintTest`.

Purpose: HUB-04 success-criterium 5+6: "ConnectionResource toont alleen fingerprints + Revoke roept OAuthFlow::revoke aan". D-04 invariant: één Resource-class voor alle providers; nieuwe provider = nieuwe descriptor + nieuwe form-section.

Output:
- 1 Resource-class + 2 Page-classes (List + View — geen Edit, geen Create)
- 2 nieuwe feature-tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@app/Models/Connection.php
@app/OAuth/OAuthFlowRegistry.php
@app/OAuth/Contracts/OAuthFlow.php
@app/Support/ProviderCredentialDescriptor.php
@database/factories/ConnectionFactory.php
@tests/Feature/Api/V1/Snelstart/PassThroughAuditNoSecretsTest.php

<interfaces>
<!-- Connection-model + #[Hidden] redaction -->
From app/Models/Connection.php:
- `#[Hidden(['access_token', 'refresh_token', 'client_key', 'subscription_key'])]` — toArray() redacts deze velden
- Encrypted-at-rest casts blijven (Phase 3 invariant)
- `fingerprint()` is descriptor-aware na plan 09-04

<!-- OAuth-flow revoke -->
From app/OAuth/OAuthFlowRegistry.php regels 24-33:
```php
public function for(string $provider): OAuthFlow
{
    if (! isset($this->providers[$provider])) {
        throw new InvalidArgumentException(
            "Geen OAuthFlow geregistreerd voor provider '{$provider}'."
        );
    }
    return $this->container->make($this->providers[$provider]);
}
```
→ Voor Snelstart-Connections is `OAuthFlowRegistry::for('snelstart')` een InvalidArgumentException → Revoke-action MOET conditioneel `->visible()` zijn op `ProviderCredentialDescriptor::for($record->provider)->oauthFlowKey !== null`.

<!-- ProviderCredentialDescriptor van plan 09-04 -->
From app/Support/ProviderCredentialDescriptor.php:
- `for(string $provider): self`
- `->encryptedFields` (list<string>)
- `->primaryFingerprintLabel` (string)
- `->oauthFlowKey` (?string)
- `::all(): array`

<!-- No-secret-leak test analog -->
From tests/Feature/Api/V1/Snelstart/PassThroughAuditNoSecretsTest.php:
- `private const RAW_CLIENT_KEY = 'CK-test-rawkey-DO-NOT-LEAK';`
- Loop door alle output (rij-kolommen of HTML) + `assertStringNotContainsString(RAW_X, $val)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resource-skelet + table (provider-badge + fingerprint kolom)</name>
  <files>
    app/Filament/Resources/ConnectionResource.php,
    app/Filament/Resources/ConnectionResource/Pages/ListConnections.php,
    app/Filament/Resources/ConnectionResource/Pages/ViewConnection.php
  </files>
  <read_first>
    - 09-PATTERNS.md regels 88-141 — ConnectionResource (read + revoke) pattern + fingerprint-accessor analog
    - `app/Models/Connection.php` (volledig) — fields + casts + relaties + fingerprint-method (post 09-04)
    - 09-CONTEXT.md §"Plan 09-04 ConnectionResource" (regels 169-171) + §D-04 (regels 111-134)
    - Filament v4-docs via Context7: `filament v4 make filament-resource view-only`, `filament v4 table BadgeColumn`, `filament v4 table filters`
  </read_first>
  <acceptance_criteria>
    - `php artisan make:filament-resource Connection --view --no-interaction` genereert Resource + 2 Pages (List + View; geen Create/Edit/Delete)
    - `ConnectionResource::table()` heeft 5 kolommen: provider (BadgeColumn met kleuren-map per provider), `account.external_id` (TextColumn searchable), `fingerprint` (TextColumn met `state(fn (Connection $record) => $record->fingerprint())`), `revoked_at` (TextColumn datetime, sortable), `created_at` (TextColumn datetime, sortable)
    - `ConnectionResource::table()` heeft 3 filters: SelectFilter `provider` (mollie/snelstart), SelectFilter `consumer_id` via Account-relation (`SelectFilter::make('consumer')->relationship('account.consumer', 'slug')`), TernaryFilter `revoked` (op `revoked_at IS NOT NULL`)
    - `/admin/connections` is bereikbaar voor staff-User (HTTP 200)
    - Form is `->disabled()` op alle velden (Resource is read-only)
    - Pint clean
  </acceptance_criteria>
  <action>
    1. Run `php artisan make:filament-resource Connection --view --no-interaction`. Verifieer dat 3 files zijn aangemaakt (Resource + ListConnections + ViewConnection page).
    2. Open `app/Filament/Resources/ConnectionResource.php`. Verwijder de gegenereerde CRUD-bits voor Create + Edit (Filament v4 met `--view` flag genereert geen Create/Edit page-classes; verifieer via Context7-docs).
    3. Set `protected static ?string $model = Connection::class;` + `protected static ?string $navigationIcon = 'heroicon-o-link';`.
    4. In `table(Table $table): Table`:
       - 5 kolommen zoals beschreven in acceptance_criteria.
       - Filters: `Tables\Filters\SelectFilter::make('provider')->options(['mollie' => 'Mollie', 'snelstart' => 'Snelstart'])`, `Tables\Filters\SelectFilter::make('consumer')->relationship('account.consumer', 'slug')->label('Consumer')`, `Tables\Filters\TernaryFilter::make('revoked')->nullable()->trueLabel('Revoked')->falseLabel('Active')->queries(true: fn (Builder $query) => $query->whereNotNull('revoked_at'), false: fn (Builder $query) => $query->whereNull('revoked_at'), blank: fn (Builder $query) => $query)`
    5. In `getPages()`: alleen `index` (ListConnections) + `view` (ViewConnection). Geen `create`/`edit`-routes.

    6. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    test -f app/Filament/Resources/ConnectionResource.php &&
    test -f app/Filament/Resources/ConnectionResource/Pages/ListConnections.php &&
    test -f app/Filament/Resources/ConnectionResource/Pages/ViewConnection.php &&
    grep -q "Connection::class" app/Filament/Resources/ConnectionResource.php &&
    grep -q "fingerprint" app/Filament/Resources/ConnectionResource.php &&
    grep -q "make('provider')" app/Filament/Resources/ConnectionResource.php &&
    php artisan route:list --path=admin/connections 2>&1 | grep -q "admin/connections"
    </automated>
  </verify>
  <done>
    - 3 files aangemaakt
    - Tabel met provider-badge + fingerprint kolom + filters
    - View-page geregistreerd (geen Edit/Create)
    - Pint clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-provider form-sections (D-04 conditional visibility) + Revoke-action</name>
  <files>app/Filament/Resources/ConnectionResource.php</files>
  <read_first>
    - 09-CONTEXT.md §D-04 (regels 111-134) — descriptor-driven section-visibility + revoke-conditional
    - 09-PATTERNS.md regels 115-141 — Revoke-action target via OAuthFlowRegistry
    - `app/Support/ProviderCredentialDescriptor.php` (plan 09-04 output) — `->oauthFlowKey` field
    - `app/OAuth/OAuthFlowRegistry.php` regels 24-33 — `for()`-method shape
    - `app/OAuth/Contracts/OAuthFlow.php` regels 29-32 — `revoke()`-contract
  </read_first>
  <acceptance_criteria>
    - `ConnectionResource::infolist()` (Filament v4 read-only detail-view) of `form()->disabled()` toont 2 sections, één voor Mollie en één voor Snelstart, beide `->visible(fn (Connection $record) => $record->provider === 'mollie')` resp. `'snelstart'`
    - Mollie-section toont: `provider` (badge), `account.external_id`, `fingerprint` (via accessor), `expires_at` (datetime), `scopes` (json), `revoked_at`. Geen raw `access_token`/`refresh_token` velden.
    - Snelstart-section toont: `provider`, `account.external_id`, `fingerprint`, `subscription_id` (niet encrypted per Phase-3-decision — OK voor weergave), `revoked_at`. Geen raw `client_key`/`subscription_key` velden.
    - `ConnectionResource::table()` heeft een `Tables\Actions\Action::make('revoke')` met:
      - `->visible(fn (Connection $record) => ProviderCredentialDescriptor::for($record->provider)->oauthFlowKey !== null && $record->revoked_at === null)`
      - `->requiresConfirmation()` + `->modalHeading('Connection intrekken bij provider')`
      - `->action(fn (Connection $record) => app(OAuthFlowRegistry::class)->for($record->provider)->revoke($record))`
      - Bij success: Notification `'Connection ingetrokken'`
    - Pint clean
  </acceptance_criteria>
  <behavior>
    - Mollie-Connection in /admin/connections → ViewAction toont Mollie-section + Snelstart-section verborgen
    - Snelstart-Connection → Snelstart-section zichtbaar + Mollie-section verborgen
    - Revoke-action zichtbaar op Mollie-Connection, verborgen op Snelstart-Connection (per `oauthFlowKey === null`)
    - Revoke-action submit → `OAuthFlowRegistry::for('mollie')->revoke($connection)` aangeroepen → upstream revoke + lokale `revoked_at`-flip
  </behavior>
  <action>
    1. Open `app/Filament/Resources/ConnectionResource.php`. Voeg imports toe: `App\OAuth\OAuthFlowRegistry`, `App\Support\ProviderCredentialDescriptor`, `Filament\Forms\Components\Section`, `Filament\Notifications\Notification`, `Filament\Tables\Actions\Action`.

    2. In `form(Form $form): Form`-method (of `infolist()` als Filament v4 dit prefereert voor view-only — verifieer via Context7-docs): bouw 2 `Section::make()`-blocks. Mollie-section:
       ```
       Section::make('Mollie OAuth')
           ->visible(fn (?Connection $record) => $record?->provider === 'mollie')
           ->schema([
               TextInput::make('provider')->disabled(),
               TextInput::make('account.external_id')->disabled()->label('Account'),
               TextInput::make('fingerprint')->disabled()->formatStateUsing(fn (Connection $record) => $record->fingerprint()),
               TextInput::make('expires_at')->disabled(),
               TextInput::make('scopes')->disabled(),
               TextInput::make('revoked_at')->disabled(),
           ])
       ```
       Snelstart-section analoog met `subscription_id` ipv `expires_at`/`scopes`. Form is `->disabled()` op alle velden.

    3. In `table()->actions([...])`: voeg `Action::make('revoke')` toe zoals beschreven in acceptance_criteria. Imports en class-resolution via `app(OAuthFlowRegistry::class)->for(...)->revoke(...)`. Notification op success.

    4. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    grep -q "ProviderCredentialDescriptor::for" app/Filament/Resources/ConnectionResource.php &&
    grep -q "OAuthFlowRegistry" app/Filament/Resources/ConnectionResource.php &&
    grep -q "->revoke" app/Filament/Resources/ConnectionResource.php &&
    grep -q "->visible(fn" app/Filament/Resources/ConnectionResource.php
    </automated>
  </verify>
  <done>
    - Mollie + Snelstart sections leven met conditional visibility
    - Revoke-action wired naar OAuthFlowRegistry
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConnectionFingerprintTest (no-secret-leak)</name>
  <files>tests/Feature/Admin/ConnectionFingerprintTest.php</files>
  <read_first>
    - `tests/Feature/Api/V1/Snelstart/PassThroughAuditNoSecretsTest.php` (volledig, ~80 regels) — exact pattern voor raw-secret-scan
    - 09-PATTERNS.md regels 667-740 — Filament Livewire-HTML scan variant
    - `database/factories/ConnectionFactory.php` — `forMollie()` + `forSnelstart()` state-methods (Phase 3-01 decision: default = Snelstart-shape; states gebruiken)
  </read_first>
  <acceptance_criteria>
    - `tests/Feature/Admin/ConnectionFingerprintTest.php` bestaat met namespace `Tests\Feature\Admin`
    - Class-constants: `RAW_ACCESS_TOKEN = 'access_test-DO-NOT-LEAK-09-06'`, `RAW_REFRESH_TOKEN = 'refresh_test-DO-NOT-LEAK-09-06'`, `RAW_CLIENT_KEY = 'CK-test-DO-NOT-LEAK-09-06'`, `RAW_SUBSCRIPTION_KEY = 'SK-test-DO-NOT-LEAK-09-06'`
    - Test 1: `test_list_page_html_contains_no_raw_credentials` — maak 1 Mollie- en 1 Snelstart-Connection met de 4 RAW_* constants; GET `/admin/connections` met staff-User; `assertDontSee(RAW_ACCESS_TOKEN)` × 4 raw constants
    - Test 2: `test_livewire_list_render_contains_no_raw_credentials` — zelfde data, gebruik `Livewire::test(ListConnections::class)` + `assertDontSee(RAW_X)` × 4
    - Test 3: `test_view_page_html_contains_no_raw_credentials_mollie` — GET `/admin/connections/{id}` voor Mollie-Connection; `assertDontSee(RAW_ACCESS_TOKEN)`
    - Test 4: `test_view_page_html_contains_no_raw_credentials_snelstart` — idem voor Snelstart-Connection
    - Alle 4 tests groen
    - Full suite blijft groen
  </acceptance_criteria>
  <behavior>
    - Filament list-render: 0 raw waardes in HTML
    - Filament view-render: 0 raw waardes in HTML
    - Test dekt zowel HTTP-response als Livewire-render
  </behavior>
  <action>
    Maak `tests/Feature/Admin/ConnectionFingerprintTest.php` aan via `php artisan make:test --phpunit Admin/ConnectionFingerprintTest --no-interaction`. Use `RefreshDatabase`. Imports: `App\Filament\Resources\ConnectionResource\Pages\ListConnections`, `App\Models\Connection`, `App\Models\User`, `Database\Seeders\EmeqStaffSeeder`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Livewire\Livewire`, `Spatie\Permission\Models\Role`, `Tests\TestCase`.

    `setUp()` met `$this->seedRoles()` helper (zelfde shape als plan 09-03 task 3).

    Voor elke test:
    1. Setup: `$admin = User::factory()->create(); $admin->assignRole('staff');`
    2. Maak Connections: `Connection::factory()->forMollie()->create(['access_token' => self::RAW_ACCESS_TOKEN, 'refresh_token' => self::RAW_REFRESH_TOKEN]);` (Mollie) + `Connection::factory()->forSnelstart()->create(['client_key' => self::RAW_CLIENT_KEY, 'subscription_key' => self::RAW_SUBSCRIPTION_KEY]);` (Snelstart)
    3. `$this->actingAs($admin);`

    Test 1 body:
    ```
    $response = $this->get('/admin/connections');
    $response->assertOk();
    $response->assertDontSee(self::RAW_ACCESS_TOKEN);
    $response->assertDontSee(self::RAW_REFRESH_TOKEN);
    $response->assertDontSee(self::RAW_CLIENT_KEY);
    $response->assertDontSee(self::RAW_SUBSCRIPTION_KEY);
    ```

    Test 2 body:
    ```
    $component = Livewire::test(ListConnections::class);
    $html = $component->html();
    $this->assertStringNotContainsString(self::RAW_ACCESS_TOKEN, $html);
    // ... 3 andere
    ```

    Test 3+4: GET `/admin/connections/{id}` per provider en assertDontSee per relevante RAW_* constant.

    Run `php artisan test --compact --filter=ConnectionFingerprintTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ConnectionFingerprintTest && php artisan test --compact</automated>
  </verify>
  <done>
    - 4 no-secret-leak tests groen
    - Full suite groen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ConnectionRevokeActionTest — OAuthFlow::revoke delegation</name>
  <files>tests/Feature/Admin/ConnectionRevokeActionTest.php</files>
  <read_first>
    - `app/OAuth/Testing/FakeOAuthFlow.php` (Phase 4) — container-bindable test-fixture
    - 09-PATTERNS.md regels 115-141 — Revoke-action target via OAuthFlowRegistry
    - `tests/Feature/Api/V1/AccountSubscriptions/ListAccountSubscriptionsTest.php` — Livewire-action-test pattern
  </read_first>
  <acceptance_criteria>
    - `tests/Feature/Admin/ConnectionRevokeActionTest.php` bestaat met namespace `Tests\Feature\Admin`
    - Test 1: `test_revoke_action_visible_for_mollie_connection` — Livewire-test asserteert dat `revoke`-action zichtbaar is op een Mollie-Connection
    - Test 2: `test_revoke_action_hidden_for_snelstart_connection` — `revoke`-action verborgen op Snelstart-Connection
    - Test 3: `test_revoke_action_calls_oauth_flow_revoke` — bind een FakeOAuthFlow via `app()->bind(...)`, trigger de revoke-action via Livewire `callTableAction('revoke', $connection)`, assert dat FakeOAuthFlow's `revoke()` is aangeroepen met de juiste Connection
    - Test 4: `test_revoke_action_hidden_for_already_revoked_connection` — Mollie-Connection met `revoked_at = now()` → action verborgen
    - Alle 4 tests groen
  </acceptance_criteria>
  <behavior>
    - Revoke-action conditioneel zichtbaar op `oauthFlowKey !== null && revoked_at === null`
    - Action delegates to OAuthFlowRegistry → OAuthFlow::revoke (Phase 4-contract)
    - FakeOAuthFlow bewijst delegation zonder echte Mollie-API
  </behavior>
  <action>
    Maak `tests/Feature/Admin/ConnectionRevokeActionTest.php` aan. Use `RefreshDatabase`. Imports: `App\Filament\Resources\ConnectionResource\Pages\ListConnections`, `App\Models\Connection`, `App\Models\User`, `App\OAuth\Contracts\OAuthFlow`, `App\OAuth\Testing\FakeOAuthFlow`, `App\OAuth\OAuthFlowRegistry`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Livewire\Livewire`, `Spatie\Permission\Models\Role`, `Tests\TestCase`.

    `setUp()`: helper `seedRoles()` + `$admin = User::factory()->create(); $admin->assignRole('staff'); $this->actingAs($admin);`.

    Test 1: Mollie-Connection → `Livewire::test(ListConnections::class)->assertTableActionVisible('revoke', $mollie);`
    Test 2: Snelstart-Connection → `Livewire::test(ListConnections::class)->assertTableActionHidden('revoke', $snelstart);`
    Test 3: Bind FakeOAuthFlow. Verifieer via Phase 4-pattern (FakeOAuthFlow heeft een spy/recorder voor `revoke()`-calls). Roep Livewire-action aan: `Livewire::test(ListConnections::class)->callTableAction('revoke', $mollie)->assertHasNoTableActionErrors();`. Asserteer dat FakeOAuthFlow's recorder de revoke-call kreeg.
    Test 4: Maak Mollie-Connection met `revoked_at => now()`. `assertTableActionHidden('revoke', $mollie)`.

    Run `php artisan test --compact --filter=ConnectionRevokeActionTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ConnectionRevokeActionTest && php artisan test --compact</automated>
  </verify>
  <done>
    - 4 revoke-tests groen
    - Full suite groen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filament read-render → browser-HTML | Encrypted Connection-fields worden via `#[Hidden]`-attribuut redacted in `toArray()`. Filament gebruikt model-state voor rendering; geen direct raw-render mogelijk tenzij expliciet `->columns(['access_token'])` (we doen dit NIET). |
| Filament Revoke-action → OAuthFlow → partner-API | Outgoing call naar partner (Mollie) — Phase 4 OAuthFlowRegistry handelt dit af; ConnectionResource alleen wired. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Information Disclosure | Plain-text `access_token`/`refresh_token`/`client_key`/`subscription_key` lekt in Filament-HTML (lijst of view) | mitigate | Resource form bevat ALLEEN fingerprint + non-secret velden (`account.external_id`, `expires_at`, `scopes`, `subscription_id`, `revoked_at`). Geen `TextInput::make('access_token')` of dergelijke. ConnectionFingerprintTest Tasks 1+2+3+4 scant HTML-render + Livewire-render op alle 4 raw constants. Failing test bij regressie. |
| T-09-06-02 | Elevation of Privilege | Revoke-action triggered door staff zonder rationale → cross-Consumer abuse | accept | `staff`-rol kan revoke per ontwerp (CONTEXT.md). Audit-log via Spatie activitylog is backlog HUB-AUDIT — out of scope voor Phase 9. Documenteren in plan 09-11 ADR. |
| T-09-06-03 | Tampering | Revoke-action voor Snelstart-Connection gooit InvalidArgumentException uit OAuthFlowRegistry → 500-error in Filament | mitigate | Action `->visible()` filtert op `descriptor->oauthFlowKey !== null` voordat de action zichtbaar wordt. ConnectionRevokeActionTest Test 2 bewijst hidden-state. Server-side: ook al wordt de action geforceerd geüpload (Livewire bypass), `OAuthFlowRegistry::for('snelstart')` gooit early-fail met NL-message — Filament toont generic error notification. |
| T-09-06-04 | CSRF | Livewire revoke-POST | accept | Livewire forceert CSRF-token; standaard Filament v4-flow. |
| T-09-06-05 | Information Disclosure | Cross-Consumer-data: staff-A in Consumer-X kan Connections van Consumer-Y zien | accept | Per ontwerp: Emeq-staff ziet ALLE Consumers (Hub-level admin, niet tenant-scoped). Phase 9 staff-rol is multi-tenant-by-design. Documenteren in plan 09-11 ADR. Cross-Consumer-isolation geldt voor PAT-Bearer-routes (zie Phase 5a tests), niet voor admin-panel. |
| T-09-06-SC | Tampering | Geen package-install | accept | Plan 09-06 wijzigt alleen Resource-files + tests. |
</threat_model>

<verification>
1. `php artisan make:filament-resource Connection --view` heeft 3 files gegenereerd
2. `/admin/connections` returnt 200 voor staff-User
3. `php artisan test --compact --filter=ConnectionFingerprintTest` exit 0 (4 tests groen — no-secret-leak)
4. `php artisan test --compact --filter=ConnectionRevokeActionTest` exit 0 (4 tests groen — revoke-delegation)
5. `php artisan test --compact` exit 0 (geen regressie)
6. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- ConnectionResource is descriptor-aware (D-04 invariant gerespecteerd)
- Geen plain-text credentials in Filament-render (bewezen via 4 no-secret-leak-tests)
- Revoke-action delegates to OAuthFlowRegistry (Phase 4-contract)
- HUB-04 success-criterium 5+6 bewezen
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-06-SUMMARY.md` when done
</output>
