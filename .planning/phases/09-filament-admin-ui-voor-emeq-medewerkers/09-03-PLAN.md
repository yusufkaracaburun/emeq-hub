---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 03
type: execute
wave: 2
depends_on: ['09-02']
files_modified:
  - app/Models/User.php
  - database/seeders/EmeqStaffSeeder.php
  - database/factories/UserFactory.php
  - tests/Feature/Admin/PanelAccessTest.php
  - tests/Feature/Admin/EmeqStaffSeederTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "Een User zonder Spatie-rol kan NIET in `/admin` (401/302-redirect naar login bij unauth; 403 of redirect bij auth zonder rol — Filament v4 default)"
    - "Een User met rol `staff` of `super-admin` kan WEL in `/admin` (200)"
    - "`EmeqStaffSeeder` maakt 2 rollen (`super-admin` + `staff`) met 6 permissions (D-05)"
    - "`super-admin` heeft alle 6 permissions; `staff` heeft 5 (NIET `manage-staff`)"
    - "Met `EMEQ_STAFF_SEED_EMAIL` + `EMEQ_STAFF_SEED_PASSWORD` env-vars maakt seeder een bootstrap-User met `super-admin`-rol; zonder beide vars no-op (production-safe)"
    - "Seeder is idempotent: 2× draaien crasht niet, maakt geen duplicaten"
  artifacts:
    - path: "app/Models/User.php"
      provides: "FilamentUser-interface + HasRoles-trait + canAccessPanel-method"
      contains: "implements FilamentUser"
    - path: "database/seeders/EmeqStaffSeeder.php"
      provides: "Idempotente env-driven rol/permission-seeder + bootstrap super-admin User"
      contains: "class EmeqStaffSeeder extends Seeder"
    - path: "tests/Feature/Admin/PanelAccessTest.php"
      provides: "3-tier role-gating feature-tests"
      contains: "class PanelAccessTest"
  key_links:
    - from: "app/Models/User.php"
      to: "Spatie\\Permission\\Traits\\HasRoles"
      via: "use HasRoles trait"
      pattern: "use HasRoles"
    - from: "app/Models/User.php"
      to: "Filament\\Models\\Contracts\\FilamentUser"
      via: "implements FilamentUser + canAccessPanel"
      pattern: "canAccessPanel\\(Panel"
    - from: "database/seeders/EmeqStaffSeeder.php"
      to: "Spatie\\Permission\\Models\\Role / Permission"
      via: "Role::firstOrCreate + givePermissionTo"
      pattern: "Role::firstOrCreate"
---

<objective>
Wijzig `User`-model zodat het Spatie's `HasRoles`-trait gebruikt en Filament's `FilamentUser`-contract implementeert met een gate die alleen `super-admin` of `staff`-rollen toelaat op `/admin`. Schrijf `EmeqStaffSeeder` die idempotent 2 rollen + 6 permissions seedt en — als env-vars aanwezig zijn — een bootstrap super-admin-User aanmaakt. Schrijf 2 feature-tests (PanelAccessTest + EmeqStaffSeederTest) die de gate + de seeder bewijzen.

Geen Filament-Resource in dit plan. UserResource (de Filament-Resource-class voor User-management) is scope van plan 09-10.

Purpose: D-05 (RBAC via Spatie laravel-permission, drop `is_emeq_staff`-boolean). Zonder dit plan kan niemand op `/admin` inloggen — plans 09-05 t/m 09-10 (Resource-builds) hebben dit als hard-dependency.

Output:
- `User` krijgt `HasRoles` + `implements FilamentUser` + `canAccessPanel(Panel $panel): bool`
- `EmeqStaffSeeder` aangemaakt (env-driven, idempotent)
- `UserFactory` minimaal aangevuld zodat factory-create() werkt voor tests (al aanwezig sinds 03-05, verifieer en alleen aanpassen indien nodig)
- 2 nieuwe feature-tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@app/Models/User.php
@app/Models/Consumer.php
@database/seeders/DatabaseSeeder.php

<interfaces>
<!-- Bestaande User-model shape -->
From app/Models/User.php:
```php
#[Fillable(['name', 'email', 'password'])]
#[Hidden(['password', 'remember_token'])]
class User extends Authenticatable
{
    use HasFactory, Notifiable;
    // ...
}
```
→ Modificatie: trait-stack uitbreiden met `HasRoles`; interface `implements FilamentUser`; method `canAccessPanel(Panel $panel): bool` toevoegen. `#[Fillable]`/`#[Hidden]` blijven onveranderd (Spatie's HasRoles voegt geen fillable toe; rol-assignments gaan via `->assignRole()`).

<!-- Bestaande seeder-pattern -->
From database/seeders/DatabaseSeeder.php:
```php
if (! User::where('email', 'test@example.com')->exists()) {
    User::factory()->create([...]);
}

$consumer = Consumer::firstOrCreate(['slug' => 'naschool'], ['name' => 'Naschool']);
```
→ EmeqStaffSeeder volgt zelfde idempotent-pattern: `firstOrCreate` voor rollen + permissions + bootstrap User. **Wel** in productie draaibaar (via `db:seed --class=EmeqStaffSeeder`), DUS geen `if (app()->isProduction()) return;`-guard.

<!-- Spatie laravel-permission v6 imports -->
- `Spatie\Permission\Traits\HasRoles` (trait op User)
- `Spatie\Permission\Models\Role` + `Spatie\Permission\Models\Permission` (in seeder)
- `Filament\Models\Contracts\FilamentUser` (interface op User)
- `Filament\Panel` (param-type op canAccessPanel)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: User-model — HasRoles + FilamentUser + canAccessPanel</name>
  <files>app/Models/User.php</files>
  <read_first>
    - `app/Models/User.php` (volledig, 33 regels) — huidige shape
    - `app/Models/Consumer.php` regels 13-30 — multi-trait stacking pattern (`use Billable, HasApiTokens, HasFactory;`)
    - 09-CONTEXT.md §D-05 (regels 135-160) — RBAC-decisions
    - 09-PATTERNS.md regels 456-485 — VERWACHTE WIJZIGING-pattern voor User-model
  </read_first>
  <acceptance_criteria>
    - `App\Models\User` `implements Filament\Models\Contracts\FilamentUser`
    - Trait-stack bevat `HasFactory, HasRoles, Notifiable` (alfabetische volgorde aanhouden indien dit de bestaande conventie is; volg `Consumer::class` als referentie)
    - `public function canAccessPanel(Filament\Panel $panel): bool { return $panel->getId() === 'admin' && $this->hasAnyRole(['super-admin', 'staff']); }` aanwezig
    - `#[Fillable(['name', 'email', 'password'])]` blijft onveranderd
    - `#[Hidden(['password', 'remember_token'])]` blijft onveranderd
    - `casts()`-method blijft onveranderd
    - Pint clean
    - `php artisan tinker --execute 'echo (new App\Models\User()) instanceof Filament\Models\Contracts\FilamentUser ? "OK" : "FAIL";'` returnt `OK`
  </acceptance_criteria>
  <behavior>
    - User-model voldoet aan FilamentUser-contract
    - User-model heeft HasRoles-trait beschikbaar (`->assignRole()`, `->hasRole()` werken)
    - `canAccessPanel()` returnt false zonder rol, true met `staff` of `super-admin`
  </behavior>
  <action>
    Open `app/Models/User.php`. Voeg toe (chirurgisch — engineering.md):
    - Imports: `use Filament\Models\Contracts\FilamentUser;`, `use Filament\Panel;`, `use Spatie\Permission\Traits\HasRoles;`
    - Class-signature: `class User extends Authenticatable implements FilamentUser`
    - Trait-stack: `use HasFactory, HasRoles, Notifiable;` (HasRoles toevoegen in alfabetische volgorde; volg Consumer's stacking-stijl)
    - Method onder `casts()`:
      ```
      public function canAccessPanel(Panel $panel): bool
      {
          return $panel->getId() === 'admin'
              && $this->hasAnyRole(['super-admin', 'staff']);
      }
      ```
    Geen wijzigingen aan `#[Fillable]`/`#[Hidden]`/`casts()`. Run `./vendor/bin/pint --dirty --format agent`.

    **Niet** rollen seeden in dit task — dat is Task 2 (EmeqStaffSeeder).
  </action>
  <verify>
    <automated>
    grep -q "implements FilamentUser" app/Models/User.php &&
    grep -q "use HasRoles" app/Models/User.php &&
    grep -q "canAccessPanel(Panel \$panel): bool" app/Models/User.php &&
    php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$k = \$app->make(Illuminate\Contracts\Console\Kernel::class); \$k->bootstrap(); echo (new App\Models\User()) instanceof Filament\Models\Contracts\FilamentUser ? 'OK' : 'FAIL';" &&
    ./vendor/bin/pint --test --format agent app/Models/User.php
    </automated>
  </verify>
  <done>
    - User-model implementeert FilamentUser
    - HasRoles-trait actief
    - canAccessPanel-method aanwezig met juiste gate-logica
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmeqStaffSeeder — idempotent env-driven role + bootstrap user</name>
  <files>database/seeders/EmeqStaffSeeder.php</files>
  <read_first>
    - `database/seeders/DatabaseSeeder.php` regels 17-39 — `firstOrCreate`-idempotent pattern + production-guard semantiek (DatabaseSeeder zelf draait niet in productie; EmeqStaffSeeder is bedoeld om expliciet via `--class=` aangeroepen te worden)
    - 09-CONTEXT.md §D-05 (regels 135-160) — initial roles + permissions mapping; env-driven bootstrap-user pattern
    - 09-PATTERNS.md regels 489-554 — EmeqStaffSeeder pattern (incl. `Role::firstOrCreate`/`Permission::firstOrCreate`/`Hash::make`-imports)
  </read_first>
  <acceptance_criteria>
    - `database/seeders/EmeqStaffSeeder.php` bestaat met namespace `Database\Seeders`
    - `run()`-method begint met `if (! env('EMEQ_STAFF_SEED_EMAIL') || ! env('EMEQ_STAFF_SEED_PASSWORD')) { return; }` — production-safe no-op zonder beide vars
    - Roles aangemaakt via `Role::firstOrCreate(['name' => 'super-admin'])` + idem `'staff'`
    - 6 permissions aangemaakt via `Permission::firstOrCreate(['name' => $perm])` voor: `manage-staff`, `manage-consumers`, `manage-connections`, `view-webhooks`, `view-account-subscriptions`, `view-billing`
    - `super-admin` krijgt alle 6 permissions via `givePermissionTo(...)`
    - `staff` krijgt 5 permissions (zonder `manage-staff`)
    - Bootstrap User via `User::firstOrCreate(['email' => $email], ['name' => 'Emeq Super Admin', 'password' => Hash::make($password)])`
    - Bootstrap User krijgt `super-admin`-rol via `assignRole($superAdmin)` (idempotent: tweede call faalt niet)
    - 2× achter elkaar draaien (zelfde env-vars) maakt geen duplicaten en crasht niet
  </acceptance_criteria>
  <behavior>
    - Seeder idempotent (firstOrCreate-pattern)
    - Zonder env-vars: no-op
    - Met env-vars: roles + permissions + bootstrap-user aanwezig
    - super-admin krijgt manage-staff; staff niet
  </behavior>
  <action>
    Maak `database/seeders/EmeqStaffSeeder.php` aan via `php artisan make:seeder EmeqStaffSeeder --no-interaction`. Vervang body door:

    Imports nodig:
    - `App\Models\User`
    - `Illuminate\Database\Seeder`
    - `Illuminate\Support\Facades\Hash`
    - `Spatie\Permission\Models\Permission`
    - `Spatie\Permission\Models\Role`

    `run()`-method:
    1. Lees env-vars: `$email = env('EMEQ_STAFF_SEED_EMAIL');` + `$password = env('EMEQ_STAFF_SEED_PASSWORD');`. Return early indien één van beide leeg.
    2. Role-create: `$superAdmin = Role::firstOrCreate(['name' => 'super-admin']);` + `$staff = Role::firstOrCreate(['name' => 'staff']);`
    3. Shared 5 permissions (loop): voor elke naam in `['manage-consumers', 'manage-connections', 'view-webhooks', 'view-account-subscriptions', 'view-billing']` → `$p = Permission::firstOrCreate(['name' => $perm]); $superAdmin->givePermissionTo($p); $staff->givePermissionTo($p);`
    4. Super-admin-only permission: `$managePerm = Permission::firstOrCreate(['name' => 'manage-staff']); $superAdmin->givePermissionTo($managePerm);` (staff NIET).
    5. Bootstrap user: `$user = User::firstOrCreate(['email' => $email], ['name' => 'Emeq Super Admin', 'password' => Hash::make($password)]); $user->assignRole($superAdmin);`

    **Geen** `app()->isProduction()`-guard (deze seeder is expliciet bedoeld voor productie-bootstrap via `--class=`). Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    grep -q "EMEQ_STAFF_SEED_EMAIL" database/seeders/EmeqStaffSeeder.php &&
    grep -q "Role::firstOrCreate" database/seeders/EmeqStaffSeeder.php &&
    grep -q "manage-staff" database/seeders/EmeqStaffSeeder.php &&
    grep -q "view-billing" database/seeders/EmeqStaffSeeder.php &&
    grep -q "assignRole" database/seeders/EmeqStaffSeeder.php &&
    ./vendor/bin/pint --test --format agent database/seeders/EmeqStaffSeeder.php
    </automated>
  </verify>
  <done>
    - Seeder-file aangemaakt + Pint-clean
    - Bevat env-guard, 2 rollen, 6 permissions (5 shared + 1 super-admin-only)
    - Idempotent via firstOrCreate + assignRole
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PanelAccessTest + EmeqStaffSeederTest</name>
  <files>
    tests/Feature/Admin/PanelAccessTest.php,
    tests/Feature/Admin/EmeqStaffSeederTest.php
  </files>
  <read_first>
    - 09-PATTERNS.md regels 596-664 (PanelAccessTest analog + Livewire-test-optie)
    - `tests/Feature/Api/V1/AccountSubscriptions/ListAccountSubscriptionsTest.php` regels 1-30 — TestCase + RefreshDatabase pattern
    - `database/seeders/DatabaseSeeder.php` regels 17-25 — `User::factory()->create()`-pattern (factory bestaat reeds)
  </read_first>
  <acceptance_criteria>
    - `tests/Feature/Admin/PanelAccessTest.php` heeft 3 tests:
      1. `test_unauthenticated_user_is_redirected_to_admin_login` — `$this->get('/admin')` → redirect naar `/admin/login`
      2. `test_authenticated_user_without_role_cannot_access_admin_panel` — User zonder rol → 403 of redirect (Filament v4 default: redirect of forbidden; gebruik `assertStatus()` met `[403, 302]`-tolerantie of `assertForbidden()`/`assertRedirect()` na empirisch checken)
      3. `test_staff_user_can_access_admin_panel` — User met rol `staff` → 200
    - `tests/Feature/Admin/EmeqStaffSeederTest.php` heeft 3 tests:
      1. `test_seeder_is_noop_without_env_vars` — zonder env → 0 rollen + 0 users
      2. `test_seeder_creates_roles_permissions_and_bootstrap_user_with_env` — met env → 2 rollen + 6 permissions + 1 user met `super-admin`-rol
      3. `test_seeder_is_idempotent_when_run_twice` — 2× draaien met zelfde env → exact 2 rollen + 6 permissions + 1 user (geen duplicates)
    - Alle 6 tests groen
    - Full suite blijft groen
  </acceptance_criteria>
  <behavior>
    - 3-tier gate-test (anonymous / role-less / role)
    - Seeder no-op zonder env
    - Seeder maakt expected entities met env
    - Seeder idempotent
  </behavior>
  <action>
    **PanelAccessTest:** Maak file aan via `php artisan make:test --phpunit Admin/PanelAccessTest --no-interaction`. Use `RefreshDatabase`. Imports: `App\Models\User`, `Database\Seeders\EmeqStaffSeeder`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Tests\TestCase`.

    In `setUp()`-method (na `parent::setUp()`): seed rollen + permissions handmatig via `$this->seed(EmeqStaffSeeder::class)` ZONDER env (no-op), of seed rollen direct in test. Beter: maak een helper `private function seedRoles(): void { Role::firstOrCreate(['name' => 'super-admin']); Role::firstOrCreate(['name' => 'staff']); }` en roep deze aan in elke test die rollen nodig heeft. Reden: EmeqStaffSeeder is env-gated — directe role-create via Spatie's models is deterministischer in tests.

    Test 1: `$response = $this->get('/admin'); $response->assertRedirect('/admin/login');` (geen `$this->actingAs(...)`).

    Test 2: `$this->seedRoles(); $user = User::factory()->create();` (geen role-assign). `$this->actingAs($user); $response = $this->get('/admin'); $response->assertStatus(403);` — Filament v4 returnt 403 voor `FilamentUser::canAccessPanel = false`. Indien empirisch een 302-redirect → pas test aan naar `assertStatus(302)` of `assertForbidden() OR assertRedirect()` met expliciete check.

    Test 3: `$this->seedRoles(); $user = User::factory()->create(); $user->assignRole('staff'); $this->actingAs($user); $response = $this->get('/admin'); $response->assertOk();`.

    **EmeqStaffSeederTest:** Maak file aan via `php artisan make:test --phpunit Admin/EmeqStaffSeederTest --no-interaction`. Use `RefreshDatabase`. Imports: `App\Models\User`, `Database\Seeders\EmeqStaffSeeder`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Spatie\Permission\Models\Permission`, `Spatie\Permission\Models\Role`, `Tests\TestCase`.

    Test 1 (no env): Verzeker dat de env-vars niet gezet zijn met `putenv('EMEQ_STAFF_SEED_EMAIL'); putenv('EMEQ_STAFF_SEED_PASSWORD');` (unset). Run `$this->seed(EmeqStaffSeeder::class);`. Asserteer `Role::count() === 0` + `User::count() === 0`.

    Test 2 (with env): `putenv('EMEQ_STAFF_SEED_EMAIL=admin@emeq.test'); putenv('EMEQ_STAFF_SEED_PASSWORD=test-secret');`. Run `$this->seed(EmeqStaffSeeder::class);`. Asserteer `Role::count() === 2`, `Permission::count() === 6`, `User::where('email', 'admin@emeq.test')->first()->hasRole('super-admin') === true`. Cleanup met `putenv('EMEQ_STAFF_SEED_EMAIL'); putenv('EMEQ_STAFF_SEED_PASSWORD');` in `tearDown()`.

    Test 3 (idempotent): zelfde env-vars als Test 2, run `$this->seed(EmeqStaffSeeder::class)` 2×. Asserteer `Role::count() === 2`, `Permission::count() === 6`, `User::count() === 1`.

    Run via `php artisan test --compact --filter='PanelAccessTest|EmeqStaffSeederTest'`.
  </action>
  <verify>
    <automated>
    php artisan test --compact --filter=PanelAccessTest &&
    php artisan test --compact --filter=EmeqStaffSeederTest &&
    php artisan test --compact
    </automated>
  </verify>
  <done>
    - 6 tests groen
    - Full suite groen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/admin` | Authenticated request via `web`-guard; Filament's eigen middleware-stack handelt CSRF + auth af. Phase 9 voegt expliciete role-gate toe via `canAccessPanel`. |
| `EmeqStaffSeeder` env-vars → DB | Env-vars met plain-text password (eenmalige bootstrap). Niet gepersisteerd in DB; password wordt via `Hash::make()` opgeslagen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege | `User` zonder rol kan in `/admin` | mitigate | `canAccessPanel()` returnt expliciet `false` als `hasAnyRole(['super-admin', 'staff'])` false is. PanelAccessTest Test 2 bewijst dit gedrag. Geen anonieme of role-loze toegang mogelijk. |
| T-09-03-02 | Spoofing | Een staff-user faked super-admin-permission via session-injection | accept | Spatie's rol-check leest DB (`model_has_roles`-tabel), niet session. Geen client-side data wordt vertrouwd. `manage-staff`-gate (plan 09-10) check Spatie permission opnieuw bij elke request. |
| T-09-03-03 | Information Disclosure | `EMEQ_STAFF_SEED_PASSWORD` blijft in `.env` na bootstrap | accept | Operatie-procedure (te documenteren in plan 09-11 ADR): na 1× run + login moet env-var verwijderd worden uit productie-secrets. Hash van password staat in DB; raw env-var niet meer nodig. |
| T-09-03-04 | Tampering | Iemand muteert `users` rij om rol toe te voegen zonder admin-actie | accept | Phase 9 admin-paneel is geen public mutation-surface voor `users` direct (UserResource is super-admin-gated in plan 09-10). DB-toegang zelf is buiten Hub-scope (infra-policy). |
| T-09-03-05 | Repudiation | Wie creëerde welke staff-user — geen audit-trail | accept | Out of scope per CONTEXT.md ("Audit-log via spatie/laravel-activitylog — backlog HUB-AUDIT"). Documenteren in plan 09-11. |
| T-09-03-SC | Tampering | Geen package-install in dit plan | accept | Plan 09-03 wijzigt alleen User-model + seeder + tests. Geen `composer require`. Slopcheck N/A. |
</threat_model>

<verification>
1. `php artisan tinker --execute 'echo (new App\Models\User()) instanceof Filament\Models\Contracts\FilamentUser ? "OK" : "FAIL";'` returnt `OK`
2. `php artisan test --compact --filter=PanelAccessTest` exit 0 (3 tests groen)
3. `php artisan test --compact --filter=EmeqStaffSeederTest` exit 0 (3 tests groen)
4. `php artisan test --compact` exit 0 (geen regressie op Phase 3/5a/6/7-tests)
5. Met env-vars: `EMEQ_STAFF_SEED_EMAIL=… EMEQ_STAFF_SEED_PASSWORD=… php artisan db:seed --class=EmeqStaffSeeder` slaagt
6. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- User-model FilamentUser-compatibel + HasRoles + canAccessPanel
- EmeqStaffSeeder idempotent + env-driven + production-safe
- 3-tier gate-test groen
- Plan 09-04 + 09-05 t/m 09-10 kunnen op deze laag bouwen (rollen + permissions + canAccessPanel)
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-03-SUMMARY.md` when done
</output>
