---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 09
type: execute
wave: 3
depends_on: ['09-03']
files_modified:
  - app/Filament/Resources/CashierSubscriptionResource.php
  - app/Filament/Resources/CashierSubscriptionResource/Pages/ListCashierSubscriptions.php
  - app/Filament/Resources/CashierSubscriptionResource/Pages/ViewCashierSubscription.php
  - tests/Feature/Admin/CashierSubscriptionResourceTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "`/admin/cashier-subscriptions` is bereikbaar voor staff-User en toont een tabel met `owner.slug` (= Consumer), `name`, `plan`, `derived_status` (BadgeColumn), `ends_at`"
    - "Resource is volledig read-only (geen Create/Edit/Delete/mutate-actions)"
    - "`derived_status` wordt afgeleid via Cashier's `->active()`/`->cancelled()`/`->ended()`/`->onTrial()`/`->onGracePeriod()` methods (Cashier-Mollie heeft geen DB-status-kolom — Phase 6 D-02)"
    - "Tabel heeft filter op `derived_status` (computed via whereClause op `ends_at`/`trial_ends_at`)"
  artifacts:
    - path: "app/Filament/Resources/CashierSubscriptionResource.php"
      provides: "Read-only Filament Resource voor Cashier-Mollie subscriptions (Emeq's eigen Consumer-billing)"
      contains: "class CashierSubscriptionResource extends Resource"
    - path: "tests/Feature/Admin/CashierSubscriptionResourceTest.php"
      provides: "Test die derived-status + Consumer owner-relation bewijst"
      contains: "class CashierSubscriptionResourceTest"
  key_links:
    - from: "app/Filament/Resources/CashierSubscriptionResource.php"
      to: "Laravel\\Cashier\\Subscription (vendor model)"
      via: "Cashier's Subscription accessors voor derived-status"
      pattern: "Cashier.Subscription"
---

<objective>
Bouw `CashierSubscriptionResource` als read-only viewer voor Cashier-Mollie's `subscriptions`-tabel (Phase 6, use-case A: Emeq → Consumers billing). Cashier's `Subscription`-model heeft geen `status`-kolom (Phase 6 D-02) — derived status komt uit accessor-methods. Resource toont owner.slug (= Consumer via polymorphic morphTo), name, plan, ends_at en een derived-status BadgeColumn.

Geen mutate-actions (Cashier-billing wordt gemuteerd via `app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php` REST-API — Phase 6).

Purpose: HUB-04 deelcriterium: "Cashier-Subscriptions overzicht voor Emeq-eigen Consumer-facturatie." Maakt admin-debug mogelijk zonder tinker.

Output:
- 1 Resource-class + 2 Page-classes (List + View)
- 1 nieuwe feature-test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@app/Models/Consumer.php
@database/factories/ConsumerFactory.php

<interfaces>
<!-- Consumer (= Billable owner) -->
From app/Models/Consumer.php:
- `use Billable, HasApiTokens, HasFactory;`
- Cashier's Billable trait voegt `subscriptions()`-relation toe

<!-- Cashier Subscription model -->
Cashier-Mollie's `Laravel\Cashier\Subscription` heeft:
- `name` (string) — subscription-naam
- `plan` (string) — slug van plan-config
- `owner_id` + `owner_type` (morphTo) — points to Consumer
- `ends_at` (datetime nullable) — voor cancellation grace-period
- `trial_ends_at` (datetime nullable)
- GEEN `status`-kolom (Phase 6 D-02)

Accessors die derived-status berekenen:
- `->active(): bool` — currently active
- `->cancelled(): bool` — marked for cancellation
- `->ended(): bool` — past grace-period
- `->onTrial(): bool` — in trial
- `->onGracePeriod(): bool` — cancelled maar nog niet ended

<!-- Phase 6 ConsumerFactory state -->
From database/factories/ConsumerFactory.php regels 45-60:
`withActiveSubscription(string $planSlug = 'naschool-license', string $subscriptionName = 'main')` — maakt een Cashier-subscription-rij gekoppeld aan de Consumer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resource-skelet + derived-status BadgeColumn + filters</name>
  <files>
    app/Filament/Resources/CashierSubscriptionResource.php,
    app/Filament/Resources/CashierSubscriptionResource/Pages/ListCashierSubscriptions.php,
    app/Filament/Resources/CashierSubscriptionResource/Pages/ViewCashierSubscription.php
  </files>
  <read_first>
    - 09-PATTERNS.md regels 209-228 — CashierSubscriptionResource pattern + derived-status
    - `app/Models/Consumer.php` — Billable trait
    - `database/factories/ConsumerFactory.php` regels 45-60 — Cashier-subscription-insert-pattern
    - 09-CONTEXT.md §"Plan 09-04 Cashier\\SubscriptionResource" (regel 182)
    - Filament v4-docs: `filament v4 make filament-resource model namespace`, `filament v4 badge column state`, `filament v4 select filter where`
  </read_first>
  <acceptance_criteria>
    - `php artisan make:filament-resource CashierSubscription --view --no-interaction` genereert 3 files (handmatig `protected static ?string $model = \Laravel\Cashier\Subscription::class;` zetten — generator gebruikt App-namespace per default)
    - Tabel-kolommen: `owner.slug` (TextColumn polymorphic via owner-morphTo — `->state(fn (Subscription $record) => $record->owner?->slug)`), `name`, `plan`, `derived_status` (BadgeColumn via `->state(fn ($record) => $record->active() ? 'active' : ($record->onTrial() ? 'trialing' : ($record->onGracePeriod() ? 'grace' : ($record->ended() ? 'ended' : ($record->cancelled() ? 'cancelled' : 'unknown')))))`), `ends_at` (TextColumn datetime nullable)
    - Filter `derived_status`: `SelectFilter::make('derived_status')->options(['active', 'trialing', 'grace', 'cancelled', 'ended'])->query(...)`  — query gebruikt `whereNull/whereNotNull` op `ends_at` en `trial_ends_at` voor de derived-status-condition. Verifieer Cashier's accessor-implementation om query-equivalent te bouwen.
    - View-detail toont alle relevant velden (owner, name, plan, ends_at, trial_ends_at, created_at).
    - Geen Create/Edit
    - Pint clean
    - `/admin/cashier-subscriptions` returnt 200 voor staff-User
  </acceptance_criteria>
  <action>
    1. Run `php artisan make:filament-resource CashierSubscription --view --no-interaction`. Verifieer dat 3 files zijn aangemaakt. De gegenereerde Resource zal default naar `App\Models\CashierSubscription` wijzen — dat bestaat niet.
    2. Open `app/Filament/Resources/CashierSubscriptionResource.php` en wijzig:
       - `protected static ?string $model = \Laravel\Cashier\Subscription::class;`
       - `protected static ?string $navigationLabel = 'Cashier subscriptions';`
       - `protected static ?string $navigationIcon = 'heroicon-o-credit-card';`
       - `protected static ?string $slug = 'cashier-subscriptions';` (om URL `/admin/cashier-subscriptions` te forceren — verifieer Filament v4 slug-syntax via Context7)
    3. Imports: `Laravel\Cashier\Subscription`.
    4. `table()`: kolommen zoals beschreven in acceptance_criteria. Voor `derived_status` BadgeColumn-state-callback: chained ternary die de juiste case retourneert; gebruik `->color(fn (string $state) => match($state) { 'active' => 'success', 'trialing' => 'info', 'grace' => 'warning', 'cancelled' => 'danger', 'ended' => 'gray', default => 'gray' })`.
    5. Filter `derived_status`: gebruik een `SelectFilter::make('derived_status')` met `->options([...])` en `->query(function (Builder $query, array $data) { return match($data['value'] ?? null) { 'active' => $query->whereNull('ends_at')->where(function ($q) { $q->whereNull('trial_ends_at')->orWhere('trial_ends_at', '<=', now()); }), 'trialing' => $query->where('trial_ends_at', '>', now()), 'cancelled' => $query->whereNotNull('ends_at'), default => $query }; })`. (Cashier's `active()` checkt `ends_at IS NULL OR ends_at > now()`; verifieer exacte conditie via `Laravel\Cashier\Subscription`-source.)
    6. View-detail: gebruik Infolist met alle kolommen + owner-relation pretty-print.
    7. `getPages()`: `index` + `view`.
    8. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    test -f app/Filament/Resources/CashierSubscriptionResource.php &&
    grep -q "Laravel.Cashier.Subscription" app/Filament/Resources/CashierSubscriptionResource.php &&
    grep -q "derived_status" app/Filament/Resources/CashierSubscriptionResource.php &&
    grep -q "owner" app/Filament/Resources/CashierSubscriptionResource.php &&
    php artisan route:list --path=admin/cashier-subscriptions 2>&1 | grep -q "admin/cashier-subscriptions"
    </automated>
  </verify>
  <done>
    - 3 files aangemaakt
    - Tabel + filter wired
    - Derived-status logic correct (Cashier-accessors-equivalent)
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CashierSubscriptionResourceTest</name>
  <files>tests/Feature/Admin/CashierSubscriptionResourceTest.php</files>
  <read_first>
    - `database/factories/ConsumerFactory.php` regels 45-60 — `withActiveSubscription` state
    - Phase 6 tests in `tests/Feature/Api/V1/Billing/` — Cashier-subscription-creation pattern
    - 09-PATTERNS.md regels 209-228
  </read_first>
  <acceptance_criteria>
    - 3 tests:
      1. `test_list_shows_consumer_subscriptions` — maak Consumer met `withActiveSubscription`-state → 1 rij zichtbaar; `owner.slug` toont Consumer-slug
      2. `test_derived_status_is_active_for_subscription_without_ends_at` — sub zonder `ends_at` → derived_status='active' in HTML
      3. `test_derived_status_is_cancelled_for_subscription_with_ends_at_set` — sub met `ends_at = now()->addDay()` → derived_status='cancelled' of 'grace' in HTML
    - Alle 3 tests groen
  </acceptance_criteria>
  <behavior>
    - List werkt
    - Derived-status reflecteert Cashier-accessor-output
  </behavior>
  <action>
    Maak `tests/Feature/Admin/CashierSubscriptionResourceTest.php` aan. Use `RefreshDatabase`. Helper `seedRoles()` + acting staff. Imports: `App\Filament\Resources\CashierSubscriptionResource\Pages\ListCashierSubscriptions`, `App\Models\Consumer`, `App\Models\User`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Laravel\Cashier\Subscription`, `Livewire\Livewire`, `Spatie\Permission\Models\Role`, `Tests\TestCase`.

    Test 1: `$consumer = Consumer::factory()->withActiveSubscription()->create();`. Verifieer `Subscription::count() === 1`. Acting staff. `Livewire::test(ListCashierSubscriptions::class)->assertCanSeeTableRecords(Subscription::all());`. GET `/admin/cashier-subscriptions` → `assertSee($consumer->slug)`.

    Test 2: Setup zelfde. Update sub om zeker te zijn dat `ends_at = null` (default in factory-state). GET `/admin/cashier-subscriptions` → `assertSee('active')` (de derived-status badge tekst).

    Test 3: `$consumer = Consumer::factory()->withActiveSubscription()->create(); $sub = Subscription::first(); $sub->update(['ends_at' => now()->addDay()]);`. GET `/admin/cashier-subscriptions` → response bevat 'cancelled' of 'grace' (Cashier returnt `onGracePeriod() === true` voor sub met `ends_at > now()` na cancel). Asserteer beide opties via `assertSeeText` met fallback (één van twee tekst-states geaccepteerd).

    Run `php artisan test --compact --filter=CashierSubscriptionResourceTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=CashierSubscriptionResourceTest && php artisan test --compact</automated>
  </verify>
  <done>
    - 3 tests groen
    - Full suite groen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filament read-render → browser | Cashier's Subscription-model bevat geen credentials. Mandate-id staat in Cashier's `mandates`-tabel (separate); deze Resource toont alleen subscription-rij. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-01 | Information Disclosure | Cashier-subscription details (plan-slug, dates) per ongeluk PII-bevattend | accept | Cashier-data: plan-slug + dates + owner-id. Geen klant-PII. Audit-OK. |
| T-09-09-02 | Tampering | Resource per ongeluk Edit-page registreert → admin kan dates muteren zonder Mollie-sync | mitigate | `getPages()` lijst alleen `index` + `view`. Geen Edit-page-class. Cashier-mutations gaan via Phase 6's REST-API met Mollie-sync. |
| T-09-09-03 | Spoofing | Owner-relation morph-spoofing (sub gekoppeld aan onbestaande Consumer) | accept | Cashier's morphTo werkt met validate-eloquent — geen externe input bypasses dit. Geen risk in Resource-read-context. |
| T-09-09-SC | Tampering | Geen package-install | accept | Plan 09-09 wijzigt alleen Resource + test. |
</threat_model>

<verification>
1. `/admin/cashier-subscriptions` returnt 200 voor staff-User
2. `php artisan test --compact --filter=CashierSubscriptionResourceTest` exit 0 (3 tests groen)
3. `php artisan test --compact` exit 0 (geen regressie)
4. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- CashierSubscriptionResource read-only werkt
- Derived-status correct gemapt naar Cashier-accessor-output
- HUB-04 Cashier-overzicht-deelcriterium geleverd
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-09-SUMMARY.md` when done
</output>
