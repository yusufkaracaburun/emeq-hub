---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php
  - tests/Feature/Models/WebhookCallAuditColumnsTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "De tabel `webhook_calls` heeft 4 nieuwe kolommen: `direction` (enum incoming/outgoing), `provider` (nullable string), `consumer_id` (nullable FK → consumers), `status` (enum pending/processed/failed default processed)"
    - "Een bestaande `webhook_calls`-rij behoudt geldigheid na `migrate` (NULL toegestaan op `provider`/`consumer_id`; `direction` default-aanwezig zodat NOT NULL niet faalt op bestaande rijen)"
    - "Een nieuwe `WebhookCall::create([...])` met `direction='incoming'`/`provider='mollie'`/`consumer_id=<id>`/`status='processed'` persist en is leesbaar terug"
    - "`Schema::hasColumn('webhook_calls', 'direction')` returnt `true` na migratie"
  artifacts:
    - path: "database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php"
      provides: "DDL voor 4 audit-kolommen op webhook_calls (D-02)"
      contains: "Schema::table('webhook_calls'"
    - path: "tests/Feature/Models/WebhookCallAuditColumnsTest.php"
      provides: "Feature-test die migratie + persistence dekt"
      contains: "class WebhookCallAuditColumnsTest"
  key_links:
    - from: "database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php"
      to: "webhook_calls table (Spatie default-shape)"
      via: "Schema::table additive DDL"
      pattern: "\\$table->enum\\('direction'"
    - from: "tests/Feature/Models/WebhookCallAuditColumnsTest.php"
      to: "Spatie\\WebhookClient\\Models\\WebhookCall"
      via: "WebhookCall::create([...])"
      pattern: "WebhookCall::create"
---

<objective>
Schrijf één forward-only additive migratie die 4 nieuwe kolommen toevoegt aan de Spatie `webhook_calls`-tabel zodat het Filament-paneel (plan 09-08) op die kolommen kan filteren. Geen backfill van bestaande rijen — historische audit-data blijft NULL (D-02). Geen aanpassing aan Spatie's eigen migraties of model. Plan 09-08 (WebhookCallResource) leunt expliciet op deze migratie; zonder 09-01 kan 09-08 niet bouwen.

Purpose: D-02 uit `09-CONTEXT.md` (revisie 2026-05-15) eist dat `webhook_calls` `direction`/`provider`/`consumer_id`/`status` heeft vóór Filament-Resource erop bouwt. Plan 5c (Snelstart webhook-handler, in progress) zal deze kolommen natuurlijk gaan vullen — Phase 9 maakt de kolommen alvast beschikbaar.

Output:
- 1 nieuwe migratie (additive, forward-only-tagged in `down()`-comment)
- 1 feature-test die schema + persistence + nullable-toleratie bewijst
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@database/migrations/2026_05_13_223628_create_webhook_calls_table.php
@database/migrations/2026_05_17_000002_add_inbound_columns_to_pass_through_calls_table.php
@app/Models/Consumer.php

<interfaces>
<!-- Spatie's default webhook-calls schema heeft alleen: id / name / url / headers / payload / attachments / exception / timestamps. -->
<!-- Phase 9 voegt: direction (enum), provider (nullable string), consumer_id (nullable FK), status (enum) toe. -->
<!-- Spatie\WebhookClient\Models\WebhookCall blijft het canonical model — geen extension nodig in Phase 9. -->

From app/Models/Consumer.php:
- `Consumer` is een Authenticatable + Billable + HasApiTokens model. `consumers.id` is `bigIncrements` (zelfde FK-type als overal in deze repo).

From database/migrations/2026_05_17_000002_add_inbound_columns_to_pass_through_calls_table.php:
- Pattern: meerdere `Schema::table()` blocks per soort DDL (cast → add → index → constraint)
- Pattern: `down()` met inline-comment "forward-only-prod-policy"
- Pattern: ->after('<column>') voor kolom-volgorde
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive migratie + Postgres-compatibele indexen</name>
  <files>database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php</files>
  <read_first>
    - `database/migrations/2026_05_13_223628_create_webhook_calls_table.php` — Spatie default-shape die we uitbreiden (kolomvolgorde + indexen)
    - `database/migrations/2026_05_17_000002_add_inbound_columns_to_pass_through_calls_table.php` regels 1-50 — exacte template (`Schema::table` × meerdere blocks + `->after()` + forward-only-comment in `down()`)
    - 09-CONTEXT.md §D-02 (kolom-definities) + §"Migration forward-only down() comment" in 09-PATTERNS.md regels 938-953
    - `app/Models/Consumer.php` regels 13-17 — Consumer-id is bigIncrements
  </read_first>
  <acceptance_criteria>
    - Bestand `database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php` bestaat
    - `up()` voegt 4 kolommen toe in volgorde: `direction` (enum incoming/outgoing, default `incoming`, indexed) → `provider` (string(32) nullable, indexed) → `consumer_id` (foreignId nullable, `constrained('consumers')->nullOnDelete()`) → `status` (enum pending/processed/failed default `processed`, indexed)
    - Alle 4 kolommen `->after()` correct gepositioneerd (volgorde na bestaande Spatie-kolommen: `direction` after `id`; `provider` after `direction`; `consumer_id` after `provider`; `status` after `consumer_id`)
    - `down()` aanwezig met `dropForeign + dropColumn` voor `consumer_id`/`status`/`provider`/`direction` én een NL-comment-block over forward-only-prod-policy
    - `direction` heeft een sane default (`'incoming'`) zodat bestaande rijen niet falen op NOT NULL
    - `php artisan migrate` slaagt zonder errors op een database met of zonder bestaande `webhook_calls`-rijen
    - `Schema::hasColumn('webhook_calls', 'direction')` returnt `true` na migratie
    - Pint clean
  </acceptance_criteria>
  <behavior>
    - Test (in plan-09-01-Task-2): `php artisan migrate:fresh` slaagt en alle 4 nieuwe kolommen bestaan
    - Test: Een nieuwe `WebhookCall::create([...])` met de 4 audit-kolommen gevuld persist
    - Test: Bestaande Spatie-shape (`WebhookCall::create(['name' => ..., 'url' => ..., 'headers' => ..., 'payload' => ...])` zonder de nieuwe velden) blijft werken (defaults dekken NOT NULL-rules)
  </behavior>
  <action>
    Maak de migratie aan via `php artisan make:migration --no-interaction add_audit_columns_to_webhook_calls_table` en hernoem indien nodig naar `database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php` (volgnummer past op de bestaande migratie-volgorde: laatste Phase-7-migratie is 2026_05_18_*).

    Body volgt het pattern uit `2026_05_17_000002_add_inbound_columns_to_pass_through_calls_table.php`:
    1. Eerste `Schema::table('webhook_calls', function (Blueprint $table): void { ... })` block voegt `direction` toe als enum met `->default('incoming')->after('id')->index()`. Default = `'incoming'` zodat bestaande rijen valide blijven (Spatie's webhook-server schrijft typisch incoming-payloads).
    2. Tweede `Schema::table` block voegt `provider` toe (`->string('provider', 32)->nullable()->after('direction')->index()`).
    3. Derde `Schema::table` block voegt `consumer_id` toe (`->foreignId('consumer_id')->nullable()->after('provider')->constrained('consumers')->nullOnDelete()`).
    4. Vierde `Schema::table` block voegt `status` toe (`->enum('status', ['pending', 'processed', 'failed'])->default('processed')->after('consumer_id')->index()`).
    5. `down()`-methode dropt in omgekeerde volgorde + bevat NL-comment `// Forward-only-policy in productie (CLAUDE.md). down() bestaat voor lokale migrate:fresh.`. Roep `$table->dropForeign(['consumer_id'])` aan vóór `$table->dropColumn(['status', 'consumer_id', 'provider', 'direction'])` om FK-constraint correct te verwijderen.

    Geen wijziging aan Spatie's model of config. Geen backfill van bestaande rijen. Run `php artisan migrate` + `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    php artisan migrate:fresh --no-interaction &&
    php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$k = \$app->make(Illuminate\Contracts\Console\Kernel::class); \$k->bootstrap(); echo (Illuminate\Support\Facades\Schema::hasColumn('webhook_calls','direction') && Illuminate\Support\Facades\Schema::hasColumn('webhook_calls','provider') && Illuminate\Support\Facades\Schema::hasColumn('webhook_calls','consumer_id') && Illuminate\Support\Facades\Schema::hasColumn('webhook_calls','status')) ? 'OK' : 'FAIL';" &&
    grep -q "Schema::table('webhook_calls'" database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php &&
    grep -q "enum('direction'" database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php
    </automated>
  </verify>
  <done>
    - Migratie-bestand bestaat met 4 nieuwe kolommen
    - `php artisan migrate:fresh` exit 0
    - `Schema::hasColumn('webhook_calls', 'direction')` returnt true (idem voor `provider`/`consumer_id`/`status`)
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feature-test voor schema + persistence + nullable-toleratie</name>
  <files>tests/Feature/Models/WebhookCallAuditColumnsTest.php</files>
  <read_first>
    - `tests/Feature/Models/AccountSubscriptionTest.php` (Phase 7-01-Task-2) — RefreshDatabase + model-laag test-pattern (geen voorbeeld in deze prompt; gebruik file in repo)
    - `tests/Feature/Models/ConnectionEncryptionTest.php` (Phase 3-04) — Schema-assert + persist-pattern
    - `app/Models/Consumer.php` regels 13-17 — Consumer-factory aanwezig
  </read_first>
  <acceptance_criteria>
    - `tests/Feature/Models/WebhookCallAuditColumnsTest.php` bestaat met PHPUnit-namespace `Tests\Feature\Models`
    - Test 1: `test_webhook_calls_table_has_audit_columns_after_migration` — gebruikt `Schema::hasColumn(...)` voor alle 4 kolommen
    - Test 2: `test_full_audit_row_persists_with_all_new_columns` — `DB::table('webhook_calls')->insert([...])` met alle 4 kolommen + `Spatie\WebhookClient\Models\WebhookCall::first()` retrieves back. Gebruik `DB::table` (geen Eloquent-extension nodig — Spatie's model accepteert geen onbekende fillable; we testen schema, niet model)
    - Test 3: `test_legacy_spatie_shape_still_persists_without_new_columns` — `WebhookCall::create(['name' => 'legacy', 'url' => 'https://example.test', 'headers' => [], 'payload' => []])` slaagt; resulting row heeft `direction='incoming'` (default), `provider=null`, `consumer_id=null`, `status='processed'` (default)
    - Alle 3 tests groen
    - Full suite blijft groen (`php artisan test --compact` exit 0)
  </acceptance_criteria>
  <behavior>
    - schema_check test
    - persist met alle nieuwe velden test
    - legacy-shape blijft werken (defaults dekken NOT NULL)
  </behavior>
  <action>
    Maak `tests/Feature/Models/WebhookCallAuditColumnsTest.php` aan via `php artisan make:test --phpunit Models/WebhookCallAuditColumnsTest --no-interaction`. Use `RefreshDatabase`-trait.

    Imports nodig: `App\Models\Consumer`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\DB`, `Illuminate\Support\Facades\Schema`, `Spatie\WebhookClient\Models\WebhookCall`, `Tests\TestCase`.

    Schrijf 3 tests:
    1. `test_webhook_calls_table_has_audit_columns_after_migration`: asserteer `Schema::hasColumn('webhook_calls', 'direction')` t/m `'status'`. Geen DB-state nodig.
    2. `test_full_audit_row_persists_with_all_new_columns`: maak `$consumer = Consumer::factory()->create();`, gebruik `DB::table('webhook_calls')->insert(['name' => 'test', 'url' => 'https://example.test/hook', 'headers' => json_encode([]), 'payload' => json_encode(['k' => 'v']), 'direction' => 'incoming', 'provider' => 'mollie', 'consumer_id' => $consumer->id, 'status' => 'processed', 'created_at' => now(), 'updated_at' => now()]);`. Asserteer met `DB::table('webhook_calls')->where('provider', 'mollie')->first()` dat `consumer_id` en `status` matchen.
    3. `test_legacy_spatie_shape_still_persists_without_new_columns`: `WebhookCall::create(['name' => 'legacy', 'url' => 'https://example.test', 'headers' => [], 'payload' => []])` (Spatie's model accepteert deze 4 als fillable). Asserteer dat de resulterende rij `direction='incoming'`, `provider=null`, `consumer_id=null`, `status='processed'` heeft (via `DB::table('webhook_calls')->where('name', 'legacy')->first()`).

    Run via `php artisan test --compact --filter=WebhookCallAuditColumnsTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallAuditColumnsTest && php artisan test --compact</automated>
  </verify>
  <done>
    - 3 tests groen in WebhookCallAuditColumnsTest
    - Volledige suite blijft groen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-schema → app | Phase 9-08 (WebhookCallResource) leest hierop; Phase 5c (Snelstart webhook-handler in progress) schrijft hierop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Migration corrupteert bestaande Spatie-rows tijdens upgrade | mitigate | Additive-only migratie (geen `->change()` op bestaande kolommen); `direction` krijgt sane default `'incoming'` zodat NOT NULL niet faalt op bestaande rijen. Test 3 bewijst legacy-shape blijft werken. |
| T-09-01-02 | Tampering | `consumer_id` FK constrained zonder cascade — `Consumer::delete()` zou kunnen falen door pending audit-rijen | accept | `nullOnDelete()` (D-02 expliciet): audit-rijen worden bewaard maar `consumer_id` wordt NULL. Hub-invariant: Consumer-delete is admin-only via Phase 9 — geen public route. |
| T-09-01-03 | Information Disclosure | Audit-kolommen geen secrets | accept | Direction/provider/status/consumer_id zijn alle non-secret metadata. Geen `encrypted` cast nodig. Payload-kolom (Spatie's eigen kolom) blijft ongewijzigd in deze migratie. |
| T-09-01-04 | Denial of Service | Toevoegen 3 indexen op grote tabel zou kunnen lock-en | accept | Tabel is in lokale dev + UAT klein (<10k rijen); productie nog geen verkeer. Toekomstige scale: Postgres-`CREATE INDEX CONCURRENTLY` als pattern als nodig (niet voor dit volume). |
| T-09-01-SC | Tampering | Geen package-installs in dit plan | accept | Plan 09-01 wijzigt alleen DDL + test; geen `composer require`. Slopcheck N/A. |
</threat_model>

<verification>
1. `php artisan migrate:fresh` exit 0
2. `Schema::hasColumn('webhook_calls', 'direction')` t/m `'status'` returnt `true`
3. `php artisan test --compact --filter=WebhookCallAuditColumnsTest` exit 0 (3 tests groen)
4. `php artisan test --compact` exit 0 (geen regressie)
5. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- Migratie bestaat en is additive (geen `->change()`)
- 4 nieuwe kolommen aanwezig met juiste defaults
- Spatie's bestaande webhook-flow (legacy-shape) blijft werken
- Plan 09-08 (WebhookCallResource) heeft nu een stabiele tabel om Resource op te bouwen
- Phase 5c (Snelstart webhook-handler in progress) kan de nieuwe kolommen vullen zonder additional migration
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-01-SUMMARY.md` when done
</output>
