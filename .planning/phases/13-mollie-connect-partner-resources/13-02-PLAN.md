---
phase: 13-mollie-connect-partner-resources
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php
  - app/Http/Controllers/Api/V1/Mollie/Connect/ClientLinksController.php
  - app/Http/Controllers/Api/V1/Mollie/Connect/OnboardingController.php
  - app/Http/Controllers/Api/V1/Mollie/Connect/OrganizationsController.php
  - app/Http/Controllers/Api/V1/Mollie/Connect/PermissionsController.php
  - app/Http/Controllers/Api/V1/Mollie/Connect/ProfilesController.php
  - app/Http/Requests/Api/V1/Mollie/Connect/CreateClientLinkRequest.php
  - app/Http/Requests/Api/V1/Mollie/Connect/CreateProfileRequest.php
  - routes/api.php
  - tests/Feature/Api/V1/Mollie/Connect/ConnectRouteRegistrationTest.php
autonomous: true
requirements:
  - MOLL-05
must_haves:
  truths:
    - "9 routes geregistreerd onder `/v1/mollie/connect/*`: GET onboarding/me, GET organizations/me, GET organizations/{id}, GET profiles, POST profiles, GET profiles/{id}, GET permissions, GET permissions/{id}, POST client-links."
    - "Alle Connect-routes zitten achter `auth:sanctum` + `feature.provider:mollie` middleware-keten; géén `resolve.mollie.account`-middleware (D-07)."
    - "`AbstractMollieConnectPassThroughController::handle()` bouwt elke call een vers `MollieApiClient`-instance, zet `setAccessToken($resolver->resolveFor('partner'))`, forward't `Idempotency-Key`-header naar de client, ving Mollie-exceptions via `MollieUpstreamErrorMapper`, schrijft een audit-rij naar `pass_through_calls` met `token_type='partner'` en `partner_token_fingerprint = substr(hash('sha256', $partnerToken), 0, 12)`, `connection_id = NULL`, `account_id = NULL`."
    - "GET-routes vereisen ability `mollie:read|mollie:write|*`; POST-routes vereisen `mollie:write|*` (D-14 — hergebruikt bestaande abilities)."
    - "Form Requests `CreateClientLinkRequest` + `CreateProfileRequest` valideren minimaal Mollie's verplichte velden per partner-docs (`owner.email`, `owner.givenName`, `owner.familyName`, `name`, `address.*` voor ClientLinks; `name`, `website`, `email` voor Profiles)."
    - "Elke Connect-controller heeft `#[Group(name: 'Mollie · Connect', ...)]`-attribute zodat Scramble alle 5 resources onder één groep rendert (D-12)."
    - "`ConnectRouteRegistrationTest` bewijst dat alle 9 routes via `php artisan route:list` worden gevonden onder de juiste prefix met de juiste middleware-set."
  artifacts:
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php"
      provides: "Connect-pass-through-base — partner-token-binding + ability-guard + 415-guard + audit + error-mapping (D-03)"
      exports: ["AbstractMollieConnectPassThroughController"]
      min_lines: 100
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/ClientLinksController.php"
      provides: "POST /v2/client-links pass-through (D-15)"
      exports: ["ClientLinksController"]
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/OnboardingController.php"
      provides: "GET /v2/onboarding/me pass-through (D-15)"
      exports: ["OnboardingController"]
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/OrganizationsController.php"
      provides: "GET /v2/organizations/me + /{id} (D-15)"
      exports: ["OrganizationsController"]
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/PermissionsController.php"
      provides: "GET /v2/permissions + /{id} (D-15)"
      exports: ["PermissionsController"]
    - path: "app/Http/Controllers/Api/V1/Mollie/Connect/ProfilesController.php"
      provides: "GET /v2/profiles + POST /v2/profiles + GET /v2/profiles/{id} (D-15)"
      exports: ["ProfilesController"]
    - path: "app/Http/Requests/Api/V1/Mollie/Connect/CreateClientLinkRequest.php"
      provides: "Form-Request voor ClientLinks-create"
    - path: "app/Http/Requests/Api/V1/Mollie/Connect/CreateProfileRequest.php"
      provides: "Form-Request voor Profiles-create"
    - path: "routes/api.php"
      provides: "Nieuwe route-group `mollie/connect/*` (D-07)"
      contains: "mollie/connect"
  key_links:
    - from: "app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php"
      to: "App\\Mollie\\MollieAccessTokenResolver"
      via: "constructor-injection"
      pattern: "MollieAccessTokenResolver"
    - from: "routes/api.php"
      to: "App\\Http\\Controllers\\Api\\V1\\Mollie\\Connect\\*"
      via: "Route::middleware('auth:sanctum')->prefix('mollie/connect')->group(...)"
      pattern: "prefix\\('mollie/connect'\\)"
    - from: "app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php"
      to: "PassThroughCall::create"
      via: "audit-write met token_type='partner'"
      pattern: "'token_type' => 'partner'"
---

<objective>
Bouw de Connect-pass-through-laag: één `AbstractMollieConnectPassThroughController` die het partner-token-pad encapsuleert (geen `MollieConnectionContext`, geen `Account`-resolution, géén facade — eigen `MollieApiClient`-instance per call), 5 concrete resource-controllers (ClientLinks, Onboarding, Organizations, Permissions, Profiles), 2 Form Requests voor de write-endpoints, en de nieuwe route-group onder `/v1/mollie/connect/*` ná de bestaande Phase-5a-Mollie-block.

Purpose: Plan 13-03 schrijft de feature-/integration-tests die deze routes via `MollieApiClient::fake()` / een token-capturing spy aanroepen. Plan 13-04 schrijft de ADR. Zonder dit plan zijn er geen Connect-routes en geen controller-substrate om tegen te testen.

Output:
- 1 abstract base controller (Connect-equivalent van Phase-5a's `AbstractMolliePassThroughController`, GEEN gedeelde super-class — D-03 verbiedt premature abstractie).
- 5 concrete controllers, allen `#[Group(name: 'Mollie · Connect', ...)]` getagd.
- 2 Form Requests met minimale required-field-validatie per Mollie-docs.
- Route-group in `routes/api.php` met 9 routes en de juiste middleware-keten.
- 1 smoke-test die alle routes via `php artisan route:list` valideert (echte happy-path-tests komen in 13-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
@.planning/phases/13-mollie-connect-partner-resources/13-01-SUMMARY.md
@.docs/decisions/mollie-passthrough-api.md
@app/Http/Controllers/Api/V1/Mollie/AbstractMolliePassThroughController.php
@app/Http/Controllers/Api/V1/Mollie/CustomersController.php
@app/Http/Controllers/Api/V1/Mollie/PaymentsController.php
@app/Http/Requests/Api/V1/Mollie/CreateCustomerRequest.php
@app/Sanctum/TokenAbilities.php
@app/Support/Mollie/MollieUpstreamErrorMapper.php
@app/Mollie/MollieAccessTokenResolver.php
@routes/api.php
@packages/mollie-api/docs/partners/oauth-overview.md
@packages/mollie-api/docs/partners/api-idempotency.md
@packages/mollie-api/docs/partners/errors.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md

<interfaces>
<!-- Phase 5a-template — de Connect-base modelleert hier op (D-03: gescheiden hiërarchie, geen super-class) -->

From app/Http/Controllers/Api/V1/Mollie/AbstractMolliePassThroughController.php (Phase 5a):
- Methode `handle(Request $request, string $endpoint, callable $sdkCall): Response` — Connect-base implementeert dezelfde signature, anders body.
- Methode `resourceToArray(BaseResource $resource): array` — copy/adapt naar Connect-base (D-03: chirurgische duplicatie OK, geen shared trait verplicht).
- Methode `collectionToArray(BaseCollection $collection): array` — idem.

From app/Mollie/MollieAccessTokenResolver.php (Plan 13-01):
```php
public function resolveFor(string $tokenType): string;
// 'partner' | 'connection'; throws MissingPartnerTokenException of MissingConnectionContextException.
```

From app/Sanctum/TokenAbilities.php:
- MOLLIE_READ = 'mollie:read'
- MOLLIE_WRITE = 'mollie:write'
- ADMIN = '*'

From Mollie SDK (vendor/mollie/mollie-api-php):
- `Mollie\Api\MollieApiClient` — `new MollieApiClient()` + `->setAccessToken(string)` + `->setIdempotencyKey(string)`
- Endpoint-properties die we gebruiken: `$client->clientLinks`, `$client->onboarding`, `$client->organizations`, `$client->profiles`, `$client->permissions`
- Methodes per resource (vendor/mollie/mollie-api-php/src/Endpoints/*):
  - `clientLinks->create(array $payload): ClientLink`
  - `onboarding->get(): Onboarding` (geen ID — altijd "me")
  - `organizations->get(string $id): Organization` (`$id = 'me'` valide)
  - `profiles->page(?string $from = null, ?int $limit = null): ProfileCollection`
  - `profiles->create(array $payload): Profile`
  - `profiles->get(string $id): Profile`
  - `permissions->all(): PermissionCollection` (geen pagination — kleine lijst)
  - `permissions->get(string $id): Permission`

NB: Mocht een endpoint-property of methode-naam in vendor afwijken (versie-drift), check `vendor/mollie/mollie-api-php/src/MollieApiClient.php` voor de public properties en `vendor/mollie/mollie-api-php/src/Endpoints/*Endpoint.php` voor de signatures. Pas in dat geval de controller-method-call aan; de route-paden + Hub-shape blijven exact zoals deze plan voorschrijft.

From routes/api.php (bestaande Phase-5a block — Connect-block komt eronder):
```php
Route::prefix('mollie')->middleware(['feature.provider:mollie', 'resolve.mollie.account'])->group(function (): void {
    // payments, customers, etc.
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AbstractMollieConnectPassThroughController base + 5 controllers + 2 Form Requests</name>
  <files>
    app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/ClientLinksController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/OnboardingController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/OrganizationsController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/PermissionsController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/ProfilesController.php,
    app/Http/Requests/Api/V1/Mollie/Connect/CreateClientLinkRequest.php,
    app/Http/Requests/Api/V1/Mollie/Connect/CreateProfileRequest.php
  </files>
  <read_first>
    .planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md,
    app/Http/Controllers/Api/V1/Mollie/AbstractMolliePassThroughController.php,
    app/Http/Controllers/Api/V1/Mollie/CustomersController.php,
    app/Http/Controllers/Api/V1/Mollie/PaymentsController.php,
    app/Http/Requests/Api/V1/Mollie/CreateCustomerRequest.php,
    app/Sanctum/TokenAbilities.php,
    app/Mollie/MollieAccessTokenResolver.php,
    app/Support/Mollie/MollieUpstreamErrorMapper.php,
    packages/mollie-api/docs/partners/oauth-overview.md
  </read_first>
  <behavior>
    - AbstractMollieConnectPassThroughController::handle() bouwt een verse MollieApiClient (geen Mollie-facade), zet partner-token, forward't Idempotency-Key, vangt Mollie-exceptions via MollieUpstreamErrorMapper, schrijft audit-rij met token_type='partner', connection_id=null, account_id=null, partner_token_fingerprint=substr(hash('sha256',$token),0,12)
    - Bij MissingPartnerTokenException: 503 + body.error='partner_token_missing' (van MollieUpstreamErrorMapper); audit-rij krijgt token_type='partner', partner_token_fingerprint=null (geen token om te hashen), upstream_error='partner_token_missing'
    - Ability-guard identiek aan Phase 5a: GET → [mollie:read, mollie:write, *]; POST/PATCH/DELETE → [mollie:write, *]
    - 415-guard identiek aan Phase 5a: POST zonder application/json → 415 unsupported_content_type
    - Elke concrete controller delegeert via ->handle($request, '/v2/<endpoint>', fn () => SDK-call) en gebruikt $this->client() om de Mollie-client te bouwen
    - resourceToArray/collectionToArray serializeren via $resource->getResponse()->body() met JSON-fallback (identiek pattern als Phase 5a)
  </behavior>
  <action>
1. Maak directory-structuur:
   - `app/Http/Controllers/Api/V1/Mollie/Connect/`
   - `app/Http/Requests/Api/V1/Mollie/Connect/`

2. Maak `AbstractMollieConnectPassThroughController.php` (Connect-equivalent van Phase-5a-base; D-03: gescheiden hiërarchie, géén shared parent). Class-shape:
   - Namespace `App\Http\Controllers\Api\V1\Mollie\Connect`
   - `abstract class AbstractMollieConnectPassThroughController extends \App\Http\Controllers\Controller`
   - Constructor met DI: `MollieAccessTokenResolver $tokenResolver` (singleton uit AppServiceProvider; Plan 13-01).
   - Methode `protected function client(\Illuminate\Http\Request $request): \Mollie\Api\MollieApiClient`:
     - `$token = $this->tokenResolver->resolveFor('partner');` — kan `MissingPartnerTokenException` gooien, die bubble't naar `handle()` en wordt door `MollieUpstreamErrorMapper` afgevangen.
     - `$client = new \Mollie\Api\MollieApiClient();`
     - `$client->setAccessToken($token);`
     - `$consumerKey = $request->header('Idempotency-Key'); if (is_string($consumerKey) && $consumerKey !== '') { $client->setIdempotencyKey($consumerKey); }`
     - `return $client;`
   - Methode `protected function handle(Request $request, string $endpoint, callable $sdkCall): Response` — kopieer Phase-5a-logica met deze verschillen:
     - Audit-velden: `'consumer_id' => $request->user()->getKey()`, `'account_id' => null`, `'connection_id' => null`, `'token_type' => 'partner'`.
     - `partner_token_fingerprint` berekenen door `$this->tokenResolver->resolveFor('partner')` opnieuw aan te roepen IN een try/catch — bij `MissingPartnerTokenException` blijft de fingerprint `null` (en die exception is dan al door $sdkCall via `MollieUpstreamErrorMapper` afgevangen voordat we hier komen).
     - Geen `mollie_account` of `mollie_connection` request-attributes lezen.
     - Behoud: ability-guard, 415-guard, exception-mapping via `MollieUpstreamErrorMapper::mapException($e)`, default POST→201/GET→200 status, response()->json met extra headers.
   - Methodes `resourceToArray(BaseResource $resource): array` + `collectionToArray(BaseCollection $collection): array` — kopieer 1-op-1 uit Phase-5a-base. Geen shared trait — D-03 staat chirurgische duplicatie toe boven generieke abstract-explosion.

3. Maak `ClientLinksController.php`:
   - `#[Group(name: 'Mollie · Connect', description: 'Mollie Connect partner-resources (onboarding, organizations, profiles, permissions, client-links).', weight: 60)]`
   - `class ClientLinksController extends AbstractMollieConnectPassThroughController`
   - Methode `store(CreateClientLinkRequest $request): Response` → `$this->handle($request, '/v2/client-links', fn (Request $r) => ['status' => 201, 'body' => $this->resourceToArray($this->client($r)->clientLinks->create($r->validated()))])`
   - Wrap SDK-call in try/catch op `\Mollie\Api\Exceptions\ApiException` en gooi door via `\Emeq\MollieApi\Exceptions\MollieExceptionMapper::map($e)` (zelfde pattern als Phase 5a — zie `CustomersController` regels 36-38).

4. Maak `OnboardingController.php`:
   - Zelfde `#[Group(name: 'Mollie · Connect', ...)]`-attribute (Scramble groepeert op naam, dus delen mag).
   - Methode `me(Request $request): Response` → `$this->handle($request, '/v2/onboarding/me', fn (Request $r) => $this->resourceToArray($this->client($r)->onboarding->get()))`

5. Maak `OrganizationsController.php` met twee methods:
   - `me(Request $request): Response` → endpoint-template `/v2/organizations/me`, SDK-call `$client->organizations->get('me')`.
   - `show(Request $request, string $id): Response` → endpoint-template `/v2/organizations/{id}`, SDK-call `$client->organizations->get($id)`.

6. Maak `PermissionsController.php` met twee methods:
   - `index(Request $request): Response` → endpoint-template `/v2/permissions`, SDK-call `$this->collectionToArray($client->permissions->all())`.
   - `show(Request $request, string $id): Response` → endpoint-template `/v2/permissions/{id}`, SDK-call `$this->resourceToArray($client->permissions->get($id))`.

7. Maak `ProfilesController.php` met drie methods:
   - `index(Request $request): Response` → `/v2/profiles`. SDK-call: `$from = $r->query('from'); $limit = $r->query('limit'); $page = $client->profiles->page(is_string($from) ? $from : null, is_numeric($limit) ? (int) $limit : null);` → return `$page instanceof BaseCollection ? $this->collectionToArray($page) : $this->resourceToArray($page)` (zelfde defensive shape als `CustomersController::index`).
   - `store(CreateProfileRequest $request): Response` → `/v2/profiles`. SDK-call `$client->profiles->create($r->validated())` → `['status' => 201, 'body' => $this->resourceToArray(...)]`.
   - `show(Request $request, string $id): Response` → `/v2/profiles/{id}`. SDK-call `$client->profiles->get($id)`.

8. Maak `CreateClientLinkRequest.php` (Form Request):
   - `authorize(): bool { return true; }` (ability-guard staat in controller, identiek aan Phase 5a).
   - `rules(): array` per Mollie's Client Link spec (https://docs.mollie.com/reference/v2/client-links-api/create-client-link):
     ```php
     return [
         'owner' => ['required', 'array'],
         'owner.email' => ['required', 'email'],
         'owner.givenName' => ['required', 'string', 'max:255'],
         'owner.familyName' => ['required', 'string', 'max:255'],
         'name' => ['required', 'string', 'max:255'],
         'address' => ['required', 'array'],
         'address.streetAndNumber' => ['required', 'string', 'max:255'],
         'address.postalCode' => ['required', 'string', 'max:32'],
         'address.city' => ['required', 'string', 'max:255'],
         'address.country' => ['required', 'string', 'size:2'],
         'registrationNumber' => ['nullable', 'string', 'max:64'],
         'vatNumber' => ['nullable', 'string', 'max:64'],
     ];
     ```

9. Maak `CreateProfileRequest.php` (Form Request):
   - `authorize(): bool { return true; }`.
   - `rules(): array` per Mollie's Profile spec (https://docs.mollie.com/reference/v2/profiles-api/create-profile):
     ```php
     return [
         'name' => ['required', 'string', 'max:255'],
         'website' => ['required', 'url', 'max:255'],
         'email' => ['required', 'email'],
         'phone' => ['nullable', 'string', 'max:32'],
         'description' => ['nullable', 'string', 'max:500'],
         'businessCategory' => ['nullable', 'string'],
         'mode' => ['nullable', 'string', 'in:live,test'],
     ];
     ```

10. Run `vendor/bin/pint --dirty --format agent`. Geen tests in deze task — die komen in task 2 (smoke) en Plan 13-03 (per-resource).
  </action>
  <verify>
    <automated>php artisan tinker --execute 'foreach ([\App\Http\Controllers\Api\V1\Mollie\Connect\AbstractMollieConnectPassThroughController::class, \App\Http\Controllers\Api\V1\Mollie\Connect\ClientLinksController::class, \App\Http\Controllers\Api\V1\Mollie\Connect\OnboardingController::class, \App\Http\Controllers\Api\V1\Mollie\Connect\OrganizationsController::class, \App\Http\Controllers\Api\V1\Mollie\Connect\PermissionsController::class, \App\Http\Controllers\Api\V1\Mollie\Connect\ProfilesController::class, \App\Http\Requests\Api\V1\Mollie\Connect\CreateClientLinkRequest::class, \App\Http\Requests\Api\V1\Mollie\Connect\CreateProfileRequest::class] as $c) { class_exists($c) or throw new RuntimeException("Missing $c"); } echo "ok\n";'</automated>
  </verify>
  <done>
    - 8 nieuwe files bestaan op de paden in `<files>`.
    - `php artisan tinker --execute '...'` (zie verify) print `ok` — alle 8 classes autoloaden.
    - `grep -c "Mollie · Connect" app/Http/Controllers/Api/V1/Mollie/Connect/*.php` ≥ 5 (één per controller, Scramble-Group).
    - `vendor/bin/pint --dirty --format agent` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route-group + smoke-test voor routes</name>
  <files>
    routes/api.php,
    tests/Feature/Api/V1/Mollie/Connect/ConnectRouteRegistrationTest.php
  </files>
  <read_first>
    routes/api.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php,
    bootstrap/app.php,
    .planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
  </read_first>
  <behavior>
    - php artisan route:list --path=v1/mollie/connect toont minstens 9 routes:
      GET    v1/mollie/connect/onboarding/me
      GET    v1/mollie/connect/organizations/me
      GET    v1/mollie/connect/organizations/{id}
      GET    v1/mollie/connect/profiles
      POST   v1/mollie/connect/profiles
      GET    v1/mollie/connect/profiles/{id}
      GET    v1/mollie/connect/permissions
      GET    v1/mollie/connect/permissions/{id}
      POST   v1/mollie/connect/client-links
    - Elke route heeft middleware-keten: api, throttle:api, auth:sanctum, feature.provider:mollie
    - Geen Connect-route heeft 'resolve.mollie.account' middleware (D-07)
    - Naam-pattern: api.mollie.connect.<resource>.<action>
  </behavior>
  <action>
1. Open `routes/api.php`. Voeg na de bestaande `Route::prefix('mollie')->middleware(['feature.provider:mollie', 'resolve.mollie.account'])->group(...)` block (regel 63-90), nog binnen de overkoepelende `Route::middleware('auth:sanctum')->group(function () {...})` (regel 30+), een NIEUWE block toe:

   ```php
   Route::prefix('mollie/connect')
       ->middleware(['feature.provider:mollie'])
       ->name('api.mollie.connect.')
       ->group(function (): void {
           Route::get('/onboarding/me', [\App\Http\Controllers\Api\V1\Mollie\Connect\OnboardingController::class, 'me'])
               ->name('onboarding.me');

           Route::get('/organizations/me', [\App\Http\Controllers\Api\V1\Mollie\Connect\OrganizationsController::class, 'me'])
               ->name('organizations.me');
           Route::get('/organizations/{id}', [\App\Http\Controllers\Api\V1\Mollie\Connect\OrganizationsController::class, 'show'])
               ->name('organizations.show');

           Route::get('/profiles', [\App\Http\Controllers\Api\V1\Mollie\Connect\ProfilesController::class, 'index'])
               ->name('profiles.index');
           Route::post('/profiles', [\App\Http\Controllers\Api\V1\Mollie\Connect\ProfilesController::class, 'store'])
               ->name('profiles.store');
           Route::get('/profiles/{id}', [\App\Http\Controllers\Api\V1\Mollie\Connect\ProfilesController::class, 'show'])
               ->name('profiles.show');

           Route::get('/permissions', [\App\Http\Controllers\Api\V1\Mollie\Connect\PermissionsController::class, 'index'])
               ->name('permissions.index');
           Route::get('/permissions/{id}', [\App\Http\Controllers\Api\V1\Mollie\Connect\PermissionsController::class, 'show'])
               ->name('permissions.show');

           Route::post('/client-links', [\App\Http\Controllers\Api\V1\Mollie\Connect\ClientLinksController::class, 'store'])
               ->name('client-links.store');
       });
   ```

   Plaats deze block NA het bestaande `Route::prefix('mollie')...`-block en VÓÓR `Route::prefix('account-subscriptions')...` om de Mollie-routes bij elkaar te houden.

   Voeg de `use`-imports voor de 5 Connect-controllers bovenaan `routes/api.php` toe, gesorteerd alfabetisch tussen de bestaande Mollie-imports (na `use App\Http\Controllers\Api\V1\Mollie\AbstractMolliePassThroughController;` etc.):
   ```php
   use App\Http\Controllers\Api\V1\Mollie\Connect\ClientLinksController;
   use App\Http\Controllers\Api\V1\Mollie\Connect\OnboardingController;
   use App\Http\Controllers\Api\V1\Mollie\Connect\OrganizationsController;
   use App\Http\Controllers\Api\V1\Mollie\Connect\PermissionsController;
   use App\Http\Controllers\Api\V1\Mollie\Connect\ProfilesController;
   ```
   Vervang dan de `\App\Http\...`-FQNs in de route-definities door de aliased class-names voor leesbaarheid.

   NB ability-guard: anders dan Phase-5a-merchant-routes (die geen `ability:`-middleware in de route-definitie hebben omdat het in `AbstractMolliePassThroughController::handle()` zit), volgen we hier hetzelfde patroon — de Connect-base implementeert dezelfde ability-guard. Géén `'ability:mollie:write'`-middleware op de route-definitie nodig.

2. Maak directory `tests/Feature/Api/V1/Mollie/Connect/`.

3. Maak `tests/Feature/Api/V1/Mollie/Connect/ConnectRouteRegistrationTest.php`:
   - `namespace Tests\Feature\Api\V1\Mollie\Connect;`
   - `use Tests\TestCase;`
   - `class ConnectRouteRegistrationTest extends TestCase`
   - Test 1: `test_all_nine_connect_routes_are_registered_under_v1_mollie_connect_prefix()`:
     - Roep `\Route::getRoutes()` aan, filter op uri begin met `v1/mollie/connect`.
     - Assert dat de set exact 9 routes bevat, met de juiste (method, uri)-tupels:
       ```
       GET  v1/mollie/connect/onboarding/me
       GET  v1/mollie/connect/organizations/me
       GET  v1/mollie/connect/organizations/{id}
       GET  v1/mollie/connect/profiles
       POST v1/mollie/connect/profiles
       GET  v1/mollie/connect/profiles/{id}
       GET  v1/mollie/connect/permissions
       GET  v1/mollie/connect/permissions/{id}
       POST v1/mollie/connect/client-links
       ```
   - Test 2: `test_connect_routes_have_required_middleware_and_no_resolve_mollie_account()`:
     - Voor élke connect-route: assert dat `$route->gatherMiddleware()` bevat `auth:sanctum` én `feature.provider:mollie`, en NIET bevat `resolve.mollie.account`.
   - Test 3: `test_connect_route_names_follow_api_mollie_connect_namespace()`:
     - Assert elke route heeft een name die start met `api.mollie.connect.`.

4. Pint draaien na de wijzigingen.
  </action>
  <verify>
    <automated>php artisan route:list --path=v1/mollie/connect --json 2>/dev/null | php -r 'echo count(json_decode(stream_get_contents(STDIN), true)) === 9 ? "ok\n" : "fail\n";' && php artisan test --compact --filter=ConnectRouteRegistrationTest</automated>
  </verify>
  <done>
    - `php artisan route:list --path=v1/mollie/connect` toont exact 9 routes.
    - `php artisan test --compact --filter=ConnectRouteRegistrationTest` exit 0 met 3 passed.
    - `php artisan test --compact` (volledige suite) exit 0 — geen regressie op bestaande routes/Mollie-tests.
    - `vendor/bin/pint --dirty --format agent` exit 0.
  </done>
</task>

</tasks>

<verification>
- Per `php artisan route:list`: 9 Connect-routes zichtbaar met juiste prefix, naam-pattern `api.mollie.connect.*`, en middleware-keten `web → api → throttle:api → auth:sanctum → feature.provider:mollie` (web wordt door Laravel auto-toegevoegd; api/throttle:api via bootstrap/app.php middleware-config; auth:sanctum + feature.provider:mollie via route-group).
- `AbstractMollieConnectPassThroughController` heeft géén dependency op `MollieConnectionContext`, géén dependency op `Mollie`-facade (`Emeq\MollieApi\Facades\Mollie`); bouwt eigen `new MollieApiClient()`.
- Geen wijziging op bestaande Phase-5a-controllers, Phase-5a-routes of Phase-5a-tests.
- Bestaande `php artisan test --compact` suite blijft groen — geen regressies op ~510 tests.
</verification>

<success_criteria>
- 10 nieuwe artefacten geleverd: 1 base + 5 controllers + 2 form requests + 1 route-group-edit + 1 smoke-test.
- 9 routes geregistreerd onder `/v1/mollie/connect/*`.
- `ConnectRouteRegistrationTest` met 3 passed tests.
- `composer audit` exit 0.
- `vendor/bin/pint --dirty --format agent` exit 0.
</success_criteria>

<output>
Schrijf `.planning/phases/13-mollie-connect-partner-resources/13-02-SUMMARY.md` met:
- 10 geraakte files, met regelnummer-ranges van routes/api.php-edit.
- Route-tabel: 9 routes met method/uri/name/controller-action.
- Reden voor géén shared super-class (D-03 quote).
- Lijst van Mollie SDK-method-signatures die zijn gebruikt (per resource) + opmerking als er een vendor-signature-drift bleek (anders: "alle vendor-signatures matched specs").
- ConnectRouteRegistrationTest-output: aantal passed.
</output>
