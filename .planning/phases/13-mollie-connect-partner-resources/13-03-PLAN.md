---
phase: 13-mollie-connect-partner-resources
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php
  - tests/Concerns/StubsMollieConnectClient.php
  - tests/Feature/Api/V1/Mollie/Connect/ClientLinksTest.php
  - tests/Feature/Api/V1/Mollie/Connect/OnboardingTest.php
  - tests/Feature/Api/V1/Mollie/Connect/OrganizationsTest.php
  - tests/Feature/Api/V1/Mollie/Connect/PermissionsTest.php
  - tests/Feature/Api/V1/Mollie/Connect/ProfilesTest.php
  - tests/Feature/Api/V1/Mollie/Connect/TokenResolverIntegrationTest.php
  - tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php
autonomous: true
requirements:
  - MOLL-05
  - MOLL-06
must_haves:
  truths:
    - "Per resource bestaat een feature-test die happy-path bewijst: ClientLinks POST→201 met `_links.clientLink.href`, Onboarding GET /me→200, Organizations GET /me + /{id}→200, Profiles GET list + POST→201 + GET /{id}→200, Permissions GET list + GET /{id}→200."
    - "Per resource bestaat een 401-error-mapping-test (MOLL-05 SC-1 requirement): Mollie `AuthenticationException` → Hub 502 `mollie_auth_failed` met `upstream_status: 401`."
    - "Audit-rij bewijs: na elke happy-path-call bestaat een `pass_through_calls`-rij met `provider='mollie'`, `token_type='partner'`, `account_id=NULL`, `connection_id=NULL`, `partner_token_fingerprint` 12-char-string identiek aan `substr(hash('sha256', $partnerToken), 0, 12)`."
    - "Geen `X-Account-Id`-header op Connect-routes — calls zonder dat header krijgen geen 400 zoals merchant-routes wel doen (D-07-bewijs)."
    - "`TokenResolverIntegrationTest` bewijst beide token-paden expliciet (MOLL-06 SC-2): één test capture't de access-token op een merchant-route (Connection-token), één capture't de access-token op een Connect-route (partner-token uit `MOLLIE_PARTNER_ACCESS_TOKEN`). Twee aparte assertions tegen exact dezelfde spy-mechaniek."
    - "Idempotency-Key auto-forward werkt voor Connect-POST-routes: een ClientLinks POST met `Idempotency-Key: test-key-xyz` zet die exact door op `MollieApiClient::setIdempotencyKey()` (capture via stub)."
    - "Scramble `/docs/api.json` toont alle 9 Connect-routes met tag `Mollie · Connect` als hun (eerste of enige) tag."
  artifacts:
    - path: "tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php"
      provides: "Test-only MollieApiClient subclass met endpoint-stubs voor 5 Connect-resources + access-token-capture (sibling van Phase-5a's StubMollieClient)"
      exports: ["StubMollieConnectClient"]
      min_lines: 50
    - path: "tests/Concerns/StubsMollieConnectClient.php"
      provides: "Helper-trait `setupMollieConnectConsumer()` + `bindMollieConnectStubs()` + `callMollieConnect()` (zonder X-Account-Id)"
      exports: ["StubsMollieConnectClient"]
      min_lines: 80
    - path: "tests/Feature/Api/V1/Mollie/Connect/ClientLinksTest.php"
      provides: "Happy-path + 401-error + Idempotency-forward + audit-row tests"
      min_lines: 60
    - path: "tests/Feature/Api/V1/Mollie/Connect/OnboardingTest.php"
      provides: "GET /me happy-path + 401 + audit"
    - path: "tests/Feature/Api/V1/Mollie/Connect/OrganizationsTest.php"
      provides: "GET /me + /{id} happy-path + 401 + audit"
    - path: "tests/Feature/Api/V1/Mollie/Connect/PermissionsTest.php"
      provides: "GET list + /{id} happy-path + 401 + audit"
    - path: "tests/Feature/Api/V1/Mollie/Connect/ProfilesTest.php"
      provides: "GET list + POST create + GET /{id} happy-path + 401 + audit + Form-Request-validatie"
    - path: "tests/Feature/Api/V1/Mollie/Connect/TokenResolverIntegrationTest.php"
      provides: "MOLL-06 SC-2 bewijs: beide token-paden expliciet in één test-file"
      min_lines: 60
  key_links:
    - from: "tests/Feature/Api/V1/Mollie/Connect/*Test.php"
      to: "tests/Concerns/StubsMollieConnectClient.php"
      via: "use Tests\\Concerns\\StubsMollieConnectClient"
      pattern: "StubsMollieConnectClient"
    - from: "tests/Feature/Api/V1/Mollie/Connect/TokenResolverIntegrationTest.php"
      to: "Mollie merchant-route (/v1/mollie/payments)"
      via: "spy-vergelijking tussen Connection-token en partner-token"
      pattern: "lastUsedAccessToken|capturedAccessToken"
    - from: "tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php"
      to: "Connect-route-discovery + Mollie · Connect tag-assertions"
      via: "nieuwe test-methods aan bestaande class"
      pattern: "mollie/connect"
---

<objective>
Schrijf de tests die de Connect-routes uit Plan 13-02 valideren en de MOLL-05 + MOLL-06 success-criteria expliciet bewijzen. Lever 6 feature-test-files (5 per resource + 1 integratie-test voor MOLL-06 SC-2), één test-only stub-client (`StubMollieConnectClient`), één test-trait (`StubsMollieConnectClient`), en breid `ScrambleRouteDiscoveryTest` uit met assertions dat de 9 Connect-routes onder de tag `Mollie · Connect` in de OpenAPI-output staan.

Purpose: De Phase-13-substrate (resolver + base controller + 5 controllers + routes) is functioneel pas bewezen als (a) elke route happy-path + 401-error door een test heen komt, (b) audit-rows met `token_type='partner'` worden geschreven, (c) één integratie-test beide token-paden zijbij-zij test (= het exacte bewijs dat MOLL-06 SC-2 vraagt: "Een integration-test dekt beide paden expliciet"), en (d) Scramble's OpenAPI groepeert de routes onder de juiste tag (= MOLL-05 SC-3).

Output:
- 1 test-only `MollieApiClient` subclass + 1 helper-trait.
- 5 per-resource feature-tests.
- 1 integration-test voor MOLL-06 SC-2.
- ~10 nieuwe test-methods in `ScrambleRouteDiscoveryTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
@.planning/phases/13-mollie-connect-partner-resources/13-01-SUMMARY.md
@.planning/phases/13-mollie-connect-partner-resources/13-02-SUMMARY.md
@tests/Concerns/StubsMollieClient.php
@tests/Feature/Api/V1/Mollie/StubMollieClient.php
@tests/Feature/Api/V1/Mollie/CustomersTest.php
@tests/Feature/Api/V1/Mollie/MolliePassThroughErrorMappingTest.php
@tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php
@app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php
@app/Http/Controllers/Api/V1/Mollie/Connect/ClientLinksController.php
@app/Sanctum/TokenAbilities.php
@app/Mollie/MollieAccessTokenResolver.php
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
<!-- Phase-5a-test-patterns: nieuwe Connect-stubs spiegelen deze shape -->

From tests/Feature/Api/V1/Mollie/StubMollieClient.php:
```php
class StubMollieClient extends \Mollie\Api\MollieApiClient
{
    public function __construct(object $paymentsStub, array $extraStubs = []);
    public function __get(string $name): mixed; // returnt stub uit extraStubs of parent
}
```

From tests/Concerns/StubsMollieClient.php (relevant helpers):
- `setupMollieConsumer(array $abilities, string $externalId): array{Consumer, string $token, Account, Connection}`
- `callMollie(string $token, string $method, string $uri, array $payload, array $extraHeaders, string $accountId): TestResponse`
- bindMollieStub/s — binds Stub via `app()->bind(\Emeq\MollieApi\Mollie::class, fn() => $wrapperMock)` waar wrapperMock's `client()` de stub returnt.

NB: Connect-controllers gebruiken GEEN `Emeq\MollieApi\Facades\Mollie` — ze instantiëren `new \Mollie\Api\MollieApiClient()` direct. Dus de Connect-stub-strategy moet anders: ofwel (a) een `app()->bind(MollieApiClient::class, fn() => $stub)` + Connect-base-controller laten gebruiken van `app(MollieApiClient::class)` ipv `new`, ofwel (b) een functionele mock via een test-side decorator/factory.

Kies optie (a): **breid `AbstractMollieConnectPassThroughController::client()` aan** zodat het de client uit de container haalt (`app(MollieApiClient::class)`) ipv `new MollieApiClient()`. Default-binding hoeft niet in production (Laravel's container construeert `new` zonder dependencies automatisch); test-side bindt een `StubMollieConnectClient`-instance.

**ACTIE in deze plan:** Patch Plan-13-02's `AbstractMollieConnectPassThroughController::client()` zodat het via `app(MollieApiClient::class)` resolved ipv `new MollieApiClient()`. Dit is een 1-regel-edit en hoort technisch in 13-02, maar wordt hier gedaan omdat de test-noodzaak hier wordt gevoeld. Documenteer in 13-03-SUMMARY.md dat 13-02's `AbstractMollieConnectPassThroughController.php` ÉÉN regel is aangepast als test-affordance.

From app/Sanctum/TokenAbilities.php:
- MOLLIE_READ = 'mollie:read'
- MOLLIE_WRITE = 'mollie:write'

From app/Models/PassThroughCall.php (na Plan 13-01):
- Fillable bevat 'token_type', 'partner_token_fingerprint'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StubMollieConnectClient + StubsMollieConnectClient trait + container-binding-affordance</name>
  <files>
    tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php,
    tests/Concerns/StubsMollieConnectClient.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php
  </files>
  <read_first>
    tests/Feature/Api/V1/Mollie/StubMollieClient.php,
    tests/Concerns/StubsMollieClient.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php,
    app/Mollie/MollieAccessTokenResolver.php
  </read_first>
  <behavior>
    - StubMollieConnectClient extends MollieApiClient, exposes resource-stubs voor: clientLinks, onboarding, organizations, profiles, permissions
    - Stub capture't access-token bij elke setAccessToken-call in $lastUsedAccessToken
    - Stub capture't Idempotency-Key bij setIdempotencyKey in $lastIdempotencyKey
    - StubsMollieConnectClient trait heeft:
      - setupMollieConnectConsumer(array $abilities): array{Consumer, string $token} — geen Account/Connection nodig (D-07)
      - bindMollieConnectStubs(array $resolvers): StubMollieConnectClient — bindt stub in container voor MollieApiClient
      - callMollieConnect(string $token, string $method, string $uri, array $payload = [], array $extraHeaders = []): TestResponse — geen X-Account-Id-header
      - setPartnerToken(string $token): void — config(['services.mollie.partner_access_token' => $token]) + forgetInstance(MollieAccessTokenResolver::class) zodat singleton opnieuw bound wordt
    - AbstractMollieConnectPassThroughController::client() resolved MollieApiClient via app() ipv new
  </behavior>
  <action>
1. Patch `app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php` (Plan-13-02-artefact): vervang `$client = new \Mollie\Api\MollieApiClient();` in `client()` door `$client = app(\Mollie\Api\MollieApiClient::class);`. Voeg een 1-regel docblock toe boven die regel: `// Container-resolved zodat tests een StubMollieConnectClient kunnen injecteren via app()->bind().`

2. Maak `tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php`:
   - Namespace `Tests\Feature\Api\V1\Mollie\Connect;`
   - `class StubMollieConnectClient extends \Mollie\Api\MollieApiClient`
   - Public properties: `?string $lastUsedAccessToken = null;`, `?string $lastIdempotencyKey = null;`
   - Constructor: `public function __construct(array $stubs = [])` — `parent::__construct();` + `$this->stubs = $stubs;`
   - `public function setAccessToken(string $accessToken): self` — `$this->lastUsedAccessToken = $accessToken; parent::setAccessToken($accessToken); return $this;`
   - `public function setIdempotencyKey(string $key): self` — `$this->lastIdempotencyKey = $key; parent::setIdempotencyKey($key); return $this;`
   - `public function __get(string $name): mixed` — returnt `$this->stubs[$name] ?? parent::__get($name)`.
   - PHPDoc-properties: `@property mixed $clientLinks`, `@property mixed $onboarding`, `@property mixed $organizations`, `@property mixed $profiles`, `@property mixed $permissions`.

3. Maak `tests/Concerns/StubsMollieConnectClient.php`:
   - Namespace `Tests\Concerns;`
   - `trait StubsMollieConnectClient`
   - Methodes:
     - `protected ?StubMollieConnectClient $mollieConnectClient = null;`
     - `protected function setupMollieConnectConsumer(array $abilities = [\App\Sanctum\TokenAbilities::MOLLIE_READ]): array` → maakt Consumer + PAT met abilities, returnt `[Consumer, $plainTextToken]`.
     - `protected function setPartnerToken(string $token = 'access_partner_xyz'): void` → `config(['services.mollie.partner_access_token' => $token]); app()->forgetInstance(\App\Mollie\MollieAccessTokenResolver::class);` zodat de singleton vers wordt met de nieuwe config.
     - `protected function bindMollieConnectStubs(array $resolvers): StubMollieConnectClient` → bouwt anonymous-class-stubs per resource, instantieert `StubMollieConnectClient` met deze stubs, bindt via `$this->app->bind(\Mollie\Api\MollieApiClient::class, fn () => $this->mollieConnectClient);`, returnt de stub.
       - Per resource (clientLinks/onboarding/organizations/profiles/permissions): anonymous class met methodes die `$resolvers['<resource>']('<op>', $arg)` aanroepen en de return-value teruggeven. Op-namen per resource:
         - clientLinks: `create($payload)`
         - onboarding: `get()` (geen arg)
         - organizations: `get($id)`
         - profiles: `page($from, $limit)`, `create($payload)`, `get($id)`
         - permissions: `all()`, `get($id)`
       - Capture: per resource bewaar de op + arg in `$this->mollieConnectCaptured['<resource>_<op>'][]` zodat tests kunnen asserten welke payload naar de stub ging.
     - `protected function callMollieConnect(string $token, string $method, string $uri, array $payload = [], array $extraHeaders = []): TestResponse` — net als `callMollie` maar zónder `X-Account-Id`-header (D-07: Connect-routes hebben geen Account-context).
     - `protected array $mollieConnectCaptured = [...]` initialized voor alle 5 resources + idempotency_keys-capture.
   - Pattern verbatim van `Tests\Concerns\StubsMollieClient` aanpassen — kopieer waar mogelijk om consistentie te garanderen.

4. Pint draaien.
  </action>
  <verify>
    <automated>php artisan tinker --execute 'class_exists(\Tests\Feature\Api\V1\Mollie\Connect\StubMollieConnectClient::class) and trait_exists(\Tests\Concerns\StubsMollieConnectClient::class) ? print "ok\n" : exit(1);' && grep -c "app(.*MollieApiClient::class)" app/Http/Controllers/Api/V1/Mollie/Connect/AbstractMollieConnectPassThroughController.php | grep -v '^0$'</automated>
  </verify>
  <done>
    - Beide test-helper-files bestaan en autoload schoon.
    - `AbstractMollieConnectPassThroughController::client()` resolved client via `app(MollieApiClient::class)`.
    - `php artisan test --compact tests/Feature/Api/V1/Mollie/Connect/ConnectRouteRegistrationTest.php` blijft groen (route-registratie-smoke uit Plan 13-02).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 per-resource feature-tests (happy-path + 401-error-mapping + audit-row + idempotency-forward voor write-routes)</name>
  <files>
    tests/Feature/Api/V1/Mollie/Connect/ClientLinksTest.php,
    tests/Feature/Api/V1/Mollie/Connect/OnboardingTest.php,
    tests/Feature/Api/V1/Mollie/Connect/OrganizationsTest.php,
    tests/Feature/Api/V1/Mollie/Connect/PermissionsTest.php,
    tests/Feature/Api/V1/Mollie/Connect/ProfilesTest.php
  </files>
  <read_first>
    tests/Concerns/StubsMollieConnectClient.php,
    tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php,
    tests/Feature/Api/V1/Mollie/CustomersTest.php,
    tests/Feature/Api/V1/Mollie/MolliePassThroughErrorMappingTest.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/ClientLinksController.php,
    app/Http/Controllers/Api/V1/Mollie/Connect/ProfilesController.php,
    app/Sanctum/TokenAbilities.php,
    .planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
  </read_first>
  <behavior>
    - Elke test-file gebruikt RefreshDatabase + StubsMollieConnectClient + setPartnerToken() in setUp() of in elke test
    - ClientLinksTest: POST happy → 201 met body geserialiseerd uit stub-response (assert id, _links.clientLink.href); POST 401-error van Mollie → Hub 502 mollie_auth_failed; POST met Idempotency-Key → $stub->lastIdempotencyKey === verzonden key; Form-Request faalt → 422 met error 'validation_failed' (Laravel-default voor Form Request)
    - OnboardingTest: GET /me → 200 met onboarding-resource; 401 → 502 mollie_auth_failed; audit-rij met token_type='partner', connection_id=null, account_id=null, partner_token_fingerprint = 12-char-hash van partner-token
    - OrganizationsTest: GET /me + GET /{id} happy-paths; 401 mapping; audit per call
    - PermissionsTest: GET list + GET /{id} happy-paths met PermissionCollection; 401 mapping
    - ProfilesTest: GET list + POST create (Form Request) + GET /{id}; 401 mapping; CreateProfileRequest validation faalt op missing required (assert 422)
  </behavior>
  <action>
1. Maak `tests/Feature/Api/V1/Mollie/Connect/ClientLinksTest.php`:
   - Class extends `Tests\TestCase`, gebruikt `RefreshDatabase` + `StubsMollieConnectClient`.
   - Test 1: `test_post_client_link_creates_resource_and_returns_201_with_audit_row()`:
     - `$this->setPartnerToken('access_partner_test_001');`
     - `[$consumer, $token] = $this->setupMollieConnectConsumer([TokenAbilities::MOLLIE_WRITE]);`
     - Stub clientLinks->create returnt een hand-gemaakte `ClientLink`-resource via een dummy-class die `getResponse()` returnt met een JSON-body `{"id":"cl_test","resource":"client-link","_links":{"clientLink":{"href":"https://my.mollie.com/dashboard/client-link/cl_test"}}}`.
     - Payload: zie CreateClientLinkRequest-rules (`owner.email`, `owner.givenName`, `owner.familyName`, `name`, `address.*`).
     - `$response = $this->callMollieConnect($token, 'POST', '/v1/mollie/connect/client-links', $payload);`
     - Asserts: `assertCreated()`, `assertJsonPath('id', 'cl_test')`, `assertJsonPath('_links.clientLink.href', '...')`, `assertSame('access_partner_test_001', $stub->lastUsedAccessToken)`, en `assertDatabaseHas('pass_through_calls', ['provider' => 'mollie', 'method' => 'POST', 'path' => '/v2/client-links', 'status' => 201, 'token_type' => 'partner', 'connection_id' => null, 'account_id' => null, 'partner_token_fingerprint' => substr(hash('sha256', 'access_partner_test_001'), 0, 12)])`.
   - Test 2: `test_post_client_link_with_idempotency_key_forwards_to_mollie_client()`:
     - Set partner-token + token. Roep POST aan met header `Idempotency-Key: ck-test-xyz`.
     - Assert `$stub->lastIdempotencyKey === 'ck-test-xyz'`.
   - Test 3: `test_post_client_link_with_mollie_auth_failure_maps_to_502_mollie_auth_failed()`:
     - Stub clientLinks->create gooit `new \Mollie\Api\Exceptions\ApiException('Unauthorized', 401)` (of `\Emeq\MollieApi\Exceptions\AuthenticationException`). Door MollieExceptionMapper → AuthenticationException → MollieUpstreamErrorMapper → 502.
     - Assert response status 502, JSON path `error = mollie_auth_failed`, `upstream_status = 401`.
   - Test 4: `test_post_client_link_with_invalid_payload_returns_422_validation_failed()`:
     - Geen partner-token nodig — Form Request blokkeert vóór SDK-call.
     - POST met lege body.
     - Assert 422 (Laravel-default voor Form Request); body bevat `errors` key met `owner.email`, `name`, `address.streetAndNumber`, etc.

2. Maak `tests/Feature/Api/V1/Mollie/Connect/OnboardingTest.php`:
   - Test 1: `test_get_onboarding_me_returns_200_with_resource()`:
     - Stub `onboarding->get()` returnt een Onboarding-resource met JSON `{"resource":"onboarding","name":"Emeq B.V.","status":"completed","canReceivePayments":true,"canReceiveSettlements":true,"signedUpAt":"2026-05-18T12:00:00+00:00"}`.
     - Set partner-token, GET `/v1/mollie/connect/onboarding/me`, assertOk, assert `name = Emeq B.V.`, assert audit-rij met `path = /v2/onboarding/me`, `method = GET`, `token_type = partner`.
   - Test 2: `test_get_onboarding_with_auth_failure_maps_to_502()` — analoog aan ClientLinks 401-test.

3. Maak `tests/Feature/Api/V1/Mollie/Connect/OrganizationsTest.php`:
   - Test 1: `test_get_organizations_me_returns_resource()`:
     - Stub `organizations->get('me')` returnt Organization-resource met `id = 'org_emeq'`, `name = 'Emeq B.V.'`.
     - GET `/v1/mollie/connect/organizations/me`, assertOk, audit-path `/v2/organizations/me`.
   - Test 2: `test_get_organization_by_id_returns_resource()`:
     - Stub `organizations->get('org_xyz')` returnt Organization-resource.
     - GET `/v1/mollie/connect/organizations/org_xyz`, assert id, audit-path `/v2/organizations/{id}`.
   - Test 3: `test_get_organization_with_auth_failure_maps_to_502()`.

4. Maak `tests/Feature/Api/V1/Mollie/Connect/PermissionsTest.php`:
   - Test 1: `test_get_permissions_returns_collection_200()`:
     - Stub `permissions->all()` returnt PermissionCollection met 2 items: `{id:'payments.read', granted:true}` + `{id:'payments.write', granted:false}`.
     - GET `/v1/mollie/connect/permissions`, assertOk, audit-path `/v2/permissions`.
   - Test 2: `test_get_permission_by_id_returns_resource()`:
     - Stub `permissions->get('payments.read')` returnt enkele Permission.
     - GET `/v1/mollie/connect/permissions/payments.read`, audit-path `/v2/permissions/{id}`.
   - Test 3: `test_get_permissions_with_auth_failure_maps_to_502()`.

5. Maak `tests/Feature/Api/V1/Mollie/Connect/ProfilesTest.php`:
   - Test 1: `test_get_profiles_returns_paginated_collection()`:
     - Stub `profiles->page(null, null)` returnt ProfileCollection met 1 item.
     - GET `/v1/mollie/connect/profiles`, assertOk, audit-path `/v2/profiles`.
   - Test 2: `test_post_profile_creates_resource_returns_201()`:
     - Stub `profiles->create($payload)` returnt Profile-resource met `id = 'pfl_test'`.
     - Payload: `{name: 'Emeq Test', website: 'https://emeq.test', email: 'info@emeq.test'}` (per CreateProfileRequest-rules).
     - POST `/v1/mollie/connect/profiles`, assertCreated, assertJsonPath id, audit met method=POST + status=201.
   - Test 3: `test_get_profile_by_id_returns_resource()`:
     - Stub `profiles->get('pfl_test')` returnt Profile.
     - GET `/v1/mollie/connect/profiles/pfl_test`, audit-path `/v2/profiles/{id}`.
   - Test 4: `test_post_profile_with_missing_required_fields_returns_422()`:
     - POST met empty body, assert 422 + errors-keys `name`, `website`, `email`.
   - Test 5: `test_get_profiles_with_auth_failure_maps_to_502()`.

6. Voor alle resource-resources die niet via `getResponse()` te serializeren zijn in een unit-test-context: gebruik dezelfde dummy-resource-pattern als Phase-5a's `StubsMollieClient::makeCustomer/makePayment` — een lichte resource-subclass die de attributes setter accepteert. Voor `_links` of geneste velden moet de stub `getResponse()` overschrijven om een fake `Response` te returnen met JSON-body — kopieer dit pattern verbatim uit hoe `CustomersTest` met `assertJsonPath` werkt.

7. Pint draaien.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Api/V1/Mollie/Connect/</automated>
  </verify>
  <done>
    - `php artisan test --compact tests/Feature/Api/V1/Mollie/Connect/` exit 0 met ≥17 passed tests (4+2+3+3+5).
    - `php artisan test --compact` (volledige suite) exit 0.
    - `vendor/bin/pint --dirty --format agent` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TokenResolverIntegrationTest (MOLL-06 SC-2) + ScrambleRouteDiscoveryTest extension</name>
  <files>
    tests/Feature/Api/V1/Mollie/Connect/TokenResolverIntegrationTest.php,
    tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php
  </files>
  <read_first>
    tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php,
    tests/Concerns/StubsMollieClient.php,
    tests/Concerns/StubsMollieConnectClient.php,
    tests/Feature/Api/V1/Mollie/StubMollieClient.php,
    tests/Feature/Api/V1/Mollie/Connect/StubMollieConnectClient.php,
    app/Mollie/MollieAccessTokenResolver.php,
    .planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
  </read_first>
  <behavior>
    - TokenResolverIntegrationTest::test_merchant_route_uses_connection_access_token() — POST /v1/mollie/payments stuurt 'access_<connection_specific>' naar de stub (geen partner-token); audit-rij heeft token_type='connection' OF NULL (Phase 5a default behoudt NULL — niet gewijzigd door dit plan, dus accepteer NULL voor merchant-rijen).
    - TokenResolverIntegrationTest::test_connect_route_uses_partner_access_token() — GET /v1/mollie/connect/permissions stuurt 'access_partner_xyz' naar de stub; audit-rij heeft token_type='partner'.
    - TokenResolverIntegrationTest::test_connect_route_with_missing_partner_token_returns_503_partner_token_missing() — config-clear van partner-token, hit Connect-route, assert 503 + body.error='partner_token_missing'.
    - ScrambleRouteDiscoveryTest krijgt minstens 3 extra test-methods: (a) presence van alle 9 /mollie/connect/* paths in spec, (b) elke connect-path heeft tag 'Mollie · Connect', (c) bestaande Mollie · Payments/Customers/etc tags blijven onveranderd (regressie-vangst).
  </behavior>
  <action>
1. Maak `tests/Feature/Api/V1/Mollie/Connect/TokenResolverIntegrationTest.php`:
   - Class extends `Tests\TestCase`, gebruikt `RefreshDatabase` + `StubsMollieClient` (voor merchant-pad) + `StubsMollieConnectClient` (voor connect-pad).
   - Test 1: `test_merchant_route_uses_connection_access_token()`:
     - Setup merchant: `[, $token, , $connection] = $this->setupMollieConsumer([TokenAbilities::MOLLIE_WRITE]);`
     - Bind merchant-stub via `$this->bindMollieStubs(['payments' => fn ($op) => $this->makePayment(['id' => 'tr_test', 'status' => 'open'])]);`
     - POST `/v1/mollie/payments` met minimale valide payload (`{amount: {value: '10.00', currency: 'EUR'}, description: 'test', redirectUrl: 'https://example.test/return'}`).
     - Assert response is 201 of 200 (afhankelijk van Phase-5a-behavior).
     - Assert (via merchant-stub's captured access-token of via `$connection->access_token`) dat de Connection's encrypted-decrypted `access_token` is gebruikt — bijvoorbeeld door te asserten dat de merchant-stub's `$this->mollieClient->lastUsedAccessToken === $connection->access_token` (NB: dit vereist dat `StubMollieClient` óók een `lastUsedAccessToken` capture; zo niet, capture via `$this->mollieCaptured` of breid `StubMollieClient` 1-regel uit).
   - Test 2: `test_connect_route_uses_partner_access_token()`:
     - `$this->setPartnerToken('access_partner_xyz');`
     - `[, $token] = $this->setupMollieConnectConsumer([TokenAbilities::MOLLIE_READ]);`
     - `$stub = $this->bindMollieConnectStubs(['permissions' => fn ($op) => $op === 'all' ? $this->makePermissionCollection() : null]);` (helper-methode bouwt een lege PermissionCollection-resource).
     - GET `/v1/mollie/connect/permissions`.
     - Assert `$stub->lastUsedAccessToken === 'access_partner_xyz'`.
     - Assert response 200.
     - Assert audit-rij heeft `token_type = 'partner'`.
   - Test 3: `test_connect_route_with_missing_partner_token_returns_503_partner_token_missing()`:
     - `config(['services.mollie.partner_access_token' => null]); app()->forgetInstance(\App\Mollie\MollieAccessTokenResolver::class);`
     - `[, $token] = $this->setupMollieConnectConsumer([TokenAbilities::MOLLIE_READ]);`
     - GET `/v1/mollie/connect/permissions`.
     - Assert response 503, `assertJsonPath('error', 'partner_token_missing')`.
     - Assert audit-rij bestaat met `upstream_error = 'partner_token_missing'`.

2. Indien `StubMollieClient` (Phase-5a) géén `lastUsedAccessToken`-capture heeft: voeg 1 publieke property + override van `setAccessToken()` toe analoog aan `StubMollieConnectClient`. Dit is een kleine sibling-test-affordance — documenteer in 13-03-SUMMARY.md.

3. Open `tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php` en voeg drie nieuwe test-methods toe (helemaal aan het einde, vóór `private function fetchSpec()`):
   - `test_openapi_spec_contains_all_nine_mollie_connect_routes()`:
     - `$spec = $this->fetchSpec();`
     - `$paths = $spec['paths'] ?? [];`
     - Assert elk van deze 9 paths bestaat (`assertArrayHasKey`):
       - `/mollie/connect/onboarding/me` (GET)
       - `/mollie/connect/organizations/me` (GET)
       - `/mollie/connect/organizations/{id}` (GET)
       - `/mollie/connect/profiles` (GET, POST)
       - `/mollie/connect/profiles/{id}` (GET)
       - `/mollie/connect/permissions` (GET)
       - `/mollie/connect/permissions/{id}` (GET)
       - `/mollie/connect/client-links` (POST)
   - `test_openapi_spec_groups_connect_routes_under_mollie_connect_tag()`:
     - Voor elke connect-path: `$op = $paths['/mollie/connect/<...>']['<method>'] ?? null;` → assert `$op['tags']` bevat `'Mollie · Connect'`.
     - Test minimaal 3 paths (onboarding/me, profiles POST, client-links POST) om bewijs te leveren zonder alle 9 te dupliceren.
   - `test_openapi_spec_preserves_existing_mollie_merchant_tags()`:
     - Sanity-regressie: voor `/mollie/payments` POST → tag bevat `'Mollie · Payments'`; voor `/mollie/customers` GET → tag bevat `'Mollie · Customers'`. Dit vangt onbedoelde Group-attribute-edits.

4. Pint draaien.
  </action>
  <verify>
    <automated>php artisan test --compact --filter='TokenResolverIntegrationTest|ScrambleRouteDiscoveryTest'</automated>
  </verify>
  <done>
    - `php artisan test --compact --filter=TokenResolverIntegrationTest` exit 0 met 3 passed.
    - `php artisan test --compact --filter=ScrambleRouteDiscoveryTest` exit 0 — minstens 3 nieuwe tests + bestaande tests passed.
    - `php artisan test --compact` (volledige suite) exit 0 — geen regressie op ~510 tests.
    - `vendor/bin/pint --dirty --format agent` exit 0.
  </done>
</task>

</tasks>

<verification>
- MOLL-05 SC-1 bewezen: per-resource happy-path + 401-error-mapping in Task 2.
- MOLL-06 SC-2 bewezen: `TokenResolverIntegrationTest` in Task 3 met beide paden expliciet (de "een integration-test dekt beide paden expliciet"-eis uit ROADMAP).
- MOLL-05 SC-3 bewezen: `ScrambleRouteDiscoveryTest`-extensie laat zien dat alle 9 routes met `Mollie · Connect`-tag in `/docs/api.json` staan, ZONDER bestaande Mollie · Payments/Customers/etc tags te breken.
- Audit-rij-shape bewezen: elke happy-path-test assert `token_type='partner'`, `connection_id=null`, `account_id=null`, `partner_token_fingerprint` exact 12 chars (sha256 prefix).
- Idempotency-Key forward bewezen voor ClientLinks (write-route).
- Form-Request-validatie bewezen voor ClientLinks + Profiles (write-routes).
</verification>

<success_criteria>
- 6 nieuwe test-files (5 per-resource + 1 integration) + 2 helper-files (Stub + Trait) + 3 nieuwe test-methods in ScrambleRouteDiscoveryTest.
- ≥17 nieuwe passed-tests in `tests/Feature/Api/V1/Mollie/Connect/` directory.
- ≥3 nieuwe passed-tests in `tests/Feature/Documentation/ScrambleRouteDiscoveryTest.php`.
- 1-regel patch op `AbstractMollieConnectPassThroughController` (test-affordance: `new MollieApiClient()` → `app(MollieApiClient::class)`).
- 1-regel patch op `StubMollieClient` (Phase-5a) als die nog geen `lastUsedAccessToken`-capture had — gedocumenteerd in SUMMARY.
- `php artisan test --compact` exit 0.
- `composer audit` exit 0.
- `vendor/bin/pint --dirty --format agent` exit 0.
</success_criteria>

<output>
Schrijf `.planning/phases/13-mollie-connect-partner-resources/13-03-SUMMARY.md` met:
- Lijst van 8 nieuwe testfiles + 1-regel patches op 2 bestaande files (controller-base + StubMollieClient indien nodig).
- MOLL-05 SC-1 mapping: per route welke test 200 + 401 dekt (mini-tabel 9 routes × 2 paden).
- MOLL-06 SC-2 mapping: TokenResolverIntegrationTest method-namen + asserties.
- MOLL-05 SC-3 mapping: ScrambleRouteDiscoveryTest extension-method-namen + assertion-tellingen.
- Final test-count: aantal nieuwe tests + totale suite-count vóór/na.
- Eventuele vendor-signature-drift of stub-aanpassing-notities.
</output>
