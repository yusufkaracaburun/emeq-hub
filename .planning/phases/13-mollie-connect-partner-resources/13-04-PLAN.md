---
phase: 13-mollie-connect-partner-resources
plan: 04
type: execute
wave: 4
depends_on:
  - 13-02
  - 13-03
files_modified:
  - .docs/decisions/mollie-connect-partner-resources.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MOLL-05
  - MOLL-06
must_haves:
  truths:
    - "Nieuwe ADR `.docs/decisions/mollie-connect-partner-resources.md` bestaat met sections: Status / Keuze / Context / Alternatieven afgewogen / Consequences / Wanneer herzien — identiek format als bestaande ADRs in `.docs/decisions/`."
    - "ADR legt 4 dingen vast: (1) route-prefix-rule (`/v1/mollie/connect/*` → partner-token), (2) `MollieAccessTokenResolver`-shape + binding-strategy, (3) `MOLLIE_PARTNER_ACCESS_TOKEN` env-var als bron met fingerprint-only-in-logs-invariant, (4) `pass_through_calls.token_type`-kolom + waarom geen `partner Connection`-row in DB (D-06)."
    - "ADR Consequences-sectie noteert expliciet dat D-05 boot-time warning niet is geïmplementeerd in v0.3 en levert backlog-ID `MOLLIE-CONNECT-BOOT-WARN` voor latere DX-verfijning."
    - "ADR cross-refereert `.docs/decisions/mollie-passthrough-api.md` als baseline-pattern."
    - "ADR is lokaal opgeslagen in `.docs/decisions/` (gitignored werkdocument per CLAUDE.md werkdocument-conventie); traceability via tracked SUMMARY-files + STATE.md."
    - "STATE.md is bijgewerkt: Phase 13 marked Complete, Phase 13-mollie-connect-partner-resources in Shipped-section, MOLL-05 + MOLL-06 status `Complete` in Traceability-tabel."
    - "ROADMAP.md is bijgewerkt: Phase 13 marked als `Complete` met date 2026-05-18 (of de werkelijke completion-datum); Plans-section toont alle 4 plans afgevinkt."
    - "REQUIREMENTS.md is bijgewerkt: MOLL-05 + MOLL-06 staan `- [x]` (regels 29-30) en hun Traceability-tabel-rijen tonen `Complete` (regels 85-86)."
    - "Geen edit op `.docs/decisions/mollie-passthrough-api.md` zelf (D-13: NIET de baseline-ADR uitbreiden — eigen ADR voor Connect-keuze)."
  artifacts:
    - path: ".docs/decisions/mollie-connect-partner-resources.md"
      provides: "ADR voor Phase 13 token-resolver + route-prefix + env-token-bron"
      min_lines: 80
      contains: "MollieAccessTokenResolver"
    - path: ".planning/STATE.md"
      provides: "Phase 13 closure-status, MOLL-05/06 → Complete, current_focus → next phase"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 entry checkbox + completion-date + Plans-tabel update"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MOLL-05 + MOLL-06 als `- [x]` + Traceability-tabel-status `Complete`"
  key_links:
    - from: ".docs/decisions/mollie-connect-partner-resources.md"
      to: ".docs/decisions/mollie-passthrough-api.md"
      via: "cross-reference in Context-section"
      pattern: "mollie-passthrough-api"
    - from: ".planning/STATE.md"
      to: "Phase 13 SUMMARY-files"
      via: "Resolved Blockers-sectie of Shipped Phases-tabel"
      pattern: "13-mollie-connect-partner-resources"
    - from: ".planning/REQUIREMENTS.md"
      to: "MOLL-05 + MOLL-06 closure"
      via: "`- [x]`-checkbox + Traceability-status `Complete`"
      pattern: "^- \\[x\\] \\*\\*MOLL-0[56]"
---

<objective>
Sluit Phase 13 af met de architecturele beslissing-documentatie (ADR) en de planning-artefact-updates (STATE.md + ROADMAP.md + REQUIREMENTS.md). De ADR codificeert wat Phase 13 over Mollie Connect-partner-resources besloten heeft zodat toekomstige planners + verifiers het niet hoeven te reconstrueren uit de 3 SUMMARY-files. STATE.md + ROADMAP.md + REQUIREMENTS.md krijgen de standaard closure-updates: phase complete, requirements complete (MOLL-05 + MOLL-06 → `[x]` + `Complete`), current_focus naar volgende phase.

Purpose: Zonder ADR is de "waarom"-laag van Phase 13 enkel impliciet (in code + SUMMARY-files); volgende phases die Connect-resources uitbreiden of een tweede SDK Connect-pad opzetten zouden de keuzes opnieuw moeten afleiden. Zonder STATE/ROADMAP/REQUIREMENTS-update is Phase 13 niet zichtbaar als gesloten voor `/gsd-resume` of milestone-audit-tooling, en de phase-frontmatter-claim "MOLL-05 + MOLL-06 → Complete" zou intern tegenstrijdig zijn met REQUIREMENTS.md's `- [ ]` + `Pending`.

Output:
- 1 nieuwe ADR-file in `.docs/decisions/` (lokaal/gitignored; traceability via tracked SUMMARY + STATE.md).
- STATE.md updates: Current Position, Shipped sections, Traceability-velden.
- ROADMAP.md updates: Phase 13 checkbox + completion-line + Plans-tabel.
- REQUIREMENTS.md updates: MOLL-05 + MOLL-06 `[ ]` → `[x]` + Traceability-tabel `Pending` → `Complete`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md
@.planning/phases/13-mollie-connect-partner-resources/13-01-SUMMARY.md
@.planning/phases/13-mollie-connect-partner-resources/13-02-SUMMARY.md
@.planning/phases/13-mollie-connect-partner-resources/13-03-SUMMARY.md
@.docs/decisions/mollie-passthrough-api.md
@.docs/decisions/oauth-flow-registry.md
@.docs/decisions/feature-flags-pennant-kill-switch.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
<!-- ADR-format-templates uit bestaande .docs/decisions/-files -->

Voorbeeld van een recente ADR die deze plan als template gebruikt:
- `.docs/decisions/mollie-passthrough-api.md` (sections: Status, Keuze, Context, Alternatieven afgewogen, Consequences, Wanneer herzien)
- `.docs/decisions/feature-flags-pennant-kill-switch.md`
- `.docs/decisions/pass-through-calls-table.md`

Allen hebben dezelfde header-shape en sections-volgorde — copy/adapt voor consistentie.

STATE.md-mutations (zie bestaande shape):
- `current_focus` line update
- `Current Position` block update
- `Shipped Milestones` of `Shipped Phases` toevoeging (afhankelijk van of we per phase of per milestone shippen — in deze repo per phase binnen v0.3)
- `Accumulated Context > Decisions` mag een nieuwe regel krijgen voor Phase 13's belangrijkste keuze
- `Resolved Blockers` blijft onaangeroerd tenzij MOLL-05/06 daar als open blocker stond

ROADMAP.md-mutations:
- Phase 13 entry: `- [ ]` → `- [x]` + `Complete   | 2026-05-18` in de Progress-tabel
- Plans-tabel onder "Phase 13"-detail-section: alle 4 plans krijgen `[x]` checkboxes
- Eventueel completed_phases counter in frontmatter? Check huidige shape — STATE.md heeft die counter, ROADMAP doet alleen de Progress-tabel.

REQUIREMENTS.md-mutations (MANDATORY — BLOCKER-3-fix):
- Regels 29-30 (v0.3 v1 Requirements > Mollie Connect partner-resources): `- [ ] **MOLL-05**` → `- [x] **MOLL-05**` en idem voor MOLL-06.
- Regels 85-86 (Traceability-tabel): `| MOLL-05 | Phase 13 | Pending |` → `| MOLL-05 | Phase 13 | Complete |` en idem voor MOLL-06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schrijf ADR `.docs/decisions/mollie-connect-partner-resources.md`</name>
  <files>.docs/decisions/mollie-connect-partner-resources.md</files>
  <read_first>
    .docs/decisions/mollie-passthrough-api.md,
    .docs/decisions/feature-flags-pennant-kill-switch.md,
    .docs/decisions/pass-through-calls-table.md,
    .planning/phases/13-mollie-connect-partner-resources/13-CONTEXT.md,
    .planning/phases/13-mollie-connect-partner-resources/13-01-SUMMARY.md,
    .planning/phases/13-mollie-connect-partner-resources/13-02-SUMMARY.md,
    .planning/phases/13-mollie-connect-partner-resources/13-03-SUMMARY.md
  </read_first>
  <action>
1. Maak `.docs/decisions/mollie-connect-partner-resources.md` aan met deze sections (exact volgorde + heading-niveau zoals `mollie-passthrough-api.md`):

   **# Mollie Connect partner-resources — route-prefix bepaalt token-type**

   **## Status**
   - Gekozen 2026-05-18 (Phase 13).
   - Aangenomen in productie zodra een Consumer de eerste Connect-route belt; geen migratie-pad nodig (additive uitbreiding op Phase 5a).

   **## Keuze**
   - Vier expliciete keuzes oplijsten (één per paragraph of bullet):
     1. **Route-prefix is single source of truth voor token-type.** `/v1/mollie/connect/*` → partner-access-token; `/v1/mollie/*` → Connection-access-token. Geen per-route lookup-table.
     2. **`App\Mollie\MollieAccessTokenResolver`** met één methode `resolveFor(string $tokenType): string` waar `$tokenType ∈ {'partner','connection'}`. Singleton-bound in `AppServiceProvider`.
     3. **`MOLLIE_PARTNER_ACCESS_TOKEN` env-var** als bron voor partner-token; `config('services.mollie.partner_access_token')`. Fingerprint-only (`sha256[0..12]`) in audit-logs, nooit raw in DB of log-lines.
     4. **`pass_through_calls.token_type`-kolom** voor querybaarheid (admin wil "alle partner-calls vandaag" als filter); géén dedicated `Connection { purpose: 'partner' }`-row in DB voor v0.3.

   **## Context**
   - Eén Emeq-platform-token volstaat voor v0.3 (één partner-app in Mollie's portal).
   - Mollie Connect-partner-resources zijn semantisch verschillend van merchant-resources: ze acteren namens het platform (Emeq), niet namens een individuele merchant.
   - Phase 5a's `AbstractMolliePassThroughController` is verweven met `MollieConnectionContext` (per-Account-resolutie). Forceren dat dezelfde class beide token-typen kent zou óf een conditional in elke method introduceren, óf een super-class waarvan alleen 2 methods gedeeld zijn — beide premature abstracties.
   - Bestaande Mollie-SDK (`emeq/mollie-api`) ondersteunt `setAccessToken()` per-call. Connect-base bouwt verse `MollieApiClient` met partner-token via `app(MollieApiClient::class)` (container-resolved zodat tests via `$this->app->instance(...)` een stub kunnen injecteren); merchant-base haalt token via `HubMollieCredentialResolver` + lazy refresh.
   - Centrale exception-wrapping via `AbstractMollieConnectPassThroughController::dispatchMollieCall()` mapt raw `\Mollie\Api\Exceptions\ApiException` via `Emeq\MollieApi\Exceptions\MollieExceptionMapper::map()` naar de Hub-getypeerde exception-tree, waarna `MollieUpstreamErrorMapper` (Phase 5a) de juiste 502/422/404-branch raakt. Zonder die wrapper zou een raw 401 doorvallen naar `mollie_unknown`.

   **## Alternatieven afgewogen**

   Tabel met 4 rijen:

   | Optie | Waarom afgewezen |
   |---|---|
   | Per-Consumer partner-tokens in DB | Overkill bij één Emeq-platform; promoteren wanneer een tweede Consumer eigen Mollie-partner-account heeft. |
   | Generieke super-class voor merchant- én Connect-pad | Premature abstractie — 2 gedeelde serializer-methods rechtvaardigen geen abstract-explosion (D-03). Duplicatie chirurgisch geaccepteerd, traits-extractie pas bij ≥3 echt-gedeelde methods. |
   | `metadata`-jsonb-kolom op `pass_through_calls` ipv `token_type`-string-kolom | Querybaarheid (`WHERE token_type = 'partner'` is een snelle index-scan; `jsonb_extract` is duurder en niet-getypeerd in admin-filter-builders). |
   | Aparte Pennant feature-flag `provider:mollie-connect` | Connect-resources gaan met merchant-routes mee als Mollie-provider down is — geen scenario waar Connect-routes wel mogen en merchant-routes niet. Vereenvoudigt kill-switch-logica. |
   | Per-controller try/catch op `\Mollie\Api\Exceptions\ApiException` ipv centrale `dispatchMollieCall()` in base | DRY-overweging: 5x dezelfde try/catch zou betekenen dat een uitbreiding van de mapper-tree op 5 plekken aangepast moet worden. Centraal in de base = één plek om de exception-normalisatie te onderhouden. |

   **## Consequences**

   - **Toevoegen van een nieuwe Connect-resource = nieuwe route onder `/connect/` + nieuwe controller die `AbstractMollieConnectPassThroughController` extends.** Geen resolver-config-edit, geen ADR-edit.
   - **Een tweede SDK met Connect-equivalent (Stripe Connect, Adyen MarketPay) hoeft niet hetzelfde patroon te kopiëren** — dit ADR documenteert Mollie-specifieke keuzes; andere providers maken hun eigen ADR als hun OAuth/partner-model wezenlijk verschilt.
   - **Dev-omgevingen zonder partner-token mogen booten** (geen exception bij `php artisan serve`). Eerste call op `/v1/mollie/connect/*` faalt met 503 `partner_token_missing`. Dit is een Hub-side configuratie-fout (mapped naar 503, niet 500 of 502).
   - **D-05 boot-time warning bij missing partner-token NIET geïmplementeerd in v0.3.** De `MissingPartnerTokenException` → HTTP 503 bij eerste call is voldoende DX-signaal — een dev die een Connect-route hit krijgt een expliciete error-body terug. Een boot-time warning (b.v. via `php artisan about`-extensie of een log-line bij app-boot) is een nice-to-have die niet de scope-grens van Phase 13 rechtvaardigt. **Promote naar backlog-ID `MOLLIE-CONNECT-BOOT-WARN`** zodra dev-feedback aangeeft dat vroegere detectie nodig is (bv. CI-failures op forgotten env-vars).
   - **`MollieAccessTokenResolver` is bestaand-pad-kandidaat voor Phase-5a-refactor.** D-03 Claude's-Discretion-punt; backlog `MOLL-PT-RESOLVER-REFACTOR` als Phase 13 het niet meeneemt. (Verifiëer in Plan 13-04-execution of Plan 13-02 dit alsnog heeft aangedurfd; bij ja: update consequences om "AbstractMolliePassThroughController nu ook via resolver" te noemen.)

   **## Wanneer herzien**

   - Tweede Consumer wil eigen Mollie-Connect-partner-account → promote per-Consumer partner-tokens (uit "Deferred").
   - Mollie publiceert `client_credentials`-grant voor partner-token-rotatie → vervang statische env-var door dynamische flow.
   - Filament admin-paneel toevoeging vereist partner-token UI-rotatie → introduceer `Connection { purpose: 'partner' }`-row en migreer env-var-pad naar DB.
   - Meer dan 3 partner-resources delen exact dezelfde audit/header/error-mapping-logica én óók gelden voor merchant-resources → extract gedeelde trait, dan herevalueer of super-class alsnog rechtvaardigt.
   - Dev-feedback signaleert dat `MissingPartnerTokenException` te laat in dev-flow opduikt → activeer backlog `MOLLIE-CONNECT-BOOT-WARN` (boot-time warning + `php artisan about`-sectie).

   **## Cross-referenties**
   - Baseline pass-through-pattern: `.docs/decisions/mollie-passthrough-api.md`
   - Phase 5a substraat: `.planning/milestones/v0.2-phases/05a-mollie-sdk-resources-webhooks-pass-through-api/05a-CONTEXT.md`
   - Phase 4 OAuth-broker (merchant-pad): `.planning/milestones/v0.2-phases/04-mollie-connect-oauth-broker/04-CONTEXT.md`

2. ADR mag Nederlands zijn voor doc-prose (CLAUDE.md global rule: docs Nederlands, identifiers Engels).
3. Pint niet vereist voor .md-files.
  </action>
  <verify>
    <automated>test -f .docs/decisions/mollie-connect-partner-resources.md && grep -c "MollieAccessTokenResolver" .docs/decisions/mollie-connect-partner-resources.md | grep -v '^0$' && grep -c "mollie-passthrough-api" .docs/decisions/mollie-connect-partner-resources.md | grep -v '^0$' && grep -c "MOLLIE-CONNECT-BOOT-WARN" .docs/decisions/mollie-connect-partner-resources.md | grep -v '^0$'</automated>
  </verify>
  <done>
    - File bestaat met ≥80 regels.
    - Bevat alle 6 verplichte H2-sections (`grep -c '^## ' .docs/decisions/mollie-connect-partner-resources.md` ≥ 6).
    - Cross-ref naar `mollie-passthrough-api.md` aanwezig.
    - Bevat `MollieAccessTokenResolver` minstens 2x.
    - Bevat `pass_through_calls` minstens 1x.
    - Bevat `MOLLIE-CONNECT-BOOT-WARN` minstens 1x (D-05 backlog-promotie expliciet).
  </done>
</task>

<task type="auto">
  <name>Task 2: STATE.md + ROADMAP.md + REQUIREMENTS.md closure-updates</name>
  <files>.planning/STATE.md,.planning/ROADMAP.md,.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/13-mollie-connect-partner-resources/13-01-SUMMARY.md,
    .planning/phases/13-mollie-connect-partner-resources/13-02-SUMMARY.md,
    .planning/phases/13-mollie-connect-partner-resources/13-03-SUMMARY.md
  </read_first>
  <action>
1. `.planning/STATE.md` updates (read-then-increment — geen hardcoded transities):
   - Frontmatter: `last_updated` → ISO timestamp van uitvoeringsmoment. `last_activity` → datum (vandaag).
   - **Progress-counters: lees-dan-verhoog.** Lees huidige `progress.completed_phases` waarde, verhoog met 1. Lees huidige `progress.total_plans`, verhoog met 4 (Phase 13 levert 4 plans). Lees huidige `progress.completed_plans`, verhoog met 4. Herbereken `progress.percent` = `completed_phases / total_phases * 100` (afgerond, geen decimalen). Geen hardcoded "van X naar Y"-transitie — de huidige waarde kan al verschoven zijn door parallelle phase-completions.
   - `status` regel: `Idle — ready voor volgende phase`.
   - `stopped_at`: `Phase 13 complete — Mollie Connect partner-resources (4/4 plans, 9 routes, ADR lokaal)`.
   - `## Current Position` block: `Phase: 13 — Mollie Connect partner-resources (Complete YYYY-MM-DD, 4/4 plans)` + `Last activity: <date>`.
   - `## Accumulated Context > ### Decisions`: voeg één regel toe onder de bestaande Phase-11-regels: `Validated v0.3 (Phase 13):` of voeg een sub-bullet toe `- Route-prefix bepaalt Mollie-token-type (partner vs Connection) — ADR mollie-connect-partner-resources.md`.
   - `## Performance Metrics` tabel: voeg Phase 13 rij toe met plan-count + tasks-count + files-count (lees uit 13-01/02/03-SUMMARY.md).
   - `## Session Continuity`: `Last session: <iso>`, `Stopped at: Phase 13 complete — Mollie Connect partner-resources (4/4 plans, 9 routes onder /v1/mollie/connect/*, MOLL-05 + MOLL-06 → Complete, ADR lokaal)`. `Resume file: None`.
   - `Next action options` update: vervang door:
     1. `/gsd-execute-phase 12` — Snelstart productie-cert closeout (wacht op partner-respons, deadline 2026-05-26).
     2. `/gsd-execute-phase 15` — VERIFICATION.md backfill (parallel low-risk doc-track).
     3. `/gsd-execute-phase 14` — Naschool live E2E (geblokkeerd op Phase 12 closure voor productie-Snelstart-call).

2. `.planning/ROADMAP.md` updates:
   - `## Progress` tabel: vink Phase 13-rij om naar `Complete   | 2026-05-18`. Locate-strategie via `grep -n '| Phase 13' .planning/ROADMAP.md` voor exact regelnummer.
   - `### Phase 13: Mollie Connect partner-resources` block (regio rond regel 68): vink de 4 plan-vinkjes om van `[ ]` naar `[x]`. Locate-strategie: `grep -n '13-0[1-4]-PLAN' .planning/ROADMAP.md` (per verifier-Warning-10: exacte regelnummers via grep — geen prescriptieve numerieke transities). Voeg na `**Plans:** 4 plans` toe: ` (4/4 complete 2026-05-18)`.
   - Onderaan de file: vervang de bestaande "Roadmap last updated"-regel door `*Roadmap last updated: 2026-05-18 — Phase 13 (Mollie Connect partner-resources) complete: 4 plans, 9 routes, MOLL-05/06 → Complete.*`.

3. `.planning/REQUIREMENTS.md` updates (MANDATORY — BLOCKER-3-fix; was OPTIONEEL, niet meer):
   - Open `.planning/REQUIREMENTS.md`.
   - Flip checkboxen voor MOLL-05 + MOLL-06:
     - Regel 29: `- [ ] **MOLL-05**: ...` → `- [x] **MOLL-05**: ...`
     - Regel 30: `- [ ] **MOLL-06**: ...` → `- [x] **MOLL-06**: ...`
   - Update Traceability-tabel:
     - Regel 85: `| MOLL-05 | Phase 13 | Pending |` → `| MOLL-05 | Phase 13 | Complete |`
     - Regel 86: `| MOLL-06 | Phase 13 | Pending |` → `| MOLL-06 | Phase 13 | Complete |`
   - Geen "OPTIONEEL"-pad meer — als de execute-plan-tooling de file-modificatie blokkeert is dat een echte tooling-bug die als blocker wordt gerapporteerd (niet als TODO).

4. Geen pint nodig (alleen .md-files).
  </action>
  <verify>
    <automated>grep -c "Phase 13 complete" .planning/STATE.md | grep -v '^0$' && grep -c "13-04-PLAN.md" .planning/ROADMAP.md | grep -v '^0$' && grep -c "13-mollie-connect-partner-resources" .planning/STATE.md | grep -v '^0$' && [ "$(grep -c '^- \[x\] \*\*MOLL-0[56]' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c '| MOLL-0[56] | Phase 13 | Complete |' .planning/REQUIREMENTS.md)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \\[x\\] \\*\\*MOLL-0[56]" .planning/REQUIREMENTS.md` = 2 (beide flips uitgevoerd).
    - `grep -c "| MOLL-0[56] | Phase 13 | Complete |" .planning/REQUIREMENTS.md` = 2 (Traceability-tabel-status `Complete`).
    - `grep -c "^- \\[ \\] \\*\\*MOLL-0[56]" .planning/REQUIREMENTS.md` = 0 (geen restanten van `- [ ]`).
    - `grep -c "Pending" .planning/REQUIREMENTS.md` minder dan vóór de edit (specifiek: 2 minder — MOLL-05 + MOLL-06 zijn niet langer Pending).
  </acceptance_criteria>
  <done>
    - STATE.md `status` regel toont `Idle — ready voor volgende phase`.
    - STATE.md `Current Position` toont Phase 13 als Complete met datum.
    - STATE.md `progress.completed_phases` is +1 vs vorige waarde.
    - ROADMAP.md Progress-tabel toont Phase 13 als `Complete`.
    - ROADMAP.md Phase 13-block toont alle 4 plans met `[x]` checkboxen.
    - REQUIREMENTS.md regels 29-30 tonen `- [x]` voor MOLL-05 + MOLL-06.
    - REQUIREMENTS.md Traceability-tabel-rijen 85-86 tonen `Complete`.
    - `php artisan test --compact` exit 0 (sanity-regressie — geen doc-edit mag tests breken).
  </done>
</task>

</tasks>

<verification>
- ADR is lokaal (`.docs/decisions/`), niet getrackt door git (gitignored werkdocument), traceability via SUMMARY + STATE.md (CLAUDE.md werkdocument-conventie + .ai/rules).
- STATE.md frontmatter-counters consistent: completed_phases + total_plans + completed_plans + percent allen geüpdatet via read-then-increment (geen hardcoded transities).
- ROADMAP.md geen verloren entries — alleen Phase 13-rij + Plans-block aangepast; andere phases blijven onaangeroerd.
- REQUIREMENTS.md MOLL-05 + MOLL-06 beide flipped (checkbox + Traceability-tabel) — frontmatter-claim consistent met file-content.
- Geen edit op `.docs/decisions/mollie-passthrough-api.md` (D-13 verbiedt dat).
- Geen code-changes in dit plan; alleen docs + planning-artefacten.
</verification>

<success_criteria>
- ADR-file `.docs/decisions/mollie-connect-partner-resources.md` bestaat met alle 6 verplichte sections + cross-references + `MOLLIE-CONNECT-BOOT-WARN` backlog-mention.
- STATE.md + ROADMAP.md + REQUIREMENTS.md closure-updates aangebracht; counters consistent.
- `php artisan test --compact` blijft groen.
- Een nieuwe `/gsd-resume`-sessie ziet Phase 13 als Complete en stelt Phase 12 of 15 als next action voor.
</success_criteria>

<output>
Schrijf `.planning/phases/13-mollie-connect-partner-resources/13-04-SUMMARY.md` met:
- ADR-locatie + section-count + sleutel-keuzes-summary (4 keuzes uit Task 1) + expliciete vermelding dat D-05 → backlog `MOLLIE-CONNECT-BOOT-WARN`.
- STATE.md/ROADMAP.md/REQUIREMENTS.md diff-summary (oude waarde → nieuwe waarde per veld).
- REQUIREMENTS.md flip-bevestiging: regels 29/30 + Traceability-tabel rijen 85/86.
- Phase 13 closure-statement: 4 plans, X tasks totaal, Y nieuwe tests, 9 routes, MOLL-05 + MOLL-06 → Complete.
</output>
</output>
