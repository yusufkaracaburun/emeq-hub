---
phase: 07-account-level-subscriptions-use-case-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Billing/Account/SubscriptionStatus.php
  - app/Billing/Account/StateTransitions.php
  - app/Billing/Account/Exceptions/InvalidStateTransitionException.php
  - tests/Unit/Billing/Account/StateTransitionsTest.php
autonomous: true
requirements: ['SUB-02']
must_haves:
  truths:
    - "Legal state-transitions (D-04 matrix) gaan zonder exception door"
    - "Illegal state-transitions gooien `InvalidStateTransitionException` met `from`/`to`-properties leesbaar"
    - "Terminal states (`canceled`, `completed`, `unknown`) accepteren GEEN outbound transitions"
    - "`paused → active` is bidirectional toegestaan (D-04)"
  artifacts:
    - path: "app/Billing/Account/SubscriptionStatus.php"
      provides: "PHP-enum backed by string met 6 cases (pending/active/paused/canceled/completed/unknown)"
      contains: "enum SubscriptionStatus: string"
    - path: "app/Billing/Account/StateTransitions.php"
      provides: "Static `assertTransition(from, to)` helper"
      contains: "public static function assertTransition"
    - path: "app/Billing/Account/Exceptions/InvalidStateTransitionException.php"
      provides: "Exception met readonly `from`/`to` enum-properties"
      contains: "public readonly SubscriptionStatus \\$from"
  key_links:
    - from: "app/Billing/Account/StateTransitions.php"
      to: "app/Billing/Account/Exceptions/InvalidStateTransitionException.php"
      via: "throws-on-illegal-transition"
      pattern: "throw InvalidStateTransitionException::for"
---

<objective>
Lever de state-machine fundering voor de Account-subscription-laag. Drie nieuwe bestanden, alle from-scratch (geen analog in repo — `final class` + `public const`-pattern uit `TokenAbilities` is NIET passend hier; D-04 vraagt een echte backed PHP enum).

- `App\Billing\Account\SubscriptionStatus` — backed string enum met 6 cases (TitleCase keys per Laravel Boost PHP-rule)
- `App\Billing\Account\StateTransitions` — `final class` met `public static function assertTransition(SubscriptionStatus $from, SubscriptionStatus $to): void`
- `App\Billing\Account\Exceptions\InvalidStateTransitionException` — `RuntimeException` met `public readonly SubscriptionStatus $from`/`$to`-properties, NL-message-conventie (analog: `UnknownPlanException`)

Plan 07-01 (model) krijgt in plan 07-03 een aanvulling met `'status' => SubscriptionStatus::class`-cast — die binding gebeurt NIET hier (vermijdt circulaire dependency Wave 1↔Wave 2).

Purpose: Plan 07-03's `AccountSubscriptionManager` en plan 07-05's webhook-handlers leunen op deze helper. Het is dé centrale plek waar legality wordt afgedwongen — alle transitions in service- en webhook-laag gaan via `StateTransitions::assertTransition()`.

Output:
- 3 nieuwe bestanden onder `app/Billing/Account/`
- 1 unit-test met data-provider voor de volledige legal/illegal-matrix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-level-subscriptions-use-case-b/07-CONTEXT.md
@.planning/phases/07-account-level-subscriptions-use-case-b/07-PATTERNS.md
@app/Billing/Exceptions/UnknownPlanException.php
@app/Sanctum/TokenAbilities.php
@app/Support/Mollie/MollieUpstreamErrorMapper.php
@tests/Unit/Billing/PlanResolverTest.php

<interfaces>
<!-- Bestaande style-references -->

From app/Billing/Exceptions/UnknownPlanException.php:
- `final class … extends RuntimeException`
- NL-message-conventie: `'Onbekende plan-slug: "%s". Definieer in config/billing-plans.php.'`
- Static `forSlug(string)`-factory die `new self(...)` retourneert

From app/Support/Mollie/MollieUpstreamErrorMapper.php:
- `final class` + `public static function mapException(Throwable $exception): array`
- Patroon voor pure-functie-utility (geen DI, geen state)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubscriptionStatus enum + InvalidStateTransitionException + StateTransitions helper</name>
  <files>
    app/Billing/Account/SubscriptionStatus.php,
    app/Billing/Account/Exceptions/InvalidStateTransitionException.php,
    app/Billing/Account/StateTransitions.php
  </files>
  <read_first>
    - `app/Billing/Exceptions/UnknownPlanException.php` regels 1-19 — exception-shape, namespacing, NL-message, static factory
    - `app/Support/Mollie/MollieUpstreamErrorMapper.php` regels 1-30 — `final class`-style + static-helper-pattern
    - 07-CONTEXT.md §D-04 — state-machine tabel met 6 states + 9 legal-transitions
    - 07-CONTEXT.md §<specifics> "State-machine test-matrix" — exhaustive legal/illegal lijst
    - 07-PATTERNS.md regels 225-329 — file-shapes voor enum + exception + helper
  </read_first>
  <behavior>
    - Enum heeft 6 backed-string cases met TitleCase keys: `Pending='pending'`, `Active='active'`, `Paused='paused'`, `Canceled='canceled'`, `Completed='completed'`, `Unknown='unknown'`.
    - `StateTransitions::assertTransition(Pending, Active)` retourneert void (legal).
    - `StateTransitions::assertTransition(Canceled, Active)` gooit `InvalidStateTransitionException` met `$e->from === Canceled` + `$e->to === Active`.
    - `InvalidStateTransitionException::for(Pending, Paused)` retourneert een instance waar `$e->getMessage()` `'Ongeldige state-transition: pending → paused.'` bevat (NL-message + enum-`value` interpolatie).
  </behavior>
  <action>
    Maak `app/Billing/Account/SubscriptionStatus.php`:
    - Namespace `App\Billing\Account`
    - `declare(strict_types=1);` (matched Hub-conventie zoals `app/Billing/PlanResolver.php`)
    - `enum SubscriptionStatus: string` met 6 cases zoals in `<behavior>` Test 1
    - Geen extra methods — pure data-enum

    Maak `app/Billing/Account/Exceptions/InvalidStateTransitionException.php`:
    - Namespace `App\Billing\Account\Exceptions`
    - `declare(strict_types=1);`
    - `final class InvalidStateTransitionException extends \RuntimeException`
    - Constructor `public function __construct(public readonly SubscriptionStatus $from, public readonly SubscriptionStatus $to)` met `parent::__construct(sprintf('Ongeldige state-transition: %s → %s.', $from->value, $to->value));`
    - Static factory `public static function for(SubscriptionStatus $from, SubscriptionStatus $to): self { return new self($from, $to); }`

    Maak `app/Billing/Account/StateTransitions.php`:
    - Namespace `App\Billing\Account`
    - `declare(strict_types=1);`
    - `final class StateTransitions`
    - Private static const-array (of via private static method retourneerd) met de legal-pairs uit D-04 + §<specifics>:
      - `Pending → Active`
      - `Pending → Canceled`
      - `Active → Paused`
      - `Active → Canceled`
      - `Active → Completed`
      - `Active → Unknown`
      - `Paused → Active`
      - `Paused → Canceled`
      - `Paused → Unknown`
    - Method `public static function assertTransition(SubscriptionStatus $from, SubscriptionStatus $to): void` — als pair `[$from, $to]` NIET in legal-set staat, `throw InvalidStateTransitionException::for($from, $to);`. Self-transition (`$from === $to`) → no-op (geen exception; idempotency voor webhook-replay).
    - Eventuele tweede helper `public static function isLegal(SubscriptionStatus $from, SubscriptionStatus $to): bool` voor introspectie zonder exception (optioneel — alleen als unit-test 'm gebruikt).

    Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$k = \$app->make(Illuminate\Contracts\Console\Kernel::class); \$k->bootstrap(); use App\\Billing\\Account\\SubscriptionStatus; use App\\Billing\\Account\\StateTransitions; use App\\Billing\\Account\\Exceptions\\InvalidStateTransitionException; StateTransitions::assertTransition(SubscriptionStatus::Pending, SubscriptionStatus::Active); try { StateTransitions::assertTransition(SubscriptionStatus::Canceled, SubscriptionStatus::Active); echo 'FAIL'; } catch (InvalidStateTransitionException \$e) { echo (\$e->from === SubscriptionStatus::Canceled && \$e->to === SubscriptionStatus::Active) ? 'OK' : 'FAIL'; }"
    </automated>
  </verify>
  <done>
    - 3 nieuwe bestanden onder `app/Billing/Account/` + `Exceptions/`
    - Enum heeft 6 cases met juiste string-values
    - `assertTransition` gooit voor illegal, niet voor legal
    - `InvalidStateTransitionException::for()` factory werkt
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test voor exhaustive transition-matrix</name>
  <files>tests/Unit/Billing/Account/StateTransitionsTest.php</files>
  <read_first>
    - `tests/Unit/Billing/PlanResolverTest.php` regels 1-87 — Unit-test-shape, `expectException`-pattern, `setUp` met `parent::setUp()`
    - 07-CONTEXT.md §<specifics> "State-machine test-matrix" — exhaustive legal + illegal lijst
    - 07-PATTERNS.md regels 833-884 — data-provider-pattern voor matrix
  </read_first>
  <behavior>
    - Test `legal_transitions_do_not_throw` — data-provider met 9 legal-pairs (D-04 + §<specifics>), elk `assertTrue(true)` na call.
    - Test `illegal_transitions_throw_with_from_to_properties` — data-provider met illegal-pairs: `Canceled → Active`, `Completed → Pending`, `Unknown → Active`, `Pending → Paused`, `Pending → Completed`. Elke `expectException(InvalidStateTransitionException::class)`, en in een try/catch asserteren dat `$e->from`/`$e->to` overeenkomen met inputs.
    - Test `self_transition_is_noop` — `assertTransition(Active, Active)` gooit niet (idempotency-grond voor webhook-replay).
    - Test `terminal_states_block_all_outbound` — voor elke terminal state (`Canceled`, `Completed`, `Unknown`) en elke andere state als `$to`: assertException. Self-transition excluderen (zie noop-test).
  </behavior>
  <action>
    Maak via `php artisan make:test --phpunit --unit Billing/Account/StateTransitionsTest --no-interaction`. Schrijf 4 testmethods. Gebruik PHPUnit 12 `#[DataProvider('legalPairs')]`-attribute pattern (analog: `tests/Unit/Billing/PlanResolverTest.php` als referentie; als die geen data-providers gebruikt, kies dan inline-cases per legal/illegal-pair als aparte test of een loop in 1 test). Test runt zonder `RefreshDatabase` (pure logic). Run `php artisan test --compact --filter=StateTransitionsTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=StateTransitionsTest</automated>
  </verify>
  <done>
    - 4+ tests groen in `StateTransitionsTest`
    - Alle 9 legal-pairs uit §<specifics> getest
    - Alle 5+ illegal-pairs uit §<specifics> getest
    - Volledige suite geen regressie: `php artisan test --compact` exit 0
    - Pint clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-laag → State-machine | `AccountSubscriptionManager` (plan 07-03) en webhook-handlers (plan 07-05) leunen op `StateTransitions::assertTransition` om illegal state-flips af te dwingen. State-corruption hier propageert naar financial-state-bugs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | State-machine bypass door direct `$sub->status = 'active'` zonder transition-check | mitigate | `AccountSubscriptionManager` (plan 07-03) is single-entry-point voor state-flips en roept `StateTransitions::assertTransition` aan vóór elke `save()`. Geen public setter op model bypass't dit — controllers gebruiken altijd de manager. |
| T-07-02-02 | Elevation of Privilege | Terminal-state escape (`Canceled → Active`) waardoor gecancelde sub heractiveert | mitigate | Test `terminal_states_block_all_outbound` bewijst dat alle 3 terminal states geen outbound transitions hebben. |
| T-07-02-03 | Repudiation | Stille state-flip zonder spoor | accept (deferred) | D-22 stelt log-only voor v0.2 (geen state-history DB-tabel); manager logt `account_subscription.transition` (plan 07-03). Audit-tabel = Phase 9 feature-wens. |
</threat_model>

<verification>
1. `php artisan test --compact --filter=StateTransitionsTest` exit 0
2. `php artisan test --compact` exit 0 (geen regressie)
3. `grep -q 'enum SubscriptionStatus: string' app/Billing/Account/SubscriptionStatus.php` matches
4. `grep -q 'public readonly SubscriptionStatus' app/Billing/Account/Exceptions/InvalidStateTransitionException.php` matches
5. Pint clean
</verification>

<success_criteria>
- Enum, exception en helper bestaan op de juiste paden
- Volledige legal/illegal-matrix uit D-04 + §<specifics> is getest
- Plan 07-03 (manager) en plan 07-05 (webhook-handlers) kunnen `assertTransition` consumeren
- Self-transition is idempotent (geen exception — webhook-replay-safe)
</success_criteria>

<output>
Create `.planning/phases/07-account-level-subscriptions-use-case-b/07-02-SUMMARY.md` when done
</output>
