---
phase: 07-account-level-subscriptions-use-case-b
plan: 06
type: execute
wave: 4
depends_on: ['07-04', '07-05']
files_modified:
  - tests/Feature/Api/V1/AccountSubscriptions/CreateAccountSubscriptionTest.php
  - tests/Feature/Api/V1/AccountSubscriptions/CancelAccountSubscriptionTest.php
  - tests/Feature/Api/V1/AccountSubscriptions/PauseResumeAccountSubscriptionTest.php
  - tests/Feature/Api/V1/AccountSubscriptions/ListAccountSubscriptionsTest.php
  - tests/Feature/Api/V1/AccountSubscriptions/AccountSubscriptionWebhookFlowTest.php
  - tests/Feature/Api/V1/AccountSubscriptions/MollieAndAccountSubscriptionsCoexistenceTest.php
autonomous: true
requirements: ['SUB-02']
must_haves:
  truths:
    - "SC-1 bewezen: `POST /v1/account-subscriptions` met valid body + `mollie:write`-PAT → 201 + AccountSubscription-row + Mollie-create-call met juiste payload"
    - "SC-2 bewezen: `payment.failed` webhook met `details.failureReason='mandate_invalid'` + `subscriptionId='sub_x'` → matching AccountSubscription.status = paused, geen Mollie-cancel-call"
    - "SC-3 bewezen: Cross-Consumer GET/DELETE/POST .../{id}/pause op vreemde sub-id → 404 (D-12 not 403). Same-Consumer-other-Account mutate → 200 (per-Consumer scope, bewust gekozen — zie 07-08 ADR)."
    - "SC-4 happy-paths: create + cancel + webhook-update getest"
    - "SC-4 edge cases (3+): revoked mandate (mandate_invalid pause), failed retry (paid-after-failed → sub blijft active maar last_payment_status updated), deleted customer (Mollie GET 404 → state Unknown)"
    - "Phase 5a's `/v1/mollie/customers/{cust}/subscriptions/*` blijft pure pass-through naast `/v1/account-subscriptions/*` (D-30 coëxistentie)"
    - "Write-token-with-mollie-write-only kan read-routes aanroepen (CheckForAnyAbility OR-logic, D-10)"
  artifacts:
    - path: "tests/Feature/Api/V1/AccountSubscriptions/CreateAccountSubscriptionTest.php"
      provides: "SC-1 + ability-matrix + validation + cross-Consumer + idempotency"
      contains: "test_creates_account_subscription"
    - path: "tests/Feature/Api/V1/AccountSubscriptions/AccountSubscriptionWebhookFlowTest.php"
      provides: "SC-2 + SC-4 edge cases (deleted customer, paid-after-failed)"
      contains: "mandate_invalid"
    - path: "tests/Feature/Api/V1/AccountSubscriptions/MollieAndAccountSubscriptionsCoexistenceTest.php"
      provides: "D-30 invariant: beide routes naast elkaar werken"
      contains: "coexistence"
  key_links:
    - from: "AccountSubscriptionWebhookFlowTest"
      to: "MollieWebhookController (plan 07-05)"
      via: "POST /webhooks/mollie/{connection_id} met geldige HMAC"
      pattern: "POST.*/webhooks/mollie/"
    - from: "CreateAccountSubscriptionTest"
      to: "AccountSubscriptionController (plan 07-04)"
      via: "POST /v1/account-subscriptions"
      pattern: "post.*/v1/account-subscriptions"
---

<objective>
Lever de feature-test-suite die SC-1 t/m SC-4 bewijst voor Phase 7. 6 test-classes, samen ~27-32 testmethods. Geen integration-tests (komen in plan 07-07).

Test-classes:
1. **`CreateAccountSubscriptionTest`** — SC-1 + ability-matrix + Form-Request-validatie + cross-Consumer + Idempotency-Key
2. **`CancelAccountSubscriptionTest`** — happy + already-canceled-409 + cross-Consumer-404 + Mollie-upstream-error-mapping
3. **`PauseResumeAccountSubscriptionTest`** — happy paths + illegal-transition-409 + cross-Consumer-404 + same-Consumer-other-Account-200 (scope-niveau bewijs) + ability-gating
4. **`ListAccountSubscriptionsTest`** — filter op `account_external_id` + scope-isolation + lege-list voor missing-account (geen 404 leak) + write-token-can-read
5. **`AccountSubscriptionWebhookFlowTest`** — SC-2 (mandate_invalid → paused) + SC-4 edge cases (deleted customer 404 → unknown; paid-after-failed retry blijft active)
6. **`MollieAndAccountSubscriptionsCoexistenceTest`** — D-30: beide routes draaien naast elkaar in 1 request-cycle zonder credential-cross-contamination

Test-helpers: `StubsMollieClient`-trait (`tests/Concerns/StubsMollieClient.php`) levert `setupMollieConsumer()`, `bindMollieStubs()`, `makeSubscription()`, `makePayment()`, `MollieWebhookSignature::sign()`. Hergebruik 1:1 — geen nieuwe trait.

Purpose: D-32 acceptance-criteria 2/3/4 vereisen deze tests groen. Plan 07-08 (acceptance) verifieert volledige suite + Pint + Scramble.

Output:
- 6 nieuwe test-classes (~27-32 methods totaal)
- 0 source-code wijzigingen (alleen tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-level-subscriptions-use-case-b/07-CONTEXT.md
@.planning/phases/07-account-level-subscriptions-use-case-b/07-PATTERNS.md
@tests/Concerns/StubsMollieClient.php
@tests/Feature/Api/V1/Mollie/SubscriptionsTest.php
@tests/Feature/Api/V1/StoreAccountTest.php
@tests/Feature/Webhooks/MollieWebhookAntiSpoofingTest.php
@tests/Feature/Webhooks/MollieWebhookIngressTest.php
@app/Http/Controllers/Api/V1/AccountSubscriptions/AccountSubscriptionController.php
@app/Http/Controllers/Api/V1/AccountSubscriptions/PauseController.php
@app/Http/Controllers/Api/V1/AccountSubscriptions/ResumeController.php
@routes/api.php

<interfaces>
<!-- Test-helper-API's -->

From tests/Concerns/StubsMollieClient.php:
- `setupMollieConsumer(array $abilities = [TokenAbilities::MOLLIE_READ]): [Consumer, string $token, Account, Connection]`
- `bindMollieStubs(array $resourceMap): void` — `$resourceMap['subscriptions'] = fn(string $op, mixed $arg) => $resource`
- `makeSubscription(array $attrs): Subscription`
- `makePayment(array $attrs): Payment`
- `bindMollieClientThatThrows(\Throwable $e): void`

From plan 07-01:
- `AccountSubscription::factory()->forConnection($connection)->active()/paused()/pending()/canceled()->create([...])`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateAccountSubscriptionTest + CancelAccountSubscriptionTest + PauseResumeAccountSubscriptionTest + ListAccountSubscriptionsTest</name>
  <files>
    tests/Feature/Api/V1/AccountSubscriptions/CreateAccountSubscriptionTest.php,
    tests/Feature/Api/V1/AccountSubscriptions/CancelAccountSubscriptionTest.php,
    tests/Feature/Api/V1/AccountSubscriptions/PauseResumeAccountSubscriptionTest.php,
    tests/Feature/Api/V1/AccountSubscriptions/ListAccountSubscriptionsTest.php
  </files>
  <read_first>
    - `tests/Feature/Api/V1/Mollie/SubscriptionsTest.php` regels 29-57 — `setupMollieConsumer` + `bindMollieStubs(['subscriptions' => ...])` pattern
    - `tests/Feature/Api/V1/StoreAccountTest.php` regels 14-110 — ability-matrix-pattern (`consumerWithToken` met verschillende ability-arrays)
    - `tests/Feature/Api/V1/StoreConnectionTest.php` — cross-Consumer-404-pattern voor write-endpoint
    - 07-CONTEXT.md §D-08 (routes) + §D-09 (body) + §D-10 (abilities) + §D-12 (cross-Consumer 404 not 403) + §D-25 (test-cases)
    - 07-PATTERNS.md regels 988-1024 (HTTP-test-setup + ability-fail + cross-Consumer)
  </read_first>
  <behavior>
    **CreateAccountSubscriptionTest** (8 tests):
    - `happy_path_creates_subscription_and_returns_201_with_resource_shape` — valid body + `mollie:write`-PAT + Mollie-stub retourneert `sub_new`. Assert 201, `data.status === 'active'`, `data.mollie_subscription_id === 'sub_new'`, AccountSubscription-row bestaat met juiste account_id/connection_id.
    - `mollie_create_call_uses_correct_payload` — assert dat Mollie's `subscriptions->createForId('cst_abc', $body)` aangeroepen met juiste body (amount/interval/description/mandateId/times/metadata).
    - `read_only_token_returns_403_on_create` — PAT met alleen `mollie:read` → 403.
    - `missing_required_field_returns_422` — body zonder `amount.value` → 422 met validation-error.
    - `invalid_amount_format_returns_422` — `amount.value = '10'` (geen 2 decimals) → 422.
    - `account_external_id_of_other_consumer_returns_422` — Consumer A's PAT met Consumer B's `account_external_id` → 422 (Rule::exists faalt door consumer_id-where-clause; D-12 invariant — 422 niet 404 omdat Form Request vroeg faalt).
    - `mollie_validation_error_propagates_as_422_via_error_mapper` — Mollie-stub gooit `ApiException` met 422-status → controller mapt naar 422 via `MollieUpstreamErrorMapper`.
    - `idempotency_key_forwarded_to_mollie_client` — POST met header `Idempotency-Key: uuid-test`, Mollie-client-spy assert `setIdempotencyKey('uuid-test')` exact 1 keer aangeroepen.

    **CancelAccountSubscriptionTest** (4 tests):
    - `happy_path_cancels_active_subscription` — `$sub = ->active()` + Mollie-stub voor `subscriptions->cancelForId()` → DELETE → 204 + `$sub->fresh()->status === Canceled` + canceled_at set.
    - `already_canceled_returns_409_invalid_state_transition` — `$sub = ->canceled()` → DELETE → 409 met `error_code: invalid_state_transition`, `from: 'canceled'`, `to: 'canceled'`. *Let op*: `StateTransitions::assertTransition(Canceled, Canceled)` is self-transition (no-op per plan 07-02), dus dit test mag óf 204 retourneren (idempotent) óf 409. Implementatie-decision: controller `try { $manager->cancel($sub); } catch ...` — als self-transition no-op is, dan retourneert het 204. Adjust test naar verwacht gedrag: bij re-cancel op already-canceled → 204 + Mollie-cancel-call wordt overgeslagen (manager-guard).
    - `cross_consumer_destroy_returns_404` — Consumer A's PAT + sub van Consumer B → 404.
    - `read_only_token_returns_403_on_destroy` — `mollie:read`-only → 403.

    **PauseResumeAccountSubscriptionTest** (7 tests):
    - `pause_active_subscription_returns_200_and_paused_state` — `$sub = ->active()` → POST `.../pause` → 200 + `data.status === 'paused'`.
    - `pause_on_already_paused_is_idempotent_or_409` — implementatie-choice (zie cancel-test). Assert deterministic gedrag.
    - `resume_paused_subscription_returns_200_and_active_state` — `$sub = ->paused()` → POST `.../resume` → 200 + `data.status === 'active'`.
    - `resume_on_canceled_returns_409_invalid_state_transition` — terminal state.
    - `cross_consumer_pause_returns_404` — Consumer A's PAT + sub die bij een vreemde Consumer hoort → 404 (D-12).
    - **`pause_on_subscription_of_other_account_same_consumer_returns_200` (SC-3 mutate-isolation bewijs, optie B per-Consumer scope)** — Eén Consumer met TWEE Accounts (`Account-A` en `Account-B`) en twee Mollie-Connections, elk een eigen AccountSubscription (`sub-A` op Account-A, `sub-B` op Account-B). Consumer's PAT (`mollie:write,*`) wordt gebruikt om `POST /v1/account-subscriptions/{sub-B.id}/pause` aan te roepen. Verwacht 200 + `sub-B.fresh()->status === SubscriptionStatus::Paused`. Bewijs: scope-niveau van mutate-endpoints is **per-Consumer**, niet per-Account — bewust gekozen, matched de `findOwnedSubscription`-helper-implementatie van plan 07-04 en is gedocumenteerd in 07-08 ADR §"Scope-niveau van Account-Subscription-mutate-endpoints".
    - `read_only_token_returns_403_on_pause`.

    **ListAccountSubscriptionsTest** (6 tests):
    - `list_with_account_external_id_returns_only_own_account_subs` — 2 Accounts bij dezelfde Consumer, beide met 1 sub. GET `?account_external_id=school-a` → 1 result met `data[0].account_id === $accountA->id`.
    - `list_without_account_external_id_returns_422` — GET zonder query-string → 422 met `error_code: account_external_id_required`.
    - `list_with_other_consumer_account_external_id_returns_empty_list` — Consumer A query Consumer B's external_id → 200 + `data === []`.
    - `list_returns_canceled_and_active_subs_sorted_by_latest` — 3 subs (active/paused/canceled), order desc by created_at.
    - `read_only_token_can_list` — `mollie:read`-PAT → 200.
    - **`write_token_can_access_read_routes_returns_200_when_ability_includes_mollie_write` (LOW #6 — pin Sanctum OR-logic)** — PAT met ABLE `mollie:write` (zonder `mollie:read`) → GET `/v1/account-subscriptions?account_external_id=school-a` → 200. Bewijst CheckForAnyAbility (OR) gedrag op read-routes: write-tokens hebben impliciet read-toegang door de comma-list `ability:mollie:read,mollie:write,*`.
  </behavior>
  <action>
    Maak 4 test-files via `php artisan make:test --phpunit Api/V1/AccountSubscriptions/{Naam}Test --no-interaction`. Voor elk test-file:
    - `use RefreshDatabase;`
    - `use StubsMollieClient;`
    - `setupMollieConsumer([TokenAbilities::MOLLIE_WRITE])` in elke testmethod die het nodig heeft
    - Bearer-header via `$this->withHeader('Authorization', "Bearer {$token}")`
    - JSON-requests via `->postJson('/v1/account-subscriptions', $body)`, `->deleteJson('/v1/account-subscriptions/{$id}')` etc.
    - Cross-Consumer-tests: maak 2 Consumers met `Consumer::factory()->create()`, geef Consumer A een PAT en sub bij Consumer B.

    **Voor `pause_on_subscription_of_other_account_same_consumer_returns_200`:**
    - Setup helper:
      ```
      $consumer = Consumer::factory()->create();
      $accountA = Account::factory()->for($consumer)->create(['external_id' => 'school-a']);
      $accountB = Account::factory()->for($consumer)->create(['external_id' => 'school-b']);
      $connectionA = Connection::factory()->forMollie()->active()->for($accountA)->create();
      $connectionB = Connection::factory()->forMollie()->active()->for($accountB)->create();
      $subA = AccountSubscription::factory()->forConnection($connectionA)->active()->create(['mollie_subscription_id' => 'sub_A']);
      $subB = AccountSubscription::factory()->forConnection($connectionB)->active()->create(['mollie_subscription_id' => 'sub_B']);
      $token = $consumer->createToken('test-write', [TokenAbilities::MOLLIE_WRITE])->plainTextToken;
      ```
    - Call: `$this->withHeader('Authorization', "Bearer {$token}")->postJson("/v1/account-subscriptions/{$subB->id}/pause", ['reason' => 'manual_pause']);`
    - Assert: `$response->assertOk();` + `$subB->refresh()->status === SubscriptionStatus::Paused`
    - Comment in test: "Bewijst per-Consumer scope (optie B, gekozen 2026-05-15 — zie 07-08 ADR). Mutate op vreemde-Consumer sub blijft 404 (zie `cross_consumer_pause_returns_404`)."

    Run elke test-file apart na schrijven:
    - `php artisan test --compact --filter=CreateAccountSubscriptionTest`
    - `php artisan test --compact --filter=CancelAccountSubscriptionTest`
    - `php artisan test --compact --filter=PauseResumeAccountSubscriptionTest`
    - `php artisan test --compact --filter=ListAccountSubscriptionsTest`
  </action>
  <verify>
    <automated>php artisan test --compact --filter='CreateAccountSubscriptionTest|CancelAccountSubscriptionTest|PauseResumeAccountSubscriptionTest|ListAccountSubscriptionsTest'</automated>
  </verify>
  <done>
    - ~25 tests groen verspreid over 4 classes
    - SC-1 (Create happy) bewezen
    - SC-3 cross-Consumer-isolation bewezen op alle 4 endpoints (404 op vreemde-Consumer)
    - SC-3 mutate-isolation explicit gepinst: same-Consumer-other-Account mutate → 200 (per-Consumer scope, optie B)
    - Idempotency-Key forward bewezen op create
    - State-transition error → 409 bewezen
    - LOW #6: write-token-can-read bewezen op read-routes (CheckForAnyAbility OR)
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AccountSubscriptionWebhookFlowTest + MollieAndAccountSubscriptionsCoexistenceTest</name>
  <files>
    tests/Feature/Api/V1/AccountSubscriptions/AccountSubscriptionWebhookFlowTest.php,
    tests/Feature/Api/V1/AccountSubscriptions/MollieAndAccountSubscriptionsCoexistenceTest.php
  </files>
  <read_first>
    - `tests/Feature/Webhooks/MollieWebhookAntiSpoofingTest.php` regels 19-118 — HMAC-signature pattern + `Bus::fake()` voor fan-out-job-assert
    - `tests/Feature/Webhooks/MollieWebhookIngressTest.php` — happy-path webhook-flow
    - 07-CONTEXT.md §<specifics> "SC-2 webhook-test concreet" (regels 327-340) — exacte stub-payload + assertions
    - 07-CONTEXT.md §D-16 (mandate_invalid trigger) + §D-17 (404 → unknown) + §D-30 (coëxistentie) + §D-31 (5a regressie-vrij)
    - 07-PATTERNS.md regels 1026-1056 (webhook-flow-test pattern)
  </read_first>
  <behavior>
    **AccountSubscriptionWebhookFlowTest** (5 tests):
    - `payment_failed_with_mandate_invalid_transitions_subscription_to_paused` (SC-2):
      - Setup: `$sub = AccountSubscription::factory()->forConnection($connection)->active()->create(['mollie_subscription_id' => 'sub_123'])`
      - Stub `payments->get('tr_failed')` retourneert `makePayment(['id'=>'tr_failed', 'status'=>'failed', 'subscriptionId'=>'sub_123', 'details'=>(object)['failureReason'=>'mandate_invalid']])`
      - POST `/webhooks/mollie/{$connection->id}` met geldige `X-Mollie-Signature` (gebruik `MollieWebhookSignature::sign($body, $secret)`) en body `{"id":"tr_failed"}`
      - Assert response 202
      - Assert `$sub->fresh()->status === SubscriptionStatus::Paused`
      - Assert `$sub->paused_at !== null`
      - Assert `$sub->last_payment_status` matched `failed_mandate_invalid`
      - Assert NO `Mollie::subscriptions->cancelForId` aangeroepen (D-16: geen Mollie-cancel)
    - `subscription_webhook_with_sub_prefix_syncs_state_from_mollie` (SC-4 edge: deleted customer):
      - Setup: `$sub = ...->active()` met `mollie_subscription_id='sub_lost'`
      - Stub `subscriptions->getForId(...,$sub->mollie_subscription_id)` gooit `NotFoundException`
      - POST `/webhooks/mollie/{$connection->id}` met body `{"id":"sub_lost"}`
      - Assert response 202
      - Assert `$sub->fresh()->status === SubscriptionStatus::Unknown` (D-17)
    - `paid_payment_recurring_updates_last_payment_status_without_state_flip` (SC-4 edge: failed retry happy):
      - `$sub = ->active()` met `last_payment_status='failed_insufficient_funds'`
      - Stub retourneert payment `status='paid'`, `subscriptionId='sub_x'`
      - POST webhook → 202
      - Assert state blijft `Active`, `last_payment_status === 'paid'`, `last_webhook_event_at !== null`
    - `tampered_signature_returns_401_without_state_mutation` (D-31 regressie-vrij):
      - Setup `$sub = ->active()`
      - POST webhook met INVALID signature → 401 of 400 (matched Phase-5a-gedrag)
      - Assert `$sub->fresh()->status === SubscriptionStatus::Active` (state niet aangeraakt)
    - `unknown_subscription_id_with_sub_prefix_returns_202_no_state_mutation`:
      - GEEN AccountSubscription-rij voor `sub_orphan`
      - POST webhook `{"id":"sub_orphan"}` met geldige signature → 202 + result.skip (handler retourneert skip, controller geeft 202)

    **MollieAndAccountSubscriptionsCoexistenceTest** (3 tests, D-30):
    - `phase_5a_passthrough_subscription_create_still_works_after_phase_7_refactor`:
      - `setupMollieConsumer([TokenAbilities::MOLLIE_WRITE])`
      - POST `/v1/mollie/customers/cst_abc/subscriptions` met body uit Phase 5a → 201 + Mollie-pass-through-response (geen AccountSubscription-row gecreëerd; Phase 5a-route is puur pass-through).
      - Assert `AccountSubscription::count() === 0`.
    - `phase_7_create_and_phase_5a_create_can_coexist_in_same_request_cycle`:
      - Eerst POST `/v1/account-subscriptions` (Phase 7) → 201 + AccountSubscription-row
      - Direct erna POST `/v1/mollie/customers/cst_abc/subscriptions` (Phase 5a) → 201
      - Assert `AccountSubscription::count() === 1` (5a-call maakt geen extra Hub-row)
      - Assert beide Mollie-calls op de juiste credentials uitgevoerd (zelfde Connection — geen cross-contamination)
    - `phase_5a_webhook_payment_without_subscription_id_still_dispatches_fanout_job` (D-31):
      - `$sub` factory NIET nodig
      - Stub `payments->get('tr_oneshot')` retourneert payment ZONDER `subscriptionId`
      - POST `/webhooks/mollie/{$connection->id}` met `{"id":"tr_oneshot"}` + geldige signature
      - `Bus::fake()` → assert `ForwardMollieWebhookToConsumer::dispatched` (5a default-pad bewezen werkend na refactor)
  </behavior>
  <action>
    Maak 2 test-files. Beide met `use RefreshDatabase; use StubsMollieClient;`. Voor webhook-tests: `use Illuminate\Support\Facades\Bus; Bus::fake();` om fan-out-job-dispatches te kunnen asserten.

    Voor signature-creation gebruik `\Emeq\MollieApi\Webhooks\MollieWebhookSignature::sign($rawJsonBody, $secret)` (Phase 5a SDK-helper). Webhook-secret kun je via `config('services.mollie.webhook_secret', 'test-secret')` reachen; zorg dat test-env de juiste secret heeft (zie `MollieWebhookAntiSpoofingTest::setUp` voor pattern).

    Voor `Bus::assertNotDispatched` (anti-spoof-pad) en `Bus::assertDispatched` (happy + coëxistentie): direct na `Bus::fake()` opnemen.

    Run:
    - `php artisan test --compact --filter=AccountSubscriptionWebhookFlowTest`
    - `php artisan test --compact --filter=MollieAndAccountSubscriptionsCoexistenceTest`
    - `php artisan test --compact --filter='MollieWebhookIngressTest|MollieWebhookAntiSpoofingTest'` (D-31 regressie-check)
  </action>
  <verify>
    <automated>
    php artisan test --compact --filter='AccountSubscriptionWebhookFlowTest|MollieAndAccountSubscriptionsCoexistenceTest' &&
    php artisan test --compact --filter='MollieWebhookIngressTest|MollieWebhookAntiSpoofingTest'
    </automated>
  </verify>
  <done>
    - 8 tests groen verspreid over 2 classes
    - SC-2 bewezen (mandate_invalid → paused)
    - SC-4 edge cases gedekt (deleted customer → unknown; paid-after-failed; orphan-sub)
    - D-30 coëxistentie bewezen (Phase 5a routes + Phase 7 routes parallel)
    - D-31 invariant bewezen (Phase 5a webhook tests blijven groen na controller-refactor)
    - Volledige suite groen
    - Pint clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Hub-routes | Feature-tests beproeven dezelfde HTTP-laag als Consumers. Test-asserts moeten security-invariants bewijzen, niet alleen happy-paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Spoofing | Cross-Consumer-access-tests | mitigate | `cross_consumer_show_returns_404`, `cross_consumer_destroy_returns_404`, `cross_consumer_pause_returns_404`, `list_with_other_consumer_account_external_id_returns_empty_list` bewijzen D-12 invariant op alle 6 endpoints. Mutate-isolation explicit gepinst via `pause_on_subscription_of_other_account_same_consumer_returns_200` (per-Consumer scope, optie B). |
| T-07-06-02 | Elevation of Privilege | Ability-gating-tests | mitigate | `read_only_token_returns_403_on_*`-tests op alle write-routes bewijzen `ability:mollie:write,*`-middleware actief. `write_token_can_access_read_routes_returns_200_when_ability_includes_mollie_write` pin't OR-gedrag van read-routes. |
| T-07-06-03 | Tampering | Form-Request-bypass | mitigate | `missing_required_field_returns_422`, `invalid_amount_format_returns_422` bewijzen edge-validatie actief (D-09 regex). |
| T-07-06-04 | Spoofing | Webhook-tampered-signature | mitigate | `tampered_signature_returns_401_without_state_mutation` bewijst D-31 regressie-vrij + voorkomt financial-state-corruption via spoofed payload. |
| T-07-06-05 | Replay | Webhook-replay | mitigate | `unknown_subscription_id_with_sub_prefix_returns_202_no_state_mutation` bewijst dat onbekende `sub_*`-id skip retourneert (geen Mollie GET, geen state-mutatie). `idempotency_key_forwarded_to_mollie_client` bewijst dedup op create. |
| T-07-06-06 | Information Disclosure | Cross-Consumer info-leak | mitigate | `list_with_other_consumer_account_external_id_returns_empty_list` retourneert 200+`[]` (niet 404), zodat aanwezigheid-van-account niet via response-status lekt. |
</threat_model>

<verification>
1. `php artisan test --compact --filter='AccountSubscription'` exit 0 (~33 tests in dit plan)
2. `php artisan test --compact --filter='MollieWebhookIngressTest|MollieWebhookAntiSpoofingTest'` exit 0 (Phase 5a regressie-check)
3. `php artisan test --compact` exit 0 (volledige suite)
4. Pint clean
</verification>

<success_criteria>
- SC-1 + SC-2 + SC-3 + SC-4 happy + ≥3 edge cases bewezen via 33+ tests
- SC-3 mutate-isolation explicit gepinst (per-Consumer scope, optie B — 07-08 ADR documenteert keuze)
- D-30 (Phase 5a + Phase 7 coëxistentie) bewezen
- D-31 (Phase 5a regressie-vrij) bewezen
- LOW #6 Sanctum OR-gedrag bewezen op read-routes
- Plan 07-07 (integration-test) en plan 07-08 (acceptance) kunnen op deze suite leunen
</success_criteria>

<output>
Create `.planning/phases/07-account-level-subscriptions-use-case-b/07-06-SUMMARY.md` when done
</output>
