---
phase: "10"
plan: "04"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php
  - app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php
  - tests/Feature/Admin/WebhookCallResourceTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "WebhookCallsTable Consumer-kolom rendert via Eloquent-relatie `consumer.slug` — geen Consumer::find() meer"
    - "WebhookCallInfolist exception-veld wordt niet meer door json_encode() heen gehaald (plain text rendering)"
    - "WebhookCallInfolist Consumer-veld rendert via consumer.slug — geen Consumer::find() meer"
    - "Bestaande WebhookCallResourceTest 3 tests blijven groen na uitbreiding"
    - "Eager-load + $model-rebinding op WebhookCallResource zelf gebeurt in Plan 10-03 (file-conflict-vermijding); deze plan consumeert die change"
  artifacts:
    - path: "app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php"
      provides: "TextColumn::make('consumer.slug') vervangt inline Consumer::find()"
      contains: "TextColumn::make('consumer.slug')"
    - path: "app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php"
      provides: "exception-veld zonder json_encode + consumer.slug entry"
      contains: "TextEntry::make('exception')"
  key_links:
    - from: "app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php"
      to: "app/Models/WebhookCall.php (consumer() belongs-to uit 10-01)"
      via: "TextColumn::make('consumer.slug')"
      pattern: "consumer\\.slug"
---

<objective>
Sluit WR-02 (exception dubbel-encoded) + IN-01 deel-2 (per-row N+1 op Consumer-lookup in Tables/Infolist): wire `WebhookCallsTable` en `WebhookCallInfolist` op de `App\Models\WebhookCall::consumer()`-relatie uit Plan 10-01, en repareer de exception-rendering in de Infolist.

Purpose: Plan 10-01 levert de Hub-eigen subclass met `consumer()` belongs-to. Plan 10-03 herbindt `WebhookCallResource::$model` aan die subclass + voegt `getEloquentQuery()->with('consumer')` toe (file-conflict-vermijding tussen 10-03 en 10-04 in wave 2). Deze plan consumeert beide changes — vervangt de per-row `Consumer::find($record->consumer_id)?->slug` calls (regels 42-46 in Tables/WebhookCallsTable.php en regels 25-29 in Schemas/WebhookCallInfolist.php) door Eloquent-relatie-kolommen. Tegelijk vervangt de exception-rendering in de Infolist (regels 42-46) — `json_encode()` op een tekst-kolom levert escaped quotes + escaped newlines op — door Filament's default TextEntry-rendering.

Output: 2 gewijzigde files (Table + Infolist) + 1 uitgebreide test op de bestaande `WebhookCallResourceTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-REVIEW.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-CONTEXT.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-01-PLAN.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-03-PLAN.md

@app/Filament/Resources/WebhookCalls/WebhookCallResource.php
@app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php
@app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php
@tests/Feature/Admin/WebhookCallResourceTest.php
@app/Models/WebhookCall.php

<interfaces>
<!-- Na 10-01 + 10-03: App\Models\WebhookCall::consumer() is een BelongsTo(Consumer::class); -->
<!-- WebhookCallResource::$model wijst naar App\Models\WebhookCall; getEloquentQuery eager-loadt 'consumer'. -->

Huidige WebhookCallsTable.php:42-46:
```php
TextColumn::make('consumer_id')
    ->label('Consumer')
    ->state(fn ($record): string => $record->consumer_id
        ? (Consumer::find($record->consumer_id)?->slug ?? '—')
        : '—'),
```

Huidige WebhookCallInfolist.php:25-29:
```php
TextEntry::make('consumer_id')
    ->label('Consumer')
    ->state(fn ($record): string => $record->consumer_id
        ? (Consumer::find($record->consumer_id)?->slug ?? '—')
        : '—'),
```

Huidige WebhookCallInfolist.php:42-46 (exception bug):
```php
TextEntry::make('exception')
    ->label('Exception')
    ->state(fn ($record): string => $record->exception
        ? json_encode($record->exception, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES)
        : '—')
    ->columnSpanFull(),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebhookCallsTable consumer-relation column</name>
  <files>
    app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php
  </files>
  <read_first>
    - 09-REVIEW.md CR-02 + IN-01 (lines 130-175, 372-380) — fix-snippet voor consumer-relatie
    - 10-CONTEXT.md D-2 (lines 52-60) — WebhookCallResource modifyQueryUsing requirement (geïmplementeerd in 10-03)
    - 10-03-PLAN.md — bevestigt dat WebhookCallResource al naar App\Models\WebhookCall wijst en `->with('consumer')` eager-loadt
    - app/Models/WebhookCall.php (gecreëerd in 10-01) — consumer() belongs-to bestaat
    - app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php (alle 115 regels)
  </read_first>
  <behavior>
    - Test 1: Listing `/admin/webhook-calls` met 2 rijen + 1 Consumer triggert geen N+1 (totaal aantal queries < 5 voor 2 rijen-render dankzij 10-03's eager-load)
    - Test 2: WebhookCallsTable Consumer-kolom toont de slug van de related Consumer
    - Test 3: WebhookCallsTable Consumer-kolom toont '—' (of leeg) wanneer consumer_id NULL is
    - Test 4: Bestaande `test_list_shows_audit_rows_for_staff_user` blijft groen — slug is nog steeds zichtbaar
  </behavior>
  <action>
    In `WebhookCallsTable.php`:

    1. Vervang regels 42-46 (de `TextColumn::make('consumer_id')` met inline `state(fn ...)`) door één regel: `TextColumn::make('consumer.slug')->label('Consumer')->placeholder('—'),`. Behoud sortable/searchable indien aanwezig — match het bestaande gedrag.

    2. Behoud de `use App\Models\Consumer;`-import bovenaan (regel 7) — wordt nog gebruikt door de SelectFilter op regels 85-91 (`Consumer::query()->orderBy('slug')->pluck('slug','id')->all()` levert de filter-dropdown-opties op de listing-page). Die query gebeurt eenmaal, niet per-row, dus blijft acceptabel.

    3. Verifieer dat de SelectFilter op regel 85-91 nog werkt; geen wijziging nodig — die query is bedoeld (`distinct` consumer-list voor filter-dropdown).

    Géén wijziging aan `WebhookCallResource.php` zelf — die wordt al in 10-03 herbonden + krijgt daar `getEloquentQuery()->with('consumer')`. Hier alleen de Table-kolom.

    Geen tests modificeren nog — Task 3 handles dat.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallResourceTest</automated>
  </verify>
  <done>
    - `grep -c "Consumer::find" app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php` returnt `0`
    - `grep -c "TextColumn::make('consumer.slug')" app/Filament/Resources/WebhookCalls/Tables/WebhookCallsTable.php` returnt `1`
    - Bestaande WebhookCallResourceTest tests (3 tests) blijven groen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebhookCallInfolist consumer.slug + exception unwrap</name>
  <files>app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php</files>
  <read_first>
    - 09-REVIEW.md WR-02 (lines 218-244) — exacte fix-snippet voor exception-veld
    - 10-CONTEXT.md D-5 (lines 78-87) — locked decision exception unwrap
    - app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php (alle 54 regels)
  </read_first>
  <behavior>
    - Test 1: View-page `/admin/webhook-calls/{id}` rendert exception-veld als plain text wanneer record->exception is een string
    - Test 2: View-page toont '—' (placeholder) wanneer record->exception is NULL
    - Test 3: View-page toont consumer slug via consumer.slug relatie (niet via Consumer::find)
  </behavior>
  <action>
    In `WebhookCallInfolist.php`:

    1. Vervang regels 25-29 (de `TextEntry::make('consumer_id')` met inline state-callback) door:
    `TextEntry::make('consumer.slug')->label('Consumer')->placeholder('—'),`

    2. Vervang regels 42-46 (exception-veld met `json_encode`-bug) door:
    `TextEntry::make('exception')->label('Exception')->placeholder('—')->columnSpanFull(),`

    Filament's default TextEntry-rendering HTML-escaped de string en behoudt newlines. Geen `->markdown()` of `->html(false)` nodig — default is veilig. Géén `->copyable()` om de scope minimal te houden (kan in een toekomstige UX-pass komen).

    3. Verwijder `use App\Models\Consumer;`-import op regel 7 — wordt niet meer gebruikt (Infolist had geen SelectFilter, dus die import was alleen voor de inline state-callback).

    Behoud de `TextEntry::make('payload')` (regels 38-41) — die gebruikt `json_encode($record->payload ?? [], JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES)` en is correct: Spatie's `payload` is een echte JSON-cast (array via `$casts = ['payload' => 'array']` op parent class), dus `json_encode` levert echte pretty-printed JSON. Alleen `exception` is een string-kolom.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallResourceTest</automated>
  </verify>
  <done>
    - `grep -c 'json_encode' app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php` returnt `1` (alleen voor payload, niet meer voor exception)
    - `grep -c "TextEntry::make('exception')" app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php` returnt `1`
    - `grep -c "Consumer::find" app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php` returnt `0`
    - `grep -c "consumer.slug" app/Filament/Resources/WebhookCalls/Schemas/WebhookCallInfolist.php` returnt `1`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend WebhookCallResourceTest met consumer-relation + exception render tests</name>
  <files>tests/Feature/Admin/WebhookCallResourceTest.php</files>
  <read_first>
    - tests/Feature/Admin/WebhookCallResourceTest.php (alle 113 regels — bestaande tests blijven; uitbreiden)
    - 09-REVIEW.md WR-02 — verwacht gedrag voor exception-rendering
  </read_first>
  <action>
    Voeg drie nieuwe test-methodes toe aan bestaande `WebhookCallResourceTest`:

    1. `test_list_shows_consumer_slug_via_relation` — creëer `Consumer::factory()->create(['slug' => 'test-slug-xyz'])`, insert webhook_calls-row met `consumer_id = $consumer->id`, dan `Livewire::test(ListWebhookCalls::class)->assertCanSeeTableRecords(...)` + assert dat 'test-slug-xyz' in de HTML voorkomt via `assertSee('test-slug-xyz')` op de page-response.

    2. `test_view_page_renders_exception_as_plain_text_not_json_encoded` — insert row met `exception = "Stack trace line 1\nStack trace line 2"`, GET `/admin/webhook-calls/{id}`, `$response->assertSee('Stack trace line 1')` en `$response->assertDontSee('"Stack trace line 1\\nStack trace line 2"')` — de double-encoded JSON-vorm mag niet meer in de HTML voorkomen. (Filament html-escaped de string, dus 'Stack trace' staat er wel maar zonder omringende escaped quotes.)

    3. `test_view_page_shows_consumer_slug_via_relation` — analoog aan Test 1 maar voor view-page: GET `/admin/webhook-calls/{id}` en `$response->assertSee('test-slug-xyz')`.

    Behoud bestaande 3 tests onveranderd. Gebruik bestaande `insertWebhookCall()` helper en `actAsStaff()` helper. **Belangrijk:** `actAsStaff()` moet de `view-webhooks` permission krijgen — anders blokkeert Plan 10-03's `canAccess()` deze test. Pas `seedRoles()` helper aan: voeg `Permission::firstOrCreate(['name' => 'view-webhooks'])` toe en wijs die toe aan de `staff`-role. Of update `actAsStaff()` direct met `$user->givePermissionTo('view-webhooks')`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallResourceTest</automated>
  </verify>
  <done>
    - `grep -c 'public function test_' tests/Feature/Admin/WebhookCallResourceTest.php` returnt minimaal `6` (was 3)
    - `php artisan test --compact --filter=WebhookCallResourceTest` rapporteert minimaal `6 passed`
    - `grep -c 'view-webhooks' tests/Feature/Admin/WebhookCallResourceTest.php` returnt minimaal `1` (permission gewired)
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=WebhookCallResourceTest` → 6+ tests passed
- `php artisan test --compact tests/Feature/Admin/` → volledige admin-suite groen, geen regressie op andere Phase-9-tests
- `./vendor/bin/pint --dirty --format agent` levert geen fixes
</verification>

<success_criteria>
- WebhookCallsTable + WebhookCallInfolist gebruiken `consumer.slug`-relatie — geen `Consumer::find()` meer
- Infolist exception-veld is plain text — geen dubbel-encoded JSON
- 6+ tests groen; WR-02 + IN-01 deel-2 closed (model-rebinding + eager-load deel-1 zit in 10-03)
- Geen file-conflict met 10-03 in wave 2 — `WebhookCallResource.php` is uitsluitend in 10-03 aangeraakt
</success_criteria>

<output>
Create `.planning/phases/10-phase-9-polish-deferred-review-findings/10-04-SUMMARY.md` when done.
</output>
