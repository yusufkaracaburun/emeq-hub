---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/WebhookCall.php
  - app/Models/Consumer.php
  - config/webhook-client.php
  - tests/Feature/Models/WebhookCallConsumerRelationTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hub-eigen App\\Models\\WebhookCall extends Spatie\\WebhookClient\\Models\\WebhookCall bestaat"
    - "config/webhook-client.php webhook_model wijst naar App\\Models\\WebhookCall::class"
    - "App\\Models\\WebhookCall::consumer() returnt een BelongsTo naar Consumer"
    - "Bestaande Spatie-rijen (consumer_id NULL) blijven valide via nullable relation"
  artifacts:
    - path: "app/Models/WebhookCall.php"
      provides: "Hub-eigen WebhookCall met consumer() belongs-to"
      contains: "class WebhookCall extends \\Spatie\\WebhookClient\\Models\\WebhookCall"
    - path: "config/webhook-client.php"
      provides: "Model-binding override naar App\\Models\\WebhookCall"
      contains: "App\\Models\\WebhookCall::class"
  key_links:
    - from: "config/webhook-client.php"
      to: "App\\Models\\WebhookCall"
      via: "webhook_model config-key (Spatie reads this on incoming webhooks)"
      pattern: "'webhook_model' => App\\\\Models\\\\WebhookCall::class"
    - from: "App\\Models\\WebhookCall"
      to: "App\\Models\\Consumer"
      via: "belongsTo(Consumer::class) op consumer_id"
      pattern: "belongsTo\\(Consumer::class\\)"
---

<objective>
Wave 1 fundament voor de WebhookCallResource-polish: introduceer een Hub-eigen `App\Models\WebhookCall` subclass van Spatie's webhook-client model met een `consumer()` belongs-to relatie, en wire die in via `config/webhook-client.php` zodat Spatie's webhook-server/-client nieuwe rijen op deze subclass schrijft. Dit unlocked Plan 10-04 (WebhookCallsTable + WebhookCallInfolist `consumer.slug` eager-load) en sluit IN-01 (N+1) samen met CR-02 deel-2 (Hub-eigen model met relatie).

Purpose: Spatie's eigen `WebhookCall`-model heeft géén `consumer()` belongs-to. Plan 09-01 voegde `consumer_id` toe als losse `unsignedBigInteger`-kolom — relaties bestaan alleen op SQL-niveau, niet op Eloquent-niveau. Zonder Hub-eigen subclass kunnen we niet eager-loaden (`->with('consumer')`) en moeten we per-row `Consumer::find()` doen (huidige WebhookCallsTable.php:42-46 + WebhookCallInfolist.php:25-29).

Output: Nieuw `App\Models\WebhookCall`-bestand + 1-line wijziging in `config/webhook-client.php` + nieuwe feature-test die de relatie en model-binding bewijst.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-REVIEW.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-CONTEXT.md
@CLAUDE.md
@.ai/rules/engineering.md

@app/Models/Consumer.php
@config/webhook-client.php
@database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php

<interfaces>
<!-- Spatie webhook-client v3 model + Hub Consumer model -->

From vendor/spatie/laravel-webhook-client/src/Models/WebhookCall.php:
```php
namespace Spatie\WebhookClient\Models;
class WebhookCall extends Model {
    protected $guarded = [];
    public $casts = ['headers' => 'array', 'payload' => 'array', 'exception' => 'array'];
    // …
}
```

From app/Models/Consumer.php (read for type alignment):
```php
namespace App\Models;
class Consumer extends Authenticatable { /* hasMany Account; hasManyThrough Connection */ }
```

From database/migrations/2026_05_19_000001_add_audit_columns_to_webhook_calls_table.php:
- `consumer_id` is `unsignedBigInteger NULL` op `webhook_calls`
- FK naar `consumers.id` met `nullOnDelete` op Postgres (skipped op SQLite)
- Bestaande Spatie-rijen krijgen NULL — relatie MOET nullable zijn

Spatie config-key (config/webhook-client.php:49):
```php
'webhook_model' => Spatie\WebhookClient\Models\WebhookCall::class,
```
→ wordt overschreven naar `App\Models\WebhookCall::class`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create App\Models\WebhookCall + Consumer.webhookCalls() + config binding</name>
  <files>app/Models/WebhookCall.php, app/Models/Consumer.php, config/webhook-client.php</files>
  <read_first>
    - 09-REVIEW.md CR-02 (lines 130-175) — Hub-eigen WebhookCall + consumer() requirement
    - 10-CONTEXT.md D-2 (lines 52-60) — exacte file-paths en relatie-vorm
    - vendor/spatie/laravel-webhook-client/src/Models/WebhookCall.php — parent class signature
    - config/webhook-client.php line 49 — webhook_model config-key
    - app/Models/Consumer.php line 32-37 — bestaande accounts() / connections() relaties
  </read_first>
  <behavior>
    - Test 1: `App\Models\WebhookCall::class` extends `Spatie\WebhookClient\Models\WebhookCall::class`
    - Test 2: `$webhookCall->consumer()` returnt een `BelongsTo`-instance gericht op `App\Models\Consumer`
    - Test 3: `$webhookCall->consumer` is `null` wanneer `consumer_id` NULL is
    - Test 4: `$webhookCall->consumer` returnt de juiste Consumer wanneer `consumer_id` gevuld is
    - Test 5: `config('webhook-client.configs.0.webhook_model')` equals `App\Models\WebhookCall::class`
    - Test 6: `$consumer->webhookCalls()` returnt een `HasMany`-instance van `App\Models\WebhookCall`
  </behavior>
  <action>
    Maak `app/Models/WebhookCall.php` aan als final class (per IN-04 stijl in `ProviderCredentialDescriptor`) die `Spatie\WebhookClient\Models\WebhookCall` extends. Definieer één publieke `consumer(): BelongsTo` methode die `belongsTo(Consumer::class)` returnt — Eloquent inferenceert FK-name `consumer_id` automatisch op basis van methode-naam. Geen casts override; parent class behandelt JSON-casts al. Toevoegen `declare(strict_types=1)` per file-conventie van Hub-eigen models (zie `app/Support/ProviderCredentialDescriptor.php`). Geen `$table` override — Spatie's model wijst al naar `webhook_calls` via parent `$table = 'webhook_calls'`.

    Voeg in `app/Models/Consumer.php` een `webhookCalls(): HasMany` methode toe (parallel met bestaande `accounts(): HasMany` op regel 32) die `hasMany(WebhookCall::class)` returnt. Importeer `App\Models\WebhookCall` aan top of file.

    In `config/webhook-client.php`: vervang `Spatie\WebhookClient\Models\WebhookCall` import op regel 3 door `App\Models\WebhookCall` (én/of houd beide imports — config-bestand mag de `WebhookCall`-alias niet meer naar Spatie laten wijzen op regel 49). Zorg dat `'webhook_model' => App\Models\WebhookCall::class,` resoluteert naar de Hub-subclass.

    Note: Spatie's webhook-client v3 leest deze config bij elke incoming-webhook (zie `vendor/spatie/laravel-webhook-client/src/WebhookConfig.php`). Bestaande tests die `Spatie\WebhookClient\Models\WebhookCall` rechtstreeks gebruiken (zoals `WebhookCallResourceTest::test_list_shows_audit_rows_for_staff_user` die `WebhookCall::all()` via Spatie's class doet) blijven groen — Hub's subclass is volledig type-compatible. Implementeert D-2.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallConsumerRelationTest</automated>
  </verify>
  <done>
    - `app/Models/WebhookCall.php` exists with `class WebhookCall extends \Spatie\WebhookClient\Models\WebhookCall` + `public function consumer(): BelongsTo` body
    - `app/Models/Consumer.php` heeft `public function webhookCalls(): HasMany` methode
    - `config/webhook-client.php` regel 49 luidt `'webhook_model' => App\Models\WebhookCall::class,` (geen Spatie-class meer)
    - `grep -c 'class WebhookCall extends' app/Models/WebhookCall.php` returnt `1`
    - `grep -c 'App\\\\Models\\\\WebhookCall::class' config/webhook-client.php` returnt minimaal `1`
    - Volledige Phase-9 suite (390+ tests) blijft groen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write WebhookCallConsumerRelationTest</name>
  <files>tests/Feature/Models/WebhookCallConsumerRelationTest.php</files>
  <read_first>
    - app/Models/WebhookCall.php (gecreëerd in Task 1)
    - tests/Feature/Models/WebhookCallAuditColumnsTest.php — bestaand test-pattern voor `webhook_calls`-tabel-invoer via `DB::table`
    - tests/Feature/Admin/WebhookCallResourceTest.php regels 45-61 — `insertWebhookCall()` helper-pattern hergebruiken
  </read_first>
  <action>
    Maak `tests/Feature/Models/WebhookCallConsumerRelationTest.php` aan in PHPUnit-stijl (geen Pest — Hub gebruikt PHPUnit 12 per `composer.json`). Gebruik `RefreshDatabase`-trait. Drie testmethodes:

    1. `test_webhook_call_extends_spatie_class` — asserteert via `is_subclass_of(App\Models\WebhookCall::class, Spatie\WebhookClient\Models\WebhookCall::class)`.

    2. `test_consumer_relation_returns_null_when_consumer_id_is_null` — insert row via `DB::table('webhook_calls')->insertGetId([...])` met `consumer_id` NULL, dan `App\Models\WebhookCall::find($id)` → `assertNull($webhookCall->consumer)`.

    3. `test_consumer_relation_returns_consumer_when_consumer_id_is_set` — creëer `Consumer::factory()->create()`, insert row met `consumer_id` = $consumer->id, dan `App\Models\WebhookCall::find($id)->consumer` is een `Consumer`-instance met juiste id en slug.

    4. `test_consumer_has_many_webhook_calls_relation` — creëer Consumer + 2 rijen op `webhook_calls` met `consumer_id` = $consumer->id, dan `$consumer->webhookCalls()->count() === 2`.

    5. `test_webhook_model_config_resolves_to_hub_subclass` — asserteert `config('webhook-client.configs.0.webhook_model')` equals `App\Models\WebhookCall::class`.

    Volg de bestaande test-class-shape in `tests/Feature/Admin/WebhookCallResourceTest.php` (RefreshDatabase + Tests\TestCase + private insertWebhookCall-helper).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallConsumerRelationTest</automated>
  </verify>
  <done>
    - `tests/Feature/Models/WebhookCallConsumerRelationTest.php` exists with 5 test-methods
    - `php artisan test --compact --filter=WebhookCallConsumerRelationTest` reports `5 passed`
    - `grep -c 'public function test_' tests/Feature/Models/WebhookCallConsumerRelationTest.php` returnt `5`
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=WebhookCallConsumerRelationTest` → 5 passed
- `php artisan test --compact tests/Feature/` → existing 390+ tests blijven groen (Hub-subclass is type-compatible)
- `php artisan tinker --execute 'echo config("webhook-client.configs.0.webhook_model");'` toont `App\Models\WebhookCall`
- `./vendor/bin/pint --dirty --format agent` levert geen fixes (Pint-clean)
</verification>

<success_criteria>
- Hub-eigen `App\Models\WebhookCall extends Spatie\WebhookClient\Models\WebhookCall` bestaat
- `consumer()` BelongsTo + `Consumer::webhookCalls()` HasMany zijn beide opgezet
- `config/webhook-client.php` `webhook_model` wijst naar Hub-subclass
- 5 nieuwe tests groen; volledige suite regresseert niet
- 10-04 unlocked: WebhookCallsTable kan `TextColumn::make('consumer.slug')` doen + Resource kan `->with('consumer')` eager-loaden
</success_criteria>

<output>
Create `.planning/phases/10-phase-9-polish-deferred-review-findings/10-01-SUMMARY.md` when done — 1-pager met:
- Test-counts vóór + na
- Bevestiging dat geen Phase-9-test gebroken is (Spatie-subclass is volledig backward-compatible)
- Eventuele Pint-fixes
</output>
