---
phase: "10"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/ProviderCredentialDescriptor.php
  - app/Models/Connection.php
  - tests/Feature/Admin/ProviderDescriptorTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "ProviderCredentialDescriptor::tryFor(string): ?self bestaat en wraps for() met InvalidArgumentException-catch"
    - "Connection::fingerprint() gebruikt tryFor() en heeft geen inline try/catch meer"
    - "Bestaande ConnectionFingerprintTest blijft groen (gedrag identiek)"
  artifacts:
    - path: "app/Support/ProviderCredentialDescriptor.php"
      provides: "static tryFor() helper"
      contains: "public static function tryFor"
    - path: "app/Models/Connection.php"
      provides: "fingerprint() via tryFor — geen inline catch"
      contains: "ProviderCredentialDescriptor::tryFor"
  key_links:
    - from: "app/Models/Connection.php"
      to: "app/Support/ProviderCredentialDescriptor.php"
      via: "tryFor($this->provider)?->encryptedFields[0]"
      pattern: "ProviderCredentialDescriptor::tryFor"
---

<objective>
Sluit IN-04 (D-11): introduceer `ProviderCredentialDescriptor::tryFor(string): ?self` als expressievere variant van het inline `try { for() } catch (InvalidArgumentException) {…}`-pattern in `Connection::fingerprint()`. Pure refactor met gedragsbehoud — bestaande `ConnectionFingerprintTest` (4 tests) blijft groen zonder testfile-wijziging.

Purpose: De inline try/catch in `Connection::fingerprint()` (regels 48-65) is ad-hoc; een dedicated `tryFor()` helper op de descriptor-class is consistenter met het discovery-contract (`::for()` + `::all()`) en maakt callsites schoner.

Output: 1 nieuwe static-method op de descriptor + 1 callsite-refactor in `Connection::fingerprint()` + 1 nieuwe test op `ProviderDescriptorTest` voor de `tryFor()`-happy-path en unknown-provider-path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-REVIEW.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-CONTEXT.md

@app/Support/ProviderCredentialDescriptor.php
@app/Models/Connection.php
@tests/Feature/Admin/ProviderDescriptorTest.php
@tests/Feature/Admin/ConnectionFingerprintTest.php

<interfaces>
<!-- Current shape — extracted from app/Support/ProviderCredentialDescriptor.php -->

```php
final class ProviderCredentialDescriptor
{
    public function __construct(
        public readonly string $key,
        public readonly array $encryptedFields,
        public readonly string $primaryFingerprintLabel,
        public readonly ?string $oauthFlowKey,
    ) {}

    public static function for(string $provider): self  // throws InvalidArgumentException
    public static function all(): array  // returns list<self>
}
```

Current Connection::fingerprint() (app/Models/Connection.php:48-65):
```php
public function fingerprint(): ?string
{
    try {
        $descriptor = ProviderCredentialDescriptor::for($this->provider);
    } catch (InvalidArgumentException) {
        return null;
    }
    $primaryField = $descriptor->encryptedFields[0] ?? null;
    if (! $primaryField) { return null; }
    $secret = $this->{$primaryField};
    return $secret ? substr(hash('sha256', (string) $secret), 0, 12) : null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProviderCredentialDescriptor::tryFor() + Connection refactor</name>
  <files>app/Support/ProviderCredentialDescriptor.php, app/Models/Connection.php</files>
  <read_first>
    - 09-REVIEW.md IN-04 (lines 420-440) — exacte fix-snippet
    - 10-CONTEXT.md D-11 (lines 128-133) — locked decision
    - app/Support/ProviderCredentialDescriptor.php (volledig — 75 regels)
    - app/Models/Connection.php regels 48-65 — huidige inline try/catch in fingerprint()
    - tests/Feature/Admin/ConnectionFingerprintTest.php — bestaand gedrag-contract dat groen MOET blijven
  </read_first>
  <behavior>
    - Test 1: `ProviderCredentialDescriptor::tryFor('mollie')` returnt een `ProviderCredentialDescriptor`-instance met `key === 'mollie'`
    - Test 2: `ProviderCredentialDescriptor::tryFor('non-existent')` returnt `null` (geen exception)
    - Test 3: `ProviderCredentialDescriptor::tryFor('snelstart')->oauthFlowKey === null`
    - Test 4 (regressie): `Connection::factory()->forMollie()->create()->fingerprint()` returnt een 12-char-hex-string
    - Test 5 (regressie): `Connection::factory()->state(['provider' => 'unknown'])->create()->fingerprint()` returnt `null`
  </behavior>
  <action>
    Voeg `public static function tryFor(string $provider): ?self` toe aan `ProviderCredentialDescriptor` direct ná `public static function for()` (rond regel 60, vóór `public static function all()`). Body:

    Returnt `self::for($provider)` wrapped in try/catch op `InvalidArgumentException` — bij catch returnt `null`. Geen `Throwable` — alleen de exception die `for()` daadwerkelijk gooit (specifieker = veiliger refactor). Per D-11 locked decision.

    In `app/Models/Connection.php::fingerprint()` (regels 48-65): vervang het inline try/catch-block door één expressie `$descriptor = ProviderCredentialDescriptor::tryFor($this->provider);` gevolgd door `if ($descriptor === null) { return null; }`. Verwijder `use InvalidArgumentException;` op regel 13 (niet langer nodig). Rest van de methode (primary-field + hash-resolver) blijft ongewijzigd — pure refactor, geen gedragswijziging.

    Verifieer dat `ConnectionFingerprintTest`-bestaande 4 tests (test_list_page_html_contains_no_raw_credentials etc.) en `ProviderDescriptorTest::test_mollie_descriptor_resolves_from_config` + `test_unknown_provider_throws_invalid_argument_exception` allemaal blijven groen. De `for()`-method gooit nog steeds — `tryFor()` is alleen een wrapper.
  </action>
  <verify>
    <automated>php artisan test --compact --filter='ProviderDescriptorTest|ConnectionFingerprintTest'</automated>
  </verify>
  <done>
    - `grep -c 'public static function tryFor' app/Support/ProviderCredentialDescriptor.php` returnt `1`
    - `grep -c 'InvalidArgumentException' app/Models/Connection.php` returnt `0` (use-statement verwijderd)
    - `grep -c 'ProviderCredentialDescriptor::tryFor' app/Models/Connection.php` returnt `1`
    - `ConnectionFingerprintTest` 4 tests blijven groen (gedrag identiek)
    - `ProviderDescriptorTest` bestaande 4 tests blijven groen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ProviderDescriptorTest with tryFor coverage</name>
  <files>tests/Feature/Admin/ProviderDescriptorTest.php</files>
  <read_first>
    - tests/Feature/Admin/ProviderDescriptorTest.php (huidige 4 tests — file is bestaand)
    - app/Support/ProviderCredentialDescriptor.php (na Task 1 — bevat tryFor)
  </read_first>
  <action>
    Voeg twee nieuwe test-methodes toe aan de bestaande `ProviderDescriptorTest`-class:

    1. `test_try_for_returns_descriptor_for_known_provider` — `ProviderCredentialDescriptor::tryFor('mollie')` is een `ProviderCredentialDescriptor`-instance met `$descriptor->key === 'mollie'` en `$descriptor->oauthFlowKey === 'mollie'`.

    2. `test_try_for_returns_null_for_unknown_provider` — `ProviderCredentialDescriptor::tryFor('non-existent-provider-xyz')` is strict `=== null` (geen exception).

    Behoud bestaande test-class-shape (PHPUnit + Tests\TestCase + bestaande imports). Geen wijzigingen aan bestaande tests.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ProviderDescriptorTest</automated>
  </verify>
  <done>
    - `grep -c 'public function test_' tests/Feature/Admin/ProviderDescriptorTest.php` returnt minimaal `6` (was 4)
    - `php artisan test --compact --filter=ProviderDescriptorTest` rapporteert minimaal `6 passed`
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter='ProviderDescriptorTest|ConnectionFingerprintTest'` → alle tests passed
- `php artisan test --compact tests/Feature/` → volledige Phase-9 + nieuwe tests groen, geen regressie
- `./vendor/bin/pint --dirty --format agent` levert geen fixes
</verification>

<success_criteria>
- `tryFor()` static-method op `ProviderCredentialDescriptor` levert `?self` zonder exceptie
- `Connection::fingerprint()` heeft geen inline try/catch meer + geen `use InvalidArgumentException`
- 2 nieuwe tests; bestaande `ConnectionFingerprintTest` + `ProviderDescriptorTest` blijven groen
- D-11 closed
</success_criteria>

<output>
Create `.planning/phases/10-phase-9-polish-deferred-review-findings/10-02-SUMMARY.md` when done.
</output>
