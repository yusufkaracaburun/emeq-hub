---
phase: 01-snelstart-sdk-finalize
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - SNEL-02
must_haves:
  truths:
    - "De `fix/pest-crash-fase-4` feature-branch is gemerged in `main` van de SDK-repo (fast-forward of merge-commit)"
    - "`git log origin/main..HEAD` in `packages/snelstart-api/` retourneert leeg — alle local commits (fase-4, fase-5, fase-1-fix) staan op GitHub"
    - "`git branch -vv` toont `main` met upstream `[origin/main]`"
    - "Een verse `composer install` in een tijdelijke directory met VCS-repository-entry slaagt zonder authenticatie en levert `emeq/snelstart-api:dev-main`"
    - "De ge-installeerde package in de tijdelijke directory bevat `Emeq\\SnelstartApi\\Http\\SnelstartConnector` (autoload-check)"
  artifacts:
    - path: "/tmp/snelstart-vcs-smoke/composer.json"
      provides: "Smoke-test consumer composer.json met VCS-entry"
      contains: "emeq/snelstart-api"
    - path: "/tmp/snelstart-vcs-smoke/SMOKE-RESULT.txt"
      provides: "Bewijs dat composer install via VCS slaagt en de autoload werkt"
      contains: "EXIT: 0"
  key_links:
    - from: "packages/snelstart-api/main (local)"
      to: "github.com:yusufkaracaburun/emeq-snelstart-api main (remote)"
      via: "git push -u origin main"
      pattern: "origin/main"
    - from: "/tmp/snelstart-vcs-smoke/composer.json"
      to: "github.com:yusufkaracaburun/emeq-snelstart-api"
      via: "repositories[].type=vcs"
      pattern: "yusufkaracaburun/emeq-snelstart-api"
---

<objective>
Sluit fase 1 af: merge de feature-branch naar `main` in de SDK-repo, push met upstream-tracking, en bewijs dat de gepubliceerde SDK door een derde Laravel-project geinstalleerd kan worden via een VCS-repository-entry zonder authenticatie. Dit is een ops-plan met een human-checkpoint voor de push (per CLAUDE.md git-policy).

Purpose: Phase 2 (Mollie-SDK) gebruikt deze SDK als referentie-pattern, en phase 4 (Naschool) doet `composer require emeq/snelstart-api` vanuit een derde repo. Beide vereisen dat de SDK publiek op GitHub staat en feitelijk installeerbaar is. Een groene Pest-suite alleen is niet genoeg.

Output: Gepubliceerde SDK op GitHub met upstream-tracking + verifieerbare smoke-installatie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/rules/global.md
@.planning/phases/01-snelstart-sdk-finalize/01-02-SUMMARY.md

<interfaces>
<!-- Relevante git-state na plan 02. -->

Working repo: packages/snelstart-api/ (sub-repo, eigen git history)
Pre-plan-03 staat:
  - Branch `fix/pest-crash-fase-4` ahead van main met 2 commits (`fix(pest): …` en `test(connector): …`)
  - main HEAD = 76e0797 (lokaal, NIET gepusht — `git log origin/main..HEAD` toont vooraf alle commits sinds initial skeleton)
  - Origin remote: `git@github.com:yusufkaracaburun/emeq-snelstart-api.git` (configured, geen upstream-tracking)

Local commits die nog gepusht moeten worden (gecheckt tijdens planning):
  76e0797 feat(fase-5): OData query builder + RawSnelstartRequest
  669204d feat(fase-4): SnelstartConnector + error hierarchy
  62e2bf8 feat(fase-3): OAuth2 clientkey authenticator + token caching
  ee1b04b feat(fase-2): config defaults + container bindings + credential contracts
  ed121df chore: initial skeleton — emeq/snelstart-api Laravel package
  + de twee commits uit plan 02 op de feature-branch

Composer config in de SDK (`composer.json`):
  - name: `emeq/snelstart-api`
  - minimum-stability: `dev`, prefer-stable: true
  - homepage: `https://github.com/emeq/snelstart-api` (let op: dit is een gewenste-org-URL, echte repo is yusufkaracaburun/emeq-snelstart-api — niet wijzigen in deze fase)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User-approval — merge feature-branch, push naar GitHub, set upstream</name>
  <read_first>
    - packages/snelstart-api uit `git status`, `git branch -vv`, `git log --oneline origin/main..HEAD` (om te tonen wat gepusht wordt — moet `git fetch origin` eerst draaien)
    - CLAUDE.md (Git policy — nooit push zonder approval, nooit force-push, nooit --no-verify)
  </read_first>
  <what-built>
    Plan 02 heeft de Pest-suite groen gemaakt op branch `fix/pest-crash-fase-4`. Deze checkpoint vraagt user-approval om:
      1. De feature-branch te mergen in `main` (fast-forward indien mogelijk)
      2. `main` te pushen naar origin met `-u` (upstream-tracking instellen)
      3. De feature-branch lokaal op te ruimen na succesvolle merge

    Dit is een push-actie en per CLAUDE.md/global.md mag dat alleen met expliciete user-toestemming.
  </what-built>
  <how-to-verify>
    Lees de output van deze commando's (de executor draait ze en toont de output):

    ```
    cd packages/snelstart-api
    git fetch origin
    git status                                  # working tree clean?
    git branch -vv                              # huidige branch + tracking
    git log --oneline origin/main..HEAD          # wat wordt gepusht?
    ./vendor/bin/pest 2>&1 | tail -5            # Pest nog steeds groen?
    ```

    Verwachting:
      - Working tree clean
      - `git log origin/main..HEAD` toont 5 fase-commits + 2 plan-02-commits (`fix(pest):` en `test(connector):`)
      - Pest exit 0, ≥30 passed

    Als bovenstaande klopt: type `approved` of `merge+push+set-upstream`.
    Als iets afwijkt: beschrijf wat — de executor pauzeert.

    NB: de feature-branch heeft geen remote-counterpart (alleen lokaal aangemaakt in plan 01), dus alleen lokaal `git branch -d` na de merge.
  </how-to-verify>
  <resume-signal>
    Type "approved" om merge+push+upstream uit te voeren, OF beschrijf afwijkingen.
  </resume-signal>
  <files>
    - packages/snelstart-api (.git state only — geen tracked file changes in deze checkpoint)
  </files>
  <action>
    Geen automatisering in deze task. De executor draait de read-only commando's onder `<how-to-verify>` en presenteert de output. Daarna pauzeert het workflow op het `<resume-signal>`. Pas na expliciete "approved" gaat task 2 verder.
  </action>
  <verify>
    <automated>cd packages/snelstart-api &amp;&amp; git fetch origin &amp;&amp; [ -n "$(git log origin/main..HEAD 2>/dev/null)" ] &amp;&amp; ./vendor/bin/pest 2>&amp;1 | tail -5 | grep -qE "Tests:\s+([3-9][0-9]|[1-9][0-9]{2,}) passed"</automated>
  </verify>
  <done>
    User heeft "approved" of equivalent expliciet bevestigd; pre-flight (working tree clean + Pest groen + onpushed commits aanwezig) is gevisualiseerd; task 2 mag merge + push uitvoeren.
  </done>
</task>

<task type="auto">
  <name>Task 2: Voer merge + push + upstream-tracking uit</name>
  <read_first>
    - resume-signal van task 1 (moet "approved" zijn)
    - packages/snelstart-api/.git/config (verifieer remote 'origin')
  </read_first>
  <files>
    - packages/snelstart-api (.git state only — geen tracked file changes)
  </files>
  <action>
    Working repo: packages/snelstart-api/.

    1. **Pre-flight**:
       ```
       cd packages/snelstart-api
       git fetch origin
       git status                          # moet clean zijn
       ./vendor/bin/pest 2>&1 | tail -5    # nog steeds groen
       ```
       Als Pest rood is of working tree dirty: abort en rapporteer.

    2. **Merge feature -> main** (fast-forward; geen merge-commit nodig omdat main niet vooruit is):
       ```
       git checkout main
       git merge --ff-only fix/pest-crash-fase-4
       ```
       Als `--ff-only` faalt: rapporteer en stop (er is iets onverwachts op main veranderd). Geen `--no-ff` of `-X theirs` zonder nieuwe approval.

    3. **Push met upstream-tracking**:
       ```
       git push -u origin main
       ```
       Geen `--force`, geen `--no-verify`. Eerste push zet `[origin/main]` als upstream.

    4. **Verifieer upstream**:
       ```
       git branch -vv | grep -E '^\* main .*\[origin/main\]'
       git log origin/main..HEAD          # moet leeg zijn
       ```

    5. **Cleanup feature-branch** (lokaal alleen — never remote-pushed):
       ```
       git branch -d fix/pest-crash-fase-4
       ```
       `-d` (niet `-D`) — als hij niet gemerged blijkt: abort.

    6. **Verifieer remote zichtbaar**:
       ```
       gh repo view yusufkaracaburun/emeq-snelstart-api --json defaultBranchRef,visibility
       gh api repos/yusufkaracaburun/emeq-snelstart-api/commits/main --jq '.sha'
       ```
       De gerapporteerde sha moet matchen met lokaal `git rev-parse main`.

    Geen documentatie-updates in deze task — die staan eventueel in een vervolg-plan of in een fase-transitie via /gsd-transition.
  </action>
  <verify>
    <automated>cd packages/snelstart-api &amp;&amp; git fetch origin &amp;&amp; [ -z "$(git log origin/main..HEAD)" ] &amp;&amp; git branch -vv | grep -qE '^\* main .*\[origin/main' &amp;&amp; [ "$(git rev-parse main)" = "$(gh api repos/yusufkaracaburun/emeq-snelstart-api/commits/main --jq '.sha')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..HEAD` in `packages/snelstart-api/` retourneert leeg
    - `git branch -vv` toont `main` met `[origin/main]` tracking (`git branch -vv | grep -cE '^\* main .*\[origin/main'` == 1)
    - `gh api repos/yusufkaracaburun/emeq-snelstart-api/commits/main --jq '.sha'` matched `git rev-parse main` lokaal (bash test `[ "$(local_sha)" = "$(remote_sha)" ]` slaagt)
    - Branch `fix/pest-crash-fase-4` bestaat niet meer lokaal (`git branch --list fix/pest-crash-fase-4` retourneert leeg)
    - Geen `--force`, `--no-verify`, of `-X theirs` in `git reflog` van laatste 10 entries (`git reflog -10 | grep -cE '(force|no-verify|theirs)'` == 0)
  </acceptance_criteria>
  <done>
    SDK is publiek beschikbaar op `github.com/yusufkaracaburun/emeq-snelstart-api` met upstream-tracking; phase-2 referentie-pattern is consumeerbaar via VCS.
  </done>
</task>

<task type="auto">
  <name>Task 3: VCS smoke-test — composer install vanuit een fresh derde directory</name>
  <read_first>
    - packages/snelstart-api/composer.json (de package-naam is `emeq/snelstart-api`)
    - .claude/rules/global.md (geen verzonnen partner-features; geen secrets in commits)
  </read_first>
  <files>
    - /tmp/snelstart-vcs-smoke/ (nieuwe directory, buiten beide repos)
    - /tmp/snelstart-vcs-smoke/composer.json (new)
    - /tmp/snelstart-vcs-smoke/SMOKE-RESULT.txt (new — bewijslast)
  </files>
  <action>
    Werkt **buiten** beide repos om te bewijzen dat de SDK installeerbaar is zonder workspace-specifieke configuratie.

    1. **Maak fresh dir + minimal consumer composer.json**:
       ```
       rm -rf /tmp/snelstart-vcs-smoke
       mkdir -p /tmp/snelstart-vcs-smoke
       cd /tmp/snelstart-vcs-smoke
       ```

       Schrijf `/tmp/snelstart-vcs-smoke/composer.json` (gebruik de Write-tool):
       ```json
       {
           "name": "smoke/snelstart-consumer",
           "minimum-stability": "dev",
           "prefer-stable": true,
           "repositories": [
               {
                   "type": "vcs",
                   "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git"
               }
           ],
           "require": {
               "emeq/snelstart-api": "dev-main"
           }
       }
       ```

       NB: HTTPS-URL (niet SSH) — een derde dev-shop heeft geen SSH-key voor deze repo. De repo is publiek, dus HTTPS zonder auth moet werken.

    2. **Run composer install** (niet `require` — composer.json is al klaar):
       ```
       cd /tmp/snelstart-vcs-smoke
       composer install --no-interaction --prefer-dist 2>&1 | tee SMOKE-RESULT.txt
       echo "---EXIT: $?---" >> SMOKE-RESULT.txt
       ```

       Verwacht: exit 0, `Installing emeq/snelstart-api (dev-main <sha>)` in output, geen authenticatie-prompt.

    3. **Verifieer autoload werkt**:
       ```
       cd /tmp/snelstart-vcs-smoke
       php -r 'require "vendor/autoload.php"; echo class_exists("Emeq\\\\SnelstartApi\\\\Http\\\\SnelstartConnector") ? "OK\n" : "FAIL\n";' >> SMOKE-RESULT.txt
       php -r 'require "vendor/autoload.php"; echo class_exists("Emeq\\\\SnelstartApi\\\\OData\\\\QueryBuilder") ? "OK\n" : "FAIL\n";' >> SMOKE-RESULT.txt
       ```

       Beide regels moeten `OK` printen (twee `OK`-lijnen in SMOKE-RESULT.txt).

    4. **Verifieer commit-sha matched**:
       ```
       cd /tmp/snelstart-vcs-smoke
       echo "vendor-sha: $(cd vendor/emeq/snelstart-api && git rev-parse HEAD)" >> SMOKE-RESULT.txt
       echo "local-sha:  $(cd /Users/yusufkaracaburun/Sites/localhost/emeq-hub/packages/snelstart-api && git rev-parse main)" >> SMOKE-RESULT.txt
       ```

       Beide sha's moeten matchen. Acceptance grep'ed hierop.

    5. **Geen wijzigingen aan de SDK-repo** in deze task. Smoke-test is read-only consumer-perspectief.

    6. **Cleanup**: laat `/tmp/snelstart-vcs-smoke/` staan tot de SUMMARY geschreven is — handig voor visuele inspectie. Geen aparte cleanup-task; `/tmp` wordt door macOS zelf opgeruimd.
  </action>
  <verify>
    <automated>cd /tmp/snelstart-vcs-smoke &amp;&amp; grep -q "Installing emeq/snelstart-api" SMOKE-RESULT.txt &amp;&amp; grep -q "EXIT: 0" SMOKE-RESULT.txt &amp;&amp; [ "$(grep -c '^OK$' SMOKE-RESULT.txt)" -ge 2 ] &amp;&amp; test -f vendor/emeq/snelstart-api/src/Http/SnelstartConnector.php &amp;&amp; test -f vendor/emeq/snelstart-api/src/OData/QueryBuilder.php &amp;&amp; [ "$(grep '^vendor-sha:' SMOKE-RESULT.txt | awk '{print $2}')" = "$(grep '^local-sha:' SMOKE-RESULT.txt | awk '{print $2}')" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `/tmp/snelstart-vcs-smoke/composer.json` bevat de string `"emeq/snelstart-api"` en `"type": "vcs"` (grep beide)
    - File `/tmp/snelstart-vcs-smoke/SMOKE-RESULT.txt` bevat `Installing emeq/snelstart-api` (composer-output bewijs)
    - File bevat `EXIT: 0` marker (composer install slaagde)
    - File bevat minstens 2 regels exact gelijk aan `OK` (twee autoload-class-checks geslaagd; `grep -c '^OK$'` ≥ 2)
    - File `/tmp/snelstart-vcs-smoke/vendor/emeq/snelstart-api/src/Http/SnelstartConnector.php` bestaat
    - File `/tmp/snelstart-vcs-smoke/vendor/emeq/snelstart-api/src/OData/QueryBuilder.php` bestaat
    - `vendor-sha:`-regel en `local-sha:`-regel in SMOKE-RESULT.txt zijn identiek (zelfde HEAD)
    - Geen authenticatie-prompt of credentials-helper-vraag in de composer-output (`grep -ciE '(authentication|GitHub token|credentials)' SMOKE-RESULT.txt` == 0)
  </acceptance_criteria>
  <done>
    Een verse derde directory met alleen een VCS-repository-entry kan de SDK ophalen en autoloaden. Phase 4 (Naschool wiring) kan deze SDK consumeren met exact dezelfde composer-configuratie.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → GitHub (push) | Een push exposeert alle commit-history publiekelijk |
| GitHub repo → derde consumer | Een derde dev-shop installeert ongeauthenticeerd onze code via VCS |
| /tmp/snelstart-vcs-smoke → user shell | Smoke-output kan paden of git-config-details bevatten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | I (Information disclosure) | Push exposeert per ongeluk een `.env` of test-credentials in commit-history | mitigate | Plan 01 heeft `.planning-scratch/` aan `.gitignore` toegevoegd; de SDK-repo heeft geen `.env`-tracked. Verifieer pre-push met `git ls-files | grep -iE '(\.env$|\.env\.local|credentials)'` — moet leeg zijn. Pre-flight in task 2 voegt deze check niet expliciet toe; vertrouwt op het feit dat de skeleton hem niet bevat (gechecked: `composer.json` toont geen env-templates) |
| T-01-03-02 | I | Public repo lekt private `composer.json` repositories-entries (bv. naar private packagist) | accept | `composer.json` gelezen tijdens planning: geen `repositories`-blok, alleen require + autoload. Geen actie nodig |
| T-01-03-03 | T (Tampering) | Force-push wist commit-history voor andere consumers (Mollie-pattern reference) | mitigate | Action expliciet: geen `--force`, geen `--force-with-lease`. `git reflog` post-push acceptance check |
| T-01-03-04 | E (Elevation of privilege) | Smoke-test draait `composer install` met externe code-execution-mogelijkheid via `post-autoload-dump` script | accept | composer.json bevat `"post-autoload-dump": "@composer run prepare"` -> `testbench package:discover` — eigen code, geen externe scripts. Verifieer dat de smoke-output deze stap toont en niet onverwacht naar derden gaat |
| T-01-03-05 | I | SMOKE-RESULT.txt belandt in een gecommit pad | mitigate | File leeft in `/tmp/` buiten beide repos; niets te committen vanuit `/tmp` |
</threat_model>

<verification>
- `<automated>` blokken in beide auto-tasks (git log empty + upstream tracking voor task 2; composer install + autoload voor task 3).
- Visuele review: na task 3 toont `/tmp/snelstart-vcs-smoke/SMOKE-RESULT.txt` de full composer-output; user kan inspecteren of er onverwachte warnings staan.
- Roadmap success criteria 2-4 zijn allemaal gedekt:
    - SC2 (`git log origin/main..HEAD` leeg): acceptance task 2
    - SC3 (`git branch -vv` toont upstream): acceptance task 2
    - SC4 (composer require werkt vanuit derde project): acceptance task 3
</verification>

<success_criteria>
- SNEL-02 success criteria 2, 3, 4 alle drie verifieerbaar gehaald
- Pest blijft groen (geen wijzigingen aan src/ of tests/ in deze plan)
- Geen force-push, geen `--no-verify`, geen scope-uitbreiding buiten de drie taken
- Phase 1 is afgerond; phase 2 (Mollie-SDK) kan starten
</success_criteria>

<output>
After completion, create `.planning/phases/01-snelstart-sdk-finalize/01-03-SUMMARY.md` met:
- Push-resultaat: aantal commits gepusht, sha-van-main, branch-cleanup gedaan
- Smoke-test-resultaat: composer install duur, sha-match, autoload-checks
- Eventuele afwijking (bv. force-push nodig geweest — als dat zo is, document waarom + dat de user erom gevraagd heeft)
- Phase 1 succescriteria-tabel (1-4) met groen/rood per item
</output>
