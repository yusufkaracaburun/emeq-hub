---
phase: 01-snelstart-sdk-finalize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt
autonomous: true
requirements:
  - SNEL-01
must_haves:
  truths:
    - "Een feature-branch `fix/pest-crash-fase-4` bestaat in de SDK-repo, afgesplitst van `main` op commit 76e0797"
    - "Een raw capture van `./vendor/bin/pest` output staat in packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt en bevat het exit-code-cijfer plus de eerste falende test"
    - "De root-cause is benoemd in datzelfde bestand onder een `## Root cause` header (één regel — welk bestand/regel, welke aanname klopte niet)"
  artifacts:
    - path: "packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt"
      provides: "Diagnostic output + root-cause hypothese"
      contains: "## Root cause"
  key_links:
    - from: "packages/snelstart-api (feature-branch)"
      to: "packages/snelstart-api (main @ 76e0797)"
      via: "git checkout -b fix/pest-crash-fase-4"
      pattern: "fix/pest-crash-fase-4"
---

<objective>
Reproduceer de Snelstart-SDK Pest-crash op een feature-branch en leg de root-cause vast. Dit is puur diagnostiek — geen code-fixes in deze plan. Output: een feature-branch, een ruwe crash-capture en een one-liner root-cause-hypothese die plan 02 stuurt.

Purpose: De user heeft expliciet gevraagd om de crash tijdens executie te analyseren (niet upfront). Plan 02 baseert zijn aanpak op de capture uit deze plan; zonder concrete output is plan 02 blind.

Output: `packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt` met de Pest-output, de PHP-versie, het Saloon-versie-pinning en een korte root-cause-hypothese. Plus een fresh feature-branch klaar voor plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.claude/rules/global.md
@.claude/rules/engineering.md

<interfaces>
<!-- Bestanden die plan 02 zal aanpakken — verzameld zodat de diagnose meteen
     in context staat zonder dat de executor opnieuw moet zoeken. -->

Working repo: packages/snelstart-api/ (sub-repo, eigen git history)
Current branch: main, HEAD = 76e0797 ("feat(fase-5): OData query builder + RawSnelstartRequest")
Origin: git@github.com:yusufkaracaburun/emeq-snelstart-api.git (geen upstream-tracking)

Verdachte file (1 zwakke test, vermoedelijke crash-bron):
  packages/snelstart-api/tests/Unit/Http/SnelstartConnectorTest.php

Connector + error-mapping (subject under test):
  packages/snelstart-api/src/Http/SnelstartConnector.php
  packages/snelstart-api/src/Exceptions/SnelstartException.php
  packages/snelstart-api/src/Exceptions/ValidationException.php
  packages/snelstart-api/src/Exceptions/AuthenticationException.php
  packages/snelstart-api/src/Exceptions/RateLimitException.php
  packages/snelstart-api/src/Exceptions/NotFoundException.php
  packages/snelstart-api/src/Exceptions/ServerException.php

Pest config: packages/snelstart-api/phpunit.xml.dist (failOnRisky=true, failOnWarning=true, beStrictAboutOutputDuringTests=true — een echoende test crasht hier)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch off + reproduceer crash + leg root-cause vast</name>
  <read_first>
    - packages/snelstart-api/tests/Unit/Http/SnelstartConnectorTest.php (huidige inhoud — 1 zwakke test)
    - packages/snelstart-api/src/Http/SnelstartConnector.php (de class onder test, met name getRequestException + handleRetry)
    - packages/snelstart-api/phpunit.xml.dist (strict flags die crashes amplificeren)
    - packages/snelstart-api/composer.json (Saloon v3 + Testbench versies)
  </read_first>
  <files>
    - packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt (new)
    - .gitignore in packages/snelstart-api (verifieer dat .planning-scratch/ genegeerd is; voeg toe als ontbrekend)
  </files>
  <action>
    Working repo: packages/snelstart-api/

    1. Branch off:
       `cd packages/snelstart-api && git checkout -b fix/pest-crash-fase-4`
       Bevestig: `git rev-parse HEAD` = 76e0797 (start-commit van main).

    2. Verifieer dat .planning-scratch/ in .gitignore staat in de SDK-repo. Zo niet, voeg toe (één regel: `.planning-scratch/`) — dit zijn diagnostische scratchpads die niet in de SDK-repo thuishoren.

    3. Maak het scratch-bestand: `mkdir -p packages/snelstart-api/.planning-scratch`.

    4. `composer install` (idempotent — overslaan als vendor/ al up-to-date). Capture eventuele install-warnings naar het scratch-bestand onder een `## composer install` header.

    5. Reproduceer crash. Capture exact:
       `cd packages/snelstart-api && ./vendor/bin/pest 2>&1 | tee .planning-scratch/01-pest-crash-output.txt; echo "---EXIT: $?---" >> .planning-scratch/01-pest-crash-output.txt`

       Run de suite minimaal **twee keer** (Pest gebruikt random execution order, zie phpunit.xml.dist `executionOrder="random"`) — als de crash flakey is en niet altijd in dezelfde test triggert, leg dat ook vast. Run twee: append met `--- RUN 2 ---` header.

    6. Voeg aan de top van de file metadata toe:
       ```
       ## Environment
       PHP: <output of `php -v` first line>
       Pest: <grep -E "pestphp/pest" composer.lock | head -2>
       Saloon: <grep -E "saloonphp/saloon" composer.lock | head -2>
       Testbench: <grep -E "orchestra/testbench" composer.lock | head -2>
       HEAD: <git rev-parse HEAD> on branch <git branch --show-current>
       ```

    7. **Analyse**. Aan de bovenkant van het bestand (na de Environment-block) voeg toe:
       ```
       ## Root cause
       <one line: bestand:regel — wat klopt er niet>

       ## Hypothese voor plan 02
       <2-4 regels: welke specifieke MockClient/pipeline-aanname klopt niet en welke directe unit-test-aanpak voor getRequestException() het vervangt>
       ```

       Vorm de hypothese vanuit het master-plan-pad: "drop the MockClient-pipeline en unit-test `getRequestException()` + de exception factories (`ValidationException::fromBody`, `AuthenticationException::tokenFetchFailed`, `RateLimitException::fromBody`, `NotFoundException::forUrl`, `ServerException::fromResponse`) direct op de Response-mock-laag".

       Concreet: lees `tests/Unit/Http/SnelstartConnectorTest.php` (één test die de container-binding test). Als die crasht, ligt het waarschijnlijk aan een Testbench-bootstrap-issue of een ontbrekend config-default. Als die slaagt en een ándere test crasht (bv. iets dat een MockClient probeert te builden tegen `SnelstartConnector::__construct` met onvolledige args), valt dat hier op door de RUN 1/RUN 2 capture.

    8. **Geen code-fixes**. Deze plan eindigt met een gedocumenteerde root-cause. De fix gebeurt in plan 02.

    9. Optioneel: als de suite onverwacht volledig groen is (exit 0 met ≥30 tests), markeer in de file `## STATUS: NO CRASH REPRO` en noteer hoe vaak je hebt geprobeerd. In dat geval is plan 02's eerste task een no-op en kan plan 02 direct doorgaan met het versterken van SnelstartConnectorTest (nu slechts 1 case).

    Werkt strict per `.claude/rules/engineering.md`: chirurgisch — alleen .planning-scratch/ aanmaken plus eventueel .gitignore. Geen wijzigingen in src/ of tests/ in deze plan.
  </action>
  <verify>
    <automated>
      test -f packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt &amp;&amp; \
      grep -q "^## Environment" packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt &amp;&amp; \
      grep -q "^## Root cause" packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt &amp;&amp; \
      grep -q "EXIT:" packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt &amp;&amp; \
      cd packages/snelstart-api &amp;&amp; [ "$(git branch --show-current)" = "fix/pest-crash-fase-4" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - File `packages/snelstart-api/.planning-scratch/01-pest-crash-output.txt` bestaat
    - File bevat `## Environment` header met PHP/Pest/Saloon/Testbench versies (grep `^## Environment`)
    - File bevat `## Root cause` header met one-liner (grep `^## Root cause`)
    - File bevat `## Hypothese voor plan 02` header met 2-4 regels (grep `^## Hypothese voor plan 02`)
    - File bevat minstens één `---EXIT:` marker (grep `EXIT:`)
    - Working branch in de SDK-repo is `fix/pest-crash-fase-4` (git branch --show-current)
    - `.planning-scratch/` staat in `packages/snelstart-api/.gitignore` (grep -c `^\.planning-scratch/$` retourneert ≥1)
    - Geen wijzigingen in `packages/snelstart-api/src/` of `packages/snelstart-api/tests/` (git diff --name-only main -- src tests retourneert leeg)
  </acceptance_criteria>
  <done>
    Crash gereproduceerd OF expliciet als "geen crash" gemarkeerd, root-cause vastgelegd, plan 02 kan zonder verder uitzoekwerk verder.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → scratch file | Pest output kan ENV-vars echo'en bij verkeerd geconfigureerde tests |
| local dev → SDK-repo git | Diagnostic scratchpads mogen niet de SDK-repo binnenglippen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information disclosure) | `.planning-scratch/01-pest-crash-output.txt` bevat mogelijk env-vars of stack-traces met paden | mitigate | File leeft alleen lokaal: `.planning-scratch/` toegevoegd aan `packages/snelstart-api/.gitignore` zodat het nooit gecommit/gepusht wordt; geen secrets in `.env` van de SDK (alleen testbench defaults) |
| T-01-02 | I | Pest-output kan een raw `SNELSTART_CLIENT_KEY` echo'en als een fixture per ongeluk een echte key bevat | mitigate | Bestaande fixtures gebruiken sentinel-strings (`'ck'`, `'sk'`); diagnose-task wijzigt geen fixtures, dus geen nieuwe leak-paden |
| T-01-03 | T (Tampering) | Diagnostic file bewerkt na-de-feit zodat plan 02 verkeerde fixes maakt | accept | Single-user workflow; output is verifieerbaar door plan 02-executor zelf opnieuw `./vendor/bin/pest` te draaien |
</threat_model>

<verification>
- File-existence + header-greps via `<automated>` block boven.
- Visuele review door de user na task-completion: open `.planning-scratch/01-pest-crash-output.txt`, lees de Root cause + Hypothese, accepteer of corrigeer voordat plan 02 start.
</verification>

<success_criteria>
- Working branch `fix/pest-crash-fase-4` bestaat
- Crash-output gecaptured (of expliciet als no-repro gemarkeerd)
- Root-cause one-liner staat in het scratch-bestand
- Hypothese voor plan 02 staat in het scratch-bestand
- `.planning-scratch/` is gitignored in de SDK-repo
- Geen wijzigingen in src/ of tests/ — diagnostiek-only
</success_criteria>

<output>
After completion, create `.planning/phases/01-snelstart-sdk-finalize/01-01-SUMMARY.md` met:
- Wat is gevonden (root-cause + hypothese — kopieer uit het scratch-bestand)
- Welke files in plan 02 worden geraakt
- Eventuele afwijking van de master-plan-strategie (bv. als de crash niet in MockClient zit)
</output>
