---
phase: "10"
plan: "03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/Filament/Resources/Consumers/ConsumerResource.php
  - app/Filament/Resources/Connections/ConnectionResource.php
  - app/Filament/Resources/Accounts/AccountResource.php
  - app/Filament/Resources/WebhookCalls/WebhookCallResource.php
  - app/Filament/Resources/AccountSubscriptions/AccountSubscriptionResource.php
  - app/Filament/Resources/CashierSubscriptions/CashierSubscriptionResource.php
  - tests/Feature/Admin/ResourceCanAccessTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "6 Filament Resources (Consumer/Connection/Account/WebhookCall/AccountSubscription/CashierSubscription) hebben public static function canAccess(): bool"
    - "Elke canAccess() consulteert de gemapte Spatie-permission via auth()->user()?->can(...) ?? false"
    - "Elke resource heeft public static function shouldRegisterNavigation(): bool { return static::canAccess(); }"
    - "Staff zonder de gemapte permission ziet de Resource niet in nav én krijgt 403 op directe URL"
    - "UserResource blijft ongewijzigd — al gegate via manage-staff (Gate-based, niet permission-based)"
    - "WebhookCallResource::$model points to App\\Models\\WebhookCall (Hub subclass uit 10-01), niet Spatie's WebhookCall"
    - "WebhookCallResource heeft public static function getEloquentQuery(): Builder die parent::getEloquentQuery()->with('consumer') retourneert (eager-load consumer-relatie, voorbereiding voor 10-04)"
  artifacts:
    - path: "app/Filament/Resources/Consumers/ConsumerResource.php"
      provides: "canAccess via manage-consumers"
      contains: "can('manage-consumers')"
    - path: "app/Filament/Resources/Connections/ConnectionResource.php"
      provides: "canAccess via manage-connections"
      contains: "can('manage-connections')"
    - path: "app/Filament/Resources/Accounts/AccountResource.php"
      provides: "canAccess via manage-consumers (Accounts erven onder Consumer-domein per D-1)"
      contains: "can('manage-consumers')"
    - path: "app/Filament/Resources/WebhookCalls/WebhookCallResource.php"
      provides: "canAccess via view-webhooks + $model gebonden aan App\\Models\\WebhookCall + getEloquentQuery met ->with('consumer')"
      contains: "can('view-webhooks')"
    - path: "app/Filament/Resources/AccountSubscriptions/AccountSubscriptionResource.php"
      provides: "canAccess via view-account-subscriptions"
      contains: "can('view-account-subscriptions')"
    - path: "app/Filament/Resources/CashierSubscriptions/CashierSubscriptionResource.php"
      provides: "canAccess via view-billing"
      contains: "can('view-billing')"
  key_links:
    - from: "EmeqStaffSeeder permissions (manage-consumers/manage-connections/view-webhooks/view-account-subscriptions/view-billing)"
      to: "Resource canAccess()-methoden"
      via: "auth()->user()?->can(<permission>) ?? false"
      pattern: "can\\('(manage-consumers|manage-connections|view-webhooks|view-account-subscriptions|view-billing)'\\)"
    - from: "WebhookCallResource::$model"
      to: "App\\Models\\WebhookCall (Hub subclass uit 10-01)"
      via: "Eloquent class-binding (regel ~26) + matching import (regel ~16)"
      pattern: "App\\\\Models\\\\WebhookCall"
---

<objective>
Sluit CR-02 hoofd-component (BLOCKER): voeg `canAccess()` + `shouldRegisterNavigation()` toe aan alle 6 niet-User-resources zodat het D-05 permission-model daadwerkelijk wordt ge-enforced. Permissions zijn al door `EmeqStaffSeeder` provisioneerd; tot deze fix waren ze dead code. Bind tegelijk `WebhookCallResource` aan de nieuwe Hub-eigen `App\Models\WebhookCall`-subclass uit 10-01 en voeg `getEloquentQuery()->with('consumer')` toe — dat opent de file toch al voor de `canAccess()`-edit, dus consolideren we beide wijzigingen om file-conflict met 10-04 te vermijden.

Purpose: `09-REVIEW.md` regels 130-175 stellen vast dat de 5 seeder-permissions (`view-webhooks`, `view-account-subscriptions`, `view-billing`, `manage-consumers`, `manage-connections`) nergens werden gecontroleerd. Een staff-User met permission-revoke kon nog steeds alle resources lezen via direct URL én alle nav-items zien. Dit plan gate't elke resource met `auth()->user()?->can(<permission>) ?? false` zoals D-1 in 10-CONTEXT.md voorschrijft. De WebhookCallResource model-rebinding + eager-load (uit 09-REVIEW CR-02 deel-2 + IN-01) wordt hier al opgenomen omdat 10-04 anders dezelfde file zou aanraken in dezelfde wave — file-conflict-vermijding.

Output: 6 gewijzigde Resource-files (elk +2 methodes; WebhookCallResource +3: canAccess, shouldRegisterNavigation, getEloquentQuery, plus $model-rebinding) + 1 nieuwe feature-test (`ResourceCanAccessTest`) die voor elke resource bewijst: zonder permission → `canAccess()` false + directe URL → 403; met permission → `canAccess()` true + 200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-REVIEW.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-CONTEXT.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-01-PLAN.md
@CLAUDE.md
@.ai/rules/engineering.md

@app/Filament/Resources/Consumers/ConsumerResource.php
@app/Filament/Resources/Connections/ConnectionResource.php
@app/Filament/Resources/Accounts/AccountResource.php
@app/Filament/Resources/WebhookCalls/WebhookCallResource.php
@app/Filament/Resources/AccountSubscriptions/AccountSubscriptionResource.php
@app/Filament/Resources/CashierSubscriptions/CashierSubscriptionResource.php
@app/Filament/Resources/Users/UserResource.php
@database/seeders/EmeqStaffSeeder.php
@tests/Feature/Admin/PermissionGatingTest.php

<interfaces>
<!-- Reference: UserResource canAccess + shouldRegisterNavigation pattern (al geland in Phase 9) -->
From app/Filament/Resources/Users/UserResource.php:40-48:
```php
public static function canAccess(): bool
{
    return Gate::allows('manage-staff');
}

public static function shouldRegisterNavigation(): bool
{
    return Gate::allows('manage-staff');
}
```

<!-- Spatie permission API (al gebruikt in EmeqStaffSeeder + via HasRoles trait op User) -->
`auth()->user()?->can('view-webhooks')` — Spatie hookt in op Laravel's Gate via `HasRoles` trait.

<!-- Permission mapping uit 10-CONTEXT.md D-1 -->
- ConsumerResource → `manage-consumers`
- ConnectionResource → `manage-connections`
- AccountResource → `manage-consumers` (Accounts erven onder Consumer-domein per D-1)
- WebhookCallResource → `view-webhooks`
- AccountSubscriptionResource → `view-account-subscriptions`
- CashierSubscriptionResource → `view-billing`

<!-- Filament v4 eager-load pattern (van AccountResource) -->
```php
public static function getEloquentQuery(): Builder
{
    return parent::getEloquentQuery()->with('consumer');
}
```

<!-- WebhookCallResource huidige class-binding (vóór deze plan) -->
- Regel 16: `use Spatie\WebhookClient\Models\WebhookCall;`
- Regel 26: `protected static ?string $model = WebhookCall::class;`
Na deze plan: beide pointen naar `App\Models\WebhookCall` (subclass uit 10-01) zodat de `consumer()`-belongs-to relatie aanroepbaar is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canAccess() + shouldRegisterNavigation() to all 6 resources + WebhookCallResource $model-rebinding + getEloquentQuery</name>
  <files>
    app/Filament/Resources/Consumers/ConsumerResource.php,
    app/Filament/Resources/Connections/ConnectionResource.php,
    app/Filament/Resources/Accounts/AccountResource.php,
    app/Filament/Resources/WebhookCalls/WebhookCallResource.php,
    app/Filament/Resources/AccountSubscriptions/AccountSubscriptionResource.php,
    app/Filament/Resources/CashierSubscriptions/CashierSubscriptionResource.php
  </files>
  <read_first>
    - 09-REVIEW.md CR-02 fix-section (lines 140-167) — exacte snippet voor canAccess + shouldRegisterNavigation
    - 09-REVIEW.md IN-01 (lines 372-380) — eager-load motivatie voor WebhookCallResource
    - 10-CONTEXT.md D-1 (lines 42-50) — locked permission-mapping per resource
    - 10-CONTEXT.md D-2 (lines 52-60) — WebhookCallResource modifyQueryUsing requirement
    - 10-01-PLAN.md — bevestigt dat `App\Models\WebhookCall` met `consumer()` belongs-to bestaat na Wave 1
    - app/Filament/Resources/Users/UserResource.php regels 40-48 — pattern-reference (Gate-based, niet permission-based; voor de 6 andere resources gebruiken we `can('<perm>')` direct)
    - database/seeders/EmeqStaffSeeder.php regels 26-37 — bevestigt dat de 5 permissions bestaan (`manage-consumers`, `manage-connections`, `view-webhooks`, `view-account-subscriptions`, `view-billing`)
    - app/Filament/Resources/WebhookCalls/WebhookCallResource.php (alle 60 regels) — voor de model-rebinding + getEloquentQuery-insert
    - tests/Feature/Admin/PermissionGatingTest.php — bestaande tests die nog steeds groen moeten blijven
  </read_first>
  <behavior>
    - Test 1: Voor ELKE van de 6 resources: `Resource::canAccess()` returnt `false` wanneer `auth()->user()` mist
    - Test 2: Voor ELKE resource: `canAccess()` returnt `false` voor een staff-User met de gemapte permission gerevoked
    - Test 3: Voor ELKE resource: `canAccess()` returnt `true` voor staff met de gemapte permission
    - Test 4: Voor ELKE resource: `shouldRegisterNavigation()` returnt exact wat `canAccess()` returnt (geen divergence)
    - Test 5: Directe URL-GET op `/admin/<resource>` zonder permission → 403; met permission → 200
    - Test 6: WebhookCallResource::$model === App\Models\WebhookCall::class (geen Spatie-class-binding meer); getEloquentQuery levert een Builder met `->with('consumer')` eager-load gepland (assertion via `getEloquentBuilders()`-equivalent of via `WebhookCallResource::getEloquentQuery()->getEagerLoads()` array-check op key `consumer`)
  </behavior>
  <action>
    Voor elk van de 6 Resource-files: voeg `public static function canAccess(): bool` toe direct vóór de eerste bestaande `public static function`-methode (typisch `form()` of `infolist()`). Body returnt `auth()->user()?->can('<permission>') ?? false`. Voeg dáár direct ná `public static function shouldRegisterNavigation(): bool { return static::canAccess(); }` toe.

    Permission-mapping (per D-1, niet wijzigen — locked decision):
    - `ConsumerResource` → `manage-consumers` (per D-1)
    - `ConnectionResource` → `manage-connections` (per D-1)
    - `AccountResource` → `manage-consumers` (per D-1 — Accounts erven onder Consumer-domein; geen aparte `manage-accounts`-permission in de seeder)
    - `WebhookCallResource` → `view-webhooks` (per D-1)
    - `AccountSubscriptionResource` → `view-account-subscriptions` (per D-1)
    - `CashierSubscriptionResource` → `view-billing` (per D-1)

    Géén edits aan `UserResource` — die is al `Gate::allows('manage-staff')` (regels 40-48 in `app/Filament/Resources/Users/UserResource.php`).

    Géén nieuwe permissions toevoegen aan `EmeqStaffSeeder` — gebruik exact de bestaande 5 (plus `manage-staff` voor UserResource).

    Géén imports toevoegen voor de canAccess-helpers — `auth()` is een globale helper en hoeft niet ge-`use`'d te worden.

    **Extra in `WebhookCallResource.php` (consolidatie uit 10-04 om wave-2 file-conflict te vermijden):**

    1. **Model-rebinding (CR-02 deel-2):** Wijzig regel 16 import van `use Spatie\WebhookClient\Models\WebhookCall;` naar `use App\Models\WebhookCall;`. Op regel 26 (`protected static ?string $model = WebhookCall::class;`) hoeft de body niet te wijzigen — die verwijst nu via de nieuwe import naar de Hub-subclass. Verifieer dat er nergens anders in de file nog een fully-qualified `Spatie\WebhookClient\Models\WebhookCall` reference staat.

    2. **getEloquentQuery (IN-01):** Voeg `public static function getEloquentQuery(): \Illuminate\Database\Eloquent\Builder` toe direct vóór `getPages()` (rond regel 53). Body: `return parent::getEloquentQuery()->with('consumer');`. Importeer `use Illuminate\Database\Eloquent\Builder;` aan top of file zodat de return-type-hint zonder leading-backslash kan.

    Reden voor consolidatie: 10-04 zou anders óók deze file aanraken in dezelfde wave-2. Door beide edits hier te bundelen blijft 10-04 file-disjoint (Tables + Infolist + tests) en kan het parallel met 10-03 draaien. Cross-Consumer-isolatie staat **expliciet niet** in scope van deze fix per D-3 (en 10-CONTEXT.md regels 62-66): SC-7 wordt in v0.2 ingevuld als permission-gated, niet als consumer-scoped. Plan 10-06 zal dit expliciet bewijzen met een gating-test.
  </action>
  <verify>
    <automated>php artisan test --compact --filter='ResourceCanAccessTest|PermissionGatingTest|WebhookCallResourceTest'</automated>
  </verify>
  <done>
    - `grep -c "can('manage-consumers')" app/Filament/Resources/Consumers/ConsumerResource.php app/Filament/Resources/Accounts/AccountResource.php` returnt totaal `2`
    - `grep -c "can('manage-connections')" app/Filament/Resources/Connections/ConnectionResource.php` returnt `1`
    - `grep -c "can('view-webhooks')" app/Filament/Resources/WebhookCalls/WebhookCallResource.php` returnt `1`
    - `grep -c "can('view-account-subscriptions')" app/Filament/Resources/AccountSubscriptions/AccountSubscriptionResource.php` returnt `1`
    - `grep -c "can('view-billing')" app/Filament/Resources/CashierSubscriptions/CashierSubscriptionResource.php` returnt `1`
    - In elk van de 6 files: `grep -c 'public static function canAccess' <file>` returnt `1` én `grep -c 'public static function shouldRegisterNavigation' <file>` returnt `1`
    - `grep -c "use App\\\\Models\\\\WebhookCall;" app/Filament/Resources/WebhookCalls/WebhookCallResource.php` returnt `1`
    - `grep -c "Spatie\\\\WebhookClient\\\\Models\\\\WebhookCall" app/Filament/Resources/WebhookCalls/WebhookCallResource.php` returnt `0`
    - `grep -c "->with('consumer')" app/Filament/Resources/WebhookCalls/WebhookCallResource.php` returnt `1`
    - `grep -c 'public static function getEloquentQuery' app/Filament/Resources/WebhookCalls/WebhookCallResource.php` returnt `1`
    - Bestaande `PermissionGatingTest`-tests blijven groen (geen regressie op UserResource-gate)
    - Bestaande `WebhookCallResourceTest`-tests (3 tests) blijven groen na model-rebinding
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write ResourceCanAccessTest covering all 6 resources</name>
  <files>tests/Feature/Admin/ResourceCanAccessTest.php</files>
  <read_first>
    - tests/Feature/Admin/PermissionGatingTest.php — bestaand pattern voor permission-driven tests met Spatie Role + Permission setup
    - tests/Feature/Admin/PanelAccessTest.php — bestaand pattern voor `assertForbidden` op `/admin/<resource>`
    - tests/Feature/Admin/WebhookCallResourceTest.php regels 29-44 — `seedRoles()` + `actAsStaff()` helper-pattern (kopiëren, niet hergebruiken om class-couplering te vermijden)
    - database/seeders/EmeqStaffSeeder.php — permission-namen die we toekennen
  </read_first>
  <action>
    Maak `tests/Feature/Admin/ResourceCanAccessTest.php` aan in PHPUnit-stijl (geen Pest). Gebruik `RefreshDatabase`-trait. Setup:

    Private helper `seedRolesAndPermissions()` die Spatie's `Role::firstOrCreate(['name' => 'staff'])` + 5 `Permission::firstOrCreate`-rows aanmaakt voor de relevante permissions (`manage-consumers`, `manage-connections`, `view-webhooks`, `view-account-subscriptions`, `view-billing`). Geen `super-admin`-role nodig — die zou alle permissions overrulen.

    Voor ELKE van de 6 resources twee testmethodes (12 totaal):

    1. `test_<resource>_returns_403_for_staff_without_permission` — User factory + `assignRole('staff')` zónder de relevante permission toe te kennen → `$this->get('/admin/<resource>')->assertForbidden()`.

    2. `test_<resource>_returns_200_for_staff_with_permission` — User factory + `assignRole('staff')` + `$user->givePermissionTo('<permission>')` → `$this->get('/admin/<resource>')->assertOk()`.

    URL-paden per resource:
    - `/admin/consumers` (manage-consumers)
    - `/admin/connections` (manage-connections)
    - `/admin/accounts` (manage-consumers)
    - `/admin/webhook-calls` (view-webhooks)
    - `/admin/account-subscriptions` (view-account-subscriptions)
    - `/admin/cashier-subscriptions` (view-billing)

    Belangrijk: gebruik `actingAs($user)` met de `web`-guard (Filament's panel-guard) per `AdminPanelProvider->authGuard('web')`. Volg de bestaande test-pattern in `PermissionGatingTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ResourceCanAccessTest</automated>
  </verify>
  <done>
    - `tests/Feature/Admin/ResourceCanAccessTest.php` exists met minimaal 12 test-methodes (6 resources × 2 paths)
    - `grep -c 'public function test_' tests/Feature/Admin/ResourceCanAccessTest.php` returnt minimaal `12`
    - `php artisan test --compact --filter=ResourceCanAccessTest` rapporteert minimaal `12 passed`
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter='ResourceCanAccessTest|PermissionGatingTest|PanelAccessTest|WebhookCallResourceTest'` → alle tests passed
- `php artisan test --compact tests/Feature/Admin/` → volledige admin-suite groen, geen regressie op Phase 9-tests
- `./vendor/bin/pint --dirty --format agent` levert geen fixes
</verification>

<success_criteria>
- 6 resources hebben `canAccess()` + `shouldRegisterNavigation()` gewired op exact de juiste Spatie-permission per D-1
- WebhookCallResource $model gebonden aan App\Models\WebhookCall (Hub-subclass uit 10-01); Spatie-import weg
- WebhookCallResource heeft getEloquentQuery met `->with('consumer')` eager-load (vereiste voor 10-04's TextColumn('consumer.slug') zonder N+1)
- 12 nieuwe gating-tests bewijzen 403/200-flow voor alle 6 resources
- Bestaande Phase-9 testpopulatie (391 tests) regresseert niet
- D-1 + D-2 (model-binding subset) closed; permission-model is geen dead code meer
</success_criteria>

<output>
Create `.planning/phases/10-phase-9-polish-deferred-review-findings/10-03-SUMMARY.md` when done.
</output>
