---
phase: "10"
plan: "06"
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified:
  - app/Filament/Resources/Consumers/Pages/ListConsumers.php
  - app/Filament/Resources/Consumers/ConsumerResource.php
  - resources/views/filament/resources/consumers/pages/list-consumers.blade.php
  - tests/Feature/Admin/UserResourceTest.php
  - tests/Feature/Admin/WebhookCallResourceTest.php
  - tests/Feature/Admin/ConsumerTokenActionTest.php
autonomous: true
requirements: ["HUB-04 SC-7"]
must_haves:
  truths:
    - "Plain PAT-token wordt via Cache::pull() one-shot uitgereikt — niet meer als Livewire property `$lastIssuedPat` of in Alpine x-data"
    - "test_edit_user_without_password_keeps_existing_hash bewijst dat UserForm op edit met lege password-input de hash bewaart"
    - "test_cross_consumer_isolation_staff_with_view_webhooks_permission_sees_all_webhooks bewijst HUB-04 SC-7 als permission-gated (v0.2 keuze per D-3)"
    - "test_staff_without_view_webhooks_permission_cannot_access_webhooks_resource bewijst dat permission-gating de access-control is in v0.2 (geen consumer-scoping)"
    - "Bestaande ConsumerTokenActionTest blijft groen — token-uitreik werkt nog steeds, alleen via een veilige flash-cache i.p.v. Livewire-state"
    - "Volledige test-suite (Phase 9 baseline 389 + Phase 10 nieuwe tests) is groen"
  artifacts:
    - path: "app/Filament/Resources/Consumers/Pages/ListConsumers.php"
      provides: "Geen `$lastIssuedPat` property meer — token-flash via Cache::pull"
      contains: "Cache::pull"
    - path: "resources/views/filament/resources/consumers/pages/list-consumers.blade.php"
      provides: "Blade leest Cache::pull één keer — geen Alpine x-data met token meer"
      contains: "Cache::pull"
    - path: "tests/Feature/Admin/UserResourceTest.php"
      provides: "test_edit_user_without_password_keeps_existing_hash regression"
      contains: "test_edit_user_without_password_keeps_existing_hash"
    - path: "tests/Feature/Admin/WebhookCallResourceTest.php"
      provides: "2× cross-Consumer-isolation tests (closes HUB-04 SC-7)"
      contains: "test_cross_consumer_isolation"
  key_links:
    - from: "ConsumerResource issuePatAction"
      to: "Cache::put('pat-flash:{livewire-id}', $plainToken, 60)"
      via: "Cache facade (server-side, one-shot via blade Cache::pull)"
      pattern: "Cache::put\\('pat-flash:"
    - from: "list-consumers.blade.php"
      to: "Cache::pull('pat-flash:{livewire-id}')"
      via: "@php-block of pre-rendered variable, one-shot read"
      pattern: "Cache::pull\\('pat-flash:"
---

<objective>
Sluit de drie laatste deferred items van Phase 10:

1. **WR-06 / D-9** — Plain PAT-token uit Livewire `wire:snapshot` + Alpine `x-data` halen; vervangen door server-side `Cache::pull()` one-shot. Token verschijnt niet meer in HTTP-response-state na de eerste render.

2. **WR-05 / D-8** — Regression-test `test_edit_user_without_password_keeps_existing_hash` op `UserResourceTest`. Géén productiecode-wijziging tenzij de test de bestaande `dehydrateStateUsing`+`dehydrated(filled)`-pattern als gebroken bewijst — in dat geval ook switchen naar `mutateDehydratedStateUsing`.

3. **CR-02 deel-3 / D-3** — Cross-Consumer-isolation test in `WebhookCallResourceTest`. Per D-3 (10-CONTEXT.md regels 62-66) wordt SC-7 in v0.2 ingevuld als **permission-gated**, niet als consumer-scoped: staff zonder `view-webhooks`-permission → geen toegang; staff met de permission ziet alle webhooks (een latere fase introduceert staff↔consumer-binding). De tests documenteren dit expliciet zodat de keuze traceerbaar is.

Purpose: Dit is de SC-7-sluitende plan voor `requirements: ["HUB-04 SC-7"]`. Na deze plan zijn 11/11 deferred findings dicht en is de Phase 9-quality-claim ship-ready.

Output: 3 modified production-files (ListConsumers + ConsumerResource + blade-view) + 3 modified test-files (1 nieuwe test op UserResourceTest, 2 nieuwe tests op WebhookCallResourceTest, optional update op ConsumerTokenActionTest om Cache::pull-flow te exerciseren).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-REVIEW.md
@.planning/phases/10-phase-9-polish-deferred-review-findings/10-CONTEXT.md
@.ai/rules/global.md
@CLAUDE.md

@app/Filament/Resources/Consumers/Pages/ListConsumers.php
@app/Filament/Resources/Consumers/ConsumerResource.php
@resources/views/filament/resources/consumers/pages/list-consumers.blade.php
@tests/Feature/Admin/ConsumerTokenActionTest.php
@tests/Feature/Admin/UserResourceTest.php
@tests/Feature/Admin/WebhookCallResourceTest.php
@app/Filament/Resources/Users/Schemas/UserForm.php

<interfaces>
<!-- Filament v4 Livewire ID API -->
- `$livewire->getId(): string` — Livewire component-id (uniek per request-page)

<!-- Laravel Cache facade -->
- `Cache::put(string $key, mixed $value, int $seconds): bool`
- `Cache::pull(string $key, mixed $default = null): mixed` — read + delete one-shot

<!-- Current ListConsumers Livewire state (regel 14) -->
- `public ?array $lastIssuedPat = null;` — VERWIJDEREN
- `public function dismissIssuedPat(): void` — VERWIJDEREN

<!-- Current ConsumerResource::issuePatAction action-callback (regels 175-195) -->
```php
->action(function (Consumer $record, array $data, $livewire): void {
    $abilities = ...;
    $result = $record->createToken($data['name'], $abilities);
    if (property_exists($livewire, 'lastIssuedPat')) {
        $livewire->lastIssuedPat = ['token' => $result->plainTextToken, 'name' => $data['name']];
    }
    Notification::make()->title('PAT uitgegeven — bekijk hierboven om te kopiëren')->success()->send();
});
```

<!-- Current blade (resources/views/.../list-consumers.blade.php regels 25-46) -->
- Alpine `x-data="{ token: @js($this->lastIssuedPat['token']), copied: false, ... }"` — token zit dubbel in HTTP-payload
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PAT-token via Cache::pull() one-shot — verwijder Livewire-state (WR-06 / D-9)</name>
  <files>
    app/Filament/Resources/Consumers/Pages/ListConsumers.php,
    app/Filament/Resources/Consumers/ConsumerResource.php,
    resources/views/filament/resources/consumers/pages/list-consumers.blade.php,
    tests/Feature/Admin/ConsumerTokenActionTest.php
  </files>
  <read_first>
    - 09-REVIEW.md WR-06 (lines 336-365) — Livewire-snapshot exposure + fix pattern
    - 10-CONTEXT.md D-9 (lines 105-110) — locked decision: Cache::put + Cache::pull one-shot
    - app/Filament/Resources/Consumers/Pages/ListConsumers.php (alle 36 regels)
    - app/Filament/Resources/Consumers/ConsumerResource.php regels 32 + 175-195 (issuePatAction)
    - resources/views/filament/resources/consumers/pages/list-consumers.blade.php (alle 58 regels)
    - tests/Feature/Admin/ConsumerTokenActionTest.php — bestaande pattern; mag blijven staan maar moet door de refactor heen blijven werken
  </read_first>
  <behavior>
    - Test 1: na issuePatAction is `Cache::has("pat-flash:{livewire-id}")` true direct na callTableAction
    - Test 2: Op de listing-page render is `Cache::pull` aangeroepen (cache leeg na render) — token zit ÉÉNMAAL in HTTP-response (in de blade) en niet meer in `wire:snapshot`
    - Test 3: Bestaande `ConsumerTokenActionTest` — token-row in `personal_access_tokens` wordt correct aangemaakt
    - Test 4 (optioneel smoke): Plain token verschijnt NIET in `Livewire::test(ListConsumers::class)->callTableAction('issuePat', $consumer, [...])` snapshot-encoding — gebruik response-HTML grep om de plain token-string te zoeken; mag alleen voorkomen in het blade-rendered token-div, niet in Livewire-snapshot-attribuut.
  </behavior>
  <action>
    1. **In `app/Filament/Resources/Consumers/Pages/ListConsumers.php`:**

       Verwijder `public ?array $lastIssuedPat = null;` (regel 14) + bijbehorende docstring.

       Verwijder `public function dismissIssuedPat(): void` methode (regels 24-27).

       Behoud `protected string $view = 'filament.resources.consumers.pages.list-consumers';` + getSubheading() + getHeaderActions(). De blade-view consumeert nu één keer via Cache::pull en heeft geen state-prop meer nodig.

    2. **In `app/Filament/Resources/Consumers/ConsumerResource.php::issuePatAction()` action-callback (regels 175-195):**

       Vervang het `if (property_exists($livewire, 'lastIssuedPat')) { $livewire->lastIssuedPat = …; }`-blok door:

       `Cache::put("pat-flash:{$livewire->getId()}", $result->plainTextToken, now()->addSeconds(60));`
       `Cache::put("pat-flash-name:{$livewire->getId()}", $data['name'], now()->addSeconds(60));`

       (Twee keys om token + token-name separat te flashen — blade leest beide.)

       Importeer `use Illuminate\Support\Facades\Cache;` aan top of file.

       Pas Notification-message aan: `'PAT uitgegeven — token verschijnt eenmalig bovenaan de listing'`.

    3. **In `resources/views/filament/resources/consumers/pages/list-consumers.blade.php`:**

       Vervang regels 3-55 (de hele `@if ($this->lastIssuedPat !== null)`-block + Alpine x-data) door:

       Een `@php`-block bovenaan dat één keer uit de cache leest:
       ```
       @php
           $issuedToken = \Illuminate\Support\Facades\Cache::pull('pat-flash:'.$this->getId());
           $issuedName = \Illuminate\Support\Facades\Cache::pull('pat-flash-name:'.$this->getId());
       @endphp
       ```

       Daarna `@if ($issuedToken !== null)`-block met een section die het token toont via `{{ $issuedToken }}` (direct in HTML — geen Alpine `x-data` met token meer; geen `@js(...)` met token; gebruik server-side rendered `<code>{{ $issuedToken }}</code>`).

       Behoud de visuele section-shape (warning icon, heading "PAT uitgegeven — {{ $issuedName }}", description "Eenmalig zichtbaar — kopieer dit token nu naar je wachtwoordmanager"). De Alpine `copy()`-functie mag blijven maar moet de `<code>`-element-tekst lezen via `$el.previousElementSibling.innerText` of `navigator.clipboard.writeText` op een `data-token`-attribuut. **Geen `@js($issuedToken)` of `x-data="{ token: '{{ $issuedToken }}' ... }"`** — beide patronen serialiseren het token weer in JavaScript-state.

       Simpelste pattern: `<button x-on:click="navigator.clipboard.writeText($refs.tokenCode.innerText)">Kopieer</button>` met `<code x-ref="tokenCode">{{ $issuedToken }}</code>`. Geen `wire:click="dismissIssuedPat"` knop meer — token verdwijnt vanzelf bij volgende page-render (Cache::pull is destructief).

       Behoud `{{ $this->content }}` op het einde — Filament's ListRecords-content.

    4. **Optioneel in `tests/Feature/Admin/ConsumerTokenActionTest.php`:** voeg `Cache::shouldReceive('put')->twice()` mock toe OF `Cache::spy()` + `Cache::shouldHaveReceived('put', ...)`-assertion in de bestaande happy-path test om te bewijzen dat de cache-write daadwerkelijk plaatsvindt. Niet vereist — bestaande tests moeten primair groen blijven (token-row + plain-token-Notification).

       Voeg één nieuwe test toe: `test_plain_token_not_in_livewire_snapshot` — `Livewire::test(...)->callTableAction('issuePat', ...)->assertDontSeeHtml('wire:snapshot="*'.$plainToken.'*')` (gebruik `assertDontSee` met de plain token-string + regex of substring-check op `wire:snapshot`-attribute).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ConsumerTokenActionTest</automated>
  </verify>
  <done>
    - `grep -c 'lastIssuedPat' app/Filament/Resources/Consumers/Pages/ListConsumers.php` returnt `0`
    - `grep -c 'dismissIssuedPat' app/Filament/Resources/Consumers/Pages/ListConsumers.php` returnt `0`
    - `grep -c 'Cache::put' app/Filament/Resources/Consumers/ConsumerResource.php` returnt minimaal `1`
    - `grep -c 'Cache::pull' resources/views/filament/resources/consumers/pages/list-consumers.blade.php` returnt minimaal `1`
    - `grep -c '@js(\$this->lastIssuedPat' resources/views/filament/resources/consumers/pages/list-consumers.blade.php` returnt `0` (geen Alpine token-binding meer)
    - `grep -c 'x-data=.*token:' resources/views/filament/resources/consumers/pages/list-consumers.blade.php` returnt `0`
    - `php artisan test --compact --filter=ConsumerTokenActionTest` rapporteert all passed (bestaande + nieuwe smoke-test)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserResource edit-without-password regression test (WR-05 / D-8)</name>
  <files>tests/Feature/Admin/UserResourceTest.php</files>
  <read_first>
    - 09-REVIEW.md WR-05 (lines 296-333) — pattern + verwachte test-vorm
    - 10-CONTEXT.md D-8 (lines 101-103) — locked decision: alleen test toevoegen tenzij bug oppervlakt
    - app/Filament/Resources/Users/Schemas/UserForm.php (alle 45 regels) — huidige dehydrateStateUsing + dehydrated pattern
    - tests/Feature/Admin/UserResourceTest.php (na 10-05 — heeft inmiddels 8+ tests)
  </read_first>
  <action>
    Voeg één nieuwe testmethode `test_edit_user_without_password_keeps_existing_hash` toe aan `UserResourceTest`:

    1. Setup: `actingAsSuperAdmin()` + creëer een tweede User via factory met expliciete `password = Hash::make('original-pass')`.

    2. Open `Livewire::test(EditUser::class, ['record' => $target->getRouteKey()])`-page.

    3. Fill form met **lege** password: `->fillForm(['name' => 'Updated Name', 'email' => $target->email, 'password' => ''])` (of laat password expliciet null).

    4. `->call('save')->assertHasNoFormErrors()`.

    5. Refresh: `$target->refresh()`.

    6. Assert: `$this->assertTrue(Hash::check('original-pass', $target->password));` — de oude hash bewaard.

    7. Assert: `$this->assertSame('Updated Name', $target->name);` — andere velden zijn wél geüpdatet.

    Als deze test FAALT (de huidige `dehydrateStateUsing` + `dehydrated(filled)`-pattern blijkt gebroken):
    - Refactor `UserForm.php` regels 36-42 naar de `mutateDehydratedStateUsing`-pattern uit 09-REVIEW.md regels 311-315:
      ```
      TextInput::make('password')
          ->password()
          ->revealable()
          ->maxLength(255)
          ->mutateDehydratedStateUsing(fn ($state) => filled($state) ? Hash::make((string) $state) : null)
          ->dehydrated(fn ($state) => filled($state))
          ->required(fn (string $operation): bool => $operation === 'create'),
      ```
    - Re-run de test.

    Per D-8 is dit een test-eerst-pad: schrijf de test, run hem, alleen patchen wanneer rood. Volg dat protocol.

    Imports: `use App\Filament\Resources\Users\Pages\EditUser;` toevoegen aan UserResourceTest.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=UserResourceTest</automated>
  </verify>
  <done>
    - `grep -c 'test_edit_user_without_password_keeps_existing_hash' tests/Feature/Admin/UserResourceTest.php` returnt `1`
    - `php artisan test --compact --filter='UserResourceTest::test_edit_user_without_password_keeps_existing_hash'` rapporteert `1 passed`
    - Volledige UserResourceTest blijft groen (9+ tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HUB-04 SC-7 closure — cross-Consumer-isolation permission-gating tests</name>
  <files>tests/Feature/Admin/WebhookCallResourceTest.php</files>
  <read_first>
    - 09-REVIEW.md CR-02 (lines 130-175) — SC-7 acceptance-gap
    - 10-CONTEXT.md D-3 (lines 62-66) — locked decision: in v0.2 = permission-gated, niet consumer-scoped
    - 09-CONTEXT.md regel 242 — originele SC-7 wording ("WebhookCallResource toont … cross-Consumer-isolatie via gefilterde queries")
    - tests/Feature/Admin/WebhookCallResourceTest.php — na 10-04 heeft deze file 6+ tests inclusief `view-webhooks`-permission setup
  </read_first>
  <action>
    Voeg twee nieuwe testmethodes toe aan `WebhookCallResourceTest`:

    1. `test_staff_without_view_webhooks_permission_cannot_access_webhooks_resource` — User factory + `assignRole('staff')` ZONDER `givePermissionTo('view-webhooks')` → `$this->get('/admin/webhook-calls')->assertForbidden();`. Dit overlapt met 10-03's ResourceCanAccessTest maar legt de SC-7 closure-claim **specifiek voor WebhookCallResource** vast in WebhookCallResourceTest zodat de SC-7-acceptance traceable is in één file.

    2. `test_cross_consumer_isolation_staff_with_view_webhooks_permission_sees_all_webhooks_per_v02_decision_d3` — Setup:
       - 2 Consumers (`'consumer-a'`, `'consumer-b'`)
       - 2 webhook_calls-rows, één per consumer
       - User factory + `assignRole('staff')` + `givePermissionTo('view-webhooks')`
       - `Livewire::test(ListWebhookCalls::class)->assertCountTableRecords(2)`
       - Toelichting via PHPDoc op de test-class of inline `@see 10-CONTEXT.md D-3`: in v0.2 ziet een staff-user met `view-webhooks` ALLE consumer-webhooks; per-Consumer-scoping wordt geïntroduceerd in v1.0+ wanneer externe staff per Consumer beschikbaar komt.

    Beide tests sluiten samen HUB-04 SC-7 zoals 10-CONTEXT.md D-3 het scope-d: SC-7 wordt expliciet ingevuld als permission-gated. De testbody-comment moet expliciet zeggen dat dit een **bewuste v0.2-keuze** is en niet een gat in acceptance-coverage.

    Test-class-update: zorg dat de inline-PHPDoc bovenaan de class een sectie `* HUB-04 SC-7 closure (v0.2):` toevoegt met 3-regels uitleg over de keuze. Helpt reviewers om de scope-fence later terug te vinden.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=WebhookCallResourceTest</automated>
  </verify>
  <done>
    - `grep -c 'test_cross_consumer_isolation' tests/Feature/Admin/WebhookCallResourceTest.php` returnt minimaal `1`
    - `grep -c 'test_staff_without_view_webhooks_permission' tests/Feature/Admin/WebhookCallResourceTest.php` returnt `1`
    - `grep -c 'HUB-04 SC-7' tests/Feature/Admin/WebhookCallResourceTest.php` returnt minimaal `1` (PHPDoc-toelichting)
    - `php artisan test --compact --filter=WebhookCallResourceTest` rapporteert `8+ passed`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Full-suite regressie-check + pint-clean</name>
  <files></files>
  <read_first>
    - .planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-11-ACCEPTANCE.md — baseline 389 tests + 1 incomplete
    - 10-CONTEXT.md success-criterium 12 — volledige suite groen
  </read_first>
  <action>
    Run de volledige test-suite via `php artisan test --compact` en bevestig:

    1. Baseline 389 tests blijven groen (geen Phase-9 regressie).
    2. Nieuwe tests uit Phase 10 plannen (10-01, 10-02, 10-03, 10-04, 10-05, 10-06) zijn allemaal groen.
    3. Pre-existing 1 incomplete (`SanctumAbilityTest::test_token_without_required_ability_is_rejected` — Phase 3-03 placeholder) blijft incomplete; dit is verwacht en unrelated.

    Verwachte test-counts (indicatief, niet hard-gated):
    - 10-01: +5 (WebhookCallConsumerRelationTest)
    - 10-02: +2 (ProviderDescriptorTest extension)
    - 10-03: +12 (ResourceCanAccessTest)
    - 10-04: +3 (WebhookCallResourceTest extension)
    - 10-05: +5 (UserResourceTest) + 1 (EmeqStaffSeederTest replace) + 1 (AccountSubscriptionStateActionsTest)
    - 10-06: +2 (WebhookCallResourceTest) + 1 (UserResourceTest)

    **Geschatte delta: +31 tests** boven Phase-9-baseline van 389 → totaal ~420 tests / 0 failed / 1 incomplete.

    Run daarna `./vendor/bin/pint --dirty --format agent` voor formattering. Mag geen fixes opleveren (alle Phase-10-files hebben Pint-clean style gevolgd).

    Documenteer counts in `10-06-SUMMARY.md`.
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <done>
    - Volledige `php artisan test --compact` exit-code = 0
    - Failed-count = 0
    - Nieuwe tests delta = +~31 t.o.v. Phase-9-baseline 389
    - 1 pre-existing incomplete (SanctumAbilityTest) blijft incomplete — niet failed
    - `./vendor/bin/pint --dirty --format agent` levert geen fixes
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact` → 420+ passed / 0 failed / 1 incomplete
- `php artisan test --compact tests/Feature/Admin/` → admin-suite volledig groen
- `grep -rn 'wire:snapshot' resources/views/filament/resources/consumers/ | grep -c 'token'` returnt `0`
- `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- WR-06 closed: PAT-token via Cache::pull one-shot — geen `wire:snapshot` token-leak meer
- WR-05 closed: `test_edit_user_without_password_keeps_existing_hash` regression-vangnet geland
- HUB-04 SC-7 closed via D-3 (v0.2 = permission-gated): 2 nieuwe tests in WebhookCallResourceTest documenteren expliciet de keuze
- Volledige test-suite (~420 tests) groen — Phase 9 baseline regresseert niet
- 11/11 deferred review-findings (CR-02 + WR-01..06 + IN-01..04) zijn nu dicht
- Phase 10 is ship-ready voor checkpoint-approval + ROADMAP/REQUIREMENTS/STATE sync
</success_criteria>

<output>
Create `.planning/phases/10-phase-9-polish-deferred-review-findings/10-06-SUMMARY.md` when done — bevat:
- Test-counts vóór + na alle Phase-10-plannen (baseline 389 → final ~420)
- Mapping van elke deferred finding (CR-02 + WR-01..06 + IN-01..04) naar de Phase-10-plan(s) die het sloten
- Bevestiging dat geen Phase-9-test gebroken is
- HUB-04 SC-7 explicit-closure note met verwijzing naar D-3
- Next steps: ROADMAP/REQUIREMENTS/STATE sync via een follow-up acceptance-plan of inline-checkpoint
</output>
