---
phase: 04-mollie-connect-oauth-broker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/OAuth/Contracts/OAuthFlow.php
  - app/OAuth/Testing/FakeOAuthFlow.php
  - database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php
  - app/Models/Connection.php
  - database/factories/ConnectionFactory.php
  - tests/Feature/OAuth/OAuthFlowContractTest.php
autonomous: true
requirements: [MOLL-02, HUB-02]
tags:
  - laravel
  - oauth
  - phpunit
  - migrations

must_haves:
  truths:
    - "OAuthFlow-contract bestaat als provider-agnostische interface in app/OAuth/Contracts/ (D-13)"
    - "FakeOAuthFlow test-fixture leeft in app/OAuth/Testing/ (NIET tests/) — runtime-bindable via container (D-12)"
    - "FakeOAuthFlow bewijst SC-4 (contract is niet Mollie-specifiek) via OAuthFlowContractTest"
    - "connections-tabel heeft oauth_state (string,nullable) + oauth_state_expires_at (timestamp,nullable) kolommen via forward-only migration"
    - "Connection-model fillable + casts uitgebreid met de 2 nieuwe velden zonder bestaande casts/Hidden te raken"
    - "ConnectionFactory heeft pending() + active() + expired() states voor Mollie-OAuth-lifecycle"
  artifacts:
    - path: "app/OAuth/Contracts/OAuthFlow.php"
      provides: "interface OAuthFlow met 4 methods: getAuthorizationUrl, exchangeCode, refreshToken, revoke"
      contains: "interface OAuthFlow"
    - path: "app/OAuth/Testing/FakeOAuthFlow.php"
      provides: "Deterministic test-fixture implementeert OAuthFlow met wasCalled()-teller"
      contains: "implements OAuthFlow"
    - path: "database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php"
      provides: "ALTER TABLE connections ADD oauth_state + oauth_state_expires_at + indexen"
      contains: "Schema::table('connections'"
    - path: "app/Models/Connection.php"
      provides: "Fillable + casts uitgebreid voor oauth_state + oauth_state_expires_at"
      contains: "'oauth_state'"
    - path: "database/factories/ConnectionFactory.php"
      provides: "pending() + active() + expired() factory-states voor Mollie-OAuth-lifecycle"
      contains: "public function pending(): static"
    - path: "tests/Feature/OAuth/OAuthFlowContractTest.php"
      provides: "ROADMAP SC-4 bewijs — FakeOAuthFlow satisfies OAuthFlow-contract"
      contains: "assertInstanceOf(OAuthFlow::class"
  key_links:
    - from: "App\\OAuth\\Testing\\FakeOAuthFlow"
      to: "App\\OAuth\\Contracts\\OAuthFlow"
      via: "implements"
      pattern: "implements OAuthFlow"
    - from: "Connection model"
      to: "connections.oauth_state column"
      via: "Fillable attribute + casts() method"
      pattern: "'oauth_state'"
---

<objective>
Foundation-laag voor Phase 4: het provider-agnostische `OAuthFlow`-contract (D-13), de runtime test-fixture `FakeOAuthFlow` (D-12), de schema-uitbreiding op `connections` (oauth_state-kolommen), de model-wijziging die de nieuwe velden fillable+cast maakt, de factory-states die Mollie-OAuth-lifecycle bewijzen, plus de contract-test die SC-4 ("contract niet Mollie-specifiek") groen maakt.

Purpose: Deze laag is de basis waarop alle volgende plannen leunen. Het contract bepaalt de signatures die `MollieConnectOAuthFlow` (Plan 02) en de controllers (Plan 04) implementeren/aanroepen. De schema-velden + factory-states zijn nodig om Init-/Callback-tests en de prune-command in latere plannen testbaar te maken. Bewijst ROADMAP SC-4 (FakeOAuthFlow als drop-in) al in deze wave.

Output: 5 nieuwe files (contract + fixture + migration + 1 test) + 2 modified files (Connection model + ConnectionFactory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md
@.planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@app/Models/Connection.php
@database/factories/ConnectionFactory.php
@database/migrations/2026_05_14_000003_create_connections_table.php
@database/migrations/2026_05_14_151327_add_active_unique_to_connections.php

<interfaces>
<!-- Bestaande types/contracts die Plan 04-01 raakt. Executor moet deze niet opnieuw ontdekken. -->

From app/Models/Connection.php (huidige fillable + casts):
```php
#[Fillable([
    'account_id', 'provider', 'status',
    'access_token', 'refresh_token', 'expires_at', 'scopes',
    'client_key', 'subscription_key', 'subscription_id',
    'metadata', 'revoked_at',
])]
#[Hidden(['access_token', 'refresh_token', 'client_key', 'subscription_key'])]
class Connection extends Model {
    protected function casts(): array {
        return [
            'access_token' => 'encrypted',
            'refresh_token' => 'encrypted',
            'client_key' => 'encrypted',
            'subscription_key' => 'encrypted',
            'scopes' => 'array',
            'metadata' => 'array',
            'expires_at' => 'datetime',
            'revoked_at' => 'datetime',
        ];
    }
}
```

From database/factories/ConnectionFactory.php (huidige states):
```php
public function definition(): array; // provider=snelstart, status=active
public function forSnelstart(): static; // CK-/SK-/uuid creds
public function forMollie(): static; // access_/refresh_/expires_at/scopes
```

Nieuw contract dat Plan 04-02 (MollieConnectOAuthFlow) en Plan 04-04 (controllers) gaan implementeren/aanroepen:
```php
namespace App\OAuth\Contracts;

interface OAuthFlow {
    public function getAuthorizationUrl(Account $account, array $scopes, string $state): string;
    public function exchangeCode(Connection $connection, string $code): Connection;
    public function refreshToken(Connection $connection): Connection;
    public function revoke(Connection $connection): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OAuthFlow-contract + FakeOAuthFlow test-fixture + OAuthFlowContractTest</name>
  <files>
    app/OAuth/Contracts/OAuthFlow.php
    app/OAuth/Testing/FakeOAuthFlow.php
    tests/Feature/OAuth/OAuthFlowContractTest.php
  </files>
  <read_first>
    .planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md (lock D-12 + D-13)
    .planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md (sections: OAuthFlow contract, FakeOAuthFlow, OAuthFlowContractTest)
    packages/mollie-api/src/Contracts/MollieCredentialResolver.php (analog: SDK-laag interface)
    packages/mollie-api/src/Testing/FakeMollieCredentialResolver.php (analog: runtime test-fixture in src/)
    app/Models/Connection.php (signature voor Connection-arg)
    app/Models/Account.php (signature voor Account-arg)
    .ai/rules/global.md (taal-conventie: code Engels)
  </read_first>
  <action>
    Maak 3 files aan.

    **1. app/OAuth/Contracts/OAuthFlow.php** — Hub-laag interface (D-13: NIET in packages/, contract leeft in Hub omdat het multi-provider scope is). Hub-tree heeft géén `declare(strict_types=1)` (matcht Connection.php / PingController.php) — **wel** strict_types? PATTERNS.md §"PHP-conventies" zegt expliciet: GEEN strict_types in app/-tree. Volg Hub-conventie. Inhoud:

    ```php
    <?php

    namespace App\OAuth\Contracts;

    use App\Models\Account;
    use App\Models\Connection;

    interface OAuthFlow
    {
        /**
         * Bouw de authorize-URL die de browser naar de partner stuurt.
         *
         * @param  list<string>  $scopes
         */
        public function getAuthorizationUrl(Account $account, array $scopes, string $state): string;

        /**
         * Ruil authorization-code in voor access/refresh tokens en schrijf
         * encrypted naar de Connection. Zet status='active' en oauth_state=null.
         */
        public function exchangeCode(Connection $connection, string $code): Connection;

        /**
         * Lazy-refresh de access_token bij naderende expiry. Idempotent —
         * mag meermaals aangeroepen worden binnen het refresh-window.
         */
        public function refreshToken(Connection $connection): Connection;

        /**
         * Trek de koppeling in bij de partner én zet status='revoked' lokaal.
         */
        public function revoke(Connection $connection): void;
    }
    ```

    **2. app/OAuth/Testing/FakeOAuthFlow.php** — D-12: namespace `App\OAuth\Testing\` (NIET `Tests\…`), composer's `psr-4 App\\ => app/` pakt deze op zodat feature-tests via container kunnen binden. `final class` met `callCounts` array + `wasCalled(string $method): int`. Inhoud per PATTERNS.md §"app/OAuth/Testing/FakeOAuthFlow.php" copy-pattern (regels 244-318):

    - 4 callCounts initialized: getAuthorizationUrl, exchangeCode, refreshToken, revoke → 0.
    - `getAuthorizationUrl(Account, array $scopes, string $state)`: increment teller; return `"https://fake.oauth.local/authorize?state={$state}"`.
    - `exchangeCode(Connection, string $code): Connection`: increment teller; `$nonce = bin2hex(random_bytes(8))`; fill connection met access_token=`access_test_fake_{$nonce}`, refresh_token=`refresh_test_fake_{$nonce}`, expires_at=now()->addHour(), scopes=['payments.read','payments.write'], status='active', oauth_state=null, oauth_state_expires_at=null; ->save(); return $connection.
    - `refreshToken(Connection): Connection`: increment teller; `$nonce = bin2hex(random_bytes(8))`; fill access_token=`access_test_fake_{$nonce}`, expires_at=now()->addHour(); ->save(); return.
    - `revoke(Connection): void`: increment teller; update(['status'=>'revoked','revoked_at'=>now()]).
    - `wasCalled(string $method): int` return `$this->callCounts[$method] ?? 0`.

    **3. tests/Feature/OAuth/OAuthFlowContractTest.php** — PHPUnit feature-test (NIET Pest), namespace `Tests\Feature\OAuth`, `use RefreshDatabase`. 3 testmethodes per PATTERNS.md §"tests/Feature/OAuth/OAuthFlowContractTest.php":

    1. `test_fake_oauth_flow_satisfies_contract()`: instantieer FakeOAuthFlow, `assertInstanceOf(OAuthFlow::class, $flow)`.
    2. `test_fake_oauth_flow_exchange_code_marks_connection_active()`: maak pending Mollie-Connection via factory state `forMollie()->pending()` (state komt uit Task 4 — als die nog niet bestaat op uitvoer-tijd, gebruik `forMollie()` en handmatig `update(['status'=>'pending','oauth_state'=>Str::random(48),'oauth_state_expires_at'=>now()->addMinutes(30)])`. Roep `$flow->exchangeCode($connection, 'fake_code')` aan, refresh, assert status='active', access_token startsWith 'access_test_fake_', `wasCalled('exchangeCode') === 1`.
    3. `test_fake_oauth_flow_revoke_sets_revoked_status()`: maak Mollie-Connection, roep `$flow->revoke($connection)`, refresh, assert status='revoked' en revoked_at niet null.

    Bewijst ROADMAP SC-4 ("OAuthFlow-contract heeft een tweede dummy-implementatie die laat zien dat het pattern niet Mollie-specifiek is").
  </action>
  <verify>
    <automated>php artisan test --compact --filter=OAuthFlowContractTest</automated>
  </verify>
  <acceptance_criteria>
    - File `app/OAuth/Contracts/OAuthFlow.php` bestaat
    - `grep -q "interface OAuthFlow" app/OAuth/Contracts/OAuthFlow.php` exits 0
    - `grep -q "public function getAuthorizationUrl(Account" app/OAuth/Contracts/OAuthFlow.php` exits 0
    - `grep -q "public function exchangeCode(Connection" app/OAuth/Contracts/OAuthFlow.php` exits 0
    - `grep -q "public function refreshToken(Connection" app/OAuth/Contracts/OAuthFlow.php` exits 0
    - `grep -q "public function revoke(Connection" app/OAuth/Contracts/OAuthFlow.php` exits 0
    - File `app/OAuth/Testing/FakeOAuthFlow.php` bestaat (NIET in `tests/`)
    - `grep -q "namespace App.OAuth.Testing" app/OAuth/Testing/FakeOAuthFlow.php` exits 0
    - `grep -q "implements OAuthFlow" app/OAuth/Testing/FakeOAuthFlow.php` exits 0
    - `grep -q "wasCalled" app/OAuth/Testing/FakeOAuthFlow.php` exits 0
    - File `tests/Feature/OAuth/OAuthFlowContractTest.php` bestaat
    - `php artisan test --compact --filter=OAuthFlowContractTest` exits 0
  </acceptance_criteria>
  <done>Contract + fixture + contract-test bestaan; tests groen; SC-4 bewezen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration + Connection-model edit + ConnectionFactory states</name>
  <files>
    database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php
    app/Models/Connection.php
    database/factories/ConnectionFactory.php
  </files>
  <read_first>
    .planning/phases/04-mollie-connect-oauth-broker/04-PATTERNS.md (sections: migration, Connection.php, ConnectionFactory.php)
    .planning/phases/04-mollie-connect-oauth-broker/04-CONTEXT.md (D-01 + D-02)
    app/Models/Connection.php (huidige fillable, casts, hidden — chirurgisch wijzigen)
    database/factories/ConnectionFactory.php (huidige forSnelstart/forMollie pattern)
    database/migrations/2026_05_14_000003_create_connections_table.php (status-kolom default='active' bestaat al — alleen kolommen toevoegen)
    database/migrations/2026_05_14_151327_add_active_unique_to_connections.php (alter-table anonymous-class pattern)
    .ai/rules/engineering.md (chirurgisch wijzigen — niet bestaande fillable/casts/hidden velden raken)
  </read_first>
  <behavior>
    - Test 1: na `php artisan migrate`, `connections`-tabel heeft kolommen `oauth_state` (varchar(64), nullable) en `oauth_state_expires_at` (timestamp, nullable). Index op `oauth_state`. Composite index `(status, oauth_state_expires_at)`.
    - Test 2: Connection-factory state `pending()` maakt rij met provider='mollie', status='pending', oauth_state=48-char string, oauth_state_expires_at=now()+30min, access_token=null.
    - Test 3: Connection-factory state `active()` zet status='active' en oauth_state=null + oauth_state_expires_at=null.
    - Test 4: Connection-factory state `expired()` zet oauth_state_expires_at op now()-1min.
    - Test 5: Connection-model accepteert `Connection::create(['account_id'=>…, 'provider'=>'mollie', 'status'=>'pending', 'oauth_state'=>'abc', 'oauth_state_expires_at'=>now()])` zonder MassAssignmentException; `$conn->oauth_state_expires_at` is een Carbon-instance (datetime cast).
  </behavior>
  <action>
    Maak 1 file aan en wijzig 2 files.

    **1. database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php** (nieuw) — Anonymous-class migration per PATTERNS.md §"migration". Inhoud:

    ```php
    <?php

    use Illuminate\Database\Migrations\Migration;
    use Illuminate\Database\Schema\Blueprint;
    use Illuminate\Support\Facades\Schema;

    return new class extends Migration
    {
        public function up(): void
        {
            Schema::table('connections', function (Blueprint $table) {
                $table->string('oauth_state', 64)->nullable()->after('scopes');
                $table->timestamp('oauth_state_expires_at')->nullable()->after('oauth_state');

                $table->index('oauth_state');
                $table->index(['status', 'oauth_state_expires_at']);
            });
        }

        public function down(): void
        {
            Schema::table('connections', function (Blueprint $table) {
                $table->dropIndex(['status', 'oauth_state_expires_at']);
                $table->dropIndex(['oauth_state']);
                $table->dropColumn(['oauth_state', 'oauth_state_expires_at']);
            });
        }
    };
    ```

    Status-kolom bestaat al (Phase 3 — `string('status')->default('active')`) — NIET wijzigen. CONTEXT.md zegt "enum: pending/active/revoked" maar PATTERNS.md regel 813 verklaart: string-kolom accepteert die 3 waardes prima; enum-validatie op model-laag. Géén DROP+RECREATE.

    **2. app/Models/Connection.php** (modify) — Chirurgisch toevoegen aan Fillable-array (na 'revoked_at') de strings `'oauth_state', 'oauth_state_expires_at'`. Aan `casts()`-array toevoegen `'oauth_state_expires_at' => 'datetime'`. NIET wijzigen: `#[Hidden]` (oauth_state is publiek — browser stuurt 'm terug), `fingerprint()`-methode, `account()` BelongsTo, andere casts. `oauth_state` blijft een rauwe string (geen encrypted cast — niet gevoelig na expiry).

    **3. database/factories/ConnectionFactory.php** (modify) — Voeg 3 methods toe ná de bestaande `forMollie()` (regel 55-56):

    ```php
    public function pending(): static
    {
        return $this->state(fn (array $attributes) => [
            'provider' => 'mollie',
            'status' => 'pending',
            'oauth_state' => Str::random(48),
            'oauth_state_expires_at' => now()->addMinutes(30),
            'access_token' => null,
            'refresh_token' => null,
            'expires_at' => null,
            'scopes' => null,
            'client_key' => null,
            'subscription_key' => null,
            'subscription_id' => null,
        ]);
    }

    public function active(): static
    {
        return $this->state(fn (array $attributes) => [
            'status' => 'active',
            'oauth_state' => null,
            'oauth_state_expires_at' => null,
        ]);
    }

    public function expired(): static
    {
        return $this->state(fn (array $attributes) => [
            'oauth_state_expires_at' => now()->subMinute(),
        ]);
    }
    ```

    Per D-02 → 30min state-TTL. `forMollie()->pending()` is "post-init"-staat; `forMollie()->active()` is "post-callback"-staat (al via bestaande forMollie()-defaults goed); `pending()->expired()` voor prune-tests.

    Run migration: `php artisan migrate --no-interaction` (forward-only invariant — bestaande tabel wordt ge-ALTERed, geen data-loss).
  </action>
  <verify>
    <automated>php artisan migrate --no-interaction --force &amp;&amp; php -r "require 'vendor/autoload.php'; (require 'bootstrap/app.php')->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); use Illuminate\Support\Facades\Schema; echo Schema::hasColumns('connections', ['oauth_state','oauth_state_expires_at']) ? 'OK' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - File `database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php` bestaat
    - `grep -q "oauth_state" database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php` exits 0
    - `grep -q "oauth_state_expires_at" database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php` exits 0
    - `grep -q "index('oauth_state')" database/migrations/2026_05_15_000001_add_oauth_state_to_connections_table.php` exits 0
    - `grep -q "'oauth_state'" app/Models/Connection.php` exits 0
    - `grep -q "'oauth_state_expires_at' => 'datetime'" app/Models/Connection.php` exits 0
    - `grep -q "public function pending(): static" database/factories/ConnectionFactory.php` exits 0
    - `grep -q "public function active(): static" database/factories/ConnectionFactory.php` exits 0
    - `grep -q "public function expired(): static" database/factories/ConnectionFactory.php` exits 0
    - `php artisan migrate --no-interaction --force` exits 0
    - `php artisan tinker --execute 'echo (\App\Models\Connection::factory()->forMollie()->pending()->make())->status;'` outputs `pending`
  </acceptance_criteria>
  <done>Migration uitgevoerd, kolommen + indexen bestaan, factory-states werken, Connection-model accepteert nieuwe velden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → connections-tabel | Schema-wijziging — moet idempotent zijn binnen forward-only invariant |
| factory-state → DB-row | Test-fixture-data mag nooit echte tokens lekken; gebruikt deterministic 'access_'/'refresh_'-prefixed test strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Migration down() | accept | Forward-only invariant — `down()` bestaat voor dev/test, in productie niet aangeroepen (CLAUDE.md §"Migrations zijn forward-only in prod"). |
| T-04-01-02 | Information Disclosure | Connection.oauth_state cast | mitigate | `oauth_state` is publiek (browser stuurt 'm terug) — geen encrypted cast. NIET toevoegen aan `#[Hidden]` om duidelijk te maken dat dit géén credential is. Wel: state-TTL van 30min (D-02) limiteert replay-window. |
| T-04-01-03 | Repudiation | Schema-change zonder migrate | accept | Plan 04-05 doet `php artisan migrate` als BLOCKING task vóór verificatie. |
</threat_model>

<verification>
- `php artisan migrate --no-interaction --force` exits 0; `connections`-tabel heeft 2 nieuwe kolommen + 2 nieuwe indexen.
- `php artisan test --compact --filter=OAuthFlowContractTest` exits 0 (3 tests passed).
- `php artisan test --compact` (full-suite) exits 0 — geen bestaande tests gebroken door Connection-model-edit.
- Grep `oauth_state` in app/Models/Connection.php matches 2 keer (Fillable-array + casts).
</verification>

<success_criteria>
- OAuthFlow-contract bestaat met 4 methods exact zoals gespec'd.
- FakeOAuthFlow implementeert OAuthFlow én leeft in app/OAuth/Testing/ (NIET tests/) — bewezen door PSR-4 autoload + container-bindbaar in Plan 04-04 tests.
- OAuthFlowContractTest groen — SC-4 ("contract niet Mollie-specifiek") bewezen.
- connections-tabel geupdate, factory-states werken, Connection-model breidt fillable+casts uit zonder bestaande contracten te raken.
- Volledige test-suite blijft groen.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mollie-connect-oauth-broker/04-01-SUMMARY.md`
</output>
