---
phase: 07-account-level-subscriptions-use-case-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_18_000001_create_account_subscriptions_table.php
  - app/Models/AccountSubscription.php
  - app/Models/Account.php
  - app/Models/Connection.php
  - database/factories/AccountSubscriptionFactory.php
  - tests/Feature/Models/AccountSubscriptionTest.php
autonomous: true
requirements: ['SUB-02']
must_haves:
  truths:
    - "Account-row kan via `$account->accountSubscriptions()` 0..n AccountSubscription-rijen ophalen"
    - "Connection-row kan via `$connection->accountSubscriptions()` 0..n AccountSubscription-rijen ophalen"
    - "Een `AccountSubscription` met status `pending` kan worden gepersist zonder Mollie-id's (mollie_subscription_id NULL toegestaan)"
    - "Twee actieve subscriptions met dezelfde `mollie_subscription_id` op dezelfde Connection faalt (partial unique index)"
  artifacts:
    - path: "database/migrations/2026_05_18_000001_create_account_subscriptions_table.php"
      provides: "AccountSubscription DDL"
      contains: "Schema::create('account_subscriptions'"
    - path: "app/Models/AccountSubscription.php"
      provides: "Eloquent model met casts + relaties"
      contains: "class AccountSubscription extends Model"
    - path: "database/factories/AccountSubscriptionFactory.php"
      provides: "Factory met pending/active/paused/canceled/forConnection states"
      contains: "public function forConnection"
  key_links:
    - from: "app/Models/AccountSubscription.php"
      to: "app/Models/Account.php"
      via: "belongsTo + hasMany"
      pattern: "belongsTo\\(Account::class\\)"
    - from: "app/Models/AccountSubscription.php"
      to: "app/Models/Connection.php"
      via: "belongsTo + hasMany"
      pattern: "belongsTo\\(Connection::class\\)"
---

<objective>
Leg de persistentie-laag van Phase 7 vast: één forward-only migration die `account_subscriptions` aanmaakt (D-03 schema + partial unique index), een Eloquent `AccountSubscription`-model met de juiste casts (D-03), `belongsTo`-relaties naar Account/Connection, een factory met `pending`/`active`/`paused`/`canceled`/`forConnection`-states (PATTERNS.md analog: `ConnectionFactory` + `PassThroughCallFactory`), en `hasMany`-relaties toegevoegd aan `Account` + `Connection`. **Geen** service-laag, **geen** state-enum (D-04 ligt in plan 07-02), **geen** controllers.

Purpose: Plan 07-02 (enum) en plan 07-03 (manager-service) hebben deze tabel + model + factory nodig als deterministische fundering. Alle subsequente feature-tests (07-06) en integration-tests (07-07) bouwen via factory-states.

Output:
- 1 nieuwe migration
- 1 nieuw model
- 1 nieuwe factory
- 2 model-mutaties (Account + Connection krijgen `accountSubscriptions()`-relatie)
- 1 feature-test die aantoont dat persist + relatie + partial unique index correct werken
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-level-subscriptions-use-case-b/07-CONTEXT.md
@.planning/phases/07-account-level-subscriptions-use-case-b/07-PATTERNS.md
@app/Models/Account.php
@app/Models/Connection.php
@app/Models/PassThroughCall.php
@database/migrations/2026_05_15_000001_create_pass_through_calls_table.php
@database/migrations/2026_05_14_151327_add_active_unique_to_connections.php
@database/factories/PassThroughCallFactory.php
@database/factories/ConnectionFactory.php

<interfaces>
<!-- Bestaande types waar dit plan op leunt -->

From app/Models/Account.php:
- `belongsTo(Consumer::class)`
- `hasMany(Connection::class)` — zelfde shape gebruiken voor `hasMany(AccountSubscription::class)`

From app/Models/Connection.php:
- Encrypted casts (`access_token`, `refresh_token`) blijven onaangeroerd
- `revoked_at` blijft bestaan; geen wijziging in connections-tabel (D-29)
- `account()` `belongsTo`-relatie aanwezig

From app/Models/PassThroughCall.php:
- `#[Fillable([...])]`-attribute (Laravel-12+ conventie in deze repo)
- `protected function casts(): array { return [...]; }` (geen `protected $casts`-array)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + model + factory + relaties</name>
  <files>
    database/migrations/2026_05_18_000001_create_account_subscriptions_table.php,
    app/Models/AccountSubscription.php,
    app/Models/Account.php,
    app/Models/Connection.php,
    database/factories/AccountSubscriptionFactory.php
  </files>
  <read_first>
    - `app/Models/PassThroughCall.php` — Fillable-attribute + casts()-method shape
    - `app/Models/Account.php` regels 18-26 — `hasMany`-pattern (chirurgisch toevoegen, niet refactoren)
    - `app/Models/Connection.php` — `account()`-relatie als referentie (chirurgisch toevoegen `hasMany(AccountSubscription::class)`)
    - `database/migrations/2026_05_15_000001_create_pass_through_calls_table.php` regels 8-34 — `Schema::create` + FK-rules + `DB::statement` voor partial-index
    - `database/migrations/2026_05_14_151327_add_active_unique_to_connections.php` regels 8-13 — partial-unique-index-syntax voor Postgres
    - `database/factories/ConnectionFactory.php` regels 29-88 — state-method-pattern
    - `database/factories/PassThroughCallFactory.php` regels 19-37 — FK-chain pattern (`Consumer::factory()` + `Account::factory()->for($consumer)`)
    - 07-CONTEXT.md §D-03 (schema) + §D-04 (state-machine waardes) + §D-29 (geen wijziging connections)
    - 07-PATTERNS.md regels 54-196 (migration + model + factory analogs)
  </read_first>
  <behavior>
    - Test 1: `AccountSubscription::factory()->forConnection($connection)->pending()->create()` persist + retournable via `$account->accountSubscriptions`-relatie (zelfde Account als die van de Connection).
    - Test 2: `$connection->accountSubscriptions` retourneert alle subscriptions op die connection (1+ stuks na 2 factory-creates met dezelfde Connection).
    - Test 3: Twee `active`-subscriptions met dezelfde `mollie_subscription_id` op dezelfde Connection → tweede insert gooit `QueryException` (partial unique index `WHERE mollie_subscription_id IS NOT NULL`). Twee `pending`-subscriptions met `mollie_subscription_id = NULL` op dezelfde Connection mag wel.
    - Test 4: Cascade-delete: `$account->delete()` ruimt alle gerelateerde `account_subscriptions`-rijen op (cascadeOnDelete FK).
    - Test 5: Restrict-delete: `$connection->delete()` met bestaande AccountSubscription gooit een `QueryException` (restrictOnDelete FK, D-03).
  </behavior>
  <action>
    Maak migration `database/migrations/2026_05_18_000001_create_account_subscriptions_table.php` aan via `php artisan make:migration --no-interaction create_account_subscriptions_table` en hernoem zo nodig naar het pad hierboven. Migration body volgt CONTEXT.md D-03 letterlijk:
    - `$table->id();`
    - `$table->foreignId('account_id')->constrained('accounts')->cascadeOnDelete();`
    - `$table->foreignId('connection_id')->constrained('connections')->restrictOnDelete();`
    - `$table->string('mollie_customer_id')->index();`
    - `$table->string('mollie_subscription_id')->nullable();`
    - `$table->string('mollie_mandate_id')->nullable();`
    - `$table->string('status')->index();` (default `'pending'` — string-cast nu, enum-cast komt in plan 07-02)
    - `$table->char('amount_currency', 3)->default('EUR');`
    - `$table->string('amount_value');` (decimal als string — Mollie-shape, D-03)
    - `$table->string('interval');`
    - `$table->string('description');`
    - `$table->unsignedInteger('times')->nullable();`
    - `$table->date('start_date')->nullable();`
    - `$table->timestamp('starts_at')->nullable();`
    - `$table->timestamp('paused_at')->nullable();`
    - `$table->timestamp('canceled_at')->nullable();`
    - `$table->timestamp('completed_at')->nullable();`
    - `$table->jsonb('metadata')->nullable();`
    - `$table->string('last_payment_status')->nullable();`
    - `$table->timestamp('last_webhook_event_at')->nullable();`
    - `$table->timestamps();`
    - `$table->index(['account_id', 'status']);`
    - `$table->index(['connection_id', 'status']);`
    Direct erna in dezelfde `up()` (na `Schema::create`-callback):
    - `DB::statement("CREATE UNIQUE INDEX account_subscriptions_connection_mollie_sub_unique ON account_subscriptions (connection_id, mollie_subscription_id) WHERE mollie_subscription_id IS NOT NULL");`
    Geen `down()` (PROJECT.md forward-only invariant) — laat de standaard `down()`-method weg of stub die met een no-op-comment.

    Maak `app/Models/AccountSubscription.php` aan via `php artisan make:model --factory --no-interaction AccountSubscription`. Vervang gegenereerde body door:
    - Namespace `App\Models`
    - `#[Fillable([...])]`-attribute met alle 19 D-03-velden (exclusief `id`/`created_at`/`updated_at`)
    - `use HasFactory; /** @use HasFactory<AccountSubscriptionFactory> */`
    - `protected function casts(): array` met: `metadata => 'array'`, `start_date => 'date'`, `starts_at/paused_at/canceled_at/completed_at/last_webhook_event_at => 'datetime'`, `times => 'integer'`. **NIET** `status => SubscriptionStatus::class` — die cast wordt toegevoegd in plan 07-02 (vermijdt class-not-found tijdens 07-01-tests).
    - Relaties: `public function account(): BelongsTo { return $this->belongsTo(Account::class); }` en `public function connection(): BelongsTo { return $this->belongsTo(Connection::class); }`.

    Voeg in `app/Models/Account.php` exact 1 method toe (vóór de closing brace, achter bestaande `connections()`-relatie):
    `public function accountSubscriptions(): HasMany { return $this->hasMany(AccountSubscription::class); }`
    Zelfde method-naam in `app/Models/Connection.php`. Geen andere edits in deze twee files (engineering.md "chirurgisch wijzigen").

    Maak `database/factories/AccountSubscriptionFactory.php` aan via `php artisan make:factory --no-interaction --model=AccountSubscription AccountSubscriptionFactory`. `definition()` retourneert de pending-default-shape:
    - FK-chain: `$account = Account::factory(); $connection = Connection::factory()->forMollie()->for($account);` → `account_id => $account`, `connection_id => $connection`
    - `mollie_customer_id => 'cst_'.fake()->bothify('??????????')`
    - `mollie_subscription_id => null` (default = pending, geen Mollie-id)
    - `mollie_mandate_id => null`
    - `status => 'pending'`
    - `amount_currency => 'EUR'`, `amount_value => '10.00'`, `interval => '1 month'`, `description => 'Test bijdrage'`
    - rest van timestamps/nullable-velden default `null`
    State-methodes (mirror ConnectionFactory regels 29-88):
    - `public function pending(): static` → status `pending`, mollie_subscription_id NULL
    - `public function active(): static` → status `active`, mollie_subscription_id `'sub_'.fake()->bothify('??????????')`, starts_at `now()`
    - `public function paused(): static` → status `paused`, paused_at `now()`, mollie_subscription_id gevuld
    - `public function canceled(): static` → status `canceled`, canceled_at `now()`, mollie_subscription_id gevuld
    - `public function forConnection(Connection $connection): static` → zet `connection_id => $connection->id`, `account_id => $connection->account_id` (zodat tests een specifieke Connection kunnen pinnen).

    Run `php artisan migrate:fresh --seed` om schema-correctheid te bewijzen. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    php artisan migrate:fresh --seed &&
    php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$k = \$app->make(Illuminate\Contracts\Console\Kernel::class); \$k->bootstrap(); echo (Illuminate\Support\Facades\Schema::hasTable('account_subscriptions') && Illuminate\Support\Facades\Schema::hasColumn('account_subscriptions','mollie_subscription_id')) ? 'OK' : 'FAIL';" &&
    grep -q "hasMany(AccountSubscription::class)" app/Models/Account.php &&
    grep -q "hasMany(AccountSubscription::class)" app/Models/Connection.php &&
    grep -q "CREATE UNIQUE INDEX account_subscriptions_connection_mollie_sub_unique" database/migrations/2026_05_18_000001_create_account_subscriptions_table.php
    </automated>
  </verify>
  <done>
    - `php artisan migrate:fresh --seed` exit 0
    - `account_subscriptions`-tabel bestaat met alle 19 D-03-kolommen
    - Partial unique-index `account_subscriptions_connection_mollie_sub_unique` is gecreëerd
    - `Account::factory()->create()->accountSubscriptions()` werkt syntactisch (relatie geregistreerd)
    - `Connection::factory()->forMollie()->create()->accountSubscriptions()` werkt syntactisch
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feature-test voor persist + relaties + partial unique + cascade/restrict</name>
  <files>tests/Feature/Models/AccountSubscriptionTest.php</files>
  <read_first>
    - `tests/Feature/Models/ConnectionEncryptionTest.php` (Phase 3) — RefreshDatabase + model-laag-tests pattern
    - `tests/Feature/Models/ConsumerAccountScopingTest.php` — relatie-asserts via factory-builders
    - 07-CONTEXT.md §D-03 (cascade-rules) + §D-29 (geen wijziging connections-tabel)
  </read_first>
  <behavior>
    - Test `creates_account_subscription_via_factory_in_pending_state` — happy-path persist + cast-check (`status === 'pending'`, `metadata` is null-castable naar array).
    - Test `account_can_access_subscriptions_via_relation` — `$account->accountSubscriptions` is een Collection met 1 element na 1 factory-create.
    - Test `connection_can_access_subscriptions_via_relation` — idem voor Connection.
    - Test `partial_unique_index_blocks_duplicate_active_mollie_subscription_id` — twee `active`-subscriptions met dezelfde `mollie_subscription_id` + dezelfde `connection_id` → expectException QueryException (Postgres unique-violation).
    - Test `partial_unique_index_allows_multiple_pending_with_null_mollie_subscription_id` — twee `pending`-subscriptions zonder mollie_subscription_id op zelfde Connection → succes.
    - Test `cascade_delete_account_removes_subscriptions` — `$account->delete()` → `AccountSubscription::count() === 0`.
    - Test `restrict_delete_connection_with_subscription_throws` — `$connection->delete()` met bestaande sub → expectException QueryException.
  </behavior>
  <action>
    Maak het feature-test-bestand via `php artisan make:test --phpunit Models/AccountSubscriptionTest --no-interaction`. Use `RefreshDatabase`. Schrijf 7 tests die de behaviors uit `<behavior>` dekken — happy/relatie/partial-unique-violation/null-allow/cascade/restrict. Gebruik PHPUnit `expectException(Illuminate\Database\QueryException::class)` voor de DB-constraint-asserts. Run via `php artisan test --compact --filter=AccountSubscriptionTest`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=AccountSubscriptionTest</automated>
  </verify>
  <done>
    - 7 tests groen in `AccountSubscriptionTest`
    - Geen regressie in volledige suite: `php artisan test --compact` exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-laag → app | AccountSubscription-rijen worden later geschreven vanaf Hub-API (plan 07-04) en webhook-ingress (plan 07-05); Phase 7-01 levert het schema waarop die endpoints leunen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `account_subscriptions.mollie_subscription_id` duplicate-insert via concurrent requests | mitigate | Partial unique index `(connection_id, mollie_subscription_id) WHERE mollie_subscription_id IS NOT NULL` (D-03); test `partial_unique_index_blocks_duplicate_active_mollie_subscription_id` bewijst constraint actief. |
| T-07-01-02 | Information Disclosure | Encrypted-at-rest leakage door nieuwe tabel | accept | Mollie-id's (`cst_*`, `sub_*`, `mdt_*`) zijn opaque references, GEEN secrets (D-02). Geen `encrypted` casts nodig; tokens blijven op `connections`-tabel uit Phase 3. |
| T-07-01-03 | Denial of Service | Cascade-delete-storm bij `account.delete()` | accept | `cascadeOnDelete` op `account_id` is per design — Account-delete is admin-action via Phase 9 (geen public route). Restrict op `connection_id` voorkomt accidentele revoke-cascades. |
| T-07-01-04 | Tampering | Mass-assignment via `->fill($request->all())` op model | mitigate | `#[Fillable([...])]`-attribute met explicit whitelist (geen `id`/`created_at`/`updated_at`). Controllers in plan 07-04 gebruiken Form Request `validated()`-output, geen rauwe request. |
</threat_model>

<verification>
1. `php artisan migrate:fresh --seed` exit 0
2. Schema-check via `database-schema` MCP-tool of Postgres `\d account_subscriptions` toont alle 19 kolommen + 3 indexes (waarvan 1 partial unique)
3. `php artisan test --compact --filter=AccountSubscriptionTest` exit 0 (7 tests groen)
4. `php artisan test --compact` exit 0 (geen regressie op Phase 5a/6-tests)
5. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- Migration + model + factory + relaties bestaan en zijn getest
- Partial unique index actief en bewezen via failing-then-passing test
- Geen wijziging in `connections`-tabel (D-29)
- Geen state-enum cast op model (komt in plan 07-02)
- Plan 07-02 + 07-03 kunnen op deze fundering bouwen
</success_criteria>

<output>
Create `.planning/phases/07-account-level-subscriptions-use-case-b/07-01-SUMMARY.md` when done
</output>
