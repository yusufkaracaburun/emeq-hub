---
phase: 09-filament-admin-ui-voor-emeq-medewerkers
plan: 08
type: execute
wave: 3
depends_on: ['09-03']
files_modified:
  - app/Filament/Resources/AccountSubscriptionResource.php
  - app/Filament/Resources/AccountSubscriptionResource/Pages/ListAccountSubscriptions.php
  - app/Filament/Resources/AccountSubscriptionResource/Pages/ViewAccountSubscription.php
  - tests/Feature/Admin/AccountSubscriptionResourceTest.php
  - tests/Feature/Admin/AccountSubscriptionStateActionsTest.php
autonomous: true
requirements: ['HUB-04']
must_haves:
  truths:
    - "`/admin/account-subscriptions` is bereikbaar voor een staff-User en toont een tabel met `account.external_id`, `connection.provider`, `status` (BadgeColumn — 6 states), `amount`, `interval`, `description`, `last_webhook_event_at`"
    - "Tabel heeft filters op `status` (SelectFilter via SubscriptionStatus-enum), `connection.provider`, `account_id`"
    - "Detail-view toont `mollie_customer_id`/`mollie_subscription_id`/`mollie_mandate_id` (opaque Mollie-IDs, geen secrets per Phase 7 D-02)"
    - "Pause/Resume/Cancel actions roepen `AccountSubscriptionManager::pause/resume/cancel` aan — NOOIT direct `$sub->update(['status' => ...])`"
    - "Pause/Resume/Cancel actions respecteren state-machine — Cancel op een `canceled`-subscription gooit een Filament-validation-error zonder DB-mutatie (D-04 invariant uit Phase 7 + CONTEXT.md success-criterium 8)"
  artifacts:
    - path: "app/Filament/Resources/AccountSubscriptionResource.php"
      provides: "Filament Resource met state-flip table-actions"
      contains: "class AccountSubscriptionResource extends Resource"
    - path: "tests/Feature/Admin/AccountSubscriptionStateActionsTest.php"
      provides: "Tests die state-machine-respect en manager-delegation bewijzen"
      contains: "class AccountSubscriptionStateActionsTest"
  key_links:
    - from: "app/Filament/Resources/AccountSubscriptionResource.php"
      to: "app/Billing/Account/AccountSubscriptionManager.php"
      via: "table-actions delegeren naar manager-methoden (pause/resume/cancel)"
      pattern: "AccountSubscriptionManager"
    - from: "app/Filament/Resources/AccountSubscriptionResource.php"
      to: "app/Billing/Account/SubscriptionStatus.php"
      via: "BadgeColumn + SelectFilter via enum"
      pattern: "SubscriptionStatus::"
---

<objective>
Bouw `AccountSubscriptionResource` als read-only Resource met 3 mutate-actions die exclusief via `AccountSubscriptionManager` (Phase 7-03 service) lopen. Filament-tabel toont 6 states via BadgeColumn met kleuren-map. Filters op status/provider/account. Pause/Resume/Cancel actions zijn alleen zichtbaar als de huidige state een legale transitie toelaat (state-machine respect via Phase 7's `StateTransitions`-class). Bij illegale transitie: action gooit `IllegalStateTransitionException` → Filament toont Notification-error zonder DB-mutatie (T-07-03-03 invariant).

Geen Create/Edit-pages. Create gebeurt via `POST /v1/account-subscriptions` (Phase 7-04).

Purpose: HUB-04 success-criterium 8 ("Cancel op canceled-subscription gooit validation-error zonder mutatie") + ADR `account-subscriptions.md` (Phase 7) per-Consumer scope-niveau.

Output:
- 1 Resource-class + 2 Page-classes (List + View)
- 2 nieuwe feature-tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-CONTEXT.md
@.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-PATTERNS.md
@app/Models/AccountSubscription.php
@app/Billing/Account/AccountSubscriptionManager.php
@app/Billing/Account/SubscriptionStatus.php
@app/Billing/Account/StateTransitions.php
@database/factories/AccountSubscriptionFactory.php

<interfaces>
<!-- AccountSubscription-model -->
From app/Models/AccountSubscription.php:
- `belongsTo(Account::class)` + `belongsTo(Connection::class)`
- `status` cast naar `SubscriptionStatus`-enum
- 4 timestamp-casts (`starts_at`, `paused_at`, `canceled_at`, `completed_at`, `last_webhook_event_at`)
- `metadata` cast naar array

<!-- SubscriptionStatus enum (6 states) -->
From app/Billing/Account/SubscriptionStatus.php:
- Pending = 'pending'
- Active = 'active'
- Paused = 'paused'
- Canceled = 'canceled'
- Completed = 'completed'
- Unknown = 'unknown'

<!-- AccountSubscriptionManager (state-flip single-entry-point) -->
From app/Billing/Account/AccountSubscriptionManager.php:
- `pause(AccountSubscription $sub): void`
- `resume(AccountSubscription $sub): void`
- `cancel(AccountSubscription $sub): void`
Alle 3 methoden gooien `IllegalStateTransitionException` (uit StateTransitions::assertTransition) bij ongeldige overgang. **NOOIT** `$sub->update(['status' => ...])` direct — Phase 7 invariant.

<!-- StateTransitions -->
From app/Billing/Account/StateTransitions.php:
- Legal pause: active → paused
- Legal resume: paused → active
- Legal cancel: active|paused → canceled
- Self-transitions zijn idempotent (Phase 7-02 decision)
- Illegal: cancel op canceled, pause op pending, etc.

<!-- Factory state-methodes -->
From database/factories/AccountSubscriptionFactory.php:
- `pending()`, `active()`, `paused()`, `canceled()`, `forConnection($connection)` states aanwezig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resource-skelet + table + filters + view-detail</name>
  <files>
    app/Filament/Resources/AccountSubscriptionResource.php,
    app/Filament/Resources/AccountSubscriptionResource/Pages/ListAccountSubscriptions.php,
    app/Filament/Resources/AccountSubscriptionResource/Pages/ViewAccountSubscription.php
  </files>
  <read_first>
    - 09-PATTERNS.md regels 187-206 — AccountSubscriptionResource (read + state-flip) pattern
    - `app/Models/AccountSubscription.php` (volledig) — fields + casts + relaties
    - `app/Billing/Account/SubscriptionStatus.php` — 6 enum cases
    - 09-CONTEXT.md §"Plan 09-04 AccountSubscriptionResource" (regels 178-179)
    - Filament v4-docs: `filament v4 badge column enum`, `filament v4 select filter enum`
  </read_first>
  <acceptance_criteria>
    - `php artisan make:filament-resource AccountSubscription --view --no-interaction` genereert 3 files
    - Tabel-kolommen: `account.external_id` (TextColumn), `connection.provider` (BadgeColumn), `status` (BadgeColumn met kleuren: pending=warning, active=success, paused=info, canceled=danger, completed=gray, unknown=gray), `amount` (TextColumn `->state(fn ($record) => $record->amount_value.' '.$record->amount_currency)`), `interval` (TextColumn), `description` (TextColumn), `last_webhook_event_at` (TextColumn datetime sortable)
    - Filters: `SelectFilter::make('status')->options(collect(SubscriptionStatus::cases())->mapWithKeys(fn ($s) => [$s->value => $s->name])->all())`, `SelectFilter::make('connection_provider')->relationship('connection', 'provider')`, `SelectFilter::make('account_id')->relationship('account', 'external_id')->label('Account')`
    - ViewPage / infolist toont: alle tabel-kolommen + `mollie_customer_id` + `mollie_subscription_id` + `mollie_mandate_id` + alle 4 status-timestamps + `metadata` (JSON pretty)
    - Geen Create/Edit
    - Pint clean
    - `/admin/account-subscriptions` returnt 200 voor staff-User
  </acceptance_criteria>
  <action>
    1. Run `php artisan make:filament-resource AccountSubscription --view --no-interaction`. Verifieer 3 files.
    2. Set `protected static ?string $model = AccountSubscription::class;` + `protected static ?string $navigationIcon = 'heroicon-o-arrow-path';`.
    3. Imports: `App\Models\AccountSubscription`, `App\Billing\Account\SubscriptionStatus`, `Filament\Tables\Columns\TextColumn`, `Filament\Tables\Filters\SelectFilter`.
    4. `table()`: 7 kolommen + 3 filters zoals beschreven in acceptance_criteria.
    5. Status-BadgeColumn kleuren-map: gebruik `->color(fn (SubscriptionStatus $state) => match($state) { SubscriptionStatus::Active => 'success', SubscriptionStatus::Paused => 'info', SubscriptionStatus::Canceled => 'danger', SubscriptionStatus::Pending => 'warning', SubscriptionStatus::Completed => 'gray', SubscriptionStatus::Unknown => 'gray' })`.
    6. View-detail: gebruik `Filament\Infolists\Components\TextEntry` voor alle relevant velden incl. de 3 Mollie-IDs (per Phase 7 D-02 zijn deze opaque references, geen secrets — OK om te tonen).
    7. `getPages()`: alleen `index` + `view`.
    8. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    test -f app/Filament/Resources/AccountSubscriptionResource.php &&
    test -f app/Filament/Resources/AccountSubscriptionResource/Pages/ListAccountSubscriptions.php &&
    test -f app/Filament/Resources/AccountSubscriptionResource/Pages/ViewAccountSubscription.php &&
    grep -q "AccountSubscription::class" app/Filament/Resources/AccountSubscriptionResource.php &&
    grep -q "SubscriptionStatus" app/Filament/Resources/AccountSubscriptionResource.php &&
    grep -q "make('status')" app/Filament/Resources/AccountSubscriptionResource.php &&
    php artisan route:list --path=admin/account-subscriptions 2>&1 | grep -q "admin/account-subscriptions"
    </automated>
  </verify>
  <done>
    - 3 files aangemaakt
    - Tabel met 7 kolommen + status-BadgeColumn + 3 filters
    - View-detail toont Mollie-IDs + timestamps + metadata
    - Pint clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Pause/Resume/Cancel table-actions met state-machine-respect</name>
  <files>app/Filament/Resources/AccountSubscriptionResource.php</files>
  <read_first>
    - `app/Billing/Account/AccountSubscriptionManager.php` (volledig) — pause/resume/cancel signatures + IllegalStateTransitionException
    - `app/Billing/Account/StateTransitions.php` — legale transities
    - 09-CONTEXT.md §"Plan 09-04 AccountSubscriptionResource" (regel 179) — manager-only delegation
    - 09-PATTERNS.md regels 200-206 — manager-delegation invariant
    - Filament v4-docs: `filament v4 table action visible condition`, `filament v4 action exception handling`
  </read_first>
  <acceptance_criteria>
    - 3 `Tables\Actions\Action`-instances toegevoegd aan `table()->actions([...])`:
      - `Action::make('pause')` — `->visible(fn (AccountSubscription $record) => $record->status === SubscriptionStatus::Active)` + `->requiresConfirmation()` + `->action(fn (AccountSubscription $record) => app(AccountSubscriptionManager::class)->pause($record))` + Notification op success
      - `Action::make('resume')` — `->visible(fn ($record) => $record->status === SubscriptionStatus::Paused)` + zelfde shape met `->resume()`
      - `Action::make('cancel')` — `->visible(fn ($record) => in_array($record->status, [SubscriptionStatus::Active, SubscriptionStatus::Paused]))` + `->requiresConfirmation()` + `->color('danger')` + zelfde shape met `->cancel()`
    - Bij `IllegalStateTransitionException`: catch + `Notification::make()->title('Ongeldige state-transitie')->danger()->send();` (Filament v4 handelt exceptions normaal automatisch — verifieer of een expliciete try/catch nodig is via Context7)
    - Pint clean
  </acceptance_criteria>
  <behavior>
    - Pause zichtbaar alleen op Active
    - Resume zichtbaar alleen op Paused
    - Cancel zichtbaar op Active OF Paused
    - Op illegale transitie: geen DB-mutatie + Filament-error-notification
  </behavior>
  <action>
    1. Imports toevoegen: `App\Billing\Account\AccountSubscriptionManager`, `App\Billing\Account\Exceptions\IllegalStateTransitionException` (Phase 7-02 exception), `Filament\Notifications\Notification`, `Filament\Tables\Actions\Action`.

    2. In `table()->actions([...])` blok, voeg 3 actions toe zoals beschreven in acceptance_criteria.

       Voorbeeld pause-action:
       ```
       Action::make('pause')
           ->visible(fn (AccountSubscription $record) => $record->status === SubscriptionStatus::Active)
           ->requiresConfirmation()
           ->modalHeading('Subscription pauzeren')
           ->action(function (AccountSubscription $record): void {
               try {
                   app(AccountSubscriptionManager::class)->pause($record);
                   Notification::make()->title('Subscription gepauzeerd')->success()->send();
               } catch (IllegalStateTransitionException $e) {
                   Notification::make()->title('Ongeldige state-transitie')->body($e->getMessage())->danger()->send();
               }
           })
       ```
       Idem voor resume + cancel (cancel met `->color('danger')`).

    3. Run `./vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>
    grep -q "AccountSubscriptionManager" app/Filament/Resources/AccountSubscriptionResource.php &&
    grep -q "Action::make('pause')" app/Filament/Resources/AccountSubscriptionResource.php &&
    grep -q "Action::make('resume')" app/Filament/Resources/AccountSubscriptionResource.php &&
    grep -q "Action::make('cancel')" app/Filament/Resources/AccountSubscriptionResource.php &&
    ! grep -E "->update\\(\\[?'status'" app/Filament/Resources/AccountSubscriptionResource.php
    </automated>
  </verify>
  <done>
    - 3 actions wired naar AccountSubscriptionManager
    - Geen direct `$sub->update(['status' => ...])` in Resource (T-07-03-03 invariant)
    - Pint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AccountSubscriptionResourceTest + AccountSubscriptionStateActionsTest</name>
  <files>
    tests/Feature/Admin/AccountSubscriptionResourceTest.php,
    tests/Feature/Admin/AccountSubscriptionStateActionsTest.php
  </files>
  <read_first>
    - `tests/Feature/Api/V1/AccountSubscriptions/ListAccountSubscriptionsTest.php` — Phase 7 reference + Livewire-action-test pattern
    - `app/Billing/Account/StateTransitions.php` + Phase 7-02 tests — illegale transities
    - 09-PATTERNS.md regels 187-206
  </read_first>
  <acceptance_criteria>
    - `AccountSubscriptionResourceTest` heeft 3 tests:
      1. `test_list_shows_subscriptions_for_staff` — maak 2 subscriptions in verschillende statuses → beide zichtbaar
      2. `test_status_filter_narrows_to_active` — filter `status=active` → alleen active-rij
      3. `test_view_page_shows_mollie_ids` — GET view-page → assertSee de Mollie-customer-id/subscription-id/mandate-id
    - `AccountSubscriptionStateActionsTest` heeft 5 tests:
      1. `test_pause_action_visible_on_active_subscription_only` — Active → visible, Paused/Canceled/Pending → hidden
      2. `test_pause_action_flips_status_via_manager` — Active → callTableAction('pause') → status === Paused; assert NO direct update (manager moet zijn aangeroepen, gecheckt via DB-resultaat)
      3. `test_resume_action_visible_only_on_paused_subscription`
      4. `test_cancel_action_visible_on_active_and_paused_not_canceled`
      5. `test_cancel_action_on_already_canceled_throws_validation_notification_without_db_mutation` — Cancel op een al-canceled subscription → action `->visible()` filtert het uit; als de action geforceerd wordt (via direct Livewire-call) en `IllegalStateTransitionException` wordt gegooid, dan blijft `status === Canceled` en geen extra mutatie

    - Alle 8 tests groen
    - Full suite groen
  </acceptance_criteria>
  <behavior>
    - List + filter werken
    - Pause/Resume/Cancel respecteren state-machine
    - Illegal cancel laat geen DB-mutatie achter
  </behavior>
  <action>
    **AccountSubscriptionResourceTest:** Maak file aan. Use `RefreshDatabase`. Helper `seedRoles()` + acting staff. Imports `App\Models\AccountSubscription`, etc.

    Test 1: Maak 2 subs via factory states (`active()` + `paused()`). Livewire-test `->assertCanSeeTableRecords([...])`.

    Test 2: Setup zelfde. Livewire `->filterTable('status', SubscriptionStatus::Active->value)->assertCanSeeTableRecords([$active])->assertCanNotSeeTableRecords([$paused]);`.

    Test 3: Maak sub met specifieke mollie-IDs (factory `->state(['mollie_customer_id' => 'cst_TEST_VIEW', 'mollie_subscription_id' => 'sub_TEST_VIEW', 'mollie_mandate_id' => 'mdt_TEST_VIEW'])`). GET `/admin/account-subscriptions/{id}` → assertSee 3 IDs.

    **AccountSubscriptionStateActionsTest:** Maak file aan. Imports incl. `App\Billing\Account\AccountSubscriptionManager`, `App\Billing\Account\SubscriptionStatus`.

    Test 1: Per status, asserteer visibility van pause-action via Livewire's `assertTableActionVisible('pause', $record)` / `assertTableActionHidden`.

    Test 2: Active-sub → `Livewire::test(ListAccountSubscriptions::class)->callTableAction('pause', $active)->assertHasNoTableActionErrors();`. Refresh `$active->fresh()->status === SubscriptionStatus::Paused`. (Phase 7 manager doet ook Mollie-API-call — gebruik FakeMollieClient of `Mollie::fake()` om upstream te muten. Verifieer pattern via Phase 7-03 tests.)

    Test 3+4: Visibility-tests zoals Test 1.

    Test 5: Maak Canceled-sub. Cancel-action `->visible()` filtert die uit. Asserteer `assertTableActionHidden('cancel', $canceled)`. Daarna manueel: `$this->expectException(IllegalStateTransitionException::class); app(AccountSubscriptionManager::class)->cancel($canceled);` — bewijst dat manager early-fail toepast. Daarna: `$this->assertSame(SubscriptionStatus::Canceled, $canceled->fresh()->status);` (geen mutatie).

    Run `php artisan test --compact --filter='AccountSubscriptionResourceTest|AccountSubscriptionStateActionsTest'`.
  </action>
  <verify>
    <automated>
    php artisan test --compact --filter=AccountSubscriptionResourceTest &&
    php artisan test --compact --filter=AccountSubscriptionStateActionsTest &&
    php artisan test --compact
    </automated>
  </verify>
  <done>
    - 8 tests groen
    - Full suite groen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filament admin → AccountSubscriptionManager → Mollie API | Pause/Resume/Cancel triggert upstream Mollie subscription state-flip via Phase 7's manager. State-machine guards in Hub voorkomen illegale transities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Tampering | Filament-action bypassed manager → direct `$sub->update(['status' => ...])` → state-machine-skip | mitigate | Action delegates ALLEEN naar `AccountSubscriptionManager::pause/resume/cancel`. Test 2 in StateActionsTest bewijst dat status-flip via manager loopt. Verify-grep in `<verify>` checkt afwezigheid van `->update(['status'` in Resource-class. |
| T-09-08-02 | Tampering | Race-condition: 2 staff-users gelijktijdig Cancel-action triggeren | accept | Manager-laag in Phase 7 heeft DB-transactie + state-check. Filament-action delegates atomic naar manager. Risico: dubbele Mollie-API-call (idempotency via mollie-id). |
| T-09-08-03 | Information Disclosure | Mollie-customer-id / subscription-id / mandate-id zijn opaque, GEEN secrets per Phase 7-01 D-02 | accept | Documented. View-page mag deze IDs tonen — Mollie-dashboard-cross-reference is gewenst voor debugging. |
| T-09-08-04 | Elevation of Privilege | Staff zonder `view-account-subscriptions`-permission ziet Resource | mitigate | Phase 9-03 EmeqStaffSeeder geeft `staff`-role de `view-account-subscriptions` permission. Plan 09-11 ADR documenteert. Toekomstige tightening: per-permission Resource-gating via `canAccess()` (backlog). |
| T-09-08-05 | Denial of Service | Mollie API onbereikbaar bij Cancel-action → unhandled exception | mitigate | Try/catch in action-callback vangt `IllegalStateTransitionException`; andere exceptions (network/Mollie 5xx) bubblen tot Filament's generic error-notification. Documenteer in ADR. |
| T-09-08-SC | Tampering | Geen package-install | accept | Plan 09-08 wijzigt alleen Resource + tests. |
</threat_model>

<verification>
1. `/admin/account-subscriptions` returnt 200 voor staff-User
2. `php artisan test --compact --filter=AccountSubscriptionResourceTest` exit 0 (3 tests groen)
3. `php artisan test --compact --filter=AccountSubscriptionStateActionsTest` exit 0 (5 tests groen)
4. `php artisan test --compact` exit 0 (geen regressie op Phase 7's 337 tests)
5. `./vendor/bin/pint --dirty --format agent` clean
</verification>

<success_criteria>
- AccountSubscriptionResource read + 3 state-flip actions
- State-machine respect bewezen (Pause op niet-Active = hidden; Cancel op canceled = no-mutate)
- Manager-only delegation (T-07-03-03 invariant)
- HUB-04 success-criterium 8 bewezen
</success_criteria>

<output>
Create `.planning/phases/09-filament-admin-ui-voor-emeq-medewerkers/09-08-SUMMARY.md` when done
</output>
