---
phase: 06-cashier-mollie-integratie-use-case-a
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SUB-01]
tags: [adr, compat-check, cashier-mollie, research]
files_modified:
  - .docs/decisions/cashier-mollie-compat.md
  - .planning/REQUIREMENTS.md
user_setup: []

must_haves:
  truths:
    - "ADR `.docs/decisions/cashier-mollie-compat.md` bestaat en bevat een expliciete keuze tussen pad (a) out-of-box, (b) fork-and-patch, of (c) eigen subscription-laag (Phase 6 SC-1)."
    - "De ADR-conclusie is verifieerbaar onderbouwd: bevat `mollie/laravel-cashier-mollie` master HEAD's PHP- en Laravel-constraints uit composer.json plus een dry-run-uitkomst óf het gedocumenteerde alternatief (waarom dry-run niet kon)."
    - "REQUIREMENTS.md SUB-01 status is bijgewerkt naar `In Progress (06-01 done — compat-check landed)`."
    - "Geen productiecode aangeraakt: `composer.json`, `composer.lock`, `app/`, `routes/`, `database/`, `tests/`, `config/` blijven byte-identiek aan HEAD vóór dit plan."
  artifacts:
    - path: ".docs/decisions/cashier-mollie-compat.md"
      provides: "Compat-check ADR met gekozen pad (a/b/c) + onderbouwing"
      contains: "## Decision"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SUB-01 traceability-rij bijgewerkt"
      contains: "06-01 done"
  key_links:
    - from: ".docs/decisions/cashier-mollie-compat.md"
      to: ".planning/REQUIREMENTS.md"
      via: "expliciete cross-ref vanuit REQUIREMENTS.md SUB-01 status-cell naar de ADR-path"
      pattern: "06-01"
    - from: ".docs/decisions/cashier-mollie-compat.md §Decision"
      to: ".planning/phases/06-cashier-mollie-integratie-use-case-a/06-DEFERRED-PLANS.md"
      via: "ADR's `## Next Steps` sectie verwijst expliciet naar de deferred-plans-set die door dit pad geactiveerd wordt"
      pattern: "06-DEFERRED-PLANS"
---

<objective>
Bepaal de werkelijke PHP 8.4 / Laravel 13 compat-status van `mollie/laravel-cashier-mollie` (master HEAD) en kies één van drie paden voor de uitvoering van Phase 6: (a) out-of-box composer require, (b) fork-and-patch, of (c) eigen subscription-laag bovenop `emeq/mollie-api`.

Dit plan levert UITSLUITEND een ADR — geen productiecode, geen migrations, geen tests, geen composer-edits. De ADR-conclusie is de input voor Plan 06-02 t/m 06-08 (zie `06-DEFERRED-PLANS.md`). Plannen 02+ worden pas geschreven nadat dit plan committed is, omdat hun inhoud volledig afhangt van pad (a) vs (b) vs (c).

Purpose: De compat-risk uit STATE.md (`mollie/laravel-cashier-mollie` master hangt historisch op PHP 7.2 / Laravel 6-8) staat al sinds milestone-kickoff geparkeerd als "te onderzoeken bij use-case A". Zonder deze ADR is verder plannen verspilling — drie wezenlijk verschillende code-paden zouden parallel moeten worden uitgewerkt.

Output: één markdown-ADR + één REQUIREMENTS.md status-update + één git commit. Phase 6 SC-1 (compat-check gedocumenteerd in `.docs/decisions/`) is daarmee bewezen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@composer.json

<interfaces>
<!-- Geen code-interfaces aangeraakt in dit plan. ADR is een markdown-deliverable. -->
<!-- Wel relevant: bestaande ADR-conventie voor `.docs/decisions/`. -->

Bestaande ADRs (referentie-stijl te matchen):
- `.docs/decisions/hosting-platform.md`
- `.docs/decisions/intern-admin-ui-filament.md`
- `.docs/decisions/mollie-passthrough-api.md`
- `.docs/decisions/pass-through-calls-table.md`
- `.docs/decisions/snelstart-certificering-pad.md`
- `.docs/decisions/upstream-error-mapping.md`

Conventie: titel "# ADR-XX: <titel>", secties `## Status`, `## Context`, `## Decision`, `## Consequences`, optioneel `## Next Steps` / `## Onderbouwing`. Lees minstens één bestaande ADR vóór schrijven om frontmatter/secties exact te matchen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compat-check uitvoeren — bepaal PHP/Laravel constraints van mollie/laravel-cashier-mollie master HEAD</name>
  <read_first>
    - `.docs/decisions/mollie-passthrough-api.md` (ADR-stijl-template; structuur en toon hergebruiken)
    - `.docs/decisions/snelstart-certificering-pad.md` (ADR met een vergelijkbare drie-paden-keuze; copy-structure als anchor)
    - `composer.json` (huidige Hub-stack: PHP `^8.4`, Laravel `^13.8`, geen Cashier-Mollie aanwezig)
    - `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md` §D-01 + D-02 (decision-tree definitie van pad a/b/c)
  </read_first>
  <files>(geen — alleen research; geen file wordt in deze task gecreëerd)</files>
  <action>
    Voer een drie-stappen compat-check uit en verzamel concrete cijfers/strings. Sla de uitkomsten op in een lokale notitie (scratchpad of variabelen in je werkgeheugen) — die worden in Task 2 in de ADR verwerkt.

    **Stap A — Lees master HEAD's composer.json via WebFetch:**

    URL: `https://raw.githubusercontent.com/mollie/laravel-cashier-mollie/master/composer.json`

    Extract en log letterlijk:
    1. `require.php` constraint (string)
    2. `require."illuminate/contracts"` of `require."laravel/framework"` constraint (string)
    3. `require."mollie/laravel-mollie"` constraint (string)
    4. Eventuele andere `illuminate/*` constraints (string of `null` als niet aanwezig)
    5. De `version` of `branch-alias` key in `extra` (indien aanwezig)
    6. De laatste git-tag/release-naam (haal via `https://api.github.com/repos/mollie/laravel-cashier-mollie/tags` — top entry)
    7. De laatste commit SHA + commit-datum op master (`https://api.github.com/repos/mollie/laravel-cashier-mollie/commits/master`)

    Als WebFetch faalt op één van deze URLs: documenteer dat in de ADR (Task 2) als "fetch-failure-fallback" en gebruik `gh api repos/mollie/laravel-cashier-mollie/contents/composer.json --jq '.content' | base64 -d` als alternatief via Bash.

    **Stap B — Beslis pad (a/b/c) op basis van constraints:**

    Regel:
    - **Pad (a)** als PHP-constraint `^8.0`, `^8.1`, `^8.2`, `^8.3`, of `^8.4` toestaat EN Laravel-constraint Laravel 13 toestaat (bv. `^10.0 || ^11.0 || ^12.0 || ^13.0` of breder).
    - **Pad (b)** als PHP- of Laravel-constraint moderne versies niet toestaat maar de codebase plausibel patch-baar is (bv. PHP `^7.2 || ^8.0` met losse deprecated calls; Laravel `^6.0 || ^7.0 || ^8.0` met geïsoleerde `Illuminate\Support\Str::startsWith`-style breakage). Patch-baarheid = subjective judgement; conservatief blijven.
    - **Pad (c)** als PHP-constraint < 8.0 hard pin't, Laravel-constraint < 10 hard pin't, OF `mollie/laravel-mollie` een conflicterende facade-alias (`Mollie`) dwingt die met `EmeqMollie` (Phase 2 D-08 verificatie) zou botsen ondanks de in PROJECT.md afgesproken naam-isolatie.

    **Stap C — Dry-run-poging (alleen informatief, geen commit):**

    Probeer in een wegwerp-werkfolder (`/tmp/cashier-compat-check-<ts>/`):

    ```bash
    mkdir -p /tmp/cashier-compat-check-$(date +%s) && cd "$_"
    composer init --no-interaction --name=emeq/compat-probe --require=php:^8.4 --require=laravel/framework:^13.8 -q
    composer require mollie/laravel-cashier-mollie --dry-run 2>&1 | tee dry-run.log
    ```

    Log de exit-code + de laatste 20 regels van `dry-run.log` (succes → "Nothing to install or update" of een complete dependency-tree; failure → meestal een `requires` constraint-mismatch). Dit is een **informatief** signaal — de Stap A composer.json-read is leidend voor de paden-keuze.

    Als de dry-run om externe redenen niet kan (geen internet, packagist down, composer niet beschikbaar in CI): documenteer dat in de ADR; Stap A is de leidende evidence.
  </action>
  <verify>
    <automated>test -n "$(cat /tmp/cashier-compat-check-*/dry-run.log 2>/dev/null || echo 'fetch-fallback-documented')" && echo "research-evidence-captured"</automated>
  </verify>
  <acceptance_criteria>
    - Een set concrete strings is in werkgeheugen aanwezig voor Task 2: minimaal `require.php`, `require.illuminate/contracts` of `require.laravel/framework`, `require.mollie/laravel-mollie`, laatste tag, laatste commit SHA + datum.
    - Pad-keuze (a/b/c) is bepaald via de in Stap B beschreven regel, niet via vooringenomen voorkeur.
    - Dry-run-log bestaat in `/tmp/cashier-compat-check-*/dry-run.log` OF de ADR documenteert expliciet waarom dry-run niet uitvoerbaar was.
  </acceptance_criteria>
  <done>
    Concrete PHP/Laravel/laravel-mollie-constraint-strings + laatste tag + laatste commit-SHA zijn vastgesteld; pad-keuze (a/b/c) is bepaald conform de regel uit Stap B; bewijsmateriaal is klaar voor verwerking in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR schrijven — `.docs/decisions/cashier-mollie-compat.md`</name>
  <read_first>
    - `.docs/decisions/snelstart-certificering-pad.md` (target ADR-stijl en toon — drie-paden-keuze, Nederlandse beschouwing)
    - `.docs/decisions/mollie-passthrough-api.md` (target frontmatter/sectie-structuur)
    - `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md` §D-01 + D-02 + D-03 + D-08 (decision-tree, Billable-target, facade-collision-controle)
  </read_first>
  <files>.docs/decisions/cashier-mollie-compat.md</files>
  <action>
    Maak `.docs/decisions/cashier-mollie-compat.md` aan met onderstaande exacte structuur. Gebruik Nederlandse prose (project-conventie uit `.ai/rules/global.md`: docs in NL, code-identifiers in EN). Vermijd AI-clichés uit `.ai/rules/global.md` ("Furthermore", "Naadloos", "Daarnaast" als mechanische opener, enz.).

    Verplichte secties (in deze volgorde):

    ```markdown
    # ADR: Cashier-Mollie compat-pad voor Phase 6 (use-case A)

    **Status:** Accepted
    **Datum:** 2026-05-15
    **Phase:** 06-cashier-mollie-integratie-use-case-a
    **Plan:** 06-01

    ## Context

    [1-2 paragrafen: waarom deze ADR bestaat. Verwijs naar STATE.md "Cashier-Mollie compat-risico (v0.2)" blocker, naar memory `reference_cashier_mollie_compat_risk.md`, naar Phase 6 SC-1 in ROADMAP.md. Maak duidelijk dat use-case A (Emeq → Consumers via Emeq's eigen Mollie) afwijkt van use-case B (Phase 7, Connect, multi-tenant). Use-case A is de enige use-case waar Cashier-Mollie überhaupt past — single-tenant by design.]

    ## Onderzoek

    ### Master HEAD van `mollie/laravel-cashier-mollie`

    - **Repo:** `https://github.com/mollie/laravel-cashier-mollie`
    - **Laatste commit op `master`:** `<SHA>` (`<datum>`)
    - **Laatste tag/release:** `<tag>`
    - **`require.php`:** `<exacte string uit composer.json>`
    - **`require."laravel/framework"`** (of `illuminate/contracts`)**:** `<exacte string>`
    - **`require."mollie/laravel-mollie"`:** `<exacte string>`
    - **Andere relevante `illuminate/*` constraints:** `<lijst of "n.v.t.">`

    ### Composer dry-run-poging

    Uitgevoerd in een wegwerp-werkfolder met `php:^8.4` + `laravel/framework:^13.8` als baseline:

    ```
    $ composer require mollie/laravel-cashier-mollie --dry-run
    <laatste 10-20 regels van dry-run.log, exact>
    ```

    Exit-code: `<0 of >0>`.

    [Of, als dry-run niet uitvoerbaar was: één paragraaf die uitlegt waarom — geen internet, packagist down, composer-versie te oud, etc. — en bevestigt dat de composer.json-read uit het vorige sub-blok leidend is.]

    ### Facade-collision-controle

    [1 paragraaf: bevestig dat `laravel-mollie`'s `Mollie`-facade (indien meegetrokken door Cashier-Mollie) naast `EmeqMollie` kan staan — zie Phase 2 SC-3 in ROADMAP.md. Als pad (c) gekozen wordt, vervalt deze controle.]

    ## Decision

    **Gekozen pad: `<a | b | c>`**

    [Eén volzin die het pad benoemt — bv. "Pad (a): `composer require mollie/laravel-cashier-mollie:^X.Y` werkt out-of-box tegen PHP 8.4 + Laravel 13."]

    ### Onderbouwing

    [3-5 bullets die de keuze koppelen aan de evidence uit de Onderzoek-sectie. Geen "ik denk dat" — alleen "constraint X laat versie Y toe, dus pad Z."]

    ### Verworpen alternatieven

    [Voor elk van de twee niet-gekozen paden: 1-2 zinnen waarom niet. Dit is niet optioneel — de andere twee paden waren wezenlijk overwogen; documenteer waarom ze afvielen.]

    ## Consequences

    ### Voor Phase 6 plan-uitvoering

    [4-6 bullets die concreet zeggen wat plan 06-02 t/m 06-08 nu gaat doen. Bv. bij pad (a):
    - Plan 06-02: `composer require mollie/laravel-cashier-mollie:^X.Y` + `vendor:publish --tag=cashier-migrations` + `migrate`.
    - Plan 06-03: `Billable` trait op `App\Models\Consumer` + `config/billing-plans.php` met 2 test-plans.
    - Plan 06-04: `billing:read`/`billing:write` Sanctum-abilities + routes `GET /v1/billing/subscription` + admin endpoints.
    - Plan 06-05: subscription create/cancel + Mandate-handling via Cashier's API.
    - Plan 06-06: Cashier-webhook ingress + signature-verify (hergebruik Phase 5a hard-fail guard pattern).
    - Plan 06-07: integration tests met Mollie test-mode (`@group integration`).
    - Plan 06-08: BLOCKING phase-acceptance (3 SC's, 0 regressies).
    ]

    [Bij pad (b) of (c): pas de bullets aan. Bij pad (b): fork-repo + `composer.json` VCS-entry + patch-strategie. Bij pad (c): `app/Billing/`-namespace + eigen Eloquent-models + ≥80% coverage uit D-13.]

    ### Risico's en open vragen

    [2-4 bullets: wat nog niet weten we niet, wat valideert plan 06-08 BLOCKING. Houd het kort — geen speculatie. Concreet: facade-alias-collision (alleen relevant bij pad a/b), Mollie test-mode key beschikbaarheid in `.env.example`, Cashier's default dunning-config-defaults.]

    ## Next Steps

    1. ADR + REQUIREMENTS.md status committen (Task 3).
    2. Gebruiker triggert `/gsd-plan-phase 6 --gaps` (of een fresh planner-run) met deze ADR als locked context.
    3. Planner schrijft plannen 06-02 t/m 06-08 conform de Consequences-sectie.
    4. Zie `06-DEFERRED-PLANS.md` in de phase-folder voor de deferred-plan-set die door deze ADR geactiveerd wordt.

    ## Referenties

    - STATE.md §"Blockers/Concerns" (Cashier-Mollie compat-risico)
    - ROADMAP.md §"Phase 6: Cashier-Mollie integratie (use-case A)" — Goal + 4 SC's
    - REQUIREMENTS.md §SUB-01
    - 06-CONTEXT.md §D-01 + D-02 (decision-tree)
    - 06-DEFERRED-PLANS.md (deferred plan-set die deze ADR ontblokt)
    - `https://github.com/mollie/laravel-cashier-mollie` (upstream repo)
    ```

    Vul élke `<placeholder>` in met de concrete string/cijfer uit Task 1. Geen "TBD", geen "wordt nog bepaald". Als één van de feiten niet beschikbaar is, documenteer expliciet de fetch-fallback in de "Onderzoek"-sectie.

    Houd het document **chirurgisch** kort: streef naar ≤200 regels. Geen marketing-prose. Geen "Discover", "Transform", "Unlock" (zie `.ai/rules/global.md` anti-cliché-lijst).
  </action>
  <verify>
    <automated>test -f .docs/decisions/cashier-mollie-compat.md && grep -q "^## Decision$" .docs/decisions/cashier-mollie-compat.md && grep -E "^\*\*Gekozen pad: \`(a|b|c)\`\*\*" .docs/decisions/cashier-mollie-compat.md && grep -q "^## Next Steps$" .docs/decisions/cashier-mollie-compat.md && grep -q "06-DEFERRED-PLANS" .docs/decisions/cashier-mollie-compat.md</automated>
  </verify>
  <acceptance_criteria>
    - Bestand `.docs/decisions/cashier-mollie-compat.md` bestaat.
    - Sectie-headers `## Context`, `## Onderzoek`, `## Decision`, `## Consequences`, `## Next Steps`, `## Referenties` zijn allemaal aanwezig (grep `^## ` levert ≥6 hits).
    - Een regel matched de regex `^\*\*Gekozen pad: \`(a|b|c)\`\*\*` (exact één pad genoemd, geen "a/b" of "TBD").
    - Geen literal `<placeholder>` of `<SHA>` of `<datum>` strings in het bestand (`grep -E "<[a-z]+>" .docs/decisions/cashier-mollie-compat.md` levert 0 hits in body-tekst — code-blocks/regex-examples in deze plan-instructie tellen niet, maar het ADR-bestand zelf moet zonder placeholders zijn).
    - Geen forbidden AI-clichés uit `.ai/rules/global.md`: `grep -iE "(naadloos|seamless|game-changer|empower|unlock|transform|innovative solution|in the modern world)" .docs/decisions/cashier-mollie-compat.md` levert 0 hits.
    - `## Next Steps` verwijst expliciet naar `06-DEFERRED-PLANS.md` (grep verifieerbaar).
  </acceptance_criteria>
  <done>
    ADR `.docs/decisions/cashier-mollie-compat.md` bestaat met de zes verplichte secties, één expliciete pad-keuze (a/b/c), concrete evidence uit Task 1 ingevuld, en een `Next Steps`-sectie die naar `06-DEFERRED-PLANS.md` wijst.
  </done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md SUB-01 status bijwerken + git commit</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` (huidige Traceability-tabel; SUB-01 status is nu `Pending`)
    - `.ai/rules/global.md` §"Taal" (commit-messages in NL)
    - `.ai/git-policy` (uit CLAUDE.md `.ai/project rules` block: nooit `--no-verify`, geen push zonder approval, geen `--no-gpg-sign`)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Bewerk `.planning/REQUIREMENTS.md` en update de SUB-01 status-cel in de Traceability-tabel.

    Huidige regel (regel ~90):
    ```
    | SUB-01 | Phase 6 | Pending |
    ```

    Vervangen door:
    ```
    | SUB-01 | Phase 6 | In Progress (06-01 done — compat-check landed) |
    ```

    Geen andere wijzigingen aan REQUIREMENTS.md. Geen rij toevoegen, geen body-tekst aanpassen, geen out-of-scope-bullets aanraken.

    Daarna git-commit met deze twee files:

    ```bash
    git status                       # bevestig alleen de twee verwachte files dirty
    git add .docs/decisions/cashier-mollie-compat.md .planning/REQUIREMENTS.md
    git commit -m "$(cat <<'EOF'
    docs(06-01): cashier-mollie compat-ADR + SUB-01 status in-progress

    Plan 06-01 levert de compat-check uit Phase 6 SC-1 en kiest één pad
    (a/b/c) voor de uitvoering van plannen 06-02 t/m 06-08. Productiecode
    blijft ongeraakt — Task wegens decision-tree-blocker uit STATE.md.

    Refs: .planning/phases/06-cashier-mollie-integratie-use-case-a/06-DEFERRED-PLANS.md

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    EOF
    )"
    git status                       # bevestig clean tree
    ```

    Géén `git push`. Géén `--no-verify`. Géén `--amend`. Bij hook-failure: fix root cause, re-stage, NEW commit.

    Belangrijke guards:
    - `.docs/` is gitignored — controleer met `git check-ignore -v .docs/decisions/cashier-mollie-compat.md`. **VERWACHT GEDRAG: het bestand IS gitignored** (zie STATE.md quick `260514-tny`). Daarom landt het bestand WEL op disk, maar NIET in git. De commit bevat dan alleen `.planning/REQUIREMENTS.md`. Pas de commit-actie aan:
      ```bash
      git check-ignore -v .docs/decisions/cashier-mollie-compat.md && echo "ADR is gitignored — commit alleen REQUIREMENTS.md"
      git add .planning/REQUIREMENTS.md
      git commit -m "<zelfde message als boven>"
      ```
    - Als `.docs/` om welke reden ook NIET gitignored is (regel weg, nieuwe `.gitignore`): commit beide files conform het oorspronkelijke `git add`-commando.
  </action>
  <verify>
    <automated>grep -q "^| SUB-01 | Phase 6 | In Progress (06-01 done — compat-check landed) |$" .planning/REQUIREMENTS.md && git log -1 --pretty=%s | grep -q "docs(06-01)" && git status --porcelain | grep -v '^??' | wc -l | grep -q "^[[:space:]]*0$"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md SUB-01 rij is exact `| SUB-01 | Phase 6 | In Progress (06-01 done — compat-check landed) |`.
    - De laatste commit op deze branch heeft een subject dat met `docs(06-01)` begint.
    - `git status --porcelain` (na commit) bevat geen modified (`M`/`A`/`D`) tracked files; alleen optionele untracked (`??`) zoals het 06-DEFERRED-PLANS.md uit Task 4 in een eerdere stap, of helemaal niets.
    - Géén push uitgevoerd: `git rev-parse HEAD` ≠ `git rev-parse @{u}` (lokale commit zit voor op upstream) — tenzij de user expliciet om push vraagt (niet in scope).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md SUB-01-rij is geüpdatet, één nieuwe commit met `docs(06-01)`-prefix is gemaakt op de huidige branch, en de working tree is clean voor de tracked files die dit plan raakt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (geen nieuwe) | Dit plan voegt geen attack surface toe. Alleen documentatie + één status-update in `.planning/`. Geen routes, geen credentials, geen DB-schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | ADR-content | accept | ADR bevat geen credentials, geen tokens, alleen publieke composer.json-strings en commit-SHAs van een publieke OSS-repo. `.docs/` is bovendien gitignored — ADR landt lokaal, niet in git-history. |
| T-06-02 | Tampering | git commit-content | accept | Commit raakt alleen `.planning/REQUIREMENTS.md`. Geen executable wijziging. Pre-commit hooks (Pint, etc.) blijven actief. |

Plan 1 is documentation-only; de volledige threat-model met trust-boundaries (Cashier-webhook ingress, billing-routes, Mollie-API-key isolation) wordt in plan-06-02+ uitgewerkt zodra de implementatie-laag concreet is. Dit volgt de planning-policy uit `<threat_model_requirement>`: minimal placeholder voor docs-only plans.
</threat_model>

<verification>
- Bestand `.docs/decisions/cashier-mollie-compat.md` bestaat op disk en bevat alle zes verplichte secties.
- ADR-conclusie is exact één pad: regex `^\*\*Gekozen pad: \`(a|b|c)\`\*\*` matched precies één regel.
- ADR bevat geen onvervangen `<placeholder>`-strings.
- REQUIREMENTS.md SUB-01 status is `In Progress (06-01 done — compat-check landed)`.
- Eén nieuwe commit met `docs(06-01)`-prefix op de huidige branch.
- Geen productiecode aangeraakt: `git diff HEAD~1 -- app/ routes/ database/ tests/ config/ composer.json composer.lock` retourneert leeg.
</verification>

<success_criteria>
1. **Phase 6 SC-1 bewezen:** `.docs/decisions/cashier-mollie-compat.md` bestaat met expliciete pad-keuze (a/b/c) onderbouwd door concrete composer.json-evidence van `mollie/laravel-cashier-mollie` master HEAD.
2. **Plan 06-02+ ontblokt:** De ADR's `## Consequences`-sectie beschrijft concreet wat plan 06-02 t/m 06-08 moeten doen per pad-uitkomst, zodat een vervolg-planner-run (`/gsd-plan-phase 6 --gaps`) deterministisch plannen kan schrijven.
3. **Geen productiecode aangeraakt:** `composer.json`, `composer.lock`, `app/`, `routes/`, `database/`, `tests/`, `config/` blijven byte-identiek aan HEAD vóór dit plan; `git diff HEAD~1 -- <die paden>` is leeg.
4. **Traceability up-to-date:** REQUIREMENTS.md SUB-01 status reflecteert dat 06-01 done is.
5. **Eén commit, NL-message:** `git log -1` toont een `docs(06-01)`-commit met Nederlandstalige body en `Co-Authored-By: Claude Opus 4.7`-trailer.
</success_criteria>

<output>
Na completion: creëer `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-01-SUMMARY.md` met:
- Eén-regel-pad-conclusie ("Gekozen pad: a | b | c — <reden>")
- Letterlijke composer.json-constraint-strings die de keuze onderbouwen
- Verwijzing naar `.docs/decisions/cashier-mollie-compat.md`
- Verwijzing naar `06-DEFERRED-PLANS.md`
- Volgende stap: "User runt `/gsd-plan-phase 6 --gaps` (of fresh planner-run) om plannen 06-02 t/m 06-08 te genereren met deze ADR als locked context."
</output>
