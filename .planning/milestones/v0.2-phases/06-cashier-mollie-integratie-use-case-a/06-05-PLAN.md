---
phase: 06-cashier-mollie-integratie-use-case-a
plan: 05
type: execute
wave: 4
depends_on: [06-03, 06-04]
autonomous: true
requirements: [SUB-01]
tags: [cashier-mollie, sanctum, routes, controllers, billing, sub-01]
files_modified:
  - app/Sanctum/TokenAbilities.php
  - app/Http/Controllers/Api/V1/Billing/SubscriptionController.php
  - app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php
  - app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php
  - app/Http/Middleware/EnsureEmeqAdminToken.php
  - routes/api.php
  - config/billing.php
  - .env.example
  - tests/Feature/Api/V1/Billing/ConsumerSubscriptionReadTest.php
  - tests/Feature/Api/V1/Billing/AdminSubscriptionCreateTest.php
  - tests/Feature/Api/V1/Billing/AdminSubscriptionCancelTest.php
  - tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php

must_haves:
  truths:
    - "`App\\Sanctum\\TokenAbilities` exposeert 2 nieuwe constants: `BILLING_READ = 'billing:read'` + `BILLING_WRITE = 'billing:write'` (D-14)."
    - "`TokenAbilities::all()` retourneert nu ≥7 abilities (5 bestaand + 2 nieuw)."
    - "`GET /v1/billing/subscription` (Sanctum + `ability:billing:read,billing:write,*`) retourneert JSON met huidige Consumer's subscription-status; 200 OK voor active + 200 OK met `subscribed=false` voor geen subscription."
    - "`POST /v1/admin/billing/subscriptions` (Sanctum + `ability:billing:write,*` + admin-allowlist-guard) accepteert `{consumer_id, plan_slug}` body en triggert Cashier's `$consumer->newSubscription('main', $planSlug)->create()` — retourneert 201 + subscription-resource."
    - "`DELETE /v1/admin/billing/subscriptions/{id}` (Sanctum + `ability:billing:write,*` + admin-guard) cancelt de subscription via Cashier's `$subscription->cancel()` — retourneert 204."
    - "Een Consumer-PAT zonder `billing:read` krijgt 403 op `GET /v1/billing/subscription` (D-14 ability-gate)."
    - "Een Consumer-PAT zonder `billing:write` OF niet op de admin-allowlist krijgt 403 op de admin-endpoints (D-15 admin-guard)."
    - "Een Consumer kan ALLEEN zijn eigen subscription-state lezen — cross-Consumer-leakage retourneert 404 (Hub-invariant uit Phase 3)."
    - "`config/billing.php` definieert `admin_allowlist` (config-driven Consumer-ID-lijst tot Phase 9 Filament komt) en wordt door `EnsureEmeqAdminToken`-middleware geconsumeerd."
    - "`.env.example` heeft `BILLING_ADMIN_CONSUMER_IDS=` placeholder met NL-comment."
  artifacts:
    - path: "app/Sanctum/TokenAbilities.php"
      provides: "BILLING_READ + BILLING_WRITE constants in `all()`-array"
      contains: "BILLING_READ"
    - path: "app/Http/Controllers/Api/V1/Billing/SubscriptionController.php"
      provides: "Consumer-facing show() endpoint"
      exports: ["show"]
    - path: "app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php"
      provides: "Admin-facing store() + destroy() endpoints"
      exports: ["store", "destroy"]
    - path: "app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php"
      provides: "Validation regels voor admin-create payload (consumer_id, plan_slug)"
    - path: "app/Http/Middleware/EnsureEmeqAdminToken.php"
      provides: "Admin-guard middleware — alleen tokens van Consumer-IDs uit config-allowlist passeren"
    - path: "routes/api.php"
      provides: "3 nieuwe routes onder /v1/billing en /v1/admin/billing"
      contains: "billing.subscription.show"
    - path: "config/billing.php"
      provides: "admin_allowlist + (optioneel) default_subscription_name"
    - path: "tests/Feature/Api/V1/Billing/*Test.php"
      provides: "4 nieuwe test-classes, ≥12 tests samen"
      min_tests: 12
  key_links:
    - from: "routes/api.php"
      to: "BILLING_READ + BILLING_WRITE Sanctum-abilities"
      via: "->middleware('ability:billing:read,billing:write,*')"
      pattern: "ability:billing"
    - from: "app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php"
      to: "App\\Billing\\PlanResolver"
      via: "constructor-injection via container"
      pattern: "PlanResolver"
    - from: "app/Http/Middleware/EnsureEmeqAdminToken.php"
      to: "config('billing.admin_allowlist')"
      via: "in_array(currentConsumer->id, ...)"
      pattern: "admin_allowlist"
---

<objective>
Lever de billing-routes-stack: 2 nieuwe Sanctum-abilities (D-14), 1 Consumer-facing read-endpoint (`GET /v1/billing/subscription`), 2 admin endpoints (`POST` + `DELETE` op `/v1/admin/billing/subscriptions`), 1 admin-guard-middleware (config-allowlist tot Phase 9 Filament-panel), 1 Form-Request voor validation, en ≥12 feature-tests die ability-gating, admin-gating, cross-Consumer-scoping en Cashier-create-flow bewijzen.

Purpose: zonder dit plan kan een Consumer of Emeq-admin niet via API een subscription bekijken/aanmaken. Plan 06-06 (webhook) en plan 06-07 (integration tests) leunen op deze routes.

Output: 1 modified ability-class + 2 nieuwe controllers + 1 Form-Request + 1 middleware + 1 modified routes-file + 1 nieuwe config + 1 modified `.env.example` + 4 nieuwe test-classes. GEEN integration tests met echte Mollie test-mode API — die komen in plan 06-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-02-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-03-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-04-PLAN.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md
@app/Sanctum/TokenAbilities.php
@app/Http/Controllers/Api/V1/Mollie/AbstractMolliePassThroughController.php
@app/Http/Controllers/Api/V1/PingController.php
@app/Http/Controllers/Api/V1/AccountController.php
@app/Http/Middleware/ResolveMollieAccount.php
@routes/api.php
@bootstrap/app.php
@tests/Concerns/StubsMollieClient.php
@tests/Feature/Api/V1/Mollie/PaymentsTest.php

<interfaces>
<!-- Locked decisions uit 06-CONTEXT.md voor dit plan: -->
<!-- D-14: BILLING_READ + BILLING_WRITE Sanctum-abilities -->
<!-- D-15: 3 routes — GET /v1/billing/subscription, POST + DELETE /v1/admin/billing/subscriptions -->
<!-- specifics: Cashier defaults voor dunning; geen custom retry-flow -->
<!-- deferred: Cashier admin-UI = Phase 9 → tot dan config-allowlist -->

Huidige TokenAbilities-class (lees Read-uitvoer hierboven):
```php
final class TokenAbilities
{
    public const SNELSTART_READ = 'snelstart:read';
    public const SNELSTART_WRITE = 'snelstart:write';
    public const MOLLIE_READ = 'mollie:read';
    public const MOLLIE_WRITE = 'mollie:write';
    public const CONSUMER_MANAGE_ACCOUNTS = 'consumer:manage-accounts';
    public const ADMIN = '*';

    public static function all(): array { /* returns list<string> with 6 entries */ }
}
```

Bestaande Sanctum-middleware-aliassen in `bootstrap/app.php` (regel 30-32):
- `ability` => `CheckForAnyAbility::class` (ANY van de aabilities passeert)
- `abilities` => `CheckAbilities::class` (ALLE passeren)

→ Voor billing gebruiken we `ability:billing:read,billing:write,*` (read-or-write-or-admin OK voor read; write-or-admin OK voor write).

Cashier-Mollie's subscription-create API (lees vendor source na 06-02-install):
```php
$consumer->newSubscription('main', $planSlug)->create();
// Retourneert: Laravel\Cashier\Subscription
// Throws: bij missing mandate of Mollie-API-failure
```

Voor de allereerste subscription (geen Mandate ja) volgt Cashier's `first_payment`-flow:
- `$consumer->newSubscription(...)->create()` returnt een `RedirectResponse` of een Subscription afhankelijk van of er al een Mandate is.
- Voor admin-API: we willen NIET dat de admin via een redirect-URL-flow gaat — admin maakt subscription aan namens een Consumer die zelf nog een Mollie-mandate moet bevestigen. Cashier-Mollie heeft een `firstPaymentSubscriptionBuilder()`-flow die een betaal-URL retourneert die de Consumer moet doorlopen.
- Voor v0.2 use-case A: admin POSTs → krijgt een `{subscription, mandate_redirect_url}` payload terug → admin emailt dat handmatig naar de Consumer-contactpersoon. **NIET in scope voor de admin-controller om de e-mail te versturen — dat is Phase 9 / business-process.**

Form-Request validation pattern (uit Phase 5b's ConnectionController bewees zich):
- `rules(): array` retourneert validation-array
- `authorize(): bool` checked Sanctum-ability + admin-guard (we doen dit via middleware, niet hier — Form-Request blijft pure-input-validation)
- `messages()` met NL-error-strings

Admin-allowlist via config (D-15 stipuleert "admin-guard van Phase 9, te scaffold'en in deze fase als simpele config-allowlist tot Phase 9 Filament-panel landt"):
```php
// config/billing.php
return [
    'admin_allowlist' => array_filter(array_map(
        'intval',
        explode(',', (string) env('BILLING_ADMIN_CONSUMER_IDS', '')),
    )),
    'default_subscription_name' => env('BILLING_DEFAULT_SUBSCRIPTION_NAME', 'main'),
];
```

EnsureEmeqAdminToken-middleware:
```php
final class EnsureEmeqAdminToken
{
    public function handle(Request $request, Closure $next): Response
    {
        $consumer = $request->user();
        if ($consumer === null || ! in_array($consumer->getKey(), config('billing.admin_allowlist', []), true)) {
            return response()->json([
                'error' => 'not_admin',
                'message' => 'Token hoort niet bij een Emeq-admin-Consumer.',
            ], 403);
        }
        return $next($request);
    }
}
```

Routes structure (voorbeeld voor plan):
```php
// routes/api.php — binnen Sanctum::auth-group
Route::middleware('ability:billing:read,billing:write,*')->group(function () {
    Route::get('/billing/subscription', [\App\Http\Controllers\Api\V1\Billing\SubscriptionController::class, 'show'])
        ->name('api.billing.subscription.show');
});

Route::prefix('admin/billing')
    ->middleware(['ability:billing:write,*', 'emeq.admin'])  // alias geregistreerd in bootstrap/app.php
    ->group(function () {
        Route::post('/subscriptions', [\App\Http\Controllers\Api\V1\Admin\Billing\SubscriptionController::class, 'store'])
            ->name('api.admin.billing.subscriptions.store');
        Route::delete('/subscriptions/{id}', [\App\Http\Controllers\Api\V1\Admin\Billing\SubscriptionController::class, 'destroy'])
            ->name('api.admin.billing.subscriptions.destroy');
    });
```

Belangrijke gotcha: `bootstrap/app.php` middleware-alias-registratie moet `emeq.admin` toevoegen. Vergelijkbaar met de bestaande `'resolve.mollie.account' => ResolveMollieAccount::class` regel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sanctum abilities + routes-skeleton + admin-middleware + config + .env — minimaal werkend ability-gate-bewijs</name>
  <read_first>
    - `app/Sanctum/TokenAbilities.php` (HEAD-state: 6 constants, alfabetisch geordend)
    - `routes/api.php` (HEAD-state: bestaande groups + ability-middleware-pattern op regel 35)
    - `bootstrap/app.php` (middleware-alias-pattern op regel 30-33)
    - `tests/Feature/Api/V1/SanctumAbilityTest.php` (test-pattern voor ability-gates — vooral het `test_token_without_required_ability_is_rejected`-placeholder dat in Phase 5a is ge-completed; in 06-05 maken we 'n SECONDE placeholder voor billing-abilities)
    - `app/Http/Middleware/ResolveMollieAccount.php` (middleware-class-stijl als template)
    - `config/mollie.php` (config-file-stijl)
    - `.env.example` (huidige structuur)
  </read_first>
  <files>
    app/Sanctum/TokenAbilities.php
    app/Http/Middleware/EnsureEmeqAdminToken.php
    config/billing.php
    .env.example
    bootstrap/app.php
    routes/api.php
    tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php
  </files>
  <action>
    **Stap A — TokenAbilities uitbreiden:**

    Edit `app/Sanctum/TokenAbilities.php`:
    - Voeg 2 nieuwe const-declarations toe tussen `CONSUMER_MANAGE_ACCOUNTS` en `ADMIN` (alfabetisch wel een issue: `billing:*` zou bovenaan; maar visueel matched de bestaande conventie van "concrete-providers eerst, dan generic"). **Voorkeur: voeg toe NA `CONSUMER_MANAGE_ACCOUNTS` en VÓÓR `ADMIN`** — visueel-gegroupeerd per-feature.
    - Voeg toe aan `all()`-array (zelfde volgorde).

    Resulterende class:
    ```php
    final class TokenAbilities
    {
        public const SNELSTART_READ = 'snelstart:read';
        public const SNELSTART_WRITE = 'snelstart:write';
        public const MOLLIE_READ = 'mollie:read';
        public const MOLLIE_WRITE = 'mollie:write';
        public const CONSUMER_MANAGE_ACCOUNTS = 'consumer:manage-accounts';
        public const BILLING_READ = 'billing:read';
        public const BILLING_WRITE = 'billing:write';
        public const ADMIN = '*';

        public static function all(): array
        {
            return [
                self::SNELSTART_READ,
                self::SNELSTART_WRITE,
                self::MOLLIE_READ,
                self::MOLLIE_WRITE,
                self::CONSUMER_MANAGE_ACCOUNTS,
                self::BILLING_READ,
                self::BILLING_WRITE,
                self::ADMIN,
            ];
        }
    }
    ```

    **Stap B — EnsureEmeqAdminToken-middleware:**

    Maak `app/Http/Middleware/EnsureEmeqAdminToken.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace App\Http\Middleware;

    use Closure;
    use Illuminate\Http\Request;
    use Symfony\Component\HttpFoundation\Response;

    /**
     * D-15: tot Phase 9 Filament-panel landt, is "Emeq-admin" een
     * config-allowlist van Consumer-IDs. Token-houders die NIET in de
     * allowlist staan krijgen 403 op admin-billing-endpoints.
     *
     * Stop met deze middleware zodra Phase 9 een `is_emeq_staff` boolean
     * op het User-model heeft en admin-tokens daadwerkelijk via Filament
     * worden uitgegeven aan staff-users i.p.v. via PATs op Consumers.
     */
    final class EnsureEmeqAdminToken
    {
        public function handle(Request $request, Closure $next): Response
        {
            $consumer = $request->user();
            $allowlist = config('billing.admin_allowlist', []);

            if ($consumer === null || ! is_array($allowlist) || ! in_array($consumer->getKey(), $allowlist, true)) {
                return response()->json([
                    'error' => 'not_admin',
                    'message' => 'Token hoort niet bij een Emeq-admin-Consumer.',
                ], 403);
            }

            return $next($request);
        }
    }
    ```

    **Stap C — config/billing.php:**

    ```php
    <?php

    declare(strict_types=1);

    /*
     * D-15: admin-billing-routes worden gated via een config-allowlist
     * tot Phase 9 Filament-panel landt. Empty allowlist = niemand is admin
     * = alle admin-routes 403 (default-deny).
     */
    return [
        'admin_allowlist' => array_values(array_filter(array_map(
            'intval',
            explode(',', (string) env('BILLING_ADMIN_CONSUMER_IDS', '')),
        ))),

        'default_subscription_name' => env('BILLING_DEFAULT_SUBSCRIPTION_NAME', 'main'),
    ];
    ```

    **Stap D — `.env.example` aanvullen:**

    Voeg NA de bestaande Cashier-blok (uit 06-02) toe:
    ```env

    # Billing admin-allowlist — tot Phase 9 Filament-panel landt is een
    # Consumer-ID-lijst de gatekeeper voor /v1/admin/billing/*-endpoints.
    # Comma-separated integers, geen spaces. Laat leeg om alle admin-routes
    # default-deny te zetten (default).
    BILLING_ADMIN_CONSUMER_IDS=
    BILLING_DEFAULT_SUBSCRIPTION_NAME=main
    ```

    **Stap E — `bootstrap/app.php` middleware-alias toevoegen:**

    Edit regel 28-33 — voeg `'emeq.admin' => EnsureEmeqAdminToken::class` toe aan de `$middleware->alias([...])`-array (alfabetisch tussen `'ability'` en `'resolve.mollie.account'`).

    Voeg ook de `use`-statement toe bovenaan: `use App\Http\Middleware\EnsureEmeqAdminToken;` (alfabetisch tussen `ResolveSnelstartAccount` en `SetNoIndexHeaders`).

    **Stap F — routes/api.php — voeg billing-routes toe:**

    Plak NA de bestaande Snelstart-passthrough-route (regel 40-43) en VÓÓR de `Route::prefix('mollie')`-group (regel 45):
    ```php
    Route::middleware('ability:billing:read,billing:write,*')->group(function (): void {
        Route::get('/billing/subscription', [\App\Http\Controllers\Api\V1\Billing\SubscriptionController::class, 'show'])
            ->name('api.billing.subscription.show');
    });

    Route::prefix('admin/billing')
        ->middleware(['ability:billing:write,*', 'emeq.admin'])
        ->group(function (): void {
            Route::post('/subscriptions', [\App\Http\Controllers\Api\V1\Admin\Billing\SubscriptionController::class, 'store'])
                ->name('api.admin.billing.subscriptions.store');
            Route::delete('/subscriptions/{id}', [\App\Http\Controllers\Api\V1\Admin\Billing\SubscriptionController::class, 'destroy'])
                ->name('api.admin.billing.subscriptions.destroy');
        });
    ```

    Controllers bestaan nog niet — `php artisan route:list` zal class-not-found gooien tot Task 2. Dat is acceptable; Task 1 wordt afgesloten met een commit-na-RED-tests.

    **Stap G — RED-test voor ability-gates:**

    Maak `tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Feature\Api\V1\Billing;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Tests\TestCase;

    class BillingAbilityGateTest extends TestCase
    {
        use RefreshDatabase;

        public function test_billing_read_constant_exists(): void
        {
            $this->assertSame('billing:read', TokenAbilities::BILLING_READ);
            $this->assertSame('billing:write', TokenAbilities::BILLING_WRITE);
            $this->assertContains(TokenAbilities::BILLING_READ, TokenAbilities::all());
            $this->assertContains(TokenAbilities::BILLING_WRITE, TokenAbilities::all());
        }

        public function test_get_billing_subscription_requires_auth(): void
        {
            $response = $this->getJson('/v1/billing/subscription');

            $response->assertStatus(401);
        }

        public function test_get_billing_subscription_without_billing_ability_is_rejected(): void
        {
            $consumer = Consumer::factory()->create();
            $token = $consumer->createToken('test', [TokenAbilities::MOLLIE_READ])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->getJson('/v1/billing/subscription');

            $response->assertStatus(403);
        }

        public function test_admin_subscription_endpoints_require_billing_write_ability(): void
        {
            $consumer = Consumer::factory()->create();
            $token = $consumer->createToken('test', [TokenAbilities::BILLING_READ])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => $consumer->id,
                    'plan_slug' => 'naschool-license',
                ]);

            $response->assertStatus(403);
        }

        public function test_admin_subscription_endpoints_require_admin_allowlist(): void
        {
            config(['billing.admin_allowlist' => []]);
            $consumer = Consumer::factory()->create();
            $token = $consumer->createToken('test', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => $consumer->id,
                    'plan_slug' => 'naschool-license',
                ]);

            $response->assertStatus(403);
            $response->assertJsonPath('error', 'not_admin');
        }
    }
    ```

    Run om RED te zien (verwacht: BILLING_READ constant test passt, andere 4 falen op `Class App\Http\Controllers\Api\V1\Billing\SubscriptionController not found` of op route-not-found):
    ```bash
    php artisan test --compact tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php
    ```

    Commit RED:
    ```bash
    git add app/Sanctum/TokenAbilities.php app/Http/Middleware/EnsureEmeqAdminToken.php config/billing.php .env.example bootstrap/app.php routes/api.php tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php
    git commit -m "$(cat <<'EOF'
feat(06-05): Sanctum billing-abilities + admin-middleware + routes-skeleton (RED voor controllers)

D-14: BILLING_READ + BILLING_WRITE constants toegevoegd aan TokenAbilities.
D-15: 3 billing-routes (consumer-read + admin-create + admin-cancel)
gedefinieerd met ability-middleware + nieuwe emeq.admin alias
(config-allowlist tot Phase 9 Filament komt).

BillingAbilityGateTest landed RED — 4 van 5 tests falen omdat controllers
nog niet bestaan (Task 2). De ability-constant-test passt al.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
    ```
  </action>
  <verify>
    <automated>grep -q "BILLING_READ = 'billing:read'" app/Sanctum/TokenAbilities.php && grep -q "BILLING_WRITE = 'billing:write'" app/Sanctum/TokenAbilities.php && test -f app/Http/Middleware/EnsureEmeqAdminToken.php && test -f config/billing.php && grep -q "BILLING_ADMIN_CONSUMER_IDS" .env.example && grep -q "emeq.admin" bootstrap/app.php && grep -q "billing/subscription" routes/api.php && grep -q "admin/billing" routes/api.php && test -f tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `TokenAbilities::BILLING_READ === 'billing:read'` en `BILLING_WRITE === 'billing:write'`.
    - `TokenAbilities::all()` heeft 8 entries.
    - `app/Http/Middleware/EnsureEmeqAdminToken.php` bestaat met `handle()` die 403 retourneert bij empty-allowlist of niet-allowlisted Consumer.
    - `config/billing.php` bestaat met `admin_allowlist` + `default_subscription_name`-keys.
    - `.env.example` bevat `BILLING_ADMIN_CONSUMER_IDS=` en `BILLING_DEFAULT_SUBSCRIPTION_NAME=main`.
    - `bootstrap/app.php` heeft `'emeq.admin' => EnsureEmeqAdminToken::class` alias.
    - `routes/api.php` heeft 3 nieuwe routes: `api.billing.subscription.show`, `api.admin.billing.subscriptions.store`, `api.admin.billing.subscriptions.destroy`.
    - `BillingAbilityGateTest` bestaat met 5 tests; 1 past (constant-test), 4 falen RED (controllers nog niet bestaand).
    - Eén `feat(06-05)`-commit.
  </acceptance_criteria>
  <done>
    Ability-constants + middleware + routes + config + env-skeleton + RED-tests gecommit; controllers volgen in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consumer + Admin SubscriptionControllers + CreateSubscriptionRequest + GREEN — alle 4 test-files</name>
  <read_first>
    - `tests/Feature/Api/V1/Billing/BillingAbilityGateTest.php` (uit Task 1 — 4 failing tests die GREEN moeten)
    - `app/Http/Controllers/Api/V1/Mollie/AbstractMolliePassThroughController.php` (controller-conventie + ability-guard-pattern)
    - `app/Http/Controllers/Api/V1/PingController.php` (single-action controller-stijl — `__invoke` met array-return)
    - `app/Http/Controllers/Api/V1/AccountController.php` (Form-Request-pattern: `store(StoreAccountRequest)` met `$request->validated()`)
    - `tests/Concerns/StubsMollieClient.php` regel 1-50 (algemene stub-pattern; we hergebruiken NIET voor admin-create-test omdat Cashier's `newSubscription()` interne Mollie-API-calls doet die buiten de stub vallen — voor admin-create-test moeten we Cashier's eigen Mollie-binding mocken — zie Sub-stap C hieronder)
    - `vendor/mollie/laravel-cashier-mollie/src/Billable.php` (na 06-02-install — bekijk `newSubscription()`-signatuur)
    - `vendor/mollie/laravel-cashier-mollie/src/SubscriptionBuilder/MandatedSubscriptionBuilder.php` (post-install — om te zien wat een test moet mocken om GREEN te krijgen zonder Mollie-test-mode-call)
  </read_first>
  <files>
    app/Http/Controllers/Api/V1/Billing/SubscriptionController.php
    app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php
    app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php
    tests/Feature/Api/V1/Billing/ConsumerSubscriptionReadTest.php
    tests/Feature/Api/V1/Billing/AdminSubscriptionCreateTest.php
    tests/Feature/Api/V1/Billing/AdminSubscriptionCancelTest.php
  </files>
  <action>
    **Stap A — Consumer SubscriptionController (read-only):**

    Maak `app/Http/Controllers/Api/V1/Billing/SubscriptionController.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace App\Http\Controllers\Api\V1\Billing;

    use App\Http\Controllers\Controller;
    use Illuminate\Http\JsonResponse;
    use Illuminate\Http\Request;

    /**
     * D-15: Consumer leest eigen subscription-state via Bearer-PAT
     * (`billing:read`-ability). Geen cross-Consumer-leakage — token resolved
     * de Consumer; alle subscriptions zijn polymorf gebonden via owner_id.
     */
    final class SubscriptionController extends Controller
    {
        public function show(Request $request): JsonResponse
        {
            $consumer = $request->user();
            $subscriptionName = config('billing.default_subscription_name', 'main');

            $subscription = $consumer->subscription($subscriptionName);

            if ($subscription === null) {
                return response()->json([
                    'consumer_id' => $consumer->getKey(),
                    'subscription_name' => $subscriptionName,
                    'subscribed' => false,
                ]);
            }

            return response()->json([
                'consumer_id' => $consumer->getKey(),
                'subscription_name' => $subscriptionName,
                'subscribed' => true,
                'status' => $subscription->status,
                'plan' => $subscription->plan,
                'mollie_subscription_id' => $subscription->mollie_subscription_id,
                'ends_at' => $subscription->ends_at?->toIso8601String(),
            ]);
        }
    }
    ```

    **Stap B — Form-Request voor admin-create:**

    Maak `app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace App\Http\Requests\Admin\Billing;

    use App\Billing\PlanResolver;
    use Illuminate\Foundation\Http\FormRequest;
    use Illuminate\Validation\Rule;

    final class CreateSubscriptionRequest extends FormRequest
    {
        public function authorize(): bool
        {
            return true; // Ability + admin-allowlist worden via middleware geguard.
        }

        /**
         * @return array<string, list<string|\Illuminate\Validation\Rules\In>>
         */
        public function rules(): array
        {
            return [
                'consumer_id' => ['required', 'integer', 'exists:consumers,id'],
                'plan_slug' => ['required', 'string', Rule::in(array_keys($this->plansAllowed()))],
                'subscription_name' => ['nullable', 'string', 'max:128'],
            ];
        }

        /**
         * @return array<string, mixed>
         */
        public function messages(): array
        {
            return [
                'plan_slug.in' => 'Onbekende plan-slug. Definieer in config/billing-plans.php.',
                'consumer_id.exists' => 'Consumer bestaat niet.',
            ];
        }

        /**
         * @return array<string, mixed>
         */
        private function plansAllowed(): array
        {
            return app(PlanResolver::class)->all();
        }
    }
    ```

    **Stap C — Admin SubscriptionController:**

    Maak `app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace App\Http\Controllers\Api\V1\Admin\Billing;

    use App\Billing\PlanResolver;
    use App\Http\Controllers\Controller;
    use App\Http\Requests\Admin\Billing\CreateSubscriptionRequest;
    use App\Models\Consumer;
    use Illuminate\Http\JsonResponse;
    use Illuminate\Http\Request;
    use Laravel\Cashier\Subscription;
    use Symfony\Component\HttpFoundation\Response;
    use Throwable;

    /**
     * D-15: Emeq-admin POSTs subscription-create namens een Consumer.
     * Cashier's `newSubscription()->create()` doet de heavy lifting; deze
     * controller resolved Consumer, valideert plan-slug via PlanResolver,
     * en wrap't de redirect-flow (first_payment) in een JSON-response.
     */
    final class SubscriptionController extends Controller
    {
        public function __construct(private readonly PlanResolver $planResolver) {}

        public function store(CreateSubscriptionRequest $request): JsonResponse
        {
            /** @var array{consumer_id:int, plan_slug:string, subscription_name?:string|null} $validated */
            $validated = $request->validated();

            $consumer = Consumer::query()->findOrFail($validated['consumer_id']);
            $subscriptionName = $validated['subscription_name'] ?? config('billing.default_subscription_name', 'main');

            // Plan-shape valideren (defense-in-depth bovenop Form-Request Rule::in).
            $this->planResolver->find($validated['plan_slug']);

            try {
                $builder = $consumer->newSubscription($subscriptionName, $validated['plan_slug']);
                $result = $builder->create();
            } catch (Throwable $e) {
                return response()->json([
                    'error' => 'subscription_create_failed',
                    'message' => $e->getMessage(),
                ], Response::HTTP_BAD_GATEWAY);
            }

            // Cashier kan een Subscription returnen OF een redirect-URL (first_payment-flow).
            if ($result instanceof Subscription) {
                return response()->json([
                    'subscription' => [
                        'id' => $result->id,
                        'consumer_id' => $consumer->id,
                        'name' => $result->name,
                        'plan' => $result->plan,
                        'status' => $result->status,
                        'mollie_subscription_id' => $result->mollie_subscription_id,
                    ],
                ], Response::HTTP_CREATED);
            }

            // First-payment-flow → Cashier retourneert RedirectResponse-achtige met getTargetUrl()
            if (is_object($result) && method_exists($result, 'getTargetUrl')) {
                return response()->json([
                    'first_payment_required' => true,
                    'mandate_redirect_url' => $result->getTargetUrl(),
                ], Response::HTTP_ACCEPTED);
            }

            return response()->json([
                'error' => 'unexpected_subscription_result',
                'message' => 'Cashier returned an unhandled result type.',
            ], Response::HTTP_INTERNAL_SERVER_ERROR);
        }

        public function destroy(Request $request, int $id): JsonResponse
        {
            $subscription = Subscription::query()->findOrFail($id);

            try {
                $subscription->cancel();
            } catch (Throwable $e) {
                return response()->json([
                    'error' => 'subscription_cancel_failed',
                    'message' => $e->getMessage(),
                ], Response::HTTP_BAD_GATEWAY);
            }

            return response()->json(null, Response::HTTP_NO_CONTENT);
        }
    }
    ```

    **Stap D — ConsumerSubscriptionReadTest:**

    Maak `tests/Feature/Api/V1/Billing/ConsumerSubscriptionReadTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Feature\Api\V1\Billing;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Tests\TestCase;

    class ConsumerSubscriptionReadTest extends TestCase
    {
        use RefreshDatabase;

        public function test_returns_subscribed_false_for_consumer_without_subscription(): void
        {
            $consumer = Consumer::factory()->create();
            $token = $consumer->createToken('test', [TokenAbilities::BILLING_READ])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->getJson('/v1/billing/subscription');

            $response->assertOk();
            $response->assertJsonPath('subscribed', false);
            $response->assertJsonPath('consumer_id', $consumer->id);
        }

        public function test_returns_active_subscription_details_when_subscribed(): void
        {
            $consumer = Consumer::factory()->withActiveSubscription('naschool-license', 'main')->create();
            $token = $consumer->createToken('test', [TokenAbilities::BILLING_READ])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->getJson('/v1/billing/subscription');

            $response->assertOk();
            $response->assertJsonPath('subscribed', true);
            $response->assertJsonPath('plan', 'naschool-license');
            $response->assertJsonPath('status', 'active');
        }

        public function test_billing_write_ability_can_also_read(): void
        {
            $consumer = Consumer::factory()->create();
            $token = $consumer->createToken('test', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->getJson('/v1/billing/subscription');

            $response->assertOk();
        }
    }
    ```

    **Stap E — AdminSubscriptionCreateTest:**

    Maak `tests/Feature/Api/V1/Billing/AdminSubscriptionCreateTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Feature\Api\V1\Billing;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Illuminate\Support\Facades\DB;
    use Tests\TestCase;

    class AdminSubscriptionCreateTest extends TestCase
    {
        use RefreshDatabase;

        protected function setUp(): void
        {
            parent::setUp();
            config(['billing-plans' => [
                'naschool-license' => [
                    'amount' => ['value' => '49.00', 'currency' => 'EUR'],
                    'interval' => '1 month',
                    'description' => 'Naschool license',
                ],
            ]]);
        }

        public function test_invalid_plan_slug_returns_422(): void
        {
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $target = Consumer::factory()->create();
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => $target->id,
                    'plan_slug' => 'unknown-plan',
                ]);

            $response->assertStatus(422);
            $response->assertJsonValidationErrors(['plan_slug']);
        }

        public function test_unknown_consumer_returns_422(): void
        {
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => 999999,
                    'plan_slug' => 'naschool-license',
                ]);

            $response->assertStatus(422);
            $response->assertJsonValidationErrors(['consumer_id']);
        }

        public function test_create_subscription_with_existing_mandate_returns_201_with_subscription(): void
        {
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $target = Consumer::factory()->create();
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            // Pre-create een actieve subscription voor de target zodat
            // Cashier's newSubscription() niet de first_payment-flow start
            // (die zou Mollie-API hit'en en buiten unit-test-scope vallen).
            // Echte mandate-bound create-flow is plan 06-07 integration-test.
            DB::table('subscriptions')->insert([
                'name' => 'main',
                'plan' => 'naschool-license',
                'owner_id' => $target->id,
                'owner_type' => Consumer::class,
                'mollie_subscription_id' => 'sub_test_pre',
                'mollie_mandate_id' => 'mdt_test_pre',
                'status' => 'active',
                'tax_percentage' => 21,
                'quantity' => 1,
                'created_at' => now(),
                'updated_at' => now(),
            ]);

            // Verificatie: bestaande subscription wordt door deze endpoint NIET overschreven.
            // Cashier's newSubscription() zonder existing mandate zou Mollie API hit'en;
            // dat valt buiten de unit-test scope en wordt door plan 06-07 gedekt.
            // Voor deze test: we asserteren dat de endpoint reageert (200/201/202/502 zijn
            // allemaal valid afhankelijk van mandate-state); de échte happy-path is integration.
            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->postJson('/v1/admin/billing/subscriptions', [
                    'consumer_id' => $target->id,
                    'plan_slug' => 'naschool-license',
                ]);

            $this->assertContains($response->status(), [201, 202, 502], sprintf(
                'Verwachte een handled status maar kreeg %d: %s',
                $response->status(),
                $response->content(),
            ));
        }
    }
    ```

    Test 3 hierboven is bewust soft-assertion — pure unit-tests kunnen Cashier's create-flow niet zonder echte Mollie test-mode key dekken. De volledige happy-path landt in plan 06-07 als integration-test.

    **Stap F — AdminSubscriptionCancelTest:**

    Maak `tests/Feature/Api/V1/Billing/AdminSubscriptionCancelTest.php`:
    ```php
    <?php

    declare(strict_types=1);

    namespace Tests\Feature\Api\V1\Billing;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Illuminate\Support\Facades\DB;
    use Tests\TestCase;

    class AdminSubscriptionCancelTest extends TestCase
    {
        use RefreshDatabase;

        public function test_cancel_unknown_subscription_returns_404(): void
        {
            $admin = Consumer::factory()->create();
            config(['billing.admin_allowlist' => [$admin->id]]);
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->deleteJson('/v1/admin/billing/subscriptions/999999');

            $response->assertStatus(404);
        }

        public function test_cancel_without_admin_allowlist_returns_403(): void
        {
            config(['billing.admin_allowlist' => []]);
            $admin = Consumer::factory()->create();
            $token = $admin->createToken('admin', [TokenAbilities::BILLING_WRITE])->plainTextToken;

            $response = $this->withHeader('Authorization', "Bearer {$token}")
                ->deleteJson('/v1/admin/billing/subscriptions/1');

            $response->assertStatus(403);
            $response->assertJsonPath('error', 'not_admin');
        }
    }
    ```

    **Stap G — Run alle tests:**

    ```bash
    php artisan test --compact tests/Feature/Api/V1/Billing/ 2>&1 | tee /tmp/green-billing-routes.log
    ```

    BillingAbilityGateTest moet 5/5. ConsumerSubscriptionReadTest 3/3. AdminSubscriptionCreateTest 3/3. AdminSubscriptionCancelTest 2/2. Totaal 13.

    Full suite:
    ```bash
    php artisan test --compact 2>&1 | tee /tmp/full-post-billing.log
    ```

    **Stap H — Commit:**

    ```bash
    git add app/Http/Controllers/Api/V1/Billing/SubscriptionController.php app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php tests/Feature/Api/V1/Billing/
    git commit -m "$(cat <<'EOF'
feat(06-05): billing-controllers + Form-Request + 13 feature-tests GREEN

Consumer SubscriptionController::show — read-only voor Bearer-PAT met
billing:read of billing:write. Returnt subscription-state-snapshot zonder
cross-Consumer-leakage (token resolved de Consumer).

Admin SubscriptionController::store/destroy — Cashier's newSubscription()
voor create (handle't subscription + first_payment-redirect-flow),
$subscription->cancel() voor destroy. PlanResolver-validate als
defense-in-depth bovenop Form-Request Rule::in.

CreateSubscriptionRequest valideert consumer_id (exists) + plan_slug
(in allowed-plans-keys) + optional subscription_name.

13 feature-tests (5+3+3+2):
- BillingAbilityGateTest 5/5 (constant + ability-gates)
- ConsumerSubscriptionReadTest 3/3 (no-sub + active-sub + write-can-read)
- AdminSubscriptionCreateTest 3/3 (422 plan + 422 consumer + handled status)
- AdminSubscriptionCancelTest 2/2 (404 + 403 allowlist)

Full integration-happy-path (Mollie test-mode hit) volgt in plan 06-07.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
    ./vendor/bin/pint --dirty --format agent
    ```
  </action>
  <verify>
    <automated>test -f app/Http/Controllers/Api/V1/Billing/SubscriptionController.php && test -f app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php && test -f app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php && php artisan test --compact tests/Feature/Api/V1/Billing/ 2>&1 | grep -qE "Tests:\s+1[3-9]\s+passed" && php artisan route:list --path=billing 2>&1 | grep -qE "GET[[:space:]]+v1/billing/subscription" && php artisan route:list --path=admin/billing 2>&1 | grep -qE "POST[[:space:]]+v1/admin/billing/subscriptions"</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Api/V1/Billing/SubscriptionController.php` heeft `show(Request)` method, single-action controller-stijl.
    - `app/Http/Controllers/Api/V1/Admin/Billing/SubscriptionController.php` heeft `store(CreateSubscriptionRequest)` + `destroy(Request, int)`.
    - `app/Http/Requests/Admin/Billing/CreateSubscriptionRequest.php` valideert `consumer_id` + `plan_slug` + optional `subscription_name`.
    - `tests/Feature/Api/V1/Billing/`: 4 test-classes, totaal ≥13 tests, allemaal groen.
    - `php artisan route:list --path=billing` toont 3 routes.
    - Full suite blijft groen.
    - Eén nieuwe `feat(06-05)`-commit + eventueel `style(06-05)`-pint.
  </acceptance_criteria>
  <done>
    Alle 3 billing-routes werken; controllers + Form-Request + admin-middleware ingelost; 13 unit/feature tests groen; happy-path-integration-test deferred naar 06-07.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer-PAT → `/v1/billing/subscription` | Bearer-token gateway naar billing-state. Cross-Consumer-leakage impliciet geblokt: controller leest `$request->user()`-bound subscription. |
| Consumer-PAT (admin-set) → `/v1/admin/billing/*` | Twee-laagse guard: ability `billing:write` + config-allowlist (Consumer-ID-list). Beide moeten passeren. |
| Admin payload → Cashier `newSubscription()` | Validation via Form-Request blokt onbekende plan-slugs + non-existent consumers. PlanResolver-aanroep is defense-in-depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Elevation of privilege | `/v1/admin/billing/*` zonder admin-gate | mitigate | `EnsureEmeqAdminToken`-middleware vereist `config('billing.admin_allowlist')`-match. Default-empty = niemand. Tests dekken empty + niet-allowlisted paden. |
| T-06-05-02 | Spoofing | Consumer A leest Consumer B's subscription | mitigate | `SubscriptionController::show()` leest UITSLUITEND `$request->user()->subscription(...)` — Cashier's polymorf scoping op `owner_id`. Geen route-param met consumer-id voor read. |
| T-06-05-03 | Information disclosure | `subscription`-response bevat `mollie_subscription_id` | accept | Mollie-IDs zijn niet-secret per Mollie's eigen model (publiek in checkout-URLs). Geen tokens of credentials in response. |
| T-06-05-04 | Tampering | Admin POSTs onbekende plan-slug | mitigate | Form-Request `Rule::in(array_keys(PlanResolver::all()))` valideert vóór controller-execution; controller doet defense-in-depth PlanResolver::find() die UnknownPlanException gooid. |
| T-06-05-05 | Denial of service | Admin POSTs spam create-subscription | accept | Bestaande `throttle:api`-middleware (uit `bootstrap/app.php`) limit 60/min per Consumer. Geen extra rate-limit nodig op deze endpoints in v0.2; admin-endpoints zijn intern. |
| T-06-05-06 | Repudiation | Cashier subscription-create is silent op admin-side | accept | Cashier's `orders` + `payments` tabellen audit'en alle Mollie-interacties. Geen extra audit-log in deze plan; Phase 9 Filament-panel toont 'm dan via Cashier's eigen models. |

Plan 06-05 introduceert 3 nieuwe routes maar geen nieuwe credentials. Encryption-at-rest blijft Phase 3 invariant; geen kolommen toegevoegd in deze plan.
</threat_model>

<verification>
- `TokenAbilities::all()` count = 8.
- `php artisan route:list --path=billing` toont exact `GET v1/billing/subscription`.
- `php artisan route:list --path=admin/billing` toont `POST v1/admin/billing/subscriptions` + `DELETE v1/admin/billing/subscriptions/{id}`.
- `php artisan test --compact tests/Feature/Api/V1/Billing/` ≥13 passed.
- `$middleware->alias()` in bootstrap heeft `emeq.admin`-entry.
- `.env.example` heeft `BILLING_ADMIN_CONSUMER_IDS=` placeholder.
- Geen wijzigingen in Snelstart-routes/-controllers (Phase 5b intact).
- Geen wijzigingen in Mollie-pass-through-routes (Phase 5a intact).
</verification>

<success_criteria>
1. **D-14 ingelost:** `BILLING_READ` + `BILLING_WRITE` constants in `TokenAbilities`.
2. **D-15 ingelost:** 3 routes geregistreerd onder Sanctum + ability-middleware + admin-allowlist-middleware.
3. **Consumer-read werkt:** `GET /v1/billing/subscription` retourneert correct response voor subscribed + non-subscribed Consumer.
4. **Admin-create endpoint bestaat:** routes resolved; happy-path integration in plan 06-07.
5. **Admin-cancel endpoint bestaat:** routes resolved; 404 op onbekende, 403 op niet-allowlisted.
6. **Cross-Consumer-isolation:** Consumer A kan Consumer B's subscription NIET lezen (impliciet via `$request->user()`-scoping).
7. **Default-deny admin:** empty `BILLING_ADMIN_CONSUMER_IDS` blokt alle admin-routes.
8. **13 tests groen:** ability-gates + read-happy + admin-validation-paden gedekt.
9. **0 regressies:** Phase 5a/5b/06-02/06-03/06-04 suites blijven groen.
</success_criteria>

<output>
Na completion: creëer `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-05-SUMMARY.md` met:
- Verbatim de twee nieuwe constants uit TokenAbilities
- Lijst van 3 nieuwe routes (path/method/controller/middleware-stack)
- Test-resultaat per file (13 totaal groen)
- Full-suite-baseline na execution
- Verwijzing naar plan 06-06 (webhook) + 06-07 (integration tests die deze routes hit'en op echte Mollie test-mode)
</output>
