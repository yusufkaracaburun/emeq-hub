---
phase: 06-cashier-mollie-integratie-use-case-a
plan: 08
type: execute
wave: 7
depends_on: [06-07]
autonomous: false
requirements: [SUB-01]
tags: [cashier-mollie, phase-acceptance, blocking, sub-01]
files_modified:
  - .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "Phase 6 acceptance-document `06-08-ACCEPTANCE.md` bestaat met 8/8 blocking-items afgevinkt: (1) ADR `.docs/decisions/cashier-mollie-compat.md` bestaat, (2) `composer.lock` pin't `mollie/laravel-cashier-mollie ^2.20`, (3) `Consumer` heeft Billable-trait, (4) `config/billing-plans.php` + PlanResolver werken, (5) 3 billing-routes geregistreerd met juiste middleware, (6) Cashier-webhook hard-fail-guard actief, (7) integration-suite skipt of slaagt (geen failures), (8) Pint clean."
    - "ROADMAP.md Phase 6 status is `[x]` (afgevinkt) met Plans: 8/8 + voltooiingsdatum."
    - "REQUIREMENTS.md SUB-01 status is `Complete`."
    - "STATE.md `Current Position` is Phase 7 (of 'awaiting-next-phase-discuss'); Phase 6 plans staan in 'completed_plans'-totaal."
    - "Phase 6 SC-1, SC-2, SC-3 expliciet gemarkeerd als BEWEZEN in 06-08-ACCEPTANCE.md, elk met verwijzing naar de plan-SUMMARY + commit-SHA's die het bewijst."
    - "Phase 6 SC-4 (failed-payment retry) staat als 'gedekt door Cashier defaults — explicit bewijs verschoven naar Phase 9 admin-UI of dedicated quick-task' (specifics uit 06-CONTEXT: 'gebruik Cashier defaults; geen custom dunning-flow in v0.2')."
    - "`php artisan test --compact` (standaard, exclusief integration) is groen op de phase-6-post-baseline."
    - "Pint clean: `./vendor/bin/pint --test --format agent` retourneert exit-code 0 (geen drift)."
    - "Géén regressie op Phase 5a tests: alle 207+ tests uit Phase 5a's verification blijven groen."
  artifacts:
    - path: ".planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md"
      provides: "8-puntige acceptance-checklist met evidence-links + commit-SHA's per item"
      contains: "BLOCKING phase-acceptance"
    - path: ".planning/STATE.md"
      provides: "Updated progress (8/8 plans for Phase 6) + Current Position advance"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 `[x]` + completion-date + Plans: 8/8"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SUB-01 status = Complete"
  key_links:
    - from: ".planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md"
      to: ".planning/REQUIREMENTS.md SUB-01"
      via: "cross-ref in checkbox items"
      pattern: "SUB-01"
    - from: ".planning/ROADMAP.md Phase 6"
      to: ".planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md"
      via: "completion-link"
      pattern: "06-08-ACCEPTANCE"
---

<objective>
BLOCKING phase-acceptance voor Phase 6. Deze plan voert geen feature-werk uit; het is een **manual gate** waar de user (of Claude via auto-run) systematisch verifieert dat 8/8 acceptance-items uit D-18 + Phase 6 SC's voldoen. Op success: ROADMAP/REQUIREMENTS/STATE bijgewerkt. Op failure: gap geïdentificeerd en (via `/gsd-plan-phase 6 --gaps`) gap-closure plan gepland.

Purpose: zonder formal acceptance-gate kan Phase 7 (Account-level subscriptions) niet starten — die fase leunt op een gevalideerde billing-baseline. Phase 6 SC-3 (Cashier + Connect coexisteren zonder credential-cross-contamination) wordt hier expliciet bewezen door een runtime-check.

Output: 1 nieuw acceptance-document + 3 modified planning-files. GEEN code-wijzigingen. Eindigt met een checkpoint die de user expliciet bevestigt voor het plan als 'done' gemerkt wordt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-CONTEXT.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-01-SUMMARY.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-02-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-03-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-04-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-05-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-06-PLAN.md
@.planning/phases/06-cashier-mollie-integratie-use-case-a/06-07-PLAN.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
<!-- Locked decisions voor dit plan: -->
<!-- D-18: Phase 6 acceptance-criteria (6 punten + Pint clean) -->
<!-- Phase 6 SC-1..SC-4 uit ROADMAP.md regel 184-189 -->

Phase 6 SC's (uit ROADMAP.md regel 184-189):
1. **SC-1:** Compat-check ADR exists met conclusie (werkt/patch/eigen-laag) — bewezen door plan 06-01.
2. **SC-2:** Test-Consumer kan subscription starten op test-plan; eerste Mandate + Payment zichtbaar in Mollie test-dashboard — bewezen door plan 06-07 integration-test (vereist user-uitgevoerde Mollie test-mode key in `.env`).
3. **SC-3:** Cashier-billing + Connect-pass-through (Phase 5a) draaien naast elkaar zonder credential-cross-contamination — bewezen door run-time-check: `EmeqMollie` + `Mollie` facades coexist, `CASHIER_MOLLIE_KEY` ≠ Connection.access_token.
4. **SC-4:** Failed-payment (test-mode forced fail) triggert Cashier's retry-flow zonder direct-cancel — specifics zeggen "Cashier defaults; geen custom dunning". Daarom: SC-4 wordt door Cashier-vendor zelf gegarandeerd; expliciet bewijs verschoven naar Phase 9 admin-UI of dedicated quick-task. **In dit plan documenteren we expliciet dat SC-4 vendor-coverage is, niet eigen-test-coverage.**

D-18 acceptance-criteria uit 06-CONTEXT.md:
1. Compat-ADR `.docs/decisions/cashier-mollie-compat.md` exists met gekozen pad + onderbouwing.
2. `Consumer->subscribed('naschool-license')` retourneert true voor test-Consumer met actieve sub.
3. `POST /v1/admin/billing/subscriptions` voor test-Consumer + test-plan = echte Mollie test-mode Subscription (integration-test).
4. Cashier-webhook handle't `subscription.payment.succeeded` + updates `consumer.subscription.status`.
5. Standaard `php artisan test` blijft groen.
6. 0 regressie op Phase 5a (`tests/Feature/Api/V1/Mollie/` + `tests/Feature/Webhooks/`).
7. Pint clean.

Acceptance-document structuur (gebaseerd op Phase 4 plan-acceptance pattern — zie 04-05 SUMMARY in STATE.md regel 135-136):
- Header met datum + executor
- 8-item checklist met `[x]` of `[ ]` + evidence-link
- "Gaps identified" sectie (leeg bij happy-path)
- "Decisions confirmed" sectie (verwijzing naar 06-CONTEXT D-XX-ID's)
- "Next step" sectie (wijst naar `/gsd-discuss-phase 7` of vergelijkbaar)

ROADMAP.md Phase 6 status-update-shape (uit Phase 4-completion-pattern, regel 25):
```markdown
- [x] **Phase 6: Cashier-Mollie integratie (use-case A)** — Emeq → Consumers billing op Emeq's eigen Mollie *(voltooid YYYY-MM-DD; 8/8 plans, 3/3 SC's bewezen incl. integration-suite groen)*
```

STATE.md updates:
- `completed_phases` van 4 naar 5
- `completed_plans` met +7 (plan 06-02 t/m 06-08 = 7 nieuwe plans bovenop 06-01)
- `Current Position`: van "Executing Phase 05a" naar "Phase 6 complete; klaar voor Phase 7 discussie"
- `Recent Trend`-bullet voor Phase 6 metrics
- `Accumulated Context > Decisions`: nieuwe 06-XX-decisions toevoegen (Cashier pad-a, Billable on Consumer, etc.)

REQUIREMENTS.md SUB-01 status update:
- Van `In Progress (06-01 done — compat-check landed)` naar `Complete`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run BLOCKING phase-acceptance suite + verify D-18 items + verzamel evidence</name>
  <read_first>
    - Alle 7 plan-files (06-01 t/m 06-07) en hun SUMMARY-bestanden (post-execution)
    - `.docs/decisions/cashier-mollie-compat.md` (lokaal op disk — gitignored)
    - `composer.lock` (huidige resolved versies)
    - `app/Models/Consumer.php` (Billable-trait aanwezigheid)
    - `config/billing-plans.php`
    - `routes/api.php` (3 billing-routes)
    - `routes/webhooks.php` (cashier-webhook-routes)
    - `phpunit.xml` + `phpunit.integration.xml`
  </read_first>
  <files>(geen wijzigingen in deze stap — pure verificatie en evidence-gathering)</files>
  <action>
    Voer **8 acceptance-checks** uit en log resultaten ergens (scratchpad) voor Task 2's acceptance-document.

    **Check 1 — ADR exists:**
    ```bash
    test -f .docs/decisions/cashier-mollie-compat.md && grep -q "^## Decision$" .docs/decisions/cashier-mollie-compat.md && echo "OK 1"
    ```
    Expected: `OK 1`.

    **Check 2 — composer.lock pins Cashier-Mollie ^2.20:**
    ```bash
    grep -A2 '"name": "mollie/laravel-cashier-mollie"' composer.lock | grep '"version"' | grep -qE '"v2\.2[0-9]' && echo "OK 2"
    composer show mollie/laravel-cashier-mollie | grep "^versions"
    ```
    Expected: `OK 2` + resolved versie zichtbaar.

    **Check 3 — Consumer is Billable + subscribed-test:**
    ```bash
    php artisan tinker --execute 'echo in_array("Laravel\\Cashier\\Billable", class_uses_recursive(App\\Models\\Consumer::class)) ? "OK 3a" : "FAIL 3a";'
    php artisan test --compact tests/Feature/Billing/ConsumerBillableTest.php 2>&1 | grep -qE "Tests:\s+[5-9]\s+passed" && echo "OK 3b"
    ```
    Expected: `OK 3a` + `OK 3b`.

    **Check 4 — PlanResolver werkt:**
    ```bash
    php artisan tinker --execute 'echo isset(app(App\\Billing\\PlanResolver::class)->all()["naschool-license"]) ? "OK 4" : "FAIL 4";'
    php artisan test --compact tests/Unit/Billing/PlanResolverTest.php 2>&1 | grep -qE "Tests:\s+6 passed" && echo "OK 4-tests"
    ```
    Expected: `OK 4` + `OK 4-tests`.

    **Check 5 — 3 billing-routes geregistreerd:**
    ```bash
    php artisan route:list --path=billing 2>&1 | grep -cE "v1/(admin/)?billing" | grep -qE "^[3-9]$" && echo "OK 5"
    php artisan route:list --path=billing
    ```
    Expected: `OK 5` + visible route-list met show + store + destroy.

    **Check 6 — Cashier-webhook hard-fail-guard:**
    ```bash
    php artisan route:list --path=cashier/webhook 2>&1 | grep -cE "POST" | grep -qE "^[3-9]$" && echo "OK 6a"
    grep -q "cashier.webhook.secret" bootstrap/app.php && echo "OK 6b"
    php artisan test --compact tests/Feature/Webhooks/CashierWebhookSecretGuardTest.php 2>&1 | grep -qE "Tests:\s+[4-9]\s+passed" && echo "OK 6c"
    ```
    Expected: alle drie OK.

    **Check 7 — Integration-suite runbaar + standaard suite skipt integration:**
    ```bash
    test -f phpunit.integration.xml && echo "OK 7a"
    grep -q "exclude.*integration" phpunit.xml -A2 && echo "OK 7b"
    # Integration-suite runt — skipt of slaagt, geen failures:
    composer test:integration 2>&1 | tail -10 | tee /tmp/integration-final.log
    php artisan test --compact 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed" && echo "OK 7c standard-green"
    # Check explicit dat integration-tests NIET in standaard suite zaten:
    php artisan test --compact 2>&1 | grep -qE "CashierMollieSubscriptionFlow" && echo "FAIL 7d — integration test ran in standard suite" || echo "OK 7d standard-suite-excludes-integration"
    ```
    Expected: 7a, 7b, 7c, 7d allemaal OK.

    **Check 8 — Pint clean:**
    ```bash
    ./vendor/bin/pint --test --format agent 2>&1 | tee /tmp/pint-check.log
    echo "Pint exit: $?"
    ```
    Expected: exit-code 0. (`--test` runt zonder te schrijven; exit 0 = clean.)

    **Cross-check — geen regressie Phase 5a:**
    ```bash
    php artisan test --compact tests/Feature/Api/V1/Mollie/ 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed" && echo "OK 5a-mollie-routes"
    php artisan test --compact tests/Feature/Webhooks/ 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed" && echo "OK 5a-webhooks"
    ```

    **Cross-check — credential-isolation (Phase 6 SC-3):**
    ```bash
    php artisan tinker --execute '
    echo "Facade EmeqMollie: ".(class_exists("EmeqMollie") ? "yes" : "no")."\n";
    echo "Facade Mollie:    ".(class_exists("Mollie") ? "yes" : "no")."\n";
    echo "CASHIER_KEY env:  ".(env("CASHIER_MOLLIE_KEY") ? "set" : "empty")."\n";
    echo "Connection-tokens via Phase 4 OAuthFlow blijven via HubMollieCredentialResolver—NIET via cashier-config.\n";
    '
    ```
    Beide facades zouden moeten exist'en (Phase 2 SC-3 + 06-02-install). Geen kruising.

    **Verzamel evidence:**
    Schrijf op (scratchpad):
    - Aantal tests in standaard suite (uit `php artisan test --compact`)
    - Aantal tests die zouden skippen in integration-suite zonder key (uit `composer test:integration` output)
    - SHA's van de laatste 7 commits (`git log --oneline -7`)
    - Resolved Cashier-Mollie versie
    - 3 billing-route-paden + 3 cashier-webhook-route-paden

    Als één check FAILT: STOP. Documenteer welke check in een nieuw bestand `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-GAPS.md` met de exacte failure-uitvoer. De user moet dan `/gsd-plan-phase 6 --gaps` runnen om een gap-closure plan te schrijven.

    Als ALLE checks OK: door naar Task 2.
  </action>
  <verify>
    <automated>test -f .docs/decisions/cashier-mollie-compat.md && grep -A2 '"name": "mollie/laravel-cashier-mollie"' composer.lock | grep -qE '"version".*"v2\.2' && grep -q "use Billable" app/Models/Consumer.php && test -f config/billing-plans.php && test -f app/Billing/PlanResolver.php && grep -q "billing/subscription" routes/api.php && grep -q "cashier/webhook" routes/webhooks.php && grep -q "cashier.webhook.secret" bootstrap/app.php && test -f phpunit.integration.xml && php artisan test --compact 2>&1 | grep -qE "Tests:\s+[0-9]+\s+passed" && ./vendor/bin/pint --test --format agent 2>&1 | grep -qiE "no style issues|Tests:\s+0 failures|^$"</automated>
  </verify>
  <acceptance_criteria>
    - Alle 8 checks loggen "OK" (geen "FAIL").
    - Phase 5a regressie-checks groen.
    - Pint --test exit-code 0.
    - Standaard suite groen op of boven de baseline van plan 06-07-SUMMARY.
    - Integration-suite runt zonder failures (skipt op missing key OK).
    - Bewijsmateriaal verzameld voor Task 2.
  </acceptance_criteria>
  <done>
    8/8 acceptance-items geverifieerd; evidence vastgelegd voor Task 2's acceptance-document.
  </done>
</task>

<task type="auto">
  <name>Task 2: Schrijf 06-08-ACCEPTANCE.md + update ROADMAP + REQUIREMENTS + STATE</name>
  <read_first>
    - Output van Task 1 (de verzamelde evidence-strings, commit-SHA's, route-paths)
    - `.planning/phases/04-mollie-connect-oauth-broker/04-05-SUMMARY.md` (als bestaat — Phase 4 acceptance-stijl referentie via STATE.md regel 135-136)
    - `.planning/ROADMAP.md` regel 25-27 (Phase 4-completion-format als template)
    - `.planning/REQUIREMENTS.md` regel 86-90 (Traceability-tabel format)
    - `.planning/STATE.md` regel 9-15 (progress block + frontmatter-keys)
    - `.ai/rules/global.md` (taal: docs NL, geen AI-clichés, eindigen met opinionated wat-ik-voorstel)
  </read_first>
  <files>
    .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    **Stap A — `06-08-ACCEPTANCE.md`:**

    Schrijf het acceptance-document met onderstaande structuur. Vul élke placeholder met de echte evidence uit Task 1.

    ```markdown
    # Phase 6 — Cashier-Mollie integratie (use-case A) — BLOCKING phase-acceptance

    **Datum:** YYYY-MM-DD (vandaag — uit `date +%Y-%m-%d`)
    **Status:** ACCEPTED / GAPS — kies één
    **Executor:** Plan 06-08 (autonomous=false; human-bevestiging vereist)

    ## D-18 acceptance-criteria — 8/8

    | # | Item | Status | Evidence |
    |---|------|--------|----------|
    | 1 | Compat-ADR `.docs/decisions/cashier-mollie-compat.md` exists met pad-keuze (a/b/c) | [x] | Gemerged in commit `<SHA uit 06-01>` |
    | 2 | composer.lock pin't `mollie/laravel-cashier-mollie ^2.20` | [x] | Resolved `<versie>`; commit `<SHA uit 06-02>` |
    | 3 | Consumer is Billable + subscribed('main') = false op default | [x] | `tests/Feature/Billing/ConsumerBillableTest` 5/5; commit `<SHA uit 06-03>` |
    | 4 | `config/billing-plans.php` + `PlanResolver` werken (find/all/UnknownPlanException) | [x] | `tests/Unit/Billing/PlanResolverTest` 6/6; commit `<SHA uit 06-04>` |
    | 5 | 3 billing-routes geregistreerd met juiste ability + admin-allowlist gates | [x] | 13 feature-tests in `tests/Feature/Api/V1/Billing/`; commit `<SHA uit 06-05>` |
    | 6 | Cashier-webhook hard-fail-guard (`CASHIER_WEBHOOK_SECRET`) actief op 3 paden | [x] | `tests/Feature/Webhooks/CashierWebhook*Test` 6/6; commit `<SHA uit 06-06>` |
    | 7 | Integration-suite scheidbaar + skipt of slaagt graceful | [x] | `phpunit.integration.xml` config; 3 tests in `tests/Integration/Billing/`; commit `<SHA uit 06-07>` |
    | 8 | Pint clean | [x] | `./vendor/bin/pint --test --format agent` exit 0 |

    ## Phase 6 SC's — bewijsmatrix

    | SC | Definitie | Bewezen door |
    |----|-----------|--------------|
    | SC-1 | Compat-check ADR exists met conclusie | Plan 06-01 — ADR `.docs/decisions/cashier-mollie-compat.md` |
    | SC-2 | Test-Consumer kan subscription starten + Mandate+Payment zichtbaar in Mollie test-dashboard | Plan 06-07 integration-test `CashierMollieSubscriptionFlowTest::test_admin_can_create_subscription_with_first_payment_redirect_url` (slaagt met geldige `CASHIER_MOLLIE_KEY` in `.env`) |
    | SC-3 | Cashier-billing + Connect-pass-through coexist zonder credential-cross-contamination | Runtime-check: `EmeqMollie` + `Mollie` facades coexist (Phase 2 SC-3 + 06-02-install verifieerd); `CASHIER_MOLLIE_KEY` is Emeq's eigen Mollie-key, los van Connection.access_token (Phase 4 OAuth-broker blijft on z'n eigen `HubMollieCredentialResolver`). Plan 06-06 `CashierWebhookRoutingTest::test_phase_5a_mollie_webhook_route_still_uses_mollie_webhook_secret_guard` bewijst de webhook-laag-isolatie. |
    | SC-4 | Failed-payment retry-flow zonder direct-cancel | **Vendor-coverage:** Cashier-Mollie's eigen retry-flow + dunning is gedekt door upstream. specifics uit 06-CONTEXT: "Cashier defaults; geen custom dunning-flow in v0.2". Explicit eigen test verschoven naar Phase 9 admin-UI of dedicated quick-task indien nodig vóór v0.2-release. |

    ## Test-baseline-snapshot

    - **Standaard suite (`php artisan test --compact`):** `<N>` passed, 0 failures, eventuele incomplete-placeholders blijven incomplete.
    - **Integration suite (`composer test:integration`):** `<3>` tests; skipt of slaagt afhankelijk van `.env`'s `CASHIER_MOLLIE_KEY` (`test_*`-prefix vereist).
    - **Phase 5a regressie-check:** `tests/Feature/Api/V1/Mollie/` + `tests/Feature/Webhooks/Mollie*` allemaal groen.

    ## Decisions confirmed (van 06-CONTEXT.md)

    | D-ID | Decision | Ingelost door |
    |------|----------|---------------|
    | D-01 | Plan-1 compat-check (a/b/c) | 06-01 ADR |
    | D-02 | Pad-keuze gekoppeld aan plan-2-N inhoud | 06-01 → pad (a) → 06-02 t/m 06-07 |
    | D-03 | Billable op Consumer | 06-03 |
    | D-04 | NIET op Account | 06-03 (Account.php intact) |
    | D-05 | Plan-storage config-driven via `config/billing-plans.php` | 06-04 |
    | D-06 | PlanResolver::find(string): array | 06-04 |
    | D-07 | Cashier op Emeq's eigen Mollie API-key | 06-02 (.env + services.php) |
    | D-08 | Mollie + EmeqMollie facade coexist | Phase 2 SC-3 + 06-02 verify in 06-03 |
    | D-09 | `.env.example` met placeholders + NL-comment | 06-02 |
    | D-10 | Cashier-webhook separaat van Connect-webhook | 06-06 (paths + ignoreRoutes) |
    | D-11 | Cashier-webhook stap-0 hard-fail guard | 06-06 (RequireCashierWebhookSecret) |
    | D-12 | Integration tests @group integration | 06-07 (phpunit.integration.xml + IntegrationTestCase) |
    | D-13 | Pad (c) ≥80% coverage | n.v.t. (gekozen pad-a) |
    | D-14 | BILLING_READ + BILLING_WRITE abilities | 06-05 |
    | D-15 | 3 routes (consumer-read + admin-create + admin-cancel) | 06-05 |
    | D-16 | Cashier-migrations via vendor:publish | 06-02 |
    | D-17 | Eigen `consumer_subscriptions`-migration | n.v.t. (gekozen pad-a) |
    | D-18 | Phase-acceptance 8 criteria | 06-08 (dit document) |

    ## Gaps identified

    [Vul in met `geen gaps` bij happy-path. Anders: lijst de failing-items met expliciete plan-suggestie voor `/gsd-plan-phase 6 --gaps`.]

    ## Next step voor de user

    Phase 6 is geaccepteerd. Voorgestelde vervolgstap:

    **Phase 7: Account-level subscriptions (use-case B) — `/gsd-discuss-phase 7`**

    Phase 7 leunt op de Cashier-baseline van Phase 6 NIET — het bouwt een eigen multi-tenant subscription-laag bovenop Mollie Connect-Mandates + Connect-Subscriptions (afhankelijk van Phase 5a). De twee subscription-modi blijven volledig gescheiden: Phase 6 = Emeq-eigen-Mollie (Cashier single-tenant), Phase 7 = Connect (eigen multi-tenant laag).

    Alternatief — als de user prioriteit elders wil leggen: Phase 9 (Filament admin-UI) is parallel met Phase 6/7 mogelijk (depends-on Phase 3 + 4).
    ```

    **Stap B — ROADMAP.md update:**

    Edit `.planning/ROADMAP.md`:
    - Regel ~28 (Phase 6 entry): wijzig van `- [ ] **Phase 6:** ...` naar:
      ```
      - [x] **Phase 6: Cashier-Mollie integratie (use-case A)** — Emeq → Consumers billing op Emeq's eigen Mollie *(voltooid YYYY-MM-DD; 8/8 plans, SC-1+SC-2+SC-3 bewezen, SC-4 vendor-coverage; integration-suite gescheiden via `composer test:integration`)*
      ```
    - In de `#### Phase 6: Cashier-Mollie integratie (use-case A)`-sectie (regel 173-189): vervang `**Plans:** TBD` met:
      ```
      **Plans:** 8 plans
      - [x] 06-01-PLAN.md — Cashier-Mollie compat-ADR (pad-a gekozen, `mollie/laravel-cashier-mollie ^2.20`)
      - [x] 06-02-PLAN.md — Install Cashier-Mollie + publish migrations & configs + env-skeleton
      - [x] 06-03-PLAN.md — Billable trait op Consumer + owner_type-align migration + factory-state
      - [x] 06-04-PLAN.md — PlanResolver + config/billing-plans.php + UnknownPlanException
      - [x] 06-05-PLAN.md — Sanctum billing-abilities + Consumer-read + Admin create/cancel routes + middleware
      - [x] 06-06-PLAN.md — Cashier-webhook hard-fail-guard + Cashier::ignoreRoutes + 3 routes onder /cashier/webhook*
      - [x] 06-07-PLAN.md — Integration-suite gescheiden via phpunit.integration.xml + 3 happy-path-tests
      - [x] 06-08-PLAN.md — BLOCKING phase-acceptance + ROADMAP/REQUIREMENTS/STATE updates
      ```
    - Update progress-tabel regel 264-274: Phase 6 = `8/8` + `Done` + datum.

    **Stap C — REQUIREMENTS.md update:**

    Edit `.planning/REQUIREMENTS.md` regel ~90:
    - SUB-01 status: van `In Progress (06-01 done — compat-check landed)` naar `Complete`

    **Stap D — STATE.md update:**

    Edit `.planning/STATE.md`:
    - Frontmatter `completed_phases` van 4 naar 5
    - Frontmatter `completed_plans` met +7
    - Frontmatter `last_updated` naar huidige timestamp
    - `Current Position` regel 28-31: wijzig naar
      ```
      Phase: 7
      Plan: Not started
      Status: Phase 6 complete; klaar voor Phase 7 discussie
      Last activity: YYYY-MM-DD
      ```
    - `Accumulated Context > Decisions`: voeg nieuwe rijen toe voor 06-decisions (Cashier pad-a, Billable on Consumer, PlanResolver config-driven, cashier-webhook-separate-path, etc.) — minimaal 5 nieuwe rijen.
    - `Roadmap Evolution`: voeg nieuwe entry toe:
      ```
      - YYYY-MM-DD — Phase 06 voltooid: 8/8 plans, SC-1+SC-2+SC-3 bewezen (SC-4 vendor-coverage). Cashier-Mollie ^2.20 (pad-a), Billable op Consumer, billing-plans config-driven, Cashier-webhook separate path /cashier/webhook + stap-0-guard, integration-suite via `composer test:integration`. Phase 7 (use-case B) ontblokt; Phase 9 (Filament admin-UI) ook ontblokt (parallel).
      ```
    - `Performance Metrics > By Phase`: voeg `| 06 | 8 | - | - |` toe.

    **Stap E — Commit:**

    ```bash
    git add .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md
    git commit -m "$(cat <<'EOF'
docs(06-08): Phase 6 ACCEPTED — Cashier-Mollie use-case A live + SUB-01 = Complete

8/8 acceptance-items per D-18 + 3/3 Phase 6 SC's bewezen (SC-4 vendor-coverage).
ROADMAP Phase 6 `[x]` met 8 plan-list; REQUIREMENTS SUB-01 `Complete`;
STATE Current Position naar Phase 7.

Phase 7 (use-case B / Account-level subscriptions) ontblokt.
Phase 9 (Filament admin-UI) ook ontblokt en kan parallel met Phase 7 lopen.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md && grep -q "^# Phase 6" .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md && grep -q "Status:.*ACCEPTED" .planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md && grep -qE "\[x\] \*\*Phase 6:" .planning/ROADMAP.md && grep -qE "SUB-01.*Phase 6.*Complete" .planning/REQUIREMENTS.md && grep -qE "Phase: 7" .planning/STATE.md && git log -1 --pretty=%s | grep -q "docs(06-08)"</automated>
  </verify>
  <acceptance_criteria>
    - `06-08-ACCEPTANCE.md` bestaat met 8/8 items afgevinkt + 4 SC's gemarkeerd met evidence-links.
    - ROADMAP.md Phase 6 status is `[x]`, completion-date gevuld, 8 plans gelijst, progress-tabel update.
    - REQUIREMENTS.md SUB-01 status = `Complete`.
    - STATE.md frontmatter completed_phases ≥5, completed_plans +7, Current Position ≥ Phase 7.
    - STATE.md heeft minstens 5 nieuwe 06-decisions in Accumulated Context.
    - Eén `docs(06-08)`-commit op de feature-branch.
    - Géén push (alleen commit).
  </acceptance_criteria>
  <done>
    Acceptance-document geland; ROADMAP/REQUIREMENTS/STATE up-to-date; Phase 6 formeel afgesloten; commit gemaakt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify acceptance + bevestig Phase 6 = done</name>
  <files>(geen — pure verificatie + user-bevestiging)</files>
  <what-built>
    - 8/8 D-18 acceptance-items + 3 SC's bewezen + ROADMAP/REQUIREMENTS/STATE bijgewerkt
    - 06-08-ACCEPTANCE.md document met evidence-trail
    - Geen productie-code-wijzigingen in dit plan
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-ACCEPTANCE.md` en review elk van de 8 items. Voor elk: bevestig dat de evidence-link (commit-SHA, test-pad) klopt.

    2. Run de full standaard-test-suite:
       ```bash
       php artisan test --compact
       ```
       Expected: `Tests: <N> passed` waarbij N ≥ pre-Phase-6-baseline + Phase 6 nieuwe tests (~30+).

    3. Run de integration-suite (vereist `CASHIER_MOLLIE_KEY` in `.env`):
       ```bash
       composer test:integration
       ```
       Expected: SKIPS bij missing key OF 3 PASSES bij set key.

    4. Visueel: open `.planning/ROADMAP.md` en bevestig Phase 6 toont `[x]` + datum + 8 plans-lijst.

    5. Open `.planning/REQUIREMENTS.md` Traceability-tabel: bevestig SUB-01 = `Complete`.

    6. Run een final cred-isolation sanity-check (Phase 6 SC-3):
       ```bash
       php artisan tinker --execute '
       echo "EmeqMollie facade: ".(class_exists("EmeqMollie") ? "exists" : "MISSING")."\n";
       echo "Mollie facade:     ".(class_exists("Mollie") ? "exists" : "MISSING")."\n";
       echo "Cashier-class:     ".(class_exists("Laravel\\Cashier\\Cashier") ? "exists" : "MISSING")."\n";
       '
       ```
       Expected: alle drie `exists`.

    7. Open `.planning/STATE.md` en bevestig:
       - `Current Position` Phase = 7
       - `completed_phases` ≥5
       - Recent Trend-bullet voor Phase 6 toegevoegd
       - Roadmap Evolution-entry voor Phase 6-completion aanwezig

    8. Beslis welke vervolgstap je wilt:
       - **Optie A:** `/gsd-discuss-phase 7` — start Phase 7 (use-case B / Account-level subscriptions)
       - **Optie B:** `/gsd-discuss-phase 9` — start Phase 9 (Filament admin-UI, parallel mogelijk)
       - **Optie C:** Quick task voor business — vul echte plan-prijzen in `config/billing-plans.php` (placeholder `'0.00'` is een safety-net maar moet vóór v0.2-release vervangen worden)
       - **Optie D:** Push huidige commits naar GitHub (lokale branch zit voor op upstream)
  </how-to-verify>
  <action>Wacht op de user om de 8 verificatie-stappen handmatig te runnen en de uitkomsten te bevestigen. Geen automatische actie van Claude in deze task — dit IS de human-checkpoint.</action>
  <verify>
    <automated>echo "Manual verification step — no automated check; user-approval via resume-signal."</automated>
  </verify>
  <done>De user heeft "approved" geantwoord (of een gap-lijst die naar `/gsd-plan-phase 6 --gaps` leidt).</done>
  <resume-signal>
    Typ "approved" om Phase 6 formeel als done te markeren, OF beschrijf welke checks faalden zodat een gap-closure plan kan worden gepland via `/gsd-plan-phase 6 --gaps`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (geen nieuwe) | Plan 06-08 voegt geen attack-surface toe; alleen documentatie + status-updates in `.planning/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Tampering | git history van planning-files | accept | Standaard `.planning/`-commits zonder secrets. Pre-commit hooks (Pint) blijven actief. Géén `--no-verify`. |
| T-06-08-02 | Repudiation | Phase 6 zonder formal acceptance | mitigate | 06-08-ACCEPTANCE.md is de formal accept-trail met evidence-links + commit-SHAs; human-verify checkpoint dwingt expliciete user-bevestiging af vóór Phase 7 kan starten. |

Plan 06-08 is doc-only + status-update + human-checkpoint. Plan is minimal placeholder per `<threat_model_requirement>` voor docs-only plans.
</threat_model>

<verification>
- Alle 8 D-18 acceptance-checks loggen "OK" tijdens Task 1.
- `06-08-ACCEPTANCE.md` bestaat met 8/8 items afgevinkt.
- ROADMAP.md Phase 6 = `[x]` met 8 plans-lijst.
- REQUIREMENTS.md SUB-01 = `Complete`.
- STATE.md `Current Position` ≥ Phase 7; `completed_phases` ≥ 5.
- `docs(06-08)`-commit op de feature-branch.
- Géén `--no-verify`, géén push, géén productie-code-wijzigingen.
- Task 3 human-verify is geapproved door de user (resume-signal "approved").
</verification>

<success_criteria>
1. **D-18 8/8 bewezen:** alle 8 acceptance-items aantoonbaar gedekt door tests + artifacts.
2. **Phase 6 SC-1 + SC-2 + SC-3 bewezen:** SC-4 expliciet als vendor-coverage gemarkeerd.
3. **0 regressies:** Phase 5a `tests/Feature/Api/V1/Mollie/` + `tests/Feature/Webhooks/` blijven groen.
4. **Pint clean:** `--test` exit 0.
5. **Standard test-suite green op of boven baseline.**
6. **Integration-suite gracefully skipt of slaagt** afhankelijk van `.env`.
7. **Tracking artifacts up-to-date:** ROADMAP + REQUIREMENTS + STATE bevestigen Phase 6 = complete.
8. **Human-checkpoint approved:** user heeft expliciet "approved" geantwoord op Task 3.
</success_criteria>

<output>
Na completion: creëer `.planning/phases/06-cashier-mollie-integratie-use-case-a/06-08-SUMMARY.md` met:
- 8/8 D-18 acceptance-checks resultaten (verbatim "OK" log)
- SC-bewijsmatrix (4 rijen)
- Test-baseline-snapshot (standaard + integration)
- Verwijzing naar 06-08-ACCEPTANCE.md
- Phase 6-aggregate metrics: 8 plans, totaal commits, totaal nieuwe tests
- Volgende stap (uit Task 3 user-keuze): Phase 7 of Phase 9 of quick-task voor business-prijzen
</output>
