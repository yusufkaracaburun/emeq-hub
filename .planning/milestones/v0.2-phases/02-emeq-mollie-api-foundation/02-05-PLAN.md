---
phase: 02-emeq-mollie-api-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-04
files_modified:
  - packages/mollie-api/tests/TestCase.php
  - packages/mollie-api/tests/Pest.php
  - packages/mollie-api/tests/ArchTest.php
  - packages/mollie-api/tests/PackageSmokeTest.php
  - packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "Testbench TestCase laadt MollieServiceProvider zodat alle Pest-tests Laravel app-state hebben"
    - "Pest.php hookt uses(TestCase::class)->in(__DIR__) en runt Cache::flush() voor elke test"
    - "ArchTest verifieert dat de hele Emeq\\MollieApi-namespace strict_types declareert en geen debug-funcs gebruikt"
    - "PackageSmokeTest verifieert dat MollieServiceProvider boot + config('mollie.enforce_environment') leesbaar + MollieApiClient::class binding non-singleton + MissingCredentialResolverException wordt gegooid zonder resolver"
    - "FakeMollieCredentialResolver kan zowel API-key als OAuth credentials returnen (default = test_-api-key voor backwards-compat met tests)"
    - "cd packages/mollie-api && ./vendor/bin/pest --filter=PackageSmokeTest|ArchTest slaagt met exit 0"
  artifacts:
    - path: "packages/mollie-api/tests/TestCase.php"
      provides: "Orchestra Testbench base met MollieServiceProvider"
      contains: "MollieServiceProvider::class"
    - path: "packages/mollie-api/tests/Pest.php"
      provides: "Pest bootstrap + Cache::flush hook"
      contains: "uses(TestCase::class)"
    - path: "packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php"
      provides: "Test-double voor MollieCredentialResolver met static factories voor beide credential-types"
      contains: "implements MollieCredentialResolver"
    - path: "packages/mollie-api/tests/PackageSmokeTest.php"
      provides: "End-to-end SP-boot test + config + bindings + missing-resolver-guard"
      contains: "MissingCredentialResolverException"
    - path: "packages/mollie-api/tests/ArchTest.php"
      provides: "Arch tests: strict_types + no debug funcs"
      contains: "toUseStrictTypes"
  key_links:
    - from: "tests/Pest.php"
      to: "tests/TestCase.php"
      via: "uses(TestCase::class)->in(__DIR__)"
      pattern: "uses\\(TestCase::class\\)"
    - from: "tests/Support/FakeMollieCredentialResolver.php"
      to: "src/Contracts/MollieCredentialResolver.php"
      via: "implements MollieCredentialResolver"
      pattern: "implements MollieCredentialResolver"
    - from: "tests/PackageSmokeTest.php"
      to: "src/MollieServiceProvider.php"
      via: "app->getProvider(MollieServiceProvider::class)"
      pattern: "MollieServiceProvider::class"
---

<objective>
Zet de Pest-test-infrastructuur op: Testbench TestCase + Pest bootstrap + Arch-tests + PackageSmokeTest + FakeMollieCredentialResolver. Dit is de "groene Pest-suite kan draaien" mijlpaal — alle tests in plan 02-06 en 02-07 leunen op deze infrastructuur.

Purpose: Snelstart-SDK's pattern: één TestCase + Pest.php met Cache-flush hook + ArchTest + PackageSmokeTest. Mollie spiegelt 1-op-1, plus een FakeMollieCredentialResolver test-double die zowel API-key als OAuth credentials kan returnen (anders dan Snelstart's enkele credential-type).

B-1 dependency-restructure: depends_on bevat nu expliciet 02-01 (Pest + Testbench + Mollie-lib leven in `vendor/` van 02-01) naast 02-04 (MollieServiceProvider + Mollie facade-target). Wave 3.

Output: 5 test-files; Pest moet `--filter=PackageSmokeTest|ArchTest` succesvol draaien (≥5 tests in PackageSmoke + 2 arch-tests = ≥7 tests groen).

W-6 reminder: PackageSmokeTest gebruikt GEEN `->secret()` meer — credential-checks gaan via de subclass public typed property (bv. `->apiKey`) of via `->fingerprint()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
Outputs van plan 02-04 (al in working tree):
  Emeq\MollieApi\MollieServiceProvider — Spatie DSL, bindt Mollie::class + MollieApiClient::class
  Emeq\MollieApi\Mollie                  — facade-target, ::client() factory
  Emeq\MollieApi\Facades\Mollie          — facade alias

Outputs van plan 02-02 (al in working tree):
  Emeq\MollieApi\Contracts\MollieCredentialResolver — resolve(): MollieCredentials
  Emeq\MollieApi\Data\MollieApiKeyCredentials       — ctor(string $apiKey), isTestMode(): bool, getSecretMaterial() protected
  Emeq\MollieApi\Data\MollieOAuthCredentials        — ctor(string $accessToken, ?int $expiresAt), getSecretMaterial() protected

Outputs van plan 02-03 (al in working tree):
  Emeq\MollieApi\Exceptions\MissingCredentialResolverException — ::notBound()

Outputs van plan 02-01 (al geïnstalleerd):
  packages/mollie-api/vendor/orchestra/testbench
  packages/mollie-api/vendor/pestphp/pest

Snelstart-SDK referentie:
  packages/snelstart-api/tests/TestCase.php           — getEnvironmentSetUp + getPackageProviders
  packages/snelstart-api/tests/Pest.php               — uses(TestCase) + beforeEach Cache::flush
  packages/snelstart-api/tests/ArchTest.php           — strict_types + no debug funcs
  packages/snelstart-api/tests/Support/FakeCredentialResolver.php — implements + ::with() factory
  packages/snelstart-api/tests/Unit/PackageSmokeTest.php — SP boot, config, bindings

Mollie-library:
  Mollie\Api\MollieApiClient — kan via app(MollieApiClient::class) opgevraagd worden via MollieServiceProvider's binding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestCase + Pest.php + FakeMollieCredentialResolver</name>
  <read_first>
    - packages/snelstart-api/tests/TestCase.php (1-op-1 pattern; provider-class swap)
    - packages/snelstart-api/tests/Pest.php (1-op-1)
    - packages/snelstart-api/tests/Support/FakeCredentialResolver.php (referentie ::with-factory)
    - packages/mollie-api/src/MollieServiceProvider.php
    - packages/mollie-api/src/Contracts/MollieCredentialResolver.php
    - packages/mollie-api/src/Data/MollieApiKeyCredentials.php
    - packages/mollie-api/src/Data/MollieOAuthCredentials.php
  </read_first>
  <files>
    packages/mollie-api/tests/TestCase.php
    packages/mollie-api/tests/Pest.php
    packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php
  </files>
  <action>
Maak `packages/mollie-api/tests/TestCase.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Tests;

use Emeq\MollieApi\MollieServiceProvider;
use Orchestra\Testbench\TestCase as Orchestra;

class TestCase extends Orchestra
{
    protected function getEnvironmentSetUp($app): void
    {
        config()->set('database.default', 'testing');
    }

    protected function getPackageProviders($app): array
    {
        return [
            MollieServiceProvider::class,
        ];
    }
}
```

Maak `packages/mollie-api/tests/Pest.php`:

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Tests\TestCase;
use Illuminate\Support\Facades\Cache;

uses(TestCase::class)->in(__DIR__);

// Laravel's array cache persists within the Testbench process, so a prior
// test can poison cache state for the next test. Flushing here keeps each
// test hermetic — same pattern as the Snelstart SDK.
uses()->beforeEach(function (): void {
    Cache::flush();
})->in(__DIR__);
```

Maak `packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Tests\Support;

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Data\MollieCredentials;
use Emeq\MollieApi\Data\MollieOAuthCredentials;

/**
 * Test-double for MollieCredentialResolver.
 *
 * Three construction modes:
 *  - FakeMollieCredentialResolver::withApiKey('test_xxx')
 *  - FakeMollieCredentialResolver::withOAuth('access_xxx', expiresAt: 9999)
 *  - FakeMollieCredentialResolver::sequence([cred1, cred2, ...])
 *
 * Or constructed directly with one-or-more MollieCredentials instances.
 *
 * For multi-tenant resolver tests where the credentials change between
 * resolve() calls, use ::sequence([...]) and the resolver returns each
 * credential in turn (cycling back to the start once exhausted).
 *
 * Or use the API-key-shortcut sequence: FakeMollieCredentialResolver::sequence([
 *     MollieApiKeyCredentials::class => ['test_a', 'test_b'],
 * ])
 * (handy for plan 02-06 multi-tenant key-swap test, B-6).
 */
final class FakeMollieCredentialResolver implements MollieCredentialResolver
{
    /** @var list<MollieCredentials> */
    private array $sequence;

    private int $index = 0;

    public function __construct(MollieCredentials ...$credentials)
    {
        if ([] === $credentials) {
            throw new \InvalidArgumentException('FakeMollieCredentialResolver needs at least one credential.');
        }

        $this->sequence = array_values($credentials);
    }

    public static function withApiKey(string $apiKey = 'test_fake_default'): self
    {
        return new self(new MollieApiKeyCredentials($apiKey));
    }

    public static function withOAuth(string $accessToken = 'access_fake_default', ?int $expiresAt = null): self
    {
        return new self(new MollieOAuthCredentials($accessToken, $expiresAt));
    }

    /**
     * Accepts either:
     *  - a plain list<MollieCredentials>, e.g. [new MollieApiKeyCredentials('test_a'), ...]
     *  - a shortcut map { FQCN => list<string> } for API-key/access-token strings:
     *      [MollieApiKeyCredentials::class => ['test_a', 'test_b']]
     *      [MollieOAuthCredentials::class  => ['access_a', 'access_b']]
     *
     * @param  list<MollieCredentials>|array<class-string<MollieCredentials>, list<string>>  $credentials
     */
    public static function sequence(array $credentials): self
    {
        $instances = [];

        // Detect shortcut-map form: keyed by FQCN, value is list<string>.
        foreach ($credentials as $key => $value) {
            if (is_string($key) && is_array($value)) {
                $fqcn = $key;
                foreach ($value as $raw) {
                    $instances[] = match ($fqcn) {
                        MollieApiKeyCredentials::class => new MollieApiKeyCredentials($raw),
                        MollieOAuthCredentials::class  => new MollieOAuthCredentials($raw),
                        default                        => throw new \InvalidArgumentException(
                            sprintf('Unsupported credential class in sequence shortcut: %s', $fqcn),
                        ),
                    };
                }

                continue;
            }

            // Plain list form
            if ($value instanceof MollieCredentials) {
                $instances[] = $value;
            }
        }

        return new self(...$instances);
    }

    public function resolve(): MollieCredentials
    {
        $credentials = $this->sequence[$this->index % count($this->sequence)];
        $this->index++;

        return $credentials;
    }
}
```

De `sequence()`-factory ondersteunt nu zowel de plain-list form als de FQCN-keyed shortcut-map form. Plan 02-06's multi-tenant test (B-6) gebruikt de shortcut form.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'namespace Emeq\\\\MollieApi\\\\Tests' tests/TestCase.php &amp;&amp; grep -q 'MollieServiceProvider::class' tests/TestCase.php &amp;&amp; grep -q 'uses(TestCase::class)' tests/Pest.php &amp;&amp; grep -q 'Cache::flush' tests/Pest.php &amp;&amp; grep -q 'implements MollieCredentialResolver' tests/Support/FakeMollieCredentialResolver.php &amp;&amp; grep -q 'withApiKey' tests/Support/FakeMollieCredentialResolver.php &amp;&amp; grep -q 'withOAuth' tests/Support/FakeMollieCredentialResolver.php &amp;&amp; grep -q 'sequence' tests/Support/FakeMollieCredentialResolver.php &amp;&amp; grep -q 'MollieApiKeyCredentials::class' tests/Support/FakeMollieCredentialResolver.php</automated>
  </verify>
  <done>TestCase laadt MollieServiceProvider, Pest.php uses TestCase + flusht cache, FakeMollieCredentialResolver heeft 3 factories (withApiKey, withOAuth, sequence — sequence accepteert beide vormen) en implementeert het contract.</done>
</task>

<task type="auto">
  <name>Task 2: ArchTest + PackageSmokeTest + groene Pest-run (zonder ->secret() — W-6)</name>
  <read_first>
    - packages/snelstart-api/tests/ArchTest.php (1-op-1 pattern; namespace-swap)
    - packages/snelstart-api/tests/Unit/PackageSmokeTest.php (referentie smoke-test shape)
    - packages/mollie-api/tests/TestCase.php (task 1 — moet bestaan)
    - packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php (task 1)
  </read_first>
  <files>
    packages/mollie-api/tests/ArchTest.php
    packages/mollie-api/tests/PackageSmokeTest.php
  </files>
  <action>
Maak `packages/mollie-api/tests/ArchTest.php`:

```php
<?php

declare(strict_types=1);

arch('it will not use debugging functions')
    ->expect(['dd', 'dump', 'ray', 'var_dump'])
    ->each->not->toBeUsed();

arch('strict types declared everywhere')
    ->expect('Emeq\MollieApi')
    ->toUseStrictTypes();
```

Maak `packages/mollie-api/tests/PackageSmokeTest.php` (W-6: gebruik `->apiKey` of `->fingerprint()` i.p.v. niet meer bestaande `->secret()`):

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Exceptions\MissingCredentialResolverException;
use Emeq\MollieApi\Facades\Mollie as MollieFacade;
use Emeq\MollieApi\Mollie;
use Emeq\MollieApi\MollieServiceProvider;
use Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver;
use Mollie\Api\MollieApiClient;

it('registers the service provider', function (): void {
    $provider = app()->getProvider(MollieServiceProvider::class);

    expect($provider)->toBeInstanceOf(MollieServiceProvider::class);
});

it('publishes the mollie config with expected defaults', function (): void {
    expect(config('mollie.enforce_environment'))->toBeFalse()
        ->and(config('mollie.http.timeout'))->toBe(30)
        ->and(config('mollie.idempotency.generator'))->toBeNull();
});

it('throws MissingCredentialResolverException when no resolver is bound', function (): void {
    expect(fn () => app(Mollie::class))
        ->toThrow(
            MissingCredentialResolverException::class,
            'No ' . MollieCredentialResolver::class . ' binding found',
        );
});

it('resolves the main Mollie facade-target when a resolver is bound', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_smoke'));

    $mollie = app(Mollie::class);

    $resolved = $mollie->credentials();

    expect($mollie)->toBeInstanceOf(Mollie::class)
        ->and($resolved)->toBeInstanceOf(MollieApiKeyCredentials::class)
        ->and($resolved->apiKey)->toBe('test_smoke')
        ->and($resolved->fingerprint())->toBe(substr(hash('sha256', 'test_smoke'), 0, 12));
});

it('resolves the Mollie facade through the container', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_facade'));

    expect(MollieFacade::getFacadeRoot())->toBeInstanceOf(Mollie::class);
});

it('binds MollieApiClient as a non-singleton (fresh per resolve)', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_per_resolve'));

    $a = app(MollieApiClient::class);
    $b = app(MollieApiClient::class);

    expect($a)->toBeInstanceOf(MollieApiClient::class)
        ->and($b)->toBeInstanceOf(MollieApiClient::class)
        ->and($a)->not->toBe($b);
});
```

Run de Pest-suite om te bevestigen dat de bootstrap groen is. Gebruik `--filter` om de andere test-files (uit plan 02-06/02-07) te overslaan — die bestaan misschien nog niet of zijn nog niet wired:

```bash
cd packages/mollie-api
./vendor/bin/pest --filter='PackageSmoke|Arch'
```

Verwacht: exit 0, ≥7 tests groen (6 in PackageSmoke + 2 in Arch + 7 in Data uit plan 02-02 = ≥15 als hele suite). Faalt iets:
- "Class Emeq\MollieApi\MollieServiceProvider not found" → composer dump-autoload draaien (plan 02-04 task 2 had dat moeten doen)
- "Cannot redeclare class" → pest is misschien stale; `rm -rf .phpunit.cache build/phpstan` en opnieuw
- "Class Mollie\Api\MollieApiClient not found" → `composer install` is niet gedraaid; doe `composer install` in packages/mollie-api/ eerst
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q "arch('it will not use debugging functions')" tests/ArchTest.php &amp;&amp; grep -q "arch('strict types declared everywhere')" tests/ArchTest.php &amp;&amp; grep -q 'getProvider(MollieServiceProvider::class)' tests/PackageSmokeTest.php &amp;&amp; grep -q "config('mollie.enforce_environment')" tests/PackageSmokeTest.php &amp;&amp; grep -q 'MissingCredentialResolverException' tests/PackageSmokeTest.php &amp;&amp; grep -q 'MollieApiClient' tests/PackageSmokeTest.php &amp;&amp; grep -q 'expect($a)-&gt;not-&gt;toBe($b)' tests/PackageSmokeTest.php &amp;&amp; ! grep -q '-&gt;secret()' tests/PackageSmokeTest.php &amp;&amp; ./vendor/bin/pest --filter='PackageSmoke|Arch' 2&gt;&amp;1 | tail -5 | grep -qE 'Tests:.*passed|PASS'</automated>
  </verify>
  <done>ArchTest heeft 2 arch-assertions. PackageSmokeTest heeft 6 tests over SP-boot, config-publishing, missing-resolver-guard, facade-resolution, en non-singleton MollieApiClient. Geen `->secret()`-aanroepen — credential-asserts gaan via `->apiKey` of `->fingerprint()` (W-6). `./vendor/bin/pest --filter='PackageSmoke|Arch'` is groen.</done>
</task>

</tasks>

<verification>
- grep `MollieServiceProvider::class` in tests/TestCase.php → 1 hit
- grep `uses(TestCase::class)->in(__DIR__)` in tests/Pest.php → 1 hit
- grep `implements MollieCredentialResolver` in tests/Support/FakeMollieCredentialResolver.php → 1 hit
- grep `withApiKey|withOAuth|sequence` in tests/Support/FakeMollieCredentialResolver.php → ≥3 hits
- 2 arch-rules in tests/ArchTest.php
- 6 `it(...)` blocks in tests/PackageSmokeTest.php
- Geen `->secret()`-aanroepen in tests/PackageSmokeTest.php (W-6)
- `./vendor/bin/pest --filter='PackageSmoke|Arch'` exits 0 in packages/mollie-api/
</verification>

<success_criteria>
- Pest-bootstrap werkt: een test krijgt een geconfigureerde Laravel-app via Testbench
- Cache wordt voor elke test geflusht
- ArchTest dwingt strict_types + no-debug-funcs af voor de Emeq\MollieApi namespace
- PackageSmokeTest dekt: SP-boot, config-defaults, missing-resolver-guard, resolver-binding, facade-resolution, non-singleton MollieApiClient binding
- FakeMollieCredentialResolver kan zowel API-key als OAuth credentials returnen en heeft een sequence-mode (twee vormen: plain-list en FQCN-keyed-shortcut) voor cross-tenant-tests in plan 02-06
- De Data-tests uit plan 02-02 (MollieApiKeyCredentialsTest, MollieOAuthCredentialsTest, MollieCredentialsFingerprintTest) zouden ook moeten draaien nu Pest-bootstrap er is — desgewenst valideren via `./vendor/bin/pest tests/Unit/Data`
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-05-SUMMARY.md`
</output>
</content>
</invoke>