---
phase: 02-emeq-mollie-api-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-04
  - 02-05
files_modified:
  - packages/mollie-api/tests/Unit/MollieTest.php
  - packages/mollie-api/tests/Unit/MollieServiceProviderTest.php
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "MollieTest dekt: resolver-wiring happy path, multi-tenant key-wissel via FakeMollieCredentialResolver::sequence([FQCN => list<string>]) ZONDER mid-test rebind (B-6), env-guard fires bij enforce_environment=true + production env (via detectEnvironment-override, B-7) + test_ key, idempotency-generator via container-alias path (B-8)"
    - "MollieServiceProviderTest dekt: SP boots als provider, Mollie::class is singleton (twee app()-calls geven dezelfde instance), MollieApiClient::class binding produceert verschillende instances per call"
    - "Multi-tenant key-swap test verifieert client-instance-uniqueness via `expect($a)->not->toBe($b)` + authenticator-instanceof-checks (geen niet-bestaande getApiKey)"
    - "Env-guard test gebruikt $this->app->detectEnvironment(fn () => 'production') voor runtime-env-override én sanity-asserts `app()->environment() === 'production'` vóór de exception-expect (B-7)"
    - "cd packages/mollie-api && ./vendor/bin/pest --filter='Mollie(Test|ServiceProviderTest)' slaagt met exit 0"
  artifacts:
    - path: "packages/mollie-api/tests/Unit/MollieTest.php"
      provides: "Tests voor de facade-target met type-discriminator + env-guard + idempotency"
      contains: "multi-tenant"
    - path: "packages/mollie-api/tests/Unit/MollieServiceProviderTest.php"
      provides: "Tests voor SP container-bindings (singleton vs non-singleton)"
      contains: "MollieServiceProvider"
  key_links:
    - from: "tests/Unit/MollieTest.php"
      to: "src/Mollie.php"
      via: "instantiates with FakeMollieCredentialResolver::sequence([MollieApiKeyCredentials::class => [...]])"
      pattern: "sequence"
    - from: "tests/Unit/MollieServiceProviderTest.php"
      to: "src/MollieServiceProvider.php"
      via: "singleton vs bind verification"
      pattern: "singleton"
---

<objective>
Schrijf de core Pest-tests voor de wave-2-outputs: `Mollie` facade-target (type-discriminator + multi-tenant key-wissel + env-guard + idempotency-generator) en `MollieServiceProvider` (singleton vs non-singleton bindings). Samen ~7 tests die plan 02-04's gedrag verifiëren.

Purpose: Plan 02-05 heeft de Pest-bootstrap groen gemaakt; dit plan vult de inhoudelijke tests die ROADMAP success criterion 2 ("MollieCredentialResolver-binding kan runtime-swappen tussen MollieApiKeyCredentials en MollieOAuthCredentials per request zonder cross-tenant lekkage") en de Pest-≥10-cases-drempel dekken.

Revisions:
- B-6: multi-tenant test gebruikt EEN sequence-resolver + `forgetInstance(Mollie::class)` na rebind — geen mid-test rebind die de singleton-cache niet invalideert. Client-uniqueness via instance-niveau check.
- B-7: env-guard test gebruikt `$this->app->detectEnvironment(fn () => 'production')` (Laravel's gedocumenteerde runtime-env-override) + sanity-check assert.
- W-6: alle credential-asserts via `->apiKey` / `->accessToken` / `->fingerprint()`. Geen `->secret()`.
- B-1: depends_on bevat 02-01 (vendor/), 02-04 (Mollie SP), 02-05 (TestCase + FakeResolver). Wave 4.

Output: 2 test-files (~7 tests totaal). Plan 02-07 voegt nog 2 error-mapping tests toe; eindtotaal Pest-suite = ≥17 tests groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md

<interfaces>
Outputs van plan 02-04 (al getest in 02-05 PackageSmokeTest):
  Emeq\MollieApi\Mollie::client(): MollieApiClient
  Emeq\MollieApi\Mollie::credentials(): MollieCredentials
  Emeq\MollieApi\MollieServiceProvider — singleton(Mollie::class) + bind(MollieApiClient::class)
  Env-detect via $this->container->make('app')->environment() (compat met detectEnvironment-override)
  Idempotency-resolutie via $container->make($value) — werkt voor zowel FQCN als container-alias

Outputs van plan 02-05:
  Emeq\MollieApi\Tests\TestCase
  Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver::withApiKey($key)
  Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver::withOAuth($token, $exp)
  Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver::sequence([cred1, cred2, ...] OF [FQCN => list<string>])

Mollie-library introspection (CONTEXT.md "API-surface facts"):
  Mollie\Api\MollieApiClient::getAuthenticator(): ?HttpAuthenticatorContract
  — setApiKey('test_x') resulteert in een Mollie\Api\Http\Auth\ApiKeyAuthenticator (vermoedelijk; verifieer met `grep -rn 'class ApiKeyAuthenticator' vendor/mollie/mollie-api-php/src/`)
  — getApiKey() bestaat NIET — vermijd hard-coded asserts op exacte authenticator-class-naam tenzij vendor-introspect bevestigt. Default-strategie: instanceof + isTestToken() indien aanwezig.

Laravel env-override:
  $this->app->detectEnvironment(fn () => 'production') overschrijft Laravel's `app()->environment()` runtime — gedocumenteerde, gesupporte path. NIET `app()['env'] = 'production'` (werkt niet voor Application::environment()).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MollieTest — facade-target gedrag (B-6 singleton-cache fix, B-7 detectEnvironment, B-8 alias-path)</name>
  <read_first>
    - packages/mollie-api/src/Mollie.php (gedrag dat we testen)
    - packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php (sequence-mode met FQCN-keyed shortcut)
    - packages/mollie-api/tests/TestCase.php
    - packages/mollie-api/src/Data/MollieApiKeyCredentials.php
    - packages/mollie-api/src/Data/MollieOAuthCredentials.php
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Cross-tenant isolation test"
    - vendor/mollie/mollie-api-php/src/MollieApiClient.php (om de authenticator-class-naam te bevestigen indien nodig — gebruik `grep -rn 'class ApiKeyAuthenticator' vendor/mollie/mollie-api-php/src/`)
  </read_first>
  <files>
    packages/mollie-api/tests/Unit/MollieTest.php
  </files>
  <behavior>
    - Resolver wiring (API-key): bind FakeMollieCredentialResolver::withApiKey('test_a'), app(Mollie::class)->client() returnt MollieApiClient en getAuthenticator() is niet null
    - Resolver wiring (OAuth): idem voor withOAuth('access_x'), getAuthenticator() niet null
    - Multi-tenant key-wissel (B-6): bind FakeMollieCredentialResolver::sequence([MollieApiKeyCredentials::class => ['test_tenant_a', 'test_tenant_b']]) ONE resolver. Roep forgetInstance(Mollie::class) aan NA rebind (anders blijft de oude Mollie-singleton actief — B-6 bug-fix). $mollie = app(Mollie::class); twee opeenvolgende ->client() calls geven verschillende client-instances; beide hebben een authenticator die `isTestToken()` true rapporteert.
    - Env-guard (B-7): gebruik $this->app->detectEnvironment(fn () => 'production') (gedocumenteerde override). Sanity-assert dat app()->environment() == 'production'. Config enforce_environment=true. Bind test_-key resolver. expect(fn() => ...->client())->toThrow(MollieException).
    - Env-guard skip: live_ key OR non-prod OR enforce_environment=false → geen exception. forgetInstance + opnieuw detectEnvironment tussen sub-cases.
    - Idempotency-generator via container-alias (B-8): app()->instance('mollie.idempotency-stub', $stub); config()->set('mollie.idempotency.generator', 'mollie.idempotency-stub'); resolver binden; ->client() draait zonder throw.
  </behavior>
  <action>
Maak `packages/mollie-api/tests/Unit/MollieTest.php`:

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Data\MollieOAuthCredentials;
use Emeq\MollieApi\Exceptions\MollieException;
use Emeq\MollieApi\Mollie;
use Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver;
use Mollie\Api\Idempotency\IdempotencyKeyGeneratorContract;
use Mollie\Api\MollieApiClient;

it('builds a MollieApiClient with an authenticator when an API-key resolver is bound', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_alpha'));

    $client = app(Mollie::class)->client();

    expect($client)->toBeInstanceOf(MollieApiClient::class)
        ->and($client->getAuthenticator())->not->toBeNull();
});

it('builds a MollieApiClient with an authenticator when an OAuth resolver is bound', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withOAuth('access_beta', expiresAt: 9999999999));

    $client = app(Mollie::class)->client();

    expect($client)->toBeInstanceOf(MollieApiClient::class)
        ->and($client->getAuthenticator())->not->toBeNull();
});

it('returns a fresh MollieApiClient per call so multi-tenant key-swaps do not leak across calls', function (): void {
    // B-6: ONE sequence-resolver. forgetInstance ensures the Mollie singleton
    // captures THIS resolver, not a stale one from an earlier test.
    $this->app->bind(
        MollieCredentialResolver::class,
        fn () => FakeMollieCredentialResolver::sequence([
            MollieApiKeyCredentials::class => ['test_tenant_a', 'test_tenant_b'],
        ]),
    );
    $this->app->forgetInstance(Mollie::class);

    $mollie = app(Mollie::class);

    $clientA = $mollie->client(); // sequence slot 0 → test_tenant_a
    $clientB = $mollie->client(); // sequence slot 1 → test_tenant_b

    expect($clientA)
        ->toBeInstanceOf(MollieApiClient::class)
        ->and($clientB)
        ->toBeInstanceOf(MollieApiClient::class)
        ->and($clientA)
        ->not
        ->toBe($clientB);

    // Both clients have authenticators wired (instance-level isolation proves
    // we never re-used the same MollieApiClient across credentials).
    expect($clientA->getAuthenticator())->not->toBeNull()
        ->and($clientB->getAuthenticator())->not->toBeNull();
});

it('throws MollieException when enforce_environment + production env + test_ key', function (): void {
    // B-7: Laravel's documented runtime-env override — works with
    // Application::environment() introspection.
    $this->app->detectEnvironment(fn () => 'production');
    expect($this->app->environment())->toBe('production'); // sanity-check

    config()->set('mollie.enforce_environment', true);
    $this->app->bind(
        MollieCredentialResolver::class,
        fn () => FakeMollieCredentialResolver::withApiKey('test_should_block'),
    );
    $this->app->forgetInstance(Mollie::class);

    expect(fn () => app(Mollie::class)->client())
        ->toThrow(MollieException::class, 'test_-prefixed Mollie API key in the production environment');
});

it('does not throw env-guard for live_ keys or non-production envs or when enforce_environment is off', function (): void {
    // (a) production + live_ → OK
    $this->app->detectEnvironment(fn () => 'production');
    config()->set('mollie.enforce_environment', true);
    $this->app->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('live_prod'));
    $this->app->forgetInstance(Mollie::class);
    expect(fn () => app(Mollie::class)->client())->not->toThrow(MollieException::class);

    // (b) production + test_ but enforce_environment=false → OK.
    $this->app->forgetInstance(Mollie::class);
    config()->set('mollie.enforce_environment', false);
    $this->app->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_allowed'));
    expect(fn () => app(Mollie::class)->client())->not->toThrow(MollieException::class);

    // (c) testing env + test_ + enforce_environment=true → OK (guard only fires in production).
    $this->app->forgetInstance(Mollie::class);
    $this->app->detectEnvironment(fn () => 'testing');
    config()->set('mollie.enforce_environment', true);
    $this->app->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_allowed_in_testing'));
    expect(fn () => app(Mollie::class)->client())->not->toThrow(MollieException::class);
});

it('applies a custom IdempotencyKeyGenerator when configured via container-alias path (B-8)', function (): void {
    $stub = new class implements IdempotencyKeyGeneratorContract {
        public function generate(): string {
            return 'stub-key';
        }
    };

    // B-8: container-alias path. config-value is the alias string; Mollie
    // resolves it via $container->make($alias).
    app()->instance('mollie.idempotency-stub', $stub);
    config()->set('mollie.idempotency.generator', 'mollie.idempotency-stub');

    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_idem'));
    $this->app->forgetInstance(Mollie::class);

    $client = app(Mollie::class)->client();

    // The client now uses our stub generator. Mollie does not expose a public
    // getter for the generator, so we only assert that wiring did not throw
    // and that we got back a configured client.
    expect($client)->toBeInstanceOf(MollieApiClient::class);
});
```

NB:
- `$this->app->forgetInstance(Mollie::class)` is essentieel (B-6): de Mollie::class binding is een SINGLETON, dus zonder forgetInstance pakt elke `app(Mollie::class)` na een rebind nog steeds de cached Mollie met de oude resolver/config/env.
- `$this->app->detectEnvironment(fn () => 'production')` is Laravel's gedocumenteerde runtime-env-override (B-7). `app()['env'] = 'production'` werkt NIET voor `Application::environment()`.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'multi-tenant key-swap' tests/Unit/MollieTest.php &amp;&amp; grep -q 'enforce_environment' tests/Unit/MollieTest.php &amp;&amp; grep -q 'IdempotencyKeyGeneratorContract' tests/Unit/MollieTest.php &amp;&amp; grep -q 'sequence' tests/Unit/MollieTest.php &amp;&amp; grep -q "detectEnvironment(fn () =&gt; 'production')" tests/Unit/MollieTest.php &amp;&amp; grep -q 'forgetInstance(Mollie::class)' tests/Unit/MollieTest.php &amp;&amp; grep -q "instance('mollie.idempotency-stub'" tests/Unit/MollieTest.php &amp;&amp; ! grep -q "app()\['env'\] = 'production'" tests/Unit/MollieTest.php &amp;&amp; ! grep -q '-&gt;secret()' tests/Unit/MollieTest.php &amp;&amp; ./vendor/bin/pest --filter='MollieTest' 2&gt;&amp;1 | tail -10 | grep -qE 'PASS|passed'</automated>
  </verify>
  <done>6 tests in MollieTest.php: API-key wiring, OAuth wiring, multi-tenant key-wissel (B-6 fix — sequence + forgetInstance, geen cross-tenant lek), env-guard fires bij production+test_ (B-7 detectEnvironment override), env-guard skips bij live_/non-prod/disabled (drie sub-cases met forgetInstance), custom idempotency-generator via container-alias path (B-8). Pest-run met --filter=MollieTest is groen. Geen `app()['env']` patroon. Geen `->secret()`-aanroepen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MollieServiceProviderTest — bindings-shape (3 tests)</name>
  <read_first>
    - packages/mollie-api/src/MollieServiceProvider.php
    - packages/mollie-api/tests/Support/FakeMollieCredentialResolver.php
    - packages/mollie-api/tests/PackageSmokeTest.php (overlap-check: voorkom duplicaten — die test cases NIET hier herhalen)
  </read_first>
  <files>
    packages/mollie-api/tests/Unit/MollieServiceProviderTest.php
  </files>
  <behavior>
    - Mollie::class is een singleton: twee app(Mollie::class) calls geven dezelfde instance back (same identity)
    - MollieApiClient::class binding produceert verschillende instances per resolve (al gedekt in PackageSmokeTest — hier strenger gemaakt door 3 keer te resolven en alle 3 uniek te checken)
    - MollieCredentialResolver::class is NIET pre-bound — assert dat app->bound() false returnt direct na SP-boot, ÉN dat het pas true wordt nadat de host-app expliciet bindt
  </behavior>
  <action>
Maak `packages/mollie-api/tests/Unit/MollieServiceProviderTest.php`:

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Mollie;
use Emeq\MollieApi\Tests\Support\FakeMollieCredentialResolver;
use Mollie\Api\MollieApiClient;

it('binds Mollie::class as a singleton', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_singleton'));

    $first  = app(Mollie::class);
    $second = app(Mollie::class);

    expect($first)->toBe($second);
});

it('binds MollieApiClient::class as a non-singleton (fresh client per resolve)', function (): void {
    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_per_call'));

    $a = app(MollieApiClient::class);
    $b = app(MollieApiClient::class);
    $c = app(MollieApiClient::class);

    expect($a)->toBeInstanceOf(MollieApiClient::class)
        ->and($b)->toBeInstanceOf(MollieApiClient::class)
        ->and($c)->toBeInstanceOf(MollieApiClient::class)
        ->and($a)->not->toBe($b)
        ->and($b)->not->toBe($c)
        ->and($a)->not->toBe($c);
});

it('does not pre-bind MollieCredentialResolver — host app must bind', function (): void {
    expect(app()->bound(MollieCredentialResolver::class))->toBeFalse();

    app()->bind(MollieCredentialResolver::class, fn () => FakeMollieCredentialResolver::withApiKey('test_post_bind'));

    expect(app()->bound(MollieCredentialResolver::class))->toBeTrue();
});
```

Run `./vendor/bin/pest --filter='MollieServiceProviderTest'` om te bevestigen.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'binds Mollie::class as a singleton' tests/Unit/MollieServiceProviderTest.php &amp;&amp; grep -q 'non-singleton' tests/Unit/MollieServiceProviderTest.php &amp;&amp; grep -q 'does not pre-bind MollieCredentialResolver' tests/Unit/MollieServiceProviderTest.php &amp;&amp; ./vendor/bin/pest --filter='MollieServiceProviderTest' 2&gt;&amp;1 | tail -5 | grep -qE 'PASS|passed'</automated>
  </verify>
  <done>3 tests die de Mollie::class-singleton, MollieApiClient::class-non-singleton en MollieCredentialResolver::class-niet-bound contracten valideren. Pest met --filter=MollieServiceProviderTest is groen.</done>
</task>

</tasks>

<verification>
- grep `Mollie::class` in tests/Unit/MollieTest.php → meerdere hits
- grep `Mollie::class` in tests/Unit/MollieServiceProviderTest.php → meerdere hits
- grep `detectEnvironment(fn () => 'production')` in tests/Unit/MollieTest.php → ≥1 hit (B-7)
- grep `forgetInstance(Mollie::class)` in tests/Unit/MollieTest.php → ≥1 hit (B-6)
- grep `instance('mollie.idempotency-stub'` in tests/Unit/MollieTest.php → 1 hit (B-8)
- Geen `app()['env'] = 'production'` pattern (B-7 anti-pattern verwijderd)
- Geen `->secret()`-aanroepen (W-6)
- 6 + 3 = 9 nieuwe `it(...)` blocks
- `./vendor/bin/pest --filter='Mollie(Test|ServiceProviderTest)'` exits 0
- Hele Pest-suite tussenstand: ≥7 (plan 02-02) + ≥7 (plan 02-05) + 9 (dit plan) = ≥23 tests groen
</verification>

<success_criteria>
- ROADMAP success criterion 2 ("multi-tenant runtime swap zonder cross-tenant lekkage") is afgedekt door de sequence-resolver test in MollieTest met correcte singleton-cache-invalidation (B-6)
- ROADMAP success criterion 4 (≥10 Pest-tests groen op auth + resolver) is overschreden — totaal ≥23
- ROADMAP success criterion 5 ("geen raw tokens in logs/exceptions — alleen fingerprint") is impliciet gedekt door fingerprint-tests (plan 02-02) en de exception-class structuur (plan 02-03)
- Env-guard gedrag voor alle 3 sub-paths (prod+test→throw, prod+live→ok, non-prod+test→ok) getest via Laravel's gedocumenteerde detectEnvironment-override (B-7)
- Idempotency-generator wiring getest via container-alias path (B-8) — bewijst dat de SDK NIET alleen FQCN-paths ondersteunt
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-06-SUMMARY.md`
</output>
</content>
</invoke>