---
phase: 02-emeq-mollie-api-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - packages/mollie-api/composer.json
  - packages/mollie-api/src/MollieServiceProvider.php
  - packages/mollie-api/src/Mollie.php
  - packages/mollie-api/src/Facades/Mollie.php
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "composer.json krijgt nu pas `scripts.post-autoload-dump: @composer run prepare` + `scripts.prepare: @php vendor/bin/testbench package:discover --ansi` (W-2 Optie B — gewacht tot MollieServiceProvider concreet bestaat)"
    - "MollieServiceProvider gebruikt Spatie package-tools DSL met hasConfigFile('mollie')"
    - "MollieServiceProvider::packageRegistered() bindt Mollie::class als singleton dat MissingCredentialResolverException gooit als geen MollieCredentialResolver is gebonden"
    - "MollieServiceProvider bindt MollieApiClient::class als non-singleton (per-resolve, voor multi-tenant runtime-swap)"
    - "Mollie::class facade-target heeft een client(): MollieApiClient methode die de resolver invokeert"
    - "Mollie::client() doet type-discriminator: setApiKey() voor MollieApiKeyCredentials, setAccessToken() voor MollieOAuthCredentials"
    - "Mollie::client() gooit MollieException als enforce_environment=true en de Laravel-env productie is en de credentials een test_ prefix hebben"
    - "Mollie::client() leest config('mollie.idempotency.generator') als nullable string en honoreert ZOWEL FQCN ALS container-alias paths (B-8) — beide via $container->make($value)"
    - "Facades\\Mollie::getFacadeAccessor() returnt Emeq\\MollieApi\\Mollie::class, en de facade-docblock typeert credentials() met de union return-type MollieApiKeyCredentials|MollieOAuthCredentials (W-7)"
  artifacts:
    - path: "packages/mollie-api/composer.json"
      provides: "Updated composer.json met post-autoload-dump/prepare scripts (W-2 Optie B)"
      contains: "post-autoload-dump"
    - path: "packages/mollie-api/src/MollieServiceProvider.php"
      provides: "Laravel ServiceProvider met container-bindings"
      contains: "extends PackageServiceProvider"
    - path: "packages/mollie-api/src/Mollie.php"
      provides: "Facade-target met client() factory en dual idempotency-generator resolution (FQCN of alias)"
      contains: "public function client()"
    - path: "packages/mollie-api/src/Facades/Mollie.php"
      provides: "Laravel facade alias met union-typed credentials() docblock"
      contains: "extends Facade"
  key_links:
    - from: "src/MollieServiceProvider.php"
      to: "src/Mollie.php"
      via: "singleton(Mollie::class, ...)"
      pattern: "singleton.*Mollie::class"
    - from: "src/Mollie.php"
      to: "Mollie\\Api\\MollieApiClient"
      via: "new MollieApiClient() + setApiKey/setAccessToken"
      pattern: "MollieApiClient"
    - from: "src/Mollie.php"
      to: "src/Data/MollieApiKeyCredentials.php"
      via: "match($creds instanceof MollieApiKeyCredentials)"
      pattern: "MollieApiKeyCredentials"
    - from: "src/Mollie.php"
      to: "src/Data/MollieOAuthCredentials.php"
      via: "match($creds instanceof MollieOAuthCredentials)"
      pattern: "MollieOAuthCredentials"
    - from: "src/Facades/Mollie.php"
      to: "src/Mollie.php"
      via: "getFacadeAccessor returns Mollie::class"
      pattern: "Emeq.*MollieApi.*Mollie::class"
---

<objective>
Bouw de glue van het pakket: de `MollieServiceProvider` (container-wiring), de `Mollie::class` facade-target (resolver→credentials→`MollieApiClient` factory) en de `Facades\Mollie::class` facade-alias. Dit is het hart van Phase 2 — alle andere plans hangen hieraan. Voeg ook nu pas (W-2 Optie B) het `post-autoload-dump: @composer run prepare` script toe aan `composer.json` omdat MollieServiceProvider vanaf dit plan concreet bestaat.

Purpose: De facade-target leeft tussen de host-app (die de resolver bindt) en de Mollie-library (die de client biedt). `Mollie::client()` doet runtime-typematch op de credentials en swapped tussen `setApiKey()` (API-key flow) en `setAccessToken()` (Mollie Connect OAuth flow). Dit is wat multi-tenant per-request mogelijk maakt zonder cross-tenant lekkage.

B-1 dependency-restructure: depends_on bevat nu expliciet 02-01 (vendor/spatie/laravel-package-tools, vendor/mollie/mollie-api-php zijn nodig vanuit composer install). Wave 2.

Output: 3 productie-files + 1 composer.json edit. Tests volgen in plan 02-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md

<interfaces>
Outputs van plan 02-01 (al in working tree):
  packages/mollie-api/composer.json — heeft GEEN post-autoload-dump/prepare yet (W-2 Optie B)
  packages/mollie-api/vendor/ — composer install gedraaid

Outputs van plan 02-02 (al in working tree):
  Emeq\MollieApi\Contracts\MollieCredentialResolver — interface resolve(): MollieCredentials
  Emeq\MollieApi\Data\MollieCredentials              — abstract readonly, getSecretMaterial() protected, fingerprint() public
  Emeq\MollieApi\Data\MollieApiKeyCredentials        — public string $apiKey, isTestMode(): bool
  Emeq\MollieApi\Data\MollieOAuthCredentials        — public string $accessToken, public ?int $expiresAt

Outputs van plan 02-03 (al in working tree):
  Emeq\MollieApi\Exceptions\MollieException                          — RuntimeException
  Emeq\MollieApi\Exceptions\MissingCredentialResolverException       — ::notBound()

Snelstart-SDK referentie (1-op-1 outer-layer pattern):
  packages/snelstart-api/src/SnelstartServiceProvider.php — Spatie package-tools DSL + bindings
  packages/snelstart-api/src/Snelstart.php                — facade-target (resolver→authenticator→connector)
  packages/snelstart-api/src/Facades/Snelstart.php        — alias boilerplate

Mollie-library API-facts (uit plan-agent research, CONTEXT.md "API-surface facts"):
  Mollie\Api\MollieApiClient::__construct($client = null, ?MollieHttpAdapterPickerContract $adapter = null, ?IdempotencyKeyGeneratorContract $gen = null)
  Mollie\Api\MollieApiClient::setApiKey(string $apiKey): self
  Mollie\Api\MollieApiClient::setAccessToken(string $accessToken): self
  Mollie\Api\MollieApiClient::setIdempotencyKeyGenerator(IdempotencyKeyGeneratorContract $gen): self
  Mollie\Api\MollieApiClient::getAuthenticator(): ?HttpAuthenticatorContract
  — setProfileId() bestaat NIET
  — getApiKey() bestaat NIET (geen public getter — gebruik getAuthenticator())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: composer.json — voeg post-autoload-dump/prepare scripts toe (W-2 Optie B)</name>
  <read_first>
    - packages/mollie-api/composer.json (zoals geschreven in 02-01 — heeft scripts.test/analyse/format maar nog GEEN post-autoload-dump)
    - packages/snelstart-api/composer.json (referentie — heeft beide entries)
  </read_first>
  <files>
    packages/mollie-api/composer.json
  </files>
  <action>
Edit `packages/mollie-api/composer.json`: vul de `scripts` block aan met `post-autoload-dump` + `prepare`. Resulterende `scripts` block moet er zo uitzien (volgorde mag verschillen; Pint formatting wordt later toegepast):

```json
"scripts": {
    "post-autoload-dump": "@composer run prepare",
    "prepare": "@php vendor/bin/testbench package:discover --ansi",
    "analyse": "vendor/bin/phpstan analyse",
    "test": "vendor/bin/pest",
    "test-coverage": "vendor/bin/pest --coverage",
    "format": "vendor/bin/pint"
},
```

**LET OP — geen `composer dump-autoload` in deze task.** De scripts `post-autoload-dump` en `prepare` zijn nu in `composer.json` toegevoegd, maar Testbench's `package:discover` zou crashen op een class-not-found voor `Emeq\MollieApi\MollieServiceProvider` zolang die nog niet bestaat (komt pas in Task 2 van dit plan). Task 2 voert zelf `composer dump-autoload` uit nadat de SP geschreven is — dáár triggert `package:discover` voor het eerst correct.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q '"post-autoload-dump": "@composer run prepare"' composer.json &amp;&amp; grep -q '"prepare": "@php vendor/bin/testbench package:discover --ansi"' composer.json</automated>
  </verify>
  <done>composer.json bevat nu `post-autoload-dump` + `prepare` scripts. `composer dump-autoload` draait nog NIET in deze task — Task 2 doet dat ná SP-creatie zodat package:discover groen loopt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Mollie facade-target met type-discriminator + dual-path idempotency-generator</name>
  <read_first>
    - packages/snelstart-api/src/Snelstart.php (referentie facade-target pattern)
    - packages/mollie-api/src/Contracts/MollieCredentialResolver.php (resolve() return type)
    - packages/mollie-api/src/Data/MollieApiKeyCredentials.php (isTestMode helper)
    - packages/mollie-api/src/Data/MollieOAuthCredentials.php
    - packages/mollie-api/src/Exceptions/MollieException.php
    - packages/mollie-api/config/mollie.php (B-8 docblock van idempotency.generator)
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Mollie::client() flow"
  </read_first>
  <files>
    packages/mollie-api/src/Mollie.php
  </files>
  <behavior>
    - Mollie::client() roept resolver->resolve() aan en bouwt een fresh MollieApiClient
    - Mollie::client() returnt aparte instanties per call (NIET cached); twee opeenvolgende calls = twee aparte objects
    - MollieApiKeyCredentials triggert setApiKey($creds->apiKey) op de client
    - MollieOAuthCredentials triggert setAccessToken($creds->accessToken) op de client
    - Wanneer config('mollie.enforce_environment') === true en app()->environment('production') en credentials zijn MollieApiKeyCredentials met test_ prefix → throw MollieException
    - Wanneer config('mollie.idempotency.generator') is een nullable string → $container->make($value) wordt aangeroepen. Beide paden werken (B-8): zowel een fully-qualified class name (`'App\\X\\JobAware'`) als een container alias (`'mollie.idempotency-generator'`) resolven via dezelfde call.
    - Mollie::credentials() returnt de huidige resolved credentials (handig voor logging/fingerprint)
  </behavior>
  <action>
Maak `packages/mollie-api/src/Mollie.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi;

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Data\MollieCredentials;
use Emeq\MollieApi\Data\MollieOAuthCredentials;
use Emeq\MollieApi\Exceptions\MollieException;
use Illuminate\Contracts\Config\Repository as ConfigRepository;
use Illuminate\Contracts\Container\Container;
use Mollie\Api\Idempotency\IdempotencyKeyGeneratorContract;
use Mollie\Api\MollieApiClient;

/**
 * Main client + facade target for emeq/mollie-api.
 *
 * Builds per-tenant Mollie\Api\MollieApiClient instances on demand: every call
 * to client() resolves the current credentials via the bound resolver and
 * constructs a fresh MollieApiClient with either setApiKey() (API-key flow)
 * or setAccessToken() (Mollie Connect OAuth flow).
 *
 * Multi-tenancy is achieved by NOT singleton-ing the underlying client — each
 * call resolves credentials anew, so a tenant-switch within the same request
 * lifecycle (e.g. processing multiple jobs in a queue worker) produces a
 * fresh client with the new credentials.
 */
class Mollie
{
    public function __construct(
        private readonly MollieCredentialResolver $resolver,
        private readonly ConfigRepository $config,
        private readonly Container $container,
    ) {
    }

    /**
     * Resolve the credentials for the *current* tenant context. Subsequent
     * calls re-invoke the resolver — useful when the host app switches
     * tenants mid-request (queue worker processing multiple jobs).
     */
    public function credentials(): MollieCredentials
    {
        return $this->resolver->resolve();
    }

    /**
     * Build a fresh Mollie\Api\MollieApiClient configured with the current
     * tenant's credentials. Returns a NEW instance on every call — never
     * cache or share across tenants.
     */
    public function client(): MollieApiClient
    {
        $creds = $this->credentials();

        $this->guardEnvironment($creds);

        $client = new MollieApiClient();

        match (true) {
            $creds instanceof MollieApiKeyCredentials => $client->setApiKey($creds->apiKey),
            $creds instanceof MollieOAuthCredentials  => $client->setAccessToken($creds->accessToken),
        };

        $this->applyIdempotencyGenerator($client);

        return $client;
    }

    /**
     * Apply a custom IdempotencyKeyGenerator if configured.
     *
     * config('mollie.idempotency.generator') accepts ANY of:
     *   (a) a fully-qualified class name implementing IdempotencyKeyGeneratorContract
     *       (e.g. 'App\\Mollie\\JobAwareIdempotencyGenerator')
     *   (b) a container alias / binding key that resolves to such an instance
     *       (e.g. 'mollie.idempotency-generator' bound via $app->bind() or
     *        $app->instance()).
     *
     * Both paths route through $container->make($value), so Laravel's container
     * resolves them identically. We only enforce the resulting object's contract.
     */
    private function applyIdempotencyGenerator(MollieApiClient $client): void
    {
        $value = $this->config->get('mollie.idempotency.generator');

        if (null === $value) {
            return;
        }

        $generator = $this->container->make($value);

        if ( ! $generator instanceof IdempotencyKeyGeneratorContract) {
            throw new MollieException(sprintf(
                'config(mollie.idempotency.generator) must resolve to %s; got %s.',
                IdempotencyKeyGeneratorContract::class,
                is_object($generator) ? $generator::class : gettype($generator),
            ));
        }

        $client->setIdempotencyKeyGenerator($generator);
    }

    /**
     * Production env-guard: refuse to hand out a Mollie client wired with a
     * test_-prefixed API key when running in production AND enforce_environment
     * is on. Prevents accidental use of a test-key against real money.
     *
     * OAuth credentials are never subject to this guard — Mollie's OAuth flow
     * does not have a test/live prefix on the access-token; the test/live mode
     * is determined per-request via the testmode flag on the Mollie call.
     */
    private function guardEnvironment(MollieCredentials $creds): void
    {
        if ( ! $this->config->get('mollie.enforce_environment', false)) {
            return;
        }

        if ('production' !== $this->container->make('app')->environment()) {
            return;
        }

        if ($creds instanceof MollieApiKeyCredentials && $creds->isTestMode()) {
            throw new MollieException(
                'Refusing to use a test_-prefixed Mollie API key in the production environment ' .
                '(mollie.enforce_environment=true). Provide a live_ key.',
            );
        }
    }
}
```

Belangrijk:
- De ctor injecteert `ConfigRepository` en `Container` als interfaces (NIET `app()` als helper) — dat maakt unit-testen via direct-instantiate clean.
- Environment-detection gaat via `$this->container->make('app')->environment()` — werkt samen met Laravel's `detectEnvironment()` override (zie plan 02-06 env-guard test, B-7).
- `MollieApiClient` instantiëren via `new` (geen factory) — Mollie's lib heeft een no-arg ctor.
- `match (true)` zonder default-arm: PHP gooit `UnhandledMatchError` als een nieuw credential-type later wordt toegevoegd zonder branch — dat is opzettelijk (fail-fast).
- B-8: idempotency-generator-resolutie via één `$container->make($value)` — werkt voor zowel FQCN-paden als alias-paden zonder onderscheid.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'class Mollie' src/Mollie.php &amp;&amp; grep -q 'public function client(): MollieApiClient' src/Mollie.php &amp;&amp; grep -q '$client-&gt;setApiKey' src/Mollie.php &amp;&amp; grep -q '$client-&gt;setAccessToken' src/Mollie.php &amp;&amp; grep -q 'MollieApiKeyCredentials' src/Mollie.php &amp;&amp; grep -q 'MollieOAuthCredentials' src/Mollie.php &amp;&amp; grep -q 'enforce_environment' src/Mollie.php &amp;&amp; grep -q 'setIdempotencyKeyGenerator' src/Mollie.php &amp;&amp; grep -q "container-&gt;make(\$value)" src/Mollie.php &amp;&amp; grep -q "container-&gt;make('app')-&gt;environment()" src/Mollie.php</automated>
  </verify>
  <done>Mollie::class definieert client() + credentials(); doet type-discriminator op MollieApiKeyCredentials vs MollieOAuthCredentials; honoreert enforce_environment in production via container-aware environment-detect; ondersteunt custom IdempotencyKeyGenerator via config-string die ZOWEL FQCN ALS container-alias mag zijn (B-8).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MollieServiceProvider met container-bindings</name>
  <read_first>
    - packages/snelstart-api/src/SnelstartServiceProvider.php (referentie Spatie DSL + bindings)
    - packages/mollie-api/src/Mollie.php (task 1 output — wat dit plan singleton-binds)
    - packages/mollie-api/src/Contracts/MollieCredentialResolver.php
    - packages/mollie-api/src/Exceptions/MissingCredentialResolverException.php
    - packages/mollie-api/config/mollie.php (uit plan 02-01)
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Container-bindings"
  </read_first>
  <files>
    packages/mollie-api/src/MollieServiceProvider.php
  </files>
  <behavior>
    - configurePackage() registreert package-name 'mollie-api' + hasConfigFile('mollie')
    - packageRegistered() bindt Mollie::class als singleton dat de resolver consumert
    - Wanneer geen MollieCredentialResolver is gebonden bij eerste app(Mollie::class) → throws MissingCredentialResolverException::notBound()
    - MollieApiClient::class krijgt een NON-singleton binding (`bind`, niet `singleton`) die delegeert naar app(Mollie::class)->client()
    - GEEN binding van MollieCredentialResolver zelf — host-app MUST bind
  </behavior>
  <action>
Maak `packages/mollie-api/src/MollieServiceProvider.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi;

use Emeq\MollieApi\Contracts\MollieCredentialResolver;
use Emeq\MollieApi\Exceptions\MissingCredentialResolverException;
use Illuminate\Contracts\Foundation\Application;
use Mollie\Api\MollieApiClient;
use Spatie\LaravelPackageTools\Package;
use Spatie\LaravelPackageTools\PackageServiceProvider;

class MollieServiceProvider extends PackageServiceProvider
{
    public function configurePackage(Package $package): void
    {
        $package
            ->name('mollie-api')
            ->hasConfigFile('mollie');
    }

    public function packageRegistered(): void
    {
        // The credential resolver is intentionally NOT bound here — the host
        // app must provide its own. Resolving Mollie::class without a
        // resolver throws a helpful exception rather than Laravel's generic
        // BindingResolutionException.
        $this->app->singleton(Mollie::class, function (Application $app): Mollie {
            if ( ! $app->bound(MollieCredentialResolver::class)) {
                throw MissingCredentialResolverException::notBound();
            }

            return new Mollie(
                resolver: $app->make(MollieCredentialResolver::class),
                config: $app->make('config'),
                container: $app,
            );
        });

        // MollieApiClient is bound NON-singleton on purpose: every resolve
        // produces a fresh client wired with the current tenant's credentials.
        // Use this binding when you want to inject MollieApiClient directly
        // into a class constructor and not go through the facade.
        $this->app->bind(
            MollieApiClient::class,
            fn (Application $app): MollieApiClient => $app->make(Mollie::class)->client(),
        );
    }
}
```

Geen alias-registratie hier — auto-discovery in composer.json `extra.laravel.aliases` regelt dat (zie plan 02-01).

Na het schrijven: run `composer dump-autoload` om Testbench's `package:discover` te triggeren (post-autoload-dump van task 0). Dit moet nu groen lopen omdat `Emeq\MollieApi\MollieServiceProvider` autoloadable is.

```bash
cd packages/mollie-api
composer dump-autoload
```
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'class MollieServiceProvider extends PackageServiceProvider' src/MollieServiceProvider.php &amp;&amp; grep -q "-&gt;name('mollie-api')" src/MollieServiceProvider.php &amp;&amp; grep -q "-&gt;hasConfigFile('mollie')" src/MollieServiceProvider.php &amp;&amp; grep -q 'singleton(Mollie::class' src/MollieServiceProvider.php &amp;&amp; grep -q 'MissingCredentialResolverException::notBound' src/MollieServiceProvider.php &amp;&amp; grep -q 'bind(' src/MollieServiceProvider.php &amp;&amp; grep -q 'MollieApiClient::class' src/MollieServiceProvider.php &amp;&amp; composer dump-autoload --no-interaction 2&gt;&amp;1 | tail -5 | grep -qiE 'generated|files'</automated>
  </verify>
  <done>ServiceProvider gebruikt Spatie DSL, registreert 'mollie-api' package-name, publiceert config-file 'mollie', bindt Mollie::class als singleton met resolver-guard, en bindt MollieApiClient::class als non-singleton. composer dump-autoload (met post-autoload-dump/prepare scripts active) draait groen.</done>
</task>

<task type="auto">
  <name>Task 3: Facades\Mollie alias boilerplate met union-typed credentials() (W-7)</name>
  <read_first>
    - packages/snelstart-api/src/Facades/Snelstart.php (1-op-1 copy met klassnaam-swap)
    - packages/mollie-api/src/Mollie.php (task 1 — voor de FQCN-string)
    - packages/mollie-api/src/Data/MollieApiKeyCredentials.php (W-7 docblock-typing)
    - packages/mollie-api/src/Data/MollieOAuthCredentials.php (W-7 docblock-typing)
  </read_first>
  <files>
    packages/mollie-api/src/Facades/Mollie.php
  </files>
  <action>
Maak `packages/mollie-api/src/Facades/Mollie.php` (W-7: `credentials()` docblock returnt union van de twee concrete subclass-types, niet de abstract base — geeft IDE's accurate type-info zonder dat host-code een `instanceof`-check verliest):

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Facades;

use Illuminate\Support\Facades\Facade;

/**
 * @see \Emeq\MollieApi\Mollie
 *
 * @method static \Emeq\MollieApi\Data\MollieApiKeyCredentials|\Emeq\MollieApi\Data\MollieOAuthCredentials credentials()
 * @method static \Mollie\Api\MollieApiClient client()
 */
class Mollie extends Facade
{
    protected static function getFacadeAccessor(): string
    {
        return \Emeq\MollieApi\Mollie::class;
    }
}
```

NB: de class is `Emeq\MollieApi\Facades\Mollie` (alias-name = `Mollie`). Dit overlapt qua short-name met `Mollie\Api\MollieApiClient`'s `Mollie\Api\...` namespace. Geen collision want short-name `Mollie` is een class in de facades-namespace, terwijl `Mollie\Api\...` een vendor-namespace is. Geen alias-confusion mogelijk.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'namespace Emeq\\\\MollieApi\\\\Facades;' src/Facades/Mollie.php &amp;&amp; grep -q 'class Mollie extends Facade' src/Facades/Mollie.php &amp;&amp; grep -q 'getFacadeAccessor' src/Facades/Mollie.php &amp;&amp; grep -q '\\\\Emeq\\\\MollieApi\\\\Mollie::class' src/Facades/Mollie.php &amp;&amp; grep -q 'MollieApiKeyCredentials|\\\\Emeq\\\\MollieApi\\\\Data\\\\MollieOAuthCredentials credentials()' src/Facades/Mollie.php</automated>
  </verify>
  <done>Facades\Mollie is minimale subclass van Illuminate Facade, getFacadeAccessor() wijst naar de facade-target Emeq\MollieApi\Mollie::class, en de docblock typeert credentials() met de union return-type van beide subclass-types (W-7) + client() returnt MollieApiClient.</done>
</task>

</tasks>

<verification>
- composer.json bevat `post-autoload-dump` + `prepare` scripts (W-2 Optie B)
- grep `singleton(Mollie::class` in src/MollieServiceProvider.php → 1 hit
- grep `MissingCredentialResolverException::notBound` in src/MollieServiceProvider.php → 1 hit
- grep `bind\(` in src/MollieServiceProvider.php → ≥1 hit (de MollieApiClient::class binding)
- grep `setApiKey` in src/Mollie.php → 1 hit
- grep `setAccessToken` in src/Mollie.php → 1 hit
- grep `match (true)` in src/Mollie.php → 1 hit (type-discriminator)
- grep `container->make($value)` in src/Mollie.php → 1 hit (B-8 dual-path)
- grep `container->make('app')->environment()` in src/Mollie.php → 1 hit (B-7 compat)
- grep `getFacadeAccessor` in src/Facades/Mollie.php → 1 hit
- grep `MollieApiKeyCredentials|\Emeq\MollieApi\Data\MollieOAuthCredentials credentials()` in src/Facades/Mollie.php → 1 hit (W-7)
- Geen `new \Mollie\Api\MollieApiClient` in MollieServiceProvider (instantiatie hoort in Mollie::client(), niet in de SP)
- `composer dump-autoload` in packages/mollie-api/ slaagt zonder errors
</verification>

<success_criteria>
- composer.json's `post-autoload-dump`/`prepare` scripts triggeren `package:discover` zonder errors (W-2)
- ServiceProvider auto-discovers via composer.json (al gewaarborgd in plan 02-01)
- Mollie::class is een singleton; MollieApiClient::class is een non-singleton
- Type-discriminator op MollieApiKeyCredentials vs MollieOAuthCredentials staat in `match (true)` zonder default-arm
- Env-guard alleen actief als enforce_environment=true ÉN env=production ÉN credentials zijn API-key met test_ prefix; env-detect compat met `detectEnvironment()`-override
- Idempotency-generator wordt alleen gewired als config-string gezet is, en accepteert ZOWEL FQCN ALS container-alias paths (B-8)
- Facade docblock typeert credentials() met union-type (W-7)
- Geen raw apiKey/accessToken in code-paths buiten de match-arm en de setApiKey/setAccessToken call
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-04-SUMMARY.md`
</output>
</content>
</invoke>