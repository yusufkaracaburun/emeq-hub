---
phase: 02-emeq-mollie-api-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/mollie-api/src/Contracts/MollieCredentialResolver.php
  - packages/mollie-api/src/Data/MollieCredentials.php
  - packages/mollie-api/src/Data/MollieApiKeyCredentials.php
  - packages/mollie-api/src/Data/MollieOAuthCredentials.php
  - packages/mollie-api/tests/Unit/Data/MollieApiKeyCredentialsTest.php
  - packages/mollie-api/tests/Unit/Data/MollieOAuthCredentialsTest.php
  - packages/mollie-api/tests/Unit/Data/MollieCredentialsFingerprintTest.php
autonomous: true
requirements:
  - MOLL-01
must_haves:
  truths:
    - "MollieCredentialResolver-contract bestaat in src/Contracts/ met één methode resolve() die de abstracte base type returnt"
    - "MollieApiKeyCredentials valideert dat apiKey met test_ of live_ start en gooit InvalidArgumentException anders"
    - "MollieOAuthCredentials valideert dat accessToken met access_ start en gooit InvalidArgumentException anders"
    - "Validatie gebruikt PHP-core `trim()` (NIET `mb_trim()`) — match Snelstart-blueprint pattern voor PHP ^8.3 compat (W-6 / B-2 Optie A)"
    - "Beide credential-classes hebben een fingerprint() methode die sha256 van het secret hashed en daar de eerste 12 chars van returnt; de hash-input wordt geleverd door een PROTECTED abstract getSecretMaterial(): string op de base — `secret()` is GEEN public API meer (W-6)"
    - "fromArray() static constructors bestaan op beide Data-classes voor dictionary-style instantiatie"
    - "Pest-suite op MollieApiKeyCredentials, MollieOAuthCredentials en de gedeelde fingerprint-test passeren (≥7 tests)"
  artifacts:
    - path: "packages/mollie-api/src/Contracts/MollieCredentialResolver.php"
      provides: "Multi-tenant credential resolution contract"
      contains: "interface MollieCredentialResolver"
    - path: "packages/mollie-api/src/Data/MollieCredentials.php"
      provides: "Abstract base met fingerprint() concrete + getSecretMaterial() protected abstract"
      contains: "abstract"
    - path: "packages/mollie-api/src/Data/MollieApiKeyCredentials.php"
      provides: "API-key credentials met test_|live_-prefix validatie"
      contains: "MollieApiKeyCredentials"
    - path: "packages/mollie-api/src/Data/MollieOAuthCredentials.php"
      provides: "OAuth access-token credentials met access_-prefix validatie"
      contains: "MollieOAuthCredentials"
  key_links:
    - from: "src/Contracts/MollieCredentialResolver.php"
      to: "src/Data/MollieCredentials.php"
      via: "resolve(): MollieCredentials return-type"
      pattern: "resolve\\(\\): MollieCredentials"
    - from: "src/Data/MollieApiKeyCredentials.php"
      to: "src/Data/MollieCredentials.php"
      via: "extends MollieCredentials + getSecretMaterial() override"
      pattern: "extends MollieCredentials"
    - from: "src/Data/MollieOAuthCredentials.php"
      to: "src/Data/MollieCredentials.php"
      via: "extends MollieCredentials + getSecretMaterial() override"
      pattern: "extends MollieCredentials"
---

<objective>
Definieer het `MollieCredentialResolver`-contract en de dual-credential Data-classes: een abstracte base `MollieCredentials` met de shared `fingerprint()`-pattern (concrete) en een protected abstract `getSecretMaterial(): string` voor de hash-input, en twee concrete subclasses `MollieApiKeyCredentials` (test_|live_ prefix-validatie) en `MollieOAuthCredentials` (access_ prefix-validatie). Inclusief Pest-tests die alle prefix-paths + fingerprint-determinism dekken.

Purpose: Dit is de typed-data laag waar `Mollie::client()` (plan 02-04) op leunt. Door eerst contracten + value-types neer te zetten kan plan 02-04 puur de glue schrijven (resolver→credentials→client). Test-first laag: 7 Pest-tests die de prefix-validatie + fingerprint-pattern dekken (≥10-drempel-bijdrage).

W-6 revision: `secret()` is NIET meer public. De raw-secret-getter `getSecretMaterial(): string` is `protected` op de base; alleen `fingerprint()` is public. Dat scheelt een security-leak-pad: code buiten de subclass-eigen public typed-property (`$apiKey` / `$accessToken`) kan het secret niet meer via een base-API achterhalen.

Output: 4 productie-files (1 contract + 3 data-classes) + 3 test-files (~7 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/rules/engineering.md

<interfaces>
<!-- Snelstart-SDK referentie-files voor pattern -->
packages/snelstart-api/src/Contracts/SnelstartCredentialResolver.php:
```php
interface SnelstartCredentialResolver {
    public function resolve(): SnelstartCredentials;
}
```

packages/snelstart-api/src/Data/SnelstartCredentials.php (NOTE: Snelstart gebruikt `mb_trim()` — een PHP 8.4-functie. Voor Mollie kiezen we strikter en consistenter: PHP-core `trim()`. Dit matched de PHP ^8.3 constraint zonder afhankelijkheid van mbstring-only-in-8.4 builtins. Onze SDK draait dus op zowel PHP 8.3 als 8.4 hosts):
```php
final readonly class SnelstartCredentials {
    public function __construct(
        public string $clientKey,
        public string $subscriptionKey,
        public ?string $subscriptionId = null,
    ) {
        if ('' === mb_trim($this->clientKey)) {   // <-- Snelstart-specific; wij gebruiken trim() in Mollie
            throw new InvalidArgumentException(...);
        }
    }
    public static function fromArray(array $data): self { /* ... */ }
    public function fingerprint(): string {
        return hash('sha256', $this->clientKey);   // <-- Snelstart: volledige hash; wij: eerste 12 chars
    }
}
```

<!-- Verschil voor Mollie -->
Mollie heeft GEEN subscriptionKey/subscriptionId-equivalent. WEL twee credential-types:
  - MollieApiKeyCredentials: één string `apiKey` ("test_..." of "live_...")
  - MollieOAuthCredentials: één string `accessToken` ("access_...") + optional `?int $expiresAt`

Fingerprint-spec voor Mollie: substr(hash('sha256', $secretMaterial), 0, 12)
  (eerste 12 chars i.p.v. volledige hash — CONTEXT.md "Fingerprint-pattern" sectie + Specifics)

Security-spec (W-6): de raw-secret getter is `protected abstract getSecretMaterial(): string` op de base. NIET public, NIET `secret()`. fingerprint() is de enige public hash-pad — geen public API kan een raw secret returnen vanuit base-niveau.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract + abstract base MollieCredentials (met protected getSecretMaterial)</name>
  <read_first>
    - packages/snelstart-api/src/Contracts/SnelstartCredentialResolver.php
    - packages/snelstart-api/src/Data/SnelstartCredentials.php
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Specific References / Examples" voor fingerprint-pattern
  </read_first>
  <files>
    packages/mollie-api/src/Contracts/MollieCredentialResolver.php
    packages/mollie-api/src/Data/MollieCredentials.php
    packages/mollie-api/tests/Unit/Data/MollieCredentialsFingerprintTest.php
  </files>
  <behavior>
    - fingerprint() returnt eerste 12 chars van sha256(secretMaterial) — deterministisch en stable over multiple calls
    - fingerprint() reveals NOOIT het rauwe secret (assert: rauwe key NIET in fingerprint substring)
    - Twee verschillende secrets geven twee verschillende fingerprints
    - MollieCredentialResolver::resolve() return-type is `MollieCredentials` (abstract base, niet `mixed` / niet union-string)
    - De base class heeft GEEN public `secret()` method — de enige public hash-pad is `fingerprint()`. `getSecretMaterial()` is protected.
  </behavior>
  <action>
Maak `packages/mollie-api/src/Contracts/MollieCredentialResolver.php`:

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Contracts;

use Emeq\MollieApi\Data\MollieCredentials;

/**
 * Strategy that produces Mollie credentials for the *current* request /
 * job / console-command. The host app implements this and binds it in the
 * container — see README.md "Credentials wiring".
 *
 * Implementations return either:
 *  - Emeq\MollieApi\Data\MollieApiKeyCredentials  — when the tenant uses a
 *    direct API-key (test_ or live_ prefix), e.g. Emeq's own Mollie account.
 *  - Emeq\MollieApi\Data\MollieOAuthCredentials   — when the tenant uses
 *    Mollie Connect OAuth (access_ prefix), e.g. an end-customer's Mollie
 *    account linked via the Hub's OAuth-broker.
 *
 * Typical implementations:
 *  - read from `tenant()->settings()` (stancl/tenancy)
 *  - read from `Connection->access_token` on a multi-tenant Hub
 *  - read from `config('services.mollie')` for single-tenant apps
 */
interface MollieCredentialResolver
{
    public function resolve(): MollieCredentials;
}
```

Maak `packages/mollie-api/src/Data/MollieCredentials.php` als abstract base (W-6 — `getSecretMaterial()` is `protected`; geen public `secret()`):

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Data;

/**
 * Abstract base for Mollie credentials. Two concrete shapes:
 *  - MollieApiKeyCredentials  — `apiKey` with test_|live_ prefix
 *  - MollieOAuthCredentials   — `accessToken` with access_ prefix
 *
 * The MollieCredentialResolver returns one of these per request; Mollie::client()
 * branches on the concrete type to choose between setApiKey() and setAccessToken().
 *
 * Security: the raw secret material is intentionally NOT exposed via a public
 * base-class method. The only public hash-pad is fingerprint(); the protected
 * abstract getSecretMaterial() gives the base enough information to compute
 * fingerprint() without leaking the secret outside the class hierarchy.
 * Subclasses still expose their concrete typed public property (e.g. $apiKey
 * or $accessToken) — that is where intentional, type-narrowed access lives.
 */
abstract readonly class MollieCredentials
{
    /**
     * Returns the underlying raw secret used as hash-input for fingerprint().
     * PROTECTED on purpose: callers outside the class hierarchy must use the
     * concrete subclass's typed public property if they need the actual secret.
     */
    abstract protected function getSecretMaterial(): string;

    /**
     * Stable, non-reversible identifier for log/audit/cache-key derivation.
     * Returns the first 12 chars of sha256($secretMaterial) — sufficient to
     * identify a credential without leaking the underlying secret.
     */
    public function fingerprint(): string
    {
        return substr(hash('sha256', $this->getSecretMaterial()), 0, 12);
    }
}
```

Maak `packages/mollie-api/tests/Unit/Data/MollieCredentialsFingerprintTest.php` (W-6: assert via subclass public property — niet via `->secret()`):

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Data\MollieApiKeyCredentials;
use Emeq\MollieApi\Data\MollieOAuthCredentials;

it('returns first 12 chars of sha256(secret) for both credential types', function (): void {
    $apiKey  = new MollieApiKeyCredentials('test_abc123');
    $oauth   = new MollieOAuthCredentials('access_xyz789');

    expect($apiKey->fingerprint())
        ->toBe(substr(hash('sha256', 'test_abc123'), 0, 12))
        ->toHaveLength(12)
        ->and($apiKey->fingerprint())->not->toContain('test_abc123')
        ->and($apiKey->fingerprint())->not->toContain('abc123');

    expect($oauth->fingerprint())
        ->toBe(substr(hash('sha256', 'access_xyz789'), 0, 12))
        ->toHaveLength(12)
        ->and($oauth->fingerprint())->not->toContain('access_xyz789')
        ->and($oauth->fingerprint())->not->toContain('xyz789');

    expect($apiKey->fingerprint())->not->toBe($oauth->fingerprint());
});

it('produces deterministic fingerprints across calls', function (): void {
    $creds = new MollieApiKeyCredentials('test_deterministic');
    expect($creds->fingerprint())->toBe($creds->fingerprint());
});
```

Belangrijk: deze test draait pas groen NA task 2 (waar MollieApiKeyCredentials + MollieOAuthCredentials concreet worden). De class-definitions van die twee concreet zijn een hard prereq.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'interface MollieCredentialResolver' src/Contracts/MollieCredentialResolver.php &amp;&amp; grep -q 'resolve(): MollieCredentials' src/Contracts/MollieCredentialResolver.php &amp;&amp; grep -q 'abstract readonly class MollieCredentials' src/Data/MollieCredentials.php &amp;&amp; grep -q 'abstract protected function getSecretMaterial(): string' src/Data/MollieCredentials.php &amp;&amp; ! grep -q 'abstract public function secret' src/Data/MollieCredentials.php &amp;&amp; grep -q "substr(hash('sha256'" src/Data/MollieCredentials.php</automated>
  </verify>
  <done>Contract definieert `resolve(): MollieCredentials`. Base class is abstract readonly met `getSecretMaterial()` PROTECTED abstract en concrete `fingerprint()`. Geen public `secret()` op de base. Test-bestand voor fingerprint bestaat (draait pas groen na task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MollieApiKeyCredentials + MollieOAuthCredentials + tests (trim() i.p.v. mb_trim(), getSecretMaterial() override)</name>
  <read_first>
    - packages/mollie-api/src/Data/MollieCredentials.php (task 1 output — abstract base met getSecretMaterial() protected)
    - packages/snelstart-api/src/Data/SnelstartCredentials.php (referentie-pattern voor ctor-validation + fromArray; let op: Snelstart gebruikt mb_trim — wij gebruiken trim())
    - .planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md sectie "Dual credentials"
  </read_first>
  <files>
    packages/mollie-api/src/Data/MollieApiKeyCredentials.php
    packages/mollie-api/src/Data/MollieOAuthCredentials.php
    packages/mollie-api/tests/Unit/Data/MollieApiKeyCredentialsTest.php
    packages/mollie-api/tests/Unit/Data/MollieOAuthCredentialsTest.php
  </files>
  <behavior>
    - MollieApiKeyCredentials accepteert `test_anything` en `live_anything` zonder errors
    - MollieApiKeyCredentials gooit InvalidArgumentException bij lege string, bij whitespace-only string, bij `bogus_xxx`, bij rauwe string zonder prefix
    - MollieApiKeyCredentials::fromArray(['apiKey' => 'test_x']) werkt
    - MollieOAuthCredentials accepteert `access_anything` zonder errors
    - MollieOAuthCredentials gooit InvalidArgumentException bij lege string, bij whitespace-only string, bij `test_xxx`, bij rauwe string zonder access_ prefix
    - MollieOAuthCredentials::fromArray(['accessToken' => 'access_x', 'expiresAt' => 1234]) werkt — expiresAt is optional ?int
    - Beide classes overrider `getSecretMaterial()` (protected, returnt eigen veld) — NIET een public `secret()`
  </behavior>
  <action>
Maak `packages/mollie-api/src/Data/MollieApiKeyCredentials.php` (B-2 Optie A: gebruik PHP-core `trim()`; W-6: override `getSecretMaterial()` protected i.p.v. public `secret()`):

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Data;

use InvalidArgumentException;

/**
 * Mollie API-key credentials.
 *
 * Used when a tenant authenticates with a direct Mollie API-key (i.e. they
 * have their own Mollie account and granted Emeq the raw key, or it is
 * Emeq's own Mollie account).
 *
 * Mollie's keys are prefixed: `test_xxx` for the test mode, `live_xxx` for
 * production. Both prefixes are accepted at construction time; the ServiceProvider
 * may additionally enforce that production environments use `live_` keys
 * (see config/mollie.php `enforce_environment`).
 *
 * Validation uses PHP-core trim() (NOT mb_trim()) to remain compatible with
 * the package's PHP ^8.3 constraint — mb_trim() was added in PHP 8.4.
 */
final readonly class MollieApiKeyCredentials extends MollieCredentials
{
    public function __construct(
        public string $apiKey,
    ) {
        if ('' === trim($this->apiKey)) {
            throw new InvalidArgumentException('MollieApiKeyCredentials: apiKey may not be empty.');
        }

        if ( ! str_starts_with($this->apiKey, 'test_') && ! str_starts_with($this->apiKey, 'live_')) {
            throw new InvalidArgumentException(
                'MollieApiKeyCredentials: apiKey must start with "test_" or "live_".',
            );
        }
    }

    /**
     * @param  array{apiKey: string}  $data
     */
    public static function fromArray(array $data): self
    {
        return new self(apiKey: $data['apiKey']);
    }

    protected function getSecretMaterial(): string
    {
        return $this->apiKey;
    }

    public function isTestMode(): bool
    {
        return str_starts_with($this->apiKey, 'test_');
    }
}
```

Maak `packages/mollie-api/src/Data/MollieOAuthCredentials.php` (idem trim() + getSecretMaterial()):

```php
<?php

declare(strict_types=1);

namespace Emeq\MollieApi\Data;

use InvalidArgumentException;

/**
 * Mollie Connect OAuth credentials.
 *
 * Used when a tenant connected their Mollie account to Emeq through the
 * Mollie Connect OAuth flow (Phase 4). The access-token has an `access_`
 * prefix and a finite lifetime; the Hub's OAuthFlow refreshes the token
 * before expiry, but this Data class only carries the currently-valid
 * access-token + its expiry timestamp.
 *
 * Refresh-token handling and storage live in the Hub's OAuthFlow
 * implementation, not in this SDK package.
 *
 * Validation uses PHP-core trim() (NOT mb_trim()) for PHP ^8.3 compat.
 */
final readonly class MollieOAuthCredentials extends MollieCredentials
{
    public function __construct(
        public string $accessToken,
        public ?int $expiresAt = null,
    ) {
        if ('' === trim($this->accessToken)) {
            throw new InvalidArgumentException('MollieOAuthCredentials: accessToken may not be empty.');
        }

        if ( ! str_starts_with($this->accessToken, 'access_')) {
            throw new InvalidArgumentException(
                'MollieOAuthCredentials: accessToken must start with "access_".',
            );
        }
    }

    /**
     * @param  array{accessToken: string, expiresAt?: ?int}  $data
     */
    public static function fromArray(array $data): self
    {
        return new self(
            accessToken: $data['accessToken'],
            expiresAt: $data['expiresAt'] ?? null,
        );
    }

    protected function getSecretMaterial(): string
    {
        return $this->accessToken;
    }
}
```

Maak `packages/mollie-api/tests/Unit/Data/MollieApiKeyCredentialsTest.php` (W-6: assert via `->apiKey` public property; geen `->secret()` aanroep):

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Data\MollieApiKeyCredentials;

it('accepts a valid test_ prefixed key', function (): void {
    $creds = new MollieApiKeyCredentials('test_abc123');

    expect($creds->apiKey)->toBe('test_abc123')
        ->and($creds->isTestMode())->toBeTrue()
        ->and($creds->fingerprint())->toBe(substr(hash('sha256', 'test_abc123'), 0, 12));
});

it('accepts a valid live_ prefixed key', function (): void {
    $creds = new MollieApiKeyCredentials('live_prod456');

    expect($creds->apiKey)->toBe('live_prod456')
        ->and($creds->isTestMode())->toBeFalse();
});

it('rejects an empty apiKey', function (): void {
    new MollieApiKeyCredentials('');
})->throws(InvalidArgumentException::class, 'may not be empty');

it('rejects a whitespace-only apiKey', function (): void {
    new MollieApiKeyCredentials('   ');
})->throws(InvalidArgumentException::class, 'may not be empty');

it('rejects an apiKey without test_ or live_ prefix', function (): void {
    new MollieApiKeyCredentials('bogus_xxx');
})->throws(InvalidArgumentException::class, 'must start with "test_" or "live_"');

it('builds from array via fromArray', function (): void {
    $creds = MollieApiKeyCredentials::fromArray(['apiKey' => 'test_x']);
    expect($creds)->toBeInstanceOf(MollieApiKeyCredentials::class)
        ->and($creds->apiKey)->toBe('test_x');
});
```

Maak `packages/mollie-api/tests/Unit/Data/MollieOAuthCredentialsTest.php`:

```php
<?php

declare(strict_types=1);

use Emeq\MollieApi\Data\MollieOAuthCredentials;

it('accepts a valid access_ prefixed token with optional expiresAt', function (): void {
    $creds = new MollieOAuthCredentials('access_xyz789', expiresAt: 1740000000);

    expect($creds->accessToken)->toBe('access_xyz789')
        ->and($creds->expiresAt)->toBe(1740000000)
        ->and($creds->fingerprint())->toBe(substr(hash('sha256', 'access_xyz789'), 0, 12));
});

it('accepts an access_ token without expiresAt', function (): void {
    $creds = new MollieOAuthCredentials('access_no_exp');
    expect($creds->expiresAt)->toBeNull();
});

it('rejects an empty accessToken', function (): void {
    new MollieOAuthCredentials('');
})->throws(InvalidArgumentException::class, 'may not be empty');

it('rejects a whitespace-only accessToken', function (): void {
    new MollieOAuthCredentials('   ');
})->throws(InvalidArgumentException::class, 'may not be empty');

it('rejects a token without access_ prefix', function (): void {
    new MollieOAuthCredentials('test_xxx');
})->throws(InvalidArgumentException::class, 'must start with "access_"');

it('builds from array via fromArray', function (): void {
    $creds = MollieOAuthCredentials::fromArray(['accessToken' => 'access_x', 'expiresAt' => 999]);
    expect($creds)->toBeInstanceOf(MollieOAuthCredentials::class)
        ->and($creds->accessToken)->toBe('access_x')
        ->and($creds->expiresAt)->toBe(999);
});
```

NB: deze tests draaien pas tijdens task 3 (groen na de Pest-bootstrap) — Pest.php + TestCase moeten nog niet bestaan voor het schrijven, maar je kunt ze als losse run NOG niet draaien. Pest runt pas in plan 02-05 voor het eerst. Voor nu: alle classes en files staan in working tree, geen test-run yet.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; grep -q 'final readonly class MollieApiKeyCredentials extends MollieCredentials' src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q "'' === trim(\$this-&gt;apiKey)" src/Data/MollieApiKeyCredentials.php &amp;&amp; ! grep -q 'mb_trim' src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q "str_starts_with(\$this-&gt;apiKey, 'test_')" src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q "str_starts_with(\$this-&gt;apiKey, 'live_')" src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q 'protected function getSecretMaterial(): string' src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q 'final readonly class MollieOAuthCredentials extends MollieCredentials' src/Data/MollieOAuthCredentials.php &amp;&amp; grep -q "'' === trim(\$this-&gt;accessToken)" src/Data/MollieOAuthCredentials.php &amp;&amp; ! grep -q 'mb_trim' src/Data/MollieOAuthCredentials.php &amp;&amp; grep -q "str_starts_with(\$this-&gt;accessToken, 'access_')" src/Data/MollieOAuthCredentials.php &amp;&amp; grep -q 'protected function getSecretMaterial(): string' src/Data/MollieOAuthCredentials.php &amp;&amp; grep -q 'fromArray' src/Data/MollieApiKeyCredentials.php &amp;&amp; grep -q 'fromArray' src/Data/MollieOAuthCredentials.php &amp;&amp; [ -f tests/Unit/Data/MollieApiKeyCredentialsTest.php ] &amp;&amp; [ -f tests/Unit/Data/MollieOAuthCredentialsTest.php ] &amp;&amp; ! grep -rq '-&gt;secret()' tests/Unit/Data/</automated>
  </verify>
  <done>Beide credential-classes bestaan, valideren prefix in ctor met PHP-core `trim()` (geen mb_trim), overriden `getSecretMaterial()` protected, en hebben `fromArray()`. Test-files staan in working tree (≥6 tests in elk file = ≥12 totaal + 2 fingerprint-tests = ≥14). Geen `->secret()`-aanroepen in tests (W-6). De tests draaien pas na plan 02-05 (Pest-bootstrap).</done>
</task>

</tasks>

<verification>
- grep `interface MollieCredentialResolver` in src/Contracts/MollieCredentialResolver.php → 1 hit
- grep `abstract readonly class MollieCredentials` in src/Data/MollieCredentials.php → 1 hit
- grep `abstract protected function getSecretMaterial` in src/Data/MollieCredentials.php → 1 hit
- grep `abstract public function secret` in src/Data/MollieCredentials.php → 0 hits (W-6)
- grep `final readonly class MollieApiKeyCredentials extends MollieCredentials` in src/Data/MollieApiKeyCredentials.php → 1 hit
- grep `final readonly class MollieOAuthCredentials extends MollieCredentials` in src/Data/MollieOAuthCredentials.php → 1 hit
- grep `mb_trim` in src/Data/ → 0 hits (B-2 Optie A)
- 3 test-files in tests/Unit/Data/ aanwezig met samen ≥14 `it(...)` blocks
- Geen `->secret()`-aanroepen in tests/Unit/Data/
</verification>

<success_criteria>
- `MollieCredentialResolver::resolve()` return-type is `MollieCredentials` (de abstract base)
- Beide concrete credentials zijn `final readonly`, extends `MollieCredentials`, en overriden `getSecretMaterial()` protected
- Validatie gebruikt `trim()` (B-2 Optie A) — geen mb_trim() dependency
- Prefix-validatie gooit `InvalidArgumentException` (niet `MollieException` — `MollieException` komt in plan 02-03)
- Tests dekken happy-path + empty-string + whitespace-only + invalid-prefix + fromArray voor beide credential-types + 2 fingerprint-tests
- Geen public `secret()` getter — fingerprint() is de enige public hash-pad (W-6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-02-SUMMARY.md`
</output>
</content>
</invoke>