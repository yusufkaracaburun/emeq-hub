---
phase: 02-emeq-mollie-api-foundation
plan: 08
type: execute
wave: 7
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
  - 02-07
files_modified:
  - packages/mollie-api/.git (commit + push — sub-repo)
  - packages/mollie-api/README.md
  - composer.json
autonomous: false
user_setup:
  - service: github
    why: "Push naar github.com:yusufkaracaburun/emeq-mollie-api (default branch creatie + upstream-tracking)"
    env_vars: []
    dashboard_config:
      - task: "Update repo description"
        location: "https://github.com/yusufkaracaburun/emeq-mollie-api/edit — of via `gh repo edit yusufkaracaburun/emeq-mollie-api --description \"...\"`"
requirements:
  - MOLL-01
must_haves:
  truths:
    - "packages/mollie-api/ sub-repo heeft een eerste commit op feat/foundation (~16 files: composer.json + skeleton + src/* + tests/* + config/mollie.php) — composer.lock is bewust NIET gestaged (B-3 / SDK-best-practice)"
    - "Stage-stap heeft een master-branch-guard die git add/commit weigert wanneer de Hub-repo op master/main staat (W-4)"
    - "feat/foundation is gepusht naar origin (= github.com:yusufkaracaburun/emeq-mollie-api) na user-approval"
    - "Repo-default-branch is na de push ingesteld op feat/foundation OF main (depending op user-keuze tijdens checkpoint)"
    - "GitHub-repo-description is bijgewerkt van 'Saloon v3' naar een wrap-mollie-api-php-beschrijving"
    - "Hub composer.json heeft een `mollie-path` path-repository entry voor packages/mollie-api/ (mirror van bestaande snelstart-path entry) — geen `require: emeq/mollie-api` yet"
    - "Een VCS-smoke task installeert emeq/mollie-api in een lege /tmp directory via composer + VCS-repo — bewijst ROADMAP Phase 2 success criterion 1 (W-3)"
    - "README.md bevat een sectie die uitlegt dat Phase 2 success criterion 3 (geen alias-conflict met laravel-mollie) wordt opgelost in Phase 6 (SUB-01); host-apps disambigueren handmatig tot dan (W-1)"
  artifacts:
    - path: "composer.json"
      provides: "Hub repositories[].name=mollie-path entry"
      contains: "mollie-path"
    - path: "packages/mollie-api/composer.json"
      provides: "Committed in sub-repo"
      contains: "emeq/mollie-api"
    - path: "packages/mollie-api/README.md"
      provides: "Sectie 'Facade-alias collision' verwijst naar Phase 6 SUB-01 (W-1)"
      contains: "Phase 6"
  key_links:
    - from: "composer.json (Hub)"
      to: "packages/mollie-api/composer.json (sub-repo)"
      via: "repositories[].name=mollie-path with type=path"
      pattern: "mollie-path"
    - from: "packages/mollie-api (sub-repo)"
      to: "github.com:yusufkaracaburun/emeq-mollie-api"
      via: "git push -u origin feat/foundation"
      pattern: "feat/foundation"
---

<objective>
Sluit Phase 2 af door (1) de sub-repo `packages/mollie-api/` te committen + pushen naar GitHub (creëert default branch + upstream-tracking voor het eerst), (2) de GitHub-repo-description bij te werken, (3) een path-repository entry voor `mollie-path` toe te voegen aan de Hub's `composer.json` zodat latere Hub-fasen het pakket via path-link kunnen require'en, en (4) een VCS-smoke task die ROADMAP Phase 2 success criterion 1 valideert.

Purpose: De inhoud staat klaar (plans 02-01 t/m 02-07); dit plan is puur "shipping" + Hub-wiring-pad-opening. Push naar GitHub vereist user-approval (`.ai/git-policy`: "Nooit `git push` zonder expliciete user-toestemming"). Daarom is dit plan `autonomous: false` met een checkpoint.

Revisions:
- B-3: composer.lock blijft gitignored — expliciete comment in Task 1's stage-command (`# composer.lock is intentioneel gitignored per SDK-best-practice, niet stagen`)
- W-1: README-update legt uit dat success criterion 3 (alias-collision) in Phase 6 SUB-01 wordt opgelost
- W-3: NIEUWE task — VCS-smoke install vanuit /tmp om criterion 1 te bewijzen
- W-4: master-branch-guard in Hub-stage-stap (Task 3)
- B-1: depends_on bevat nu expliciet alle voorgaande plans 02-01 t/m 02-07; wave = 6 (na 02-07's wave 5)

Output: één commit in de sub-repo (gepusht na approval), één commit in de Hub (composer.json change), één bijgewerkte repo-description op GitHub, één bewezen VCS-install in /tmp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-emeq-mollie-api-foundation/02-CONTEXT.md
@CLAUDE.md
@.ai/rules/global.md
@.ai/git-policy
@.ai/packages

<interfaces>
Outputs van plans 02-01 t/m 02-07 (staan in working tree van packages/mollie-api/, nog niet gecommit):
  composer.json (inclusief post-autoload-dump/prepare uit 02-04), .gitignore (composer.lock gitignored), .gitattributes, .editorconfig
  pint.json, phpstan.neon.dist, phpunit.xml.dist
  README.md, LICENSE.md, CHANGELOG.md
  config/mollie.php
  src/MollieServiceProvider.php, src/Mollie.php
  src/Facades/Mollie.php
  src/Contracts/MollieCredentialResolver.php
  src/Data/MollieCredentials.php, src/Data/MollieApiKeyCredentials.php, src/Data/MollieOAuthCredentials.php
  src/Exceptions/MollieException.php, src/Exceptions/MissingCredentialResolverException.php
  tests/TestCase.php, tests/Pest.php, tests/ArchTest.php, tests/PackageSmokeTest.php
  tests/Support/FakeMollieCredentialResolver.php
  tests/Unit/Data/MollieApiKeyCredentialsTest.php
  tests/Unit/Data/MollieOAuthCredentialsTest.php
  tests/Unit/Data/MollieCredentialsFingerprintTest.php
  tests/Unit/MollieTest.php
  tests/Unit/MollieServiceProviderTest.php
  tests/Unit/ErrorMappingTest.php
  .mollie-fake-fqcn-notes.md (uit 02-07 task 1 — werk-artefact, gitignored als per `*.notes.md` pattern)

Sub-repo git state vóór dit plan:
  origin = git@github.com:yusufkaracaburun/emeq-mollie-api.git (geen branches op remote)
  local branch = feat/foundation (aangemaakt in plan 02-01)
  staging = leeg of partial — dit plan stage't alles BEHALVE composer.lock (B-3) en .mollie-fake-fqcn-notes.md

Hub composer.json (huidige relevante sectie):
{
  "repositories": [
    { "name": "snelstart-path", "type": "path", "url": "packages/snelstart-api", "options": { "symlink": true } },
    { "name": "snelstart-vcs",  "type": "vcs",  "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git" }
  ]
}

Te toevoegen entry:
  { "name": "mollie-path", "type": "path", "url": "packages/mollie-api", "options": { "symlink": true } }

(Geen vcs-entry voor Mollie yet — die volgt zodra de SDK getagd is. De Hub gebruikt voor dev tijdens v0.2 alleen het path-symlink-pad. De VCS-smoke in Task 4 is een 1-time-validatie en muteert de Hub composer.json NIET.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README-update voor facade-alias collision (W-1) + stage + commit alle Phase-2-output in packages/mollie-api/ sub-repo (B-3: composer.lock NIET stagen)</name>
  <read_first>
    - .ai/git-policy (regels: nooit op master werken, nooit push zonder approval, geen --no-verify)
    - packages/mollie-api/ (controleer working-tree status: alle Phase-2-files moeten present zijn)
    - packages/mollie-api/.gitignore (verifieer dat composer.lock daar staat — uit 02-01 task 1)
  </read_first>
  <files>
    packages/mollie-api/README.md
    packages/mollie-api/.gitignore
    packages/mollie-api/.git (commit history append)
  </files>
  <action>
W-1: voeg `.mollie-fake-fqcn-notes.md`-patroon toe aan `.gitignore` als deze er nog niet in staat:

```bash
cd packages/mollie-api
grep -q '\*\.notes\.md' .gitignore || printf '\n# Inspection notes (Task 1 plan 02-07)\n*.notes.md\n.mollie-fake-fqcn-notes.md\n' >> .gitignore
```

W-1: update `packages/mollie-api/README.md`'s "Facade-alias collision" sectie. Vervang de korte versie (uit plan 02-01) door deze uitgebreide tekst:

```markdown
### Facade-alias collision

`emeq/mollie-api` registreert een facade onder de alias-naam `Mollie`. Dit
matched de Snelstart-SDK-pattern (`Snelstart` alias) en is consistent met de
verdere `emeq/*` SDK-laag.

Wanneer een host-app naast deze SDK óók
[`mollie/laravel-mollie`](https://github.com/mollie/laravel-mollie) gebruikt
(bijvoorbeeld transitive via [`mollie/laravel-cashier-mollie`](https://github.com/mollie/laravel-cashier-mollie))
ontstaat er een alias-conflict op de korte naam `Mollie` in
`config/app.php → aliases`.

**Status in Phase 2 (v0.2):** Phase 2 levert dit op met de `Mollie`-alias
zonder ingebouwde detectie of compat-laag voor laravel-mollie. Host-apps die
beide pakketten gebruiken moeten één van twee:

  1. Verwijder de auto-registered `Mollie`-alias van laravel-mollie en gebruik
     volledig gekwalificeerde imports (`use Mollie\\Laravel\\Facades\\Mollie as
     LaravelMollie;`), of
  2. Disable auto-discovery voor één van beide packages en bind de facade
     handmatig onder een eigen alias.

**Status in Phase 6 (SUB-01):** Phase 6's SUB-01 levert een eerste-class
collision-resolutie. De ROADMAP Phase 2 success criterion 3 ("emeq/mollie-api
en mollie/laravel-mollie functioneren naast elkaar zonder conflict") wordt in
Phase 2 daarom NIET via tests gevalideerd — die dekking komt in Phase 6.

Beslissingsspoor (CONTEXT.md decision, 2026-05-14): de alias blijft `Mollie`
omdat consistency met Snelstart-pattern zwaarder weegt dan future-proofing
voor laravel-mollie-coexistentie in Phase 2.
```

Run de complete Pest-suite een laatste keer om groen-status te bevestigen vóór commit:

```bash
cd packages/mollie-api
./vendor/bin/pest
```

Run Pint om formatting consistent te krijgen:

```bash
cd packages/mollie-api
./vendor/bin/pint --dirty --format agent
```

Controleer de working tree + branch:

```bash
cd packages/mollie-api
git status
git rev-parse --abbrev-ref HEAD    # MOET 'feat/foundation' returnen
```

Indien NIET op feat/foundation: STOP en rapporteer terug. Niet pushen vanaf master/main.

Stage de relevante files (B-3: composer.lock is gitignored en wordt NIET gestaged; controleer eerst):

```bash
cd packages/mollie-api
# B-3: composer.lock is intentioneel gitignored per SDK-best-practice, niet stagen.
git check-ignore composer.lock   # moet 0-exit geven (= gitignored)

# Stage alles behalve gitignored files + .mollie-fake-fqcn-notes.md (W-1 .gitignore-toevoeging)
git add composer.json .gitignore .gitattributes .editorconfig pint.json phpstan.neon.dist phpunit.xml.dist README.md LICENSE.md CHANGELOG.md config/ src/ tests/
git status     # bekijk wat staged is — composer.lock + .mollie-fake-fqcn-notes.md NIET in de staging-output
```

Commit met Nederlandstalige message (project-conventie):

```bash
cd packages/mollie-api
git commit -m "$(cat <<'EOF'
feat(foundation): initiële emeq/mollie-api skeleton

Wrapt mollie/mollie-api-php ^3.11 met een multi-tenant MollieCredentialResolver
en dual credentials (MollieApiKeyCredentials + MollieOAuthCredentials). Levert
de Mollie facade-target, MollieServiceProvider (Spatie package-tools DSL), en
een Pest-suite (≥17 tests) die auth-laag, resolver-wiring, env-guard en
error-mapping dekt.

composer.lock is intentioneel gitignored (SDK-best-practice).
README documenteert dat alias-collision met laravel-mollie in Phase 6 SUB-01
wordt opgelost.

Phase 2 / v0.2 / emeq-hub.
EOF
)"
```

Belangrijk: GEEN `--no-verify`. Indien pre-commit hooks falen: triage, fix, en maak een NIEUWE commit (geen amend).
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; git log -1 --pretty=format:"%s" | grep -q 'feat(foundation): initiële emeq/mollie-api skeleton' &amp;&amp; git log -1 --stat | grep -v '^ composer.lock' &amp;&amp; ! git log -1 --name-only | grep -q '^composer.lock$' &amp;&amp; grep -q 'Phase 6' README.md &amp;&amp; grep -q 'SUB-01' README.md &amp;&amp; git status --porcelain | wc -l | awk '$1 &lt;= 2 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Sub-repo `packages/mollie-api/` heeft een eerste commit op `feat/foundation` met alle Phase-2-outputs. composer.lock is NIET in de commit (B-3). README documenteert de Phase 6 SUB-01 follow-up voor alias-collision (W-1). Pre-commit hooks (Pint) zijn doorlopen. Working tree is clean (afgezien van eventueel composer.lock + .mollie-fake-fqcn-notes.md, beide gitignored).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User-approval voor push naar GitHub + repo-description update</name>
  <what-built>
    Commit `feat(foundation): initiële emeq/mollie-api skeleton` staat klaar op feat/foundation in packages/mollie-api/ (sub-repo). 1 commit, ~25 files, +N regels code. Push creëert de default branch op github.com:yusufkaracaburun/emeq-mollie-api en activeert upstream-tracking. composer.lock is NIET in de commit (SDK-best-practice).
  </what-built>
  <how-to-verify>
    1. Bekijk de commit:

       ```bash
       cd packages/mollie-api
       git log -1 --stat
       ```

    2. Bekijk de remote-config en confirmeer dat origin naar de juiste GitHub-repo wijst:

       ```bash
       cd packages/mollie-api
       git remote -v
       # Verwacht: origin git@github.com:yusufkaracaburun/emeq-mollie-api.git
       ```

    3. Bekijk de huidige stale repo-description op GitHub:

       ```bash
       gh repo view yusufkaracaburun/emeq-mollie-api --json description -q .description
       # Verwacht: "Saloon v3" of vergelijkbaar — moet bijgewerkt worden
       ```

    4. Beslis: pushen we feat/foundation als de default branch, of pushen we hem als feature-branch en draaien we daarna een PR via gh? Voor een initiale repo zonder andere branches is direct `git push -u origin feat/foundation` ÉN GitHub-default-branch op feat/foundation zetten (of renamen naar main na merge) de gebruikelijke route.

    Mogelijke commando's die Claude na approval mag uitvoeren:

    ```bash
    # Push feature-branch + upstream
    cd packages/mollie-api
    git push -u origin feat/foundation

    # Set repo-default-branch op feat/foundation (alleen als er nog geen default branch is)
    gh api -X PATCH repos/yusufkaracaburun/emeq-mollie-api -f default_branch=feat/foundation

    # Optioneel: rename naar main
    gh api -X POST repos/yusufkaracaburun/emeq-mollie-api/branches/feat/foundation/rename -f new_name=main

    # Update description
    gh repo edit yusufkaracaburun/emeq-mollie-api \
      --description "Laravel SDK wrap around mollie/mollie-api-php met multi-tenant credential resolver en OAuth support"
    ```

    Indien je voorkeur hebt voor PR-flow ipv direct-push naar default-branch, communiceer dat — Claude zal dan een tijdelijke `init` branch creëren als basis, daar feat/foundation tegenaan PR'en, en mergen.
  </how-to-verify>
  <action>
    Wacht op user-approval voordat een push of repo-config-change wordt uitgevoerd. Bij approval-tekst "approve push feat/foundation als default branch + description-update" voer de drie gh-commando's uit (push + default_branch + description). Bij approval-tekst voor PR-flow: maak een lege `init` branch via `gh api repos/.../git/refs`, push feat/foundation daartegen, open een PR via `gh pr create` en wacht op verdere instructies. Bij "stop" return immediately zonder push. Geen `--no-verify`, geen force-push.
  </action>
  <verify>
    <automated>cd packages/mollie-api &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -q feat/foundation</automated>
  </verify>
  <done>User heeft een resume-signal gegeven; Claude heeft de bijbehorende push/PR/stop-action uitgevoerd. GitHub repo-description is bijgewerkt (indien approval gold dat in). Sub-repo branch staat lokaal op feat/foundation.</done>
  <resume-signal>
    Reageer met één van:
    - "approve push feat/foundation als default branch + description-update"
    - "approve push feat/foundation als feature-branch + open PR; rename na merge"
    - "stop, niet pushen yet" — Claude commit blijft lokaal en plan eindigt hier
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Hub composer.json — add mollie-path repository entry (W-4 master-branch-guard)</name>
  <read_first>
    - composer.json (root van Hub-repo, NIET sub-repo)
    - .ai/git-policy (regels)
  </read_first>
  <files>
    composer.json
  </files>
  <action>
W-4: VÓÓR enige `git add`/`git commit` in de Hub-repo: master-branch-guard.

```bash
cd /Users/yusufkaracaburun/Sites/localhost/emeq-hub
BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [[ "$BRANCH" == "master" || "$BRANCH" == "main" ]]; then
  echo "STOP: refuses to commit on master/main branch (per .ai/git-policy)"
  exit 1
fi
echo "Branch ok: $BRANCH"
```

Edit `composer.json` (Hub root) en voeg `mollie-path` entry toe in `repositories`. Behoud de bestaande snelstart-entries 1-op-1.

Huidige `repositories` (al aanwezig):

```json
"repositories": [
    {
        "name": "snelstart-path",
        "type": "path",
        "url": "packages/snelstart-api",
        "options": { "symlink": true }
    },
    {
        "name": "snelstart-vcs",
        "type": "vcs",
        "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git"
    }
]
```

Nieuwe `repositories` (mollie-path toegevoegd, snelstart-entries unchanged):

```json
"repositories": [
    {
        "name": "snelstart-path",
        "type": "path",
        "url": "packages/snelstart-api",
        "options": { "symlink": true }
    },
    {
        "name": "snelstart-vcs",
        "type": "vcs",
        "url": "https://github.com/yusufkaracaburun/emeq-snelstart-api.git"
    },
    {
        "name": "mollie-path",
        "type": "path",
        "url": "packages/mollie-api",
        "options": { "symlink": true }
    }
]
```

NIET toevoegen aan deze keer:
- `require: { "emeq/mollie-api": "dev-master" }` — pas wanneer een Hub-fase het pakket actief gebruikt (Phase 3+). De path-repo entry is genoeg om de Hub bewust te maken van de symlink zonder dat composer iets installeert.
- `mollie-vcs` (VCS-fallback voor productie-CI) — komt zodra de SDK een eerste release-tag heeft.

NA edit run:

```bash
composer validate --no-check-publish
```

(Mag warnings geven over abandoned packages — accepteren. Errors over JSON-syntax of duplicate repo-names moeten worden gefixed.)

Commit in Hub-repo (NIET in sub-repo) op de huidige branch — guard is hierboven al gerund:

```bash
cd /Users/yusufkaracaburun/Sites/localhost/emeq-hub
# W-4 master-branch guard: nogmaals zekerstellen we niet master/main raken
BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [[ "$BRANCH" == "master" || "$BRANCH" == "main" ]]; then
  echo "STOP: refuses to commit on master/main branch (per .ai/git-policy)"
  exit 1
fi

git add composer.json
git commit -m "$(cat <<'EOF'
chore(02): voeg mollie-path repository entry toe in Hub composer.json

Mirror van snelstart-path: maakt packages/mollie-api/ (sub-repo) beschikbaar
via composer symlink-pad. Geen require yet — die volgt zodra een Hub-fase
emeq/mollie-api actief gebruikt (Phase 3+).
EOF
)"
```
  </action>
  <verify>
    <automated>cd /Users/yusufkaracaburun/Sites/localhost/emeq-hub &amp;&amp; BRANCH=$(git rev-parse --abbrev-ref HEAD) &amp;&amp; [[ "$BRANCH" != "master" &amp;&amp; "$BRANCH" != "main" ]] &amp;&amp; grep -q 'mollie-path' composer.json &amp;&amp; grep -q 'packages/mollie-api' composer.json &amp;&amp; composer validate --no-check-publish 2&gt;&amp;1 | grep -qiE 'valid|^$' &amp;&amp; git log -1 --pretty=format:"%s" | grep -q 'chore(02): voeg mollie-path repository entry'</automated>
  </verify>
  <done>W-4 master-branch-guard is gepasseerd vóór add/commit (current branch != master/main). Hub composer.json heeft `mollie-path` entry toegevoegd, snelstart-entries unchanged. `composer validate` is groen. Hub heeft een nieuwe commit met chore-message op de huidige feature-branch.</done>
</task>

<task type="auto">
  <name>Task 4: VCS-smoke vanuit /tmp — bewijst ROADMAP Phase 2 success criterion 1 (W-3)</name>
  <read_first>
    - packages/mollie-api/composer.json (verifieer dat de naam `emeq/mollie-api` is)
    - Resultaat van Task 2: feat/foundation moet zijn gepusht naar github.com:yusufkaracaburun/emeq-mollie-api — anders kan de VCS-resolve niet slagen. Indien Task 2's resume-signal "stop" was → skip deze task met een note.
  </read_first>
  <files>
    (geen file-edits — alleen een /tmp-smoke en logoutput)
  </files>
  <action>
W-3: bewijs dat een fresh consumer-app `emeq/mollie-api` via de gepushte VCS-branch kan installeren zonder authenticatie of path-symlink.

```bash
# Skip-clause: alleen draaien indien Task 2's push daadwerkelijk is uitgevoerd.
cd packages/mollie-api
if ! git ls-remote --exit-code origin feat/foundation >/dev/null 2>&1; then
  echo "SKIP: feat/foundation is niet gepusht (Task 2 'stop'-pad?). VCS-smoke wordt overgeslagen."
  exit 0
fi

mkdir -p /tmp/mollie-vcs-smoke
cd /tmp/mollie-vcs-smoke

# Init een leeg consumer-project, point repository naar de gepushte VCS-repo,
# require de dev-branch van onze SDK.
composer init --no-interaction \
  --name=tmp/smoke \
  --require=emeq/mollie-api:dev-feat/foundation \
  --repository='{"type":"vcs","url":"https://github.com/yusufkaracaburun/emeq-mollie-api"}'

composer install --no-interaction --prefer-dist

# Assert: package is geïnstalleerd in vendor/
test -f vendor/emeq/mollie-api/composer.json
grep -q '"emeq/mollie-api"' vendor/emeq/mollie-api/composer.json

echo "VCS-smoke OK: emeq/mollie-api geïnstalleerd via VCS-resolve."
```

Output van deze task: stdout-log die in de plan-summary terechtkomt. Geen file-edits in de Hub of sub-repo.

Indien Task 2 met "stop" werd beëindigd (= push nog niet uitgevoerd): skip dit pad met een notitie. ROADMAP success criterion 1 wordt dan later (zodra de push wel is uitgevoerd) opnieuw gedraaid als losse smoke-call.
  </action>
  <verify>
    <automated>cd /tmp/mollie-vcs-smoke 2&gt;/dev/null &amp;&amp; [ -f vendor/emeq/mollie-api/composer.json ] &amp;&amp; grep -q '"emeq/mollie-api"' vendor/emeq/mollie-api/composer.json || { cd packages/mollie-api &amp;&amp; ! git ls-remote --exit-code origin feat/foundation &gt;/dev/null 2&gt;&amp;1; }</automated>
  </verify>
  <done>Eén van twee:
  (a) /tmp/mollie-vcs-smoke/vendor/emeq/mollie-api/composer.json bestaat — bewijs van een werkende VCS-install zonder auth (ROADMAP Phase 2 success criterion 1).
  (b) Task 2 was "stop"; deze task is correct geskipped en de skip-notitie staat in de plan-summary.</done>
</task>

</tasks>

<verification>
- Sub-repo `packages/mollie-api/` heeft één nieuwe commit op feat/foundation met alle Phase 2 source-files (composer.lock + .mollie-fake-fqcn-notes.md NIET in de commit — B-3)
- README.md bevat een sectie die naar Phase 6 SUB-01 verwijst voor alias-collision-resolutie (W-1)
- Pest-suite groen in sub-repo (final check vóór push)
- Hub `composer.json` toont `mollie-path` repository entry
- W-4: master-branch-guard ran vóór Hub add/commit; current branch != master/main
- `composer validate --no-check-publish` slaagt
- Push gebeurt alleen NA user-approval (checkpoint:human-action gate)
- GitHub-repo-description bijgewerkt van "Saloon v3" naar een wrap-mollie-api-php beschrijving (na approval)
- W-3: VCS-smoke task ran succesvol OF skipped met expliciete reden (Task 2 "stop"-pad)
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 ("composer require emeq/mollie-api via VCS-repository slaagt zonder authenticatie") is expliciet gevalideerd door Task 4 in /tmp (W-3)
- ROADMAP Phase 2 success criterion 3 (alias-collision) is bewust GEDEFERRED naar Phase 6 SUB-01; README documenteert dat (W-1)
- Sub-repo heeft upstream-tracking en een default branch op GitHub (na approval)
- Hub-repo kan vanaf nu `composer require emeq/mollie-api` doen wanneer een latere fase dat nodig heeft (alleen de require-entry toevoegen is nog gewenst)
- Git-policy is gerespecteerd: geen master-commits (W-4 guard actief), geen push zonder approval, geen --no-verify
- composer.lock blijft uit beide commits — SDK-best-practice (B-3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-emeq-mollie-api-foundation/02-08-SUMMARY.md`
</output>
</content>
</invoke>