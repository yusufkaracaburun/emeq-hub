---
phase: 03-hub-skeleton
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/Feature/ConnectionEncryptionTest.php
  - tests/Feature/ConsumerAccountScopingTest.php
autonomous: true
requirements:
  - HUB-01
tags:
  - laravel
  - phpunit
  - security
  - encryption
  - multi-tenant
must_haves:
  truths:
    - "Een Connection met `client_key = 'CK-secret-123'` heeft een DB-rauw-veld dat NIET letterlijk `'CK-secret-123'` is (encrypted at rest bewezen)"
    - "Dezelfde Connection retourneert via `$conn->client_key` weer `'CK-secret-123'` (decrypted on access)"
    - "`$conn->toArray()` bevat geen `client_key`, `subscription_key`, `access_token` of `refresh_token` keys (Hidden-attribute bewezen)"
    - "`$conn->fingerprint()` voor een Snelstart-Connection retourneert `substr(hash('sha256', $client_key), 0, 12)` (12-char hex)"
    - "`$conn->fingerprint()` voor een Mollie-Connection retourneert `substr(hash('sha256', $access_token), 0, 12)`"
    - "Cross-Consumer Account-query (`Account::where('consumer_id', $consumerA->id)->where('external_id', $consumerB->accounts[0]->external_id)`) retourneert geen rij — geen leak naar Consumer A van Consumer B's Account"
  artifacts:
    - path: "tests/Feature/ConnectionEncryptionTest.php"
      provides: "DB-bypass tests die encryption-at-rest + hidden-from-toArray + fingerprint-accessor bewijzen"
      contains: "use Illuminate\\\\Support\\\\Facades\\\\DB"
    - path: "tests/Feature/ConsumerAccountScopingTest.php"
      provides: "Query-laag tests die cross-Consumer-leakage bewijzen onmogelijk te zijn"
      contains: "class ConsumerAccountScopingTest"
  key_links:
    - from: "tests/Feature/ConnectionEncryptionTest.php"
      to: "app/Models/Connection.php"
      via: "Connection::factory()->forSnelstart()->create() consumeert encrypted casts"
      pattern: "Connection::factory\\(\\)->forSnelstart\\(\\)"
    - from: "tests/Feature/ConnectionEncryptionTest.php"
      to: "database/migrations/2026_05_14_000003_create_connections_table.php"
      via: "DB::table('connections')->value() leest rauwe ciphertext"
      pattern: "DB::table\\('connections'\\)"
    - from: "tests/Feature/ConsumerAccountScopingTest.php"
      to: "app/Models/Account.php"
      via: "Account::query()->where('consumer_id', ...)"
      pattern: "Account::query\\(\\)"
---

<objective>
Bewijs via gerichte PHPUnit-tests dat HUB-01's twee security-pijlers daadwerkelijk werken:
1. **Encryption-at-rest** (SC-3): Connection-credentials zijn in de DB encrypted, decrypted bij Eloquent-access, en hidden in `toArray()`/JSON
2. **Cross-Consumer-scoping** (SC-4): query-laag-isolation tussen Consumers — een Account van Consumer B is nooit reachable via `Account::where('consumer_id', $consumerA->id)`

Purpose: De casts en `#[Hidden]`-attributen uit plan 03-01 zijn alleen waardevol als ze daadwerkelijk verifieerbaar zijn. Dit plan levert de security-vangnetten waarmee Phase 5b's pass-through-API ervan uit mag gaan dat: (a) credentials nooit in audit-logs lekken, (b) route-level scoping niet de enige defense is — query-laag ook scopet correct.

Output: 2 nieuwe test-classes, 5-6 tests totaal. Geen wijzigingen aan models, migrations of seeders. Parallelliseerbaar met plan 03-02 (geen file-overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hub-skeleton/03-CONTEXT.md
@.planning/phases/03-hub-skeleton/03-PATTERNS.md
@.planning/phases/03-hub-skeleton/03-01-SUMMARY.md
@.ai/rules/global.md
@CLAUDE.md

<interfaces>
<!-- Connection-model interface uit plan 03-01. -->
```php
class Connection extends Model
{
    use HasFactory;

    // #[Hidden(['access_token', 'refresh_token', 'client_key', 'subscription_key'])]
    // casts: encrypted op deze 4 velden + array op scopes/metadata + datetime op expires_at/revoked_at

    public function account(): BelongsTo;
    public function fingerprint(): ?string; // sha256-prefix per provider
}
```

<!-- ConnectionFactory states uit plan 03-01. -->
```php
class ConnectionFactory extends Factory
{
    public function forSnelstart(): static; // vult client_key, subscription_key, subscription_id
    public function forMollie(): static;    // vult access_token, refresh_token, expires_at, scopes
}
```

<!-- DB-bypass-pattern (Laravel standard) — leest rauwe ciphertext zonder Eloquent-caster -->
```php
$rawAtRest = DB::table('connections')->where('id', $conn->id)->value('client_key');
// $rawAtRest != $plainSecret (anders is encryption stuk)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConnectionEncryptionTest — at-rest + hidden + fingerprint</name>
  <files>tests/Feature/ConnectionEncryptionTest.php</files>
  <read_first>
    - app/Models/Connection.php (uit 03-01 — casts, #[Hidden], fingerprint())
    - database/factories/ConnectionFactory.php (uit 03-01 — forSnelstart, forMollie states)
    - tests/Feature/NoIndexHeaderTest.php (Feature-test base-shape)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 1034-1093 (ConnectionEncryptionTest pattern)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 299-318 (encrypted-cast + fingerprint-test-assertions)
  </read_first>
  <behavior>
    - Test 1 (Snelstart encryption-at-rest): persisted client_key in DB is niet de plain-string, maar `$conn->client_key` retourneert wel de plain-string na re-fetch
    - Test 2 (Mollie encryption-at-rest): zelfde voor `access_token` op een `forMollie()`-Connection
    - Test 3 (toArray hides credentials): geen van de 4 credential-keys (`access_token`, `refresh_token`, `client_key`, `subscription_key`) verschijnt in `$conn->toArray()`
    - Test 4 (fingerprint Snelstart): retourneert sha256-prefix van client_key (12 chars hex)
    - Test 5 (fingerprint Mollie): retourneert sha256-prefix van access_token (12 chars hex)
    - Test 6 (fingerprint unknown provider): retourneert `null`
  </behavior>
  <action>
    Maak `tests/Feature/ConnectionEncryptionTest.php`. Behoud Tests\Feature-namespace (geen sub-folder — encryption
    is geen Api-test). DB-bypass via `Illuminate\Support\Facades\DB` om Eloquent-caster te omzeilen.

    ```php
    <?php

    namespace Tests\Feature;

    use App\Models\Connection;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Illuminate\Support\Facades\DB;
    use Tests\TestCase;

    class ConnectionEncryptionTest extends TestCase
    {
        use RefreshDatabase;

        public function test_snelstart_client_key_is_encrypted_at_rest(): void
        {
            $connection = Connection::factory()
                ->forSnelstart()
                ->create(['client_key' => 'CK-secret-123']);

            $rawAtRest = DB::table('connections')
                ->where('id', $connection->id)
                ->value('client_key');

            $this->assertNotSame('CK-secret-123', $rawAtRest);
            $this->assertNotEmpty($rawAtRest);
            $this->assertSame('CK-secret-123', $connection->fresh()->client_key);
        }

        public function test_snelstart_subscription_key_is_encrypted_at_rest(): void
        {
            $connection = Connection::factory()
                ->forSnelstart()
                ->create(['subscription_key' => 'SK-secret-456']);

            $rawAtRest = DB::table('connections')
                ->where('id', $connection->id)
                ->value('subscription_key');

            $this->assertNotSame('SK-secret-456', $rawAtRest);
            $this->assertSame('SK-secret-456', $connection->fresh()->subscription_key);
        }

        public function test_mollie_access_token_is_encrypted_at_rest(): void
        {
            $connection = Connection::factory()
                ->forMollie()
                ->create(['access_token' => 'access_secret-789']);

            $rawAtRest = DB::table('connections')
                ->where('id', $connection->id)
                ->value('access_token');

            $this->assertNotSame('access_secret-789', $rawAtRest);
            $this->assertSame('access_secret-789', $connection->fresh()->access_token);
        }

        public function test_to_array_hides_all_credential_fields(): void
        {
            $snelstart = Connection::factory()->forSnelstart()->create();
            $mollie = Connection::factory()->forMollie()->create();

            foreach ([$snelstart, $mollie] as $connection) {
                $array = $connection->toArray();

                $this->assertArrayNotHasKey('access_token', $array);
                $this->assertArrayNotHasKey('refresh_token', $array);
                $this->assertArrayNotHasKey('client_key', $array);
                $this->assertArrayNotHasKey('subscription_key', $array);
            }
        }

        public function test_fingerprint_returns_truncated_sha256_for_snelstart(): void
        {
            $connection = Connection::factory()
                ->forSnelstart()
                ->create(['client_key' => 'CK-secret-123']);

            $expected = substr(hash('sha256', 'CK-secret-123'), 0, 12);

            $this->assertSame($expected, $connection->fingerprint());
            $this->assertSame(12, strlen($connection->fingerprint()));
        }

        public function test_fingerprint_returns_truncated_sha256_for_mollie(): void
        {
            $connection = Connection::factory()
                ->forMollie()
                ->create(['access_token' => 'access_secret-789']);

            $expected = substr(hash('sha256', 'access_secret-789'), 0, 12);

            $this->assertSame($expected, $connection->fingerprint());
        }

        public function test_fingerprint_returns_null_for_unknown_provider(): void
        {
            $connection = Connection::factory()->create([
                'provider' => 'future-provider',
                'client_key' => null,
                'access_token' => null,
            ]);

            $this->assertNull($connection->fingerprint());
        }
    }
    ```

    Belangrijk:
    - PHPUnit-assertions (geen Pest `expect()`)
    - `DB::table()->value()` voor rauwe-DB-leesactie — omzeilt encrypted-cast
    - `->fresh()` om model uit DB te herladen i.p.v. in-memory cache
    - Geen MockEncrypter / config-tweaks — gebruik echte `APP_KEY` uit phpunit.xml env

    Run `vendor/bin/pint --dirty --format agent tests/Feature/ConnectionEncryptionTest.php`.
  </action>
  <verify>
    <automated>
      php artisan test --compact --filter=ConnectionEncryptionTest
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/ConnectionEncryptionTest.php` bestaat
    - `grep -c "public function test_" tests/Feature/ConnectionEncryptionTest.php` == 7
    - `grep -c "DB::table('connections')" tests/Feature/ConnectionEncryptionTest.php` >= 3 (rauwe DB-reads voor 3 velden)
    - `grep -c "assertNotSame" tests/Feature/ConnectionEncryptionTest.php` >= 3
    - `php artisan test --compact --filter=ConnectionEncryptionTest` exit code 0 (alle 7 groen)
    - Geen Pest-syntax (`grep -c "^it(" tests/Feature/ConnectionEncryptionTest.php` == 0)
    - `vendor/bin/pint --test --format agent tests/Feature/ConnectionEncryptionTest.php` exit code 0
  </acceptance_criteria>
  <done>
    7 tests groen. Encryption-at-rest, Hidden-from-toArray, en fingerprint-accessor zijn alle drie machine-geverifieerd
    voor zowel Snelstart-shape als Mollie-shape. SC-3 uit ROADMAP is bewezen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConsumerAccountScopingTest — query-laag cross-Consumer-isolation</name>
  <files>tests/Feature/ConsumerAccountScopingTest.php</files>
  <read_first>
    - app/Models/Consumer.php (uit 03-01 — accounts() HasMany)
    - app/Models/Account.php (uit 03-01 — consumer() BelongsTo + Consumer-scoped queries)
    - database/factories/{Consumer,Account}Factory.php (uit 03-01)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 221-223 (Cross-Consumer scoping decision: 404 i.p.v. 403)
    - .ai/rules/global.md (Multi-tenant scope rule — cross-consumer-leakage = security incident)
  </read_first>
  <behavior>
    - Test 1: Twee Consumers met identieke `external_id` ("school1") op hun eigen Account creëren slaagt (uniqueness is per consumer-tuple, niet globaal)
    - Test 2: `Account::where('consumer_id', $consumerA->id)->where('external_id', $consumerB_account_external_id)` retourneert `null` (geen leak)
    - Test 3: `$consumerA->accounts()->where('external_id', ...)`-query op Consumer B's external_id retourneert empty collection
    - Test 4: De globale unique constraint `(consumer_id, external_id)` op `accounts`-tabel staat duplicate external_id WEL toe wanneer consumer_id verschilt — bewijst multi-tenant scope-design
  </behavior>
  <action>
    Maak `tests/Feature/ConsumerAccountScopingTest.php`. Bewijst via query-pattern dat Phase 5b's pass-through-API
    veilig kan vertrouwen op `Account::where('consumer_id', $request->user()->id)` als scoping-mechanisme.

    ```php
    <?php

    namespace Tests\Feature;

    use App\Models\Account;
    use App\Models\Consumer;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Tests\TestCase;

    class ConsumerAccountScopingTest extends TestCase
    {
        use RefreshDatabase;

        public function test_two_consumers_can_have_same_external_id(): void
        {
            $consumerA = Consumer::factory()->create(['slug' => 'naschool']);
            $consumerB = Consumer::factory()->create(['slug' => 'planny']);

            $accountA = $consumerA->accounts()->create([
                'external_id' => 'school1',
                'display_name' => 'Naschool School 1',
            ]);

            $accountB = $consumerB->accounts()->create([
                'external_id' => 'school1',
                'display_name' => 'Planny School 1',
            ]);

            $this->assertNotSame($accountA->id, $accountB->id);
            $this->assertSame('school1', $accountA->external_id);
            $this->assertSame('school1', $accountB->external_id);
        }

        public function test_consumer_a_cannot_query_consumer_b_account_via_scope(): void
        {
            $consumerA = Consumer::factory()->create();
            $consumerB = Consumer::factory()->create();

            $consumerB->accounts()->create([
                'external_id' => 'b-only-school',
                'display_name' => 'Belongs to B',
            ]);

            $leaked = Account::query()
                ->where('consumer_id', $consumerA->id)
                ->where('external_id', 'b-only-school')
                ->first();

            $this->assertNull($leaked);
        }

        public function test_consumer_relation_query_only_returns_own_accounts(): void
        {
            $consumerA = Consumer::factory()->create();
            $consumerB = Consumer::factory()->create();

            $consumerA->accounts()->create(['external_id' => 'a-school', 'display_name' => 'A']);
            $consumerB->accounts()->create(['external_id' => 'b-school', 'display_name' => 'B']);

            $accountsForA = $consumerA->accounts()->get();

            $this->assertCount(1, $accountsForA);
            $this->assertSame('a-school', $accountsForA->first()->external_id);
        }

        public function test_duplicate_external_id_within_same_consumer_fails(): void
        {
            $consumer = Consumer::factory()->create();

            $consumer->accounts()->create([
                'external_id' => 'duplicate-key',
                'display_name' => 'First',
            ]);

            $this->expectException(\Illuminate\Database\QueryException::class);

            $consumer->accounts()->create([
                'external_id' => 'duplicate-key',
                'display_name' => 'Second',
            ]);
        }
    }
    ```

    Belangrijk:
    - Test 4 (`test_duplicate_external_id_within_same_consumer_fails`) bewijst de DB-level unique-constraint `(consumer_id, external_id)` uit plan 03-01's migration
    - Geen Sanctum / route-laag — puur query-pattern bewijs voor Phase 5b
    - PHPUnit `$this->expectException(...)` voor de QueryException-path

    Run `vendor/bin/pint --dirty --format agent tests/Feature/ConsumerAccountScopingTest.php`.
  </action>
  <verify>
    <automated>
      php artisan test --compact --filter=ConsumerAccountScopingTest
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/ConsumerAccountScopingTest.php` bestaat
    - `grep -c "public function test_" tests/Feature/ConsumerAccountScopingTest.php` == 4
    - `grep -c "QueryException" tests/Feature/ConsumerAccountScopingTest.php` == 1 (test 4)
    - `grep -c "assertNull" tests/Feature/ConsumerAccountScopingTest.php` >= 1 (cross-consumer leak-bewijs)
    - `php artisan test --compact --filter=ConsumerAccountScopingTest` exit code 0 (alle 4 groen)
    - `vendor/bin/pint --test --format agent tests/Feature/ConsumerAccountScopingTest.php` exit code 0
  </acceptance_criteria>
  <done>
    4 tests groen. Cross-Consumer-leakage is structureel onmogelijk via standaard Eloquent-queries; DB-level
    unique-constraint is functioneel. SC-4 fundament voor Phase 5b is gelegd.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application ↔ Database | Encrypted-cast garandeert dat een database-dump geen plain credentials lekt |
| Consumer A ↔ Consumer B | Eloquent-query-laag is een security-boundary; `consumer_id`-scope filter is de eerste defense (route-level filter is de tweede defense in Phase 5b) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | DB-dump exposes plain credentials | mitigate (test) | `test_*_is_encrypted_at_rest`-tests bewijzen rauwe DB-value != plain. Defense is APP_KEY-encryption op model-laag |
| T-03-15 | Information Disclosure | `$conn->toArray()` lekt credentials in JSON-responses (toekomstige `/v1/connections` GET in Phase 5b) | mitigate (test) | `test_to_array_hides_all_credential_fields` bewijst dat Hidden-attribute alle 4 velden ondervangt |
| T-03-16 | Information Disclosure | Logged Connection-object in exception/Telescope exposes credentials | mitigate (test) | Fingerprint-accessor-tests bewijzen dat alleen `substr(hash('sha256', secret), 0, 12)` als publieke identifier bruikbaar is. Phase 5b audit-log gebruikt deze accessor |
| T-03-17 | Spoofing | Consumer A vraagt Consumer B's Account via direct ID-guess | mitigate (test) | `test_consumer_a_cannot_query_consumer_b_account_via_scope` bewijst dat de standard query-pattern leak voorkomt. Phase 5b's middleware bouwt op deze garantie |
| T-03-18 | Tampering | Verwijderen van encryption-cast door regressie | mitigate (test) | Encryption-tests gaan stuk als iemand een cast verwijdert — CI-gate. Dit is precies waarom de tests in Wave 2 leven (parallel met config) — verifieert plan 03-01 in isolatie |
</threat_model>

<verification>
- `php artisan test --compact --filter=ConnectionEncryptionTest` exit 0 (7 groen)
- `php artisan test --compact --filter=ConsumerAccountScopingTest` exit 0 (4 groen)
- Tests blijven groen op SQLite in-memory én op lokale Postgres
- `vendor/bin/pint --test --format agent tests/Feature/` exit 0
</verification>

<success_criteria>
- HUB-01 SC-3 (geen raw credentials in `toArray()`) is bewezen via 7 tests
- HUB-01 SC-4 (cross-Consumer-scoping faalt) is op query-laag bewezen via 4 tests (route-laag-bewijs volgt in Phase 5b)
- Phase 5b mag ervan uitgaan dat `Account::where('consumer_id', $user->id)->where('external_id', $header)` veilig is
</success_criteria>

<output>
Maak `.planning/phases/03-hub-skeleton/03-04-SUMMARY.md` met:
- 11 tests groen (7 encryption + 4 scoping)
- Welke security-claims uit HUB-01 nu geverifieerd zijn (SC-3 en SC-4 query-laag)
- Welke claim NOG niet is bewezen (SC-4 route-laag — wacht op Phase 5b's `/v1/snelstart/{path}` + `X-Account-Id`-header-flow)
</output>
