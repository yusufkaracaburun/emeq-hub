---
phase: 03-hub-skeleton
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - app/Console/Commands/HubConsumerCreate.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Console/HubConsumerCreateTest.php
autonomous: true
requirements:
  - HUB-01
tags:
  - laravel
  - artisan
  - seeder
  - sanctum
  - cli
must_haves:
  truths:
    - "`php artisan migrate:fresh --seed` slaagt en levert demo-Consumer (`slug = 'naschool'`) + demo-Account (`external_id = 'school1'`)"
    - "`php artisan hub:consumer:create --slug=acme --name=Acme --abilities=snelstart:read,snelstart:write` maakt een Consumer + PAT en print de plain-token éénmalig in CLI-output"
    - "Zonder `--slug` of `--name` faalt het command met exit-code != 0 en een Nederlandse error-message"
    - "DatabaseSeeder runt NIET wanneer `app()->isProduction()` true is (production-guard)"
    - "Een via `hub:consumer:create` gemaakte PAT werkt op `/v1/ping` (end-to-end smoke)"
  artifacts:
    - path: "app/Console/Commands/HubConsumerCreate.php"
      provides: "Artisan-command voor CLI Consumer + PAT-uitgifte"
      contains: "hub:consumer:create"
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Production-guarded seeder met demo-Consumer + demo-Account"
      contains: "isProduction"
    - path: "tests/Feature/Console/HubConsumerCreateTest.php"
      provides: "PHPUnit-tests voor command happy-path + missing-args-path"
      contains: "class HubConsumerCreateTest"
  key_links:
    - from: "app/Console/Commands/HubConsumerCreate.php"
      to: "app/Models/Consumer.php"
      via: "Consumer::create + createToken (HasApiTokens)"
      pattern: "Consumer::create"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "app/Models/Consumer.php"
      via: "Consumer::firstOrCreate + $consumer->accounts()->firstOrCreate"
      pattern: "Consumer::firstOrCreate"
---

<objective>
Lever het laatste stukje HUB-01: een CLI-flow waarmee een ontwikkelaar zonder UI een Consumer + PAT kan aanmaken (vereist voor smoke-testing van Phase 5b's pass-through-API), en een idempotente DatabaseSeeder die `php artisan migrate:fresh --seed` een werkbare demo-state geeft. Het command is geen Filament-vervanger — dat komt in Phase 9 — maar overbrugt de gap tussen "skeleton staat" en "Phase 5b kan smoke-testen via curl".

Purpose: HUB-01 SC-1 vereist `migrate:fresh --seed` levert demo-Consumer + demo-Account. Dit plan voltooit dat criterion. Het `hub:consumer:create`-command is bovendien dé manier waarop nieuwe Consumers in productie (later) worden aangemaakt totdat Phase 9 Filament-UI er is.

Output: 1 artisan-command, 1 uitgebreide seeder, 1 test-class. Run `php artisan test` na completion om alle Phase 3-tests (uit 03-03, 03-04, en dit plan) collectief groen te zien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hub-skeleton/03-CONTEXT.md
@.planning/phases/03-hub-skeleton/03-PATTERNS.md
@.planning/phases/03-hub-skeleton/03-01-SUMMARY.md
@.planning/phases/03-hub-skeleton/03-02-SUMMARY.md
@.planning/phases/03-hub-skeleton/03-03-SUMMARY.md
@.ai/rules/global.md
@CLAUDE.md

<interfaces>
<!-- Consumer-model uit plan 03-01 — wat we via CLI aanmaken. -->
```php
class Consumer extends Authenticatable
{
    use HasApiTokens, HasFactory;

    public string $slug;
    public string $name;

    public function accounts(): HasMany;
    public function createToken(string $name, array $abilities): NewAccessToken;
}
```

<!-- NewAccessToken — Sanctum's return-type, levert plain-text token éénmalig. -->
```php
// vendor/laravel/sanctum/src/NewAccessToken.php
class NewAccessToken
{
    public string $plainTextToken; // "1|abcd1234..." — toon EENMAAL in CLI
    public PersonalAccessToken $accessToken;
}
```

<!-- TokenAbilities uit plan 03-02 — default voor --abilities=*. -->
```php
final class App\Sanctum\TokenAbilities
{
    public const ADMIN = '*';
    public static function all(): array; // 6 abilities
}
```

<!-- Laravel command-base (huidige routes/console.php gebruikt closure-style; we maken class-based). -->
```php
class HubConsumerCreate extends Illuminate\Console\Command
{
    protected $signature = 'hub:consumer:create {--slug=} {--name=} {--abilities=*} {--token-name=cli-default}';
    public function handle(): int; // SUCCESS = 0, INVALID = 2, FAILURE = 1
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hub:consumer:create artisan-command</name>
  <files>app/Console/Commands/HubConsumerCreate.php</files>
  <read_first>
    - routes/console.php (huidige closure-style command voor referentie)
    - app/Models/Consumer.php (uit 03-01 — createToken via HasApiTokens)
    - app/Sanctum/TokenAbilities.php (uit 03-02 — ADMIN-default + alle abilities)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 292-364 (HubConsumerCreate pattern)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 191-205 (command-spec)
    - .ai/rules/global.md (Nederlandse user-facing CLI-output, Engelse option-names)
  </read_first>
  <behavior>
    - `php artisan hub:consumer:create --slug=naschool --name=Naschool` → maakt Consumer, maakt PAT met default `['*']`-abilities, exit 0
    - `php artisan hub:consumer:create --slug=acme --name=Acme --abilities=snelstart:read,snelstart:write` → comma-separated abilities worden gesplitst tot `['snelstart:read', 'snelstart:write']`
    - `php artisan hub:consumer:create` (geen args) → exit 2 (`Command::INVALID`) + Nederlandse error
    - `php artisan hub:consumer:create --slug=duplicate --name=X` op een bestaande slug → exit 1 (`Command::FAILURE`) — wegens unique-constraint QueryException
    - Plain-text token verschijnt EENMAAL in `$this->warn()`-output; nergens in log-files of returns
  </behavior>
  <action>
    Genereer via `php artisan make:command HubConsumerCreate --no-interaction`. Vervang body met:

    ```php
    <?php

    namespace App\Console\Commands;

    use App\Models\Consumer;
    use App\Sanctum\TokenAbilities;
    use Illuminate\Console\Command;
    use Illuminate\Database\QueryException;

    class HubConsumerCreate extends Command
    {
        protected $signature = 'hub:consumer:create
                                {--slug= : Unieke slug (kebab-case identifier)}
                                {--name= : Vrije weergave-naam}
                                {--abilities=* : Comma-separated of meermaals (default: *)}
                                {--token-name=cli-default : Naam van het PAT-record}';

        protected $description = 'Maak een Consumer + Personal Access Token aan vanaf de CLI';

        public function handle(): int
        {
            $slug = (string) $this->option('slug');
            $name = (string) $this->option('name');

            if ('' === $slug || '' === $name) {
                $this->error('--slug en --name zijn verplicht.');

                return self::INVALID;
            }

            try {
                $consumer = Consumer::create(['slug' => $slug, 'name' => $name]);
            } catch (QueryException $e) {
                $this->error("Aanmaken Consumer mislukt: {$e->getMessage()}");

                return self::FAILURE;
            }

            $abilities = $this->resolveAbilities();
            $tokenName = (string) $this->option('token-name');
            $token = $consumer->createToken($tokenName, $abilities);

            $this->info("Consumer aangemaakt: id={$consumer->id}, slug={$consumer->slug}");
            $this->info("Token name: {$tokenName}");
            $this->info('Abilities: '.implode(', ', $abilities));
            $this->warn("Plain-text token (toon eenmalig): {$token->plainTextToken}");

            return self::SUCCESS;
        }

        /**
         * @return list<string>
         */
        private function resolveAbilities(): array
        {
            /** @var list<string> $raw */
            $raw = (array) $this->option('abilities');

            if ([] === $raw) {
                return [TokenAbilities::ADMIN];
            }

            return collect($raw)
                ->flatMap(fn (string $item): array => explode(',', $item))
                ->map(fn (string $item): string => trim($item))
                ->filter()
                ->values()
                ->all();
        }
    }
    ```

    Belangrijk:
    - `$signature` met `--abilities=*` (array-option) zodat zowel `--abilities=a,b` als `--abilities=a --abilities=b` werkt
    - `$description` in Nederlands, option-descriptions in Nederlands (per .ai/rules/global.md)
    - `Command::INVALID` (= 2) bij missing args, `Command::FAILURE` (= 1) bij QueryException, `Command::SUCCESS` (= 0) bij success
    - Plain-token alleen via `$this->warn()` — Laravel logt `info`/`warn` niet automatisch naar `storage/logs/`
    - `resolveAbilities()` als private helper voor testbaarheid + leesbaarheid
    - `TokenAbilities::ADMIN` als default i.p.v. hard-coded `'*'` — refactor-safe koppeling 03-02 ↔ 03-05

    Run `vendor/bin/pint --dirty --format agent app/Console/Commands/HubConsumerCreate.php`.
  </action>
  <verify>
    <automated>
      php artisan list --raw 2>/dev/null | grep -c "^hub:consumer:create" &amp;&amp;
      php artisan hub:consumer:create --help 2>/dev/null | grep -c -- "--slug"
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/Console/Commands/HubConsumerCreate.php` bestaat
    - `grep -c "hub:consumer:create" app/Console/Commands/HubConsumerCreate.php` == 1 (in signature)
    - `grep -c "Command::INVALID" app/Console/Commands/HubConsumerCreate.php` == 1
    - `grep -c "Command::SUCCESS" app/Console/Commands/HubConsumerCreate.php` == 1
    - `grep -c "TokenAbilities::ADMIN" app/Console/Commands/HubConsumerCreate.php` == 1
    - `php artisan list --raw` toont `hub:consumer:create`-regel
    - `php artisan hub:consumer:create --help` exit 0 en toont alle 4 options
    - `vendor/bin/pint --test --format agent app/Console/Commands/HubConsumerCreate.php` exit 0
  </acceptance_criteria>
  <done>
    Command is auto-discovered (Laravel 11+ scant `app/Console/Commands/` zonder kernel-registratie). Signature bevat
    4 options. Defaults werken (abilities=`*`, token-name=`cli-default`). Error-paden retourneren juiste exit-codes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubConsumerCreateTest — happy + invalid + duplicate</name>
  <files>tests/Feature/Console/HubConsumerCreateTest.php</files>
  <read_first>
    - app/Console/Commands/HubConsumerCreate.php (task 1 output — wat we testen)
    - tests/Feature/Api/PingTest.php (uit 03-03 — RefreshDatabase + Consumer-fixture-pattern)
    - phpunit.xml (SQLite in-memory voor isolation)
    - .ai/rules/global.md (Nederlandse error-strings — we asserteren tegen literal)
  </read_first>
  <behavior>
    - Test 1 (happy): `artisan('hub:consumer:create', ['--slug' => 'test-slug', '--name' => 'Test'])` exit 0; Consumer + PAT-record bestaan in DB
    - Test 2 (missing slug): exit 2 (`INVALID`); output bevat "verplicht"; geen Consumer in DB
    - Test 3 (missing name): exit 2; geen Consumer
    - Test 4 (duplicate slug): tweede call met zelfde slug → exit 1 (`FAILURE`)
    - Test 5 (abilities split): `--abilities=snelstart:read,snelstart:write` → PAT in DB heeft abilities `['snelstart:read', 'snelstart:write']`
  </behavior>
  <action>
    Maak directory `tests/Feature/Console/` en bestand `tests/Feature/Console/HubConsumerCreateTest.php`.

    ```php
    <?php

    namespace Tests\Feature\Console;

    use App\Models\Consumer;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Laravel\Sanctum\PersonalAccessToken;
    use Tests\TestCase;

    class HubConsumerCreateTest extends TestCase
    {
        use RefreshDatabase;

        public function test_creates_consumer_with_default_admin_ability(): void
        {
            $this->artisan('hub:consumer:create', [
                '--slug' => 'naschool-test',
                '--name' => 'Naschool Test',
            ])->assertExitCode(0);

            $consumer = Consumer::where('slug', 'naschool-test')->first();
            $this->assertNotNull($consumer);
            $this->assertSame('Naschool Test', $consumer->name);

            $token = PersonalAccessToken::where('tokenable_id', $consumer->id)
                ->where('tokenable_type', Consumer::class)
                ->first();
            $this->assertNotNull($token);
            $this->assertSame(['*'], $token->abilities);
        }

        public function test_fails_with_invalid_exit_when_slug_missing(): void
        {
            $this->artisan('hub:consumer:create', ['--name' => 'Only Name'])
                ->expectsOutputToContain('verplicht')
                ->assertExitCode(2);

            $this->assertSame(0, Consumer::count());
        }

        public function test_fails_with_invalid_exit_when_name_missing(): void
        {
            $this->artisan('hub:consumer:create', ['--slug' => 'only-slug'])
                ->assertExitCode(2);

            $this->assertSame(0, Consumer::count());
        }

        public function test_duplicate_slug_returns_failure_exit_code(): void
        {
            $this->artisan('hub:consumer:create', [
                '--slug' => 'dup',
                '--name' => 'First',
            ])->assertExitCode(0);

            $this->artisan('hub:consumer:create', [
                '--slug' => 'dup',
                '--name' => 'Second',
            ])->assertExitCode(1);

            $this->assertSame(1, Consumer::where('slug', 'dup')->count());
        }

        public function test_abilities_csv_is_split_into_array(): void
        {
            $this->artisan('hub:consumer:create', [
                '--slug' => 'abilities-test',
                '--name' => 'Abilities Test',
                '--abilities' => 'snelstart:read,snelstart:write',
            ])->assertExitCode(0);

            $consumer = Consumer::where('slug', 'abilities-test')->first();
            $token = PersonalAccessToken::where('tokenable_id', $consumer->id)->first();

            $this->assertSame(['snelstart:read', 'snelstart:write'], $token->abilities);
        }
    }
    ```

    Belangrijk:
    - `$this->artisan(...)->assertExitCode(0)` is Laravel's command-testing-API (geen Symfony Console-shimming nodig)
    - `expectsOutputToContain('verplicht')` voor Nederlandse error-string assertion
    - PersonalAccessToken-tabel werd al gemigreerd in repo (`2026_05_13_223626_create_personal_access_tokens_table.php`) en heeft `abilities` als JSON-cast
    - `--abilities` als string (niet array) werkt omdat command's `resolveAbilities()` op `explode(',')` doet

    Run `vendor/bin/pint --dirty --format agent tests/Feature/Console/HubConsumerCreateTest.php`.
  </action>
  <verify>
    <automated>
      php artisan test --compact --filter=HubConsumerCreateTest
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Console/HubConsumerCreateTest.php` bestaat
    - `grep -c "public function test_" tests/Feature/Console/HubConsumerCreateTest.php` == 5
    - `grep -c "assertExitCode(0)" tests/Feature/Console/HubConsumerCreateTest.php` >= 2
    - `grep -c "assertExitCode(2)" tests/Feature/Console/HubConsumerCreateTest.php` == 2
    - `grep -c "assertExitCode(1)" tests/Feature/Console/HubConsumerCreateTest.php` == 1
    - `php artisan test --compact --filter=HubConsumerCreateTest` exit 0 (5 groen)
    - `vendor/bin/pint --test --format agent tests/Feature/Console/HubConsumerCreateTest.php` exit 0
  </acceptance_criteria>
  <done>
    5 tests groen. Command happy-path + 2 missing-arg-paden + duplicate-slug-pad + abilities-CSV-split zijn allemaal
    gedekt. Plain-text token-output wordt niet asserted (`$this->warn` schrijft naar STDERR — niet kritisch voor
    test-pad).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DatabaseSeeder demo-Consumer + demo-Account + production-guard</name>
  <files>database/seeders/DatabaseSeeder.php</files>
  <read_first>
    - database/seeders/DatabaseSeeder.php (huidige body — User-factory-call mag blijven)
    - app/Models/Consumer.php + app/Models/Account.php (uit 03-01)
    - .planning/phases/03-hub-skeleton/03-PATTERNS.md regels 829-890 (DatabaseSeeder-uitbreidings-pattern)
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md regels 207-218 (seeder-spec + production-guard)
  </read_first>
  <action>
    Pas `database/seeders/DatabaseSeeder.php` aan: voeg `App\Models\Consumer` import toe, voeg een
    `app()->isProduction()` early-return toe, en seed één demo-Consumer ("naschool") + één demo-Account
    ("school1") via `firstOrCreate` (idempotent — geen duplicaten bij herhaalde seed-runs).

    ```php
    <?php

    namespace Database\Seeders;

    use App\Models\Consumer;
    use App\Models\User;
    use Illuminate\Database\Console\Seeds\WithoutModelEvents;
    use Illuminate\Database\Seeder;

    class DatabaseSeeder extends Seeder
    {
        use WithoutModelEvents;

        /**
         * Seed the application's database.
         */
        public function run(): void
        {
            if (app()->isProduction()) {
                return;
            }

            User::factory()->create([
                'name' => 'Test User',
                'email' => 'test@example.com',
            ]);

            $consumer = Consumer::firstOrCreate(
                ['slug' => 'naschool'],
                ['name' => 'Naschool'],
            );

            $consumer->accounts()->firstOrCreate(
                ['external_id' => 'school1'],
                ['display_name' => 'Demo School 1'],
            );
        }
    }
    ```

    Belangrijk:
    - `app()->isProduction()` als early-return — NOOIT seed-data in productie (PROJECT-rule)
    - `firstOrCreate` (niet `create`) zodat `migrate:fresh --seed` herhaalbaar werkt zonder unique-violations bij scenarios waar de schema niet vers is
    - Geen Connection seeden — die komen in Phase 5b via `POST /v1/connections` of in Phase 9 admin-UI (CONTEXT.md regel 217)
    - `User::factory()->create()` blijft — die hoort bij Filament Phase 9 en is een onafhankelijk pad

    Run `vendor/bin/pint --dirty --format agent database/seeders/DatabaseSeeder.php`. Smoke-test handmatig:
    `php artisan migrate:fresh --seed --no-interaction`.
  </action>
  <verify>
    <automated>
      php artisan migrate:fresh --seed --no-interaction &amp;&amp;
      php artisan tinker --execute 'use App\Models\Consumer; use App\Models\Account; $c = Consumer::where("slug", "naschool")->first(); $a = Account::where("external_id", "school1")->first(); echo $c &amp;&amp; $c->name === "Naschool" ? "consumer:ok " : "consumer:FAIL "; echo $a &amp;&amp; $a->consumer_id === $c?->id ? "account:ok " : "account:FAIL "; echo \App\Models\Connection::count() === 0 ? "no-connection:ok\n" : "no-connection:FAIL\n";'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use App\\\\Models\\\\Consumer" database/seeders/DatabaseSeeder.php` == 1
    - `grep -c "isProduction" database/seeders/DatabaseSeeder.php` == 1
    - `grep -c "firstOrCreate" database/seeders/DatabaseSeeder.php` == 2 (Consumer + Account)
    - `grep -c "User::factory" database/seeders/DatabaseSeeder.php` == 1 (Test User blijft)
    - `php artisan migrate:fresh --seed --no-interaction` exit 0
    - Tinker-output bevat `consumer:ok account:ok no-connection:ok`
    - `vendor/bin/pint --test --format agent database/seeders/DatabaseSeeder.php` exit 0
  </acceptance_criteria>
  <done>
    `migrate:fresh --seed` produceert in dev/test exact: 1 User, 1 Consumer ("naschool"), 1 Account ("school1"), 0
    Connections. In productie (`APP_ENV=production`) seed-call is no-op. SC-1 uit ROADMAP is bewezen.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: End-to-end smoke — Phase 3 acceptance run</name>
  <files></files>
  <read_first>
    - .planning/phases/03-hub-skeleton/03-CONTEXT.md (Phase Boundary + Success Criteria)
    - .planning/ROADMAP.md Phase 3 Success Criteria (regels 76-82)
  </read_first>
  <action>
    Geen file-mutaties — alleen end-to-end verificatie dat alle drie Phase 3-stukken (data-model, Sanctum-config,
    routes/api.php + smoke-endpoint, security-tests, seeder, CLI-command) samen werken.

    **Run de complete acceptance-flow:**

    1. `php artisan migrate:fresh --seed --no-interaction` → moet slagen (SC-1)
    2. `php artisan hub:consumer:create --slug=smoke-test --name="Smoke Test" --abilities=snelstart:read` → moet slagen en plain-token printen
    3. Kopieer de plain-token (zichtbaar in `$this->warn`-output)
    4. Curl-smoke (lokaal — alleen als `php artisan serve` of caddy draait):
       ```bash
       curl -s -H "Authorization: Bearer <plain-token>" -H "Accept: application/json" http://localhost:8001/v1/ping
       # Expected JSON: {"pong":true,"consumer":"smoke-test","abilities":["snelstart:read"]}
       ```
       Indien geen lokale server actief: vervang door tinker-based assertion:
       ```bash
       php artisan tinker --execute 'use Illuminate\Support\Facades\Http; $r = $this->call("GET", "/v1/ping", [], [], [], ["HTTP_AUTHORIZATION" => "Bearer <plain-token>", "HTTP_ACCEPT" => "application/json"]); echo $r->status();'
       ```
    5. `php artisan test --compact` → moet alle Phase 3-tests groen tonen (5+ in 03-03, 11 in 03-04, 5 in dit plan = ~21 tests + bestaande NoIndexHeaderTest + ExampleTest)
    6. `vendor/bin/pint --test --format agent` → exit 0 op de hele app

    Documenteer in 03-05-SUMMARY.md welke commando's exact zijn gedraaid en wat de outputs waren.
  </action>
  <verify>
    <automated>
      php artisan migrate:fresh --seed --no-interaction &amp;&amp;
      php artisan test --compact &amp;&amp;
      vendor/bin/pint --test --format agent
    </automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh --seed --no-interaction` exit 0
    - `php artisan test --compact` exit 0 (geen failures; incomplete is OK)
    - Test-summary toont minimaal: 3 PingTest, 3 SanctumAbilityTest (2 passed + 1 incomplete), 7 ConnectionEncryptionTest, 4 ConsumerAccountScopingTest, 5 HubConsumerCreateTest, 1 NoIndexHeaderTest = minstens 22 test-methods uitgevoerd
    - `vendor/bin/pint --test --format agent` exit 0 op de hele app
    - Demo-Consumer + demo-Account zichtbaar via `php artisan tinker --execute 'echo App\Models\Consumer::count();'` == 1 (na seed)
  </acceptance_criteria>
  <done>
    Phase 3 is end-to-end groen: alle 5 ROADMAP success-criteria zijn bewijsbaar (SC-1 via seeder, SC-2 via PingTest +
    smoke-curl, SC-3 via ConnectionEncryptionTest, SC-4 via ConsumerAccountScopingTest, SC-5 via ConnectionFactory
    state-methodes in plan 03-01). Phase 4 / 5a / 5b kunnen op deze fundatie verder bouwen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI-operator ↔ `hub:consumer:create` | Operator heeft shell-access; plain-token-print is bewust éénmalig zichtbaar |
| `php artisan db:seed` ↔ productie-omgeving | Demo-data mag nooit in production-DB landen — guard noodzakelijk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Information Disclosure | Plain-text PAT in shell-history of log-file | accept | CLI-output via `$this->warn()` gaat naar STDERR; Laravel logt `info`/`warn` niet naar `storage/logs/`. Operator is verantwoordelijk voor shell-history (out-of-scope security-boundary) |
| T-03-20 | Information Disclosure | Demo-Consumer "naschool" in productie-DB | mitigate | `if (app()->isProduction()) { return; }` early-return in seeder; geverifieerd in tinker-flow (geen production-test maar code-pad is direct readable) |
| T-03-21 | Elevation of Privilege | Operator maakt admin-token (`['*']`) zonder noodzaak | accept | Standard-flow voor v0.2; minder-privilege-defaults zijn nice-to-have voor Phase 9. CLI-output toont expliciet welke abilities zijn uitgegeven — operator-awareness |
| T-03-22 | Repudiation | Operator wist welke PATs zijn uitgegeven | accept | `personal_access_tokens.created_at` + `name`-kolom geven enige audit-trail. Filament-paneel (Phase 9 HUB-04) levert UI voor revoke/audit |
| T-03-23 | Denial of Service | Bulk-create-spam via repeated calls | accept | CLI is operator-only; geen public endpoint. Niet in scope voor mitigation |
</threat_model>

<verification>
- `php artisan migrate:fresh --seed --no-interaction` slaagt; demo-data correct
- `php artisan hub:consumer:create --help` toont alle 4 options
- `php artisan test --compact` exit 0 (alle Phase 3-tests groen — som van 03-03, 03-04, 03-05)
- `vendor/bin/pint --test --format agent` exit 0
- Curl-smoke (of tinker-based-call) op `/v1/ping` met fresh-created PAT slaagt
</verification>

<success_criteria>
- HUB-01 SC-1 ("`migrate:fresh --seed` levert demo-data") bewezen ✓
- HUB-01 SC-2 ("Consumer kan via PAT /v1/ping bereiken") end-to-end bewezen (PingTest in 03-03 + dit plan's smoke) ✓
- Phase 3 is volledig dichtgespijkerd; Phase 5b kan zijn `POST /v1/connections` + `POST /v1/accounts` flow bovenop dit skeleton bouwen
</success_criteria>

<output>
Maak `.planning/phases/03-hub-skeleton/03-05-SUMMARY.md` met:
- Welke artisan-command nu beschikbaar is (`hub:consumer:create`) + signature
- Demo-data-shape (`naschool` Consumer + `school1` Account, geen Connections)
- Resultaat van end-to-end smoke (curl-output of tinker-status)
- Totaal aantal Phase 3-tests groen + breakdown
- Lijst van openstaande items voor Phase 4 (OAuth-broker) en Phase 5b (pass-through API)
</output>
